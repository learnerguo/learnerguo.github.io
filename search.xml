<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>书单记录</title>
    <url>/2023/d298f472fcd1/index.html</url>
    <content><![CDATA[<h2 id="算法"><a href="#算法" class="headerlink" title="算法"></a>算法</h2><ul>
<li>《剑指Offer：专项突破版. 数据结构与算法名企面试题精讲》  – 何海涛  【微信读书】</li>
</ul>
<h2 id="java"><a href="#java" class="headerlink" title="java"></a>java</h2><ul>
<li>📖 《Java并发编程深度解析与实战》 – 谭锋   【微信读书】</li>
<li>📖 《剑指JVM：虚拟机时间与性能调优》   – 尚硅谷教育      【微信读书】</li>
<li>《设计模式就该这么学：基于经典框架源码和真实业务场景》   – 谭勇德   【微信读书】</li>
<li>《互联网轻量级SSM框架解密：Spring、Spring MVC、MyBatis源码深度剖析》   – 李艳鹏   【微信读书】</li>
<li>《分布式中间件技术(Java版)》  <a href="https://cmsblogs.cn/4303.html">下载地址</a>    【微信读书】</li>
</ul>
<h2 id="Spring系列"><a href="#Spring系列" class="headerlink" title="Spring系列"></a>Spring系列</h2><ul>
<li>《Spring Cloud Alibaba微服务原理与实战》  <a href="https://cmsblogs.cn/2647.html">下载地址</a>   【微信读书】</li>
<li>《SpringBoot技术内幕》   【微信读书】</li>
</ul>
<h2 id="Mysql"><a href="#Mysql" class="headerlink" title="Mysql"></a>Mysql</h2><ul>
<li>📖《MySQL技术内幕：InnoDB存储引擎》 – 姜承尧</li>
<li>📖 <a href="https://relph1119.github.io/mysql-learning-notes/#/">《MySQL 是怎样运行的：从根儿上理解 MySQL》</a></li>
</ul>
<h2 id="网络"><a href="#网络" class="headerlink" title="网络"></a>网络</h2><ul>
<li>《深入理解 TCP 协议：从原理到实战》 <a href="https://juejin.cn/book/6844733788681928712">掘金小册</a></li>
<li>《趣谈网络协议》  – 刘超  </li>
<li>《深入浅出计算机网络》  –韩立刚</li>
<li>《完全图解服务器工作原理》</li>
<li>《深入理解Linux网络》</li>
<li>《深入浅出TCP&#x2F;IP》</li>
<li>《图解http》   【微信读书】</li>
</ul>
<h2 id="Netty"><a href="#Netty" class="headerlink" title="Netty"></a>Netty</h2><ul>
<li>《跟闪电侠学Netty:Netty即时聊天实战与底层原理》  – 俞超  【微信读书】</li>
<li>《Netty进阶之路：跟着案例学Netty》  – 李林锋  【微信读书】</li>
<li>《Netty源码全解与架构思维》 暂无</li>
</ul>
<h2 id="MQ"><a href="#MQ" class="headerlink" title="MQ"></a>MQ</h2><ul>
<li>📖《RocketMQ实战与原理解析》  – 杨开元</li>
<li>📖《RocketMQ分布式中间件：核心原理与最佳实践》   – 李伟  【微信读书】</li>
<li>📖《RocketMQ技术内幕：RocketMQ架构设计与实现原理》  【微信读书】   源码分析重点看这本</li>
</ul>
<h2 id="RPC"><a href="#RPC" class="headerlink" title="RPC"></a>RPC</h2><ul>
<li>《深入理解RPC框RPC架原理与实现》 <a href="https://pan.baidu.com/s/1sQMae0v9yGKctRsoin_dow?pwd=y3s3">下载链接</a></li>
<li>《深度剖析ApacheDubbo核心技术内幕》  – 翟陆续   【微信读书】</li>
</ul>
<h2 id="云原生"><a href="#云原生" class="headerlink" title="云原生"></a>云原生</h2><ul>
<li>《Kubernetes网络权威指南：基础，原理与实践》 – 杜军   【微信读书】</li>
<li>《云原生Kubernetes全栈架构师实战》 – 杜宽    【微信读书】</li>
</ul>
<h2 id="计算机基础"><a href="#计算机基础" class="headerlink" title="计算机基础"></a>计算机基础</h2><ul>
<li>《漫画计算机原理》</li>
<li>《计算机时怎样跑起来的》</li>
</ul>
<h2 id="DevOps"><a href="#DevOps" class="headerlink" title="DevOps"></a>DevOps</h2><ul>
<li>《持续交付2.0：业务引领的DevOps精要》   – 乔梁      【微信读书】</li>
</ul>
<h2 id="架构"><a href="#架构" class="headerlink" title="架构"></a>架构</h2><ul>
<li>《可伸缩废物架构：框架与中间件》  – 李艳鹏   【微信读书】</li>
<li>《程序员必读之软件架构》    【微信读书】</li>
<li>《未来架构：从服务化到云原生》 【微信读书】</li>
</ul>
<h2 id="其他"><a href="#其他" class="headerlink" title="其他"></a>其他</h2><ul>
<li>《新程序员04：我们的技术时代，我们的程序人生》    【微信读书】</li>
<li>《程序员的修炼之道：通向务实的最高境界》           【微信读书】</li>
<li></li>
</ul>
]]></content>
      <categories>
        <category>book</category>
      </categories>
      <tags>
        <tag>book</tag>
      </tags>
  </entry>
  <entry>
    <title>Hexo中使用思维导图</title>
    <url>/2023/a8d3178908dd/index.html</url>
    <content><![CDATA[<p><a href="https://github.com/maxchang3/hexo-markmap">插件下载地址</a></p>
]]></content>
      <categories>
        <category>tools</category>
        <category>hexo</category>
      </categories>
      <tags>
        <tag>tools</tag>
        <tag>hexo</tag>
      </tags>
  </entry>
  <entry>
    <title>Next7主题自定义分类</title>
    <url>/2023/baf924a75393/index.html</url>
    <content><![CDATA[<p><a href="https://enigmatisms.github.io/2022/02/18/Hexo-NexT%E4%B8%BB%E9%A2%98-%E6%9B%B4%E5%BC%BA%E7%9A%84%E8%87%AA%E5%AE%9A%E4%B9%89%E9%A1%B5%E9%9D%A2/">参考文章</a></p>
<h2 id="添加依赖包"><a href="#添加依赖包" class="headerlink" title="添加依赖包"></a>添加依赖包</h2><figure class="highlight coffeescript"><table><tr><td class="code"><pre><code class="hljs coffeescript"><span class="hljs-built_in">npm</span> uninstall hexo-generator-index<br><span class="hljs-built_in">npm</span> install hexo-generator-indexed<br><span class="hljs-built_in">npm</span> install hexo-pagination --save<br><span class="hljs-built_in">npm</span> install timsort --save<br></code></pre></td></tr></table></figure>
<span id="more"></span>
<h2 id="添加自定义文件"><a href="#添加自定义文件" class="headerlink" title="添加自定义文件"></a>添加自定义文件</h2><ol>
<li>增加的分类是 阅读：book</li>
</ol>
<figure class="highlight yaml"><table><tr><td class="code"><pre><code class="hljs yaml"><span class="hljs-attr">menu:</span><br>  <span class="hljs-attr">home:</span> <span class="hljs-string">/</span> <span class="hljs-string">||</span> <span class="hljs-string">fa</span> <span class="hljs-string">fa-home</span><br>  <span class="hljs-attr">about:</span> <span class="hljs-string">/about/</span> <span class="hljs-string">||</span> <span class="hljs-string">fa</span> <span class="hljs-string">fa-user</span><br>  <span class="hljs-attr">tags:</span> <span class="hljs-string">/tags/</span> <span class="hljs-string">||</span> <span class="hljs-string">fa</span> <span class="hljs-string">fa-tags</span><br>  <span class="hljs-attr">categories:</span> <span class="hljs-string">/categories/</span> <span class="hljs-string">||</span> <span class="hljs-string">fa</span> <span class="hljs-string">fa-th</span><br>  <span class="hljs-attr">archives:</span> <span class="hljs-string">/archives/</span> <span class="hljs-string">||</span> <span class="hljs-string">fa</span> <span class="hljs-string">fa-archive</span><br>  <span class="hljs-comment"># schedule: /schedule/ || fa fa-calendar</span><br>  <span class="hljs-attr">book:</span> <span class="hljs-string">/book/</span> <span class="hljs-string">||</span> <span class="hljs-string">fa</span> <span class="hljs-string">fa-book</span> <span class="hljs-comment">#这里是新添加的菜单</span><br></code></pre></td></tr></table></figure>

<ol start="2">
<li>在博客根目录下创建 scripts 目录并创建 <code>customcategory.js</code> 文件，内容如下</li>
</ol>
<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-keyword">const</span> pagination = <span class="hljs-built_in">require</span>(<span class="hljs-string">&#x27;hexo-pagination&#x27;</span>);<br><span class="hljs-keyword">const</span> &#123; sort &#125; = <span class="hljs-built_in">require</span>(<span class="hljs-string">&#x27;timsort&#x27;</span>);<br><span class="hljs-keyword">const</span> filteredCategory = <span class="hljs-string">&#x27;book&#x27;</span>;<br><br>hexo.<span class="hljs-property">extend</span>.<span class="hljs-property">generator</span>.<span class="hljs-title function_">register</span>(<span class="hljs-string">&#x27;customcategory&#x27;</span>, <span class="hljs-keyword">function</span>(<span class="hljs-params">locals</span>)&#123;<br>  <span class="hljs-keyword">var</span> posts = locals.<span class="hljs-property">posts</span>.<span class="hljs-title function_">filter</span>(<span class="hljs-keyword">function</span>(<span class="hljs-params">x</span>) &#123;<br>    <span class="hljs-keyword">return</span> x.<span class="hljs-property">categories</span>.<span class="hljs-property">data</span>[<span class="hljs-number">0</span>].<span class="hljs-property">name</span> == filteredCategory;<br>  &#125;).<span class="hljs-title function_">sort</span>(<span class="hljs-string">&#x27;-date&#x27;</span>).<span class="hljs-title function_">slice</span>(<span class="hljs-number">0</span>);				<span class="hljs-comment">// 复制，而非原地操作</span><br>  <span class="hljs-title function_">sort</span>(posts.<span class="hljs-property">data</span>, <span class="hljs-function">(<span class="hljs-params">a, b</span>) =&gt;</span> (b.<span class="hljs-property">sticky</span> || <span class="hljs-number">0</span>) - (a.<span class="hljs-property">sticky</span> || <span class="hljs-number">0</span>));<br>  <span class="hljs-keyword">return</span> <span class="hljs-title function_">pagination</span>(<span class="hljs-string">&#x27;book&#x27;</span>, posts, &#123;<br>    <span class="hljs-attr">perPage</span>: <span class="hljs-number">10</span>,<br>    <span class="hljs-attr">layout</span>: [<span class="hljs-string">&#x27;customcategory&#x27;</span>],<br>    <span class="hljs-attr">data</span>: &#123;<br>      <span class="hljs-attr">__index</span>: <span class="hljs-literal">true</span><br>    &#125;<br>  &#125;);<br>&#125;);<br></code></pre></td></tr></table></figure>
<ol start="3">
<li>在主题的 layout 目录下创建文件 <code>customcategory.njk</code> 文件内容如下</li>
</ol>
<figure class="highlight django"><table><tr><td class="code"><pre><code class="hljs django"><span class="hljs-template-tag">&#123;% <span class="hljs-name"><span class="hljs-name">extends</span></span> &#x27;_layout.njk&#x27; %&#125;</span><span class="language-xml"></span><br><span class="language-xml"></span><span class="hljs-template-tag">&#123;% <span class="hljs-name">import</span> &#x27;_macro/post-collapse.njk&#x27; <span class="hljs-keyword">as</span> post_template with context %&#125;</span><span class="language-xml"></span><br><span class="language-xml"></span><span class="hljs-template-tag">&#123;% <span class="hljs-name">import</span> &#x27;_macro/sidebar.njk&#x27; <span class="hljs-keyword">as</span> sidebar_template with context %&#125;</span><span class="language-xml"></span><br><span class="language-xml"></span><br><span class="language-xml"></span><span class="hljs-template-tag">&#123;% <span class="hljs-name"><span class="hljs-name">block</span></span> title %&#125;</span><span class="hljs-template-variable">&#123;&#123; __(&#x27;title.category&#x27;) &#125;&#125;</span><span class="language-xml">: </span><span class="hljs-template-variable">&#123;&#123; page.category &#125;&#125;</span><span class="language-xml"> | </span><span class="hljs-template-variable">&#123;&#123; title &#125;&#125;</span><span class="hljs-template-tag">&#123;% <span class="hljs-name"><span class="hljs-name">endblock</span></span> %&#125;</span><span class="language-xml"></span><br><span class="language-xml"></span><br><span class="language-xml"></span><span class="hljs-template-tag">&#123;% <span class="hljs-name"><span class="hljs-name">block</span></span> class %&#125;</span><span class="language-xml">index posts-expand</span><span class="hljs-template-tag">&#123;% <span class="hljs-name"><span class="hljs-name">endblock</span></span> %&#125;</span><span class="language-xml"></span><br><span class="language-xml"></span><br><span class="language-xml"></span><span class="hljs-template-tag">&#123;% <span class="hljs-name"><span class="hljs-name">block</span></span> content %&#125;</span><span class="language-xml"></span><br><span class="language-xml"></span><br><span class="language-xml">  </span><span class="hljs-template-tag">&#123;%- <span class="hljs-name"><span class="hljs-name">for</span></span> post <span class="hljs-keyword">in</span> page.posts.toArray() %&#125;</span><span class="language-xml"></span><br><span class="language-xml">    </span><span class="hljs-template-variable">&#123;&#123; partial(&#x27;_macro/post.njk&#x27;, &#123;post: post, is_index: true&#125;) &#125;&#125;</span><span class="language-xml"></span><br><span class="language-xml">  </span><span class="hljs-template-tag">&#123;%- <span class="hljs-name"><span class="hljs-name">endfor</span></span> %&#125;</span><span class="language-xml"></span><br><span class="language-xml"></span><br><span class="language-xml">  </span><span class="hljs-template-tag">&#123;%- <span class="hljs-name"><span class="hljs-name">include</span></span> &#x27;_partials/pagination.njk&#x27; -%&#125;</span><span class="language-xml"></span><br><span class="language-xml"></span><br><span class="language-xml"></span><span class="hljs-template-tag">&#123;% <span class="hljs-name"><span class="hljs-name">endblock</span></span> %&#125;</span><span class="language-xml"></span><br><span class="language-xml"></span><br><span class="language-xml"></span><span class="hljs-template-tag">&#123;% <span class="hljs-name"><span class="hljs-name">block</span></span> sidebar %&#125;</span><span class="language-xml"></span><br><span class="language-xml">  </span><span class="hljs-template-variable">&#123;&#123; sidebar_template.render(false) &#125;&#125;</span><span class="language-xml"></span><br><span class="language-xml"></span><span class="hljs-template-tag">&#123;% <span class="hljs-name"><span class="hljs-name">endblock</span></span> %&#125;</span><br></code></pre></td></tr></table></figure>


]]></content>
      <categories>
        <category>tools</category>
        <category>hexo</category>
      </categories>
      <tags>
        <tag>tools</tag>
        <tag>hexo</tag>
      </tags>
  </entry>
  <entry>
    <title>butterfly主题多级分类折叠与展开</title>
    <url>/2023/acbd53e5de56/index.html</url>
    <content><![CDATA[<ol>
<li>引入 <code>jquery</code> 和 自定义的 js 文件</li>
</ol>
<p>打开 <code>_config.butterfly.yml</code> 文件，添加如下内容</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><code class="hljs yaml"><span class="hljs-attr">inject:</span><br>  <span class="hljs-attr">head:</span><br>    <span class="hljs-bullet">-</span> <span class="hljs-string">&lt;link</span> <span class="hljs-string">rel=&quot;stylesheet&quot;</span> <span class="hljs-string">href=&quot;/cusconfig/css/hidemermaid.css?1&quot;&gt;</span><br>    <span class="hljs-comment"># 引入jquery</span><br>    <span class="hljs-bullet">-</span> <span class="hljs-string">&lt;script</span> <span class="hljs-string">src=&quot;https://apps.bdimg.com/libs/jquery/2.1.4/jquery.min.js&quot;&gt;&lt;/script&gt;</span><br>    <span class="hljs-comment"># 引入自定义文件，自定义文件放在博客根目录的 /cusconfig/js 目录下，名称是 category.js</span><br>    <span class="hljs-bullet">-</span> <span class="hljs-string">&lt;script</span> <span class="hljs-string">src=&quot;/cusconfig/js/category.js&quot;&gt;&lt;/script&gt;</span><br>  <span class="hljs-attr">bottom:</span><br>    <span class="hljs-comment"># - &lt;script src=&quot;xxxx&quot;&gt;&lt;/script&gt;</span><br>    <span class="hljs-comment"># - &lt;script src=&quot;https://apps.bdimg.com/libs/jquery/2.1.4/jquery.min.js&quot;&gt;&lt;/script&gt;</span><br></code></pre></td></tr></table></figure>

<ol start="2">
<li>category.js 文件内容</li>
</ol>
<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js">$(<span class="hljs-variable language_">document</span>).<span class="hljs-title function_">ready</span>(<span class="hljs-keyword">function</span>(<span class="hljs-params"></span>) &#123;<br>	<span class="hljs-title function_">categoryUnfold</span>();<br>	<span class="hljs-keyword">function</span> <span class="hljs-title function_">categoryUnfold</span>(<span class="hljs-params"></span>) &#123;<br>		$(<span class="hljs-string">&quot;.category-list-link&quot;</span>).<span class="hljs-title function_">filter</span>(<span class="hljs-keyword">function</span>(<span class="hljs-params"></span>) &#123;<br>			<span class="hljs-keyword">if</span>($(<span class="hljs-variable language_">this</span>).<span class="hljs-title function_">siblings</span>(<span class="hljs-string">&quot;.category-list-child&quot;</span>).<span class="hljs-property">length</span> &gt; <span class="hljs-number">0</span>)<br>                <span class="hljs-comment">// 这里引入了向下展开的图标，可以直接从本站下载放入对应目录即可</span><br>				$(<span class="hljs-variable language_">this</span>).<span class="hljs-title function_">parent</span>().<span class="hljs-title function_">css</span>(<span class="hljs-string">&quot;list-style-image&quot;</span>,<span class="hljs-string">&quot;url(&#x27;/cusconfig/img/category-unfold-16x16.png&#x27;&quot;</span>);<br>		&#125;);<br>		<br>		<span class="hljs-comment">//点击含有子标签category-list-child的category-list-link标签时，不进行跳转</span><br>		$(<span class="hljs-string">&quot;.category-list-link&quot;</span>).<span class="hljs-title function_">filter</span>(<span class="hljs-keyword">function</span>(<span class="hljs-params"></span>) &#123;<br>			<span class="hljs-keyword">return</span> $(<span class="hljs-variable language_">this</span>).<span class="hljs-title function_">siblings</span>(<span class="hljs-string">&quot;.category-list-child&quot;</span>).<span class="hljs-property">length</span> &gt; <span class="hljs-number">0</span><br>		&#125;).<span class="hljs-title function_">attr</span>(<span class="hljs-string">&quot;href&quot;</span>, <span class="hljs-string">&quot;javascript:void(0)&quot;</span>);<br>		<br>		<span class="hljs-comment">//含有子标签category-list-child的category-list-link标签将自动隐藏子标签</span><br>		$(<span class="hljs-string">&quot;.category-list-link&quot;</span>).<span class="hljs-title function_">filter</span>(<span class="hljs-keyword">function</span>(<span class="hljs-params"></span>) &#123;<br>			<span class="hljs-keyword">if</span>($(<span class="hljs-variable language_">this</span>).<span class="hljs-title function_">siblings</span>(<span class="hljs-string">&quot;.category-list-child&quot;</span>).<span class="hljs-property">length</span> &gt; <span class="hljs-number">0</span>)<br>				$(<span class="hljs-variable language_">this</span>).<span class="hljs-title function_">siblings</span>(<span class="hljs-string">&quot;.category-list-child&quot;</span>).<span class="hljs-title function_">hide</span>();<br>		&#125;);	<br>		<br>		<span class="hljs-comment">//点击含有子标签category-list-child的category-list-link标签时，将隐藏子标签展开</span><br>		$(<span class="hljs-string">&quot;.category-list-link&quot;</span>).<span class="hljs-title function_">click</span>(<span class="hljs-keyword">function</span>(<span class="hljs-params"></span>) &#123;<br>			$(<span class="hljs-variable language_">this</span>).<span class="hljs-title function_">siblings</span>(<span class="hljs-string">&quot;.category-list-child&quot;</span>).<span class="hljs-title function_">slideToggle</span>();<br>		&#125;);<br>	&#125;<br>&#125;);<br></code></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>tools</category>
        <category>hexo</category>
      </categories>
      <tags>
        <tag>tools</tag>
        <tag>hexo</tag>
      </tags>
  </entry>
  <entry>
    <title>Next8版本主题优化修改的文件</title>
    <url>/2023/44b973fe011c/index.html</url>
    <content><![CDATA[<p><a href="https://zhuanlan.zhihu.com/p/252983030">参考文章</a></p>
<span id="more"></span>

<p>首页每篇文章变成圆角，只需要在 <code>styles.styl</code> 中添加如下内容</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs styles">// 添加首页每篇文章圆角独立 start ------------<br>.post-block&#123;<br>        background-color: rgba(255, 255, 255, 1);<br>        //margin-top: 24px;<br>        margin-bottom: 24px;<br>        padding: 20px;<br>        border-radius: 30px 50px 30px 50px;<br>        box-shadow: 8px 7px 2px 0 rgba(0,0,0,0.12), 7px 4px 1px -2px rgba(0,0,0,0.06), 0 1px 5px 0 rgba(0,0,0,0.12);<br>&#125;<br>// 添加首页每篇文章圆角独立 end ------------<br></code></pre></td></tr></table></figure>
<p><code>styles.styl</code> 在博客根目录下的，<code>source/_data</code> 下，若没有则需要自己创建并且在 <code>next</code> 主题的 <code>_config.yml</code> 中打开下面的注释</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><code class="hljs yaml"><span class="hljs-attr">custom_file_path:</span><br>  <span class="hljs-comment">#head: source/_data/head.njk</span><br>  <span class="hljs-comment">#header: source/_data/header.njk</span><br>  <span class="hljs-attr">style:</span> <span class="hljs-string">source/_data/styles.styl</span><br></code></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>tools</category>
        <category>hexo</category>
      </categories>
      <tags>
        <tag>tools</tag>
        <tag>hexo</tag>
      </tags>
  </entry>
  <entry>
    <title>butterfly主题归档分类页面展示成双栏</title>
    <url>/2023/e641424208d5/index.html</url>
    <content><![CDATA[<ol>
<li>在 博客根目录的 <code>source</code> 目录下创建文件夹 <code>cusconfig/css</code>, 并创建文件, <code>cus_butterfly.css</code>, 添加如下内容</li>
</ol>
<figure class="highlight css"><table><tr><td class="code"><pre><code class="hljs css"><span class="hljs-comment">/* 归档，分类文档页面设置双栏展示， 增加边框 */</span><br><span class="hljs-selector-class">.no-article-cover</span> &#123;<br>    <span class="hljs-attribute">width</span>: <span class="hljs-number">48%</span>;<br>    <span class="hljs-attribute">border</span>: <span class="hljs-number">1px</span> dashed lightblue;<br>    <span class="hljs-attribute">background-color</span>: <span class="hljs-built_in">rgba</span>(<span class="hljs-number">241</span>, <span class="hljs-number">242</span>, <span class="hljs-number">246</span>, <span class="hljs-number">0.5</span>);<br>    <span class="hljs-attribute">float</span>: left;<br>&#125;<br><br><span class="hljs-selector-class">.article-sort-item</span><span class="hljs-selector-class">.year</span> &#123;<br>    <span class="hljs-attribute">clear</span>: both;<br>&#125;<br><br><span class="hljs-selector-id">#pagination</span> &#123;<br>    <span class="hljs-attribute">clear</span>:both;<br>&#125;<br></code></pre></td></tr></table></figure>

<ol start="2">
<li>引入自定义 <code>css</code> 文件， 打开主题配置文件 <code>_config.butterfly.yml</code>, 添加如下内容</li>
</ol>
<figure class="highlight yaml"><table><tr><td class="code"><pre><code class="hljs yaml"><span class="hljs-attr">inject:</span><br>  <span class="hljs-attr">head:</span><br>    <span class="hljs-comment"># 添加下面这行内容引入自定义 js</span><br>    <span class="hljs-bullet">-</span> <span class="hljs-string">&lt;link</span> <span class="hljs-string">rel=&quot;stylesheet&quot;</span> <span class="hljs-string">href=&quot;/cusconfig/css/cus_butterfly.css?1&quot;&gt;</span><br></code></pre></td></tr></table></figure>

<h1 id="去掉文档前面的点和竖线"><a href="#去掉文档前面的点和竖线" class="headerlink" title="去掉文档前面的点和竖线"></a>去掉文档前面的点和竖线</h1><p>修改 <code>archives.styl</code> 文件，该文件路径为 <code>博客根目录/themes/butterfly/source/css_page</code> 下面， 有注释的地方代表修改过</p>
<figure class="highlight nestedtext"><table><tr><td class="code"><pre><code class="hljs nestedtext"><span class="hljs-attribute">.article-sort</span><br><span class="hljs-attribute">  margin-left</span><span class="hljs-punctuation">:</span> <span class="hljs-string">10px</span><br>  <span class="hljs-attribute">padding-left</span><span class="hljs-punctuation">:</span> <span class="hljs-string">20px</span><br>  <span class="hljs-attribute">// border-left</span><span class="hljs-punctuation">:</span> <span class="hljs-string">2px solid lighten($light-blue, 20)   这一行注释掉，这里去掉竖线</span><br><br>  <span class="hljs-attribute">&amp;-title</span><br><span class="hljs-attribute">    position</span><span class="hljs-punctuation">:</span> <span class="hljs-string">relative</span><br>    <span class="hljs-attribute">margin-left</span><span class="hljs-punctuation">:</span> <span class="hljs-string">10px</span><br>    <span class="hljs-attribute">padding-bottom</span><span class="hljs-punctuation">:</span> <span class="hljs-string">20px</span><br>    <span class="hljs-attribute">padding-left</span><span class="hljs-punctuation">:</span> <span class="hljs-string">20px</span><br>    <span class="hljs-attribute">font-size</span><span class="hljs-punctuation">:</span> <span class="hljs-string">1.72em</span><br><br>    &amp;:hover<br>      &amp;:before<br>        <span class="hljs-attribute">border-color</span><span class="hljs-punctuation">:</span> <span class="hljs-string">var(--pseudo-hover)</span><br><br>    &amp;:before<br>      <span class="hljs-attribute">position</span><span class="hljs-punctuation">:</span> <span class="hljs-string">absolute</span><br>      <span class="hljs-attribute">top</span><span class="hljs-punctuation">:</span> <span class="hljs-string">calc(((100% - 36px) / 2))</span><br>      <span class="hljs-attribute">left</span><span class="hljs-punctuation">:</span> <span class="hljs-string">-9px</span><br>      <span class="hljs-attribute">z-index</span><span class="hljs-punctuation">:</span> <span class="hljs-string">1</span><br>      <span class="hljs-attribute">width</span><span class="hljs-punctuation">:</span> <span class="hljs-string">w = 10px</span><br>      <span class="hljs-attribute">height</span><span class="hljs-punctuation">:</span> <span class="hljs-string">h = w</span><br>      <span class="hljs-attribute">border</span><span class="hljs-punctuation">:</span> <span class="hljs-string">.5 * w solid $light-blue</span><br>      <span class="hljs-attribute">border-radius</span><span class="hljs-punctuation">:</span> <span class="hljs-string">w</span><br>      <span class="hljs-attribute">background</span><span class="hljs-punctuation">:</span> <span class="hljs-string">var(--card-bg)</span><br>      <span class="hljs-attribute">content</span><span class="hljs-punctuation">:</span> <span class="hljs-string">&#x27;&#x27;</span><br>      <span class="hljs-attribute">line-height</span><span class="hljs-punctuation">:</span> <span class="hljs-string">h</span><br>      <span class="hljs-attribute">transition</span><span class="hljs-punctuation">:</span> <span class="hljs-string">all .2s ease-in-out</span><br><br>    &amp;:after<br>      <span class="hljs-attribute">position</span><span class="hljs-punctuation">:</span> <span class="hljs-string">absolute</span><br>      <span class="hljs-attribute">bottom</span><span class="hljs-punctuation">:</span> <span class="hljs-string">0</span><br>      <span class="hljs-attribute">left</span><span class="hljs-punctuation">:</span> <span class="hljs-string">0</span><br>      <span class="hljs-attribute">z-index</span><span class="hljs-punctuation">:</span> <span class="hljs-string">0</span><br>      <span class="hljs-attribute">width</span><span class="hljs-punctuation">:</span> <span class="hljs-string">2px</span><br>      <span class="hljs-attribute">height</span><span class="hljs-punctuation">:</span> <span class="hljs-string">1.5em</span><br>      <span class="hljs-attribute">background</span><span class="hljs-punctuation">:</span> <span class="hljs-string">lighten($light-blue, 20)</span><br>      <span class="hljs-attribute">content</span><span class="hljs-punctuation">:</span> <span class="hljs-string">&#x27;&#x27;</span><br><br>  <span class="hljs-attribute">&amp;-item</span><br><span class="hljs-attribute">    position</span><span class="hljs-punctuation">:</span> <span class="hljs-string">relative</span><br>    <span class="hljs-attribute">display</span><span class="hljs-punctuation">:</span> <span class="hljs-string">flex</span><br>    <span class="hljs-attribute">align-items</span><span class="hljs-punctuation">:</span> <span class="hljs-string">center</span><br>    <span class="hljs-attribute">margin</span><span class="hljs-punctuation">:</span> <span class="hljs-string">0 0 20px 10px</span><br>    <span class="hljs-attribute">transition</span><span class="hljs-punctuation">:</span> <span class="hljs-string">all .2s ease-in-out</span><br><br>    &amp;:hover<br>      &amp;:before<br>        <span class="hljs-attribute">border-color</span><span class="hljs-punctuation">:</span> <span class="hljs-string">var(--pseudo-hover)</span><br><br>    /* &amp;:before   这一整块都注释掉，这里是去掉文章前面的点<br>      <span class="hljs-attribute">$w = 6px</span><br><span class="hljs-attribute">      position</span><span class="hljs-punctuation">:</span> <span class="hljs-string">absolute</span><br>      <span class="hljs-attribute">left</span><span class="hljs-punctuation">:</span> <span class="hljs-string">calc(-20px - 17px)</span><br>      <span class="hljs-attribute">width</span><span class="hljs-punctuation">:</span> <span class="hljs-string">w = $w</span><br>      <span class="hljs-attribute">height</span><span class="hljs-punctuation">:</span> <span class="hljs-string">h = w</span><br>      <span class="hljs-attribute">border</span><span class="hljs-punctuation">:</span> <span class="hljs-string">.5 * w solid $light-blue</span><br>      <span class="hljs-attribute">border-radius</span><span class="hljs-punctuation">:</span> <span class="hljs-string">w</span><br>      <span class="hljs-attribute">background</span><span class="hljs-punctuation">:</span> <span class="hljs-string">var(--card-bg)</span><br>      <span class="hljs-attribute">content</span><span class="hljs-punctuation">:</span> <span class="hljs-string">&#x27;&#x27;</span><br>      <span class="hljs-attribute">transition</span><span class="hljs-punctuation">:</span> <span class="hljs-string">all .2s ease-in-out */</span><br><br>    <span class="hljs-attribute">&amp;.no-article-cover</span><br><span class="hljs-attribute">      height</span><span class="hljs-punctuation">:</span> <span class="hljs-string">80px</span><br><br>      <span class="hljs-attribute">.article-sort-item-info</span><br><span class="hljs-attribute">        padding</span><span class="hljs-punctuation">:</span> <span class="hljs-string">0</span><br><br>    <span class="hljs-attribute">&amp;.year</span><br><span class="hljs-attribute">      font-size</span><span class="hljs-punctuation">:</span> <span class="hljs-string">1.43em</span><br><br>      &amp;:hover<br>        &amp;:before<br>          <span class="hljs-attribute">border-color</span><span class="hljs-punctuation">:</span> <span class="hljs-string">$light-blue</span><br><br>      &amp;:before<br>        <span class="hljs-attribute">border-color</span><span class="hljs-punctuation">:</span> <span class="hljs-string">var(--pseudo-hover)</span><br><br>    <span class="hljs-attribute">&amp;-time</span><br><span class="hljs-attribute">      color</span><span class="hljs-punctuation">:</span> <span class="hljs-string">$theme-meta-color</span><br>      <span class="hljs-attribute">font-size</span><span class="hljs-punctuation">:</span> <span class="hljs-string">95%</span><br><br>      <span class="hljs-attribute">time</span><br><span class="hljs-attribute">        padding-left</span><span class="hljs-punctuation">:</span> <span class="hljs-string">6px</span><br>        <span class="hljs-attribute">cursor</span><span class="hljs-punctuation">:</span> <span class="hljs-string">default</span><br><br>    <span class="hljs-attribute">&amp;-title</span><br><span class="hljs-attribute">      @extend .limit-more-line</span><br><span class="hljs-attribute">      color</span><span class="hljs-punctuation">:</span> <span class="hljs-string">var(--font-color)</span><br>      <span class="hljs-attribute">font-size</span><span class="hljs-punctuation">:</span> <span class="hljs-string">1.1em</span><br>      <span class="hljs-attribute">transition</span><span class="hljs-punctuation">:</span> <span class="hljs-string">all .3s</span><br>      <span class="hljs-attribute">-webkit-line-clamp</span><span class="hljs-punctuation">:</span> <span class="hljs-string">2</span><br><br>      &amp;:hover<br>        <span class="hljs-attribute">color</span><span class="hljs-punctuation">:</span> <span class="hljs-string">$text-hover</span><br>        <span class="hljs-attribute">transform</span><span class="hljs-punctuation">:</span> <span class="hljs-string">translateX(10px)</span><br><br>    <span class="hljs-attribute">&amp;-img</span><br><span class="hljs-attribute">      overflow</span><span class="hljs-punctuation">:</span> <span class="hljs-string">hidden</span><br>      <span class="hljs-attribute">width</span><span class="hljs-punctuation">:</span> <span class="hljs-string">80px</span><br>      <span class="hljs-attribute">height</span><span class="hljs-punctuation">:</span> <span class="hljs-string">80px</span><br><br>      :first-child<br>        <span class="hljs-attribute">@extend .imgHover</span><br><span class="hljs-attribute"></span><br><span class="hljs-attribute">    &amp;-info</span><br><span class="hljs-attribute">      flex</span><span class="hljs-punctuation">:</span> <span class="hljs-string">1</span><br>      <span class="hljs-attribute">padding</span><span class="hljs-punctuation">:</span> <span class="hljs-string">0 16px</span><br><br></code></pre></td></tr></table></figure>




]]></content>
      <categories>
        <category>tools</category>
        <category>hexo</category>
      </categories>
      <tags>
        <tag>tools</tag>
        <tag>hexo</tag>
      </tags>
  </entry>
  <entry>
    <title>butterfly标签页固定每行个数</title>
    <url>/2023/1099169ec3c6/index.html</url>
    <content><![CDATA[<ol>
<li><p>可以先参考如下文章添加自定义 <code>css</code> 文件</p>
<ol><li><a href="/2023/e641424208d5/index.html" title="butterfly主题归档分类页面展示成双栏">butterfly主题归档分类页面展示成双栏</a></li></ol>
</li>
<li><p>在 <code>cus_butterfly.css</code> 文件中添加如下内容</p>
</li>
</ol>
<figure class="highlight css"><table><tr><td class="code"><pre><code class="hljs css"><span class="hljs-comment">/* 修改标签页，每个标签宽度固定  start -------------- */</span><br><span class="hljs-selector-class">.tag-cloud-list</span> &#123;<br>    <span class="hljs-attribute">display</span>: flex;<br>    <span class="hljs-attribute">flex-wrap</span>: wrap;<br>&#125;<br><br><span class="hljs-comment">/* 每行放5个标签 */</span><br><span class="hljs-selector-class">.tag-cloud-list</span> <span class="hljs-selector-tag">a</span> &#123;<br>    <span class="hljs-attribute">width</span>: <span class="hljs-number">20%</span>;<br>    <span class="hljs-attribute">text-align</span>: center;<br>&#125;<br><br><span class="hljs-comment">/* 修改标签页，每个标签宽度固定  end -------------- */</span><br></code></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>tools</category>
        <category>hexo</category>
      </categories>
      <tags>
        <tag>tools</tag>
        <tag>hexo</tag>
      </tags>
  </entry>
  <entry>
    <title>hexo使用插入本地图片路径问题</title>
    <url>/2023/563e23df91d3/index.html</url>
    <content><![CDATA[<p>Hexo version: 7.0<br>Next version: 8.18</p>
<span id="more"></span>
<ol>
<li>安装插件</li>
</ol>
<figure class="highlight ada"><table><tr><td class="code"><pre><code class="hljs ada">npm install hexo-renderer-marked <span class="hljs-comment">--save</span><br></code></pre></td></tr></table></figure>
<ol start="2">
<li>修改 hexo 配置文件 <code>_config.yml</code> (不是next 主题的配置文件)</li>
</ol>
<figure class="highlight yaml"><table><tr><td class="code"><pre><code class="hljs yaml"><span class="hljs-comment"># 这样文章生成的路径会是年/月/日/hash值形式</span><br><span class="hljs-attr">permalink:</span> <span class="hljs-string">:year/:month/:day/:hash/</span><br><span class="hljs-comment"># 开启资源文件夹功能</span><br><span class="hljs-attr">post_asset_folder:</span> <span class="hljs-literal">true</span><br><span class="hljs-attr">marked:</span><br>  <span class="hljs-comment"># 这两个值都配置为false，网上很多文章配置成true，可能是不同版本效果不同，在 Next8.18, hexo7 这个版本若配置成true，图片路径会多了日期导致显示失败 </span><br>  <span class="hljs-attr">prependRoot:</span> <span class="hljs-literal">false</span><br>  <span class="hljs-attr">postAsset:</span> <span class="hljs-literal">false</span><br></code></pre></td></tr></table></figure>

<ol start="3">
<li>在 md 文章目录中创建同名的文件夹，比如当前这篇文章的名称是 <code>hexo插入本地图片路径问题</code>， 那就需要在这篇文章的同级目录中创建一个同名的文件夹，然后图片就放在这个文件夹中</li>
<li>在使用 md 语法引入图片的时候，因为图片是放在目录中（和文章同名的那个目录），但是如果在本地 vscode 中将目录也加上，虽然本地可以访问，但是 hexo g 生成之后，访问博客地址是无法访问的，所以本地引入图片的时候只能写图片的文件名，不要路径，这样虽然本地无法预览，但是 hexo s 和 hexo d 之后的结果都可以正常查看</li>
</ol>
]]></content>
      <categories>
        <category>tools</category>
        <category>hexo</category>
      </categories>
      <tags>
        <tag>tools</tag>
        <tag>hexo</tag>
      </tags>
  </entry>
  <entry>
    <title>hexo和Next主题修改</title>
    <url>/2023/52739010ef7c/index.html</url>
    <content><![CDATA[<h1 id="hexo中-Next主题相关修改"><a href="#hexo中-Next主题相关修改" class="headerlink" title="hexo中 Next主题相关修改"></a>hexo中 Next主题相关修改</h1><h2 id="文章目录默认展开"><a href="#文章目录默认展开" class="headerlink" title="文章目录默认展开"></a>文章目录默认展开</h2><p>基于 Next7.x 版本</p>
<ol>
<li>主题配置文件 _config.yml 中搜索 custom_file_path,看到如下内容，打开 style 那一行的注释</li>
</ol>
<figure class="highlight yml"><table><tr><td class="code"><pre><code class="hljs yml"><span class="hljs-attr">custom_file_path:</span><br>  <span class="hljs-comment">#head: source/_data/head.swig</span><br>  <span class="hljs-comment">#header: source/_data/header.swig</span><br>  <span class="hljs-comment">#sidebar: source/_data/sidebar.swig</span><br>  <span class="hljs-comment">#postMeta: source/_data/post-meta.swig</span><br>  <span class="hljs-comment">#postBodyEnd: source/_data/post-body-end.swig</span><br>  <span class="hljs-comment">#footer: source/_data/footer.swig</span><br>  <span class="hljs-comment">#bodyEnd: source/_data/body-end.swig</span><br>  <span class="hljs-comment">#variable: source/_data/variables.styl</span><br>  <span class="hljs-comment">#mixin: source/_data/mixins.styl</span><br>  <span class="hljs-attr">style:</span> <span class="hljs-string">source/_data/styles.styl</span><br></code></pre></td></tr></table></figure>
<span id="more"></span>
<ol start="2">
<li>在项目根目录(注意这里不是在主题目录下新建)下的 source 目录下新建 _data 目录，并且创建 style.styl 文件，内容如下</li>
</ol>
<figure class="highlight stylus"><table><tr><td class="code"><pre><code class="hljs stylus"><span class="hljs-comment">//文章目录默认展开</span><br><span class="hljs-selector-class">.post-toc</span> <span class="hljs-selector-class">.nav</span> <span class="hljs-selector-class">.nav-child</span> &#123; <br>    <span class="hljs-attribute">display</span>: block; <br>&#125;<br><span class="hljs-comment">//文章目录字体大小调整</span><br><span class="hljs-selector-class">.post-toc</span> <span class="hljs-selector-tag">ol</span> &#123;  <br>    <span class="hljs-attribute">font-size</span> : <span class="hljs-number">14px</span>;     <br>&#125;<br></code></pre></td></tr></table></figure>
<ol start="3">
<li>修改主题配置文件 _config.yml, 搜索 toc 修改内容如下</li>
</ol>
<figure class="highlight yaml"><table><tr><td class="code"><pre><code class="hljs yaml"><span class="hljs-comment"># 文章目录</span><br><span class="hljs-attr">toc:</span><br>  <span class="hljs-attr">enable:</span> <span class="hljs-literal">true</span><br>  <span class="hljs-attr">number:</span> <span class="hljs-literal">true</span><br>  <span class="hljs-attr">wrap:</span> <span class="hljs-literal">true</span><br></code></pre></td></tr></table></figure>

<h2 id="添加搜索功能"><a href="#添加搜索功能" class="headerlink" title="添加搜索功能"></a>添加搜索功能</h2><ol>
<li>安装依赖</li>
</ol>
<figure class="highlight sql"><table><tr><td class="code"><pre><code class="hljs sql">npm install hexo<span class="hljs-operator">-</span>generator<span class="hljs-operator">-</span><span class="hljs-keyword">search</span> <span class="hljs-comment">--save</span><br></code></pre></td></tr></table></figure>

<ol start="2">
<li>在全局配置文件 _config.yml 中添加如下内容</li>
</ol>
<figure class="highlight dts"><table><tr><td class="code"><pre><code class="hljs dts"><span class="hljs-symbol">search:</span><br><span class="hljs-symbol">  path:</span> search.xml<br><span class="hljs-symbol">  field:</span> post<br><span class="hljs-symbol">  format:</span> html<br><span class="hljs-symbol">  limit:</span> <span class="hljs-number">10000</span><br></code></pre></td></tr></table></figure>

<ol start="3">
<li>修改主题配置文件 _config.yml，这里使用的是 Next 主题, 搜索 local_search 将 enable: false 改成 true</li>
</ol>
<figure class="highlight yaml"><table><tr><td class="code"><pre><code class="hljs yaml"><span class="hljs-attr">local_search:</span><br>  <span class="hljs-attr">enable:</span> <span class="hljs-literal">true</span><br></code></pre></td></tr></table></figure>

<ol start="4">
<li>重新生成</li>
</ol>
<figure class="highlight ebnf"><table><tr><td class="code"><pre><code class="hljs ebnf"><span class="hljs-attribute">hexo clean</span><br><span class="hljs-attribute">hexo g</span><br></code></pre></td></tr></table></figure>



<h2 id="新建菜单以及添加菜单图标"><a href="#新建菜单以及添加菜单图标" class="headerlink" title="新建菜单以及添加菜单图标"></a>新建菜单以及添加菜单图标</h2><p><a href="http://theme-next.iissnan.com/getting-started.html#menu-settings">next官网</a></p>
<p>在官网中查看主题设定-&gt;菜单那部分，当我们新添加一个菜单是(比如我要添加阅读)，在<code>next</code>的配置文件中(<code>_config.yml</code>)里面找到下面的配置：</p>
<p>Next8.x 版本</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><code class="hljs yaml"><span class="hljs-attr">menu:</span><br>  <span class="hljs-attr">home:</span> <span class="hljs-string">/</span> <span class="hljs-string">||</span> <span class="hljs-string">fa</span> <span class="hljs-string">fa-home</span><br>  <span class="hljs-attr">about:</span> <span class="hljs-string">/about/</span> <span class="hljs-string">||</span> <span class="hljs-string">fa</span> <span class="hljs-string">fa-user</span><br>  <span class="hljs-attr">tags:</span> <span class="hljs-string">/tags/</span> <span class="hljs-string">||</span> <span class="hljs-string">fa</span> <span class="hljs-string">fa-tags</span><br>  <span class="hljs-attr">categories:</span> <span class="hljs-string">/categories/</span> <span class="hljs-string">||</span> <span class="hljs-string">fa</span> <span class="hljs-string">fa-th</span><br>  <span class="hljs-attr">archives:</span> <span class="hljs-string">/archives/</span> <span class="hljs-string">||</span> <span class="hljs-string">fa</span> <span class="hljs-string">fa-archive</span><br>  <span class="hljs-comment"># schedule: /schedule/ || fa fa-calendar</span><br>  <span class="hljs-attr">book:</span> <span class="hljs-string">/book/</span> <span class="hljs-string">||</span> <span class="hljs-string">fa</span> <span class="hljs-string">fa-book</span> <span class="hljs-comment">#这里是新添加的菜单</span><br><br><span class="hljs-comment"># Enable/Disable menu icons.</span><br><span class="hljs-attr">menu_icons:</span><br>  <span class="hljs-attr">enable:</span> <span class="hljs-literal">true</span><br>  <span class="hljs-attr">book:</span> <span class="hljs-string">book（这里是图标）</span><br></code></pre></td></tr></table></figure>

<p>然后在开启的对应的语言文件中，比如我使用的是中文，那么就是在<code>zh-CN.yml</code>文件中找到<code>menu</code>：</p>
<figure class="highlight yml"><table><tr><td class="code"><pre><code class="hljs yml"><span class="hljs-attr">menu:</span><br>  <span class="hljs-attr">home:</span> <span class="hljs-string">首页</span><br>  <span class="hljs-attr">archives:</span> <span class="hljs-string">归档</span><br>  <span class="hljs-attr">categories:</span> <span class="hljs-string">分类</span><br>  <span class="hljs-attr">tags:</span> <span class="hljs-string">标签</span><br>  <span class="hljs-attr">search:</span> <span class="hljs-string">搜索</span><br>  <span class="hljs-attr">book:</span> <span class="hljs-string">阅读</span><br></code></pre></td></tr></table></figure>

<p>后面的book那一栏是后来添加的。需要注意的是在上面的第一个配置文件中所写的<code>book</code>是<a href="http://fontawesome.io/">Font Awesome</a>中<code>icon</code>的名称，这样才可以在我们自己的页面显示出图标。</p>
<p>在 <code>source/</code> 目录下创建目录 <code>book</code>,  然后在 <code>source/book</code> 目录下创建 <code>index.md</code>, 内容如下：</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><code class="hljs yaml"><span class="hljs-meta">---</span><br><span class="hljs-attr">title:</span> <span class="hljs-string">book</span><br><span class="hljs-attr">date:</span> <span class="hljs-number">2023-11-10 21:12:59</span><br><span class="hljs-attr">type:</span> <span class="hljs-string">book</span><br><span class="hljs-attr">comments:</span> <span class="hljs-literal">true</span><br><span class="hljs-meta">---</span><br></code></pre></td></tr></table></figure>

<h2 id="添加社交功能之邮箱"><a href="#添加社交功能之邮箱" class="headerlink" title="添加社交功能之邮箱"></a>添加社交功能之邮箱</h2><p>在next主题配置文件搜索social:</p>
<figure class="highlight yml"><table><tr><td class="code"><pre><code class="hljs yml"><span class="hljs-attr">social:</span><br>  <span class="hljs-attr">E-Mail:</span> <span class="hljs-string">mailto:1744709138@qq.com</span> <span class="hljs-string">||</span> <span class="hljs-string">envelope</span><br></code></pre></td></tr></table></figure>

<p>这里只是显示了邮箱，这些原来都是注释掉的，自己去掉前面的注释即可，需要注意的是邮箱前面的<code>mailto</code>是必须要保留的，还有最后面的<code>envelope</code>则是图标名字，也是不能修改的。</p>
<h2 id="友情链接的配置"><a href="#友情链接的配置" class="headerlink" title="友情链接的配置"></a>友情链接的配置</h2><p>在<code>next</code>主题配置文件中查找标签<code>links</code>并进行如下配置:</p>
<figure class="highlight yml"><table><tr><td class="code"><pre><code class="hljs yml"><span class="hljs-comment"># title</span><br><span class="hljs-attr">links_title:</span> <span class="hljs-string">Links</span><br><span class="hljs-attr">links:</span><br>  <span class="hljs-attr">MacTalk:</span> <span class="hljs-string">http://macshuo.com/</span><br>  <span class="hljs-attr">Title:</span> <span class="hljs-string">http://example.com/</span><br></code></pre></td></tr></table></figure>

<h2 id="Next8-增加评论功能（valine）"><a href="#Next8-增加评论功能（valine）" class="headerlink" title="Next8 增加评论功能（valine）"></a>Next8 增加评论功能（valine）</h2><p>在 Next8.1.0 中移除了 valine, <a href="https://github.com/next-theme/hexo-theme-next/issues/4">参考网址</a><br>如果还需要继续使用，官方有提供插件，具体操作如下</p>
<ol>
<li>安装插件 <code>npm install hexo-next-valine --save</code></li>
<li>编辑 Next 主题 _config.yml 文件，找到 <code>comments:</code> 标签，在下面添加 valine 相关设置</li>
</ol>
<figure class="highlight yaml"><table><tr><td class="code"><pre><code class="hljs yaml"><span class="hljs-attr">comments:</span><br>  <span class="hljs-comment"># Available values: tabs | buttons</span><br>  <span class="hljs-attr">style:</span> <span class="hljs-string">tabs</span><br>  <span class="hljs-attr">active:</span> <span class="hljs-string">valine</span><br><br><span class="hljs-attr">valine:</span><br>  <span class="hljs-attr">enable:</span> <span class="hljs-literal">true</span><br>  <span class="hljs-attr">appId:</span> <span class="hljs-string">设置apiId</span><br>  <span class="hljs-attr">appKey:</span> <span class="hljs-string">设置apiKey</span><br>  <span class="hljs-attr">serverURLs:</span> <span class="hljs-string">https://leancloud.cn</span> <span class="hljs-comment"># When the custom domain name is enabled, fill it in here</span><br>  <span class="hljs-attr">placeholder:</span> <span class="hljs-string">期待与您的交流</span> <span class="hljs-comment"># Comment box placeholder</span><br>  <span class="hljs-attr">avatar:</span> <span class="hljs-string">mm</span> <span class="hljs-comment"># Gravatar style</span><br>  <span class="hljs-attr">meta:</span> [<span class="hljs-string">nick</span>, <span class="hljs-string">mail</span>, <span class="hljs-string">link</span>] <span class="hljs-comment"># Custom comment header</span><br>  <span class="hljs-attr">pageSize:</span> <span class="hljs-number">10</span> <span class="hljs-comment"># Pagination size</span><br>  <span class="hljs-attr">lang:</span> <span class="hljs-comment"># Language, available values: en, zh-cn</span><br>  <span class="hljs-attr">visitor:</span> <span class="hljs-literal">false</span> <span class="hljs-comment"># Article reading statistic</span><br>  <span class="hljs-attr">comment_count:</span> <span class="hljs-literal">true</span> <span class="hljs-comment"># If false, comment count will only be displayed in post page, not in home page</span><br>  <span class="hljs-attr">recordIP:</span> <span class="hljs-literal">false</span> <span class="hljs-comment"># Whether to record the commenter IP</span><br>  <span class="hljs-attr">enableQQ:</span> <span class="hljs-literal">false</span> <span class="hljs-comment"># Whether to enable the Nickname box to automatically get QQ Nickname and QQ Avatar</span><br>  <span class="hljs-attr">requiredFields:</span> []<br></code></pre></td></tr></table></figure>

<h2 id="添加近期文章展示"><a href="#添加近期文章展示" class="headerlink" title="添加近期文章展示"></a>添加近期文章展示</h2><p><a href="https://blog.csdn.net/wugenqiang/article/details/88581218">查看转载文章</a></p>
<h2 id="显示文章的更新时间"><a href="#显示文章的更新时间" class="headerlink" title="显示文章的更新时间"></a>显示文章的更新时间</h2><p>在主题的配置文件中找到<code>updated_at</code>，然后将其值修改为<code>true</code>即可(这种方式应该是适用于5以后的版本)</p>
<p>对于以前的方式可以看这篇<a href="https://blog.csdn.net/ganzhilin520/article/details/79053399">别人写的文章</a></p>
<h2 id="字数统计与阅读时长"><a href="#字数统计与阅读时长" class="headerlink" title="字数统计与阅读时长"></a>字数统计与阅读时长</h2><p><a href="https://www.cnblogs.com/php-linux/p/8418518.html">查看转载文章</a></p>
<h2 id="添加版权信息"><a href="#添加版权信息" class="headerlink" title="添加版权信息"></a>添加版权信息</h2><p>在站点配置文件搜索<code>post_copyright</code>，将<code>enable</code>修改为<code>true</code>，添加<code>author</code>即可。<br><a href="https://zhuanlan.zhihu.com/p/463548944#:~:text=%E4%BA%8C%E3%80%81hexo%E5%8D%9A%E5%AE%A2%E6%96%B0%E5%A2%9E%E5%BA%95%E9%83%A8%E7%89%88%E6%9D%83%E4%BF%A1%E6%81%AF%202.1%20%E5%BC%80%E5%90%AF%E6%96%87%E7%AB%A0%E5%BA%95%E9%83%A8%E7%89%88%E6%9D%83%E4%BF%A1%E6%81%AF%E9%85%8D%E7%BD%AE%20%E7%BC%96%E8%BE%91%E9%85%8D%E7%BD%AE%E6%96%87%E4%BB%B6themes%2Fnext%2F_config.yml%EF%BC%8C%E5%B0%86creative_commons%E4%B8%8B%E9%9D%A2%E7%9A%84post%E5%80%BC%E7%94%B1false%E6%94%B9%E4%B8%BAtrue%E3%80%82%20creative_commons%3A%20license%3A%20by-nc-sa,sidebar%3A%20false%20post%3A%20true%20language%3A%202.2%20%E6%9B%B4%E6%96%B0%E7%89%88%E6%9D%83%E8%AF%B4%E6%98%8E%20%E7%BC%96%E8%BE%91%E6%96%87%E4%BB%B6themes%2Fnext%2Flayout%2F_partials%2Fpost%2Fpost-copyright.swig%EF%BC%8C%E6%9B%B4%E6%96%B0%E7%89%88%E6%9D%83%E8%AF%B4%E6%98%8E%E6%96%87%E5%AD%97%EF%BC%8C%E4%BF%9D%E5%AD%98%E3%80%82">参考文章</a></p>
<figure class="highlight yml"><table><tr><td class="code"><pre><code class="hljs yml"><span class="hljs-attr">post_copyright:</span><br>  <span class="hljs-attr">enable:</span> <span class="hljs-literal">true</span><br>  <span class="hljs-attr">license:</span> <span class="hljs-string">CC</span> <span class="hljs-string">BY-NC-SA</span> <span class="hljs-number">3.0</span><br>  <span class="hljs-attr">license_url:</span> <span class="hljs-string">https://creativecommons.org/licenses/by-nc-sa/3.0/</span><br>  <span class="hljs-attr">author:</span> <span class="hljs-string">咖啡杯里的茶</span><br></code></pre></td></tr></table></figure>

<h2 id="修改文章底部的标签"><a href="#修改文章底部的标签" class="headerlink" title="修改文章底部的标签"></a>修改文章底部的标签</h2><p>基于 Next7 修改<br>在主题配置文件 <code>_config.yml</code> 中将下面配置设置为 true 即可（该配置默认值为false）</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><code class="hljs yaml"><span class="hljs-attr">tag_icon:</span> <span class="hljs-literal">true</span><br></code></pre></td></tr></table></figure>

<h2 id="显示图片"><a href="#显示图片" class="headerlink" title="显示图片"></a>显示图片</h2><p><a href="https://www.jianshu.com/p/ea78bdd0551f">显示图片</a></p>
<h2 id="添加搜索功能-1"><a href="#添加搜索功能-1" class="headerlink" title="添加搜索功能"></a>添加搜索功能</h2><p><a href="https://blog.csdn.net/qq_40265501/article/details/80030627">添加搜索</a></p>
<h2 id="添加打赏功能"><a href="#添加打赏功能" class="headerlink" title="添加打赏功能"></a>添加打赏功能</h2><p>在<code>next</code>主题的配置文件文件中加上下面的代码</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><code class="hljs xml">reward_comment: 坚持原创技术分享，您的支持将鼓励我继续创作！<br>wechatpay: /images/微信支付.jpg<br>alipay: /images/支付宝支付.jpg<br></code></pre></td></tr></table></figure>

<p>其中<code>wechatpay</code>和<code>alipay</code>对应的是微信支付收款码和支付宝支付收款码的图片地址，我这里是存放在 <code>主题\source\images\</code>下面</p>
<h2 id="生成永久链接"><a href="#生成永久链接" class="headerlink" title="生成永久链接"></a>生成永久链接</h2><p><a href="https://zhuanlan.zhihu.com/p/134492757">Hexo 生成永久文章链接 - 知乎 (zhihu.com)</a><br>对于Heox7 版本可以有更简单的方式，直接使用 hash 即可</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><code class="hljs yaml"><span class="hljs-comment">#permalink: :year/:month/:day/:title/</span><br><span class="hljs-attr">permalink:</span> <span class="hljs-string">:hash/</span><br></code></pre></td></tr></table></figure>

<h2 id="文章收录"><a href="#文章收录" class="headerlink" title="文章收录"></a>文章收录</h2><p><a href="https://zhuanlan.zhihu.com/p/651590960">关于Hexo博客收录与SEO那些事 - 知乎 (zhihu.com)</a><br>文章中验证网站的步骤中使用的是下载 html 验证文件，有可能这种方式验证不成功，可以选择第二种方式，在网站首页添加 meta 标签中的内容，将 meta 中的内容复制到<br><code>themes\nextlayout\_partials\head\head.njk</code> 文件中</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><code class="hljs xml"><span class="hljs-tag">&lt;<span class="hljs-name">meta</span> <span class="hljs-attr">charset</span>=<span class="hljs-string">&quot;UTF-8&quot;</span>&gt;</span><br><span class="hljs-tag">&lt;<span class="hljs-name">meta</span> <span class="hljs-attr">name</span>=<span class="hljs-string">&quot;viewport&quot;</span> <span class="hljs-attr">content</span>=<span class="hljs-string">&quot;width=device-width&quot;</span>&gt;</span><br><span class="hljs-comment">&lt;!-- 这里添加的是百度的验证头文件 --&gt;</span><br><span class="hljs-tag">&lt;<span class="hljs-name">meta</span> <span class="hljs-attr">name</span>=<span class="hljs-string">&quot;baidu-site-verification&quot;</span> <span class="hljs-attr">content</span>=<span class="hljs-string">&quot;xxxx&quot;</span> /&gt;</span><br><span class="hljs-comment">&lt;!-- 这里添加的是bing验证的头文件 --&gt;</span><br><span class="hljs-tag">&lt;<span class="hljs-name">meta</span> <span class="hljs-attr">name</span>=<span class="hljs-string">&quot;msvalidate.01&quot;</span> <span class="hljs-attr">content</span>=<span class="hljs-string">&quot;xxxx&quot;</span> /&gt;</span><br></code></pre></td></tr></table></figure>

<h2 id="升级-Hexo"><a href="#升级-Hexo" class="headerlink" title="升级 Hexo"></a>升级 Hexo</h2><p><a href="https://guoguocai.github.io/2022/06/05/%E5%A6%82%E4%BD%95%E5%8D%87%E7%BA%A7-Hexo-%E7%89%88%E6%9C%AC/">升级hexo</a></p>
<h2 id="Next7-多级目录折叠"><a href="#Next7-多级目录折叠" class="headerlink" title="Next7 多级目录折叠"></a>Next7 多级目录折叠</h2><p><a href="https://alex-mcavoy.github.io/hexo/41b257b8.html">多级目录折叠</a><br>按照上面参考的文档开启 Jquery, 页面依然报错 <code>$ 符号未定义</code>, 此时可以按照如下方式修改,在进入 category.js 之前先引入一次 Jquery</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><code class="hljs xml"><span class="hljs-comment">&lt;!-- 多级目录 --&gt;</span><br><span class="hljs-tag">&lt;<span class="hljs-name">script</span> <span class="hljs-attr">src</span>=<span class="hljs-string">&quot;https://apps.bdimg.com/libs/jquery/2.1.4/jquery.min.js&quot;</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-name">script</span>&gt;</span><br><span class="hljs-tag">&lt;<span class="hljs-name">script</span> <span class="hljs-attr">src</span>=<span class="hljs-string">&quot;/js/category.js&quot;</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-name">script</span>&gt;</span><br></code></pre></td></tr></table></figure>

<h2 id="Hexo-Next主题支持-mermaid"><a href="#Hexo-Next主题支持-mermaid" class="headerlink" title="Hexo Next主题支持 mermaid"></a>Hexo Next主题支持 mermaid</h2><ol>
<li>进入博客根目录，执行安装插件命令 <code>npm install hexo-filter-mermaid-diagrams --save</code></li>
<li>打开 Next 主题下的 _config.yml 文件，将 mermaid 的配置改成 true</li>
</ol>
<figure class="highlight yml"><table><tr><td class="code"><pre><code class="hljs yml"><span class="hljs-attr">mermaid:</span><br>  <span class="hljs-attr">enable:</span> <span class="hljs-literal">true</span><br></code></pre></td></tr></table></figure>
<p>在站点配置文件中修改如下(去除图形的高亮)</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><code class="hljs yaml"><span class="hljs-attr">highlight:</span><br>  <span class="hljs-attr">enable:</span> <span class="hljs-literal">true</span><br>  <span class="hljs-comment"># 增加下面的内容</span><br>  <span class="hljs-attr">exclude_languages:</span><br>    <span class="hljs-bullet">-</span> <span class="hljs-string">mermaid</span><br></code></pre></td></tr></table></figure>
<ol start="3">
<li>重新生成部署即可</li>
</ol>
<h2 id="增加图片背景和透明度"><a href="#增加图片背景和透明度" class="headerlink" title="增加图片背景和透明度"></a>增加图片背景和透明度</h2><ol>
<li>将背景图片放在主题目录下 <code>themes/next/source/images/</code> 下面</li>
<li>修改 Next 主题配置文件 <code>_config.yml</code>, 将下面的注释打开</li>
</ol>
<figure class="highlight yaml"><table><tr><td class="code"><pre><code class="hljs yaml"><span class="hljs-attr">custom_file_path:</span><br>  <span class="hljs-comment">#head: source/_data/head.swig</span><br>  <span class="hljs-comment">#header: source/_data/header.swig</span><br>  <span class="hljs-comment">#sidebar: source/_data/sidebar.swig</span><br>  <span class="hljs-comment">#postMeta: source/_data/post-meta.swig</span><br>  <span class="hljs-comment">#postBodyEnd: source/_data/post-body-end.swig</span><br>  <span class="hljs-comment">#footer: source/_data/footer.swig</span><br>  <span class="hljs-comment">#bodyEnd: source/_data/body-end.swig</span><br>  <span class="hljs-comment">#variable: source/_data/variables.styl</span><br>  <span class="hljs-comment">#mixin: source/_data/mixins.styl</span><br>  <span class="hljs-attr">style:</span> <span class="hljs-string">source/_data/styles.styl</span><br></code></pre></td></tr></table></figure>
<ol start="3">
<li>在博客根目录下的 <code>source/</code> 下面创建 <code>_data/styles.styl</code> 文件（默认在 source 目录下没有 _data 目录，若没有也需要创建）</li>
<li>在上面创建的 <code>styles.styl</code> 文件中增加如下内容</li>
</ol>
<figure class="highlight less"><table><tr><td class="code"><pre><code class="hljs less"><span class="hljs-comment">// 图片背景 start------------</span><br><span class="hljs-selector-tag">body</span> &#123;<br>    <span class="hljs-attribute">background</span>:url(<span class="hljs-string">/images/background.jpg</span>);		<span class="hljs-comment">//图片路径，默认</span><br>    <span class="hljs-attribute">background-repeat</span>: no-repeat;			<span class="hljs-comment">//图片无法铺满时，是否重复及重复方式</span><br>    <span class="hljs-attribute">background-attachment</span>:fixed;		<span class="hljs-comment">//图片是否跟随滚动</span><br>    <span class="hljs-attribute">background-size</span>:cover;				<span class="hljs-comment">//覆盖</span><br>    <span class="hljs-attribute">background-position</span>:center;		<span class="hljs-comment">//图片显示起始位置</span><br>&#125;<br><span class="hljs-comment">// 图片背景 end------------</span><br><br><br><span class="hljs-comment">// 透明度start----------------------------</span><br><span class="hljs-comment">// 文章内容透明度设置</span><br><span class="hljs-comment">// 7 版本有效</span><br><span class="hljs-selector-class">.content-wrap</span> &#123;<br>    <span class="hljs-attribute">opacity</span>: <span class="hljs-number">0.9</span>;<br>&#125;<br><br><span class="hljs-comment">// 8版本有效</span><br><span class="hljs-selector-class">.main-inner</span> &#123;<br>    <span class="hljs-attribute">opacity</span>: <span class="hljs-number">0.9</span>;<br>&#125;<br><br><span class="hljs-selector-class">.header-inner</span> &#123;<br>    <span class="hljs-attribute">background</span>: <span class="hljs-built_in">rgba</span>(<span class="hljs-number">255</span>, <span class="hljs-number">255</span>, <span class="hljs-number">255</span>, <span class="hljs-number">0.9</span>);<br>&#125;<br><br><span class="hljs-comment">//侧边框的透明度设置</span><br><span class="hljs-selector-class">.sidebar</span> &#123;<br>  <span class="hljs-attribute">opacity</span>: <span class="hljs-number">0.9</span>;<br>&#125;<br><br><span class="hljs-comment">// 搜索框的透明度</span><br><span class="hljs-selector-class">.popup</span> &#123;<br>    <span class="hljs-attribute">opacity</span>: <span class="hljs-number">0.9</span>;<br>&#125;<br><span class="hljs-comment">// 透明度end----------------------------</span><br></code></pre></td></tr></table></figure>

<h2 id="静态资源压缩"><a href="#静态资源压缩" class="headerlink" title="静态资源压缩"></a>静态资源压缩</h2><p><a href="https://github.com/rozbo/hexo-neat">参考github hexo-neat 插件</a></p>
<ol>
<li>安装插件</li>
</ol>
<figure class="highlight ada"><table><tr><td class="code"><pre><code class="hljs ada">npm install hexo-neat <span class="hljs-comment">--save</span><br></code></pre></td></tr></table></figure>

<ol start="2">
<li>在 hexo 配置文件 <code>_config.yml</code> 中添加如下配置</li>
</ol>
<figure class="highlight yaml"><table><tr><td class="code"><pre><code class="hljs yaml"><span class="hljs-comment"># 静态文件压缩 start-----------------</span><br><span class="hljs-attr">neat_enable:</span> <span class="hljs-literal">true</span><br><span class="hljs-attr">neat_html:</span><br>  <span class="hljs-attr">enable:</span> <span class="hljs-literal">true</span><br>  <span class="hljs-attr">exclude:</span><br><span class="hljs-attr">neat_css:</span><br>  <span class="hljs-attr">enable:</span> <span class="hljs-literal">true</span><br>  <span class="hljs-attr">exclude:</span><br>    <span class="hljs-bullet">-</span> <span class="hljs-string">&#x27;*.min.css&#x27;</span><br><span class="hljs-attr">neat_js:</span><br>  <span class="hljs-attr">enable:</span> <span class="hljs-literal">true</span><br>  <span class="hljs-attr">mangle:</span> <span class="hljs-literal">true</span><br>  <span class="hljs-attr">output:</span><br>  <span class="hljs-attr">compress:</span><br>  <span class="hljs-attr">exclude:</span><br>    <span class="hljs-bullet">-</span> <span class="hljs-string">&#x27;*.min.js&#x27;</span><br>    <span class="hljs-bullet">-</span> <span class="hljs-string">&#x27;**/*.min.js&#x27;</span><br><span class="hljs-comment"># 静态文件压缩 end-----------------</span><br></code></pre></td></tr></table></figure>
<ol start="3">
<li>重新生成推送即可</li>
</ol>
<h2 id="引用自己的文章"><a href="#引用自己的文章" class="headerlink" title="引用自己的文章"></a>引用自己的文章</h2><p><code>&#123;% post_path filename %&#125;</code><br>filename 就是引用文章的名称</p>
<h2 id="阻止点击more时跳转到more位置"><a href="#阻止点击more时跳转到more位置" class="headerlink" title="阻止点击more时跳转到more位置"></a>阻止点击more时跳转到more位置</h2><ol>
<li>如果给文章添加了 <code>&lt;!--more--&gt;</code> 点击阅读更多时会跳转到 <code>more</code> 锚点位置，可以通过下面方式修改</li>
<li>找到 <code>themes\next-8.18.2\layout\_macro\post.njk</code> 文件， 搜索 <code>elif post.excerpt</code>，相关部分原来的代码如下</li>
</ol>
<figure class="highlight handlebars"><table><tr><td class="code"><pre><code class="hljs handlebars"><span class="language-xml">&#123;% elif post.excerpt %&#125;</span><br><span class="language-xml">  </span><span class="hljs-template-variable">&#123;&#123; <span class="hljs-name">post.excerpt</span> &#125;&#125;</span><span class="language-xml"></span><br><span class="language-xml">  <span class="hljs-comment">&lt;!--noindex--&gt;</span></span><br><span class="language-xml">  &#123;%- if theme.read_more_btn %&#125;</span><br><span class="language-xml">    <span class="hljs-tag">&lt;<span class="hljs-name">div</span> <span class="hljs-attr">class</span>=<span class="hljs-string">&quot;post-button&quot;</span>&gt;</span></span><br><span class="language-xml">      <span class="hljs-tag">&lt;<span class="hljs-name">a</span> <span class="hljs-attr">class</span>=<span class="hljs-string">&quot;btn&quot;</span> <span class="hljs-attr">href</span>=<span class="hljs-string">&quot;</span></span></span><span class="hljs-template-variable">&#123;&#123; <span class="hljs-name">url_for</span>(<span class="hljs-name">post.path</span>) &#125;&#125;</span><span class="hljs-template-variable">&#123;&#123;<span class="hljs-name">suffix</span>&#125;&#125;</span><span class="language-xml"><span class="hljs-tag"><span class="hljs-string">&quot;</span> <span class="hljs-attr">rel</span>=<span class="hljs-string">&quot;contents&quot;</span>&gt;</span></span><br><span class="language-xml">        </span><span class="hljs-template-variable">&#123;&#123; <span class="hljs-name">__</span>(<span class="hljs-name">&#x27;post.read_more&#x27;</span>) &#125;&#125;</span><span class="language-xml"> <span class="hljs-symbol">&amp;raquo;</span></span><br><span class="language-xml">      <span class="hljs-tag">&lt;/<span class="hljs-name">a</span>&gt;</span></span><br><span class="language-xml">    <span class="hljs-tag">&lt;/<span class="hljs-name">div</span>&gt;</span></span><br><span class="language-xml">  &#123;%- endif %&#125;</span><br><span class="language-xml">  <span class="hljs-comment">&lt;!--/noindex--&gt;</span></span><br><span class="language-xml">&#123;% else %&#125;</span><br></code></pre></td></tr></table></figure>
<p>添加部分内容后变成如下内容</p>
<figure class="highlight django"><table><tr><td class="code"><pre><code class="hljs django"><span class="hljs-template-tag">&#123;% <span class="hljs-name"><span class="hljs-name">elif</span></span> post.excerpt %&#125;</span><span class="language-xml"></span><br><span class="language-xml">  </span><span class="hljs-template-variable">&#123;&#123; post.excerpt &#125;&#125;</span><span class="language-xml"></span><br><span class="language-xml">  <span class="hljs-comment">&lt;!--noindex--&gt;</span></span><br><span class="language-xml">  </span><span class="hljs-template-tag">&#123;%- <span class="hljs-name"><span class="hljs-name">if</span></span> theme.read_more_btn %&#125;</span><span class="language-xml"></span><br><span class="language-xml">  <span class="hljs-comment">&lt;!-- 此处是添加的内容 --&gt;</span></span><br><span class="language-xml">  </span><span class="hljs-template-tag">&#123;%- <span class="hljs-name">set</span> suffix = &quot;&quot;%&#125;</span><span class="language-xml"></span><br><span class="language-xml">  </span><span class="hljs-template-tag">&#123;%- <span class="hljs-name"><span class="hljs-name">if</span></span> theme.read_more_anchor %&#125;</span><span class="language-xml"></span><br><span class="language-xml">    </span><span class="hljs-template-tag">&#123;%- <span class="hljs-name">set</span> suffix = &quot;#more&quot;%&#125;</span><span class="language-xml"></span><br><span class="language-xml">  </span><span class="hljs-template-tag">&#123;%- <span class="hljs-name"><span class="hljs-name">endif</span></span> %&#125;</span><span class="language-xml"></span><br><span class="language-xml">  <span class="hljs-comment">&lt;!-- 添加结束 --&gt;</span></span><br><span class="language-xml">    <span class="hljs-tag">&lt;<span class="hljs-name">div</span> <span class="hljs-attr">class</span>=<span class="hljs-string">&quot;post-button&quot;</span>&gt;</span></span><br><span class="language-xml">      <span class="hljs-comment">&lt;!-- 这里有修改，将 #more 改成了 </span></span><span class="hljs-template-variable">&#123;&#123;suffix&#125;&#125;</span><span class="language-xml"><span class="hljs-comment"> --&gt;</span></span><br><span class="language-xml">      <span class="hljs-tag">&lt;<span class="hljs-name">a</span> <span class="hljs-attr">class</span>=<span class="hljs-string">&quot;btn&quot;</span> <span class="hljs-attr">href</span>=<span class="hljs-string">&quot;</span></span></span><span class="hljs-template-variable">&#123;&#123; url_for(post.path) &#125;&#125;</span><span class="hljs-template-variable">&#123;&#123;suffix&#125;&#125;</span><span class="language-xml"><span class="hljs-tag"><span class="hljs-string">&quot;</span> <span class="hljs-attr">rel</span>=<span class="hljs-string">&quot;contents&quot;</span>&gt;</span></span><br><span class="language-xml">        </span><span class="hljs-template-variable">&#123;&#123; __(&#x27;post.read_more&#x27;) &#125;&#125;</span><span class="language-xml"> <span class="hljs-symbol">&amp;raquo;</span></span><br><span class="language-xml">      <span class="hljs-tag">&lt;/<span class="hljs-name">a</span>&gt;</span></span><br><span class="language-xml">    <span class="hljs-tag">&lt;/<span class="hljs-name">div</span>&gt;</span></span><br><span class="language-xml">  </span><span class="hljs-template-tag">&#123;%- <span class="hljs-name"><span class="hljs-name">endif</span></span> %&#125;</span><span class="language-xml"></span><br><span class="language-xml">  <span class="hljs-comment">&lt;!--/noindex--&gt;</span></span><br><span class="language-xml"></span><span class="hljs-template-tag">&#123;% <span class="hljs-name"><span class="hljs-name">else</span></span> %&#125;</span><br></code></pre></td></tr></table></figure>
<ol start="3">
<li>在 next 的 <code>_config.yml</code> 文件中添加如下内容即可，配置为true时依然跳转到more</li>
</ol>
<figure class="highlight yaml"><table><tr><td class="code"><pre><code class="hljs yaml"><span class="hljs-attr">read_more_anchor:</span> <span class="hljs-literal">false</span><br></code></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>tools</category>
        <category>hexo</category>
      </categories>
      <tags>
        <tag>hexo</tag>
      </tags>
  </entry>
  <entry>
    <title>比较</title>
    <url>/2023/2084759b2332/index.html</url>
    <content><![CDATA[<blockquote>
<p>人感到不幸福大多来源于比较，感到满足也源于比较</p>
</blockquote>
<span id="more"></span>
<p>在微信读书看到一位书友写到，作为农村出来的孩子，应该如何改变自己的命运，因为周围的一切在告诉我们，就算你努力了二十几年，才勉强达到了别人的起跑线，但是我们的出身是自己无法决定的，那我们应该如何更进一步呢？</p>
<p>看到另外一位书友的回答很有感触，且记录在此，和有缘人共勉</p>
<blockquote>
<p>回头看，轻舟已过万重山<br>向前看，长路漫漫亦灿灿<br>平常看，人间烟火甚灿烂<br>低头看，万里黄山皆脚下  </p>
</blockquote>
]]></content>
      <categories>
        <category>record</category>
      </categories>
      <tags>
        <tag>record</tag>
      </tags>
  </entry>
  <entry>
    <title>hexo的butterfly主题更换waline系统cdn地址</title>
    <url>/2023/0fd7aa9951f7/index.html</url>
    <content><![CDATA[<ol>
<li><p>butterfly主题默认使用 cdn.jsdelivr.net，在国内使用很慢，有时候加载不出来，可以替换成国内源，比如下面这两个</p>
<figure class="highlight dts"><table><tr><td class="code"><pre><code class="hljs dts"><span class="hljs-symbol">http:</span><span class="hljs-comment">//www.staticfile.org/</span><br><span class="hljs-symbol">https:</span><span class="hljs-comment">//cdn.onmicrosoft.cn/</span><br></code></pre></td></tr></table></figure>
<p>这里我使用的是第一个，打开 <code>http://www.staticfile.org/</code> 后，会出现搜索框，搜索 <code>waline</code>， 然后选择对应的版本就好了，如果不知道自己的版本，可以在切换前先打开自己的 <code>wailne</code> 系统，在评论框下面会显示版本</p>
</li>
<li><p>找到对应的版本后复制出对应的 <code>css</code> 和 <code>js</code> 地址，添加到 <code>_config.butterfly.yml</code> 文件中,比如我使用的是3.0版本，则打开 <code>wailne_css</code> 和 <code>waline_js</code> 注释并添加如下内容</p>
</li>
</ol>
<figure class="highlight yaml"><table><tr><td class="code"><pre><code class="hljs yaml"><span class="hljs-attr">CDN:</span><br>  <span class="hljs-attr">option:</span><br>    <span class="hljs-attr">waline_css:</span> <span class="hljs-string">https://cdn.staticfile.org/waline/3.0.0-alpha.2/waline.min.css</span><br>    <span class="hljs-attr">waline_js:</span> <span class="hljs-string">https://cdn.staticfile.org/waline/3.0.0-alpha.2/waline.min.js</span><br></code></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>tools</category>
        <category>hexo</category>
      </categories>
      <tags>
        <tag>hexo</tag>
      </tags>
  </entry>
  <entry>
    <title>经城雁子谷</title>
    <url>/2023/5f05aa7a8eab/index.html</url>
    <content><![CDATA[<ol>
<li>靠忙碌赚来的钱往往不够花，只有洞见、思考、抓住机会和趋势赚来的钱才能够让你实现阶级跃升。</li>
</ol>
]]></content>
      <categories>
        <category>record</category>
      </categories>
      <tags>
        <tag>record</tag>
      </tags>
  </entry>
  <entry>
    <title>宝贵的人生建议</title>
    <url>/2023/5792ca8e5e27/index.html</url>
    <content><![CDATA[<ul>
<li>书名：《宝贵的人生建议》</li>
<li>作者：凯文凯利</li>
</ul>
<p>看过了，觉得有道理，想要改变都不代表你真的认同并理解，”做出来”就是对”认同”最好的回复</p>
<span id="more"></span>

<blockquote>
<p>把”创造”和”改进”这两个过程分开，你不能边写作边编辑，边雕刻边打磨，边制作边分析。如果你这么做，编辑就会阻遏创造，发明时，不要选择。画草图时，不要检查。写初稿时，不要反思。一开始，创造性思维必须是自由的，不受批判的干扰。</p>
</blockquote>
<p>无论是技术类文章还是其他类型的记录，重点是写，而不是先学习各种各样的套路(套路也很重要，但内容是前提)，后续在写内容之前不要追求完美，先写出来才会有后续的故事</p>
<blockquote>
<p>养成习惯的好处是，在行动时，不必再进行内心的权衡。不再消耗能量去思考是否要做这件事。你只管去做。好习惯很多，从说真话到使用牙线。</p>
</blockquote>
<p>不要使用意志力去坚持做一件事，慢慢养成习惯，让做某件事成为身体的本能</p>
<blockquote>
<p>恐惧是因为缺乏想象力。恐惧的解药不是勇敢，而更可能是想象力</p>
</blockquote>
<ul>
<li>恐惧是因为经历的太少，知道的太少，懂得的太少</li>
</ul>
<blockquote>
<p>你并不需要更多时间，因为你已经拥有了你的所有时间</p>
</blockquote>
<blockquote>
<p>你最近一次改变是在什么年纪，你就是多少岁</p>
</blockquote>
<blockquote>
<p>不要管别人怎么看待你，因为他们根本没看你</p>
</blockquote>
<blockquote>
<p>任何有价值的事都需要无尽的工作。你无法给工作设定上限，所以你必须给工作时间设限。你唯一能管理的是时间，而不是工作</p>
</blockquote>
<blockquote>
<p>如果你想超越你心目中的英雄，那就放下自尊心，像学生一样模仿他们，直到你能超越他们。这是所有大师的成功之道</p>
</blockquote>
]]></content>
      <categories>
        <category>book</category>
      </categories>
      <tags>
        <tag>book</tag>
      </tags>
  </entry>
  <entry>
    <title>微信读书阅读挑战</title>
    <url>/2023/c4c074b722a1/index.html</url>
    <content><![CDATA[<span class="hide_first_mermaid">
<div class="mermaid-wrap"><pre class="mermaid-src" hidden>
  flowchart LR
    A
  </pre></div>
</span>

<p>参与微信阅读挑战</p>
<ol>
<li>365 天不间断</li>
<li>期间最少阅读 300 小时</li>
<li>每月更新进度</li>
</ol>
<div class="tabs" id="总进度"><ul class="nav-tabs"><button type="button" class="tab  active" data-href="总进度-1">23-12</button></ul><div class="tab-contents"><div class="tab-item-content active" id="总进度-1"><p><img src="https://blog-1308475102.cos.ap-guangzhou.myqcloud.com/picture/202401010927782.webp?imageSlim" alt="23年12月"></p></div></div><div class="tab-to-top"><button type="button" aria-label="scroll to top"><i class="fas fa-arrow-up"></i></button></div></div>
]]></content>
      <categories>
        <category>book</category>
      </categories>
      <tags>
        <tag>book</tag>
      </tags>
  </entry>
  <entry>
    <title>Ubuntu23.04中设置其他用户使用sudo无需输入密码</title>
    <url>/2023/a60a9dfd0dd7/index.html</url>
    <content><![CDATA[<p>给 <code>/etc/sudoers</code> 添加写入权限</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">sudo <span class="hljs-built_in">chmod</span> +w /etc/sudoers<br></code></pre></td></tr></table></figure>

<p>添加以下内容</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">sudo vim /etc/sudoers<br><span class="hljs-comment"># Allow members of group sudo to execute any command, 这里的lg就是当前需要免输入密码的用户</span><br>%lg ALL=(ALL:ALL) NOPASSWD:ALL<br></code></pre></td></tr></table></figure>

<p>去除写入权限</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">sudo <span class="hljs-built_in">chmod</span> -w /etc/sudoers<br></code></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>Linux</category>
      </categories>
      <tags>
        <tag>Linux</tag>
      </tags>
  </entry>
  <entry>
    <title>Ubuntu23.04安装jdk和maven</title>
    <url>/2023/e6e83576f25e/index.html</url>
    <content><![CDATA[<h2 id="安装-jdk17"><a href="#安装-jdk17" class="headerlink" title="安装 jdk17"></a>安装 jdk17</h2><p><a href="http://www.codebaoku.com/jdk/jdk-index.html">jdk镜像下载地址</a></p>
<ul>
<li>下载 <code>jdk-17.0.7_linux-x64_bin.tar.gz</code></li>
<li>将包上传到 <code>Ubuntu</code> 中，比如这里上传到了 <code>/opt/software</code> 目录下<figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">cd</span> /opt/software<br>tar -zxvf jdk-17.0.7_linux-x64_bin.tar.gz<br>sudo vim /etc/profile<br></code></pre></td></tr></table></figure>
然后将下面内容复制到 <code>/etc/profile</code> 中<figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash"><span class="hljs-comment"># /opt/software/jdk-17.0.7 就是上面解压的 jdk 目录</span><br><span class="hljs-built_in">export</span> JAVA_HOME=/opt/software/jdk-17.0.7<br><span class="hljs-built_in">export</span> PATH=<span class="hljs-variable">$JAVA_HOME</span>/bin:<span class="hljs-variable">$PATH</span><br><span class="hljs-built_in">export</span> CLASSPATH=.:<span class="hljs-variable">$JAVA_HOME</span>/lib/dt.jar:<span class="hljs-variable">$JAVA_HOME</span>/lib/tools.jar<br></code></pre></td></tr></table></figure>
保存退出，执行 <code>source /etc/profile</code> </li>
<li>验证是否安装成功<figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">java -version<br></code></pre></td></tr></table></figure></li>
</ul>
<h2 id="安装-maven"><a href="#安装-maven" class="headerlink" title="安装 maven"></a>安装 maven</h2><p><a href="https://mirrors.aliyun.com/apache/maven/">阿里云mavne镜像站</a><br><a href="https://maven.apache.org/download.cgi">maven官网下载</a></p>
<ul>
<li>下载 <code>apache-maven-3.9.6-bin.tar.gz</code></li>
<li>上传到 <code>Ubuntu</code> 中，这里是上传到 <code>/opt/software</code> 目录下<figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">cd</span> /opt/software<br>tar -zxvf apache-maven-3.9.6-bin.tar.gz<br>sudo vim /etc/profile<br></code></pre></td></tr></table></figure>
然后将下面内容复制到 <code>/etc/profile</code> 末尾<figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">export</span> MAVEN_HOME=/opt/software/apache-maven-3.9.6<br><span class="hljs-built_in">export</span> PATH=<span class="hljs-variable">$MAVEN_HOME</span>/bin:<span class="hljs-variable">$PATH</span><br></code></pre></td></tr></table></figure>
验证是否安装成功<figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">mvn -v<br></code></pre></td></tr></table></figure></li>
</ul>
]]></content>
      <categories>
        <category>Linux</category>
      </categories>
      <tags>
        <tag>Linux</tag>
      </tags>
  </entry>
  <entry>
    <title>Ubuntu中设置环境变量的几种方式</title>
    <url>/2023/1f7648c2ff11/index.html</url>
    <content><![CDATA[<h2 id="当前终端有效"><a href="#当前终端有效" class="headerlink" title="当前终端有效"></a>当前终端有效</h2> <figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash"><span class="hljs-comment">#  直接在终端中执行，则可以在当前终端的任意目录使用 mysql 命令</span><br><span class="hljs-built_in">export</span> PATH=/opt/mysql/bin:PATH<br></code></pre></td></tr></table></figure>

<h2 id="当前用户有效"><a href="#当前用户有效" class="headerlink" title="当前用户有效"></a>当前用户有效</h2><h3 id="修改-bashrc-文件"><a href="#修改-bashrc-文件" class="headerlink" title="修改 ~/.bashrc 文件"></a>修改 <code>~/.bashrc</code> 文件</h3><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">vim ~/.bashrc<br><span class="hljs-built_in">export</span> PATH=/opt/mysql/bin:PATH<br><br><span class="hljs-comment"># 保存退出后，执行 source ~/.bashrc 命令使其立即生效</span><br><span class="hljs-built_in">source</span> ~/.bashrc<br></code></pre></td></tr></table></figure>
<p>这种方式永久生效</p>
<h2 id="系统级别有效"><a href="#系统级别有效" class="headerlink" title="系统级别有效"></a>系统级别有效</h2><h3 id="修改-etc-profile-文件"><a href="#修改-etc-profile-文件" class="headerlink" title="修改 /etc/profile 文件"></a>修改 <code>/etc/profile</code> 文件</h3><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">vim /etc/profile<br><span class="hljs-built_in">export</span> PATH=/opt/mysql/bin:PATH<br><br><span class="hljs-comment"># 保存退出后，执行 source /etc/profile 命令使其立即生效</span><br><span class="hljs-built_in">source</span> /etc/profile<br></code></pre></td></tr></table></figure>
<p>这种方式永久生效，并且对所有用户有效</p>
]]></content>
      <categories>
        <category>Linux</category>
      </categories>
      <tags>
        <tag>Linux</tag>
      </tags>
  </entry>
  <entry>
    <title>vmware中Ubuntu23.04设置静态ip</title>
    <url>/2023/928cb00f8073/index.html</url>
    <content><![CDATA[<h2 id="查看网卡名称"><a href="#查看网卡名称" class="headerlink" title="查看网卡名称"></a>查看网卡名称</h2><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">ip a<br></code></pre></td></tr></table></figure>
<p>比如查询的结果是 <code>ens33</code></p>
<h2 id="修改配置文件"><a href="#修改配置文件" class="headerlink" title="修改配置文件"></a>修改配置文件</h2><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">lg@lg:~$ <span class="hljs-built_in">cd</span> /etc/netplan/<br>lg@lg:/etc/netplan$ ll<br>total 12<br>drwxr-xr-x  2 root root 4096 Dec 13 15:56 ./<br>drwxr-xr-x 98 root root 4096 Dec 13 15:32 ../<br>-rw-r--r--  1 root root  301 Dec 13 15:56 00-installer-config.yaml<br></code></pre></td></tr></table></figure>
<p>在 <code>/etc/netplan</code> 目录下，看到有一个 <code>00-installer-config.yaml</code> 文件，配置静态 <code>ip</code> 就是要修改这个文件</p>
<p>删除这个文件内容，然后将下面内容复制到文件中, 需要</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><code class="hljs yaml"><span class="hljs-attr">network:</span><br>  <span class="hljs-attr">ethernets:</span><br>    <span class="hljs-comment"># 这里的 ens33 是网卡名称，就是第一步中使用 ip a 查看的结果</span><br>    <span class="hljs-attr">ens33:</span><br>      <span class="hljs-attr">dhcp4:</span> <span class="hljs-literal">no</span><br>      <span class="hljs-attr">dhcp6:</span> <span class="hljs-literal">no</span><br>      <span class="hljs-attr">addresses:</span><br>        <span class="hljs-comment"># 这里的 192.168.119.129 是静态 ip 地址，对应的网段是根据网关来设置的，网关地址是在 vmware中查看</span><br>        <span class="hljs-bullet">-</span> <span class="hljs-number">192.168</span><span class="hljs-number">.119</span><span class="hljs-number">.129</span><span class="hljs-string">/24</span><br>      <span class="hljs-attr">routes:</span><br>        <span class="hljs-bullet">-</span> <span class="hljs-attr">to:</span> <span class="hljs-string">default</span><br>          <span class="hljs-comment"># 这个是网关的地址，需要到 vmware 中去查看</span><br>          <span class="hljs-attr">via:</span> <span class="hljs-number">192.168</span><span class="hljs-number">.119</span><span class="hljs-number">.2</span><br>      <span class="hljs-attr">nameservers:</span><br>        <span class="hljs-attr">addresses:</span><br>          <span class="hljs-bullet">-</span> <span class="hljs-number">180.76</span><span class="hljs-number">.76</span><span class="hljs-number">.76</span><br>          <span class="hljs-bullet">-</span> <span class="hljs-number">114.114</span><span class="hljs-number">.114</span><span class="hljs-number">.114</span><br>  <span class="hljs-attr">version:</span> <span class="hljs-number">2</span><br>  <span class="hljs-attr">renderer:</span> <span class="hljs-string">networkd</span><br></code></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>Linux</category>
      </categories>
      <tags>
        <tag>Linux</tag>
      </tags>
  </entry>
  <entry>
    <title>ArrayList中出现ConcurrentModificationException异常详解</title>
    <url>/2023/0dae6ecb7b1d/index.html</url>
    <content><![CDATA[<h2 id="出现-ConcurrentModificationException-异常的代码"><a href="#出现-ConcurrentModificationException-异常的代码" class="headerlink" title="出现 ConcurrentModificationException 异常的代码"></a>出现 ConcurrentModificationException 异常的代码</h2><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">TestArrayList</span> &#123;<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">main</span><span class="hljs-params">(String[] args)</span> &#123;<br>        List&lt;String&gt; list = <span class="hljs-keyword">new</span> <span class="hljs-title class_">ArrayList</span>&lt;&gt;();<br>        <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> <span class="hljs-variable">i</span> <span class="hljs-operator">=</span> <span class="hljs-number">0</span>; i &lt; <span class="hljs-number">1000</span>; i++) &#123;<br>            list.add(i + <span class="hljs-string">&quot;&quot;</span>);<br>        &#125;<br><br>        <span class="hljs-comment">// 第一个线程遍历 List</span><br>        <span class="hljs-keyword">new</span> <span class="hljs-title class_">Thread</span>(()-&gt;&#123;<br>            <span class="hljs-keyword">for</span> (String s : list) &#123;<br>                System.out.println(s);<br>            &#125;<br>        &#125;).start();<br><br>        <span class="hljs-comment">// 第一个线程往 List 中添加数据</span><br>        <span class="hljs-keyword">new</span> <span class="hljs-title class_">Thread</span>(()-&gt;&#123;<br>            <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> <span class="hljs-variable">i</span> <span class="hljs-operator">=</span> <span class="hljs-number">0</span>; i &lt; <span class="hljs-number">100</span>; i++) &#123;<br>                list.add(i+<span class="hljs-string">&quot;1&quot;</span>);<br>            &#125;<br>        &#125;).start();<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<p>上述代码 <strong>有可能</strong> 会出现下面的错误</p>
<figure class="highlight stylus"><table><tr><td class="code"><pre><code class="hljs stylus"><span class="hljs-number">739</span><br><span class="hljs-number">740</span><br>Exception <span class="hljs-keyword">in</span> thread <span class="hljs-string">&quot;Thread-0&quot;</span> java<span class="hljs-selector-class">.util</span><span class="hljs-selector-class">.ConcurrentModificationException</span><br>	at java.base/java<span class="hljs-selector-class">.util</span>.ArrayList<span class="hljs-variable">$Itr</span><span class="hljs-selector-class">.checkForComodification</span>(ArrayList<span class="hljs-selector-class">.java</span>:<span class="hljs-number">1013</span>)<br>	at java.base/java<span class="hljs-selector-class">.util</span>.ArrayList<span class="hljs-variable">$Itr</span><span class="hljs-selector-class">.next</span>(ArrayList<span class="hljs-selector-class">.java</span>:<span class="hljs-number">967</span>)<br>	at org<span class="hljs-selector-class">.example</span><span class="hljs-selector-class">.collect</span><span class="hljs-selector-class">.TestArrayList</span>.lambda<span class="hljs-variable">$main</span>$<span class="hljs-number">0</span>(TestArrayList<span class="hljs-selector-class">.java</span>:<span class="hljs-number">14</span>)<br>	at java.base/java<span class="hljs-selector-class">.lang</span><span class="hljs-selector-class">.Thread</span><span class="hljs-selector-class">.run</span>(Thread<span class="hljs-selector-class">.java</span>:<span class="hljs-number">842</span>)<br></code></pre></td></tr></table></figure>

<p>这个 <code>Thread-0</code> 就是遍历 <code>List</code> 的线程，那么为什么会出现这个问题呢？</p>
<p>上面的错误已经给出了调用栈，最初是在 <code>TestArrayList.lambda$main$0(TestArrayList.java:14)</code> 这行代码触发的，实际就是 <code>for (String s : list) &#123;</code> 这一行代码，这行代码会调用 <code>ArrayList$Itr.next</code>， 这是为什么呢？ </p>
<p>其实 <code>foreach</code> 语法就是一个语法糖(语法糖就是在语法上做了一些优化，便于程序员书写代码，但是并没有改变实际的功能)，可以在 <code>idea</code> 中通过 <code>jclasslib</code> 插件来编译看看</p>
<p><img src="https://blog-1308475102.cos.ap-guangzhou.myqcloud.com/picture/202312202106893.webp" alt="编译源代码"></p>
<p>可以看到 <code>foreach</code> 形式的遍历其实就是使用 <code>iterator</code>, 然后不断的调用 <code>hasNext</code> 和 <code>next</code> 方法，而上面错误提示中也提示了是在调用 <code>next</code> 方法时出现的错误，我们就看一下 <code>Iterator.next</code> 方法</p>
<p>下面的 <code>Itr</code> 类是 <code>ArrayList</code> 类的一个内部类</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">private</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">Itr</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">Iterator</span>&lt;E&gt; &#123;<br>    <span class="hljs-type">int</span> cursor;       <span class="hljs-comment">// index of next element to return</span><br>    <span class="hljs-type">int</span> <span class="hljs-variable">lastRet</span> <span class="hljs-operator">=</span> -<span class="hljs-number">1</span>; <span class="hljs-comment">// index of last element returned; -1 if no such</span><br>    <span class="hljs-type">int</span> <span class="hljs-variable">expectedModCount</span> <span class="hljs-operator">=</span> modCount;<br><br>    <span class="hljs-comment">// prevent creating a synthetic constructor</span><br>    Itr() &#123;&#125;<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-type">boolean</span> <span class="hljs-title function_">hasNext</span><span class="hljs-params">()</span> &#123;<br>        <span class="hljs-keyword">return</span> cursor != size;<br>    &#125;<br><br>    <span class="hljs-meta">@SuppressWarnings(&quot;unchecked&quot;)</span><br>    <span class="hljs-keyword">public</span> E <span class="hljs-title function_">next</span><span class="hljs-params">()</span> &#123;<br>        <span class="hljs-comment">// 在调用 checkForComodification 方法的时候可能会抛出异常</span><br>        checkForComodification();<br>        <span class="hljs-type">int</span> <span class="hljs-variable">i</span> <span class="hljs-operator">=</span> cursor;<br>        <span class="hljs-keyword">if</span> (i &gt;= size)<br>            <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">NoSuchElementException</span>();<br>        Object[] elementData = ArrayList.<span class="hljs-built_in">this</span>.elementData;<br>        <span class="hljs-keyword">if</span> (i &gt;= elementData.length)<br>            <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">ConcurrentModificationException</span>();<br>        cursor = i + <span class="hljs-number">1</span>;<br>        <span class="hljs-keyword">return</span> (E) elementData[lastRet = i];<br>    &#125;<br><br>    <span class="hljs-keyword">final</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">checkForComodification</span><span class="hljs-params">()</span> &#123;<br>        <span class="hljs-comment">// 当 modCount 不等于 expectedModCount 时就会抛出 ConcurrentModificationException 异常</span><br>        <span class="hljs-keyword">if</span> (modCount != expectedModCount)<br>            <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">ConcurrentModificationException</span>();<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<h2 id="ConcurrentModificationException-异常源码分析"><a href="#ConcurrentModificationException-异常源码分析" class="headerlink" title="ConcurrentModificationException 异常源码分析"></a>ConcurrentModificationException 异常源码分析</h2><p>前面看 <code>Itr</code> 类的时候已经看到了几个变量</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-type">int</span> cursor;       <span class="hljs-comment">// index of next element to return</span><br><span class="hljs-type">int</span> <span class="hljs-variable">lastRet</span> <span class="hljs-operator">=</span> -<span class="hljs-number">1</span>; <span class="hljs-comment">// index of last element returned; -1 if no such</span><br><span class="hljs-type">int</span> <span class="hljs-variable">expectedModCount</span> <span class="hljs-operator">=</span> modCount;<br></code></pre></td></tr></table></figure>

<ul>
<li><code>cursor</code>: 返回的下一个元素的下标，注意这里说的是下一个元素，而不是当前返回的元素</li>
<li><code>lastRet</code>: 最后返回的元素的下标，-1 表示没有返回过元素</li>
<li><code>expectedModCount</code>: 迭代器修改元素的次数，当 <code>modCount</code> 不等于 <code>expectedModCount</code> 时就会抛出 <code>ConcurrentModificationException</code> 异常，这说明除了当前迭代器还有其他线程在修改这个集合</li>
<li><code>modCount</code>： 这个不是迭代器中的变量，而是 <code>AbstracList</code> 中的变量，表示集合被修改的次数</li>
</ul>
<h3 id="modCount-变量的作用"><a href="#modCount-变量的作用" class="headerlink" title="modCount 变量的作用"></a>modCount 变量的作用</h3><p><code>modCount</code> 变量的是用来统计集合被修改的次数， 当执行 <code>ArrayList</code> 中的 <code>add()</code>,<code>remove()</code>,<code>addAll()</code> 等方法时，都会执行 <code>modCount++</code>, 代表集合的元素被修改了</p>
<h3 id="expectedModCount-的作用"><a href="#expectedModCount-的作用" class="headerlink" title="expectedModCount 的作用"></a>expectedModCount 的作用</h3><p><code>expectedModCount</code> 是迭代器中的变量，当初始化一个迭代器时，<code>expectedModCount=modCount</code>, 只有在执行 <strong>迭代器</strong>的 <code>remove()</code> 方法时，会对 <code>expectedModCount</code> 进行重新设置值，其实从这里也能看出来当想要在循环中对 <code>ArrayList</code> 进行删除时，为什么要通过迭代器来实现，因为迭代器的 <code>remove()</code> 方法会修改 <code>expectedModCount</code></p>
<p>总结：经过上面的分析也知道了 <code>ConcurrentModificationException</code> 异常出现的原因，就是在多线程操作 <code>ArrayList</code> 时，一个线程在通过 <code>Iterator</code> 遍历，其他线程对集合进行了修改（新增，删除元素），导致 <code>expectedModCount != modCount</code></p>
<h2 id="解决办法"><a href="#解决办法" class="headerlink" title="解决办法"></a>解决办法</h2><p>还是以最初的代码为例，如果就是有一个线程会遍历，一个线程会修改集合，那么应该如何解决这个问题呢？<br>可以使用以下几种方式</p>
<ul>
<li>加锁（无论是 <code>synchronized</code> 还是 <code>lock</code> ）, 对集合的 <strong>读</strong> 和 <strong>写</strong> 加上同一把锁</li>
<li>使用 <code>CopyOnWriteArrayList</code> 类</li>
<li>借用 <code>CopyOnWriteArrayList</code> 思想，在使用 <code>foreach</code> 遍历的时候先拷贝一份集合，比如 <code>List&lt;String&gt; list2 = new ArrayList&lt;&gt;(list);</code> 然后使用 <code>list2</code> 去遍历</li>
</ul>
]]></content>
      <categories>
        <category>java</category>
        <category>java基础</category>
      </categories>
      <tags>
        <tag>java</tag>
      </tags>
  </entry>
  <entry>
    <title>HashMap详解</title>
    <url>/2023/834224a16039/index.html</url>
    <content><![CDATA[<h2 id="HashMap的底层数据结构是什么？"><a href="#HashMap的底层数据结构是什么？" class="headerlink" title="HashMap的底层数据结构是什么？"></a>HashMap的底层数据结构是什么？</h2><ol>
<li>之前 <code>Jdk1.7</code> 的存储结构是数组+链表，到了 <code>Jdk1.8</code> 变成了数组+链表+红黑树。</li>
</ol>
<h2 id="HashMap-是如何定位索引下标的"><a href="#HashMap-是如何定位索引下标的" class="headerlink" title="HashMap 是如何定位索引下标的"></a>HashMap 是如何定位索引下标的</h2><p>定位 <code>index</code> 分成两步</p>
<ol>
<li><p>根据 hash 函数获取 hash 值</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-type">int</span> <span class="hljs-title function_">hash</span><span class="hljs-params">(Object key)</span> &#123;<br>    <span class="hljs-type">int</span> h;<br>    <span class="hljs-keyword">return</span> (key == <span class="hljs-literal">null</span>) ? <span class="hljs-number">0</span> : (h = key.hashCode()) ^ (h &gt;&gt;&gt; <span class="hljs-number">16</span>);<br>&#125;<br></code></pre></td></tr></table></figure>
<p><strong>可以看到上面代码获取到 hashCode 之后还右移 16 位，具体原因在下面分析</strong></p>
</li>
<li><p>根据第一步计算的 <code>hash</code> 值获取数组位置（一般是对数组长度取模，<code>HashMap</code> 中是对长度 -1 进行 <code>&amp;</code> 运算）</p>
</li>
</ol>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">final</span> V <span class="hljs-title function_">putVal</span><span class="hljs-params">(<span class="hljs-type">int</span> hash, K key, V value, <span class="hljs-type">boolean</span> onlyIfAbsent,</span><br><span class="hljs-params">                   <span class="hljs-type">boolean</span> evict)</span> &#123;<br>    Node&lt;K,V&gt;[] tab; Node&lt;K,V&gt; p; <span class="hljs-type">int</span> n, i;<br>    <span class="hljs-keyword">if</span> ((tab = table) == <span class="hljs-literal">null</span> || (n = tab.length) == <span class="hljs-number">0</span>)<br>        n = (tab = resize()).length;<br>        <span class="hljs-comment">// 获取下标的操作是  i = (n-1) &amp; hash</span><br>    <span class="hljs-keyword">if</span> ((p = tab[i = (n - <span class="hljs-number">1</span>) &amp; hash]) == <span class="hljs-literal">null</span>)<br>        tab[i] = newNode(hash, key, value, <span class="hljs-literal">null</span>);<br>    <span class="hljs-keyword">else</span> &#123;<br>       ...<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>
<p><strong>获取下标为什么是对 n-1 进行与操作而不是取模？具体原因下面分析</strong></p>
<h2 id="为什么需要对key的-hash-值右移-16位"><a href="#为什么需要对key的-hash-值右移-16位" class="headerlink" title="为什么需要对key的 hash 值右移 16位"></a>为什么需要对key的 hash 值右移 16位</h2><figure class="highlight yaml"><table><tr><td class="code"><pre><code class="hljs yaml"><span class="hljs-string">HashMap</span> <span class="hljs-string">中使用的</span> <span class="hljs-string">hash</span> <span class="hljs-string">函数是</span> <span class="hljs-string">key.hash</span> <span class="hljs-string">^</span> <span class="hljs-string">(key.hash</span> <span class="hljs-string">&gt;&gt;</span> <span class="hljs-number">16</span><span class="hljs-string">)</span>  <span class="hljs-string">而不是直接使用</span> <span class="hljs-string">key.hash,</span> <span class="hljs-string">这么做的原因是增加了扰动计算，使得</span> <span class="hljs-string">hash</span> <span class="hljs-string">分布的尽可能的均匀，</span> <span class="hljs-string">让元素的高16</span> <span class="hljs-string">位和低16</span> <span class="hljs-string">位都参与运算</span><br><span class="hljs-string">示例：</span><br><span class="hljs-string">比如有一个元素1换成二进制如下</span><br><span class="hljs-number">1111 </span><span class="hljs-number">1111 </span><span class="hljs-number">1111 </span><span class="hljs-number">1111 </span><span class="hljs-number">1010 </span><span class="hljs-number">1010 </span><span class="hljs-number">1010 </span><span class="hljs-number">1010</span> <span class="hljs-string">（没有右移16位的二进制）</span><br><span class="hljs-number">0000 </span><span class="hljs-number">0000 </span><span class="hljs-number">0000 </span><span class="hljs-number">0000 </span><span class="hljs-number">1111 </span><span class="hljs-number">1111 </span><span class="hljs-number">1111 </span><span class="hljs-number">1111</span>  <span class="hljs-string">（右移16位的二进制）</span><br><br><span class="hljs-string">另外一个元素2换成二进制如下</span><br><span class="hljs-number">1111 </span><span class="hljs-number">1111 </span><span class="hljs-number">1111 </span><span class="hljs-number">1110 </span><span class="hljs-number">1010 </span><span class="hljs-number">1010 </span><span class="hljs-number">1010 </span><span class="hljs-number">1010</span> <span class="hljs-string">（没有右移16位的二进制）</span><br><span class="hljs-number">0000 </span><span class="hljs-number">0000 </span><span class="hljs-number">0000 </span><span class="hljs-number">0000 </span><span class="hljs-number">1111 </span><span class="hljs-number">1111 </span><span class="hljs-number">1111 </span><span class="hljs-number">1110</span>  <span class="hljs-string">（右移16位的二进制）</span><br><br><span class="hljs-string">在计算数组位置的时候使用的是</span> <span class="hljs-string">hash</span> <span class="hljs-string">&amp;</span> <span class="hljs-string">(n</span> <span class="hljs-number">-1</span><span class="hljs-string">),</span>  <span class="hljs-string">要知道</span> <span class="hljs-string">n-1</span> <span class="hljs-string">肯定是一个相对来说比较小的数字（32个bit</span> <span class="hljs-string">只会使用比较少的低位），</span> <span class="hljs-string">比如长度是</span> <span class="hljs-number">32</span><span class="hljs-string">，</span> <span class="hljs-string">对应的二进制是</span> <span class="hljs-number">0000 </span><span class="hljs-number">0000 </span><span class="hljs-number">0000 </span><span class="hljs-number">0000 </span><span class="hljs-number">0000 </span><span class="hljs-number">0000 </span><span class="hljs-number">0001 </span><span class="hljs-number">1111</span><br><br><span class="hljs-string">使用没有右移的元素1和</span> <span class="hljs-string">（32</span> <span class="hljs-number">-1</span><span class="hljs-string">）</span> <span class="hljs-string">进行与操作（与操作是都为1才为1，否则为0）</span><br><span class="hljs-number">1111 </span><span class="hljs-number">1111 </span><span class="hljs-number">1111 </span><span class="hljs-number">1111     </span><span class="hljs-number">1010 </span><span class="hljs-number">1010 </span><span class="hljs-number">1010 </span><span class="hljs-number">1010</span> <span class="hljs-string">（没有右移16位的二进制）</span><br><span class="hljs-number">0000 </span><span class="hljs-number">0000 </span><span class="hljs-number">0000 </span><span class="hljs-number">0000      </span><span class="hljs-number">0000 </span><span class="hljs-number">0000 </span><span class="hljs-number">0001 </span><span class="hljs-number">1111</span><br><span class="hljs-number">0000 </span><span class="hljs-number">0000 </span><span class="hljs-number">0000 </span><span class="hljs-number">0000      </span><span class="hljs-number">0000 </span><span class="hljs-number">0000 </span><span class="hljs-number">0000 </span><span class="hljs-number">1010</span><br><br><span class="hljs-string">使用没有右移的元素2和</span> <span class="hljs-string">（32</span> <span class="hljs-number">-1</span><span class="hljs-string">）</span> <span class="hljs-string">进行与操作（与操作是都为1才为1，否则为0）</span><br><span class="hljs-number">1111 </span><span class="hljs-number">1111 </span><span class="hljs-number">1111 </span><span class="hljs-number">1100     </span><span class="hljs-number">1010 </span><span class="hljs-number">1010 </span><span class="hljs-number">1010 </span><span class="hljs-number">1010</span> <span class="hljs-string">（没有右移16位的二进制）</span><br><span class="hljs-number">0000 </span><span class="hljs-number">0000 </span><span class="hljs-number">0000 </span><span class="hljs-number">0000      </span><span class="hljs-number">0000 </span><span class="hljs-number">0000 </span><span class="hljs-number">0001 </span><span class="hljs-number">1111</span><br><span class="hljs-number">0000 </span><span class="hljs-number">0000 </span><span class="hljs-number">0000 </span><span class="hljs-number">0000      </span><span class="hljs-number">0000 </span><span class="hljs-number">0000 </span><span class="hljs-number">0000 </span><span class="hljs-number">1010</span><br><span class="hljs-string">可以看到元素1和元素2计算出来下标是相同的，这个不同是因为元素1和元素2的低16位是完全相同的，不同之处只在于高16位，但是当我们对</span> <span class="hljs-string">（n-1）进行与操作的时候，由于</span> <span class="hljs-string">n-1</span> <span class="hljs-string">的值比较小，绝大部分情况下，只有低16位会进行运算的</span><br><br><span class="hljs-string">接下来再看一下如果使用右移的hash进行计算会有什么不同</span><br><span class="hljs-string">使用右移的元素1和</span> <span class="hljs-string">（32</span> <span class="hljs-number">-1</span><span class="hljs-string">）</span> <span class="hljs-string">进行与操作（与操作是都为1才为1，否则为0）</span><br><span class="hljs-number">0000 </span><span class="hljs-number">0000 </span><span class="hljs-number">0000 </span><span class="hljs-number">0000      </span><span class="hljs-number">1111 </span><span class="hljs-number">1111 </span><span class="hljs-number">1111 </span><span class="hljs-number">1111</span> <span class="hljs-string">（没有右移16位的二进制）</span><br><span class="hljs-number">0000 </span><span class="hljs-number">0000 </span><span class="hljs-number">0000 </span><span class="hljs-number">0000      </span><span class="hljs-number">0000 </span><span class="hljs-number">0000 </span><span class="hljs-number">0001 </span><span class="hljs-number">1111</span><br><span class="hljs-number">0000 </span><span class="hljs-number">0000 </span><span class="hljs-number">0000 </span><span class="hljs-number">0000      </span><span class="hljs-number">0000 </span><span class="hljs-number">0000 </span><span class="hljs-number">0000  </span><span class="hljs-number">1111</span><br><br><span class="hljs-string">用右移的元素2和</span> <span class="hljs-string">（32</span> <span class="hljs-number">-1</span><span class="hljs-string">）</span> <span class="hljs-string">进行与操作（与操作是都为1才为1，否则为0）</span><br><span class="hljs-number">0000 </span><span class="hljs-number">0000 </span><span class="hljs-number">0000 </span><span class="hljs-number">0000      </span><span class="hljs-number">1111 </span><span class="hljs-number">1111 </span><span class="hljs-number">1111 </span><span class="hljs-number">1110</span> <span class="hljs-string">（没有右移16位的二进制）</span><br><span class="hljs-number">0000 </span><span class="hljs-number">0000 </span><span class="hljs-number">0000 </span><span class="hljs-number">0000      </span><span class="hljs-number">0000 </span><span class="hljs-number">0000 </span><span class="hljs-number">0001 </span><span class="hljs-number">1111</span><br><span class="hljs-number">0000 </span><span class="hljs-number">0000 </span><span class="hljs-number">0000 </span><span class="hljs-number">0000      </span><span class="hljs-number">0000 </span><span class="hljs-number">0000 </span><span class="hljs-number">0000  </span><span class="hljs-number">1110</span><br><span class="hljs-string">可以看到计算出来的</span> <span class="hljs-string">index</span> <span class="hljs-string">值是不同的，这就是右移带来的让高16位有参与运算的效果</span><br></code></pre></td></tr></table></figure>

<h2 id="为什么不对长度取模而是使用与运算"><a href="#为什么不对长度取模而是使用与运算" class="headerlink" title="为什么不对长度取模而是使用与运算"></a>为什么不对长度取模而是使用与运算</h2><ol>
<li><code>X % 2^n = X &amp; (2^n – 1)</code>  这两种运算等价，所以这也是 <code>HashMap</code> 长度取 <code>2^n</code> 的原因<br>也可以从数据角度记住，当 <code>n</code> 是 2 的倍数时， <code>X % 2^n = X &amp; (2^n – 1)</code>  等价</li>
</ol>
<p><a href="https://www.cnblogs.com/ysocean/p/9054804.html">公式计算参考链接</a></p>
<h2 id="HashMap是如何实现扩容的"><a href="#HashMap是如何实现扩容的" class="headerlink" title="HashMap是如何实现扩容的"></a>HashMap是如何实现扩容的</h2><p>扩容后索引其实也是使用 <code>hash &amp; （newLength -1）</code>, 这样计算出来的位置要是和原来的元素在相同的位置，要么是 原来的位置+旧数组长度&#x3D;新索引位置</p>
<h2 id="HashMap是如何解决hash冲突的"><a href="#HashMap是如何解决hash冲突的" class="headerlink" title="HashMap是如何解决hash冲突的"></a>HashMap是如何解决hash冲突的</h2><ol>
<li>对 <code>hash</code> 的计算进行优化，不是直接使用 <code>hashcode</code>,  而是右移16位，让高16位也参与运算，减少<code>hash</code> 冲突</li>
<li>如果遇到冲突，使用拉链法解决</li>
</ol>
]]></content>
      <categories>
        <category>java</category>
        <category>java基础</category>
      </categories>
      <tags>
        <tag>java</tag>
      </tags>
  </entry>
  <entry>
    <title>JDK动态代理详解</title>
    <url>/2023/92222e5bba3e/index.html</url>
    <content><![CDATA[<blockquote>
<p>JDK版本： 17</p>
</blockquote>
<h2 id="动态代理示例"><a href="#动态代理示例" class="headerlink" title="动态代理示例"></a>动态代理示例</h2><p>示例代码包含</p>
<ol>
<li><p><code>IUser</code> 接口</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">interface</span> <span class="hljs-title class_">IUser</span> &#123;<br>    <span class="hljs-keyword">void</span> <span class="hljs-title function_">sayHello</span><span class="hljs-params">()</span>;<br>&#125;<br></code></pre></td></tr></table></figure>
</li>
<li><p><code>IUser</code> 接口实现类</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">IUserImpl</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">IUser</span> &#123;<br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">sayHello</span><span class="hljs-params">()</span> &#123;<br>        System.out.println(<span class="hljs-string">&quot;default hello&quot;</span>);<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>
</li>
<li><p>实现了 <code>InvocationHandler</code> 接口的类</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">CusInvocationHandler</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">InvocationHandler</span> &#123;<br>    <span class="hljs-keyword">private</span> IUser target;<br>    <span class="hljs-keyword">public</span> <span class="hljs-title function_">CusInvocationHandler</span><span class="hljs-params">(IUser target)</span> &#123;<br>        <span class="hljs-built_in">this</span>.target = target;<br>    &#125;<br><br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> Object <span class="hljs-title function_">invoke</span><span class="hljs-params">(Object proxy, Method method, Object[] args)</span> <span class="hljs-keyword">throws</span> Throwable &#123;<br>        System.out.println(<span class="hljs-string">&quot;目标方法执行前&quot;</span>);<br>        method.invoke(target, args);<br>        System.out.println(<span class="hljs-string">&quot;目标方法执行后&quot;</span>);<br>        <span class="hljs-keyword">return</span> <span class="hljs-literal">null</span>;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>
</li>
<li><p>测试类</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">TestProxy</span> &#123;<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">main</span><span class="hljs-params">(String[] args)</span> &#123;<br>        <span class="hljs-type">IUser</span> <span class="hljs-variable">user</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">IUserImpl</span>();<br>        <span class="hljs-type">IUser</span> <span class="hljs-variable">proxyInstance</span> <span class="hljs-operator">=</span> (IUser)Proxy.newProxyInstance(user.getClass().getClassLoader(), user.getClass().getInterfaces(), <span class="hljs-keyword">new</span> <span class="hljs-title class_">CusInvocationHandler</span>(user));<br>        <span class="hljs-type">TestProxy</span> <span class="hljs-variable">testProxy</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">TestProxy</span>();<br>        proxyInstance.sayHello();<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure></li>
<li><p>测试结果</p>
<figure class="highlight actionscript"><table><tr><td class="code"><pre><code class="hljs actionscript">目标方法执行前<br><span class="hljs-keyword">default</span> hello<br>目标方法执行后<br></code></pre></td></tr></table></figure></li>
</ol>
<p>如果想要查看生成的代理类代码，可以参考</p>
<ol><li><a href="/2023/d513f5c497bd/index.html" title="java输出运行过程中的代理类">java输出运行过程中的代理类</a></li></ol>

<p>下面给出上面示例代码中生成的代理类(省略其他 <code>hashcode()</code> 等其他无关代码 )</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">final</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">$Proxy0</span> <span class="hljs-keyword">extends</span> <span class="hljs-title class_">Proxy</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">IUser</span> &#123;<br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> Method m0;<br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> Method m1;<br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> Method m2;<br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> Method m3;<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">final</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">sayHello</span><span class="hljs-params">()</span> &#123;<br>        <span class="hljs-keyword">try</span> &#123;<br>            <span class="hljs-built_in">super</span>.h.invoke(<span class="hljs-built_in">this</span>, m3, (Object[])<span class="hljs-literal">null</span>);<br>        &#125; <span class="hljs-keyword">catch</span> (RuntimeException | Error var2) &#123;<br>            <span class="hljs-keyword">throw</span> var2;<br>        &#125; <span class="hljs-keyword">catch</span> (Throwable var3) &#123;<br>            <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">UndeclaredThrowableException</span>(var3);<br>        &#125;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>
<p>该代理类实现了我们自己的接口 <code>IUser</code> 并且继承了 <code>Proxy</code>，<code>Proxy</code> 类中有属性 <code>InvocationHandler</code></p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">Proxy</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">java</span>.io.Serializable &#123;<br>    <span class="hljs-keyword">protected</span> InvocationHandler h;<br>&#125;<br></code></pre></td></tr></table></figure>

<h2 id="代理类执行流程"><a href="#代理类执行流程" class="headerlink" title="代理类执行流程"></a>代理类执行流程</h2><p>以上面示例代码为例，代理类的执行流程如下：</p>
<pre class="mermaid">sequenceDiagram
    participant A as IUser代理类
    participant B as CusInvocationHandler
    participant C as IUserImpl
    A->>B: 代理类的sayHello()
    B->>C: InvocationHandler的invoke()</pre>

<h2 id="是否一定需要实现类"><a href="#是否一定需要实现类" class="headerlink" title="是否一定需要实现类"></a>是否一定需要实现类</h2><p>代理类最终会执行 <code>InvocationHandler</code> 的 <code>invoke()</code> 方法，而在 <code>invoke</code> 方法中会调用具体接口实现类的方法（通过 <code>method.invoke()</code> 调用）</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">CusInvocationHandler</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">InvocationHandler</span> &#123;<br>    <span class="hljs-comment">// 目标对象可以通用的写 Object</span><br>    <span class="hljs-keyword">private</span> Object target;<br>    <span class="hljs-keyword">public</span> <span class="hljs-title function_">CusInvocationHandler</span><span class="hljs-params">(Object target)</span> &#123;<br>        <span class="hljs-built_in">this</span>.target = target;<br>    &#125;<br><br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> Object <span class="hljs-title function_">invoke</span><span class="hljs-params">(Object proxy, Method method, Object[] args)</span> <span class="hljs-keyword">throws</span> Throwable &#123;<br>        System.out.println(<span class="hljs-string">&quot;目标方法执行前&quot;</span>);<br>        <span class="hljs-comment">// 这里调用目标对象的方法</span><br>        method.invoke(target, args);<br>        System.out.println(<span class="hljs-string">&quot;目标方法执行后&quot;</span>);<br>        <span class="hljs-keyword">return</span> <span class="hljs-literal">null</span>;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>
<p>那么是否一定需要这个实现类呢？其实不是的，一般我们需要实现类是因为完成业务逻辑之外的功能，实现类才是完成主要的业务逻辑，但是如果代理类只需要根据接口里面方法相关的信息(比如接口名，接口中方法参数，接口中方法返回值)就已经知道要怎么做了（可以认为根据约定做事）那可以完全不需要实现类了，比如 <code>Mybatis</code> 框架中的 <code>Mapper</code> 接口就完全需要实现类, 具体可参考</p>
<ol><li><a href="/2023/5cc9f93ce77a/index.html" title="Mybatis获取Mapper代理对象的过程分析">Mybatis获取Mapper代理对象的过程分析</a></li></ol>


<h2 id="一句话总结"><a href="#一句话总结" class="headerlink" title="一句话总结"></a>一句话总结</h2><p><strong>使用 <code>jdk</code> 动态代理，只需要关注实现了 <code>InvocationHandler</code> 接口类的 <code>invoke()</code> 方法就能知道代理类额外做的事情是什么</strong></p>
]]></content>
      <categories>
        <category>java</category>
        <category>java基础</category>
      </categories>
      <tags>
        <tag>java</tag>
      </tags>
  </entry>
  <entry>
    <title>java的io模型</title>
    <url>/2023/180f5b629f58/index.html</url>
    <content><![CDATA[<span class="hide_first_mermaid">
<div class="mermaid-wrap"><pre class="mermaid-src" hidden>
  flowchart LR
    A
  </pre></div>
</span>

<h2 id="BIO"><a href="#BIO" class="headerlink" title="BIO"></a>BIO</h2><ol>
<li><code>bio</code> 是同步阻塞 <code>io</code>，每一个连接都需要一个线程去处理</li>
<li><code>bio</code> 会有资源浪费问题，一个线程只能处理一个连接，如果某个客户端并没有发送数据，对应的服务端线程也只能阻塞</li>
</ol>
<span id="more"></span>
<h3 id="阻塞的含义"><a href="#阻塞的含义" class="headerlink" title="阻塞的含义"></a>阻塞的含义</h3><ol>
<li>服务端 <code>ServerSocket</code> 调用 <code>accept()</code> 方法会阻塞（如果没有客户端进行连接，调用这个方法后不会继续往下执行）</li>
<li>线程从Socket的输入流读入数据时,如果输入流没有足够的数据，就会进入阻塞状态</li>
</ol>
<h3 id="java-bio-代码实现"><a href="#java-bio-代码实现" class="headerlink" title="java bio 代码实现"></a>java bio 代码实现</h3><p><strong>服务端代码</strong></p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">package</span> org.example.demo.socket;<br><br><span class="hljs-keyword">import</span> java.io.IOException;<br><span class="hljs-keyword">import</span> java.net.ServerSocket;<br><span class="hljs-keyword">import</span> java.net.Socket;<br><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">CusServerSocket</span> &#123;<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">main</span><span class="hljs-params">(String[] args)</span> &#123;<br>        <span class="hljs-keyword">try</span> (<span class="hljs-type">ServerSocket</span> <span class="hljs-variable">serverSocket</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">ServerSocket</span>(<span class="hljs-number">8080</span>);) &#123;<br>            <span class="hljs-keyword">while</span> (<span class="hljs-literal">true</span>) &#123;<br>                <span class="hljs-type">Socket</span> <span class="hljs-variable">socket</span> <span class="hljs-operator">=</span> serverSocket.accept();<br>                <span class="hljs-comment">//连接成功后新开一个线程去处理这个连接</span><br>                <span class="hljs-keyword">new</span> <span class="hljs-title class_">Thread</span>(<span class="hljs-keyword">new</span> <span class="hljs-title class_">Runnable</span>() &#123;<br>                    <span class="hljs-meta">@Override</span><br>                    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">run</span><span class="hljs-params">()</span> &#123;<br>                        <span class="hljs-type">byte</span>[] bytes = <span class="hljs-keyword">new</span> <span class="hljs-title class_">byte</span>[<span class="hljs-number">1024</span>];<br>                        <span class="hljs-type">int</span> <span class="hljs-variable">length</span> <span class="hljs-operator">=</span> -<span class="hljs-number">1</span>;<br>                        <span class="hljs-keyword">try</span> &#123;<br>                            <span class="hljs-comment">// read() 方法上的注释 If no byte is available because the stream is at the end of the file, the value -1 is returned</span><br>                            <span class="hljs-keyword">while</span> ((length = socket.getInputStream().read(bytes)) != -<span class="hljs-number">1</span>) &#123;<br>                                System.out.println(<span class="hljs-string">&quot;thredId:&quot;</span> + Thread.currentThread().getName() + <span class="hljs-keyword">new</span> <span class="hljs-title class_">String</span>(bytes, <span class="hljs-number">0</span>, length));<br>                            &#125;<br>                        &#125; <span class="hljs-keyword">catch</span> (IOException e) &#123;<br>                            e.printStackTrace();<br>                        &#125;<br>                        System.out.println(<span class="hljs-string">&quot;客户端关闭, 当前线程:&quot;</span> + Thread.currentThread().getName());<br>                    &#125;<br>                &#125;).start();<br>            &#125;<br>        &#125; <span class="hljs-keyword">catch</span> (<br>                IOException e) &#123;<br>            e.printStackTrace();<br>        &#125;<br>    &#125;<br>&#125;<br><br></code></pre></td></tr></table></figure>
<p><strong>客户端实现</strong></p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">package</span> org.example.demo.socket;<br><br><span class="hljs-keyword">import</span> java.io.IOException;<br><span class="hljs-keyword">import</span> java.net.Socket;<br><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">CusClientSocket</span> &#123;<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">main</span><span class="hljs-params">(String[] args)</span> &#123;<br>        <span class="hljs-comment">// 外层循环会创建多个 socket，用来模拟多个 socket 客户端</span><br>        <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> <span class="hljs-variable">j</span> <span class="hljs-operator">=</span> <span class="hljs-number">0</span>; j &lt; <span class="hljs-number">3</span>; j++) &#123;<br>            <span class="hljs-keyword">try</span> (<span class="hljs-type">Socket</span> <span class="hljs-variable">socket</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Socket</span>(<span class="hljs-string">&quot;127.0.0.1&quot;</span>,<span class="hljs-number">8080</span>)) &#123;<br>                <span class="hljs-comment">// 内存循环用来模拟一个客户端的多次通信</span><br>                <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> <span class="hljs-variable">i</span> <span class="hljs-operator">=</span> <span class="hljs-number">1</span>; i &lt; <span class="hljs-number">3</span>; i++) &#123;<br>                    Thread.sleep(<span class="hljs-number">20</span>);<br>                    socket.getOutputStream().write((<span class="hljs-string">&quot;客户端&quot;</span> + j + <span class="hljs-string">&quot;第&quot;</span> + i + <span class="hljs-string">&quot;次通信&quot;</span>).getBytes());<br>                &#125;<br>            &#125; <span class="hljs-keyword">catch</span> (IOException | InterruptedException e) &#123;<br>                <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">RuntimeException</span>(e);<br>            &#125;<br>        &#125;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>
<p><strong>执行结果</strong></p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java">thredId:Thread-<span class="hljs-number">0</span>客户端<span class="hljs-number">0</span>第<span class="hljs-number">1</span>次通信<br>thredId:Thread-<span class="hljs-number">0</span>客户端<span class="hljs-number">0</span>第<span class="hljs-number">2</span>次通信<br>客户端关闭, 当前线程:Thread-<span class="hljs-number">0</span><br>thredId:Thread-<span class="hljs-number">1</span>客户端<span class="hljs-number">1</span>第<span class="hljs-number">1</span>次通信<br>thredId:Thread-<span class="hljs-number">1</span>客户端<span class="hljs-number">1</span>第<span class="hljs-number">2</span>次通信<br>客户端关闭, 当前线程:Thread-<span class="hljs-number">1</span><br>thredId:Thread-<span class="hljs-number">2</span>客户端<span class="hljs-number">2</span>第<span class="hljs-number">1</span>次通信<br>thredId:Thread-<span class="hljs-number">2</span>客户端<span class="hljs-number">2</span>第<span class="hljs-number">2</span>次通信<br>客户端关闭, 当前线程:Thread-<span class="hljs-number">2</span><br></code></pre></td></tr></table></figure>
<ol>
<li>执行完成之后，服务端程序并不会关闭，会一直在运行</li>
<li>从上面结果看，一共创建了三个线程来执行</li>
</ol>
<h3 id="使用线程池优化-bio-实现"><a href="#使用线程池优化-bio-实现" class="headerlink" title="使用线程池优化 bio 实现"></a>使用线程池优化 bio 实现</h3><p>客户端代码不需要改动，只需要更改服务端代码</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">package</span> org.example.demo.socket;<br><br><span class="hljs-keyword">import</span> java.io.IOException;<br><span class="hljs-keyword">import</span> java.net.ServerSocket;<br><span class="hljs-keyword">import</span> java.net.Socket;<br><span class="hljs-keyword">import</span> java.util.concurrent.ExecutorService;<br><span class="hljs-keyword">import</span> java.util.concurrent.Executors;<br><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">CusServerSocket</span> &#123;<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">main</span><span class="hljs-params">(String[] args)</span> &#123;<br>        <span class="hljs-type">ExecutorService</span> <span class="hljs-variable">executorService</span> <span class="hljs-operator">=</span> Executors.newSingleThreadExecutor();<br>        <span class="hljs-keyword">try</span> (<span class="hljs-type">ServerSocket</span> <span class="hljs-variable">serverSocket</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">ServerSocket</span>(<span class="hljs-number">8080</span>);) &#123;<br>            <span class="hljs-keyword">while</span> (<span class="hljs-literal">true</span>) &#123;<br>                <span class="hljs-type">Socket</span> <span class="hljs-variable">socket</span> <span class="hljs-operator">=</span> serverSocket.accept();<br>                <span class="hljs-comment">//连接成功后新开一个线程去处理这个连接</span><br>                executorService.execute(<span class="hljs-keyword">new</span> <span class="hljs-title class_">ClientTask</span>(socket));<br>            &#125;<br>        &#125; <span class="hljs-keyword">catch</span> (<br>                IOException e) &#123;<br>            e.printStackTrace();<br>        &#125;<br>    &#125;<br><br>    <span class="hljs-keyword">static</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">ClientTask</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">Runnable</span> &#123;<br><br>        <span class="hljs-keyword">private</span> Socket socket;<br><br>        <span class="hljs-keyword">public</span> <span class="hljs-title function_">ClientTask</span><span class="hljs-params">(Socket socket)</span> &#123;<br>            <span class="hljs-built_in">this</span>.socket = socket;<br>        &#125;<br><br>        <span class="hljs-meta">@Override</span><br>        <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">run</span><span class="hljs-params">()</span> &#123;<br>            <span class="hljs-type">byte</span>[] bytes = <span class="hljs-keyword">new</span> <span class="hljs-title class_">byte</span>[<span class="hljs-number">1024</span>];<br>            <span class="hljs-type">int</span> <span class="hljs-variable">length</span> <span class="hljs-operator">=</span> -<span class="hljs-number">1</span>;<br>            <span class="hljs-keyword">try</span> &#123;<br>                <span class="hljs-comment">// read() 方法上的注释 If no byte is available because the stream is at the end of the file, the value -1 is returned</span><br>                <span class="hljs-keyword">while</span> ((length = socket.getInputStream().read(bytes)) != -<span class="hljs-number">1</span>) &#123;<br>                    System.out.println(<span class="hljs-string">&quot;thredId:&quot;</span> + Thread.currentThread().getName() + <span class="hljs-keyword">new</span> <span class="hljs-title class_">String</span>(bytes, <span class="hljs-number">0</span>, length));<br>                &#125;<br>            &#125; <span class="hljs-keyword">catch</span> (IOException e) &#123;<br>                e.printStackTrace();<br>            &#125;<br>            System.out.println(<span class="hljs-string">&quot;客户端关闭, 当前线程:&quot;</span> + Thread.currentThread().getName());<br>        &#125;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>
<p>执行结果</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java">thredId:pool-<span class="hljs-number">1</span>-thread-<span class="hljs-number">1</span>客户端<span class="hljs-number">0</span>第<span class="hljs-number">1</span>次通信<br>thredId:pool-<span class="hljs-number">1</span>-thread-<span class="hljs-number">1</span>客户端<span class="hljs-number">0</span>第<span class="hljs-number">2</span>次通信<br>客户端关闭, 当前线程:pool-<span class="hljs-number">1</span>-thread-<span class="hljs-number">1</span><br>thredId:pool-<span class="hljs-number">1</span>-thread-<span class="hljs-number">1</span>客户端<span class="hljs-number">1</span>第<span class="hljs-number">1</span>次通信<br>thredId:pool-<span class="hljs-number">1</span>-thread-<span class="hljs-number">1</span>客户端<span class="hljs-number">1</span>第<span class="hljs-number">2</span>次通信<br>客户端关闭, 当前线程:pool-<span class="hljs-number">1</span>-thread-<span class="hljs-number">1</span><br>thredId:pool-<span class="hljs-number">1</span>-thread-<span class="hljs-number">1</span>客户端<span class="hljs-number">2</span>第<span class="hljs-number">1</span>次通信<br>thredId:pool-<span class="hljs-number">1</span>-thread-<span class="hljs-number">1</span>客户端<span class="hljs-number">2</span>第<span class="hljs-number">2</span>次通信<br>客户端关闭, 当前线程:pool-<span class="hljs-number">1</span>-thread-<span class="hljs-number">1</span><br></code></pre></td></tr></table></figure>
<p>即使使用线程池进行了优化，本地还是阻塞io，只有当一个客户端通信完成（socket关闭了），另外一个 socket 才可以开始进行通信</p>
<h2 id="NIO"><a href="#NIO" class="headerlink" title="NIO"></a>NIO</h2><ol>
<li>nio 是非阻塞 io（Non-block io）</li>
<li>非阻塞 io 的重点是 <strong>事件</strong> 和 <strong>通知或轮询</strong></li>
<li><code>java.nio.channerls</code> 包提供了支持非阻塞通信的类<ol>
<li><code>ServerSocketChannel</code>: <code>ServerSocket</code> 的替代类，支持阻塞通信与非阻塞通信</li>
<li><code>SocketChannerl</code>： <code>Socket</code> 的替代类，支持阻塞通信与非阻塞通信</li>
<li><code>Selector</code>： 为<code>ServerSocketChannel</code>监控接收连接就绪事件，为<code>SocketChannel</code>监控连接就绪、读就绪和写就绪事件</li>
<li><code>SelectionKey</code>：代表<code>ServerSocketChannel</code>以及<code>SocketChannel</code>向<code>Selector</code>注册事件的句柄。当一个<code>SelectionKey</code>对象位于<code>Selector</code>对象的<code>selected-keys</code>集合中，就表示与这个<code>SelectionKey</code>对象相关的事件发生了</li>
</ol>
</li>
</ol>
<h3 id="NIO-执行流程"><a href="#NIO-执行流程" class="headerlink" title="NIO 执行流程"></a>NIO 执行流程</h3><pre class="mermaid">sequenceDiagram
    participant A as ServerSocketChannel
    participant B as Selector
    participant C as socketChannel
    A->>A: open()
    B-->>B: open()
    A-->>B: register(SelectionKey.OP_ACCEPT)
    C-->>C: open()
    C-->>B: register(SelectionKey.OP_CONNECT)
    B-->>A: selectionKey.isAcceptable()==true
    A-->>A: accept()
    A-->>B: register(SelectionKey.OP_READ)
    C-->>B: write(byte[])
    B-->>A: selectionKey.isReadable()==true
    A-->>A: read()
    A-->>B: register(SelectionKey.OP_WRITE)</pre>


<h2 id="代码示例"><a href="#代码示例" class="headerlink" title="代码示例"></a>代码示例</h2><p>代码执行流程</p>
<ol>
<li>服务端启动</li>
<li>客户端启动连接服务端（注册 <strong>可读</strong>和 <strong>可写</strong>事件）</li>
<li>客户端发生可写事件，开始写数据到服务端并且 <strong>取消可写事件</strong></li>
<li>服务端发生可读事件，读取数据并注册可写事件</li>
<li>服务端写数据到用户端并 <strong>取消可写事件</strong></li>
<li>客户端发生可读事件，读取数据后注册可写事件</li>
<li>重复3-6步骤</li>
</ol>
<p>上面有一个核心的步骤是，不论是管理端还是服务端，在写完数据之后都要取消可写事件，当想要写数据的时候，再进行注册写事件即可</p>
<ol>
<li>如果注册完写事件并且写完数据之后，没有进行取消，那么 <code>selectionKey.isWritable()</code> 会一直返回true， 原因请看第二点</li>
<li><code>SocketChannel</code> 在如下情况下都是可写的状态<ol>
<li>当连接建立后，可以向 <code>SocketChannel</code> 中写入数据</li>
<li>当 <code>SocketChannel</code> 中的输出缓冲区有空间时，可以写入数据, 所处只要缓冲区有空间，就会一直可写</li>
</ol>
</li>
</ol>
<h3 id="服务端"><a href="#服务端" class="headerlink" title="服务端"></a>服务端</h3><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">CusServerSocket</span> &#123;<br><br>    <span class="hljs-comment">/*缓冲区大小*/</span><br>    <span class="hljs-keyword">private</span> <span class="hljs-type">int</span> <span class="hljs-variable">BLOCK</span> <span class="hljs-operator">=</span> <span class="hljs-number">1024</span>;<br>    <span class="hljs-comment">/*接受数据缓冲区*/</span><br>    <span class="hljs-keyword">private</span> <span class="hljs-type">ByteBuffer</span> <span class="hljs-variable">sendBuffer</span> <span class="hljs-operator">=</span> ByteBuffer.allocate(BLOCK);<br>    <span class="hljs-comment">/*发送数据缓冲区*/</span><br>    <span class="hljs-keyword">private</span> <span class="hljs-type">ByteBuffer</span> <span class="hljs-variable">receiveBuffer</span> <span class="hljs-operator">=</span> ByteBuffer.allocate(BLOCK);<br>    <span class="hljs-keyword">private</span> Selector selector;<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-title function_">CusServerSocket</span><span class="hljs-params">(<span class="hljs-type">int</span> port)</span> <span class="hljs-keyword">throws</span> IOException &#123;<br>        <span class="hljs-comment">// 打开服务器套接字通道</span><br>        <span class="hljs-type">ServerSocketChannel</span> <span class="hljs-variable">serverSocketChannel</span> <span class="hljs-operator">=</span> ServerSocketChannel.open();<br>        <span class="hljs-comment">// 服务器配置为非阻塞</span><br>        serverSocketChannel.configureBlocking(<span class="hljs-literal">false</span>);<br>        serverSocketChannel.socket().bind(<span class="hljs-keyword">new</span> <span class="hljs-title class_">InetSocketAddress</span>(port));<br>        selector = Selector.open();<br>        <span class="hljs-comment">// 注册到selector，等待连接</span><br>        serverSocketChannel.register(selector, SelectionKey.OP_ACCEPT);<br>        System.out.println(<span class="hljs-string">&quot;服务启动----8080:&quot;</span>);<br>    &#125;<br><br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">listen</span><span class="hljs-params">()</span> <span class="hljs-keyword">throws</span> IOException, InterruptedException &#123;<br>        <span class="hljs-keyword">while</span> (<span class="hljs-literal">true</span>) &#123;<br>            <span class="hljs-comment">// 选择一组键，并且相应的通道已经打开</span><br>            selector.select();<br>            <span class="hljs-comment">// 返回此选择器的已选择键集。</span><br>            Set&lt;SelectionKey&gt; selectionKeys = selector.selectedKeys();<br>            Iterator&lt;SelectionKey&gt; iterator = selectionKeys.iterator();<br>            <span class="hljs-keyword">while</span> (iterator.hasNext()) &#123;<br>                <span class="hljs-type">SelectionKey</span> <span class="hljs-variable">selectionKey</span> <span class="hljs-operator">=</span> iterator.next();<br>                iterator.remove();<br>                handleEvent(selectionKey);<br>            &#125;<br>        &#125;<br>    &#125;<br><br>    <span class="hljs-comment">// 处理请求</span><br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">handleEvent</span><span class="hljs-params">(SelectionKey selectionKey)</span> <span class="hljs-keyword">throws</span> IOException, InterruptedException &#123;<br>        String receiveText;<br>        String sendText;<br>        <span class="hljs-type">int</span> <span class="hljs-variable">count</span> <span class="hljs-operator">=</span> <span class="hljs-number">0</span>;<br>        <span class="hljs-comment">// 可连接</span><br>        <span class="hljs-keyword">if</span> (selectionKey.isAcceptable()) &#123;<br>            <span class="hljs-type">ServerSocketChannel</span> <span class="hljs-variable">server</span> <span class="hljs-operator">=</span> (ServerSocketChannel) selectionKey.channel();<br>            <span class="hljs-type">SocketChannel</span> <span class="hljs-variable">client</span> <span class="hljs-operator">=</span> server.accept();<br>            <span class="hljs-comment">// 设置该通道非阻塞</span><br>            client.configureBlocking(<span class="hljs-literal">false</span>);<br>            <span class="hljs-comment">// 注册可读和可写事件</span><br>            client.register(selector, SelectionKey.OP_READ);<br>        &#125; <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (selectionKey.isReadable()) &#123;<br>            <span class="hljs-comment">// 返回为之创建此键的通道。</span><br>            <span class="hljs-type">SocketChannel</span> <span class="hljs-variable">client</span> <span class="hljs-operator">=</span> (SocketChannel) selectionKey.channel();<br>            <span class="hljs-comment">//将缓冲区清空以备下次读取</span><br>            receiveBuffer.clear();<br>            <span class="hljs-comment">//读取服务器发送来的数据到缓冲区中</span><br>            count = client.read(receiveBuffer);<br>            <span class="hljs-keyword">if</span> (count &gt; <span class="hljs-number">0</span>) &#123;<br>                receiveText = <span class="hljs-keyword">new</span> <span class="hljs-title class_">String</span>(receiveBuffer.array(), <span class="hljs-number">0</span>, count);<br>                System.out.println(<span class="hljs-string">&quot;服务器端接受客户端数据--:&quot;</span> + receiveText);<br>            &#125;<br>            client.register(selector, SelectionKey.OP_WRITE);<br>        &#125; <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (selectionKey.isWritable()) &#123;<br>            sendBuffer.clear();<br>            <span class="hljs-type">SocketChannel</span> <span class="hljs-variable">client</span> <span class="hljs-operator">=</span> (SocketChannel) selectionKey.channel();<br>            <span class="hljs-type">LocalTime</span> <span class="hljs-variable">now</span> <span class="hljs-operator">=</span> LocalTime.now();<br>            sendText = <span class="hljs-string">&quot;服务端--》客户端  &quot;</span> + now.getMinute() + <span class="hljs-string">&quot;:&quot;</span> + now.getSecond();<br>            sendBuffer.put(sendText.getBytes());<br>            <span class="hljs-comment">//将缓冲区各标志复位,因为向里面put了数据标志被改变要想从中读取数据发向服务器,就要复位</span><br>            sendBuffer.flip();<br>            <span class="hljs-comment">//输出到通道</span><br>            client.write(sendBuffer);<br>            selectionKey.interestOps(SelectionKey.OP_READ);<br>        &#125;<br>    &#125;<br><br>    <span class="hljs-comment">/**</span><br><span class="hljs-comment">     * <span class="hljs-doctag">@param</span> args</span><br><span class="hljs-comment">     * <span class="hljs-doctag">@throws</span> IOException</span><br><span class="hljs-comment">     */</span><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">main</span><span class="hljs-params">(String[] args)</span> <span class="hljs-keyword">throws</span> IOException, InterruptedException &#123;<br>        <span class="hljs-type">int</span> <span class="hljs-variable">port</span> <span class="hljs-operator">=</span> <span class="hljs-number">8080</span>;<br>        <span class="hljs-type">CusServerSocket</span> <span class="hljs-variable">server</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">CusServerSocket</span>(port);<br>        server.listen();<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<h3 id="用户端"><a href="#用户端" class="headerlink" title="用户端"></a>用户端</h3><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">CusClientSocket</span> &#123;<br><br>    <span class="hljs-comment">/*缓冲区大小*/</span><br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-type">int</span> <span class="hljs-variable">BLOCK</span> <span class="hljs-operator">=</span> <span class="hljs-number">1024</span>;<br>    <span class="hljs-comment">/*接受数据缓冲区*/</span><br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-type">ByteBuffer</span> <span class="hljs-variable">sendBuffer</span> <span class="hljs-operator">=</span> ByteBuffer.allocate(BLOCK);<br>    <span class="hljs-comment">/*发送数据缓冲区*/</span><br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-type">ByteBuffer</span> <span class="hljs-variable">receiveBuffer</span> <span class="hljs-operator">=</span> ByteBuffer.allocate(BLOCK);<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">main</span><span class="hljs-params">(String[] args)</span> <span class="hljs-keyword">throws</span> IOException, InterruptedException &#123;<br>        <span class="hljs-type">SocketChannel</span> <span class="hljs-variable">socketChannel</span> <span class="hljs-operator">=</span> SocketChannel.open();<br>        socketChannel.configureBlocking(<span class="hljs-literal">false</span>);<br>        <span class="hljs-type">Selector</span> <span class="hljs-variable">selector</span> <span class="hljs-operator">=</span> Selector.open();<br>        socketChannel.register(selector, SelectionKey.OP_CONNECT);<br>        socketChannel.connect(<span class="hljs-keyword">new</span> <span class="hljs-title class_">InetSocketAddress</span>(<span class="hljs-string">&quot;localhost&quot;</span>, <span class="hljs-number">8080</span>));<br><br>        Set&lt;SelectionKey&gt; selectionKeys;<br>        Iterator&lt;SelectionKey&gt; iterator;<br>        SelectionKey selectionKey;<br>        SocketChannel client;<br>        String receiveText;<br><br>        <span class="hljs-keyword">while</span> (<span class="hljs-literal">true</span>) &#123;<br>            selector.select();<br>            <span class="hljs-comment">//返回此选择器的已选择键集。</span><br>            selectionKeys = selector.selectedKeys();<br>            iterator = selectionKeys.iterator();<br>            <span class="hljs-keyword">while</span> (iterator.hasNext()) &#123;<br>                selectionKey = iterator.next();<br>                iterator.remove();<br>                <span class="hljs-keyword">if</span> (selectionKey.isConnectable()) &#123;<br>                    System.out.println(<span class="hljs-string">&quot;client connect&quot;</span>);<br>                    client = (SocketChannel) selectionKey.channel();<br>                    <span class="hljs-comment">// 判断此通道上是否正在进行连接操作。</span><br>                    <span class="hljs-comment">// 完成套接字通道的连接过程。</span><br>                    <span class="hljs-keyword">if</span> (client.isConnectionPending()) &#123;<br>                        client.finishConnect();<br>                        System.out.println(<span class="hljs-string">&quot;完成连接!&quot;</span>);<br>                    &#125;<br>                    <span class="hljs-comment">// 连接成功之后注册可读和可写事件</span><br>                    client.register(selector, SelectionKey.OP_READ | SelectionKey.OP_WRITE);<br>                &#125; <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (selectionKey.isReadable()) &#123;<br>                    client = (SocketChannel) selectionKey.channel();<br>                    <span class="hljs-comment">//将缓冲区清空以备下次读取</span><br>                    receiveBuffer.clear();<br>                    <span class="hljs-comment">//读取服务器发送来的数据到缓冲区中</span><br>                    <span class="hljs-type">int</span> <span class="hljs-variable">count</span> <span class="hljs-operator">=</span> client.read(receiveBuffer);<br>                    <span class="hljs-keyword">if</span> (count &gt; <span class="hljs-number">0</span>) &#123;<br>                        receiveText = <span class="hljs-keyword">new</span> <span class="hljs-title class_">String</span>(receiveBuffer.array(), <span class="hljs-number">0</span>, count);<br>                        System.out.println(<span class="hljs-string">&quot;客户端接受服务器端数据--:&quot;</span> + receiveText);<br>                    &#125;<br>                    client.register(selector, SelectionKey.OP_WRITE);<br><br>                &#125; <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (selectionKey.isWritable()) &#123;<br>                    Thread.sleep(<span class="hljs-number">2000</span>);<br>                    sendBuffer.clear();<br>                    client = (SocketChannel) selectionKey.channel();<br>                    <span class="hljs-type">LocalTime</span> <span class="hljs-variable">localTime</span> <span class="hljs-operator">=</span> LocalTime.now();<br>                    <span class="hljs-type">String</span> <span class="hljs-variable">sendText</span> <span class="hljs-operator">=</span> <span class="hljs-string">&quot;客户端发送的数据--&gt;&quot;</span> + localTime.getMinute() + <span class="hljs-string">&quot;:&quot;</span> + localTime.getSecond();<br>                    sendBuffer.put(sendText.getBytes());<br>                    <span class="hljs-comment">//将缓冲区各标志复位,因为向里面put了数据标志被改变要想从中读取数据发向服务器,就要复位</span><br>                    sendBuffer.flip();<br>                    client.write(sendBuffer);<br>                    <span class="hljs-comment">// 写完之后需要注销写事件，否则会一直判断成可写, 这里表是对读事件感兴趣，就是移除写事件的意思</span><br>                    selectionKey.interestOps(SelectionKey.OP_READ);<br>                &#125;<br>            &#125;<br>        &#125;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<div class="tabs" id="执行结果"><ul class="nav-tabs"><button type="button" class="tab  active" data-href="执行结果-1">服务端结果</button><button type="button" class="tab " data-href="执行结果-2">用户端结果</button></ul><div class="tab-contents"><div class="tab-item-content active" id="执行结果-1"><figure class="highlight brainfuck"><table><tr><td class="code"><pre><code class="hljs brainfuck"><span class="hljs-comment">服务启动</span><span class="hljs-literal">----</span><span class="hljs-comment">8080:</span><br><span class="hljs-comment">服务器端接受客户端数据</span><span class="hljs-literal">--</span><span class="hljs-comment">:客户端发送的数据</span><span class="hljs-literal">--</span>&gt;<span class="hljs-comment">59:45</span><br><span class="hljs-comment">服务器端接受客户端数据</span><span class="hljs-literal">--</span><span class="hljs-comment">:客户端发送的数据</span><span class="hljs-literal">--</span>&gt;<span class="hljs-comment">59:47</span><br><span class="hljs-comment">服务器端接受客户端数据</span><span class="hljs-literal">--</span><span class="hljs-comment">:客户端发送的数据</span><span class="hljs-literal">--</span>&gt;<span class="hljs-comment">59:49</span><br><span class="hljs-comment">服务器端接受客户端数据</span><span class="hljs-literal">--</span><span class="hljs-comment">:客户端发送的数据</span><span class="hljs-literal">--</span>&gt;<span class="hljs-comment">59:51</span><br><span class="hljs-comment">服务器端接受客户端数据</span><span class="hljs-literal">--</span><span class="hljs-comment">:客户端发送的数据</span><span class="hljs-literal">--</span>&gt;<span class="hljs-comment">59:53</span><br><span class="hljs-comment">服务器端接受客户端数据</span><span class="hljs-literal">--</span><span class="hljs-comment">:客户端发送的数据</span><span class="hljs-literal">--</span>&gt;<span class="hljs-comment">59:55</span><br><span class="hljs-comment">服务器端接受客户端数据</span><span class="hljs-literal">--</span><span class="hljs-comment">:客户端发送的数据</span><span class="hljs-literal">--</span>&gt;<span class="hljs-comment">59:57</span><br><span class="hljs-comment">服务器端接受客户端数据</span><span class="hljs-literal">--</span><span class="hljs-comment">:客户端发送的数据</span><span class="hljs-literal">--</span>&gt;<span class="hljs-comment">59:59</span><br></code></pre></td></tr></table></figure></div><div class="tab-item-content" id="执行结果-2"><figure class="highlight brainfuck"><table><tr><td class="code"><pre><code class="hljs brainfuck"><span class="hljs-comment">client connect</span><br><span class="hljs-comment">完成连接!</span><br><span class="hljs-comment">客户端接受服务器端数据</span><span class="hljs-literal">--</span><span class="hljs-comment">:服务端</span><span class="hljs-literal">--</span><span class="hljs-comment">》客户端  59:45</span><br><span class="hljs-comment">客户端接受服务器端数据</span><span class="hljs-literal">--</span><span class="hljs-comment">:服务端</span><span class="hljs-literal">--</span><span class="hljs-comment">》客户端  59:47</span><br><span class="hljs-comment">客户端接受服务器端数据</span><span class="hljs-literal">--</span><span class="hljs-comment">:服务端</span><span class="hljs-literal">--</span><span class="hljs-comment">》客户端  59:49</span><br><span class="hljs-comment">客户端接受服务器端数据</span><span class="hljs-literal">--</span><span class="hljs-comment">:服务端</span><span class="hljs-literal">--</span><span class="hljs-comment">》客户端  59:51</span><br><span class="hljs-comment">客户端接受服务器端数据</span><span class="hljs-literal">--</span><span class="hljs-comment">:服务端</span><span class="hljs-literal">--</span><span class="hljs-comment">》客户端  59:53</span><br><span class="hljs-comment">客户端接受服务器端数据</span><span class="hljs-literal">--</span><span class="hljs-comment">:服务端</span><span class="hljs-literal">--</span><span class="hljs-comment">》客户端  59:55</span><br><span class="hljs-comment">客户端接受服务器端数据</span><span class="hljs-literal">--</span><span class="hljs-comment">:服务端</span><span class="hljs-literal">--</span><span class="hljs-comment">》客户端  59:57</span><br><span class="hljs-comment">客户端接受服务器端数据</span><span class="hljs-literal">--</span><span class="hljs-comment">:服务端</span><span class="hljs-literal">--</span><span class="hljs-comment">》客户端  59:59</span><br></code></pre></td></tr></table></figure></div></div><div class="tab-to-top"><button type="button" aria-label="scroll to top"><i class="fas fa-arrow-up"></i></button></div></div>


]]></content>
      <categories>
        <category>java</category>
        <category>java基础</category>
      </categories>
      <tags>
        <tag>java</tag>
      </tags>
  </entry>
  <entry>
    <title>java输出运行过程中的代理类</title>
    <url>/2023/d513f5c497bd/index.html</url>
    <content><![CDATA[<h2 id="输出-jdk-动态代理类到文件"><a href="#输出-jdk-动态代理类到文件" class="headerlink" title="输出 jdk 动态代理类到文件"></a>输出 jdk 动态代理类到文件</h2><p>在测试代码的第一行增加下面这段代码</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java">System.getProperties().put(<span class="hljs-string">&quot;jdk.proxy.ProxyGenerator.saveGeneratedFiles&quot;</span>, <span class="hljs-string">&quot;true&quot;</span>);<br></code></pre></td></tr></table></figure>
<p>不同 <code>jdk</code> 版本的使用 <code>key</code> 可能不同，具体的 <code>key</code> 是什么可以查看 <code>ProxyGenerator</code> 类的 <code>saveGeneratedFiles</code> 属性， 比如 <code>jdk17</code> 版本 <code>ProxyGenerator</code> 类的内容如下：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">final</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">ProxyGenerator</span> <span class="hljs-keyword">extends</span> <span class="hljs-title class_">ClassWriter</span> &#123;<br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-type">boolean</span> <span class="hljs-variable">saveGeneratedFiles</span> <span class="hljs-operator">=</span><br>            java.security.AccessController.doPrivileged(<br>                    <span class="hljs-keyword">new</span> <span class="hljs-title class_">GetBooleanAction</span>(<br>                            <span class="hljs-string">&quot;jdk.proxy.ProxyGenerator.saveGeneratedFiles&quot;</span>));<br>&#125;<br></code></pre></td></tr></table></figure>
<p>执行完代码后在项目的根目录会输出动态代理类</p>
<p><a href="https://arthas.aliyun.com/doc/">arthas官方文档</a></p>
<h2 id="使用-arthas-输出-javaassist-生成的代理类"><a href="#使用-arthas-输出-javaassist-生成的代理类" class="headerlink" title="使用 arthas 输出 javaassist 生成的代理类"></a>使用 arthas 输出 javaassist 生成的代理类</h2><ol>
<li>先打端点，确定代理类的名称，比如有一个代理类名称是 <code>Hello$Adaptive</code></li>
<li>打开 <code>arthas</code> 选择对应的进程进入后，使用下面命令输出代理类</li>
</ol>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-comment">// 下面命令找到对应代理类的全路径， 比如输出结果是 com.example.dubbodemo.spi.Hello$Adaptive</span><br>sc *Hello$Adaptive*<br><span class="hljs-comment">// 根据上面找到的代理类的全路径，解析出源代码兵输出到特定的文件</span><br>jad com.example.dubbodemo.spi.Hello$Adaptive &gt; D:\\Hello$Adaptive.java<br></code></pre></td></tr></table></figure>

<h2 id="输出-cglib-代理类到文件"><a href="#输出-cglib-代理类到文件" class="headerlink" title="输出 cglib 代理类到文件"></a>输出 cglib 代理类到文件</h2><p>在测试方法的第一行加上如下代码, 第二个参数就是指定输出的文件夹</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java">System.setProperty(DebuggingClassWriter.DEBUG_LOCATION_PROPERTY, <span class="hljs-string">&quot;D:\\temp\\cglib&quot;</span>);<br></code></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>java</category>
        <category>java基础</category>
      </categories>
      <tags>
        <tag>java</tag>
      </tags>
  </entry>
  <entry>
    <title>java内存模型</title>
    <url>/2023/08549388833c/index.html</url>
    <content><![CDATA[<h2 id="主要内容"><a href="#主要内容" class="headerlink" title="主要内容"></a>主要内容</h2><p><code>java</code>内存模型实际上是一个抽象的概念，是一套规范，而不是一个实际的东西，而<code>java</code>内存结构就是根据这一套规范来进行划分的。</p>
<span id="more"></span>

<p>对于<code>java</code>内存模型我们主要需要了解以下几个内容：</p>
<ul>
<li>硬件内存架构</li>
<li>线程和<code>jvm</code></li>
<li><code>java</code>内存模型</li>
<li><code>java</code>内存模型对并发编程的支持</li>
</ul>
<h2 id="硬件内存架构"><a href="#硬件内存架构" class="headerlink" title="硬件内存架构"></a>硬件内存架构</h2><blockquote>
<p>物理机遇到的并发问题与虚拟机中的情况有很多相似之处，物理机对并发的处理方案对虚拟机的实现也有相当大的参考意义</p>
</blockquote>
<p>在计算机模型中主要是有五个部分来组成的</p>
<ol>
<li>输入</li>
<li>寄存器</li>
<li>cpu<ol>
<li>处理器</li>
<li>控制器</li>
</ol>
</li>
<li>输出</li>
</ol>
<p>其中<strong>处理器</strong>和<strong>控制器</strong>合起来就是我们所示的<code>cpu</code></p>
<p>运算器的运算速度都是很快的，但是我们的数据存储不可能所有数据都存放在寄存器中，所以数据的<code>io</code>操作都是很有必要 的，这样就不能充分利用<code>cpu</code>的运算能力，此时就需要<strong>加上读写速度和处理器运算速度差不多的的高速缓存</strong>来作为<strong>内存和处理器之间</strong>的缓冲。将运算需要使用的数据复制到缓存中，让运算能快速进行，当运算结束后再从缓存同步回内存之中，这样处理器就无须等待缓慢的内存读写了。</p>
<h3 id="缓存一致性问题"><a href="#缓存一致性问题" class="headerlink" title="缓存一致性问题"></a>缓存一致性问题</h3><p>上面加上了缓存可以解决内存的读写速度赶不上处理器的运算速度问题，但是又出现了另外一个问题。<strong>缓存一致性问题</strong></p>
<p>因为 在多处理器中，每一个处理器都有自己的高速缓存，但是内存是只有一个。对于缓存一致性问题在硬件层面主要有两个解决方式</p>
<ul>
<li><p>总线加锁</p>
<ul>
<li>所谓的总线加锁就每一次都会锁定一个<code>cpu</code>，只有这个锁定的<code>cpu</code>才可以执行，其余的<code>cpu</code>是不能执行的。这种方式会降低<code>cpu</code>的吞吐量。</li>
</ul>
</li>
<li><p>使用缓存一致性协议，所谓使用缓存一致性协议也就是在缓存和主内存之间加上一个访问规则，通过这种规则来解决缓存一致性问题。</p>
</li>
</ul>
<pre class="mermaid">graph LR
  A-->B
  subgraph A[处理器]
    direction TB
    A1[处理器1]~~~A2[处理器2]~~~A3[处理器3]
  end
  subgraph B[高速缓存]
    direction TB
    B1[缓存1]~~~B2[缓存2]~~~B3[缓存3]
  end
  B-->C
  C[缓存一致性]-->D[主内存]</pre>
<p>这里的<strong>缓存一致性其实是一个协议</strong>，是处理缓存一致性问题的一个协议(也可以任认为是处理缓存一致性问题的一个理论基础)。</p>
<h2 id="java内存模型-java-memory-mode"><a href="#java内存模型-java-memory-mode" class="headerlink" title="java内存模型(java memory mode)"></a>java内存模型(java memory mode)</h2><h3 id="什么是java内存模型-JMM规范"><a href="#什么是java内存模型-JMM规范" class="headerlink" title="什么是java内存模型(JMM规范)"></a>什么是<code>java</code>内存模型(JMM规范)</h3><p><strong><code>java</code>内存模型其实也是一个理论上的东西，是一个规范，这个规范的主要目的是定义程序中各种变量的访问规则，也就是关注的是虚拟机中把变量值存储到内存和从内存中取出变量值这样的底层细节。</strong>此处的变量和<code>java</code>编程中的变量是不同的，这里的变量包含了实例字段，静态字段和构成数组对象的元素，但是不包括局部变量和方法参数。（上面这段话是《深入理解java虚拟机》这本书中的）</p>
<p><code>jvm</code>将内存组织结构主要划分为两个部分</p>
<ul>
<li>主内存</li>
<li>工作内存</li>
</ul>
<pre class="mermaid">graph LR
  A-->B
  subgraph A[java线程]
    direction TB
    A1[线程1]~~~A2[线程2]~~~A3[线程3]
  end
  subgraph B[工作内存]
    direction TB
    B1[工作内存1]~~~B2[工作内存2]~~~B3[工作内存3]
  end
  B-->C
  C[save和load操作]-->D[主内存]</pre>

<h4 id="主内存"><a href="#主内存" class="headerlink" title="主内存"></a>主内存</h4><ul>
<li>主要包含本地方法区和堆</li>
<li>所有变量都要存储在主内存中，这些变量对于所有线程都是共享的</li>
</ul>
<h4 id="工作内存"><a href="#工作内存" class="headerlink" title="工作内存"></a>工作内存</h4><ul>
<li>每一个线程都有自己独立工作的地方，也就是工作内存</li>
<li>工作内存中保存的是主内存中某些变量的拷贝，线程堆变量的所有操作都必须在工作内存中进行，不能直接操作主内存</li>
<li><strong>如果是基本数据类型，那么直接分配到工作内存，如果是引用类型，引用的地址存在在工作空间中，但是具体的对象是存在在堆中(也就是主内存中)，是可以共享的</strong></li>
</ul>
<pre class="mermaid">graph LR
  subgraph B[java线程]
    direction BT
    B1[线程2]-->B2[工作内存2]
    B2-->B1
  end
  subgraph A[java线程]
    direction BT
    A1[线程1]-->A2[工作内存1]
    A2-->A1
  end
  C[主内存]
  A & B --> C
  C --> A & B</pre>

<p>看上图，之所以存在<code>java</code>内存模型一说是因为<code>java</code>虚拟机的工作机制。当有多个线程的时候，每一个线程可以操作的数据有两种类型</p>
<ul>
<li>线程私有的数据，也就是这个数据就算改变了也不会影响其余线程，比如说局部变量就是线程私有的数据</li>
<li>线程共享的数据，这样的数据多个线程都可以访问，就会出现线程安全的问题</li>
</ul>
<p>因为线程操作的数据有不同类型，所以在<code>java</code>中一个线程去操作数据的时候不是在主内存中直接操作，其实还有一个属于线程自己的内存(工作内存)，当操作线程私有的数据比较好理解，直接在工作内存中完成就可以咯，如果是操作共享的数据，那么就在主内存中拿一份数据到自己工作内存中操作，操作完了再将数据给到主内存中。</p>
<h3 id="内存结构和内存模型的关系"><a href="#内存结构和内存模型的关系" class="headerlink" title="内存结构和内存模型的关系"></a>内存结构和内存模型的关系</h3><p>上面说了什么是<code>java</code>的内存模型，知道<code>java</code>内存模型是一个规范，定义的是程序中变量的访问规则，那么这个内存模型和我们平时所有的内存结构有什么关系呢？先看一下<code>java</code>内存结构</p>
<h4 id="java内存结构"><a href="#java内存结构" class="headerlink" title="java内存结构"></a>java内存结构</h4><p><code>java</code>中内存结构主要是有五个部分</p>
<ul>
<li>程序计数器</li>
<li>堆</li>
<li>虚拟机栈<ul>
<li>存放方法运行时所需的数据，成为栈帧</li>
</ul>
</li>
<li>本地方法栈</li>
<li>方法区<ul>
<li>存储运行时常量池、已被虚拟机加载的类信息、常量、静态变量，即时编译后的代码等</li>
</ul>
</li>
</ul>
<p>上面<code>java</code>的五个部分其实就是根据<code>JMM</code>规范(<code>java</code>内存模型)来划分的一个内存结构。</p>
<h2 id="并发编程的三个特性"><a href="#并发编程的三个特性" class="headerlink" title="并发编程的三个特性"></a>并发编程的三个特性</h2><h3 id="原子性"><a href="#原子性" class="headerlink" title="原子性"></a>原子性</h3><p>执行的操作不可分割，可以理解为就是一步操作，因为只有一步所以肯定是不能分割的</p>
<h3 id="可见性"><a href="#可见性" class="headerlink" title="可见性"></a>可见性</h3><p>线程都是有自己的工作空间的，每一个线程都只能操作自己工作空间的数据，别的线程是看不到自己线程的工作空间的数据的</p>
<h3 id="有序性"><a href="#有序性" class="headerlink" title="有序性"></a>有序性</h3><p>程序中代码的顺序不一定就是在<code>cpu</code>中执行的顺序，因为存在两种重排</p>
<ul>
<li>编译重排序(编译期)</li>
<li>指令重排序(运行期)</li>
</ul>
<h2 id="JMM规范是如何保证并发编程的三个特征的呢"><a href="#JMM规范是如何保证并发编程的三个特征的呢" class="headerlink" title="JMM规范是如何保证并发编程的三个特征的呢"></a>JMM规范是如何保证并发编程的三个特征的呢</h2><h3 id="JMM和原子性"><a href="#JMM和原子性" class="headerlink" title="JMM和原子性"></a><code>JMM</code>和原子性</h3><h4 id="原子性案例1"><a href="#原子性案例1" class="headerlink" title="原子性案例1"></a>原子性案例1</h4><p><code>X = 10</code>这样一个操作具有原子性么？</p>
<ul>
<li>如果<code>X</code>是私有数据，是分配到工作空间的，那么就是有原子性的，因为只有一个写的操作</li>
<li>如果<code>X</code>是共享的数据，就是在主内存中，这样就需要先在主内存中读取(复制)到工作空间中，修改完成后再将数据通过到主内存中，这样就是两步，也就是没有原子性</li>
</ul>
<h4 id="原子性案例2"><a href="#原子性案例2" class="headerlink" title="原子性案例2"></a>原子性案例2</h4><p><code>i++</code>这个操作具有原子性么？</p>
<p><code>i++</code>这个操作其实是需要分为下面这几个步骤的</p>
<ul>
<li>读取<code>i</code>的值</li>
<li>将<code>i</code>的值加一</li>
<li>将修改后的值赋值给<code>i</code></li>
</ul>
<p>可以看到每一步都是原子操作，但是这三个原子操作合在一起之后去不是原子操作了</p>
<h4 id="JMM如何保证原子性的"><a href="#JMM如何保证原子性的" class="headerlink" title="JMM如何保证原子性的"></a>JMM如何保证原子性的</h4><ul>
<li>使用<code>Synchronized</code>关键字</li>
<li>使用<code>JUC</code>的<code>lock</code>和<code>unlock</code>关键字</li>
</ul>
<h3 id="JMM和可见性"><a href="#JMM和可见性" class="headerlink" title="JMM和可见性"></a><code>JMM</code>和可见性</h3><p>线程是不能操作主内存中的数据的，只能操作自己的工作空间的数据，所以需要数据时会先去工作空间找，如果找到了就会使用工作空间中的数据，找不到才会从主内存中找，但是如果一个线程在工作空间找到了，不过这个数据在别的线程中已经修改了，并且刷新到主内中了，由于当前线程的工作空间中有这个数据，没有在主内存中拿，这样当前线程拿到的就不是最新的数据。<code>JMM</code>是如何解决这个问题的呢？</p>
<h4 id="JMM如何保证可见性的"><a href="#JMM如何保证可见性的" class="headerlink" title="JMM如何保证可见性的"></a><code>JMM</code>如何保证可见性的</h4><ul>
<li><code>Volatile</code>关键字，使用这个关键字后，当线程需要使用这个关键字修饰的数据时，每一次都会从主内存中去找数据，而不是从工作空间中找。</li>
<li><code>JUC</code>  可以使用<code>JUC</code>的<code>lock</code>和<code>unlock</code></li>
</ul>
<h3 id="JMM和有序性-重排序"><a href="#JMM和有序性-重排序" class="headerlink" title="JMM和有序性(重排序)"></a><code>JMM</code>和有序性(重排序)</h3><p>对于有序性问题可以使用下面两种方式来解决</p>
<ul>
<li>使用<code>Volatile</code>关键字</li>
<li>使用<code>Synchronized</code>来加锁</li>
</ul>
]]></content>
      <categories>
        <category>java</category>
        <category>jvm</category>
      </categories>
      <tags>
        <tag>java</tag>
        <tag>jvm</tag>
      </tags>
  </entry>
  <entry>
    <title>jdk自带工具的使用</title>
    <url>/2024/19b110e0830d/index.html</url>
    <content><![CDATA[<h2 id="jdk17-使用-hsdb"><a href="#jdk17-使用-hsdb" class="headerlink" title="jdk17 使用 hsdb"></a>jdk17 使用 hsdb</h2><ol>
<li>进入 <code>jdk17</code> 安装目录下的 <code>bin</code> 目录，比如 <code>cd E:\java\jdk17\bin</code></li>
<li>执行 <code>jhsdb hsdb</code>，就会出现界面</li>
<li>选择 <code>File -&gt; Attacch to Host Spot process</code> 输入 <code>pid</code> 即可</li>
</ol>
]]></content>
      <categories>
        <category>java</category>
        <category>jvm</category>
      </categories>
      <tags>
        <tag>java</tag>
        <tag>jvm</tag>
      </tags>
  </entry>
  <entry>
    <title>jvm垃圾回收</title>
    <url>/2023/63d242ce6cb3/index.html</url>
    <content><![CDATA[<span class="hide_first_mermaid">
<div class="mermaid-wrap"><pre class="mermaid-src" hidden>
  flowchart LR
    A
  </pre></div>
</span>

<p>以下内容参考《剑指JVM:虚拟机实践与性能调优》</p>
<blockquote>
<p>Java使用的是自动内存管理机制，有内存分配器和垃圾收集器来代为分配和回收内存。自动内存管理机制使开发人员无须参与内存的分配和回收,自动内存管理就像一个黑匣子,所以了解JVM的自动内存分配和垃圾收集机制就显得非常重要</p>
</blockquote>
<h2 id="哪些对象是垃圾"><a href="#哪些对象是垃圾" class="headerlink" title="哪些对象是垃圾"></a>哪些对象是垃圾</h2><p>既然是讲解垃圾回收，自然需要先弄清楚什么样的对象才是垃圾</p>
<h3 id="对象存活判断–引用计数法"><a href="#对象存活判断–引用计数法" class="headerlink" title="对象存活判断–引用计数法"></a>对象存活判断–引用计数法</h3><ol>
<li>引用计数算法比较简单，对每个对象保存一个整型的引用计数器属性，用于记录对象被引用的次数（实现方式）</li>
<li>引用计数算法的优点是实现简单，垃圾对象便于辨识，判定效率高，回收没有延迟性（优点）</li>
<li>每个对象需要单独的字段存储计数器，这样的做法增加了存储空间的开销（缺点）</li>
<li>引用计数器有一个严重的问题，即无法处理循环引用的情况（严重的缺点）</li>
</ol>
<h4 id="循环引用示例"><a href="#循环引用示例" class="headerlink" title="循环引用示例"></a>循环引用示例</h4><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">ObjOne</span> &#123;<br>    <span class="hljs-keyword">private</span> ObjTwo objTwo;<br>    <span class="hljs-comment">// 省略set，get</span><br>&#125;<br><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">ObjTwo</span> &#123;<br>    <span class="hljs-keyword">private</span> ObjOne objOne;<br>    <span class="hljs-comment">// 省略set，get</span><br>&#125;<br><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> test &#123;<br>    <span class="hljs-type">ObjOne</span> <span class="hljs-variable">objOne</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">ObjOne</span>();<br>    <span class="hljs-type">ObjTwo</span> <span class="hljs-variable">objTwo</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">ObjTwo</span>();<br>    objOne.setObjTwo(objTwo);<br>    objTwo.setObjOne(objOne);<br>&#125;<br></code></pre></td></tr></table></figure>
<p>上面代码执行完成堆栈如果所示</p>
<pre class="mermaid">flowchart LR
  subgraph A[栈]
    A1["objOne"]
    A2["objTwo"]
  end
  subgraph B[堆]
    subgraph B1["ObjOne"]
      style B1 fill:#70b224
      B11[objTwo]
      style B11 fill:#f9f
    end
    subgraph B2["ObjTwo"]
      style B2 fill:#70b224
      B21[objOne]
      style B21 fill:#f9f
    end
    
    B11-->B21
    B21-->B11
  end
  A1-->B1
  A2-->B2</pre>
<p>当执行如下代码，堆栈引用情况会改变</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> test &#123;<br>    <span class="hljs-type">ObjOne</span> <span class="hljs-variable">objOne</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">ObjOne</span>();<br>    <span class="hljs-type">ObjTwo</span> <span class="hljs-variable">objTwo</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">ObjTwo</span>();<br>    objOne.setObjTwo(objTwo);<br>    objTwo.setObjOne(objOne);<br>    objOne = <span class="hljs-literal">null</span>;<br>    objTwo = <span class="hljs-literal">null</span>;<br>&#125;<br></code></pre></td></tr></table></figure>

<pre class="mermaid">flowchart LR
  subgraph A[栈]
    A1["objOne"]
    A2["objTwo"]
  end
  subgraph B[堆]
    subgraph B1["ObjOne"]
      style B1 fill:#70b224
      B11[objTwo]
      style B11 fill:#f9f
    end
    subgraph B2["ObjTwo"]
      style B2 fill:#70b224
      B21[objOne]
      style B21 fill:#f9f
    end
    
    B11-->B21
    B21-->B11
  end
  A1-. "X" .->B1
  A2-. "X" .->B2</pre>
<p>可以看到栈到堆中的引用虽然断开了，但是各自对象属性上面的连接依然没有断开</p>
<h3 id="对象存活判断–可达性分析算法"><a href="#对象存活判断–可达性分析算法" class="headerlink" title="对象存活判断–可达性分析算法"></a>对象存活判断–可达性分析算法</h3><ol>
<li>可达性分析算法是以 <code>GC Root</code> 为起始点，暗藏从上到下的方式搜索被跟对象集合所连接的对象是否可达</li>
</ol>
<h3 id="GC-Root-包含以下内容"><a href="#GC-Root-包含以下内容" class="headerlink" title="GC Root 包含以下内容"></a>GC Root 包含以下内容</h3><ol>
<li>虚拟机栈中对象的引用，比如，各个线程被调用的方法中使用到的引用数据类型的参数、局部变量等</li>
<li>本地方法栈内JNI（本地方法）对象的引用</li>
<li>方法区中引用数据类型的静态变量</li>
<li>方法区中常量对象的引用，比如字符串常量池<code>(String Table)</code>里的引用</li>
<li>所有被同步锁<code>synchronized</code>持有的对象引用</li>
<li><code>JVM</code>内部的引用。基本数据类型对应的Class对象引用，一些常驻的异常对象引用（如<code>NullPointerException</code>、<code>OutOfMemoryError</code>），系统类加载器对象引用等</li>
</ol>
<h2 id="垃圾回收的区域"><a href="#垃圾回收的区域" class="headerlink" title="垃圾回收的区域"></a>垃圾回收的区域</h2><p>根据 <ol><li><a href="/2023/70e7b048ccf4/index.html" title="jvm运行时内存区域">jvm运行时内存区域</a></li></ol> 可知，<code>jvm</code>运行时数据区主要包含两大类</p>
<ol>
<li>线程私有的区域： 程序计数器，本地方法栈，虚拟机栈</li>
<li>线程共享的区域： 方法区，堆<br>对于线程私有的区域是不需要垃圾回收的，因为当线程执行完成之后，线程中使用的内存会自动回收掉，<code>jvm</code>垃圾回收只需要关注 <strong>方法区</strong>和 <strong>堆</strong>即可，重点是 <strong>堆</strong></li>
</ol>
<h2 id="垃圾收集算法"><a href="#垃圾收集算法" class="headerlink" title="垃圾收集算法"></a>垃圾收集算法</h2><p>在 <code>jvm</code> 中，垃圾收集算法主要包含以下三种</p>
<ol>
<li>标记-清除算法</li>
<li>复制算法</li>
<li>标记压缩算法</li>
</ol>
<p>三种算法主要解决了将垃圾标记出来之后如何清除的问题，三种算法各有利弊，单独采用其中任何一种算法，都不能取得很好的效果。所以在JVM中，会针对内存的不同区域采用不同的垃圾收集算法，这就是分代算法</p>
<h3 id="标记-清除算法"><a href="#标记-清除算法" class="headerlink" title="标记-清除算法"></a>标记-清除算法</h3><ol>
<li>标记：垃圾收集器从引用根节点开始遍历，标记所有被引用的对象</li>
<li>清除：垃圾收集器对堆内存从头到尾进行线性遍历，如果发现某个对象为不可达对象，则将其回收</li>
</ol>
<p>优点：</p>
<ol>
<li>标记-清除算法的优点是简单、容易实现，而且不需要移动对象</li>
</ol>
<p>缺点：</p>
<ol>
<li>在进行<code>GC</code>的时候，需要停止整个应用程序，导致用户体验差</li>
<li>这种方式清理出来的空闲内存是不连续的，会产生内存碎片</li>
<li>该算法清除对象并不是真的置空，而是把需要清除的对象地址保存在空闲的地址列表里，下次有新对象申请内存时，判断某块内存空间是否充足，如果充足，新的对象将会覆盖原来标记为垃圾的对象，从而实现内存的重复使用。但是因为可用内存不连续问题，在大对象申请内存时，需要花费更多时间去找寻合适的位置，甚至失败。所以标记-清除算法效率不高，且内存利用率低下，甚至有些内存碎片无法重复利用</li>
</ol>
<h3 id="复制算法"><a href="#复制算法" class="headerlink" title="复制算法"></a>复制算法</h3><ol>
<li>核心思想是将内存空间分为两块，每次只使用其中一块，在垃圾回收时将正在使用的内存中的存活对象复制到未被使用的内存块中，之后清除正在使用的内存块中的所有对象。两块内存交替使用，从而完成垃圾对象的回收</li>
<li>复制算法没有标记和清除过程，实现简单，运行高效。内存从一块空间复制到另一块空间可以保证空间的连续性，不会出现内存碎片问题。但是复制算法需要双倍内存空间</li>
<li>复制算法需要移动对象，这就涉及修改对象引用地址值的问题</li>
<li>如果系统中的垃圾对象很少，复制算法不会很理想。因为复制算法需要复制的存活对象数量变大，使得垃圾回收器的运行效率变低</li>
</ol>
<h3 id="标记-压缩算法"><a href="#标记-压缩算法" class="headerlink" title="标记-压缩算法"></a>标记-压缩算法</h3><p>标记-压缩算法清除垃圾对象的过程</p>
<ol>
<li>第一阶段和标记–清除算法一样，从根节点开始标记所有被引用对象</li>
<li>第二阶段将所有的存活对象压缩到内存的一端，按顺序排放</li>
<li>第三阶段清理边界外所有的空间</li>
</ol>
<p>标记–压缩算法的最终效果等同于标记–清除算法执行完成后，再进行一次内存碎片整理</p>
<h2 id="分代收集算法"><a href="#分代收集算法" class="headerlink" title="分代收集算法"></a>分代收集算法</h2><ol>
<li>在程序中，不同的对象的生命周期是不同的，有些对象的生命周期很长，有些对象是朝生夕死的，因此在 <code>HotSpot</code> 的 <code>jvm</code> 中，将 <code>Java</code> 堆分为 <strong>新生代</strong> 和 <strong>老年代</strong>，生命周期较长的对象进入老年代，生命周期较短的一般放入新生代</li>
</ol>
<h3 id="新生代"><a href="#新生代" class="headerlink" title="新生代"></a>新生代</h3><ol>
<li>新生代特点是区域相对老年代较小，对象生命周期短、存活率低、回收频繁</li>
<li>复制算法的效率只和当前存活对象数量大小有关，结合新生代的特点，新生代使用复制算法，速度最快、效率也最高</li>
<li><code>HotSpot</code> 将新生代空间分成三个区域<ol>
<li>Eden(默认占80%)</li>
<li>From(默认占10%)</li>
<li>To(默认占10%)</li>
</ol>
</li>
</ol>
<p>每次新生代发生<code>GC</code>时，把<code>Eden</code>区和上次幸存区中在本次<code>GC</code>仍然存活的对象复制到另一个幸存区，幸存区在<code>From</code>区和<code>To</code>区之间切换，总有一块空间为空，从而解决内存利用率低的问题</p>
<h3 id="老年代"><a href="#老年代" class="headerlink" title="老年代"></a>老年代</h3><ol>
<li>老年代特点是区域较大，对象生命周期长、存活率高，回收不及新生代频繁。因为存在大量存活率高的对象，复制算法明显变得不合适。一般是由标记–清除或者标记–清除与标记–压缩的混合实现</li>
</ol>
<h2 id="增量收集算法"><a href="#增量收集算法" class="headerlink" title="增量收集算法"></a>增量收集算法</h2><ol>
<li>上述现有的算法，在垃圾回收过程中，应用软件将处于一种<code>STW(Stop The World)</code>的状态。在<code>STW</code>状态下，应用程序所有的线程都会挂起，暂停一切正常的工作，等待垃圾回收的完成。如果垃圾回收时间过长，应用程序会被挂起很久，将严重影响用户体验或者系统的稳定性</li>
<li>增量收集基本思想是如果一次性将所有的垃圾进行处理，会造成系统长时间的停顿，那么就可以让垃圾收集线程和应用程序线程交替执行。每次垃圾收集线程只收集一小片区域的内存空间，接着切换到应用程序线程。依次反复，直到垃圾收集完成</li>
<li>增量收集算法的基础仍是传统的标记–清除和复制算法。增量收集算法通过对线程间冲突的妥善处理，允</li>
<li>许垃圾收集线程以分阶段的方式完成标记、清理或复制工作</li>
</ol>
<h2 id="分区收集算法"><a href="#分区收集算法" class="headerlink" title="分区收集算法"></a>分区收集算法</h2><ol>
<li>分代算法按照对象的生命周期长短将堆空间划分成两个部分，分区算法将整个堆空间划分成连续的不同小区间(<code>region</code>)</li>
<li>在相同条件下，堆空间越大，一次<code>GC</code>所需要的时间就越长，有关<code>GC</code>产生的停顿也就越长。为了更好地控制<code>GC</code>产生的停顿时间，将一块大的内存区域分割成多个小块，根据目标的停顿时间，每次合理地回收若干个小区间，而不是整个堆空间，从而减少一次<code>GC</code>所产生的停顿</li>
</ol>
<h2 id="垃圾回收相关概念理解"><a href="#垃圾回收相关概念理解" class="headerlink" title="垃圾回收相关概念理解"></a>垃圾回收相关概念理解</h2><h3 id="STW-Stop-the-world"><a href="#STW-Stop-the-world" class="headerlink" title="STW(Stop the world)"></a>STW(Stop the world)</h3><ol>
<li>在垃圾回收过程中，整个应用程序都会暂停，没有任何响应，所以被形象地称为<code>“Stop-The-World”</code>，简称<code>STW</code></li>
<li>可达性分析算法中枚举根节点(<code>GC Roots</code>)造成<code>STW</code>，原因是如果出现分析过程中对象引用关系还在不断变化，则分析结果的准确性无法保证。所以分析工作必须在一个能确保一致性的快照中进行</li>
</ol>
<h3 id="安全点"><a href="#安全点" class="headerlink" title="安全点"></a>安全点</h3><ol>
<li>在垃圾回收过程中，应用程序会发生 <code>STW</code> 现象, 但是应用程序在执行过程中，并不是在任意位置都适合停顿下来进行<code>GC</code>的，只有在特定的位置才能停顿下来进行<code>GC</code>操作，这些特定的位置被称为安全点(<code>Safe Point</code>)</li>
</ol>
<h4 id="哪些位置是安全点"><a href="#哪些位置是安全点" class="headerlink" title="哪些位置是安全点"></a>哪些位置是安全点</h4><ol>
<li>安全点太少可能导致<code>GC</code>等待的时间太长，如果安全点太密可能导致运行时的性能问题</li>
<li>通常选择一些运行时间较长的指令位置作为安全点，例如方法调用、循环跳转等</li>
</ol>
<h4 id="GC-发生时如何保证应用线程都在安全点上"><a href="#GC-发生时如何保证应用线程都在安全点上" class="headerlink" title="GC 发生时如何保证应用线程都在安全点上"></a>GC 发生时如何保证应用线程都在安全点上</h4><p><strong>抢先式中断</strong><br><code>GC</code>抢先中断所有线程。如果发现某个线程不在安全点，就重新恢复该线程，让线程跑到安全点【这种方式不是应用程序主导，而是 <code>GC</code> 线程主导，所以没有使用】</p>
<p><strong>主动式中断</strong><br><code>GC</code>线程给自己设置一个中断标志，各个应用线程运行到安全点的时候主动轮询这个标志，如果此时<code>GC</code>线程的中断标志为真，则将自己中断挂起。这种方式的好处是由应用程序在安全点主动发起中断，而不会出现被迫在非安全点的位置先中断的情况</p>
<h3 id="安全区域"><a href="#安全区域" class="headerlink" title="安全区域"></a>安全区域</h3><ol>
<li>安全点机制保证了程序执行时，在不太长的时间内就会遇到可进入<code>GC</code>的安全点, 如果应用线程被阻塞了又该如何处理？—这就需要通过引入 <strong>安全区域</strong> 来解决</li>
<li>安全区域是指在一段代码片段中，对象的引用关系不会发生变化，在这个区域中的任何位置开始<code>GC</code>都是安全的</li>
<li>当线程运行安全区域的代码时，首先标识已经进入了安全区域，如果这段时间内发生<code>GC</code>,<code>JVM</code>会忽略标识为安全区域状态的线程</li>
<li>当线程即将离开安全区域时，会检查<code>JVM</code>是否已经完成<code>GC</code>，如果完成了，则继续运行，否则线程必须等待直到收到可以安全离开安全区域的信号为止</li>
</ol>
<h2 id="垃圾收集器"><a href="#垃圾收集器" class="headerlink" title="垃圾收集器"></a>垃圾收集器</h2><h3 id="垃圾收集器的分类"><a href="#垃圾收集器的分类" class="headerlink" title="垃圾收集器的分类"></a>垃圾收集器的分类</h3><p>前面讲解垃圾收集算法时提到 <code>HotSpot</code> 虚拟机中使用了分代垃圾收集思想，对应的根据垃圾收集器工作的内存区间不同，可分为新生代垃圾收集器，老年代垃圾收集器和整堆垃圾收集器</p>
<pre class="mermaid">flowchart LR
    subgraph A["新生代"]
        A1["Serial GC"]
        A2["Parallel Scavenge GC"]
        A3["ParNew GC"]
        A4["G1 GC"]
    end
    subgraph B["新生代"]
        B1["Serial Old GC"]
        B2["Parallel Old GC"]
        B3["CMS GC"]
        B4["G1 GC"]
    end</pre>

<h3 id="Serial"><a href="#Serial" class="headerlink" title="Serial"></a>Serial</h3><ol>
<li><code>Serial</code> 采用的是<strong>复制算法</strong>、<strong>串行回收</strong>和<code>STW</code>机制的方式执行内存回收</li>
<li><code>Serial</code> 用于回收 <strong>新生代</strong></li>
<li><code>Serial</code>收集器作为<code>HotSpot</code>中<code>Client</code>模式下的默认新生代垃圾收集器</li>
</ol>
<h3 id="Serial-Old"><a href="#Serial-Old" class="headerlink" title="Serial Old"></a>Serial Old</h3><ol>
<li><code>Serial Old</code> 用于回收 <strong>老年代</strong></li>
<li><code>Serial Old</code>收集器同样采用了<strong>串行回收</strong>和<code>STW</code>机制，只不过内存回收算法使用的是<strong>标记—压缩算法</strong></li>
<li><code>Serial Old</code>是运行在<code>Client</code>模式下默认的老年代的垃圾收集器</li>
<li><code>Serial Old</code>在<code>Server</code>模式下主要有两个用途<ol>
<li>与新生代的<code>Parallel Scavenge</code>垃圾收集器搭配</li>
<li>作为老年代<code>CMS</code>收集器的后备方案</li>
</ol>
</li>
</ol>
<h3 id="Serial-Serial-Old垃圾收集器过程图"><a href="#Serial-Serial-Old垃圾收集器过程图" class="headerlink" title="Serial&#x2F;Serial Old垃圾收集器过程图"></a>Serial&#x2F;Serial Old垃圾收集器过程图</h3><p><img src="https://blog-1308475102.cos.ap-guangzhou.myqcloud.com/picture/202311262309313.webp" alt="Serial"></p>
<h3 id="CMS"><a href="#CMS" class="headerlink" title="CMS"></a>CMS</h3><ol>
<li><code>CMS</code>主要适合场景是对响应时间的需求大于对吞吐量的要求。它是HotSpot虚拟机中第一款真正意义上的并发收集器，第一次实现了让垃圾收集线程与用户线程同时工作</li>
<li><code>CMS</code>的垃圾收集算法采用<strong>标记–清除算法</strong>，也会<code>STW</code></li>
<li><code>CMS</code>作为老年代的收集器，无法与新生代收集器<code>Parallel Scavenge</code>配合工作，所以使用<code>CMS</code>收集老年代时，新生代只能选择<code>ParNew</code>或者<code>Serial</code>收集器中的一个</li>
<li><code>JDK9</code> 中 <code>G1</code> 变成了默认的垃圾收集器，替换了 <code>CMS</code></li>
<li><code>JDK14</code> 中，彻底移除了 <code>CMS</code> 垃圾收集器</li>
</ol>
<h3 id="CMS-垃圾回收过程"><a href="#CMS-垃圾回收过程" class="headerlink" title="CMS 垃圾回收过程"></a>CMS 垃圾回收过程</h3><p><img src="https://blog-1308475102.cos.ap-guangzhou.myqcloud.com/picture/202311262316564.webp" alt="CMS垃圾回收过程"></p>
<ol>
<li>初始标记： 该阶段的主要任务仅仅只是标记出<code>GC Roots</code>能直接关联到的对象。一旦标记完成之后就会恢复之前被暂停的所有应用线程。由于直接关联对象比较小，所以这里的速度非常快</li>
<li>并发标记： 从<code>GC Roots</code>的直接关联对象开始遍历整个对象图的过程，这个过程耗时较长但是不需要停顿用户线程，可以与垃圾收集线程一起并发运行</li>
<li>重新标记(<code>Remark</code>): 由于在并发标记阶段中，程序的工作线程会和垃圾收集线程同时运行或者交叉运行，为了修正在并发标记期间因用户程序继续运作而导致标记产生变动的那一部分对象的标记记录，需要一次重新标记操作</li>
<li>并发清除： 此阶段清理已经被标记为死亡的对象，释放内存空间。由于不需要移动存活对象，所以这个阶段也是可以与用户线程同时并发的</li>
</ol>
<h3 id="CMS-垃圾收集器的缺点"><a href="#CMS-垃圾收集器的缺点" class="headerlink" title="CMS 垃圾收集器的缺点"></a>CMS 垃圾收集器的缺点</h3><ol>
<li>由于使用的是标记-清除算法，因此会产生内存碎片（为什么不使用整理算法呢–因为要保证垃圾收集和用户线程同时运行，所以用户线程所需的资源就不能移动位置，标记整理算法需要移动对象位置）</li>
<li>对<code>CPU</code>资源非常敏感。在并发阶段，它虽然不会导致用户停顿，但是会因为占用了一部分线程而导致应用程序变慢，总吞吐量会降低</li>
<li>由于在垃圾收集阶段用户线程没有中断，要是<code>CMS</code>运行期间预留的内存无法满足程序需要，就会出现一次<code>“Concurrent Mode Failure”</code>失败而导致另一次<code>Full GC</code>的产生</li>
<li>无法处理<strong>浮动垃圾</strong>,在并发清除阶段由于程序的工作线程和垃圾收集线程是同时运行或者交叉运行的，那么在并发清除阶段如果产生新的垃圾对象，<code>CMS</code>将无法对这些垃圾对象进行标记，最终会导致这些新产生的垃圾对象没有被及时回收</li>
</ol>
<h3 id="G1-收集器"><a href="#G1-收集器" class="headerlink" title="G1 收集器"></a>G1 收集器</h3><p><strong>为什么需要 G1 垃圾收集器</strong><br>为了实现在应用程序运行环境内存不断扩大，处理器数量不断增加的情况下，进一步降低停顿时间，同时还能兼顾良好的吞吐量的目标</p>
<p><strong>G1 垃圾收集器的工作方式</strong><br><code>G1</code>有计划地避免在整个<code>Java</code>堆中进行全区域的垃圾收集。<code>G1</code>跟踪各个<code>Region</code>里面的垃圾堆积的价值大小（回收所获得的空间大小以及回收所需时间的经验值），在后台维护一个优先列表，每次根据允许的收集时间，优先回收价值最大的<code>Region</code></p>
<p><strong>G1 垃圾收集器的使用场景</strong><br><code>G1</code>是一款面向服务端应用的垃圾收集器，主要针对配备多核<code>CPU</code>及大容量内存的机器，极大可能降低垃圾回收停顿时间的同时，还兼具高吞吐量的性能特征</p>
<h3 id="G1-垃圾收集器收集垃圾过程"><a href="#G1-垃圾收集器收集垃圾过程" class="headerlink" title="G1 垃圾收集器收集垃圾过程"></a>G1 垃圾收集器收集垃圾过程</h3><p><code>G1</code> 可以作用于整个新生代和老年代，垃圾回收过程如下</p>
<ol>
<li>新生代GC</li>
<li>老年代并发标记</li>
<li>混合回收</li>
</ol>
]]></content>
      <categories>
        <category>java</category>
        <category>jvm</category>
      </categories>
      <tags>
        <tag>java</tag>
        <tag>jvm</tag>
      </tags>
  </entry>
  <entry>
    <title>jvm执行引擎</title>
    <url>/2023/a18206d79e08/index.html</url>
    <content><![CDATA[<span class="hide_first_mermaid">
<div class="mermaid-wrap"><pre class="mermaid-src" hidden>
  flowchart LR
    A
  </pre></div>
</span>

<h2 id="执行引擎的作用"><a href="#执行引擎的作用" class="headerlink" title="执行引擎的作用"></a>执行引擎的作用</h2><pre class="mermaid">flowchart TB
    A["java源代码(.java文件)"]
    B["class字节码文件"]
    C["内存(放入jvm方法区)"]
        subgraph D["执行引擎"]
            subgraph D1["编译器"]
                D11["Client Compiler"]
                D12["Server Compiler"]
            end
        end
    E["机器码"]

    A--"javac编译器"-->B
    B--"类加载器"-->C
    C-->D
    D--解释执行-->E
    D--"编译执行(JIT)"-->E</pre>
<h3 id="字节码"><a href="#字节码" class="headerlink" title="字节码"></a>字节码</h3><ol>
<li><code>Java</code>程序可以通过编译器将源码编译成<code>Java</code>字节码，特定平台上的虚拟机将字节码转译为可以直接执行的机器码，也就实现了跨平台性</li>
<li>字节码主要为了实现特定软件运行和软件环境，与硬件环境无关</li>
</ol>
<h2 id="即时编译器-JIT-的作用"><a href="#即时编译器-JIT-的作用" class="headerlink" title="即时编译器(JIT)的作用"></a>即时编译器(JIT)的作用</h2><ol>
<li>即时编译器在最初的 <code>jvm</code> 中是没有的，没有的原因就是 <code>jvm</code> 设计的初衷是为了满足 <code>Java</code> 程序实现跨平台的特性，所以避免采用静态编译的方式直接生成机器码，所以采用了解释器在运行时逐行解释字节码执行程序</li>
<li><code>JIT</code>编译器的作用就是虚拟机将字节码直接编译成机器码</li>
<li><strong>无论是采用解释器进行解释执行，还是采用即时编译器进行编译执行，最终字节码都是需要被转换成对应平台的机器码</strong></li>
</ol>
<h2 id="为何HotSpot虚拟机要使用解释器与编译器并存的架构"><a href="#为何HotSpot虚拟机要使用解释器与编译器并存的架构" class="headerlink" title="为何HotSpot虚拟机要使用解释器与编译器并存的架构"></a>为何HotSpot虚拟机要使用解释器与编译器并存的架构</h2><p>因为解释器和编译器各有优势</p>
<ol>
<li>程序启动后，解释器可以立即发挥作用，省去编译的时间，立即执行。编译器想要发挥作用，将代码编译成机器码，需要一定的执行时间，但是编译为机器码之后，执行效率更高</li>
<li>当程序的运行环境中内存资源较大(比如嵌入式系统)时，解释执行可以更节约内存</li>
</ol>
<h2 id="为何HotSpot虚拟机要实现两个不同的即时编译器"><a href="#为何HotSpot虚拟机要实现两个不同的即时编译器" class="headerlink" title="为何HotSpot虚拟机要实现两个不同的即时编译器"></a>为何HotSpot虚拟机要实现两个不同的即时编译器</h2><p><code>HotSpot</code> 虚拟机包含两个即时编译器</p>
<ol>
<li><code>Client Compile</code>， 也叫做 <code>c1</code> 编译器</li>
<li><code>Server Compile</code>，也叫做 <code>c2</code> 编译器</li>
</ol>
<p><code>HotSpot</code> 虚拟机默认采用解释器和其中一个编译器配合的方式工作，程序使用哪个编译器，取决于虚拟机运行的模式</p>
<p>通过 <code>-client</code> 参数可以指定 <code>jvm</code> 运行在 <code>Client</code> 模式下，并使用 <code>c1</code> 编译器，<code>c1</code> 编译器会对字节码进行简单和可靠的优化，耗时短，达到更快的编译速度</p>
<p>通过 <code>-server</code> 参数可以指定 <code>jvm</code> 运行在 <code>Server</code> 模式下，并使用 <code>c2</code> 编译器，<code>c2</code> 会进行耗时较长的优化，以及激进优化，但优化的代码执行效率更高</p>
<p>实施分层编译后，<code>Client Compiler</code>和<code>Server Compiler</code>将会同时工作，许多代码都可能会被多次编译，用<code>Client Compiler</code>获取更高的编译速度，用<code>Server Compiler</code>来获取更好的编译质量，在解释执行的时候也无须再承担收集性能监控信息的任务</p>
<h2 id="程序何时使用解释器执行？何时使用编译器执行？"><a href="#程序何时使用解释器执行？何时使用编译器执行？" class="headerlink" title="程序何时使用解释器执行？何时使用编译器执行？"></a>程序何时使用解释器执行？何时使用编译器执行？</h2><p>在运行过程中会被即时编译器编译的 <strong>热点代码</strong>包含两类</p>
<ol>
<li>被<strong>多次</strong>调用的方法</li>
<li>被<strong>多次</strong>执行的循环体</li>
</ol>
<h3 id="热点探测"><a href="#热点探测" class="headerlink" title="热点探测"></a>热点探测</h3><p>无论是基于多次调用的方法还是多次执行的循环体，都强调多次，具体多少次算多次呢？ 在 <code>HotSpot</code> 中是使用 <strong>基于计数器的热点探测</strong> 方式来判断执行的次数的，在 <code>HotSpot</code> 虚拟机中为每个方法准备了两类计数器，这两类计数器都有确定的阈值，当技术器超过阈值了，就会触发 <code>JIT</code> 编译</p>
<ol>
<li>方法调用计数器： 用来统计方法调用的次数，可以通过参数 <code>-XX:CompileThreshold</code> 来认为设置阈值</li>
<li>回边计数器： 用来统计一个方法中循环体代码执行的次数，在字节码中遇到控制流向后跳转的指令称为 <strong>回边</strong></li>
</ol>
]]></content>
      <categories>
        <category>java</category>
        <category>jvm</category>
      </categories>
      <tags>
        <tag>java</tag>
        <tag>jvm</tag>
      </tags>
  </entry>
  <entry>
    <title>jvm运行时内存区域</title>
    <url>/2023/70e7b048ccf4/index.html</url>
    <content><![CDATA[<span class="hide_first_mermaid">
<div class="mermaid-wrap"><pre class="mermaid-src" hidden>
  flowchart LR
    A
  </pre></div>
</span>
以下内容参考《剑指JVM:虚拟机实践与性能调优》

<h2 id="JVM-整体结构"><a href="#JVM-整体结构" class="headerlink" title="JVM 整体结构"></a>JVM 整体结构</h2><ol>
<li><code>JVM</code> 在程序执行期间把它所管理的内存分为若干个不同的数据区域</li>
<li><code>JVM</code> 内存布局规定了 <code>Java</code> 在运行过程中内存申请、分配、管理的策略，保证了JVM的高效稳定运行</li>
<li>线程私有区域： 虚拟机栈，本地方法栈，程序计数器</li>
<li>线程共享区域： 堆，方法区</li>
</ol>
<p><img src="https://blog-1308475102.cos.ap-guangzhou.myqcloud.com/picture/202311222254661.png" alt="jvm 整体结构"></p>
<h2 id="程序计数器"><a href="#程序计数器" class="headerlink" title="程序计数器"></a>程序计数器</h2><h3 id="程序计数器相关概念"><a href="#程序计数器相关概念" class="headerlink" title="程序计数器相关概念"></a>程序计数器相关概念</h3><ol>
<li>程序计数器中的寄存器并非是广义上所指的物理寄存器，JVM中的程序计数器是对物理寄存器的一种抽象模拟</li>
<li>JVM中的程序计数器英文全称是 Program Counter Register</li>
<li>程序计数器既没有垃圾回收也没有内存溢出</li>
<li>如果线程正在执行的是一个Java方法，这个计数器记录的是正在执行的虚拟机字节码指令的地址；如果正在执行的是本地(Native)方法，这个计数器值则应为空(Undefined)</li>
<li>每一个线程都有一个程序计数器（程序计数器和寄存器是两个概念）</li>
</ol>
<h3 id="为什么需要程序计数器"><a href="#为什么需要程序计数器" class="headerlink" title="为什么需要程序计数器"></a>为什么需要程序计数器</h3><p>因为CPU需要不停地切换各个线程，切换回来以后，就需要知道接着从哪里开始继续执行。JVM的字节码解释器通过改变程序计数器的值，来明确下一条应该执行什么样的字节码指令</p>
<h2 id="虚拟机栈"><a href="#虚拟机栈" class="headerlink" title="虚拟机栈"></a>虚拟机栈</h2><ol>
<li>每个线程在创建时都会创建一个虚拟机栈，其内部由许多栈帧(Stack Frame)构成，每个栈帧对应着一个Java方法的调用</li>
<li>每个方法被执行的时候，JVM都会同步创建一个栈帧用于存储<strong>局部变量表、操作数栈、动态链接、方法出口</strong>等信息</li>
</ol>
<h3 id="栈帧-栈的存储单位"><a href="#栈帧-栈的存储单位" class="headerlink" title="栈帧-栈的存储单位"></a>栈帧-栈的存储单位</h3><ol>
<li>每个线程上正在执行的每个方法都各自对应一个栈帧</li>
</ol>
<h3 id="栈帧中存储的内容"><a href="#栈帧中存储的内容" class="headerlink" title="栈帧中存储的内容"></a>栈帧中存储的内容</h3><pre class="mermaid">flowchart TB
  subgraph A[栈]
    subgraph B[栈帧]
      direction LR
        B1[局部变量表]
        B2[方法返回值]
        B3[操作数栈]
        B4[动态链接]
        B5["附加信息\n比如对程序调试相关信息"]
        B1~~~B2~~~B3~~~B4~~~B5
    end
    subgraph C[栈帧]
      direction LR
        C1[局部变量表]
        C2[方法返回值]
        C3[操作数栈]
        C4[动态链接]
        C5["附加信息\n比如对程序调试相关信息"]
        C1~~~C2~~~C3~~~C4~~~C5
    end
  end</pre>
<h4 id="局部变量表"><a href="#局部变量表" class="headerlink" title="局部变量表"></a>局部变量表</h4><ol>
<li>局部变量表是一个数组</li>
<li>局部变量表的作用是存储方法参数和定义在方法体内的局部变量<ol>
<li>局部变量数据类型可以是基本数据类型，对象引用(存储的是指向对象的引用)</li>
</ol>
</li>
<li>局部变量表的大小在编译器确定，方法运行期间不会大概局部变量表的大小</li>
</ol>
<h5 id="slot"><a href="#slot" class="headerlink" title="slot"></a>slot</h5><ol>
<li>局部变量表达存储单元是 <code>slot</code></li>
<li>32 位以内的类型只占用一个 <code>slot</code>， 64 位的类型（<code>long</code> 和 <code>double</code> ）占用两个 <code>slot</code></li>
<li><code>byte，short，char</code> 在存储前会被转换成 <code>int</code></li>
<li><code>JVM</code> 会为局部变量表中的每一个 <code>slot</code> 都分配一个访问索引，通过这个索引即可成功访问到局部变量表中指定的局部变量值</li>
</ol>
<h4 id="操作数栈"><a href="#操作数栈" class="headerlink" title="操作数栈"></a>操作数栈</h4><ol>
<li>操作数栈主要用于保存计算过程的中间结果，同时作为计算过程中变量临时的存储空间</li>
<li>每一个操作数栈都拥有一个明确的栈深度用于存储数值，其所需的最大深度在编译期就定义好了，保存在方法的 <code>Code</code> 属性中的<code>Maximum stack size</code>数据项中</li>
<li>操作数栈只能通过入栈(<code>push</code>)和出栈(<code>pop</code>)操作来完成一次数据访问</li>
</ol>
<h4 id="动态链接"><a href="#动态链接" class="headerlink" title="动态链接"></a>动态链接</h4><ol>
<li>每一个栈帧内部都包含一个指向运行时常量池中该栈帧所属方法的引用。包含这个引用的目的就是为了支持当前方法的代码能够实现动态链接</li>
<li>动态链接的目的是在 <code>jvm</code> 加载了字节码文件，将类数据加载到内存之后，可以将字节码文件中记录的符号引用转换位为调用方法的直接引用</li>
</ol>
<h4 id="方法返回地址"><a href="#方法返回地址" class="headerlink" title="方法返回地址"></a>方法返回地址</h4><ol>
<li>方法返回地址存储的是调用该方法的程序计数器的值</li>
<li>方法正常退出时，调用者的程序计数器的值作为返回地址，即调用该方法的指令的下一条指令的地址</li>
<li>方法正常退出时，调用者的程序计数器的值作为返回地址，即调用该方法的指令的下一条指令的地址</li>
</ol>
<h2 id="堆区"><a href="#堆区" class="headerlink" title="堆区"></a>堆区</h2><ol>
<li>前面讲解的 <strong>栈</strong> 解决的是程序的运行问题，即程序是如何运行，如何处理数据的，但是数据从哪里来呢？ 这就涉及到下面要讲的堆</li>
<li>堆是存储的单位，堆解决的是数据存储的问题，即数据怎么放、放在哪儿</li>
</ol>
<h3 id="堆空间的划分"><a href="#堆空间的划分" class="headerlink" title="堆空间的划分"></a>堆空间的划分</h3><pre class="mermaid">flowchart LR
  subgraph A[堆]
    subgraph B[新生代]
      B1["\nEden\n\n"]
      B2["\nfrom\n\n"]
      B3["\nto\n\n"]
      B1~~~B2~~~B3
    end
    subgraph C["\n\n\n &nbsp&nbsp&nbsp&nbsp&nbsp&nbsp&nbsp&nbsp老年代&nbsp&nbsp&nbsp&nbsp&nbsp&nbsp&nbsp\n\n\n\n"]
    end
  end</pre>

<ol>
<li>堆空间可以通过参数 <code>-Xms</code>(代表堆最小值) 和 <code>-Xmx</code> （代表堆空间最大值）来设置</li>
<li>新生代与老年代的空间比例可以通过 <code>-XX:NewRatio=4</code> 参数来设置，这个案例代表，新生代占比1，老年代占比4，新生代占整个堆的 1&#x2F;5</li>
<li><code>Eden</code> 区和两个 <code>Survivor</code> 区默认的比例是 8:1:1, 可以通过参数 <code>-XX:SurvivorRatio=8</code> 来显示设置比例， 如果 <code>-XX:SurvivorRatio=5</code> 代表 <code>Eden</code> 和两个 <code>Survivor</code> 区占比为 5:1:1</li>
</ol>
<h3 id="对象内存分配过程"><a href="#对象内存分配过程" class="headerlink" title="对象内存分配过程"></a>对象内存分配过程</h3><p><img src="https://blog-1308475102.cos.ap-guangzhou.myqcloud.com/picture/202311232258657.png" alt="对象内存分配过程"></p>
<h3 id="TLAB"><a href="#TLAB" class="headerlink" title="TLAB"></a>TLAB</h3><ol>
<li>堆中还有一部分区域是线程私有，这部分区域称为线程本地分配缓存区(<code>Thread Local Allocation Buffer,TLAB</code>)</li>
<li><code>TLAB</code> 表示<code>JVM</code>为每个线程分配了一个私有缓存区域，这块缓存区域包含在<code>Eden</code>区内</li>
<li>可以通过参数  <code>-XX:+/-UseTLAB</code> 来设置是否开启 <code>TLAB</code> 功能</li>
<li>可以通过参数 <code>-XX:TLABWasteTargetPercent</code> 开设置 <code>TLAB</code> 占用 <code>Eden</code> 区域的大小，默认是占用 1%</li>
<li>对象在<code>TLAB</code>空间分配内存失败时，<code>JVM</code>就会尝试着通过使用加锁机制确保数据操作的原子性，从而直接在<code>Eden</code>区中分配内存</li>
</ol>
<h4 id="使用-TLAB-的原因"><a href="#使用-TLAB-的原因" class="headerlink" title="使用 TLAB 的原因"></a>使用 TLAB 的原因</h4><ol>
<li>堆区是线程共享区域，对象实例的创建在<code>JVM</code>中非常频繁，因此在并发环境下从堆区中划分内存空间是线程不安全的</li>
<li>避免多个线程操作同一地址，就需要使用加锁等机制，但是加锁会影响分配速度</li>
</ol>
<h3 id="逃逸分析"><a href="#逃逸分析" class="headerlink" title="逃逸分析"></a>逃逸分析</h3><p>逃逸分析是指：当一个对象在方法中创建后，若对象只是在方法内部使用，则认为对象没有发生逃逸，若这个对象被方法外部使用，则认为发生了逃逸</p>
<h4 id="逃逸分析之同步省略"><a href="#逃逸分析之同步省略" class="headerlink" title="逃逸分析之同步省略"></a>逃逸分析之同步省略</h4><p>遇到同步代码块时，<code>JIT</code>编译器可以借助逃逸分析，来判断同步块所使用的锁对象是否只能够被一个线程访问而没有被发布到其他线程, 如果没有被发布到其它线程，<code>JIT</code> 编译器在编译同步代码块时会取消堆这部分代码的同步</p>
<h4 id="逃逸分析之栈上分配"><a href="#逃逸分析之栈上分配" class="headerlink" title="逃逸分析之栈上分配"></a>逃逸分析之栈上分配</h4><p><code>JIT(Just In Time)</code>编译器在编译期间根据逃逸分析的结果，发现如果一个对象没有逃逸出方法的话，就可能被优化成栈上分配</p>
<h4 id="逃逸分析之标量替换"><a href="#逃逸分析之标量替换" class="headerlink" title="逃逸分析之标量替换"></a>逃逸分析之标量替换</h4><p>标量<code>(Scalar)</code>是指一个无法再分解成更小数据的数据。<code>Java</code>中的原始数据类型就是标量。相对的，那些还可以分解的数据叫作聚合量<code>(Aggregate)</code>，<code>Java</code>中的对象就是聚合量，因为它可以分解成其他聚合量和标量。在<code>JIT</code>编译器的编译阶段，如果经过逃逸分析，发现一个对象不会被外界访问的话，那么经过<code>JIT</code>优化，就会把这个对象拆解成若干个成员变量。这个过程就是标量替换</p>
<h1 id="方法区"><a href="#方法区" class="headerlink" title="方法区"></a>方法区</h1><h3 id="方法区-栈和堆三者之间的关系"><a href="#方法区-栈和堆三者之间的关系" class="headerlink" title="方法区, 栈和堆三者之间的关系"></a>方法区, 栈和堆三者之间的关系</h3><p>以下面代码为例</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-type">Student</span> <span class="hljs-variable">student</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Student</span>();<br></code></pre></td></tr></table></figure>
<ol>
<li><code>Student</code> 这个 <code>class</code> 运行时会存储到方法区</li>
<li><code>student</code> 引用变量会存储在栈区</li>
<li><code>new Student()</code> 对象是存放在堆区</li>
</ol>
<h3 id="jdk-中方法区的变化"><a href="#jdk-中方法区的变化" class="headerlink" title="jdk 中方法区的变化"></a>jdk 中方法区的变化</h3><ol>
<li>在 <code>jdk7</code> 之前，将方法区称为永久代，在 <code>jdk8</code> 中，方法区变成了 <strong>元空间</strong></li>
<li>永久代在逻辑上属于堆的一部分，但是物理存储上不是在堆中，所以永久代也叫做 <strong>非堆</strong><ol>
<li>通过<code>-XX:PermSize</code>参数设置永久代初始分配空间</li>
<li>通过<code>-XX:MaxPermSize</code>参数设置永久代最大可分配空间。32位机器默认是64MB,64位机器模式是82MB</li>
<li>当空间不足时会出现 <code>java.lang.OutOfMemoryError:PermGen space</code></li>
</ol>
</li>
<li>元空间是使用本地内存实现，只要物理内存还有容量，就不会有内存溢出的问题<ol>
<li>元空间大小可以使用参数<code>-XX:MetaspaceSize</code>和<code>-XX:MaxMetaspaceSize</code>指定，替代<code>JDK7</code>中的永久代的初始值和最大值</li>
<li>当空间不足时会出现 <code>java.lang.OutOfMemoryError:Metaspace</code></li>
</ol>
</li>
</ol>
<h3 id="方法区中存储的内容"><a href="#方法区中存储的内容" class="headerlink" title="方法区中存储的内容"></a>方法区中存储的内容</h3><ol>
<li><code>java</code> 源代码编译之后会生成 <code>class</code> 文件，经过类加载器把 <code>class</code> 文件中内容加载到 <code>jvm</code> 运行时数据区</li>
<li><code>class</code> 文件中的一部分信息会加载到方法区，主要包含的内容如下图</li>
</ol>
<pre class="mermaid">flowchart TB
  subgraph A[方法区存储的内容]
    A1[类型信息]
    A11["类class，接口interface,枚举enmu，annotaion"]
    A1-->A11

    A2[运行时常量池]
    A21["class文件中有个constant pool,\n当class文件被加载到内存之后，方法区也\n会存放class文件的常量池相关信息，\n此时class文件的常量池就变成了方法区的运行时常量池"]
    A22["运行时常量池相对于class文件常量池的\n另外一个重要特征是具备动态性,\n运行期间也可以将新的常量使用intern()放入池中"]
    A2-->A21 & A22

    A3[静态变量]
    A31["jdk7之前类变量也是方法区的一部分"]
    A32["jdk7及以后类变量放在了堆空间"]
    A3-->A31 & A32

    A4[JIT代码缓存]

    A5[域信息]
    A51["域相关信息包含域名称，类型，\n域修饰符(public，private，proteced,static,final,volatile)"]
    A5-->A51

    A6["方法信息,包含以下内容"]
    A61["方法的返回类型（或void）"]
    A62["方法参数的数量和类型"]
    A63["方法的修饰符(public，private等)"]
    A64["方法的字节码，操作数栈深度，局部变量表大小"]
    A65["异常表"]
    A6-->A61 & A62 & A63 & A64 & A65
  end</pre>

<p><a href="https://zhuanlan.zhihu.com/p/269134063">jdk7静态变量存放在堆中的工具分析</a></p>
<h3 id="不同版本jdk方法区的内容"><a href="#不同版本jdk方法区的内容" class="headerlink" title="不同版本jdk方法区的内容"></a>不同版本jdk方法区的内容</h3><h4 id="jdk6-方法区"><a href="#jdk6-方法区" class="headerlink" title="jdk6 方法区"></a>jdk6 方法区</h4><pre class="mermaid">flowchart LR
    subgraph A[方法区和堆]
        subgraph A1["方法区(永久代PermGen实现)"]
            subgraph A11["运行时常量池"]
                A111["字符串常量池"]
            end
            A12["类型信息"]
            A13["域信息"]
            A14["方法信息"]
            A15["JIT代码缓存"]
            A16["静态变量"]
            A11~~~A12~~~A13~~~A14~~~A15~~~A16
        end
        A2["\n\n\n &nbsp;&nbsp; 堆 &nbsp;&nbsp; \n\n\n"]
    end</pre>

<h4 id="jdk7-方法区"><a href="#jdk7-方法区" class="headerlink" title="jdk7 方法区"></a>jdk7 方法区</h4><pre class="mermaid">flowchart LR
    subgraph A[方法区和堆]
        subgraph A1["方法区(永久代PermGen实现)"]
            A11["运行时常量池"]
            A12["类型信息"]
            A13["域信息"]
            A14["方法信息"]
            A15["JIT代码缓存"]
            A11~~~A12~~~A13~~~A14~~~A15
        end
        subgraph A2["堆"]
            A21["字符串常量池"]
            A22["静态变量"]
            A21~~~A22
        end
    end</pre>
<h4 id="jdk8-方法区"><a href="#jdk8-方法区" class="headerlink" title="jdk8 方法区"></a>jdk8 方法区</h4><pre class="mermaid">flowchart LR
    subgraph A[方法区和堆]
        subgraph A1["方法区(元空间Metaspace实现)"]
            A11["运行时常量池"]
            A12["类型信息"]
            A13["域信息"]
            A14["方法信息"]
            A15["JIT代码缓存"]
            A11~~~A12~~~A13~~~A14~~~A15
        end
        subgraph A2[堆]
            A21["方法区(逻辑上\n是堆的一部分，\n实际不在堆里面)"]
            subgraph A22[实际堆]
                A221["静态变量"]
                A222["字符串常量池"]
            end
            A21~~~A22
        end
        A21-->A1
    end</pre>

<h3 id="方法区中为什么使用元空间替代永久代"><a href="#方法区中为什么使用元空间替代永久代" class="headerlink" title="方法区中为什么使用元空间替代永久代"></a>方法区中为什么使用元空间替代永久代</h3><ol>
<li><code>JRockit</code> 虚拟机和<code>HotSpot</code>虚拟机j进行融合，而<code>JRockit</code>不需要配置永久代</li>
<li>为永久代设置空间大小是很难确定的,如果动态加载类过多，容易产生永久代的<code>OOM</code></li>
<li>元空间和永久代之间最大的区别在于：元空间并不在虚拟机中，而是使用本地内存。因此，默认情况下，元空间的大小仅受本地内存限制</li>
<li><code>JDK7</code>之前的版本中，<code>HotSpot</code>虚拟机将类型信息、内部字符串和类静态变量存储在永久代中，垃圾收集器也会对该区域进行垃圾回收。<code>JDK7</code>将<code>HotSpot</code>虚拟机中永久代内部字符串和类静态变量数据移动到<code>Java</code>堆中，但是依然存在永久代,将元数据从永久代剥离出来放到元空间中，不仅实现了对元数据的无缝管理，而且因为元空间大小仅受本地内存限制，也简化了<code>Full GC</code>，并且可以在<code>GC</code>不暂停的情况下并发地释放元数据</li>
</ol>
<h3 id="方法区的垃圾回收"><a href="#方法区的垃圾回收" class="headerlink" title="方法区的垃圾回收"></a>方法区的垃圾回收</h3><p>方法区主要回收两部分内容</p>
<ol>
<li>常量池中废弃的常量<ol>
<li>常量池中的字面量：比如文本字符串，被声明为 <code>final</code>的常量值等</li>
<li>符号引用：类和接口的全限定名，字段的名称和描述符，方法的名称和描述符</li>
</ol>
</li>
<li>不再使用的类型信息</li>
</ol>
<p>常量池中判断字面量是否不再使用简单一些，但是判断一个类型是否属于不再被使用的类，则需要满足下面的条件</p>
<ol>
<li>该类的所有示例都已经被回收</li>
<li>加载该类的类加载器已经被回收</li>
<li>该类的 <code>java.lang.Class</code> 对象没有在任何地方被引用</li>
</ol>
<p><strong>满足了上面的条件只是代表可以会回收，并不是一定会被回收</strong></p>
]]></content>
      <categories>
        <category>java</category>
        <category>jvm</category>
      </categories>
      <tags>
        <tag>java</tag>
        <tag>jvm</tag>
      </tags>
  </entry>
  <entry>
    <title>jvm类加载机制</title>
    <url>/2024/859a04275429/index.html</url>
    <content><![CDATA[<span class="hide_first_mermaid">
<div class="mermaid-wrap"><pre class="mermaid-src" hidden>
  flowchart LR
    A
  </pre></div>
</span>

<h2 id="1-类的整个生命周期"><a href="#1-类的整个生命周期" class="headerlink" title="1. 类的整个生命周期"></a>1. 类的整个生命周期</h2><pre class="mermaid">flowchart LR
    A["加载"]
    subgraph B["链接"]
        direction LR
        B1["验证"]
        B2["准备"]
        B3["解析"]
        B1-->B2-->B3
    end
    C["初始化"]
    D["使用"]
    E["卸载"]
    A-->B-->C-->D-->E</pre>
<p>接下来看看每个阶段做的事情</p>
<h3 id="1-1-加载阶段完成的事情"><a href="#1-1-加载阶段完成的事情" class="headerlink" title="1.1. 加载阶段完成的事情"></a>1.1. 加载阶段完成的事情</h3><ul>
<li>根据类的全限定名读取类的二进制流</li>
<li>将字节流所代表的的静态存储结构转化为方法区的运行时数据结构 <strong>字节码结构是存储在方法区</strong></li>
<li>在堆区中生成一个代表这个类的<code>java.lang.Class</code>对象，用于方法区这个类的各种数据的访问入口， <strong>Class对象是在堆中</strong></li>
</ul>
<h3 id="1-2-链接阶段"><a href="#1-2-链接阶段" class="headerlink" title="1.2. 链接阶段"></a>1.2. 链接阶段</h3><h4 id="1-2-1-准备阶段完成的事情"><a href="#1-2-1-准备阶段完成的事情" class="headerlink" title="1.2.1. 准备阶段完成的事情"></a>1.2.1. 准备阶段完成的事情</h4><ul>
<li>给类变量分配内存并附初始值(零值) </li>
<li><code>final</code> 修饰的变量直接赋值，而不是初始化为零值</li>
</ul>
<h4 id="1-2-2-解析阶段完成的事情"><a href="#1-2-2-解析阶段完成的事情" class="headerlink" title="1.2.2. 解析阶段完成的事情"></a>1.2.2. 解析阶段完成的事情</h4><ul>
<li>虚拟机将常量池内的符号引用替换为直接引用</li>
</ul>
<h3 id="1-3-初始化阶段完成的事情"><a href="#1-3-初始化阶段完成的事情" class="headerlink" title="1.3. 初始化阶段完成的事情"></a>1.3. 初始化阶段完成的事情</h3><ul>
<li>为类变量赋值（在准备阶段是给类变量分配内存和赋零值），这里是设置为代码中设定的值，比如 <code>static int a = 10</code>, 这里的赋值就是将10赋值给 <code>a</code></li>
<li>当一个类中含有类变量或静态方法，<code>jvm</code> 会给当前类生成一个 <strong>类初始化方法</strong> <code>&lt;clinit&gt;</code></li>
</ul>
<h2 id="2-类加载器"><a href="#2-类加载器" class="headerlink" title="2. 类加载器"></a>2. 类加载器</h2><p>在前面讲类的整个生命周期时，第一个阶段就是加载，加载动作就是通过类加载器完成的，类加载器完成的事情就是 <strong>通过类的全限定名获取描述类的二进制流</strong></p>
<h3 id="2-1-jdk9-之前的类加载器"><a href="#2-1-jdk9-之前的类加载器" class="headerlink" title="2.1. jdk9 之前的类加载器"></a>2.1. jdk9 之前的类加载器</h3><pre class="mermaid">flowchart TB
    A["BootStrap ClassLoader"]
    B["xtension ClassLoader"]
    C["Application ClassLoader"]
    D1["自定义类加载器 User ClassLoader"]
    D2["自定义类加载器 User ClassLoader"]
    A-->B-->C
    C-->D1
    C-->D2</pre>

<h3 id="2-2-jdk9-之后的类加载器"><a href="#2-2-jdk9-之后的类加载器" class="headerlink" title="2.2. jdk9 之后的类加载器"></a>2.2. jdk9 之后的类加载器</h3><pre class="mermaid">flowchart TB
    A["BootStrap ClassLoader"]
    B["Platform  ClassLoader"]
    C["Application ClassLoader"]
    D1["自定义类加载器 User ClassLoader"]
    D2["自定义类加载器 User ClassLoader"]
    A-->B-->C
    C-->D1
    C-->D2</pre>

<h3 id="2-3-双亲委派模式"><a href="#2-3-双亲委派模式" class="headerlink" title="2.3. 双亲委派模式"></a>2.3. 双亲委派模式</h3><ol>
<li>当一个类加载器收到加载请求时会先把加载请求委托给父类加载器</li>
<li>如果父类加载器也存在父类加载器，继续向上委托，直至最顶层引导类加载器</li>
<li>如果父类加载器能完成类的加载，就交给父类加载器完成，反之由子类进行加载</li>
</ol>
<p><strong>注：这里类加载器中所说的父类不是继承关系，而是实现类加载器的层级关系，子类加载器中会包含父类加载器的引用而已</strong></p>
<h4 id="2-3-1-优点"><a href="#2-3-1-优点" class="headerlink" title="2.3.1. 优点"></a>2.3.1. 优点</h4><ul>
<li>避免核心类库被修改，即使有相同全限定类名的类，也会先交给父类去判断</li>
<li>避免全限定名相同的类被重复加载，导致程序异常</li>
</ul>
]]></content>
      <categories>
        <category>java</category>
        <category>jvm</category>
      </categories>
      <tags>
        <tag>java</tag>
        <tag>jvm</tag>
      </tags>
  </entry>
  <entry>
    <title>自定义类加载器</title>
    <url>/2024/0ca6653854a6/index.html</url>
    <content><![CDATA[<blockquote>
<p>jdk版本： 17</p>
</blockquote>
<h2 id="自定义类加载器"><a href="#自定义类加载器" class="headerlink" title="自定义类加载器"></a>自定义类加载器</h2><p>自定义类加载器需要继承 <code>ClassLoader</code> 类</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">CusClassLoader</span> <span class="hljs-keyword">extends</span> <span class="hljs-title class_">ClassLoader</span> &#123;<br><br>    <span class="hljs-comment">// 重写 findClass 类， 参数 name 是类的全限定类名</span><br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">protected</span> Class&lt;?&gt; findClass(String name) <span class="hljs-keyword">throws</span> ClassNotFoundException &#123;<br>        <span class="hljs-type">byte</span>[] data;<br>        <span class="hljs-keyword">try</span> &#123;<br>            data = findClassData(name);<br>        &#125; <span class="hljs-keyword">catch</span> (IOException e) &#123;<br>            <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">RuntimeException</span>(e);<br>        &#125;<br>        <span class="hljs-keyword">return</span> <span class="hljs-built_in">super</span>.defineClass(name, data, <span class="hljs-number">0</span>, data.length);<br>    &#125;<br><br>    <span class="hljs-keyword">private</span> <span class="hljs-type">byte</span>[] findClassData(String name) <span class="hljs-keyword">throws</span> IOException &#123;<br>        <span class="hljs-comment">// 需要根据 name 找到对应的 class 文件，这里省略了，直接加载对应的 class 文件</span><br>        <span class="hljs-type">File</span> <span class="hljs-variable">file</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">File</span>(<span class="hljs-string">&quot;D:\\logs\\TestOne.class&quot;</span>);<br>        <span class="hljs-type">InputStream</span> <span class="hljs-variable">inputStream</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">FileInputStream</span>(file);<br>        <span class="hljs-keyword">return</span> inputStream.readAllBytes();<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<h2 id="编写需要被加载的类"><a href="#编写需要被加载的类" class="headerlink" title="编写需要被加载的类"></a>编写需要被加载的类</h2><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">package</span> com.example;<br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">TestOne</span> &#123;<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">testOne</span><span class="hljs-params">()</span> &#123;<br>        System.out.println(<span class="hljs-string">&quot;test one&quot;</span>);<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<h2 id="编写测试类"><a href="#编写测试类" class="headerlink" title="编写测试类"></a>编写测试类</h2><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">TestMain</span> &#123;<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">main</span><span class="hljs-params">(String[] args)</span> <span class="hljs-keyword">throws</span> Exception &#123;<br>        <span class="hljs-type">CusClassLoader</span> <span class="hljs-variable">classLoader</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">CusClassLoader</span>();<br>        Class&lt;?&gt; aClass = classLoader.loadClass(<span class="hljs-string">&quot;com.example.TestOne&quot;</span>);<br>        System.out.println(aClass.getClassLoader());<br>        <span class="hljs-type">Method</span> <span class="hljs-variable">method</span> <span class="hljs-operator">=</span> aClass.getMethod(<span class="hljs-string">&quot;testOne&quot;</span>);<br>        method.invoke(aClass.newInstance());<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<p>输出结果</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java">com.example.CusClassLoader@6576fe71<br>test one<br></code></pre></td></tr></table></figure>

<h2 id="重写-findClass-还是-loadClass"><a href="#重写-findClass-还是-loadClass" class="headerlink" title="重写 findClass 还是 loadClass"></a>重写 findClass 还是 loadClass</h2><p>自定义类加载器时需要继承 <code>ClassLoader</code> 类，但是具体可重写的方法包含 <code>findClass</code> 和 <code>loadClass</code>，一般情况下只需要重写 <code>findClass</code> 即可，因为双亲委派的逻辑是在 <code>loadClass</code> 中完成，所以不想破坏双亲委派时就直接重写 <code>findClass</code> 即可</p>
]]></content>
      <categories>
        <category>java</category>
        <category>jvm</category>
      </categories>
      <tags>
        <tag>java</tag>
        <tag>jvm</tag>
      </tags>
  </entry>
  <entry>
    <title>Mybatis执行流程和核心组件</title>
    <url>/2023/08bb8954abe7/index.html</url>
    <content><![CDATA[<span class="hide_first_mermaid">
<div class="mermaid-wrap"><pre class="mermaid-src" hidden>
  flowchart LR
    A
  </pre></div>
</span>

<blockquote>
<p>Mybatis版本： 3.5.15<br>参考书籍： 《Mybatis3源码深度解析》</p>
</blockquote>
<h2 id="Mybatis核心组件"><a href="#Mybatis核心组件" class="headerlink" title="Mybatis核心组件"></a>Mybatis核心组件</h2><p><img src="https://blog-1308475102.cos.ap-guangzhou.myqcloud.com/picture/202312262105691.webp" alt="Mybatis核心组件"></p>
<p><strong>组件说明</strong></p>
<ul>
<li><code>Configuration</code>: 用于描述 <code>MyBatis</code> 的主配置信息，其他组件需要获取配置信息时，直接通过<code>Configuration</code> 对象获取<ul>
<li><code>Mybatis</code> 在启动时，将 <code>Mapper</code> 配置信息、类型别名、<code>TypeHandler</code> 等注册到<code>Configuration</code> 组件中，其他组件需要这些信息时，也可以从 <code>Configuration</code> 对象中获取</li>
</ul>
</li>
<li><code>MappedStatement</code>： <code>MappedStatement</code> 用于描述 <code>Mapper</code> 中的 <code>SQL</code> 配置信息，是对<code>Mapper XML</code> 配置文件中 <code>&lt;select|update|delete|insert&gt;</code> 等标签或者 <code>@Select/@Update</code> 等注解配置信息的封装</li>
<li><code>SqlSession</code>： <code>SqlSession</code> 是提供给用户操作的 <code>API</code>, 表示和数据库交互时的会话对象</li>
<li><code>Executor</code>: <code>Executor</code> 是 <code>MyBatis</code> 的 <code>SQL</code> 执行器，<code>MyBatis</code> 中对数据库所有的增删改查操作都是由 <code>Executor</code> 组件完成的</li>
<li><code>StatementHandler</code>: <code>StatementHandler</code> 封装了对 <code>JDBC Statement</code> 对象的操作</li>
<li><code>ParameterHandler</code>: 当 <code>MyBatis</code> 框架使用的 <code>Statement</code> 类型为 <code>CallableStatement</code> 和<code>PreparedStatement</code>时，<code>ParameterHandler</code> 用于为 <code>Statement</code> 对象参数占位符设置值</li>
<li><code>ResultSetHandler</code>: <code>ResultSetHandler</code>封装了对 <code>JDBC</code> 中的 <code>ResultSet</code> 对象操作</li>
<li><code>TypeHandler</code>: <code>TypeHandler</code> 是 <code>MyBatis</code> 中的类型处理器，用于处理 <code>Java</code> 类型与 <code>JDBC</code> 类型之间的映射</li>
</ul>
<h3 id="Configuration"><a href="#Configuration" class="headerlink" title="Configuration"></a>Configuration</h3><p><strong>作用1：作为主配置文件的承载</strong></p>
<blockquote>
<p>MyBatis 框架的配置信息有两种，一种是配置 MyBatis 框架属性的主配置文件；<br>另一种是配置执行 SQL语句的 Mapper 配置文件。<br>Configuration 的作用是描述 MyBatis 主配置文件的信息</p>
</blockquote>
<p>可以看 <code>Configuration</code> 类中相关属性</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">Configuration</span> &#123;<br><br>    <span class="hljs-keyword">protected</span> Environment environment;<br><br>    <span class="hljs-keyword">protected</span> <span class="hljs-type">boolean</span> safeRowBoundsEnabled;<br>    <span class="hljs-keyword">protected</span> <span class="hljs-type">boolean</span> <span class="hljs-variable">safeResultHandlerEnabled</span> <span class="hljs-operator">=</span> <span class="hljs-literal">true</span>;<br>    <span class="hljs-keyword">protected</span> <span class="hljs-type">boolean</span> mapUnderscoreToCamelCase;<br>    <span class="hljs-keyword">protected</span> <span class="hljs-type">boolean</span> aggressiveLazyLoading;<br>    <span class="hljs-keyword">protected</span> <span class="hljs-type">boolean</span> <span class="hljs-variable">multipleResultSetsEnabled</span> <span class="hljs-operator">=</span> <span class="hljs-literal">true</span>;<br>    <span class="hljs-keyword">protected</span> <span class="hljs-type">boolean</span> useGeneratedKeys;<br>    <span class="hljs-keyword">protected</span> <span class="hljs-type">boolean</span> <span class="hljs-variable">useColumnLabel</span> <span class="hljs-operator">=</span> <span class="hljs-literal">true</span>;<br>    <span class="hljs-keyword">protected</span> <span class="hljs-type">boolean</span> <span class="hljs-variable">cacheEnabled</span> <span class="hljs-operator">=</span> <span class="hljs-literal">true</span>;<br>    <span class="hljs-keyword">protected</span> <span class="hljs-type">boolean</span> callSettersOnNulls;<br>    <span class="hljs-keyword">protected</span> <span class="hljs-type">boolean</span> <span class="hljs-variable">useActualParamName</span> <span class="hljs-operator">=</span> <span class="hljs-literal">true</span>;<br>    <span class="hljs-keyword">protected</span> <span class="hljs-type">boolean</span> returnInstanceForEmptyRow;<br>    <span class="hljs-keyword">protected</span> <span class="hljs-type">boolean</span> shrinkWhitespacesInSql;<br>    <span class="hljs-keyword">protected</span> <span class="hljs-type">boolean</span> nullableOnForEach;<br>    <span class="hljs-keyword">protected</span> <span class="hljs-type">boolean</span> argNameBasedConstructorAutoMapping;<br>    ……<br>&#125;<br></code></pre></td></tr></table></figure>
<p>这个配置文件有很多配置，具体可以参考 <a href="https://mybatis.net.cn/configuration.html">Mybatis主配置文件</a></p>
<p><strong>作用2：作为容器存储TypeHandler,TypeAlias,Mapper接口信息</strong></p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">Configuration</span> &#123;<br>    <span class="hljs-comment">// 用于注册Mapper接口信息，建立Mapper接口的Class对象和MapperProxyFactory</span><br>    <span class="hljs-comment">// 对象之间的关系，其中MapperProxyFactory对象用于创建Mapper动态代理对象</span><br>    <span class="hljs-keyword">protected</span> <span class="hljs-keyword">final</span> <span class="hljs-type">MapperRegistry</span> <span class="hljs-variable">mapperRegistry</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">MapperRegistry</span>(<span class="hljs-built_in">this</span>);<br><br>    <span class="hljs-comment">// 用于注册MyBatis插件信息，MyBatis插件实际上就是一个拦截器</span><br>    <span class="hljs-keyword">protected</span> <span class="hljs-keyword">final</span> <span class="hljs-type">InterceptorChain</span> <span class="hljs-variable">interceptorChain</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">InterceptorChain</span>();<br><br>    <span class="hljs-comment">// 用于注册所有的TypeHandler，并建立Jdbc类型、JDBC类型与TypeHandler之间的对应关系</span><br>    <span class="hljs-keyword">protected</span> <span class="hljs-keyword">final</span> <span class="hljs-type">TypeHandlerRegistry</span> <span class="hljs-variable">typeHandlerRegistry</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">TypeHandlerRegistry</span>(<span class="hljs-built_in">this</span>);<br><br>    <span class="hljs-comment">// 用于注册所有的类型别名</span><br>    <span class="hljs-keyword">protected</span> <span class="hljs-keyword">final</span> <span class="hljs-type">TypeAliasRegistry</span> <span class="hljs-variable">typeAliasRegistry</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">TypeAliasRegistry</span>();<br>    <span class="hljs-keyword">protected</span> <span class="hljs-keyword">final</span> <span class="hljs-type">LanguageDriverRegistry</span> <span class="hljs-variable">languageRegistry</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">LanguageDriverRegistry</span>();<br><br>    <span class="hljs-comment">// MyBatis将所有的MappedStatement对象注册到该属性中，</span><br>    <span class="hljs-comment">// 其中Key为Mapper的Id，Value为MappedStatement对象</span><br>    <span class="hljs-keyword">protected</span> <span class="hljs-keyword">final</span> Map&lt;String, MappedStatement&gt; mappedStatements = <span class="hljs-keyword">new</span> <span class="hljs-title class_">StrictMap</span>&lt;MappedStatement&gt;(<br>        <span class="hljs-string">&quot;Mapped Statements collection&quot;</span>)<br>        .conflictMessageProducer((savedValue, targetValue) -&gt; <span class="hljs-string">&quot;. please check &quot;</span> + savedValue.getResource() + <span class="hljs-string">&quot; and &quot;</span><br>            + targetValue.getResource());<br>    ……<br>&#125;<br></code></pre></td></tr></table></figure>

<p><strong>作用3：作为Executor，Statementhandler，ResultSetHandler,ParameterHandler组件的工厂类</strong></p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">Configuration</span> &#123;<br>    <span class="hljs-keyword">public</span> Executor <span class="hljs-title function_">newExecutor</span><span class="hljs-params">(Transaction transaction)</span> &#123;<br>        <span class="hljs-keyword">return</span> newExecutor(transaction, defaultExecutorType);<br>    &#125;<br><br>    <span class="hljs-keyword">public</span> ParameterHandler <span class="hljs-title function_">newParameterHandler</span><span class="hljs-params">(MappedStatement mappedStatement, Object parameterObject,</span><br><span class="hljs-params">        BoundSql boundSql)</span> &#123;<br>        <span class="hljs-type">ParameterHandler</span> <span class="hljs-variable">parameterHandler</span> <span class="hljs-operator">=</span> mappedStatement.getLang().<br>        createParameterHandler(mappedStatement, parameterObject, boundSql);<br>        <span class="hljs-keyword">return</span> (ParameterHandler) interceptorChain.pluginAll(parameterHandler);<br>    &#125;<br><br>    <span class="hljs-keyword">public</span> ResultSetHandler <span class="hljs-title function_">newResultSetHandler</span><span class="hljs-params">(Executor executor, MappedStatement mappedStatement, RowBounds rowBounds,</span><br><span class="hljs-params">        ParameterHandler parameterHandler, </span><br><span class="hljs-params">        ResultHandler resultHandler, BoundSql boundSql)</span> &#123;<br>        <span class="hljs-type">ResultSetHandler</span> <span class="hljs-variable">resultSetHandler</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">DefaultResultSetHandler</span>(executor, mappedStatement, parameterHandler,<br>            resultHandler, boundSql, rowBounds);<br>        <span class="hljs-keyword">return</span> (ResultSetHandler) interceptorChain.pluginAll(resultSetHandler);<br>    &#125;<br><br>    <span class="hljs-keyword">public</span> StatementHandler <span class="hljs-title function_">newStatementHandler</span><span class="hljs-params">(Executor executor, </span><br><span class="hljs-params">        MappedStatement mappedStatement,</span><br><span class="hljs-params">        Object parameterObject, RowBounds rowBounds, </span><br><span class="hljs-params">        ResultHandler resultHandler, BoundSql boundSql)</span> &#123;<br>        <span class="hljs-type">StatementHandler</span> <span class="hljs-variable">statementHandler</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">RoutingStatementHandler</span>(executor, mappedStatement, parameterObject,<br>            rowBounds, resultHandler, boundSql);<br>        <span class="hljs-keyword">return</span> (StatementHandler) interceptorChain.pluginAll(statementHandler);<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<h3 id="为什么要通过-Configuration-来创建-Executor等组件"><a href="#为什么要通过-Configuration-来创建-Executor等组件" class="headerlink" title="为什么要通过 Configuration 来创建 Executor等组件"></a>为什么要通过 Configuration 来创建 Executor等组件</h3><p>主要有以下两个原因</p>
<ol>
<li><code>Mybatis</code> 的配置最终都保存在 <code>Configuration</code> 中，而创建 <code>Executor</code>, <code>ParameterHandler</code> 等组件的创建都需要用到配置(不同配置创建的对应实现类可能就不同)</li>
<li><code>Mybatis</code> 的插件机制就是作用在 <code>Executor</code> 等组件上，所以使用工厂模式创建 <code>Executor</code>, <code>StatementHandler</code>,<code>ResultSetHandler</code>,<code>ParameterHandler</code> 组件就比较方便实现拦截逻辑</li>
</ol>
<h3 id="Executor"><a href="#Executor" class="headerlink" title="Executor"></a>Executor</h3><ul>
<li><code>SqlSession</code> 是 <code>MyBatis</code> 提供的操作数据库的 <code>API</code>，但是真正执行 <code>SQL</code> 的是 <code>Executor</code> 组件</li>
<li><code>Executor</code> 接口中定义了对数据库的增删改查方法<ul>
<li><code>query()</code> 和 <code>queryCursor()</code> 用于查询</li>
<li><code>update()</code> 用于新增，删除，修改</li>
</ul>
</li>
</ul>
<p><img src="https://blog-1308475102.cos.ap-guangzhou.myqcloud.com/picture/202312262155346.webp?imageSlim" alt="Executort继承关系"></p>
<ul>
<li><code>BaseExecutor</code> 定义方法的执行流程和通用的处理逻辑(模板方法模式的运用)</li>
<li><code>SimpleExecutor</code> 是基础的 <code>Executor</code>，能够完成基本的增删改查操作</li>
<li><code>ResueExecutor</code> 对 <code>JDBC</code> 中的 <code>Statement</code> 对象做了缓存，当执行相同的 <code>SQL</code> 语句时，直接从缓存中取出 <code>Statement</code> 对象进行复用</li>
<li><code>BatchExecutor</code> 则会对调用同一个 <code>Mapper</code> 执行的 <code>update、insert</code> 和 <code>delete</code> 操作，调用<code>Statement</code> 对象的批量操作功能</li>
<li>当 <code>MyBatis</code> 开启了二级缓存功能时，会使用 <code>CachingExecutor</code> 对 <code>SimpleExecutor</code>、<code>ResueExecutor</code>、<code>BatchExecutor</code>进行装饰（装饰器模式的使用）</li>
</ul>
<h3 id="MappedStatement"><a href="#MappedStatement" class="headerlink" title="MappedStatement"></a>MappedStatement</h3><p><code>MyBatis</code> 通过 <code>MappedStatement</code> 描述 <code>&lt;select|update|insert|delete&gt;</code> 或者 <code>@Select、@Update</code> 等注解配置的 <code>SQL</code> 信息</p>
<p>下面是一个 <code>Mapper</code> 配置文件的示例</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><code class="hljs xml"><span class="hljs-meta">&lt;?xml version=<span class="hljs-string">&quot;1.0&quot;</span> encoding=<span class="hljs-string">&quot;UTF-8&quot;</span> ?&gt;</span><br><span class="hljs-meta">&lt;!DOCTYPE <span class="hljs-keyword">mapper</span></span><br><span class="hljs-meta">        <span class="hljs-keyword">PUBLIC</span> <span class="hljs-string">&quot;-//mybatis.org//DTD Mapper 3.0//EN&quot;</span></span><br><span class="hljs-meta">        <span class="hljs-string">&quot;http://mybatis.org/dtd/mybatis-3-mapper.dtd&quot;</span>&gt;</span><br><span class="hljs-tag">&lt;<span class="hljs-name">mapper</span> <span class="hljs-attr">namespace</span>=<span class="hljs-string">&quot;org.example.mapper.StudentMapper&quot;</span>&gt;</span><br>    <span class="hljs-tag">&lt;<span class="hljs-name">select</span> <span class="hljs-attr">id</span>=<span class="hljs-string">&quot;selectById&quot;</span> <span class="hljs-attr">resultType</span>=<span class="hljs-string">&quot;org.example.vo.Student&quot;</span>&gt;</span><br>        select * from student where id = #&#123;id&#125;<br>    <span class="hljs-tag">&lt;/<span class="hljs-name">select</span>&gt;</span><br><span class="hljs-tag">&lt;/<span class="hljs-name">mapper</span>&gt;</span><br></code></pre></td></tr></table></figure>
<p>每一个标签都有很多的属性可以进行配置，具体的可以参考 <a href="https://mybatis.net.cn/sqlmap-xml.html">Mybatis中xml映射器文件的配置</a></p>
<h3 id="TypeHandler"><a href="#TypeHandler" class="headerlink" title="TypeHandler"></a>TypeHandler</h3>


]]></content>
      <categories>
        <category>java</category>
        <category>Mybatis</category>
      </categories>
      <tags>
        <tag>java</tag>
        <tag>Mybatis</tag>
      </tags>
  </entry>
  <entry>
    <title>Mybatis获取Mapper代理对象的过程分析</title>
    <url>/2023/5cc9f93ce77a/index.html</url>
    <content><![CDATA[<span class="hide_first_mermaid">
<div class="mermaid-wrap"><pre class="mermaid-src" hidden>
  flowchart LR
    A
  </pre></div>
</span>

<blockquote>
<p>Mybatis版本： 3.5.15<br>JDK: 17</p>
</blockquote>
<p>使用 <code>Mapper</code> 接口的示例可以参考</p>
<ol><li><a href="/2023/1d9bedf3f03a/index.html" title="搭建Mybatis源码测试环境">搭建Mybatis源码测试环境</a></li></ol>


<h2 id="获取-Mapper-流程"><a href="#获取-Mapper-流程" class="headerlink" title="获取 Mapper 流程"></a>获取 Mapper 流程</h2><p>获取 <code>Mapper</code> 代理类的示例</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-comment">// sesion 是 DefaultSqlSession</span><br><span class="hljs-type">StudentMapper</span> <span class="hljs-variable">mapper</span> <span class="hljs-operator">=</span> session.getMapper(StudentMapper.class);<br></code></pre></td></tr></table></figure>
<p>获取 <code>Mapper</code> 代理类的整个流程如下</p>
<pre class="mermaid">sequenceDiagram
    participant A as DefaultSqlSession
    participant B as Configuration
    participant C as MapperRegistry
    participant D as MapperProxyFactory
    participant E as Proxy
    A->>B: getMapper(Class)
    B->>C: getMapper(Class,Configuration)
    C->>D: newInstance(SqlSession)
    D->>D: new MapperProxy()
    D->>D: newInstance(MapperProxy)
    D->>E: Proxy.newProxyInstance(MapperProxy)</pre>

<p>最终是通过 <code>Proxy.newProxyInstance()</code> 方法获取的代理类型，也就是 <code>JDK</code> 动态代理，对于 <code>JDK</code> 动态代理，具体可以参考</p>
<ol><li><a href="/2023/92222e5bba3e/index.html" title="JDK动态代理详解">JDK动态代理详解</a></li></ol>
<p>对于 <code>JDK</code> 动态代理，重点是实现了 <code>InvocationHandler</code> 接口的类的 <code>invoke()</code> 方法， <code>MapperProxy</code> 就是 <code>InvocationHandler</code> 的实现类，但是从上面的流程看好像还不是那么简单，还涉及到了 <code>MapperRegistry</code> 和 <code>MapperProxyFactory</code>，我生成一个代理类为什么还需要这两个类呢？</p>
<h2 id="MapperRegistry之Mapper注册流程"><a href="#MapperRegistry之Mapper注册流程" class="headerlink" title="MapperRegistry之Mapper注册流程"></a>MapperRegistry之Mapper注册流程</h2><p>前面说创建代理类的时候使用代码如下</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-type">StudentMapper</span> <span class="hljs-variable">mapper</span> <span class="hljs-operator">=</span> session.getMapper(StudentMapper.class);<br></code></pre></td></tr></table></figure>
<p>那么是任意一个类都可以通过 <code>session.getMapper()</code> 方法来获取代理类么？ 那肯定不是的，因为这个代理类是要满足 <code>Mybatis</code> 执行 <code>Sql</code> 的相关功能，总不能随便一个类都可以通过这种方式获取代理类吧，那哪些类是可以通过这种方式获取的呢？这里就用到了 <code>MapperRegistry</code>, 顾名思义也知道这个就是 <code>Mapper</code> 的注册器，只有已经注册的 <code>Mapper</code> 才能通过这种方式来获取，看看 <code>MapperRegistry</code> 的代码</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">MapperRegistry</span> &#123;<br>    <span class="hljs-comment">// 保存注册的 Mapper 类</span><br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> Map&lt;Class&lt;?&gt;, MapperProxyFactory&lt;?&gt;&gt; knownMappers = <span class="hljs-keyword">new</span> <span class="hljs-title class_">ConcurrentHashMap</span>&lt;&gt;();<br>&#125;<br></code></pre></td></tr></table></figure>
<p><code>MapperRegistry</code> 缓存注册的 <code>Mapper</code> 类时用的 <code>Map</code>, <code>value</code> 类型是 <code>MapperProxyFactory</code>， <code>MapperProxyFactory</code> 是用来生成 <code>MapperProxy</code> 的，而 <code>MapperProxy</code> 实现了 <code>InvocationHandler</code> 接口.</p>
<p><strong>Mapper注册流程图</strong></p>
<pre class="mermaid">graph LR
    A["解析Mybatis
    主配置文件"]
    B["解析Mybatis Mapper文件"]
    C["找到Mapper文件
    关联的Mapper类"]
    D["将Mapper类注册
    到MapperRegistry"]
    A-->B-->C-->D</pre>

<p>之所以可以根据 <code>Mapper</code> 文件找到 <code>Mapper</code> 接口，是因为开发 <code>Mapper</code> 接口需要遵循一定的规范</p>
<ol>
<li><code>Mapper.xml</code> 文件中的 <code>namespace</code> 与 <code>mapper</code> 接口的类路径相同 (根据这一点就可以根据 <code>Mapper.xml</code> 文件找到 <code>Mapper</code> 接口)</li>
<li><code>Mapper</code> 接口方法名和 <code>Mapper.xml</code> 中定义的每个 <code>statement</code> 的 <code>id</code> 相同</li>
<li><code>Mapper</code> 接口方法的参数类型和 <code>mapper.xml</code> 中定义的每个 <code>sql</code> 的 <code>parameterType</code> 的类型相同</li>
<li><code>Mapper</code> 口方法的返回值类型和 <code>mapper.xml</code> 中定义的每个 <code>sql</code> 的 <code>resultType</code> 的类型相同</li>
</ol>
<p><img src="https://s2.loli.net/2023/12/27/F1LhBe4GDYWO2dH.png" alt="Mappe接口规范"><br>具体在代码中根据 <code>namespace</code> 找到 <code>Mapper</code> 接口类型则是在 <code>XMLMapperBuilder#bindMapperForNamespace</code> 方法中</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">XMLMapperBuilder</span> &#123;<br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">bindMapperForNamespace</span><span class="hljs-params">()</span> &#123;<br>        <span class="hljs-type">String</span> <span class="hljs-variable">namespace</span> <span class="hljs-operator">=</span> builderAssistant.getCurrentNamespace();<br>        <span class="hljs-keyword">if</span> (namespace != <span class="hljs-literal">null</span>) &#123;<br>        Class&lt;?&gt; boundType = <span class="hljs-literal">null</span>;<br>        <span class="hljs-keyword">try</span> &#123;<br>            boundType = Resources.classForName(namespace);<br>        &#125; <span class="hljs-keyword">catch</span> (ClassNotFoundException e) &#123;<br>            <span class="hljs-comment">// ignore, bound type is not required</span><br>        &#125;<br>        <span class="hljs-keyword">if</span> (boundType != <span class="hljs-literal">null</span> &amp;&amp; !configuration.hasMapper(boundType)) &#123;<br>            configuration.addLoadedResource(<span class="hljs-string">&quot;namespace:&quot;</span> + namespace);<br>            <span class="hljs-comment">// 这里最终是调用 MapperRegistry 的 addMapper 方法进行注册</span><br>            configuration.addMapper(boundType);<br>        &#125;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>










]]></content>
      <categories>
        <category>java</category>
        <category>Mybatis</category>
      </categories>
      <tags>
        <tag>java</tag>
        <tag>Mybatis</tag>
      </tags>
  </entry>
  <entry>
    <title>Mybatis整合SpringBoot中MapperScan注解的原理分析</title>
    <url>/2023/c801a695ec29/index.html</url>
    <content><![CDATA[<blockquote>
<p>SpringBoot版本: 3.1.7<br>mybatis-spring-boot-starter版本: 3.0.2<br>JDK版本: 17</p>
</blockquote>
<p>单独使用 <code>Mybatis</code> 时核心入口是生成的 <code>Mapper</code> 代理类，可以参考</p>
<ol><li><a href="/2023/5cc9f93ce77a/index.html" title="Mybatis获取Mapper代理对象的过程分析">Mybatis获取Mapper代理对象的过程分析</a></li></ol>

<p><code>Spring</code> 生成 <code>Bean</code> 的两大核心步骤是</p>
<ol>
<li>将类变成 <code>BeanDefinition</code>, 然后注册到 <code>DefaultListableBeanFactory</code> 类的 <code>beanDefinitionMap</code> 中<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">DefaultListableBeanFactory</span> &#123;<br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> Map&lt;String, BeanDefinition&gt; beanDefinitionMap = <span class="hljs-keyword">new</span> <span class="hljs-title class_">ConcurrentHashMap</span>&lt;&gt;(<span class="hljs-number">256</span>);<br>&#125;<br></code></pre></td></tr></table></figure></li>
<li>将 <code>BeanDefinition</code> 变成真正的 <code>Bean</code>, 并且注册到 <code>DefaultListableBeanFactory</code> 类的 <code>singletonObjects</code><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">DefaultListableBeanFactory</span> &#123;<br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> Map&lt;String, Object&gt; singletonObjects = <span class="hljs-keyword">new</span> <span class="hljs-title class_">ConcurrentHashMap</span>&lt;&gt;(<span class="hljs-number">256</span>);<br>&#125;<br></code></pre></td></tr></table></figure></li>
</ol>
<p><code>Mybatis</code> 和 <code>Spring</code> 整合自然是需要将 <code>Mybatis</code> 中一些关键的类交给 <code>Spring</code> 管理，这里主要讲解 <code>MapperScan</code> 注解，也就是 <code>Mapper</code> 的代理类交给 <code>Spring</code> 管理的过程。</p>
<h2 id="Spring-扫描-Mapper接口注册为-BeanDefinition"><a href="#Spring-扫描-Mapper接口注册为-BeanDefinition" class="headerlink" title="Spring 扫描 Mapper接口注册为 BeanDefinition"></a>Spring 扫描 Mapper接口注册为 BeanDefinition</h2><p>这个过程需要先看一下 <code>MapperScan</code> 注解</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-meta">@Retention(RetentionPolicy.RUNTIME)</span><br><span class="hljs-meta">@Target(ElementType.TYPE)</span><br><span class="hljs-meta">@Documented</span><br><span class="hljs-meta">@Import(MapperScannerRegistrar.class)</span><br><span class="hljs-meta">@Repeatable(MapperScans.class)</span><br><span class="hljs-keyword">public</span> <span class="hljs-meta">@interface</span> MapperScan &#123;<br>    <span class="hljs-comment">// 省略代码……</span><br>&#125;<br></code></pre></td></tr></table></figure>

<p>可以看到 <code>MapperScan</code> 中有 <code>Import</code> <code>MapperScannerRegistrar</code> 这个类</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">MapperScannerRegistrar</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">ImportBeanDefinitionRegistrar</span>, ResourceLoaderAware &#123;<br>    <span class="hljs-comment">// 省略代码……</span><br>&#125;<br></code></pre></td></tr></table></figure>

<h3 id="导入ImportBeanDefinitionRegistrar实现类MapperScannerRegistrar"><a href="#导入ImportBeanDefinitionRegistrar实现类MapperScannerRegistrar" class="headerlink" title="导入ImportBeanDefinitionRegistrar实现类MapperScannerRegistrar"></a>导入ImportBeanDefinitionRegistrar实现类MapperScannerRegistrar</h3><p>在解析 <code>SpringBoot</code> 主类的时候，会去找主类上是否有注解引入了 <code>ImportBeanDefinitionRegistrar</code> 的实现类，先看一下主类代码，完整的 <code>Mybatis</code> 整合 <code>SpringBoot</code> 代码可以参考</p>
<ol><li><a href="/2023/c7647f7aa552/index.html" title="SpringBoot3整合Mybatis">SpringBoot3整合Mybatis</a></li></ol>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-meta">@SpringBootApplication</span><br><span class="hljs-meta">@MapperScan(&quot;com.example.testmybatis.mapper&quot;)</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">TestMybatisApplication</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">ApplicationContextAware</span> &#123;<br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> ApplicationContext applicationContext;<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">main</span><span class="hljs-params">(String[] args)</span> &#123;<br>        SpringApplication.run(TestMybatisApplication.class, args);<br>        <span class="hljs-type">StudentMapper</span> <span class="hljs-variable">studentMapper</span> <span class="hljs-operator">=</span> applicationContext.getBean(StudentMapper.class);<br>        <span class="hljs-type">Student</span> <span class="hljs-variable">student</span> <span class="hljs-operator">=</span> studentMapper.selectById(<span class="hljs-number">1</span>);<br>        System.out.println(student);<br><br>    &#125;<br><br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">setApplicationContext</span><span class="hljs-params">(ApplicationContext applicationContextParam)</span> <span class="hljs-keyword">throws</span> BeansException &#123;<br>        applicationContext = applicationContextParam;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<p><strong>解析 <code>SpringBoot</code> 主类时导入 <code>ImportBeanDefinitionRegistrar</code> 实现类的流程</strong><br>直接从 <code>AbstractApplicationContext#refresh</code> 开始，前面步骤省略</p>
<figure class="highlight leaf"><table><tr><td class="code"><pre><code class="hljs leaf">AbstractApplicationContext<span class="hljs-punctuation">#</span><span class="hljs-keyword">refresh</span><br>    AbstractApplicationContext<span class="hljs-punctuation">#</span><span class="hljs-keyword">invokeBeanFactoryPostProcessors</span><span class="hljs-params">(<span class="hljs-variable">ConfigurableListableBeanFactory</span>)</span><br>        PostProcessorRegistrationDelegate<span class="hljs-punctuation">#</span><span class="hljs-keyword">invokeBeanFactoryPostProcessors</span><span class="hljs-params">(<span class="hljs-variable">ConfigurableListableBeanFactory</span>, <span class="hljs-variable">List</span><span class="hljs-operator">&lt;</span><span class="hljs-variable">BeanFactoryPostProcessor</span><span class="hljs-operator">&gt;</span>)</span><br>            PostProcessorRegistrationDelegate<span class="hljs-punctuation">#</span><span class="hljs-keyword">invokeBeanDefinitionRegistryPostProcessors</span><br>                【注：ConfigurationClassPostProcessor 是实现了 BeanDefinitionRegistryPostProcessor 接口的类】<br>                ConfigurationClassPostProcessor<span class="hljs-punctuation">#</span><span class="hljs-keyword">postProcessBeanDefinitionRegistry</span><br>                    ConfigurationClassPostProcessor<span class="hljs-punctuation">#</span><span class="hljs-keyword">processConfigBeanDefinitions</span><br>                        ConfigurationClassBeanDefinitionReader<span class="hljs-punctuation">#</span><span class="hljs-keyword">loadBeanDefinitions</span><br>                            【这里会调用主类的 configClass.getImportBeanDefinitionRegistrars()】<br>                            ConfigurationClassBeanDefinitionReader<span class="hljs-punctuation">#</span><span class="hljs-keyword">loadBeanDefinitionsForConfigurationClass</span><br>                                ConfigurationClassBeanDefinitionReader<span class="hljs-punctuation">#</span><span class="hljs-keyword">loadBeanDefinitionsFromRegistrars</span><br></code></pre></td></tr></table></figure>
<p>从上面的流程中可以看到，核心点是 <code>ConfigurationClassBeanDefinitionReader#loadBeanDefinitionsForConfigurationClass</code> 方法，主要看一下这个方法</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">ConfigurationClassBeanDefinitionReader</span> &#123;<br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">loadBeanDefinitionsForConfigurationClass</span><span class="hljs-params">(</span><br><span class="hljs-params">			ConfigurationClass configClass, TrackedConditionEvaluator trackedConditionEvaluator)</span> &#123;<br>        <span class="hljs-comment">// 省略其他代码……</span><br>        <span class="hljs-comment">// configClass.getImportBeanDefinitionRegistrars() , 这里 configClass 就是主类</span><br>        <span class="hljs-comment">// getImportBeanDefinitionRegistrars() 方法获取的就是主类的注解上 ImportBeanDefinitionRegistrar</span><br>        <span class="hljs-comment">// 接口的实现类</span><br>		loadBeanDefinitionsFromRegistrars(configClass.getImportBeanDefinitionRegistrars());<br>	&#125;<br><br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">loadBeanDefinitionsFromRegistrars</span><span class="hljs-params">(Map&lt;ImportBeanDefinitionRegistrar, AnnotationMetadata&gt; registrars)</span> &#123;<br>		registrars.forEach((registrar, metadata) -&gt;<br>                <span class="hljs-comment">// 这里会调用 ImportBeanDefinitionRegistrar 接口中的registerBeanDefinitions 方法</span><br>				registrar.registerBeanDefinitions(metadata, <span class="hljs-built_in">this</span>.registry, <span class="hljs-built_in">this</span>.importBeanNameGenerator));<br>	&#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<p>上面代码中加载 <code>ImportBeanDefinitionRegistrar</code> 接口的实现类的来源是 <code>configClass.getImportBeanDefinitionRegistrars()</code> 方法的返回值，看一下 <code>ConfigurationClass</code> 类</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">ConfigurationClass</span> &#123;<br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> Map&lt;ImportBeanDefinitionRegistrar, AnnotationMetadata&gt; importBeanDefinitionRegistrars = <span class="hljs-keyword">new</span> <span class="hljs-title class_">LinkedHashMap</span>&lt;&gt;();<br>    <span class="hljs-comment">// 省略其他代码……</span><br>    <span class="hljs-keyword">public</span> List&lt;ImportBeanDefinitionRegistrar&gt; <span class="hljs-title function_">getImportBeanDefinitionRegistrars</span><span class="hljs-params">()</span> &#123;<br>        <span class="hljs-keyword">return</span> <span class="hljs-built_in">this</span>.importBeanDefinitionRegistrars;<br>    &#125;<br><br>    <span class="hljs-keyword">void</span> <span class="hljs-title function_">addImportBeanDefinitionRegistrar</span><span class="hljs-params">(ImportBeanDefinitionRegistrar registrar, AnnotationMetadata importingClassMetadata)</span> &#123;<br>		<span class="hljs-built_in">this</span>.importBeanDefinitionRegistrars.put(registrar, importingClassMetadata);<br>	&#125;<br>&#125;<br></code></pre></td></tr></table></figure>
<p><code>getImportBeanDefinitionRegistrars()</code> 方法只是返回，如果要想知道是什么时候添加的，也很简单，<code>importBeanDefinitionRegistrars</code> 是一个 <code>Map</code>, 所以可以搜索 <code>importBeanDefinitionRegistrars.put(</code>, 然后在这些位置打断点即可，这里直接说结论，其实也是在解析主类时也解析 <code>Import</code> 注解，对于 <code>Import</code> 注解可以参考</p>
<ol><li><a href="/2023/73d35f59945c/index.html" title="Spring中Import注解源码解析">Spring中Import注解源码解析</a></li></ol>
<p>在这里直接给出加载 <code>ImportBeanDefinitionRegistrar</code> 接口实现类的相关代码</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">ConfigurationClassParser</span> &#123;<br>    <span class="hljs-keyword">protected</span> <span class="hljs-keyword">final</span> SourceClass <span class="hljs-title function_">doProcessConfigurationClass</span><span class="hljs-params">(</span><br><span class="hljs-params">			ConfigurationClass configClass, SourceClass sourceClass, Predicate&lt;String&gt; filter)</span><br>			<span class="hljs-keyword">throws</span> IOException &#123;<br>        <span class="hljs-comment">// 省略其他代码……</span><br>		<span class="hljs-comment">// Process any @Import annotations</span><br>		processImports(configClass, sourceClass, getImports(sourceClass), filter, <span class="hljs-literal">true</span>);<br>        <span class="hljs-comment">// 省略其他代码……</span><br>	&#125;<br>&#125;<br></code></pre></td></tr></table></figure>
<p><code>doProcessConfigurationClass</code> 方法就是解析主类的方法，在 <code>processImports()</code> 方法参数中还有一个 <code>getImports()</code> 方法，也就是这个方法获取了主类上的 <code>Import</code> 注解引入的类</p>
<p>**第一部分总结：经过上面的解析可以知道 MapperScan 注解上的 MapperScannerRegistrar 类是怎么被引入的了，就是 解析主类 -&gt; 获取主类上 Import 注解引入的类 -&gt; 处理ImportBeanDefinitionRegistrar接口实现类 **</p>
<h3 id="通过MapperScannerRegistrar导入MapperScannerConfigurer"><a href="#通过MapperScannerRegistrar导入MapperScannerConfigurer" class="headerlink" title="通过MapperScannerRegistrar导入MapperScannerConfigurer"></a>通过MapperScannerRegistrar导入MapperScannerConfigurer</h3><p>经过上面解析已经知道 <code>MapperScannerRegistrar</code> 是怎么被导入到 <code>Spring</code> 中并被 <code>Spring</code> 调用的，接下来就看看 <code>MapperScannerRegistrar</code> 类【部分代码省略，只展示方法中关键代码】</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">MapperScannerRegistrar</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">ImportBeanDefinitionRegistrar</span>, ResourceLoaderAware &#123;<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">registerBeanDefinitions</span><span class="hljs-params">(AnnotationMetadata importingClassMetadata, BeanDefinitionRegistry registry)</span> &#123;<br>        <span class="hljs-type">AnnotationAttributes</span> <span class="hljs-variable">mapperScanAttrs</span> <span class="hljs-operator">=</span> AnnotationAttributes<br>            .fromMap(importingClassMetadata.getAnnotationAttributes(MapperScan.class.getName()));<br>        <span class="hljs-keyword">if</span> (mapperScanAttrs != <span class="hljs-literal">null</span>) &#123;<br>        registerBeanDefinitions(importingClassMetadata, mapperScanAttrs, registry,<br>            generateBaseBeanName(importingClassMetadata, <span class="hljs-number">0</span>));<br>        &#125;<br>    &#125;<br><br>    <span class="hljs-keyword">void</span> <span class="hljs-title function_">registerBeanDefinitions</span><span class="hljs-params">(AnnotationMetadata annoMeta, AnnotationAttributes annoAttrs,</span><br><span class="hljs-params">        BeanDefinitionRegistry registry, String beanName)</span> &#123;<br>        <span class="hljs-comment">// 【重点1】引入 MapperScannerConfigurer 类交给 Spring 管理</span><br>        <span class="hljs-type">BeanDefinitionBuilder</span> <span class="hljs-variable">builder</span> <span class="hljs-operator">=</span> BeanDefinitionBuilder.genericBeanDefinition(MapperScannerConfigurer.class);<br>        builder.addPropertyValue(<span class="hljs-string">&quot;processPropertyPlaceHolders&quot;</span>, <span class="hljs-literal">true</span>);<br><br>        Class&lt;? <span class="hljs-keyword">extends</span> <span class="hljs-title class_">MapperFactoryBean</span>&gt; mapperFactoryBeanClass = annoAttrs.getClass(<span class="hljs-string">&quot;factoryBean&quot;</span>);<br>        <span class="hljs-keyword">if</span> (!MapperFactoryBean.class.equals(mapperFactoryBeanClass)) &#123;<br>        <span class="hljs-comment">// 【重点2】：将这些 Mapper 类的 class 设置为 mapperFactoryBeanClass</span><br>        builder.addPropertyValue(<span class="hljs-string">&quot;mapperFactoryBeanClass&quot;</span>, mapperFactoryBeanClass);<br>        &#125;<br>        <span class="hljs-comment">// 【重点3】： 执行注入逻辑，但是这里的注入是只注入 MapperScannerConfigurer 这个类</span><br>        registry.registerBeanDefinition(beanName, builder.getBeanDefinition());<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<p><code>MapperScannerRegistrar</code> 类的主要作用是将 <code>MapperScannerConfigurer</code> 交给 <code>Spring</code> 管理，所以继续看 <code>MapperScannerConfigurer</code> 类</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">MapperScannerConfigurer</span><br>    <span class="hljs-keyword">implements</span> <span class="hljs-title class_">BeanDefinitionRegistryPostProcessor</span>, InitializingBean, ApplicationContextAware, BeanNameAware &#123;<br>    <br>    <span class="hljs-comment">//  MapperScannerConfigurer 实现了 BeanDefinitionRegistryPostProcessor 接口</span><br>    <span class="hljs-comment">//  所以会调用到 postProcessBeanDefinitionRegistry 方法</span><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">postProcessBeanDefinitionRegistry</span><span class="hljs-params">(BeanDefinitionRegistry registry)</span> &#123;<br>        <span class="hljs-keyword">if</span> (<span class="hljs-built_in">this</span>.processPropertyPlaceHolders) &#123;<br>        processPropertyPlaceHolders();<br>        &#125;<br><br>        <span class="hljs-type">ClassPathMapperScanner</span> <span class="hljs-variable">scanner</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">ClassPathMapperScanner</span>(registry);<br>        scanner.setAddToConfig(<span class="hljs-built_in">this</span>.addToConfig);<br>        scanner.setAnnotationClass(<span class="hljs-built_in">this</span>.annotationClass);<br>        scanner.setMarkerInterface(<span class="hljs-built_in">this</span>.markerInterface);<br>        scanner.setSqlSessionFactory(<span class="hljs-built_in">this</span>.sqlSessionFactory);<br>        scanner.setSqlSessionTemplate(<span class="hljs-built_in">this</span>.sqlSessionTemplate);<br>        scanner.setSqlSessionFactoryBeanName(<span class="hljs-built_in">this</span>.sqlSessionFactoryBeanName);<br>        scanner.setSqlSessionTemplateBeanName(<span class="hljs-built_in">this</span>.sqlSessionTemplateBeanName);<br>        scanner.setResourceLoader(<span class="hljs-built_in">this</span>.applicationContext);<br>        scanner.setBeanNameGenerator(<span class="hljs-built_in">this</span>.nameGenerator);<br>        scanner.setMapperFactoryBeanClass(<span class="hljs-built_in">this</span>.mapperFactoryBeanClass);<br>        <span class="hljs-keyword">if</span> (StringUtils.hasText(lazyInitialization)) &#123;<br>        scanner.setLazyInitialization(Boolean.valueOf(lazyInitialization));<br>        &#125;<br>        <span class="hljs-keyword">if</span> (StringUtils.hasText(defaultScope)) &#123;<br>        scanner.setDefaultScope(defaultScope);<br>        &#125;<br>        scanner.registerFilters();<br>        <span class="hljs-comment">// 【重点4】：执行 Mapper 的扫描逻辑</span><br>        scanner.scan(<br>            StringUtils.tokenizeToStringArray(<span class="hljs-built_in">this</span>.basePackage, ConfigurableApplicationContext.CONFIG_LOCATION_DELIMITERS));<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<p><code>MapperScannerConfigurer</code> 的作用是生成 <code>ClassPathMapperScanner(BeanDefinitionRegistry)</code>, 然后将 <code>Mapper</code> 的扫描工作就交给 <code>ClassPathMapperScanner</code> 完成，所以接下来看看 <code>ClassPathMapperScanner</code> 类</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">ClassPathMapperScanner</span> <span class="hljs-keyword">extends</span> <span class="hljs-title class_">ClassPathBeanDefinitionScanner</span> &#123;<br>    <span class="hljs-keyword">public</span> Set&lt;BeanDefinitionHolder&gt; <span class="hljs-title function_">doScan</span><span class="hljs-params">(String... basePackages)</span> &#123;<br>        Set&lt;BeanDefinitionHolder&gt; beanDefinitions = <span class="hljs-built_in">super</span>.doScan(basePackages);<br>        processBeanDefinitions(beanDefinitions);<br>        <span class="hljs-keyword">return</span> beanDefinitions;<br>    &#125;<br><br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">processBeanDefinitions</span><span class="hljs-params">(Set&lt;BeanDefinitionHolder&gt; beanDefinitions)</span> &#123;<br>        AbstractBeanDefinition definition;<br>        <span class="hljs-type">BeanDefinitionRegistry</span> <span class="hljs-variable">registry</span> <span class="hljs-operator">=</span> getRegistry();<br>        <span class="hljs-keyword">for</span> (BeanDefinitionHolder holder : beanDefinitions) &#123;<br>        definition = (AbstractBeanDefinition) holder.getBeanDefinition();<br>        <span class="hljs-comment">// 【重点】：这里将 BeanDefinition 中的 BeanClass 设置成了 MapperFacotryBean</span><br>        <span class="hljs-comment">// 这样就可以让 Spring 管理 MapperFactoryBean，然后可以通过 MapperFacotryBean 生成 Mapper 代理类</span><br>        definition.setBeanClass(<span class="hljs-built_in">this</span>.mapperFactoryBeanClass);<br>        <span class="hljs-comment">// 省略其他代码……</span><br>        <span class="hljs-keyword">if</span> (!definition.isSingleton()) &#123;<br>            <span class="hljs-type">BeanDefinitionHolder</span> <span class="hljs-variable">proxyHolder</span> <span class="hljs-operator">=</span> ScopedProxyUtils.createScopedProxy(holder, registry, <span class="hljs-literal">true</span>);<br>            <span class="hljs-keyword">if</span> (registry.containsBeanDefinition(proxyHolder.getBeanName())) &#123;<br>            registry.removeBeanDefinition(proxyHolder.getBeanName());<br>            &#125;<br>            registry.registerBeanDefinition(proxyHolder.getBeanName(), proxyHolder.getBeanDefinition());<br>        &#125;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>
<p><code>ClassPathMapperScanner</code> 继承 <code>ClassPathBeanDefinitionScanner</code> 类并且重写了 <code>doScan()</code> 方法，<code>ClassPathBeanDefinitionScanner</code> 是 <code>Spring</code> 扫描 <code>BeanDefinition</code> 的类，<code>Mybatis</code> 继承这个类是为了让大部分的扫描工作直接交给 <code>Spring</code> 完成，同时重写 <code>doScan()</code> 方法是为了给 <code>BeanDefinition</code> 设置一些特殊的属性，比如设置 <code>BeanClass</code>为 <code>mapperFactoryBeanClass</code></p>
<h2 id="通过-MapperFactoryBean-生成-Mapper-代理类"><a href="#通过-MapperFactoryBean-生成-Mapper-代理类" class="headerlink" title="通过 MapperFactoryBean 生成 Mapper 代理类"></a>通过 MapperFactoryBean 生成 Mapper 代理类</h2><p>通过前面的解析可知扫描 <code>Mapper</code> 接口将其解析成 <code>BeanDefinition</code> 后，会将其 <code>BeanClass</code> 设置成 <code>MapperFactoryBeanClass</code>, 接下来看看 <code>MapperFactoryBean</code>(只展示部分代码)</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">MapperFactoryBean</span>&lt;T&gt; <span class="hljs-keyword">extends</span> <span class="hljs-title class_">SqlSessionDaoSupport</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">FactoryBean</span>&lt;T&gt; &#123;<br>    <span class="hljs-keyword">private</span> Class&lt;T&gt; mapperInterface;<br>    <span class="hljs-keyword">public</span> <span class="hljs-title function_">MapperFactoryBean</span><span class="hljs-params">(Class&lt;T&gt; mapperInterface)</span> &#123;<br>        <span class="hljs-built_in">this</span>.mapperInterface = mapperInterface;<br>    &#125;<br><br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> T <span class="hljs-title function_">getObject</span><span class="hljs-params">()</span> <span class="hljs-keyword">throws</span> Exception &#123;<br>        <span class="hljs-keyword">return</span> getSqlSession().getMapper(<span class="hljs-built_in">this</span>.mapperInterface);<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<p><code>MapperFactoryBean</code> 实现了 <code>FactoryBean</code>， 对于 <code>FactoryBean</code> 可以参考</p>
<ol><li><a href="/2023/fdea11462f1f/index.html" title="Spring中FactoryBean详解">Spring中FactoryBean详解</a></li></ol>
<p>直接获取 <code>FactoryBean</code> 对象，实际获取的是 <code>getObject()</code> 方法返回的对象，也就是使用 <code> getSqlSession().getMapper(this.mapperInterface)</code> 返回 <code>Mapper</code> 代理类</p>
]]></content>
      <categories>
        <category>java</category>
        <category>Mybatis</category>
      </categories>
      <tags>
        <tag>java</tag>
        <tag>Mybatis</tag>
      </tags>
  </entry>
  <entry>
    <title>SpringBoot中编写Mybatis插件及Mybatis插件原理</title>
    <url>/2023/c46f5c1f519a/index.html</url>
    <content><![CDATA[<blockquote>
<p>SpringBoot版本: 3.1.7<br>mybatis-spring-boot-starter: 3.0.2</p>
</blockquote>
<h2 id="Mybatis插件的作用"><a href="#Mybatis插件的作用" class="headerlink" title="Mybatis插件的作用"></a>Mybatis插件的作用</h2><p><code>Mybatis</code> 插件主要是对以下四个类上的方法进行拦截</p>
<ul>
<li><code>Executor</code></li>
<li><code>StatementHandler</code></li>
<li><code>ResultSetHandler</code></li>
<li><code>ParameterHandler</code></li>
</ul>
<h2 id="SpringBoot整合Mybatis及自定义插件"><a href="#SpringBoot整合Mybatis及自定义插件" class="headerlink" title="SpringBoot整合Mybatis及自定义插件"></a>SpringBoot整合Mybatis及自定义插件</h2><p>单纯在 <code>Mybatis</code> 中编写插件，可以参考 <a href="https://juejin.cn/post/7205897684623900728">Mybatis插件的使用和原理</a><br>对于在 <code>SpringBoot</code> 使用插件也很简单，可以先参考</p>
<ol><li><a href="/2023/c7647f7aa552/index.html" title="SpringBoot3整合Mybatis">SpringBoot3整合Mybatis</a></li></ol>
<p>然后再编写一个插件类即可</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-meta">@Component</span><br><span class="hljs-meta">@Intercepts(@Signature(type= Executor.class, method=&quot;query&quot;, args=&#123;MappedStatement.class, Object.class, RowBounds.class, ResultHandler.class&#125;))</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">CusPlugin</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">Interceptor</span> &#123;<br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> Object <span class="hljs-title function_">intercept</span><span class="hljs-params">(Invocation invocation)</span> <span class="hljs-keyword">throws</span> Throwable &#123;<br>        System.out.println(<span class="hljs-string">&quot;插件执行前&quot;</span>);<br>        <span class="hljs-type">Object</span> <span class="hljs-variable">proceed</span> <span class="hljs-operator">=</span> invocation.proceed();<br>        System.out.println(<span class="hljs-string">&quot;插件执行后&quot;</span>);<br>        <span class="hljs-keyword">return</span> proceed;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>


]]></content>
      <categories>
        <category>java</category>
        <category>Mybatis</category>
      </categories>
      <tags>
        <tag>java</tag>
        <tag>Mybatis</tag>
      </tags>
  </entry>
  <entry>
    <title>SpringBoot3整合Mybatis</title>
    <url>/2023/c7647f7aa552/index.html</url>
    <content><![CDATA[<blockquote>
<p>SpringBoot版本: 3.1.7<br>MyBatis-Spring-Boot-Starter 3.0.2<br>JDK版本： 17</p>
</blockquote>
<p><a href="https://mybatis.org/spring-boot-starter/mybatis-spring-boot-autoconfigure/">官网Mybatis整合SpringBoot版本对应关系及示例代码</a></p>
<h2 id="引入依赖"><a href="#引入依赖" class="headerlink" title="引入依赖"></a>引入依赖</h2><figure class="highlight xml"><table><tr><td class="code"><pre><code class="hljs xml"><span class="hljs-tag">&lt;<span class="hljs-name">dependency</span>&gt;</span><br>    <span class="hljs-tag">&lt;<span class="hljs-name">groupId</span>&gt;</span>org.mybatis.spring.boot<span class="hljs-tag">&lt;/<span class="hljs-name">groupId</span>&gt;</span><br>    <span class="hljs-tag">&lt;<span class="hljs-name">artifactId</span>&gt;</span>mybatis-spring-boot-starter<span class="hljs-tag">&lt;/<span class="hljs-name">artifactId</span>&gt;</span><br>    <span class="hljs-tag">&lt;<span class="hljs-name">version</span>&gt;</span>3.0.2<span class="hljs-tag">&lt;/<span class="hljs-name">version</span>&gt;</span><br><span class="hljs-tag">&lt;/<span class="hljs-name">dependency</span>&gt;</span><br><span class="hljs-tag">&lt;<span class="hljs-name">dependency</span>&gt;</span><br>    <span class="hljs-tag">&lt;<span class="hljs-name">groupId</span>&gt;</span>mysql<span class="hljs-tag">&lt;/<span class="hljs-name">groupId</span>&gt;</span><br>    <span class="hljs-tag">&lt;<span class="hljs-name">artifactId</span>&gt;</span>mysql-connector-java<span class="hljs-tag">&lt;/<span class="hljs-name">artifactId</span>&gt;</span><br>    <span class="hljs-tag">&lt;<span class="hljs-name">version</span>&gt;</span>8.0.28<span class="hljs-tag">&lt;/<span class="hljs-name">version</span>&gt;</span><br><span class="hljs-tag">&lt;/<span class="hljs-name">dependency</span>&gt;</span><br><span class="hljs-tag">&lt;<span class="hljs-name">dependency</span>&gt;</span><br>    <span class="hljs-tag">&lt;<span class="hljs-name">groupId</span>&gt;</span>org.projectlombok<span class="hljs-tag">&lt;/<span class="hljs-name">groupId</span>&gt;</span><br>    <span class="hljs-tag">&lt;<span class="hljs-name">artifactId</span>&gt;</span>lombok<span class="hljs-tag">&lt;/<span class="hljs-name">artifactId</span>&gt;</span><br>    <span class="hljs-tag">&lt;<span class="hljs-name">version</span>&gt;</span>1.18.24<span class="hljs-tag">&lt;/<span class="hljs-name">version</span>&gt;</span><br>    <span class="hljs-tag">&lt;<span class="hljs-name">scope</span>&gt;</span>provided<span class="hljs-tag">&lt;/<span class="hljs-name">scope</span>&gt;</span><br><span class="hljs-tag">&lt;/<span class="hljs-name">dependency</span>&gt;</span><br></code></pre></td></tr></table></figure>

<h2 id="编写测试代码"><a href="#编写测试代码" class="headerlink" title="编写测试代码"></a>编写测试代码</h2><p>创建表及基本实体，<code>Mapper</code> 类参考</p>
<ol><li><a href="/2023/1d9bedf3f03a/index.html" title="搭建Mybatis源码测试环境">搭建Mybatis源码测试环境</a></li></ol>
<p>这里列下使用到的类</p>
<ul>
<li><code>Studentt</code> 类<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-meta">@Data</span><br><span class="hljs-meta">@ToString</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">Student</span> &#123;<br>    <span class="hljs-keyword">private</span> <span class="hljs-type">long</span> id;<br>    <span class="hljs-keyword">private</span> String name;<br>    <span class="hljs-keyword">private</span> String idCard;<br>&#125;<br></code></pre></td></tr></table></figure></li>
<li><code>StudentMapper</code> 类<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-meta">@Repository</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">interface</span> <span class="hljs-title class_">StudentMapper</span> &#123;<br>    Student <span class="hljs-title function_">selectById</span><span class="hljs-params">(<span class="hljs-type">int</span> id)</span>;<br>&#125;<br></code></pre></td></tr></table></figure></li>
<li><code>studnet.xml</code> 配置文件<figure class="highlight xml"><table><tr><td class="code"><pre><code class="hljs xml"><span class="hljs-meta">&lt;?xml version=<span class="hljs-string">&quot;1.0&quot;</span> encoding=<span class="hljs-string">&quot;UTF-8&quot;</span> ?&gt;</span><br><span class="hljs-meta">&lt;!DOCTYPE <span class="hljs-keyword">mapper</span></span><br><span class="hljs-meta">        <span class="hljs-keyword">PUBLIC</span> <span class="hljs-string">&quot;-//mybatis.org//DTD Mapper 3.0//EN&quot;</span></span><br><span class="hljs-meta">        <span class="hljs-string">&quot;http://mybatis.org/dtd/mybatis-3-mapper.dtd&quot;</span>&gt;</span><br><span class="hljs-tag">&lt;<span class="hljs-name">mapper</span> <span class="hljs-attr">namespace</span>=<span class="hljs-string">&quot;com.example.testmybatis.mapper.StudentMapper&quot;</span>&gt;</span><br>    <span class="hljs-tag">&lt;<span class="hljs-name">select</span> <span class="hljs-attr">id</span>=<span class="hljs-string">&quot;selectById&quot;</span> <span class="hljs-attr">resultType</span>=<span class="hljs-string">&quot;com.example.testmybatis.vo.Student&quot;</span>&gt;</span><br>        select * from student where id = #&#123;id&#125;<br>    <span class="hljs-tag">&lt;/<span class="hljs-name">select</span>&gt;</span><br><span class="hljs-tag">&lt;/<span class="hljs-name">mapper</span>&gt;</span><br></code></pre></td></tr></table></figure></li>
<li>启动类<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-meta">@SpringBootApplication</span><br><span class="hljs-meta">@MapperScan(&quot;com.example.testmybatis.mapper&quot;)</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">TestMybatisApplication</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">ApplicationContextAware</span> &#123;<br><br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> ApplicationContext applicationContext;<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">main</span><span class="hljs-params">(String[] args)</span> &#123;<br>        SpringApplication.run(TestMybatisApplication.class, args);<br>        <span class="hljs-type">StudentMapper</span> <span class="hljs-variable">studentMapper</span> <span class="hljs-operator">=</span> applicationContext.getBean(StudentMapper.class);<br>        <span class="hljs-type">Student</span> <span class="hljs-variable">student</span> <span class="hljs-operator">=</span> studentMapper.selectById(<span class="hljs-number">1</span>);<br>        System.out.println(student);<br><br>    &#125;<br><br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">setApplicationContext</span><span class="hljs-params">(ApplicationContext applicationContextParam)</span> <span class="hljs-keyword">throws</span> BeansException &#123;<br>        applicationContext = applicationContextParam;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure></li>
<li><code>SpringBoot</code> 配置文件<figure class="highlight properties"><table><tr><td class="code"><pre><code class="hljs properties"><span class="hljs-attr">spring.datasource.driver-class-name</span>=<span class="hljs-string">com.mysql.cj.jdbc.Driver</span><br><span class="hljs-attr">spring.datasource.url</span>=<span class="hljs-string">jdbc:mysql://127.0.0.1:3306/test?useUnicode=true&amp;characterEncoding=UTF-8</span><br><span class="hljs-attr">spring.datasource.username</span>=<span class="hljs-string">root</span><br><span class="hljs-attr">spring.datasource.password</span>=<span class="hljs-string">admin</span><br><span class="hljs-comment"></span><br><span class="hljs-comment">#mybatis</span><br><span class="hljs-attr">mybatis.type-aliases-package</span>=<span class="hljs-string">com.example.testmybatis.vo</span><br><span class="hljs-comment"># resource 目录下 Mapper 文件位置</span><br><span class="hljs-attr">mybatis.mapper-locations</span>=<span class="hljs-string">classpath:mapper/*.xml</span><br></code></pre></td></tr></table></figure></li>
</ul>
]]></content>
      <categories>
        <category>java</category>
        <category>Mybatis</category>
      </categories>
      <tags>
        <tag>java</tag>
        <tag>Mybatis</tag>
      </tags>
  </entry>
  <entry>
    <title>搭建Mybatis源码测试环境</title>
    <url>/2023/1d9bedf3f03a/index.html</url>
    <content><![CDATA[<blockquote>
<p>Mybatis版本: 3.5.15<br>JDK版本：17</p>
</blockquote>
<h2 id="下载源码"><a href="#下载源码" class="headerlink" title="下载源码"></a>下载源码</h2><p><a href="https://github.com/mybatis/mybatis-3">下载Mybatis3</a><br><code>Mybatis</code> 源码中会依赖 <code>Mybatis-parent</code>, 如下所示，还需要下载对应版本的 <code>Mybatis-parent</code></p>
<figure class="highlight xml"><table><tr><td class="code"><pre><code class="hljs xml"><span class="hljs-tag">&lt;<span class="hljs-name">parent</span>&gt;</span><br>    <span class="hljs-tag">&lt;<span class="hljs-name">groupId</span>&gt;</span>org.mybatis<span class="hljs-tag">&lt;/<span class="hljs-name">groupId</span>&gt;</span><br>    <span class="hljs-tag">&lt;<span class="hljs-name">artifactId</span>&gt;</span>mybatis-parent<span class="hljs-tag">&lt;/<span class="hljs-name">artifactId</span>&gt;</span><br>    <span class="hljs-tag">&lt;<span class="hljs-name">version</span>&gt;</span>41<span class="hljs-tag">&lt;/<span class="hljs-name">version</span>&gt;</span><br>    <span class="hljs-tag">&lt;<span class="hljs-name">relativePath</span> /&gt;</span><br><span class="hljs-tag">&lt;/<span class="hljs-name">parent</span>&gt;</span><br></code></pre></td></tr></table></figure>
<p><a href="https://github.com/mybatis/parent">下载Mybatis-parent</a></p>
<p>下载完成之后，可以先导入一个项目到 <code>idea</code> 中，然后在 <code>idea</code> 的 <code>Project Structure</code> 面板中将另外一个项目作为 <code>module</code> 导入</p>
<h2 id="安装-Mysql"><a href="#安装-Mysql" class="headerlink" title="安装 Mysql"></a>安装 Mysql</h2><p>参考如下文章，先安装 <code>Mysql</code></p>
<ol><li><a href="/2023/ff432902bea0/index.html" title="Window下安装mysql8解压版">Window下安装mysql8解压版</a></li></ol>

<h3 id="创建表"><a href="#创建表" class="headerlink" title="创建表"></a>创建表</h3><p><strong>先创建数据库</strong></p>
<figure class="highlight sql"><table><tr><td class="code"><pre><code class="hljs sql"><span class="hljs-keyword">create</span> database test;<br></code></pre></td></tr></table></figure>

<p><strong>使用数据库</strong></p>
<figure class="highlight sql"><table><tr><td class="code"><pre><code class="hljs sql">use test;<br></code></pre></td></tr></table></figure>

<p><strong>创建表</strong></p>
<figure class="highlight sql"><table><tr><td class="code"><pre><code class="hljs sql"><span class="hljs-keyword">create</span> <span class="hljs-keyword">table</span> student<br>(<br>    id      <span class="hljs-type">int</span>          <span class="hljs-keyword">not</span> <span class="hljs-keyword">null</span><br>        <span class="hljs-keyword">primary</span> key,<br>    name    <span class="hljs-type">varchar</span>(<span class="hljs-number">100</span>) <span class="hljs-keyword">null</span>,<br>    id_card <span class="hljs-type">varchar</span>(<span class="hljs-number">50</span>)  <span class="hljs-keyword">not</span> <span class="hljs-keyword">null</span>,<br>    <span class="hljs-keyword">constraint</span> s_id_card<br>        <span class="hljs-keyword">unique</span> (id_card)<br>);<br></code></pre></td></tr></table></figure>

<h3 id="添加测试数据"><a href="#添加测试数据" class="headerlink" title="添加测试数据"></a>添加测试数据</h3><figure class="highlight sql"><table><tr><td class="code"><pre><code class="hljs sql"><span class="hljs-keyword">INSERT</span> <span class="hljs-keyword">INTO</span> test.student (id, name, id_card) <span class="hljs-keyword">VALUES</span> (<span class="hljs-number">1</span>, <span class="hljs-string">&#x27;name1&#x27;</span>, <span class="hljs-string">&#x27;1_card&#x27;</span>);<br><span class="hljs-keyword">INSERT</span> <span class="hljs-keyword">INTO</span> test.student (id, name, id_card) <span class="hljs-keyword">VALUES</span> (<span class="hljs-number">5</span>, <span class="hljs-string">&#x27;name5&#x27;</span>, <span class="hljs-string">&#x27;5_card&#x27;</span>);<br><span class="hljs-keyword">INSERT</span> <span class="hljs-keyword">INTO</span> test.student (id, name, id_card) <span class="hljs-keyword">VALUES</span> (<span class="hljs-number">10</span>, <span class="hljs-string">&#x27;name10&#x27;</span>, <span class="hljs-string">&#x27;10_card&#x27;</span>);<br></code></pre></td></tr></table></figure>

<h2 id="创建测试代码"><a href="#创建测试代码" class="headerlink" title="创建测试代码"></a>创建测试代码</h2><h3 id="创建Mybatis核心配置文件"><a href="#创建Mybatis核心配置文件" class="headerlink" title="创建Mybatis核心配置文件"></a>创建Mybatis核心配置文件</h3><p>在 <code>Mybatis</code> 源码项目的 <code>resources</code> 目录下创建 <code>mybatis-config.xml</code> 文件，内容如下：</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><code class="hljs xml"><span class="hljs-meta">&lt;?xml version=<span class="hljs-string">&quot;1.0&quot;</span> encoding=<span class="hljs-string">&quot;UTF-8&quot;</span> ?&gt;</span><br><span class="hljs-meta">&lt;!DOCTYPE <span class="hljs-keyword">configuration</span></span><br><span class="hljs-meta">        <span class="hljs-keyword">PUBLIC</span> <span class="hljs-string">&quot;-//mybatis.org//DTD Config 3.0//EN&quot;</span></span><br><span class="hljs-meta">        <span class="hljs-string">&quot;http://mybatis.org/dtd/mybatis-3-config.dtd&quot;</span>&gt;</span><br><span class="hljs-tag">&lt;<span class="hljs-name">configuration</span>&gt;</span><br>    <span class="hljs-tag">&lt;<span class="hljs-name">environments</span> <span class="hljs-attr">default</span>=<span class="hljs-string">&quot;development&quot;</span>&gt;</span><br>        <span class="hljs-tag">&lt;<span class="hljs-name">environment</span> <span class="hljs-attr">id</span>=<span class="hljs-string">&quot;development&quot;</span>&gt;</span><br>            <span class="hljs-tag">&lt;<span class="hljs-name">transactionManager</span> <span class="hljs-attr">type</span>=<span class="hljs-string">&quot;JDBC&quot;</span>/&gt;</span><br>            <span class="hljs-tag">&lt;<span class="hljs-name">dataSource</span> <span class="hljs-attr">type</span>=<span class="hljs-string">&quot;POOLED&quot;</span>&gt;</span><br>                <span class="hljs-tag">&lt;<span class="hljs-name">property</span> <span class="hljs-attr">name</span>=<span class="hljs-string">&quot;driver&quot;</span> <span class="hljs-attr">value</span>=<span class="hljs-string">&quot;com.mysql.cj.jdbc.Driver&quot;</span>/&gt;</span><br>                <span class="hljs-tag">&lt;<span class="hljs-name">property</span> <span class="hljs-attr">name</span>=<span class="hljs-string">&quot;url&quot;</span> <span class="hljs-attr">value</span>=<span class="hljs-string">&quot;jdbc:mysql://localhost:3306/test?serverTimezone=UTC&quot;</span>/&gt;</span><br>                <span class="hljs-tag">&lt;<span class="hljs-name">property</span> <span class="hljs-attr">name</span>=<span class="hljs-string">&quot;username&quot;</span> <span class="hljs-attr">value</span>=<span class="hljs-string">&quot;root&quot;</span>/&gt;</span><br>                <span class="hljs-tag">&lt;<span class="hljs-name">property</span> <span class="hljs-attr">name</span>=<span class="hljs-string">&quot;password&quot;</span> <span class="hljs-attr">value</span>=<span class="hljs-string">&quot;admin&quot;</span>/&gt;</span><br>            <span class="hljs-tag">&lt;/<span class="hljs-name">dataSource</span>&gt;</span><br>        <span class="hljs-tag">&lt;/<span class="hljs-name">environment</span>&gt;</span><br>    <span class="hljs-tag">&lt;/<span class="hljs-name">environments</span>&gt;</span><br>    <span class="hljs-tag">&lt;<span class="hljs-name">mappers</span>&gt;</span><br>        <span class="hljs-tag">&lt;<span class="hljs-name">mapper</span> <span class="hljs-attr">resource</span>=<span class="hljs-string">&quot;mapper/student.xml&quot;</span>/&gt;</span><br>    <span class="hljs-tag">&lt;/<span class="hljs-name">mappers</span>&gt;</span><br><span class="hljs-tag">&lt;/<span class="hljs-name">configuration</span>&gt;</span><br></code></pre></td></tr></table></figure>

<h3 id="创建Mybatis映射文件和实现类"><a href="#创建Mybatis映射文件和实现类" class="headerlink" title="创建Mybatis映射文件和实现类"></a>创建Mybatis映射文件和实现类</h3><p><img src="https://s2.loli.net/2023/12/26/ftVYKd8b9MgvUNp.png" alt="测试类目录和结构"></p>
<p><strong>实体类 Student</strong></p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-meta">@Data</span><br><span class="hljs-meta">@ToString</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">Student</span> &#123;<br>    <span class="hljs-keyword">private</span> <span class="hljs-type">long</span> id;<br>    <span class="hljs-keyword">private</span> String name;<br>    <span class="hljs-keyword">private</span> String idCard;<br>&#125;<br></code></pre></td></tr></table></figure>

<p><strong>StudentMapper类</strong></p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">interface</span> <span class="hljs-title class_">StudentMapper</span> &#123;<br>    Student <span class="hljs-title function_">selectById</span><span class="hljs-params">(<span class="hljs-type">int</span> id)</span>;<br>&#125;<br></code></pre></td></tr></table></figure>

<p><strong>Mapper映射文件</strong></p>
<figure class="highlight xml"><table><tr><td class="code"><pre><code class="hljs xml"><span class="hljs-meta">&lt;?xml version=<span class="hljs-string">&quot;1.0&quot;</span> encoding=<span class="hljs-string">&quot;UTF-8&quot;</span> ?&gt;</span><br><span class="hljs-meta">&lt;!DOCTYPE <span class="hljs-keyword">mapper</span></span><br><span class="hljs-meta">        <span class="hljs-keyword">PUBLIC</span> <span class="hljs-string">&quot;-//mybatis.org//DTD Mapper 3.0//EN&quot;</span></span><br><span class="hljs-meta">        <span class="hljs-string">&quot;http://mybatis.org/dtd/mybatis-3-mapper.dtd&quot;</span>&gt;</span><br><span class="hljs-tag">&lt;<span class="hljs-name">mapper</span> <span class="hljs-attr">namespace</span>=<span class="hljs-string">&quot;org.example.mapper.StudentMapper&quot;</span>&gt;</span><br>    <span class="hljs-tag">&lt;<span class="hljs-name">select</span> <span class="hljs-attr">id</span>=<span class="hljs-string">&quot;selectById&quot;</span> <span class="hljs-attr">resultType</span>=<span class="hljs-string">&quot;org.example.vo.Student&quot;</span>&gt;</span><br>        select * from student where id = #&#123;id&#125;<br>    <span class="hljs-tag">&lt;/<span class="hljs-name">select</span>&gt;</span><br><span class="hljs-tag">&lt;/<span class="hljs-name">mapper</span>&gt;</span><br></code></pre></td></tr></table></figure>

<p><strong>测试类</strong><br><img src="https://s2.loli.net/2023/12/26/hWT4Ne7g98XsQkr.png" alt="测试类结构"></p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">TestMybatis</span> &#123;<br><br>    SqlSessionFactory sqlSessionFactory;<br><br>    <span class="hljs-meta">@Before</span><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">before</span><span class="hljs-params">()</span> <span class="hljs-keyword">throws</span> IOException &#123;<br>        <span class="hljs-type">String</span> <span class="hljs-variable">resource</span> <span class="hljs-operator">=</span> <span class="hljs-string">&quot;mybatis-config.xml&quot;</span>;<br>        <span class="hljs-type">InputStream</span> <span class="hljs-variable">inputStream</span> <span class="hljs-operator">=</span> Resources.getResourceAsStream(resource);<br>        sqlSessionFactory = <span class="hljs-keyword">new</span> <span class="hljs-title class_">SqlSessionFactoryBuilder</span>().build(inputStream);<br>    &#125;<br><br>    <span class="hljs-meta">@Test</span><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">selectById</span><span class="hljs-params">()</span> &#123;<br>        <span class="hljs-keyword">try</span> (<span class="hljs-type">SqlSession</span> <span class="hljs-variable">session</span> <span class="hljs-operator">=</span> sqlSessionFactory.openSession()) &#123;<br>            <span class="hljs-type">StudentMapper</span> <span class="hljs-variable">mapper</span> <span class="hljs-operator">=</span> session.getMapper(StudentMapper.class);<br>            <span class="hljs-type">Student</span> <span class="hljs-variable">student</span> <span class="hljs-operator">=</span> mapper.selectById(<span class="hljs-number">1</span>);<br>            System.out.println(student);<br>        &#125;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<p><strong>执行结果</strong></p>
<figure class="highlight routeros"><table><tr><td class="code"><pre><code class="hljs routeros">Student(<span class="hljs-attribute">id</span>=1, <span class="hljs-attribute">name</span>=name1, <span class="hljs-attribute">idCard</span>=<span class="hljs-literal">null</span>)<br></code></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>java</category>
        <category>Mybatis</category>
      </categories>
      <tags>
        <tag>java</tag>
        <tag>Mybatis</tag>
      </tags>
  </entry>
  <entry>
    <title>BeanFacotry的后置处理器详解</title>
    <url>/2023/e7d5d1305e5d/index.html</url>
    <content><![CDATA[<span class="hide_first_mermaid">
<div class="mermaid-wrap"><pre class="mermaid-src" hidden>
  flowchart LR
    A
  </pre></div>
</span>

<p><strong>Spring 版本： 6.0.6</strong></p>
<h2 id="BeanFactory-的后置处理器的作用"><a href="#BeanFactory-的后置处理器的作用" class="headerlink" title="BeanFactory 的后置处理器的作用"></a>BeanFactory 的后置处理器的作用</h2><p><code>BeanFactory</code> 的后置处理器（<code>PostProcessor</code>）是用于在 <code>BeanFactory</code> 加载和创建 <code>bean</code> 过程中对 <code>bean</code> 进行进一步处理的接口。它提供了一种机制，可以在 <code>Spring</code> 容器实例化 <code>bean</code> 之前对 <code>bean</code> 进行定制、修改或扩展</p>
<h2 id="两种-BeanFactory-的接口类型"><a href="#两种-BeanFactory-的接口类型" class="headerlink" title="两种 BeanFactory 的接口类型"></a>两种 BeanFactory 的接口类型</h2><h3 id="BeanFactoryPostProcessor"><a href="#BeanFactoryPostProcessor" class="headerlink" title="BeanFactoryPostProcessor"></a>BeanFactoryPostProcessor</h3><p>可以通过 <code>ConfigurableListableBeanFactory</code> 对 <code>BeanDefinition</code> 进行修改，这样就会改变 <code>bean</code> 的定义</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-meta">@FunctionalInterface</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">interface</span> <span class="hljs-title class_">BeanFactoryPostProcessor</span> &#123;<br>	<span class="hljs-keyword">void</span> <span class="hljs-title function_">postProcessBeanFactory</span><span class="hljs-params">(ConfigurableListableBeanFactory beanFactory)</span> <span class="hljs-keyword">throws</span> BeansException;<br>&#125;<br></code></pre></td></tr></table></figure>

<h3 id="BeanDefinitionRegistryPostProcessor"><a href="#BeanDefinitionRegistryPostProcessor" class="headerlink" title="BeanDefinitionRegistryPostProcessor"></a>BeanDefinitionRegistryPostProcessor</h3><p><code>BeanDefinitionRegistryPostProcessor</code> 继承了 <code>BeanFactoryPostProcessor</code> 接口，同时增加了一个接口，该接口提供了 <code>BeanDefinitionRegistry</code> 参数，也就是可以动态注入一些 <code>BeanDefinition</code>， 在 <code>Spring</code> 中典型的应用是 <code>ConfigurationClassPostProcessor</code>，后面会分析这个类</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">interface</span> <span class="hljs-title class_">BeanDefinitionRegistryPostProcessor</span> <span class="hljs-keyword">extends</span> <span class="hljs-title class_">BeanFactoryPostProcessor</span> &#123;<br>	<span class="hljs-keyword">void</span> <span class="hljs-title function_">postProcessBeanDefinitionRegistry</span><span class="hljs-params">(BeanDefinitionRegistry registry)</span> <span class="hljs-keyword">throws</span> BeansException;<br>&#125;<br></code></pre></td></tr></table></figure>

<h2 id="BeanFactory-后置处理器的使用示例"><a href="#BeanFactory-后置处理器的使用示例" class="headerlink" title="BeanFactory 后置处理器的使用示例"></a>BeanFactory 后置处理器的使用示例</h2><h3 id="使用示例类说明"><a href="#使用示例类说明" class="headerlink" title="使用示例类说明"></a>使用示例类说明</h3><ol>
<li><code>CusBeanFactoryPostProcessor</code> 该类实现了 <code>BeanFactoryPostProcessor</code> 接口并且标有 <code>@Component</code> 注解</li>
<li><code>CusBeanDefinitionRegistryPostProcessor</code> 该类实现了 <code>BeanDefinitionRegistryPostProcessor</code> 接口并且标有 <code>@Component</code> 注解</li>
<li><code>RegisterComponent</code> 普通类，会通过 <code>CusBeanDefinitionRegistryPostProcessor</code> 注入到 <code>Spring</code> 容器</li>
<li><code>ScanComponent</code> 标有 <code>@Component</code> 和 <code>@Scope(&quot;prototype&quot;)</code>, 会通过 <code>CusBeanFactoryPostProcessor</code> 将 <code>scope</code> 从 <code>prototype</code> 改为 <code>singleton</code></li>
<li><code>BeanFactoryPostProcessMain</code> 主类，标有 <code>@Configuration</code> 和 <code>@ComponentScan</code></li>
</ol>
<p>上述所有类都放在同一个包中</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-comment">//  普通类，并没有增加spring注解，会通过 CusBeanDefinitionRegistryPostProcessor 注入到 Spring 容器</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">RegisterComponent</span> &#123;<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">sayHello</span><span class="hljs-params">()</span> &#123;<br>        System.out.println(<span class="hljs-string">&quot;RegisterComponent.sayHello&quot;</span>);<br>    &#125;<br>&#125;<br><br><span class="hljs-comment">// 实现了BeanDefinitionRegistryPostProcessor接口，会被spring扫描，并且注入了普通类 RegisterComponent</span><br><span class="hljs-meta">@Component</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">CusBeanDefinitionRegistryPostProcessor</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">BeanDefinitionRegistryPostProcessor</span> &#123;<br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">postProcessBeanDefinitionRegistry</span><span class="hljs-params">(BeanDefinitionRegistry registry)</span> <span class="hljs-keyword">throws</span> BeansException &#123;<br>        <span class="hljs-type">AbstractBeanDefinition</span> <span class="hljs-variable">beanDefinition</span> <span class="hljs-operator">=</span> BeanDefinitionBuilder.genericBeanDefinition(RegisterComponent.class).getBeanDefinition();<br>        registry.registerBeanDefinition(<span class="hljs-string">&quot;registerComponent&quot;</span>, beanDefinition);<br>        System.out.println(<span class="hljs-string">&quot;CusBeanDefinitionRegistryPostProcessor postProcessBeanDefinitionRegistry&quot;</span>);<br>    &#125;<br><br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">postProcessBeanFactory</span><span class="hljs-params">(ConfigurableListableBeanFactory beanFactory)</span> <span class="hljs-keyword">throws</span> BeansException &#123;<br>        System.out.println(<span class="hljs-string">&quot;CusBeanDefinitionRegistryPostProcessor postProcessBeanFactory&quot;</span>);<br>    &#125;<br>&#125;<br><br><span class="hljs-comment">//  被spring管理的类，如果没有任何处理会是 prototype 作用域， 这里会通过 CusBeanFactoryPostProcessor 将 scope 改为 singleton</span><br><span class="hljs-meta">@Component</span><br><span class="hljs-meta">@Scope(&quot;prototype&quot;)</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">ScanComponent</span> &#123;<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">sayHello</span><span class="hljs-params">()</span> &#123;<br>        System.out.println(<span class="hljs-string">&quot;BeanFactoryDemo1 sayHello&quot;</span>);<br>    &#125;<br>&#125;<br><br><span class="hljs-comment">// 实现了BeanFactoryPostProcessor接口，会将scanComponent类的scope从prototype改为singleton</span><br><span class="hljs-meta">@Component</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">CusBeanFactoryPostProcessor</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">BeanFactoryPostProcessor</span> &#123;<br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">postProcessBeanFactory</span><span class="hljs-params">(ConfigurableListableBeanFactory beanFactory)</span> <span class="hljs-keyword">throws</span> BeansException &#123;<br>        <span class="hljs-type">BeanDefinition</span> <span class="hljs-variable">scanComponent</span> <span class="hljs-operator">=</span> beanFactory.getBeanDefinition(<span class="hljs-string">&quot;scanComponent&quot;</span>);<br>        scanComponent.setScope(<span class="hljs-string">&quot;singleton&quot;</span>);<br>        System.out.println(<span class="hljs-string">&quot;CusBeanFactoryPostProcessor postProcessBeanFactory&quot;</span>);<br>    &#125;<br>&#125;<br><br><span class="hljs-comment">// 配置主类</span><br><span class="hljs-meta">@Configuration</span><br><span class="hljs-meta">@ComponentScan</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">BeanFactoryPostProcessMain</span> &#123;<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">main</span><span class="hljs-params">(String[] args)</span> &#123;<br>        <span class="hljs-type">ApplicationContext</span> <span class="hljs-variable">context</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">AnnotationConfigApplicationContext</span>(BeanFactoryPostProcessMain.class);<br>        <span class="hljs-comment">// BeanFactoryPostProcessor 的测试（修改了 ScanComponent 类的 scope 属性）</span><br>        <span class="hljs-type">ScanComponent</span> <span class="hljs-variable">bean</span> <span class="hljs-operator">=</span> context.getBean(ScanComponent.class);<br>        System.out.println(bean);<br>        <span class="hljs-type">ScanComponent</span> <span class="hljs-variable">bean2</span> <span class="hljs-operator">=</span> context.getBean(ScanComponent.class);<br>        System.out.println(bean2);<br><br>        <span class="hljs-type">RegisterComponent</span> <span class="hljs-variable">registerComponent</span> <span class="hljs-operator">=</span> context.getBean(RegisterComponent.class);<br>        registerComponent.sayHello(); <span class="hljs-comment">// 会打印 &quot;RegisterComponent.sayHello&quot;</span><br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<p>执行结果</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java">CusBeanDefinitionRegistryPostProcessor postProcessBeanDefinitionRegistry<br>CusBeanDefinitionRegistryPostProcessor postProcessBeanFactory<br>CusBeanFactoryPostProcessor postProcessBeanFactory<br>org.example.beanfactorypost.ScanComponent@5e1fa5b1<br>org.example.beanfactorypost.ScanComponent@5e1fa5b1<br>RegisterComponent.sayHello<br></code></pre></td></tr></table></figure>

<p>从上面执行结果可以得出如下几个结论：</p>
<ol>
<li>先执行实现了 <code>BeanDefinitionRegistryPostProcessor</code> 接口的类，然后才是执行的 <code>BeanFactoryPostProcessor</code> 接口实现类</li>
<li><code>BeanDefinitionRegistryPostProcessor</code> 实现类确实可以注册普通对象成为 <code>bean</code>, 比如上面注册了普通对象 <code>RegisterComponent</code></li>
<li><code>CusBeanFactoryPostProcessor</code> 类已经将 <code>ScanComponent</code> 的 <code>scope</code> 属性改成 <code>singleton</code>, 因为两次 <code>getBean</code> 获取的是同一个对象</li>
</ol>
<h2 id="BeanFactory-后置处理器的执行时机"><a href="#BeanFactory-后置处理器的执行时机" class="headerlink" title="BeanFactory 后置处理器的执行时机"></a>BeanFactory 后置处理器的执行时机</h2><p>参考 <ol><li><a href="/2023/b93a520f6ddc/index.html" title="Spring上下文refresh方法解析">Spring上下文refresh方法解析</a></li></ol> 中写到的 <code>refresh()</code> 方法的第五步，就是执行后置处理器</p>
<p>执行流程如下</p>
<pre class="mermaid">sequenceDiagram
    participant A as AnnotationConfigApplicationContext
    participant B as AbstractApplicationContext
    participant C as PostProcessorRegistrationDelegate
    A->>A: new AnnotationConfigApplicationContext(classes)
    A->>B: refresh()
    B->>C: invokeBeanFactoryPostProcessors()
    C->>C: invokeBeanFactoryPostProcessors()</pre>

<p>所以最终是执行到 <code>public PostProcessorRegistrationDelegate#invokeBeanFactoryPostProcessors()</code> 方法 ， 代码这里就不贴了，这里总结一下</p>
<ol>
<li>执行实现了 <code>BeanDefinitionRegistryPostProcessors</code> 和 <code>PriorityOrdered</code> 的类的<code>postProcessBeanDefinitionRegistry()</code> 方法</li>
<li>执行实现了 <code>BeanDefinitionRegistryPostProcessors</code> 和 <code>Ordered</code> 的类的<code>postProcessBeanDefinitionRegistry()</code> 方法</li>
<li>执行实现了 <code>BeanDefinitionRegistryPostProcessors</code> 并且在 <strong>前面两步中没有执行过的类</strong></li>
<li>对于实现了 <code>BeanFactoryPostProcessor</code> 的类，调用流程也是类似，执行实现了 <code>PriorityOrdered</code> 的类 -&gt; 执行实现了 <code>Ordered</code> 的类 -&gt; 执行剩下的类</li>
</ol>
<h2 id="BeanDefinitionRegistryPostProcessor-典型实现类-ConfigurationClassPostProcessor"><a href="#BeanDefinitionRegistryPostProcessor-典型实现类-ConfigurationClassPostProcessor" class="headerlink" title="BeanDefinitionRegistryPostProcessor 典型实现类 ConfigurationClassPostProcessor"></a>BeanDefinitionRegistryPostProcessor 典型实现类 ConfigurationClassPostProcessor</h2><p>在 <code>Spring</code> 内部也是有很多实现了 <code>BeanDefinitionRegistryPostProcessor</code> 接口的类，比如 <code>ConfigurationClassPostProcessor</code> 类，下面就来看看这个类</p>
<ol>
<li>先说明一下 <code>ConfigurationClassPostProcessor</code> 这个类做的事情，在 <code>Spring</code> 中 <code>@ComponentScan</code> 注解可以批量扫描类被 <code>Spring</code> 管理，但是注解只是起到一个标记的作用，需要有地方来解析这个注解才行，而这个解析的源头就是 <code>ConfigurationClassPostProcessor</code>，真正解析是在 <code>ConfigurationClassParser</code> 中</li>
<li>前面有说过 <code>BeanDefinitionRegistryPostProcessors</code> 接口的实现类可以批量注册 <code>BeanDefinition</code> 被 <code>Spring</code> 管理，在 <code>ConfigurationClassParser</code> 中将 <code>@ComponentScan</code> 涉及到的类都解析出来之后，刚好就可以注册到 <code>Spring</code> 容器中</li>
</ol>
<p>代码执行流程如下</p>
<figure class="highlight axapta"><table><tr><td class="code"><pre><code class="hljs axapta">AbstractApplicationContext<span class="hljs-meta">#refresh</span><br>    AbstractApplicationContext<span class="hljs-meta">#invokeBeanFactoryPostProcessors</span><br>        <span class="hljs-keyword">public</span> PostProcessorRegistrationDelegate<span class="hljs-meta">#invokeBeanFactoryPostProcessors</span><br>            <span class="hljs-keyword">private</span> PostProcessorRegistrationDelegate<span class="hljs-meta">#invokeBeanDefinitionRegistryPostProcessors</span><br>                ConfigurationClassPostProcessor<span class="hljs-meta">#postProcessBeanDefinitionRegistry</span><br>                    ConfigurationClassPostProcessor<span class="hljs-meta">#processConfigBeanDefinitions</span><br>                        ConfigurationClassParser<span class="hljs-meta">#parse</span><br>                            ComponentScanAnnotationParser<span class="hljs-meta">#parse</span><br></code></pre></td></tr></table></figure>

<p>上面是代码的整体执行流程， 最终是通过 <code>ComponentScanAnnotationParser</code> 来进行解析 <code>@ComponentScan</code> 注解的</p>
<h3 id="问题"><a href="#问题" class="headerlink" title="问题"></a>问题</h3><p>上面代码执行流程中，在 <code>PostProcessorRegistrationDelegate#invokeBeanDefinitionRegistryPostProcessors</code> 中可以得到 <code>ConfigurationClassPostProcessor</code> 类，但是这个类我们并没有手动进行注册。</p>
<p>在 <code>AbstractApplicationContext</code> 中有 <code>addBeanFactoryPostProcessor()</code> 可以手动注册</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">abstract</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">AbstractApplicationContext</span> <span class="hljs-keyword">extends</span> <span class="hljs-title class_">DefaultResourceLoader</span><br>		<span class="hljs-keyword">implements</span> <span class="hljs-title class_">ConfigurableApplicationContext</span> &#123;<br>     <span class="hljs-meta">@Override</span><br>	<span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">addBeanFactoryPostProcessor</span><span class="hljs-params">(BeanFactoryPostProcessor postProcessor)</span> &#123;<br>		Assert.notNull(postProcessor, <span class="hljs-string">&quot;BeanFactoryPostProcessor must not be null&quot;</span>);<br>		<span class="hljs-built_in">this</span>.beanFactoryPostProcessors.add(postProcessor);<br>	&#125;   <br>&#125;<br></code></pre></td></tr></table></figure>

<p>所以肯定是 <code>Spring</code> 在哪个步骤中自己注册了，要想找到在哪里注册的也不难，那就是在 <code>ConfigurationClassPostProcessor</code> 类的 <code>postProcessBeanDefinitionRegistry()</code> 方法中打断点，然后看调用栈，就可以知道 <code>ConfigurationClassPostProcessor</code> 对象是从哪里获取的，不同的上下文设置的方式可能不同，在给出结论之前再看一下我们的主类测试代码</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-meta">@Configuration</span><br><span class="hljs-meta">@ComponentScan</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">BeanFactoryPostProcessMain</span> &#123;<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">main</span><span class="hljs-params">(String[] args)</span> &#123;<br>        <span class="hljs-type">AnnotationConfigApplicationContext</span> <span class="hljs-variable">context</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">AnnotationConfigApplicationContext</span>(BeanFactoryPostProcessMain.class);<br>        <span class="hljs-comment">// BeanFactoryPostProcessor 的测试（修改了 ScanComponent 类的 scope 属性）</span><br>        <span class="hljs-type">ScanComponent</span> <span class="hljs-variable">bean</span> <span class="hljs-operator">=</span> context.getBean(ScanComponent.class);<br>        System.out.println(bean);<br>        <span class="hljs-type">ScanComponent</span> <span class="hljs-variable">bean2</span> <span class="hljs-operator">=</span> context.getBean(ScanComponent.class);<br>        System.out.println(bean2);<br><br>        <span class="hljs-type">RegisterComponent</span> <span class="hljs-variable">registerComponent</span> <span class="hljs-operator">=</span> context.getBean(RegisterComponent.class);<br>        registerComponent.sayHello(); <span class="hljs-comment">// 会打印 &quot;RegisterComponent.sayHello&quot;</span><br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<p>从测试代码中看到上下文对象是 <code>AnnotationConfigApplicationContext</code>, 所以下面给出的结论也是基于这个上下文对象, 下面代码执行流程使用序号进行标记</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-comment">// new AnnotationConfigApplicationContext(classes) </span><br><span class="hljs-keyword">public</span> <span class="hljs-title function_">AnnotationConfigApplicationContext</span><span class="hljs-params">(Class&lt;?&gt;... componentClasses)</span> &#123;<br>    <span class="hljs-built_in">this</span>();  <span class="hljs-comment">// 1</span><br>    register(componentClasses);<br>    refresh();<br>&#125;<br><br><span class="hljs-comment">// AnnotationConfigApplicationContext 类</span><br><span class="hljs-keyword">public</span> <span class="hljs-title function_">AnnotationConfigApplicationContext</span><span class="hljs-params">()</span> &#123;<br>    <span class="hljs-type">StartupStep</span> <span class="hljs-variable">createAnnotatedBeanDefReader</span> <span class="hljs-operator">=</span> <span class="hljs-built_in">this</span>.getApplicationStartup().start(<span class="hljs-string">&quot;spring.context.annotated-bean-reader.create&quot;</span>);<br>    <span class="hljs-built_in">this</span>.reader = <span class="hljs-keyword">new</span> <span class="hljs-title class_">AnnotatedBeanDefinitionReader</span>(<span class="hljs-built_in">this</span>); <span class="hljs-comment">// 2</span><br>    createAnnotatedBeanDefReader.end();<br>    <span class="hljs-built_in">this</span>.scanner = <span class="hljs-keyword">new</span> <span class="hljs-title class_">ClassPathBeanDefinitionScanner</span>(<span class="hljs-built_in">this</span>);<br>&#125;<br><br><span class="hljs-comment">// AnnotatedBeanDefinitionReader 类</span><br><span class="hljs-keyword">public</span> <span class="hljs-title function_">AnnotatedBeanDefinitionReader</span><span class="hljs-params">(BeanDefinitionRegistry registry)</span> &#123;<br>    <span class="hljs-built_in">this</span>(registry, getOrCreateEnvironment(registry)); <span class="hljs-comment">// 3</span><br>&#125;<br> <br><span class="hljs-comment">// AnnotatedBeanDefinitionReader 类</span><br><span class="hljs-keyword">public</span> <span class="hljs-title function_">AnnotatedBeanDefinitionReader</span><span class="hljs-params">(BeanDefinitionRegistry registry, Environment environment)</span> &#123;<br>    Assert.notNull(registry, <span class="hljs-string">&quot;BeanDefinitionRegistry must not be null&quot;</span>);<br>    Assert.notNull(environment, <span class="hljs-string">&quot;Environment must not be null&quot;</span>);<br>    <span class="hljs-built_in">this</span>.registry = registry;<br>    <span class="hljs-built_in">this</span>.conditionEvaluator = <span class="hljs-keyword">new</span> <span class="hljs-title class_">ConditionEvaluator</span>(registry, environment, <span class="hljs-literal">null</span>);<br>    AnnotationConfigUtils.registerAnnotationConfigProcessors(<span class="hljs-built_in">this</span>.registry); <span class="hljs-comment">// 4</span><br>&#125;<br><br><span class="hljs-comment">// AnnotationConfigUtils 类</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">registerAnnotationConfigProcessors</span><span class="hljs-params">(BeanDefinitionRegistry registry)</span> &#123;<br>    registerAnnotationConfigProcessors(registry, <span class="hljs-literal">null</span>); <span class="hljs-comment">// 5</span><br>&#125;<br><br><span class="hljs-comment">// AnnotationConfigUtils 类</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> Set&lt;BeanDefinitionHolder&gt; <span class="hljs-title function_">registerAnnotationConfigProcessors</span><span class="hljs-params">(</span><br><span class="hljs-params">			BeanDefinitionRegistry registry, <span class="hljs-meta">@Nullable</span> Object source)</span> &#123;<br>    <span class="hljs-comment">// public static final String CONFIGURATION_ANNOTATION_PROCESSOR_BEAN_NAME = &quot;org.springframework.context.annotation.internalConfigurationAnnotationProcessor&quot;;</span><br>    <span class="hljs-comment">// 省略</span><br>    <span class="hljs-keyword">if</span> (!registry.containsBeanDefinition(CONFIGURATION_ANNOTATION_PROCESSOR_BEAN_NAME)) &#123;<br>        <span class="hljs-type">RootBeanDefinition</span> <span class="hljs-variable">def</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">RootBeanDefinition</span>(ConfigurationClassPostProcessor.class);<br>        def.setSource(source);<br>        beanDefs.add(registerPostProcessor(registry, def, CONFIGURATION_ANNOTATION_PROCESSOR_BEAN_NAME)); <span class="hljs-comment">// 6</span><br>    &#125;<br>    <span class="hljs-comment">// 省略</span><br>    <span class="hljs-keyword">return</span> beanDefs;<br>&#125;<br><br><span class="hljs-comment">// AnnotationConfigUtils 类</span><br><span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> BeanDefinitionHolder <span class="hljs-title function_">registerPostProcessor</span><span class="hljs-params">(</span><br><span class="hljs-params">			BeanDefinitionRegistry registry, RootBeanDefinition definition, String beanName)</span> &#123;<br>    definition.setRole(BeanDefinition.ROLE_INFRASTRUCTURE);<br>    registry.registerBeanDefinition(beanName, definition); <span class="hljs-comment">// 7</span><br>    <span class="hljs-keyword">return</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">BeanDefinitionHolder</span>(definition, beanName);<br>&#125;<br><br><span class="hljs-comment">// GenericApplicationContext 类</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">registerBeanDefinition</span><span class="hljs-params">(String beanName, BeanDefinition beanDefinition)</span><br>        <span class="hljs-keyword">throws</span> BeanDefinitionStoreException &#123;<br>    <span class="hljs-built_in">this</span>.beanFactory.registerBeanDefinition(beanName, beanDefinition); <span class="hljs-comment">// 8</span><br>&#125;<br><br><br><span class="hljs-comment">// DefaultListableBeanFactory 类</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">registerBeanDefinition</span><span class="hljs-params">(String beanName, BeanDefinition beanDefinition)</span><br>			<span class="hljs-keyword">throws</span> BeanDefinitionStoreException &#123;<br>    <span class="hljs-comment">// 省略</span><br>    <span class="hljs-built_in">this</span>.beanDefinitionNames.add(beanName); <span class="hljs-comment">// 9 </span><br>    <span class="hljs-comment">// 省略</span><br>&#125;<br></code></pre></td></tr></table></figure>

<p>至此已经知道 <code>ConfigurationClassPostProcessor</code> 是在何处添加的，而且 <code>ConfigurationClassPostProcessor</code> 类对应的 <code>beanName</code> 是 <code>org.springframework.context.annotation.internalConfigurationAnnotationProcessor</code></p>
]]></content>
      <categories>
        <category>java</category>
        <category>Spring</category>
      </categories>
      <tags>
        <tag>java</tag>
        <tag>Spring</tag>
      </tags>
  </entry>
  <entry>
    <title>Spring上下文refresh方法解析</title>
    <url>/2023/b93a520f6ddc/index.html</url>
    <content><![CDATA[<p>本文主要记录 <code>AbstractApplicationContext#refresh()</code> 方法中每个步骤的作用</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">refresh</span><span class="hljs-params">()</span> <span class="hljs-keyword">throws</span> BeansException, IllegalStateException &#123;<br>    <span class="hljs-keyword">synchronized</span> (<span class="hljs-built_in">this</span>.startupShutdownMonitor) &#123;<br>        <span class="hljs-type">StartupStep</span> <span class="hljs-variable">contextRefresh</span> <span class="hljs-operator">=</span> <span class="hljs-built_in">this</span>.applicationStartup.start(<span class="hljs-string">&quot;spring.context.refresh&quot;</span>);<br><br>        <span class="hljs-comment">/** </span><br><span class="hljs-comment">         * 1. 准备刷新上下文方法,用于设置准备刷新前的一些参数:</span><br><span class="hljs-comment">		 * 程序启动标志位/上下文拓展资源加载/上下文环境准备情况验证/监听器监听事件容器初始化准备</span><br><span class="hljs-comment">         */</span><br>        prepareRefresh();<br><br>        <span class="hljs-comment">/**</span><br><span class="hljs-comment">         * 2. 构建 BeanFactory 容器, 默认使用的 DefaultListableBeanFactory</span><br><span class="hljs-comment">         */</span><br>        <span class="hljs-type">ConfigurableListableBeanFactory</span> <span class="hljs-variable">beanFactory</span> <span class="hljs-operator">=</span> obtainFreshBeanFactory();<br><br>        <span class="hljs-comment">/**</span><br><span class="hljs-comment">         * 3. 准备 BeanFactory 容器, 主要是对 BeanFactory 进行一些初始化操作</span><br><span class="hljs-comment">         */</span><br>        prepareBeanFactory(beanFactory);<br><br>        <span class="hljs-keyword">try</span> &#123;<br>            <span class="hljs-comment">/**</span><br><span class="hljs-comment">             * 4. 后处理 BeanFactory 容器, 主要是对 BeanFactory 进行一些后处理操作</span><br><span class="hljs-comment">             */</span><br>            postProcessBeanFactory(beanFactory);<br><br>            <span class="hljs-type">StartupStep</span> <span class="hljs-variable">beanPostProcess</span> <span class="hljs-operator">=</span> <span class="hljs-built_in">this</span>.applicationStartup.start(<span class="hljs-string">&quot;spring.context.beans.post-process&quot;</span>);<br>            <br>            <span class="hljs-comment">/**</span><br><span class="hljs-comment">             * 5. 执行BeanFactory后置处理. <span class="hljs-doctag">@ComponentScan</span> 的解析就是通过后置处理器 ConfigurationClassPostProcessor 完成</span><br><span class="hljs-comment">             */</span><br>            invokeBeanFactoryPostProcessors(beanFactory);<br><br>            <span class="hljs-comment">/**</span><br><span class="hljs-comment">             * 6. 注册 BeanPostProcessor 到 bean 工厂中</span><br><span class="hljs-comment">             */</span><br>            registerBeanPostProcessors(beanFactory);<br>            beanPostProcess.end();<br><br>        <br>            <span class="hljs-comment">/**</span><br><span class="hljs-comment">             * 7. 初始化 MessageSource 资源，国际化相关的处理</span><br><span class="hljs-comment">             */</span><br>            initMessageSource();<br><br>            <span class="hljs-comment">/**</span><br><span class="hljs-comment">             * </span><br><span class="hljs-comment">             * 8. 初始化 ApplicationEventMulticaster 事件广播器</span><br><span class="hljs-comment">             */</span><br>            initApplicationEventMulticaster();<br><br>            <span class="hljs-comment">/**</span><br><span class="hljs-comment">             * 9. 子类实现,springbootstarterweb在此创建web容器,并提前生成容器所需的Bean及其对应生命周期</span><br><span class="hljs-comment">             */</span><br>            onRefresh();<br><br>            <span class="hljs-comment">/**</span><br><span class="hljs-comment">             * 10. 给广播器中注册监听器,执行初期事件</span><br><span class="hljs-comment">             */</span><br>            registerListeners();<br><br>            <span class="hljs-comment">/**</span><br><span class="hljs-comment">             * 11. 初始化所有非懒加载单例Bean</span><br><span class="hljs-comment">             */</span><br>            finishBeanFactoryInitialization(beanFactory);<br><br>            <span class="hljs-comment">/**</span><br><span class="hljs-comment">             * 12. 完成刷新,发布上下文刷新完毕事件</span><br><span class="hljs-comment">             */</span><br>            finishRefresh();<br>        &#125;<br><br>        <span class="hljs-keyword">catch</span> (BeansException ex) &#123;<br>            <span class="hljs-comment">// 省略</span><br>        &#125;<br><br>        <span class="hljs-keyword">finally</span> &#123;<br>            resetCommonCaches();<br>            contextRefresh.end();<br>        &#125;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>java</category>
        <category>Spring</category>
      </categories>
      <tags>
        <tag>java</tag>
        <tag>Spring</tag>
      </tags>
  </entry>
  <entry>
    <title>Spring中Bean的生命周期详解</title>
    <url>/2023/160e87d42e42/index.html</url>
    <content><![CDATA[<span class="hide_first_mermaid">
<div class="mermaid-wrap"><pre class="mermaid-src" hidden>
  flowchart LR
    A
  </pre></div>
</span>

<p>spring版本 6.x</p>
<h2 id="Spring-的-Bean-生命周期有哪几个阶段"><a href="#Spring-的-Bean-生命周期有哪几个阶段" class="headerlink" title="Spring 的 Bean 生命周期有哪几个阶段"></a>Spring 的 Bean 生命周期有哪几个阶段</h2><p>所谓的 Bean 的生命周期就是 Bean 在整个应用的生命周期中所经历的各种状态，从创建、初始化、使用、销毁等各个阶段。</p>
<pre class="mermaid">flowchart LR
    subgraph A["创建"]
        style A fill:#f9f
        direction LR
        A1["实例化Bean[createBeanInstance()方法]"]
        subgraph A2["初始化过程"]
            style A2 fill:#8ff600
            direction TB
            A21["设置属性值 populateBean() 方法"]
            A22["调用对应的 Aware 接口 "]
            A23[" BeanPostProcessor 前置处理 "]
            A24[" InitializingBean 的 afterPropertiesSet() 方法 "]
            A25[" 自定义 init-method 方法 "]
            A26[" BeanPostProcessor 后置处理 "]
            A21-->A22-->A23-->A24-->A25-->A26
        end
        A3["注册 Destruction 回调"]
        A1-->A2-->A3
    end
    B[" \n使用 \n\n"]
    subgraph C["销毁"]
        direction TB
        C1["DisposableBean#destroy()"]
        C2["自定义destroy-method"]
        C1-->C2
    end
    A-->B-->C</pre>

<h2 id="Bean-生命周期代码示例"><a href="#Bean-生命周期代码示例" class="headerlink" title="Bean 生命周期代码示例"></a>Bean 生命周期代码示例</h2><p>示例代码包含以下几个类</p>
<ol>
<li><code>LifeCycleBean</code> 主要是观察该类创建 Bean 的过程，该类实现了 <code>ApplicationContextAware</code>, <code>InitializingBean</code>, <code>DisposableBean</code> 接口</li>
<li><code>CusBeanPostProcessor</code> 该类实现了 <code>BeanPostProcessor</code> 接口</li>
<li><code>LifeCycleMain</code> 测试主类</li>
</ol>
<figure class="highlight java"><figcaption><span>LifeCycleBean类</span></figcaption><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-meta">@Component</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">LifeCycleBean</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">ApplicationContextAware</span>, InitializingBean, DisposableBean &#123;<br>    <span class="hljs-keyword">public</span> <span class="hljs-title function_">LifeCycleBean</span><span class="hljs-params">()</span> &#123;<br>        System.out.println(<span class="hljs-string">&quot;执行构造方法 &quot;</span>);<br>    &#125;<br><br>    <span class="hljs-meta">@PostConstruct</span><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">init</span><span class="hljs-params">()</span> &#123;<br>        System.out.println(<span class="hljs-string">&quot;@PostConstruct init&quot;</span>);<br>    &#125;<br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">destroy</span><span class="hljs-params">()</span> <span class="hljs-keyword">throws</span> Exception &#123;<br>        System.out.println(<span class="hljs-string">&quot;DisposableBean -- destroy &quot;</span>);<br>    &#125;<br><br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">afterPropertiesSet</span><span class="hljs-params">()</span> <span class="hljs-keyword">throws</span> Exception &#123;<br>        System.out.println(<span class="hljs-string">&quot;InitializingBean afterPropertiesSet &quot;</span>);<br>    &#125;<br><br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">setApplicationContext</span><span class="hljs-params">(ApplicationContext applicationContext)</span> <span class="hljs-keyword">throws</span> BeansException &#123;<br>        System.out.println(<span class="hljs-string">&quot;ApplicationContextAware setApplicationContext&quot;</span>);<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<figure class="highlight java"><figcaption><span>CusBeanPostProcessor类</span></figcaption><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-meta">@Component</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">CusBeanPostProcessor</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">BeanPostProcessor</span> &#123;<br><br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> Object <span class="hljs-title function_">postProcessBeforeInitialization</span><span class="hljs-params">(Object bean, String beanName)</span> <span class="hljs-keyword">throws</span> BeansException &#123;<br>        System.out.println(<span class="hljs-string">&quot;CusBeanPostProcessor before, current beanName:&quot;</span> + beanName);<br>        <span class="hljs-keyword">return</span> BeanPostProcessor.<span class="hljs-built_in">super</span>.postProcessBeforeInitialization(bean, beanName);<br>    &#125;<br><br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> Object <span class="hljs-title function_">postProcessAfterInitialization</span><span class="hljs-params">(Object bean, String beanName)</span> <span class="hljs-keyword">throws</span> BeansException &#123;<br>        System.out.println(<span class="hljs-string">&quot;CusBeanPostProcessor after, current beanName:&quot;</span> + beanName);<br>        <span class="hljs-keyword">return</span> BeanPostProcessor.<span class="hljs-built_in">super</span>.postProcessAfterInitialization(bean, beanName);<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<figure class="highlight java"><figcaption><span>LifeCycleMain测试主类</span></figcaption><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-meta">@Configuration</span><br><span class="hljs-meta">@ComponentScan</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">LifeCycleMain</span> &#123;<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">main</span><span class="hljs-params">(String[] args)</span> &#123;<br>        <span class="hljs-type">ApplicationContext</span> <span class="hljs-variable">context</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">AnnotationConfigApplicationContext</span>(LifeCycleMain.class);<br>        <span class="hljs-type">LifeCycleBean</span> <span class="hljs-variable">bean</span> <span class="hljs-operator">=</span> context.getBean(LifeCycleBean.class);<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<p>执行结果</p>
<figure class="highlight pgsql"><table><tr><td class="code"><pre><code class="hljs pgsql">执行构造方法 <br>ApplicationContextAware setApplicationContext<br>CusBeanPostProcessor <span class="hljs-keyword">before</span>, <span class="hljs-keyword">current</span> beanName:lifeCycleBean<br>@PostConstruct init<br>InitializingBean afterPropertiesSet <br>CusBeanPostProcessor <span class="hljs-keyword">after</span>, <span class="hljs-keyword">current</span> beanName:lifeCycleBean<br></code></pre></td></tr></table></figure>

<p>从上述测试代码来看，在创建 <code>LifeCycleBean</code> 这个 Bean 过程中，执行顺序是</p>
<ol>
<li>执行对应类的构造方法</li>
<li>执行对应 Aware 接口的方法</li>
<li>执行 BeanPostProcessor 前置处理方法</li>
<li>执行 <code>@PostConstruct</code> 注解定义的方法</li>
<li>执行 InitializingBean 中的 afterPropertiesSet 方法</li>
<li>执行 BeanPostProcessor 后置处理方法</li>
</ol>
<h2 id="Spring-Bean-生命周期执行流程"><a href="#Spring-Bean-生命周期执行流程" class="headerlink" title="Spring Bean 生命周期执行流程"></a>Spring Bean 生命周期执行流程</h2><figure class="highlight leaf"><table><tr><td class="code"><pre><code class="hljs leaf">AbstractApplicationContext<span class="hljs-punctuation">#</span><span class="hljs-keyword">refresh</span><br>    AbstractApplicationContext<span class="hljs-punctuation">#</span><span class="hljs-keyword">finishBeanFactoryInitialization</span><br>        DefaultListableBeanFactory<span class="hljs-punctuation">#</span><span class="hljs-keyword">preInstantiateSingletons</span><br>            AbstractBeanFactory<span class="hljs-punctuation">#</span><span class="hljs-keyword">getBean</span><span class="hljs-params">(<span class="hljs-variable">java</span>.<span class="hljs-variable">lang</span>.<span class="hljs-variable">String</span>)</span><br>                AbstractBeanFactory<span class="hljs-punctuation">#</span><span class="hljs-keyword">doGetBean</span><br>                    DefaultSingletonBeanRegistry<span class="hljs-punctuation">#</span><span class="hljs-keyword">getSingleton</span><span class="hljs-params">(<span class="hljs-variable">ObjectFactory</span><span class="hljs-operator">&lt;</span>?<span class="hljs-operator">&gt;</span>)</span><br>                        AbstractAutowireCapableBeanFactory<span class="hljs-punctuation">#</span><span class="hljs-keyword">createBean</span><span class="hljs-params">(<span class="hljs-variable">String</span> <span class="hljs-variable">beanName</span>, <span class="hljs-variable">RootBeanDefinition</span>, <span class="hljs-variable">java</span>.<span class="hljs-variable">lang</span>.<span class="hljs-variable">Object</span>[])</span><br>                            AbstractAutowireCapableBeanFactory<span class="hljs-punctuation">#</span><span class="hljs-keyword">doCreateBean</span><br></code></pre></td></tr></table></figure>

<p>上述流程是创建 Bean 的调用过程，最终是在 <code>AbstractAutowireCapableBeanFactory</code> 中调用 <code>doCreateBean</code> 方法来创建，所以直接看下这部分代码（省略和此次流程无关代码）</p>
<figure class="highlight java"><figcaption><span>AbstractAutowireCapableBeanFactory类</span></figcaption><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">protected</span> Object <span class="hljs-title function_">doCreateBean</span><span class="hljs-params">(String beanName, RootBeanDefinition mbd, <span class="hljs-meta">@Nullable</span> Object[] args)</span><br>        <span class="hljs-keyword">throws</span> BeanCreationException &#123;<br><br>    <span class="hljs-comment">// Instantiate the bean.</span><br>    <span class="hljs-type">BeanWrapper</span> <span class="hljs-variable">instanceWrapper</span> <span class="hljs-operator">=</span> <span class="hljs-literal">null</span>;<br>    <span class="hljs-keyword">if</span> (instanceWrapper == <span class="hljs-literal">null</span>) &#123;<br>        <span class="hljs-comment">// 步骤1： 实例化 Bean</span><br>        instanceWrapper = createBeanInstance(beanName, mbd, args);<br>    &#125;<br>    <span class="hljs-type">Object</span> <span class="hljs-variable">bean</span> <span class="hljs-operator">=</span> instanceWrapper.getWrappedInstance();<br>    Class&lt;?&gt; beanType = instanceWrapper.getWrappedClass();<br>    <span class="hljs-keyword">if</span> (beanType != NullBean.class) &#123;<br>        mbd.resolvedTargetType = beanType;<br>    &#125;<br><br>    <span class="hljs-comment">// Initialize the bean instance.</span><br>    <span class="hljs-type">Object</span> <span class="hljs-variable">exposedObject</span> <span class="hljs-operator">=</span> bean;<br>    <span class="hljs-keyword">try</span> &#123;<br>        <span class="hljs-comment">// 步骤2： 设置属性</span><br>        populateBean(beanName, mbd, instanceWrapper);<br>        <span class="hljs-comment">// 步骤3： 调用 aware 接口 -》调用 BeanPostProcessor 前置方法</span><br>        <span class="hljs-comment">// -》 调用  InitializingBean 的 afterPropertiesSet 方法</span><br>        <span class="hljs-comment">// -》 调用  自定义的 init-method 方法</span><br>        <span class="hljs-comment">// -》 调用  自定义的 init-method 方法</span><br>        <span class="hljs-comment">// -》 调用 BeanPostProcessor 后置方法</span><br>        exposedObject = initializeBean(beanName, exposedObject, mbd);<br>    &#125;<br><br>    <span class="hljs-comment">// Register bean as disposable.</span><br>    <span class="hljs-keyword">try</span> &#123;<br>        <span class="hljs-comment">// 步骤4： 注册销毁方法</span><br>        registerDisposableBeanIfNecessary(beanName, bean, mbd);<br>    &#125;<br>    <span class="hljs-keyword">return</span> exposedObject;<br>&#125;<br></code></pre></td></tr></table></figure>

<p>实例化(<strong>步骤1</strong>)和设置属性值(<strong>步骤2</strong>) 就不看了，主要看一下 <strong>步骤3</strong>，这个步骤包含了 Bean 生命周期中整个初始化过程</p>
<figure class="highlight java"><figcaption><span>AbstractAutowireCapableBeanFactory类</span></figcaption><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">protected</span> Object <span class="hljs-title function_">initializeBean</span><span class="hljs-params">(String beanName, Object bean, <span class="hljs-meta">@Nullable</span> RootBeanDefinition mbd)</span> &#123;<br>    <span class="hljs-comment">// 步骤3.1 调用对应的 Aware 接口</span><br>    invokeAwareMethods(beanName, bean);<br><br>    <span class="hljs-type">Object</span> <span class="hljs-variable">wrappedBean</span> <span class="hljs-operator">=</span> bean;<br>    <span class="hljs-keyword">if</span> (mbd == <span class="hljs-literal">null</span> || !mbd.isSynthetic()) &#123;<br>        <span class="hljs-comment">// 步骤3.2 调用 BeanPostProcessor 前置方法</span><br>        wrappedBean = applyBeanPostProcessorsBeforeInitialization(wrappedBean, beanName);<br>    &#125;<br><br>    <span class="hljs-keyword">try</span> &#123;<br>        <span class="hljs-comment">// 步骤3.3 调用 InitializingBean 的 afterPropertiesSet 方法 和 自定义的 init-method 方法</span><br>        invokeInitMethods(beanName, wrappedBean, mbd);<br>    &#125;<br>    <span class="hljs-keyword">catch</span> (Throwable ex) &#123;<br>        <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">BeanCreationException</span>(<br>                (mbd != <span class="hljs-literal">null</span> ? mbd.getResourceDescription() : <span class="hljs-literal">null</span>), beanName, ex.getMessage(), ex);<br>    &#125;<br>    <span class="hljs-keyword">if</span> (mbd == <span class="hljs-literal">null</span> || !mbd.isSynthetic()) &#123;<br>        <span class="hljs-comment">// 步骤3.4 调用 BeanPostProcessor 后置方法</span><br>        wrappedBean = applyBeanPostProcessorsAfterInitialization(wrappedBean, beanName);<br>    &#125;<br><br>    <span class="hljs-keyword">return</span> wrappedBean;<br>&#125;<br></code></pre></td></tr></table></figure>

<p><strong>步骤3.3</strong> 中是调用 <code>InitializingBean</code> 的 <code>afterPropertiesSet</code> 方法 和 自定义的 <code>init-method</code> 方法， 调用 <code>InitializingBean</code> 接口的方法还比较好说，那么什么是自定义的 <code>init-method</code> 方法呢？看如下示例代码</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-comment">//  这是一个 普通 类</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">LifeCycleBean</span> &#123;<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">initMethod</span><span class="hljs-params">()</span> &#123;<br>        System.out.println(<span class="hljs-string">&quot;LifeCycleBean initMethod&quot;</span>);<br>    &#125;<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">destroyMethod</span><span class="hljs-params">()</span> &#123;<br>        System.out.println(<span class="hljs-string">&quot;LifeCycleBean destroyMethod&quot;</span>);<br>    &#125;<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">sayHello</span><span class="hljs-params">()</span> &#123;<br>        System.out.println(<span class="hljs-string">&quot;LifeCycleBean sayHello&quot;</span>);<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-meta">@Configuration</span><br><span class="hljs-meta">@ComponentScan</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">LifeCycleMain</span> &#123;<br><br>    <span class="hljs-comment">// 使用 @Bean 注入 LifeCycleBean 类，同时添加了 initMethod 和 destroyMethod 方法</span><br>    <span class="hljs-meta">@Bean(initMethod = &quot;initMethod&quot;, destroyMethod=&quot;destroyMethod&quot;)</span><br>    <span class="hljs-keyword">public</span> LifeCycleBean <span class="hljs-title function_">lifeCycleBean</span><span class="hljs-params">()</span> &#123;<br>        <span class="hljs-keyword">return</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">LifeCycleBean</span>();<br>    &#125;<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">main</span><span class="hljs-params">(String[] args)</span> &#123;<br>        <span class="hljs-type">ApplicationContext</span> <span class="hljs-variable">context</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">AnnotationConfigApplicationContext</span>(LifeCycleMain.class);<br>        context.getBean(LifeCycleBean.class).sayHello();<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<p>执行结果</p>
<figure class="highlight ebnf"><table><tr><td class="code"><pre><code class="hljs ebnf"><span class="hljs-attribute">LifeCycleBean initMethod</span><br><span class="hljs-attribute">LifeCycleBean sayHello</span><br></code></pre></td></tr></table></figure>

<p>重点执行流程导致就结束了</p>
]]></content>
      <categories>
        <category>java</category>
        <category>Spring</category>
      </categories>
      <tags>
        <tag>java</tag>
        <tag>Spring</tag>
      </tags>
  </entry>
  <entry>
    <title>Spring中DeferredImportSelector源码解析</title>
    <url>/2023/55186be8f39b/index.html</url>
    <content><![CDATA[<span class="hide_first_mermaid">
<div class="mermaid-wrap"><pre class="mermaid-src" hidden>
  flowchart LR
    A
  </pre></div>
</span>

<p>Spring版本: 6.0.6</p>
<p><code>DeferredImportSelector</code> 是 <code>ImportSelector</code> 的子接口，所以需要先了解 <code>ImportSelector</code> 的作用，可以参考 <ol><li><a href="/2023/73d35f59945c/index.html" title="Spring中Import注解源码解析">Spring中Import注解源码解析</a></li></ol></p>
<h2 id="简述-ImportSelector-的作用"><a href="#简述-ImportSelector-的作用" class="headerlink" title="简述 ImportSelector 的作用"></a>简述 ImportSelector 的作用</h2><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">interface</span> <span class="hljs-title class_">ImportSelector</span> &#123;<br><br>	String[] selectImports(AnnotationMetadata importingClassMetadata);<br><br>	<span class="hljs-meta">@Nullable</span><br>	<span class="hljs-keyword">default</span> Predicate&lt;String&gt; <span class="hljs-title function_">getExclusionFilter</span><span class="hljs-params">()</span> &#123;<br>		<span class="hljs-keyword">return</span> <span class="hljs-literal">null</span>;<br>	&#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<p>这里直接总结一下 <code>ImportSelector</code> 的作用，<code>selectImports()</code> 方法返回值是需要导入的类的全限定类名组成的数组，也就是可以批量注册普通类给 <code>Spring</code> 进行管理， <code>getExclusionFilter()</code> 则是可以过滤掉不想要注册的类</p>
<h2 id="DeferredImportSelector-和-ImportSelector-的区别是什么"><a href="#DeferredImportSelector-和-ImportSelector-的区别是什么" class="headerlink" title="DeferredImportSelector 和 ImportSelector 的区别是什么"></a>DeferredImportSelector 和 ImportSelector 的区别是什么</h2><p>先看一下 <code>DeferredImportSelector</code> 接口</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">interface</span> <span class="hljs-title class_">DeferredImportSelector</span> <span class="hljs-keyword">extends</span> <span class="hljs-title class_">ImportSelector</span> &#123;<br>	<span class="hljs-meta">@Nullable</span><br>	<span class="hljs-keyword">default</span> Class&lt;? <span class="hljs-keyword">extends</span> <span class="hljs-title class_">Group</span>&gt; getImportGroup() &#123;<br>		<span class="hljs-keyword">return</span> <span class="hljs-literal">null</span>;<br>	&#125;<br><br>	<span class="hljs-keyword">interface</span> <span class="hljs-title class_">Group</span> &#123;<br>        <span class="hljs-comment">// 第二个参数 selector 就是对应 DeferredImportSelector 实现类</span><br>		<span class="hljs-keyword">void</span> <span class="hljs-title function_">process</span><span class="hljs-params">(AnnotationMetadata metadata, DeferredImportSelector selector)</span>;<br><br>        <span class="hljs-comment">// 返回需要被导入的类全限定类名，就和 ImportSelector 接口中的 selectImports 方法类似</span><br>		Iterable&lt;Entry&gt; <span class="hljs-title function_">selectImports</span><span class="hljs-params">()</span>;<br><br>        <span class="hljs-comment">// Entry 就是用来保存需要导入的类的全限定类名和 元数据信息</span><br>		<span class="hljs-keyword">class</span> <span class="hljs-title class_">Entry</span> &#123;<br>			<span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> AnnotationMetadata metadata;<br>			<span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> String importClassName;<br>        &#125;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<ol>
<li><p>表面上看来 <code>DeferredImportSelector</code> 与 <code>ImportSelector</code> 的区别是 <code>DeferredImportSelector</code> 中多了一个内部接口</p>
</li>
<li><p>实际是是 <code>DeferredImportSelector</code> 和 <code>ImportSelector</code> 的执行时机不一样</p>
<ol>
<li><code>ImportSelector</code> 是在解析对应的实现类时就会直接处理需要导入的类</li>
<li><code>DeferredImportSelector</code> 是提供了一种延迟导入配置类的机制，<code>SpringBoot</code> 的自动装配就是用到了 <code>DeferredImportSelector</code> 延迟导入的功能，这个延迟的意思是，非自动装配的类都解析完成之后才会去导入自动装配的类，具体源码会在下面说明</li>
</ol>
</li>
<li><p>除了延迟导入机制，如果 <code>DeferredImportSelector</code> 实现类没有实现 <code>Group</code> 接口的话，其他方法和 <code>ImportSelector</code> 一样，因为当没有实现 <code>Group</code> 接口时会使用 <code>Spring</code> 提供的默认的实现类 <code>DefaultDeferredImportSelectorGroup</code>, 可以从下面代码看到默认的实现中 <code>process()</code> 方法直接调用的就是 <code>selector.selectImports()</code> 方法，其实就是直接调用的就是 <code>ImportSelector</code> 接口的 <code>selectImports()</code> 方法，因此此时和 <code>ImportSelector</code> 接口在功能上是一致的【当然，执行时机还是不同】</p>
</li>
</ol>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">DefaultDeferredImportSelectorGroup</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">Group</span> &#123;<br><br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> List&lt;Entry&gt; imports = <span class="hljs-keyword">new</span> <span class="hljs-title class_">ArrayList</span>&lt;&gt;();<br><br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">process</span><span class="hljs-params">(AnnotationMetadata metadata, DeferredImportSelector selector)</span> &#123;<br>        <span class="hljs-keyword">for</span> (String importClassName : selector.selectImports(metadata)) &#123;<br>            <span class="hljs-built_in">this</span>.imports.add(<span class="hljs-keyword">new</span> <span class="hljs-title class_">Entry</span>(metadata, importClassName));<br>        &#125;<br>    &#125;<br><br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> Iterable&lt;Entry&gt; <span class="hljs-title function_">selectImports</span><span class="hljs-params">()</span> &#123;<br>        <span class="hljs-keyword">return</span> <span class="hljs-built_in">this</span>.imports;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<h2 id="DeferredImportSelector-使用示例"><a href="#DeferredImportSelector-使用示例" class="headerlink" title="DeferredImportSelector 使用示例"></a>DeferredImportSelector 使用示例</h2><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-comment">// 普通的类，这个类没有任何注解，就是为了让 DeferredImportSelector 来导入的</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">DefImportBean</span> &#123;<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">sayHello</span><span class="hljs-params">()</span> &#123;<br>        System.out.println(<span class="hljs-string">&quot;DefImportBean sayHello&quot;</span>);<br>    &#125;<br>&#125;<br><br><br><span class="hljs-comment">// 实现了 DeferredImportSelector 接口的类，这个类的作用是为了导入 DefImportBean 类</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">CusDeferredImportSelector</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">DeferredImportSelector</span> &#123;<br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> String[] selectImports(AnnotationMetadata importingClassMetadata) &#123;<br>        System.out.println(<span class="hljs-string">&quot;实际导入 --- 2&quot;</span>);<br>        <span class="hljs-keyword">return</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">String</span>[]&#123;DefImportBean.class.getName()&#125;;<br>    &#125;<br><br>    <span class="hljs-comment">// 自定义导入分组，这代表不使用默认的DefaultDeferredImportSelectorGroup， 如果没有导入就算写了 Group 实现类也不会使用的</span><br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> Class&lt;? <span class="hljs-keyword">extends</span> <span class="hljs-title class_">Group</span>&gt; getImportGroup() &#123;<br>        <span class="hljs-keyword">return</span> CusImportSelectorBeanGroup.class;<br>    &#125;<br><br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">CusImportSelectorBeanGroup</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">DeferredImportSelector</span>.Group &#123;<br>        <span class="hljs-comment">// 用来保存需要导入的类</span><br>        <span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> List&lt;Entry&gt; instanceImports = <span class="hljs-keyword">new</span> <span class="hljs-title class_">ArrayList</span>&lt;&gt;();<br><br>        <span class="hljs-meta">@Override</span><br>        <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">process</span><span class="hljs-params">(AnnotationMetadata metadata, DeferredImportSelector selector)</span> &#123;<br>            System.out.println(<span class="hljs-string">&quot;使用了自定义的分组 ----  1&quot;</span>);<br>            <span class="hljs-keyword">for</span> (String importClassName : selector.selectImports(metadata)) &#123;<br>                <span class="hljs-built_in">this</span>.instanceImports.add(<span class="hljs-keyword">new</span> <span class="hljs-title class_">Entry</span>(metadata, importClassName));<br>            &#125;<br>        &#125;<br><br>        <span class="hljs-meta">@Override</span><br>        <span class="hljs-keyword">public</span> Iterable&lt;Entry&gt; <span class="hljs-title function_">selectImports</span><span class="hljs-params">()</span> &#123;<br>            System.out.println(<span class="hljs-string">&quot;真正返回 -- 3&quot;</span>);<br>            <span class="hljs-keyword">return</span> instanceImports;<br>        &#125;<br>    &#125;<br>&#125;<br><br><span class="hljs-comment">// 主类，使用了 Import 导入了 实现了 CusDeferredImportSelector 接口的类</span><br><span class="hljs-meta">@ComponentScan</span><br><span class="hljs-meta">@Configuration</span><br><span class="hljs-meta">@Import(CusDeferredImportSelector.class)</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">DefImportMain</span> &#123;<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">main</span><span class="hljs-params">(String[] args)</span> &#123;<br>        <span class="hljs-type">AnnotationConfigApplicationContext</span> <span class="hljs-variable">context</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">AnnotationConfigApplicationContext</span>(DefImportMain.class);<br>        <span class="hljs-type">DefImportBean</span> <span class="hljs-variable">defImportBean</span> <span class="hljs-operator">=</span> context.getBean(DefImportBean.class);<br>        defImportBean.sayHello();<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<p>执行结果：</p>
<figure class="highlight ada"><table><tr><td class="code"><pre><code class="hljs ada">使用了自定义的分组 <span class="hljs-comment">----  1</span><br>实际导入 <span class="hljs-comment">--- 2</span><br>真正返回 <span class="hljs-comment">-- 3</span><br>DefImportBean sayHello<br></code></pre></td></tr></table></figure>

<p>从打印结果中也可以看到实现了 <code>DeferredImportSelector</code> 接口的类的执行顺序如下</p>
<pre class="mermaid">flowchart TB
    A["Group实现类中的 process() 方法"]
    B["DeferredImportSelector实现类 
     中的 selectImports() 方法 
     （该方法不一定执行，当 Group 实现类中
     有调用的时候才会执行，没有调用就没有执行
     SpringBoot自动装配时就没有执行 selectImports() 方法）"]
    C["Group实现类中的 selectImports() 方法
    这个方法的返回值才是真正要被
    导入的类"]
    A-->B-->C</pre>

<h2 id="DeferredImportSelector-执行流程源码解析"><a href="#DeferredImportSelector-执行流程源码解析" class="headerlink" title="DeferredImportSelector 执行流程源码解析"></a>DeferredImportSelector 执行流程源码解析</h2><p>在源码分析之前先熟悉一下会涉及到的几个类</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">DeferredImportSelectorHolder</span> &#123;<br>    <span class="hljs-comment">// 配置主类，在上面的示例中就是 DefImportMain 对应的 ConfigurationClass</span><br>	<span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> ConfigurationClass configurationClass;<br>    <span class="hljs-comment">// 保存的就是 DeferredImportSelector 实现类</span><br>	<span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> DeferredImportSelector importSelector;<br>&#125;<br><br><br><span class="hljs-comment">// 前面有说过 DeferredImportSelector 是会延迟注入的，既然要延迟，那就需要先将其存在一个地方，DeferredImportSelectorHandler 类就是保存所有的 DeferredImportSelector 实现类</span><br><span class="hljs-keyword">private</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">DeferredImportSelectorHandler</span> &#123;<br>	<span class="hljs-keyword">private</span> List&lt;DeferredImportSelectorHolder&gt; deferredImportSelectors = <span class="hljs-keyword">new</span> <span class="hljs-title class_">ArrayList</span>&lt;&gt;();<br>&#125;<br></code></pre></td></tr></table></figure>

<p><code>DeferredImportSelector</code> 的解析其实是通过 <code>BeanFactory</code> 的后置处理器实现的，对应的 <code>BeanFactory</code> 后置处理器类是 <code>ConfigurationClassPostProcessor</code>，而解析 <code>Import</code> 注解的类则是 <code>ConfigurationClassParser</code>，这里再简单写一下解析顺序流程</p>
<figure class="highlight leaf"><table><tr><td class="code"><pre><code class="hljs leaf">AbstractApplicationContext<span class="hljs-punctuation">#</span><span class="hljs-keyword">refresh</span><br>    AbstractApplicationContext<span class="hljs-punctuation">#</span><span class="hljs-keyword">invokeBeanFactoryPostProcessors</span><br>        public PostProcessorRegistrationDelegate<span class="hljs-punctuation">#</span><span class="hljs-keyword">invokeBeanFactoryPostProcessors</span><br>            private PostProcessorRegistrationDelegate<span class="hljs-punctuation">#</span><span class="hljs-keyword">invokeBeanDefinitionRegistryPostProcessors</span><br>                ConfigurationClassPostProcessor<span class="hljs-punctuation">#</span><span class="hljs-keyword">postProcessBeanDefinitionRegistry</span><br>                    ConfigurationClassPostProcessor<span class="hljs-punctuation">#</span><span class="hljs-keyword">processConfigBeanDefinitions</span><br>                        ConfigurationClassParser<span class="hljs-punctuation">#</span><span class="hljs-keyword">parse</span><br>                            for 循环 start[解析单个类]<br>                                ConfigurationClassParser<span class="hljs-punctuation">#</span><span class="hljs-keyword">parse</span><span class="hljs-params">(<span class="hljs-variable">AnnotationMetadata</span>, <span class="hljs-variable">java</span>.<span class="hljs-variable">lang</span>.<span class="hljs-variable">String</span>)</span><br>                                    ConfigurationClassParser<span class="hljs-punctuation">#</span><span class="hljs-keyword">processImports</span>    (1)<br>                            for 循环结束<br>                            DeferredImportSelectorHandler<span class="hljs-punctuation">#</span><span class="hljs-keyword">process</span>              (2)<br></code></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-comment">//  ConfigurationClassParser 类</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">parse</span><span class="hljs-params">(Set&lt;BeanDefinitionHolder&gt; configCandidates)</span> &#123;<br>    <span class="hljs-keyword">for</span> (BeanDefinitionHolder holder : configCandidates) &#123;<br>        <span class="hljs-type">BeanDefinition</span> <span class="hljs-variable">bd</span> <span class="hljs-operator">=</span> holder.getBeanDefinition();<br>        <span class="hljs-keyword">try</span> &#123;<br>            <span class="hljs-keyword">if</span> (bd <span class="hljs-keyword">instanceof</span> AnnotatedBeanDefinition annotatedBeanDef) &#123;<br>                <span class="hljs-comment">// 这里会对 DeferredImportSelector 进行缓存，缓存在 DeferredImportSelectorHandler 类中</span><br>                parse(annotatedBeanDef.getMetadata(), holder.getBeanName());<br>            &#125;<br>            <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (bd <span class="hljs-keyword">instanceof</span> AbstractBeanDefinition abstractBeanDef &amp;&amp; abstractBeanDef.hasBeanClass()) &#123;<br>                parse(abstractBeanDef.getBeanClass(), holder.getBeanName());<br>            &#125;<br>            <span class="hljs-keyword">else</span> &#123;<br>                parse(bd.getBeanClassName(), holder.getBeanName());<br>            &#125;<br>        &#125;<br>        <span class="hljs-keyword">catch</span> (BeanDefinitionStoreException ex) &#123;<br>            <span class="hljs-keyword">throw</span> ex;<br>        &#125;<br>        <span class="hljs-keyword">catch</span> (Throwable ex) &#123;<br>            <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">BeanDefinitionStoreException</span>(<br>                    <span class="hljs-string">&quot;Failed to parse configuration class [&quot;</span> + bd.getBeanClassName() + <span class="hljs-string">&quot;]&quot;</span>, ex);<br>        &#125;<br>    &#125;<br>    <span class="hljs-comment">// 这里是对 DeferredImportSelector 的实际处理</span><br>    <span class="hljs-built_in">this</span>.deferredImportSelectorHandler.process();<br>&#125;<br></code></pre></td></tr></table></figure>


<p>先简单总结一下：</p>
<ol>
<li>上述流程中 <code>(1)</code> 位置就是处理 <code>ImportSelector</code> 的地方， 同时也是缓存 <code>DeferredImportSelector</code> 实现类的地方</li>
<li>上述流程中 <code>(2)</code> 位置就是实际处理 <code>DeferredImportSelector</code> 的地方， 可以看到 <code>(2)</code> 位置是在循环外面，也就是等所有的 <code>BeanDefinition</code> 都解析完成之后再处理的，所以才说 <code>DeferredImportSelector</code> 实现接口有延迟导入的功能</li>
</ol>
<p>接下来就直接看 <code>processImports</code> 方法【下面所有列出代码都省略了和本次分析无关的代码】</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">private</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">processImports</span><span class="hljs-params">(ConfigurationClass configClass, SourceClass currentSourceClass,</span><br><span class="hljs-params">			Collection&lt;SourceClass&gt; importCandidates, Predicate&lt;String&gt; exclusionFilter,</span><br><span class="hljs-params">			<span class="hljs-type">boolean</span> checkForCircularImports)</span> &#123;<br>    <span class="hljs-keyword">for</span> (SourceClass candidate : importCandidates) &#123;<br>        <span class="hljs-keyword">if</span> (candidate.isAssignable(ImportSelector.class)) &#123;<br>            <span class="hljs-comment">// Candidate class is an ImportSelector -&gt; delegate to it to determine imports</span><br>            Class&lt;?&gt; candidateClass = candidate.loadClass();<br>            <span class="hljs-type">ImportSelector</span> <span class="hljs-variable">selector</span> <span class="hljs-operator">=</span> ParserStrategyUtils.instantiateClass(candidateClass, ImportSelector.class,<br>                    <span class="hljs-built_in">this</span>.environment, <span class="hljs-built_in">this</span>.resourceLoader, <span class="hljs-built_in">this</span>.registry);<br>            <span class="hljs-comment">// 这里就是处理 DeferredImportSelector 的逻辑</span><br>            <span class="hljs-keyword">if</span> (selector <span class="hljs-keyword">instanceof</span> DeferredImportSelector deferredImportSelector) &#123;<br>                <span class="hljs-comment">// 这里并没有真正的处理，只是将其存在 DeferredImportSelectorHandler 对象中</span><br>                <span class="hljs-built_in">this</span>.deferredImportSelectorHandler.handle(configClass, deferredImportSelector);<br>            &#125;<br>        &#125;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<p>上面代码就是缓存 <code>DeferredImportSelector</code> 实现类的处理逻辑，接下来就是处理 <code>DeferredImportSelector</code> 实现类的逻辑了，从前面代码已经知道了，是会在 <code>DeferredImportSelectorHandler</code> 中进行处理, 处理过程中会涉及到 <code>Group</code> 相关的类，所以这里先看一下</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">DeferredImportSelectorGrouping</span> &#123;<br>    <span class="hljs-comment">// 这里就保存了 Group 实现类</span><br>	<span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> DeferredImportSelector.Group group;<br>	<span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> List&lt;DeferredImportSelectorHolder&gt; deferredImports = <span class="hljs-keyword">new</span> <span class="hljs-title class_">ArrayList</span>&lt;&gt;();<br>&#125;<br><br><span class="hljs-keyword">private</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">DeferredImportSelectorGroupingHandler</span> &#123;<br>	<span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> Map&lt;Object, DeferredImportSelectorGrouping&gt; groupings = <span class="hljs-keyword">new</span> <span class="hljs-title class_">LinkedHashMap</span>&lt;&gt;();<br>&#125;<br></code></pre></td></tr></table></figure>

<p>接下来看  <code>this.deferredImportSelectorHandler.process();</code> 这个处理</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-comment">// DeferredImportSelectorHandler 类</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">process</span><span class="hljs-params">()</span> &#123;<br>    List&lt;DeferredImportSelectorHolder&gt; deferredImports = <span class="hljs-built_in">this</span>.deferredImportSelectors;<br>    <span class="hljs-built_in">this</span>.deferredImportSelectors = <span class="hljs-literal">null</span>;<br>    <span class="hljs-keyword">try</span> &#123;<br>        <span class="hljs-keyword">if</span> (deferredImports != <span class="hljs-literal">null</span>) &#123;<br>            <span class="hljs-type">DeferredImportSelectorGroupingHandler</span> <span class="hljs-variable">handler</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">DeferredImportSelectorGroupingHandler</span>();<br>            deferredImports.sort(DEFERRED_IMPORT_COMPARATOR);<br>            <span class="hljs-comment">// 这里涉及到 Group 的注册   (1)</span><br>            deferredImports.forEach(handler::register);<br>            <span class="hljs-comment">// 真正的处理 导入逻辑       (2)</span><br>            handler.processGroupImports();<br>        &#125;<br>    &#125;<br>    <span class="hljs-keyword">finally</span> &#123;<br>        <span class="hljs-built_in">this</span>.deferredImportSelectors = <span class="hljs-keyword">new</span> <span class="hljs-title class_">ArrayList</span>&lt;&gt;();<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<p>先看一下 <code>Group</code> 注册的相关逻辑，也就是 <code>(1)</code> 中的相关逻辑</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">private</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">DeferredImportSelectorGroupingHandler</span> &#123;<br><br>	<span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> Map&lt;Object, DeferredImportSelectorGrouping&gt; groupings = <span class="hljs-keyword">new</span> <span class="hljs-title class_">LinkedHashMap</span>&lt;&gt;();<br>	<span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> Map&lt;AnnotationMetadata, ConfigurationClass&gt; configurationClasses = <span class="hljs-keyword">new</span> <span class="hljs-title class_">HashMap</span>&lt;&gt;();<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">register</span><span class="hljs-params">(DeferredImportSelectorHolder deferredImport)</span> &#123;<br>        <span class="hljs-comment">/**</span><br><span class="hljs-comment">         *  deferredImport.getImportSelector() 获取的就是 DeferredImportSelector 实现类，再调用 getImportGroup() 就是想要获取我们自己实现了 Group 的类</span><br><span class="hljs-comment">         * 这也是为什么前面说如果单纯实现了 Group 类是没有用的，还需要使用 getImportGroup() 方法返回才行，就是因为这里会调用 getImportGroup() 方法获取我们自己实现了 Group 的类</span><br><span class="hljs-comment">         */</span><br><br>        Class&lt;? <span class="hljs-keyword">extends</span> <span class="hljs-title class_">Group</span>&gt; group = deferredImport.getImportSelector().getImportGroup();<br>        <span class="hljs-type">DeferredImportSelectorGrouping</span> <span class="hljs-variable">grouping</span> <span class="hljs-operator">=</span> <span class="hljs-built_in">this</span>.groupings.computeIfAbsent(<br>                (group != <span class="hljs-literal">null</span> ? group : deferredImport),<br>                <span class="hljs-comment">// 这里是创建 Group, 如果没有创建创建就是使用默认的，具体看下面 createGroup 方法</span><br>                key -&gt; <span class="hljs-keyword">new</span> <span class="hljs-title class_">DeferredImportSelectorGrouping</span>(createGroup(group)));<br>        grouping.add(deferredImport);<br>        <span class="hljs-built_in">this</span>.configurationClasses.put(deferredImport.getConfigurationClass().getMetadata(),<br>                deferredImport.getConfigurationClass());<br>    &#125;<br><br>    <span class="hljs-keyword">private</span> Group <span class="hljs-title function_">createGroup</span><span class="hljs-params">(<span class="hljs-meta">@Nullable</span> Class&lt;? extends Group&gt; type)</span> &#123;<br>            <span class="hljs-comment">// 这里是创建 Group, 如果我们自己没有创建，那么 type 就会是 null， 也就会使用默认的 DefaultDeferredImportSelectorGroup 类</span><br>			Class&lt;? <span class="hljs-keyword">extends</span> <span class="hljs-title class_">Group</span>&gt; effectiveType = (type != <span class="hljs-literal">null</span> ? type : DefaultDeferredImportSelectorGroup.class);<br>        <span class="hljs-keyword">return</span> ParserStrategyUtils.instantiateClass(effectiveType, Group.class,<br>                ConfigurationClassParser.<span class="hljs-built_in">this</span>.environment,<br>                ConfigurationClassParser.<span class="hljs-built_in">this</span>.resourceLoader,<br>                ConfigurationClassParser.<span class="hljs-built_in">this</span>.registry);<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<p>接下来就是看 <code>processGroupImports()</code> 方法</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-comment">//  DeferredImportSelectorGroupingHandler 类</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">processGroupImports</span><span class="hljs-params">()</span> &#123;<br>    <span class="hljs-keyword">for</span> (DeferredImportSelectorGrouping grouping : <span class="hljs-built_in">this</span>.groupings.values()) &#123;<br>        Predicate&lt;String&gt; exclusionFilter = grouping.getCandidateFilter();<br>        <span class="hljs-comment">// grouping.getImports() 中就会实际调用 Group 类的 process() 方法</span><br>        <span class="hljs-comment">// 如果使用的是默认的 Group 实现类，实际就会调用 ImportSelector 中的 selectImports 方法， </span><br>        <span class="hljs-comment">//getImports() 方法的返回值就是我们自己实现了 Group 类的 selectImports 方法的返回值</span><br>        grouping.getImports().forEach(entry -&gt; &#123;<br>            <span class="hljs-type">ConfigurationClass</span> <span class="hljs-variable">configurationClass</span> <span class="hljs-operator">=</span> <span class="hljs-built_in">this</span>.configurationClasses.get(entry.getMetadata());<br>            <span class="hljs-keyword">try</span> &#123;<br>                <span class="hljs-comment">// 拿到 扫描到的类的全限定类名，然后就按照 Import 普通类来处理，也就是当作 @Configuration 注解的类来处理</span><br>                processImports(configurationClass, asSourceClass(configurationClass, exclusionFilter),<br>                        Collections.singleton(asSourceClass(entry.getImportClassName(), exclusionFilter)),<br>                        exclusionFilter, <span class="hljs-literal">false</span>);<br>            &#125;<br>            <span class="hljs-keyword">catch</span> (BeanDefinitionStoreException ex) &#123;<br>                <span class="hljs-keyword">throw</span> ex;<br>            &#125;<br>            <span class="hljs-keyword">catch</span> (Throwable ex) &#123;<br>                <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">BeanDefinitionStoreException</span>(<br>                        <span class="hljs-string">&quot;Failed to process import candidates for configuration class [&quot;</span> +<br>                                configurationClass.getMetadata().getClassName() + <span class="hljs-string">&quot;]&quot;</span>, ex);<br>            &#125;<br>        &#125;);<br>    &#125;<br>&#125;<br><br><span class="hljs-keyword">public</span> Iterable&lt;Group.Entry&gt; getImports() &#123;<br>    <span class="hljs-keyword">for</span> (DeferredImportSelectorHolder deferredImport : <span class="hljs-built_in">this</span>.deferredImports) &#123;<br>        <span class="hljs-comment">// 在这里开始调用 Group 的 process() 方法</span><br>        <span class="hljs-built_in">this</span>.group.process(deferredImport.getConfigurationClass().getMetadata(),<br>                deferredImport.getImportSelector());<br>    &#125;<br>    <span class="hljs-keyword">return</span> <span class="hljs-built_in">this</span>.group.selectImports();<br>&#125;<br></code></pre></td></tr></table></figure>

<p>至此就解析完了 <code>Spring</code> 中 <code>DeferredImportSelectory</code> 的整体调用流程</p>
]]></content>
      <categories>
        <category>java</category>
        <category>Spring</category>
      </categories>
      <tags>
        <tag>java</tag>
        <tag>Spring</tag>
      </tags>
  </entry>
  <entry>
    <title>Spring中FactoryBean详解</title>
    <url>/2023/fdea11462f1f/index.html</url>
    <content><![CDATA[<h2 id="FactoryBean-的作用"><a href="#FactoryBean-的作用" class="headerlink" title="FactoryBean 的作用"></a>FactoryBean 的作用</h2><p><code>FactoryBean</code> 是一个接口，对于实现了该接口的 <code>Bean</code> 来说，直接调用 <code>getBean()</code> 方法时，会返回 <code>FactoryBean</code> 接口中 <code>getObject()</code> 方法返回的实例，而不是 <code>Bean</code> 本身。</p>
<h2 id="FactoryBean-示例代码"><a href="#FactoryBean-示例代码" class="headerlink" title="FactoryBean 示例代码"></a>FactoryBean 示例代码</h2><blockquote>
<p>以下所有代码在同一个包中</p>
</blockquote>
<ul>
<li><p><code>BeanOne</code> 普通类，并没有添加 <code>@Component</code> 注解</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">BeanOne</span> &#123;<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">test</span><span class="hljs-params">()</span> &#123;<br>        System.out.println(<span class="hljs-string">&quot;BeanOne test&quot;</span>);<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>
</li>
<li><p><code>CusFactoryBean</code> 自定义 <code>FactoryBean</code> 实现类，并且被 <code>Spring</code> 管理（添加了 <code>@Component</code> 注解 ），并且定义了 <code>beanName=cusBean</code></p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-meta">@Component(&quot;cusBean&quot;)</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">CusFactoryBean</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">FactoryBean</span>&lt;BeanOne&gt; &#123;<br><br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> BeanOne <span class="hljs-title function_">getObject</span><span class="hljs-params">()</span> &#123;<br>        <span class="hljs-keyword">return</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">BeanOne</span>();<br>    &#125;<br><br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> Class&lt;?&gt; getObjectType() &#123;<br>        <span class="hljs-keyword">return</span> <span class="hljs-literal">null</span>;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>
</li>
<li><p><code>FactoryBeanMain</code> 测试主类</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-meta">@Configuration</span><br><span class="hljs-meta">@ComponentScan</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">FactoryBeanMain</span> &#123;<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">main</span><span class="hljs-params">(String[] args)</span> &#123;<br>        <span class="hljs-type">ApplicationContext</span> <span class="hljs-variable">applicationContext</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">AnnotationConfigApplicationContext</span>(FactoryBeanMain.class);<br>        <span class="hljs-type">Object</span> <span class="hljs-variable">cusBean</span> <span class="hljs-operator">=</span> applicationContext.getBean(<span class="hljs-string">&quot;&amp;cusBean&quot;</span>);<br>        <span class="hljs-comment">// com.example.bean.CusFactoryBean@759d26fb</span><br>        System.out.println(cusBean);<br>        <span class="hljs-type">BeanOne</span> <span class="hljs-variable">beanOne</span> <span class="hljs-operator">=</span> (BeanOne)applicationContext.getBean(<span class="hljs-string">&quot;cusBean&quot;</span>);<br>        <span class="hljs-comment">// com.example.bean.BeanOne@3c73951</span><br>        System.out.println(beanOne);<br>        <span class="hljs-type">BeanOne</span> <span class="hljs-variable">beanOne2</span> <span class="hljs-operator">=</span> (BeanOne)applicationContext.getBean(<span class="hljs-string">&quot;cusBean&quot;</span>);<br>        <span class="hljs-comment">// com.example.bean.BeanOne@3c73951</span><br>        System.out.println(beanOne2);<br>        beanOne.test();<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>
</li>
<li><p>直接通过 <code>beanName=cusBean</code> 从 <code>Spring</code> 中获取时得到的是 <code>FactoryBean</code> 接口中 <code>getObject()</code> 返回的实例，本例中是 <code>BeanOne</code> 实例</p>
</li>
<li><p>若想获取 <code>FactoryBean</code> 实现类 <code>Bean</code>, 则需要在 <code>beanName</code> 前面添加 <code>&amp;</code> 符号，即 <code>&amp;cusBean</code></p>
</li>
</ul>
<h2 id="获取-FacotoryBean-中-getObject-对象的流程"><a href="#获取-FacotoryBean-中-getObject-对象的流程" class="headerlink" title="获取 FacotoryBean 中 getObject() 对象的流程"></a>获取 FacotoryBean 中 getObject() 对象的流程</h2><p>使用的是上面的示例，<code>CusFactoryBean</code> 实现了 <code>FactoryBean</code> 接口，<code>getObject()</code> 中返回的是 <code>BeanOne</code> 实例，程序入口是</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-type">ApplicationContext</span> <span class="hljs-variable">applicationContext</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">AnnotationConfigApplicationContext</span>(FactoryBeanMain.class);<br><span class="hljs-type">BeanOne</span> <span class="hljs-variable">beanOne</span> <span class="hljs-operator">=</span> (BeanOne)applicationContext.getBean(<span class="hljs-string">&quot;cusBean&quot;</span>);<br></code></pre></td></tr></table></figure>
<p><code>getBean()</code> 整体调用流程如下</p>
<figure class="highlight leaf"><table><tr><td class="code"><pre><code class="hljs leaf">AbstractApplicationContext<span class="hljs-punctuation">#</span><span class="hljs-keyword">getBean</span><span class="hljs-params">(<span class="hljs-variable">String</span>)</span><br>    AbstractBeanFactory<span class="hljs-punctuation">#</span><span class="hljs-keyword">getBean</span><span class="hljs-params">(<span class="hljs-variable">String</span>)</span><br>        AbstractBeanFactory<span class="hljs-punctuation">#</span><span class="hljs-keyword">doGetBean</span><span class="hljs-params">(<span class="hljs-variable">String</span>,<span class="hljs-variable">Class</span>,<span class="hljs-variable">Object</span>[],<span class="hljs-variable">boolean</span>)</span><br>            AbstractBeanFactory<span class="hljs-punctuation">#</span><span class="hljs-keyword">transformedBeanName</span><span class="hljs-params">(<span class="hljs-variable">String</span> <span class="hljs-variable">name</span>)</span>   【这里是将name转换成 beanName, 即将 &amp;cusBean 转换成 cusBean】<br>            DefaultSingletonBeanRegistry<span class="hljs-punctuation">#</span><span class="hljs-keyword">getSingleton</span><span class="hljs-params">(<span class="hljs-variable">String</span>)</span> 【这里获取的实例还是 CusFactoryBean 对象】<br>            AbstractAutowireCapableBeanFactory<span class="hljs-punctuation">#</span><span class="hljs-keyword">getObjectForBeanInstance</span><span class="hljs-params">(<span class="hljs-variable">beanInstance</span>, <span class="hljs-variable">name</span>, <span class="hljs-variable">beanName</span>, <span class="hljs-variable">RootBeanDefinition</span>)</span><br>                AbstractBeanFactory<span class="hljs-punctuation">#</span><span class="hljs-keyword">getObjectForBeanInstance</span><span class="hljs-params">(<span class="hljs-variable">beanInstance</span>, <span class="hljs-variable">name</span>, <span class="hljs-variable">beanName</span>, <span class="hljs-variable">RootBeanDefinition</span>)</span><br>                    FactoryBeanRegistrySupport<span class="hljs-punctuation">#</span><span class="hljs-keyword">getObjectFromFactoryBean</span><br>                        FactoryBeanRegistrySupport<span class="hljs-punctuation">#</span><span class="hljs-keyword">doGetObjectFromFactoryBean</span><span class="hljs-params">(<span class="hljs-variable">FactoryBean</span><span class="hljs-operator">&lt;</span>?<span class="hljs-operator">&gt;</span>, <span class="hljs-variable">beanName</span>)</span>  【这里面会调用 CusFactoryBean 的 getObject() 方法】<br><br></code></pre></td></tr></table></figure>
<p>单例 <code>Bean</code> 默认都是放在 <code>DefaultSingletonBeanRegistry</code> 类的 <code>singletonObjects</code> 中</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">DefaultSingletonBeanRegistry</span> &#123;<br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> Map&lt;String, Object&gt; singletonObjects = <span class="hljs-keyword">new</span> <span class="hljs-title class_">ConcurrentHashMap</span>&lt;&gt;(<span class="hljs-number">256</span>);<br>&#125;<br></code></pre></td></tr></table></figure>
<ul>
<li>对于 <code>FacotryBean</code> 而言，在上面例子中最终 <code>singletonObjects</code> 中的 <code>key=cusBean</code>, <code>value=CusFactoryBean</code></li>
<li><code>FactoryBean#getObject()</code> 返回的实例最终是缓存在 <code>FactoryBeanRegistrySupport</code> 中 <code>factoryBeanObjectCache</code> 的 <code>key=cusBean</code>, <code>value=BeanOne</code><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">FactoryBeanRegistrySupport</span> &#123;<br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> Map&lt;String, Object&gt; factoryBeanObjectCache = <span class="hljs-keyword">new</span> <span class="hljs-title class_">ConcurrentHashMap</span>&lt;&gt;(<span class="hljs-number">16</span>);<br>&#125;<br></code></pre></td></tr></table></figure></li>
</ul>
<h3 id="直接获取-FactoryBean-的流程"><a href="#直接获取-FactoryBean-的流程" class="headerlink" title="直接获取 FactoryBean 的流程"></a>直接获取 FactoryBean 的流程</h3><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-type">ApplicationContext</span> <span class="hljs-variable">applicationContext</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">AnnotationConfigApplicationContext</span>(FactoryBeanMain.class);<br><span class="hljs-type">Object</span> <span class="hljs-variable">cusBean</span> <span class="hljs-operator">=</span> applicationContext.getBean(<span class="hljs-string">&quot;&amp;cusBean&quot;</span>);<br></code></pre></td></tr></table></figure>
<p>这里示例中传递的 <code>beanName</code> 是 <code>&amp;cusBean</code>，不过从上面的流程也知道了，无论是 <code>DefaultSingletonBeanRegistry</code> 类的 <code>singletonObjects</code> 还是 <code>FactoryBeanRegistrySupport</code> 中 <code>factoryBeanObjectCache</code> 实际存储的 <code>beanName</code> 都是 <code>cusBean</code>, 并没有 <code>&amp;cusBean</code>，正因为如此，在上面流程中才会有 <code>AbstractBeanFactory#transformedBeanName</code> 方法对我们传递的 <code>name</code> 转换成 <code>beanName</code></p>
<figure class="highlight leaf"><table><tr><td class="code"><pre><code class="hljs leaf">AbstractApplicationContext<span class="hljs-punctuation">#</span><span class="hljs-keyword">getBean</span><span class="hljs-params">(<span class="hljs-variable">String</span>)</span><br>    AbstractBeanFactory<span class="hljs-punctuation">#</span><span class="hljs-keyword">getBean</span><span class="hljs-params">(<span class="hljs-variable">String</span>)</span><br>        AbstractBeanFactory<span class="hljs-punctuation">#</span><span class="hljs-keyword">doGetBean</span><span class="hljs-params">(<span class="hljs-variable">String</span>,<span class="hljs-variable">Class</span>,<span class="hljs-variable">Object</span>[],<span class="hljs-variable">boolean</span>)</span><br>            AbstractBeanFactory<span class="hljs-punctuation">#</span><span class="hljs-keyword">transformedBeanName</span><span class="hljs-params">(<span class="hljs-variable">String</span> <span class="hljs-variable">name</span>)</span>   【这里是将name转换成 beanName, 即将 &amp;cusBean 转换成 cusBean】<br></code></pre></td></tr></table></figure>
<p><strong>需要记住的是我们传递的名称是 <code>name=&amp;cusBean</code>, 转换后是 <code>beanName=cusBean</code></strong><br>既然最终获取的 <code>beanName</code> 都是 <code>cusBean</code>，就说明是从 <code>DefaultSingletonBeanRegistry</code> 类的 <code>singletonObjects</code> 中获取实例就可以了，后面的流程需要阻挡，具体挡的逻辑是在 <code>AbstractBeanFactory#getObjectForBeanInstance</code> 中，代码如下</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">AbstractBeanFactory</span> &#123;<br>    <span class="hljs-keyword">protected</span> Object <span class="hljs-title function_">getObjectForBeanInstance</span><span class="hljs-params">(</span><br><span class="hljs-params">			Object beanInstance, String name, String beanName, <span class="hljs-meta">@Nullable</span> RootBeanDefinition mbd)</span> &#123;<br><br>		<span class="hljs-comment">// 就是在这里判断挡住的, 这里直接判断name是否以 &amp; 开头</span><br>		<span class="hljs-keyword">if</span> (BeanFactoryUtils.isFactoryDereference(name)) &#123;<br>			<span class="hljs-keyword">if</span> (beanInstance <span class="hljs-keyword">instanceof</span> NullBean) &#123;<br>				<span class="hljs-keyword">return</span> beanInstance;<br>			&#125;<br>			<span class="hljs-keyword">if</span> (!(beanInstance <span class="hljs-keyword">instanceof</span> FactoryBean)) &#123;<br>				<span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">BeanIsNotAFactoryException</span>(beanName, beanInstance.getClass());<br>			&#125;<br>			<span class="hljs-keyword">if</span> (mbd != <span class="hljs-literal">null</span>) &#123;<br>				mbd.isFactoryBean = <span class="hljs-literal">true</span>;<br>			&#125;<br>            <span class="hljs-comment">// 因为我们传递的是 &amp;cusBean，所以这里就直接返回了，并没有走后面的逻辑</span><br>            <span class="hljs-comment">// 那么返回的就是 `DefaultSingletonBeanRegistry` 类的 `singletonObjects` </span><br>            <span class="hljs-comment">// 中获取的 Bean</span><br>			<span class="hljs-keyword">return</span> beanInstance;<br>		&#125;<br>        <span class="hljs-comment">// 省略其他代码 ……</span><br>	&#125;<br>&#125;<br><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">BeanFactoryUtils</span> &#123;<br>    <span class="hljs-type">String</span> <span class="hljs-variable">FACTORY_BEAN_PREFIX</span> <span class="hljs-operator">=</span> <span class="hljs-string">&quot;&amp;&quot;</span>;<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-type">boolean</span> <span class="hljs-title function_">isFactoryDereference</span><span class="hljs-params">(<span class="hljs-meta">@Nullable</span> String name)</span> &#123;<br>		<span class="hljs-keyword">return</span> (name != <span class="hljs-literal">null</span> &amp;&amp; name.startsWith(BeanFactory.FACTORY_BEAN_PREFIX));<br>	&#125;<br>&#125;<br></code></pre></td></tr></table></figure>



]]></content>
      <categories>
        <category>java</category>
        <category>Spring</category>
      </categories>
      <tags>
        <tag>java</tag>
        <tag>Spring</tag>
      </tags>
  </entry>
  <entry>
    <title>Spring中Import注解源码解析</title>
    <url>/2023/73d35f59945c/index.html</url>
    <content><![CDATA[<span class="hide_first_mermaid">
<div class="mermaid-wrap"><pre class="mermaid-src" hidden>
  flowchart LR
    A
  </pre></div>
</span>

<h2 id="结论"><a href="#结论" class="headerlink" title="结论"></a>结论</h2><p><code>@Import</code> 注解可以导入三种类型的类</p>
<ol>
<li>导入普通类，此时直接将普通类交给 <code>Spring</code> 管理</li>
<li>导入 <code>ImportSelector</code> 实现类中 <code>String[] selectImports(AnnotationMetadata importingClassMetadata);</code> 方法返回的类</li>
<li>导入 <code>ImportBeanDefinitionRegistrar</code> 实现类中 <code>registerBeanDefinitions</code> 方法注入的类</li>
</ol>
<h2 id="代码示例"><a href="#代码示例" class="headerlink" title="代码示例"></a>代码示例</h2><p>以下代码每种方式使用的类都在同一个包下( <code>@Configuration</code> 默认会扫描当前包和子包 )。</p>
<div class="tabs" id="import的三种使用方式"><ul class="nav-tabs"><button type="button" class="tab  active" data-href="import的三种使用方式-1">引入普通类</button><button type="button" class="tab " data-href="import的三种使用方式-2">结合 ImportSelector</button><button type="button" class="tab " data-href="import的三种使用方式-3">结合 ImportBeanDefinitionRegistrar</button></ul><div class="tab-contents"><div class="tab-item-content active" id="import的三种使用方式-1"><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-comment">// 普通类</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">Student</span> &#123;<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">sayHello</span><span class="hljs-params">()</span> &#123;<br>        System.out.println(<span class="hljs-string">&quot;test hello&quot;</span>);<br>    &#125;<br>&#125;<br><br><span class="hljs-comment">//  配置类</span><br><span class="hljs-meta">@Configuration</span><br><span class="hljs-meta">@Import(Student.class)</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">ImportConfig</span> &#123;<br>&#125;<br><br><span class="hljs-comment">//  主类</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">ImportMain</span> &#123;<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">main</span><span class="hljs-params">(String[] args)</span> &#123;<br>        <span class="hljs-type">AnnotationConfigApplicationContext</span> <span class="hljs-variable">context</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">AnnotationConfigApplicationContext</span>(ImportConfig.class);<br>        <span class="hljs-type">Student</span> <span class="hljs-variable">student</span> <span class="hljs-operator">=</span> context.getBean(Student.class);<br>        student.sayHello();  <span class="hljs-comment">// 会打印 &quot;test hello&quot;</span><br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure></div><div class="tab-item-content" id="import的三种使用方式-2"><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-comment">// 普通类</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">Student</span> &#123;<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">sayHello</span><span class="hljs-params">()</span> &#123;<br>        System.out.println(<span class="hljs-string">&quot;test hello&quot;</span>);<br>    &#125;<br>&#125;<br><br><span class="hljs-comment">//  配置类</span><br><span class="hljs-meta">@Configuration</span><br><span class="hljs-meta">@Import(CusImportSelector.class)</span>  <span class="hljs-comment">// 这里导入的是实现了 ImportSelector 接口的类</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">ImportConfig</span> &#123;<br>&#125;<br><br><span class="hljs-comment">// 自定义实现 ImportSelector 接口的类</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">CusImportSelector</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">ImportSelector</span> &#123;<br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> String[] selectImports(AnnotationMetadata importingClassMetadata) &#123;<br>        <span class="hljs-comment">// 返回的数组就是需要导入的类全限定名</span><br>        <span class="hljs-keyword">return</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">String</span>[]&#123;<span class="hljs-string">&quot;org.example.imports.Student&quot;</span>&#125;;<br>    &#125;<br>&#125;<br><br><span class="hljs-comment">//  主类</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">ImportMain</span> &#123;<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">main</span><span class="hljs-params">(String[] args)</span> &#123;<br>        <span class="hljs-type">AnnotationConfigApplicationContext</span> <span class="hljs-variable">context</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">AnnotationConfigApplicationContext</span>(ImportConfig.class);<br>        <span class="hljs-type">Student</span> <span class="hljs-variable">student</span> <span class="hljs-operator">=</span> context.getBean(Student.class);<br>        student.sayHello();  <span class="hljs-comment">// 会打印 &quot;test hello&quot;</span><br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure></div><div class="tab-item-content" id="import的三种使用方式-3"><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-comment">// 普通类</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">Student</span> &#123;<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">sayHello</span><span class="hljs-params">()</span> &#123;<br>        System.out.println(<span class="hljs-string">&quot;test hello&quot;</span>);<br>    &#125;<br>&#125;<br><br><span class="hljs-comment">//  配置类</span><br><span class="hljs-meta">@Configuration</span><br><span class="hljs-meta">@Import(CusImportBeanDefinitionRegistrar.class)</span>  <span class="hljs-comment">// 这里导入的是实现了 ImportSelector 接口的类</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">ImportConfig</span> &#123;<br>&#125;<br><br><span class="hljs-comment">// 自定义实现 ImportBeanDefinitionRegistrar 接口的类</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">CusImportBeanDefinitionRegistrar</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">ImportBeanDefinitionRegistrar</span> &#123;<br><br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">registerBeanDefinitions</span><span class="hljs-params">(AnnotationMetadata importingClassMetadata, BeanDefinitionRegistry registry, BeanNameGenerator importBeanNameGenerator)</span> &#123;<br>        <span class="hljs-type">BeanDefinitionBuilder</span> <span class="hljs-variable">beanDefinitionBuilder</span> <span class="hljs-operator">=</span> BeanDefinitionBuilder.genericBeanDefinition(Student.class);<br>        <span class="hljs-type">AbstractBeanDefinition</span> <span class="hljs-variable">beanDefinition</span> <span class="hljs-operator">=</span> beanDefinitionBuilder.getBeanDefinition();<br>        <span class="hljs-type">String</span> <span class="hljs-variable">beanName</span> <span class="hljs-operator">=</span> importBeanNameGenerator.generateBeanName(beanDefinition, registry);<br>        registry.registerBeanDefinition(beanName, beanDefinition);<br>    &#125;<br>&#125;<br><br><span class="hljs-comment">//  主类</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">ImportMain</span> &#123;<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">main</span><span class="hljs-params">(String[] args)</span> &#123;<br>        <span class="hljs-type">AnnotationConfigApplicationContext</span> <span class="hljs-variable">context</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">AnnotationConfigApplicationContext</span>(ImportConfig.class);<br>        <span class="hljs-type">Student</span> <span class="hljs-variable">student</span> <span class="hljs-operator">=</span> context.getBean(Student.class);<br>        student.sayHello();  <span class="hljs-comment">// 会打印 &quot;test hello&quot;</span><br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure></div></div><div class="tab-to-top"><button type="button" aria-label="scroll to top"><i class="fas fa-arrow-up"></i></button></div></div>

<h2 id="源码流程解析"><a href="#源码流程解析" class="headerlink" title="源码流程解析"></a>源码流程解析</h2><p><code>@Import</code> 的解析实际是通过 <code>BeanFactoryPostProcessor</code> 实现的， 在 <ol><li><a href="/2023/e7d5d1305e5d/index.html" title="BeanFacotry的后置处理器详解">BeanFacotry的后置处理器详解</a></li></ol> 提到 <code>@ComponentScan</code> 是通过 <code>ConfigurationClassPostProcessor</code> 处理的，处理流程如下：</p>
<figure class="highlight leaf"><table><tr><td class="code"><pre><code class="hljs leaf">AbstractApplicationContext<span class="hljs-punctuation">#</span><span class="hljs-keyword">refresh</span><br>    AbstractApplicationContext<span class="hljs-punctuation">#</span><span class="hljs-keyword">invokeBeanFactoryPostProcessors</span><br>        PostProcessorRegistrationDelegate<span class="hljs-punctuation">#</span><span class="hljs-keyword">invokeBeanDefinitionRegistryPostProcessors</span><br>            ConfigurationClassPostProcessor<span class="hljs-punctuation">#</span><span class="hljs-keyword">postProcessBeanDefinitionRegistry</span><br>                ConfigurationClassParser<span class="hljs-punctuation">#</span><span class="hljs-keyword">parse</span><span class="hljs-params">(<span class="hljs-variable">Set</span><span class="hljs-operator">&lt;</span><span class="hljs-variable">BeanDefinitionHolder</span><span class="hljs-operator">&gt;</span>)</span><br>                    ConfigurationClassParser<span class="hljs-punctuation">#</span><span class="hljs-keyword">doProcessConfigurationClass</span><br></code></pre></td></tr></table></figure>
<p>最终是会调用到 <code>ConfigurationClassParser#doProcessConfigurationClass</code> 方法，接下来就看看这个方法</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">protected</span> <span class="hljs-keyword">final</span> SourceClass <span class="hljs-title function_">doProcessConfigurationClass</span><span class="hljs-params">(</span><br><span class="hljs-params">			ConfigurationClass configClass, SourceClass sourceClass, Predicate&lt;String&gt; filter)</span><br>			<span class="hljs-keyword">throws</span> IOException &#123;<br><br>    <span class="hljs-comment">// 省略</span><br><br>    <span class="hljs-comment">// Process any @PropertySource annotations</span><br>    <span class="hljs-keyword">for</span> (AnnotationAttributes propertySource : AnnotationConfigUtils.attributesForRepeatable(<br>            sourceClass.getMetadata(), PropertySources.class,<br>            org.springframework.context.annotation.PropertySource.class)) &#123;<br>        <span class="hljs-keyword">if</span> (<span class="hljs-built_in">this</span>.propertySourceRegistry != <span class="hljs-literal">null</span>) &#123;<br>            <span class="hljs-built_in">this</span>.propertySourceRegistry.processPropertySource(propertySource);<br>        &#125;<br>        <span class="hljs-keyword">else</span> &#123;<br>            logger.info(<span class="hljs-string">&quot;Ignoring @PropertySource annotation on [&quot;</span> + sourceClass.getMetadata().getClassName() +<br>                    <span class="hljs-string">&quot;]. Reason: Environment must implement ConfigurableEnvironment&quot;</span>);<br>        &#125;<br>    &#125;<br><br>    <span class="hljs-comment">//  这里是处理 @ComponentScan  Process any @ComponentScan annotations</span><br>    Set&lt;AnnotationAttributes&gt; componentScans = AnnotationConfigUtils.attributesForRepeatable(<br>            sourceClass.getMetadata(), ComponentScans.class, ComponentScan.class);<br>    <span class="hljs-keyword">if</span> (!componentScans.isEmpty() &amp;&amp;<br>            !<span class="hljs-built_in">this</span>.conditionEvaluator.shouldSkip(sourceClass.getMetadata(), ConfigurationPhase.REGISTER_BEAN)) &#123;<br>        <span class="hljs-comment">// 省略</span><br>    &#125;<br><br>    <span class="hljs-comment">// 处理 @Import Process any @Import annotations</span><br>    processImports(configClass, sourceClass, getImports(sourceClass), filter, <span class="hljs-literal">true</span>);<br><br>    <span class="hljs-comment">// 省略  处理 @ImportResource 等  Process any @ImportResource annotations</span><br>    <span class="hljs-keyword">return</span> <span class="hljs-literal">null</span>;<br>&#125;<br></code></pre></td></tr></table></figure>

<p><code>processImports</code> 方法</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-comment">// ConfigurationClassParser 类， 该方法只列出了重要方法</span><br><span class="hljs-keyword">private</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">processImports</span><span class="hljs-params">(ConfigurationClass configClass, SourceClass currentSourceClass,</span><br><span class="hljs-params">			Collection&lt;SourceClass&gt; importCandidates, Predicate&lt;String&gt; exclusionFilter,</span><br><span class="hljs-params">			<span class="hljs-type">boolean</span> checkForCircularImports)</span> &#123;<br><br>    <span class="hljs-keyword">for</span> (SourceClass candidate : importCandidates) &#123;<br>        <span class="hljs-comment">// 导入的是 ImportSelector 类型的处理流程</span><br>        <span class="hljs-keyword">if</span> (candidate.isAssignable(ImportSelector.class)) &#123;<br>            <span class="hljs-comment">// Candidate class is an ImportSelector -&gt; delegate to it to determine imports</span><br>            Class&lt;?&gt; candidateClass = candidate.loadClass();<br>            <span class="hljs-type">ImportSelector</span> <span class="hljs-variable">selector</span> <span class="hljs-operator">=</span> ParserStrategyUtils.instantiateClass(candidateClass, ImportSelector.class,<br>                    <span class="hljs-built_in">this</span>.environment, <span class="hljs-built_in">this</span>.resourceLoader, <span class="hljs-built_in">this</span>.registry);<br>            Predicate&lt;String&gt; selectorFilter = selector.getExclusionFilter();<br>            <span class="hljs-keyword">if</span> (selectorFilter != <span class="hljs-literal">null</span>) &#123;<br>                exclusionFilter = exclusionFilter.or(selectorFilter);<br>            &#125;<br>            <span class="hljs-keyword">if</span> (selector <span class="hljs-keyword">instanceof</span> DeferredImportSelector deferredImportSelector) &#123;<br>                <span class="hljs-built_in">this</span>.deferredImportSelectorHandler.handle(configClass, deferredImportSelector);<br>            &#125;<br>            <span class="hljs-keyword">else</span> &#123;<br>                String[] importClassNames = selector.selectImports(currentSourceClass.getMetadata());<br>                Collection&lt;SourceClass&gt; importSourceClasses = asSourceClasses(importClassNames, exclusionFilter);<br>                processImports(configClass, currentSourceClass, importSourceClasses, exclusionFilter, <span class="hljs-literal">false</span>);<br>            &#125;<br>        &#125;<br>        <span class="hljs-comment">// 导入的是 ImportBeanDefinitionRegistrar 类型的处理流程</span><br>        <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (candidate.isAssignable(ImportBeanDefinitionRegistrar.class)) &#123;<br>            <span class="hljs-comment">// Candidate class is an ImportBeanDefinitionRegistrar -&gt;</span><br>            <span class="hljs-comment">// delegate to it to register additional bean definitions</span><br>            Class&lt;?&gt; candidateClass = candidate.loadClass();<br>            <span class="hljs-type">ImportBeanDefinitionRegistrar</span> <span class="hljs-variable">registrar</span> <span class="hljs-operator">=</span><br>                    ParserStrategyUtils.instantiateClass(candidateClass, ImportBeanDefinitionRegistrar.class,<br>                            <span class="hljs-built_in">this</span>.environment, <span class="hljs-built_in">this</span>.resourceLoader, <span class="hljs-built_in">this</span>.registry);<br>            configClass.addImportBeanDefinitionRegistrar(registrar, currentSourceClass.getMetadata());<br>        &#125;<br>        <span class="hljs-keyword">else</span> &#123;<br>            <span class="hljs-comment">// 既不是 ImportSelector 也不是 ImportBeanDefinitionRegistrar 类型的处理流程，当作 普通的 @Configuration 类处理</span><br>            <span class="hljs-built_in">this</span>.importStack.registerImport(<br>                    currentSourceClass.getMetadata(), candidate.getMetadata().getClassName());<br>            processConfigurationClass(candidate.asConfigClass(configClass), exclusionFilter);<br>        &#125;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<p><strong>导入的是普通类是的处理流程</strong></p>
<p>从上面源码可以看到，如果导入的是普通的类（不是 <code>ImportSelector</code> 也不是 <code>ImportBeanDefinitionRegistrar</code> 类型）则将这个普通类按照含有 <code>@Configuration</code> 注解的类一样进行处理, 如果是按照 <code>@Configuration</code> 注解的类进行处理，会将普通类变成 <code>ConfigurationClass</code> 类型</p>
<p><strong>导入的是 <code>ImportSelector</code> 类型</strong></p>
<p>对于导入的是 <code>ImportSelector</code> 类型处理代码如下</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">if</span> (candidate.isAssignable(ImportSelector.class)) &#123;<br>    <span class="hljs-comment">// Candidate class is an ImportSelector -&gt; delegate to it to determine imports</span><br>    Class&lt;?&gt; candidateClass = candidate.loadClass();<br>    <span class="hljs-type">ImportSelector</span> <span class="hljs-variable">selector</span> <span class="hljs-operator">=</span> ParserStrategyUtils.instantiateClass(candidateClass, ImportSelector.class,<br>            <span class="hljs-built_in">this</span>.environment, <span class="hljs-built_in">this</span>.resourceLoader, <span class="hljs-built_in">this</span>.registry);<br>    Predicate&lt;String&gt; selectorFilter = selector.getExclusionFilter();<br>    <span class="hljs-keyword">if</span> (selectorFilter != <span class="hljs-literal">null</span>) &#123;<br>        exclusionFilter = exclusionFilter.or(selectorFilter);<br>    &#125;<br>    <span class="hljs-keyword">if</span> (selector <span class="hljs-keyword">instanceof</span> DeferredImportSelector deferredImportSelector) &#123;<br>        <span class="hljs-built_in">this</span>.deferredImportSelectorHandler.handle(configClass, deferredImportSelector);<br>    &#125;<br>    <span class="hljs-keyword">else</span> &#123;<br>        String[] importClassNames = selector.selectImports(currentSourceClass.getMetadata());<br>        Collection&lt;SourceClass&gt; importSourceClasses = asSourceClasses(importClassNames, exclusionFilter);<br>        processImports(configClass, currentSourceClass, importSourceClasses, exclusionFilter, <span class="hljs-literal">false</span>);<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>
<ol>
<li>先调用 <code>selectImports</code> 方法获取导入的类名数组</li>
<li>调用 <code>asSourceClasses</code> 方法将类名数组转换为 <code>SourceClass</code> 集合</li>
<li>调用 <code>processImports</code> 方法处理 <code>SourceClass</code> 数组， 这里就是递归调用了，递归的第一层是 <code>ImportSelector</code> 类型的类，递归的第二层就是 <code>selectImports</code> 方法中返回的普通的类了，这个就和 <code>Import</code> 导入的是普通类一样的处理流程，也就是最终是按照 <code>@Configuration</code> 注解的类处理</li>
</ol>
<p><strong>导入的是 <code>ImportBeanDefinitionRegistrar</code> 类型</strong></p>
<p>对于导入的是 <code>ImportBeanDefinitionRegistrar</code> 类型处理代码如下</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">if</span> (candidate.isAssignable(ImportBeanDefinitionRegistrar.class)) &#123;<br>    <span class="hljs-comment">// Candidate class is an ImportBeanDefinitionRegistrar -&gt;</span><br>    <span class="hljs-comment">// delegate to it to register additional bean definitions</span><br>    Class&lt;?&gt; candidateClass = candidate.loadClass();<br>    <span class="hljs-type">ImportBeanDefinitionRegistrar</span> <span class="hljs-variable">registrar</span> <span class="hljs-operator">=</span><br>            ParserStrategyUtils.instantiateClass(candidateClass, ImportBeanDefinitionRegistrar.class,<br>                    <span class="hljs-built_in">this</span>.environment, <span class="hljs-built_in">this</span>.resourceLoader, <span class="hljs-built_in">this</span>.registry);<br>    <span class="hljs-comment">// configClass 就是 ConfigurationClass</span><br>    configClass.addImportBeanDefinitionRegistrar(registrar, currentSourceClass.getMetadata()); <br>&#125;<br></code></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-comment">/**</span><br><span class="hljs-comment"> *   ConfigurationClass 类</span><br><span class="hljs-comment"> * </span><br><span class="hljs-comment"> *  private final Map&lt;ImportBeanDefinitionRegistrar, AnnotationMetadata&gt; importBeanDefinitionRegistrars = new LinkedHashMap&lt;&gt;();</span><br><span class="hljs-comment"> * /</span><br><span class="hljs-comment">void addImportBeanDefinitionRegistrar(ImportBeanDefinitionRegistrar registrar, AnnotationMetadata importingClassMetadata) &#123;</span><br><span class="hljs-comment">    this.importBeanDefinitionRegistrars.put(registrar, importingClassMetadata);</span><br><span class="hljs-comment">&#125;</span><br></code></pre></td></tr></table></figure>
<p>最终是将 <code>ImportBeanDefinitionRegistrar</code> 实现类放入了 <code>ConfigurationClass</code> 类中的 <code>importBeanDefinitionRegistrars</code> 属性中，那么最终是什么时候注册成 <code>BeanDefinition</code> 的呢？</p>
<p>这个就得回到 <code>org.springframework.context.annotation.ConfigurationClassPostProcessor#processConfigBeanDefinitions</code> 方法中了， 这里通过流程图梳理一下解析和注册两个步骤的流程</p>
<pre class="mermaid">sequenceDiagram
    autonumber
    participant A as ConfigurationClassPostProcessor
    participant B as ConfigurationClassParser
    participant C as ConfigurationClassBeanDefinitionReader
    activate A
    A->>A: processConfigBeanDefinitions()
    A->>B: parse()
    B->>B: doProcessConfigurationClass（）
    A->>C: loadBeanDefinitions()
    C->>C: loadBeanDefinitionsFromRegistrars()
    deactivate  A</pre>

<ol>
<li>整体逻辑是在 <code>ConfigurationClassPostProcessor</code> 的 <code>processConfigBeanDefinitions()</code> 方法中执行的</li>
<li>对于 <code>@ComponentScan</code>, <code>@Import</code> 等解析是在 <code>ConfiguraionClassParser</code> 的 <code>doProcessConfigurationClass()</code> 方法中解析的</li>
<li>对于 <code>ImportBeanDefinitionRegistrar</code> 类型的类在第二步解析完成之后放入了 <code>ConfigurationClass</code> 类型（就是将 <code>ImportBeanDefinitionRegistrar</code> 类型的实现类转换成 <code>ConfigurationClass</code> 类型而已）中的 <code>importBeanDefinitionRegistrars</code> 属性中</li>
<li>最后在 <code>ConfigurationClassBeanDefinitionReader</code> 类的 <code>loadBeanDefinitionsFromRegistrars()</code> 方法中会进行 <code>BeanDefinition</code> 的注册</li>
</ol>
<p>至此，解析和注册两个步骤的流程就解析完了</p>
]]></content>
      <categories>
        <category>java</category>
        <category>Spring</category>
      </categories>
      <tags>
        <tag>java</tag>
        <tag>Spring</tag>
      </tags>
  </entry>
  <entry>
    <title>如何让自定义的某个bean在所有bean之前加载</title>
    <url>/2023/5cb19cbceeb7/index.html</url>
    <content><![CDATA[<p>可以通过 <code>@DependsOn</code> 注解来控制某个 <code>bean</code> 的加载顺序，但是如果有控制项目中让某个 <code>Bean</code> 先于其他的 <code>Bean</code> 加载，这种方式就不可行，所以还是需要从 <code>Spring</code> 中加载 <code>Bean</code> 的机制入手</p>
<ol>
<li>通过 <code>BeanFacotry.getBean()</code> 方法主动去加载 <code>Bean</code></li>
<li>所有非懒加载的 <code>Bean</code> 会在 <code>AbstractApplicationContext#finishBeanFactoryInitialization()</code> 方法中进行加载</li>
</ol>
<p>如果是通过第二种方式(自动加载)，那么 <code>Bean</code> 加载时机就不是可控的，所以只能使用第一种方式，进行主动加载</p>
<p>使用主动加载的方法需要获取到 <code>BeanFactory</code>，而 <code>BeanFactoryPostProcessor</code> 方法就可以提供 <code>beanFactory</code>， 而且 <code>BeanFactoryPostProcessor</code> 的执行时机是所有 <code>Bean</code> 加载之前</p>
<p><strong>示例代码</strong></p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-meta">@Component</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">CusBeanFactoryPostProcessor</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">BeanFactoryPostProcessor</span> &#123;<br><br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">postProcessBeanFactory</span><span class="hljs-params">(ConfigurableListableBeanFactory beanFactory)</span> <span class="hljs-keyword">throws</span> BeansException &#123;<br>        <span class="hljs-comment">// 主动获取 FirstDemo</span><br>        beanFactory.getBean(FirstDemo.class);<br>    &#125;<br>&#125;<br><br><span class="hljs-meta">@Component</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">FirstDemo</span> &#123;<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">sayHello</span><span class="hljs-params">()</span> &#123;<br>        System.out.println(<span class="hljs-string">&quot;FirstDemo Hello&quot;</span>);<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>java</category>
        <category>Spring</category>
      </categories>
      <tags>
        <tag>java</tag>
        <tag>Spring</tag>
      </tags>
  </entry>
  <entry>
    <title>SpringBoot3自动装配原理</title>
    <url>/2023/8e23a1d3d97d/index.html</url>
    <content><![CDATA[<span class="hide_first_mermaid">
<div class="mermaid-wrap"><pre class="mermaid-src" hidden>
  flowchart LR
    A
  </pre></div>
</span>

<p>SpringBoot版本: 3.2.0</p>
<h2 id="什么是自动装配"><a href="#什么是自动装配" class="headerlink" title="什么是自动装配"></a>什么是自动装配</h2><p><strong>装配的是什么？</strong></p>
<p>装配的就是 <code>bean</code>, 也就是可以将一个 对象交给 <code>Spring</code> 容器管理</p>
<p><strong>为什么可以自动？</strong></p>
<p><code>SpringBoot</code> 秉持的就是 <strong>约定优于配置</strong>，所以要想做到就得有一定的规则，比如</p>
<ul>
<li>总得要告诉 <code>Spring</code> 我们想要装配的是什么类，通过什么方式告诉呢？ 就是将想要注入的类集中写在一个固定位置的配置文件中<ul>
<li>在 2.7.0 之前使用的配置文件名称是 <code>spring.factories</code>, 这个文件的位置是  <code>META-INF/spring.factories</code></li>
<li>在 2.7.0 之后使用的配置文件名称是 <code>org.springframework.boot.autoconfigure.AutoConfiguration.imports</code>, 这个文件的位置是  <code>META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports</code></li>
</ul>
</li>
<li>可以认为配置文件是开发者需要提供的，当然 <code>SpringBoot</code> 内部也使用了这种机制，既然提供了配置文件，那么 <code>Spring</code> 框架就需要按照约定的位置去找这些类了，这个在 <code>SpringBoot</code> 中是通过 <code>@Import</code> 注解导入了一个 <code>DeferredImportSelector</code> 实现类来实现的，这个实现类是 <code>AutoConfigurationImportSelector</code></li>
<li>在 <code>SpringBoot</code> 中还提供了一种机制，就是通过一系列的条件判断我们在配置文件中写的这些类是否真的可以导入，这一系列条件是通过 <code>@ConditionalOnxxxx</code> 注解来实现</li>
</ul>
<p>接下来简单总结一下 <code>SpringBoot3</code> 的自动装配原理</p>
<ol>
<li>通过 <code>@Import</code> 导入 <code>AutoConfigurationImportSelector</code> 类， 可以参考 <ol><li><a href="/2023/73d35f59945c/index.html" title="Spring中Import注解源码解析">Spring中Import注解源码解析</a></li></ol></li>
<li><code>AutoConfigurationImportSelector</code> 会去找所有 <code>classpath</code> 下所有 <code>jar</code> 下的 <code>META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports</code> 文件，将文件里面的全限定类名加载进来</li>
<li>对加载加来的类进行一系列的条件判断，符合条件的才会真的进行加载到 <code>Spring</code> 容器中</li>
</ol>
<h2 id="自动装配的示例"><a href="#自动装配的示例" class="headerlink" title="自动装配的示例"></a>自动装配的示例</h2><ol><li><a href="/2023/ee343b448bf4/index.html" title="SpringBoot3自动装配案例">SpringBoot3自动装配案例</a></li></ol>

<h2 id="SpringBoot3-自动装配的流程"><a href="#SpringBoot3-自动装配的流程" class="headerlink" title="SpringBoot3 自动装配的流程"></a>SpringBoot3 自动装配的流程</h2><p><code>SpringBoot</code> 项目启动主类上只有一个 <code>SpringBootApplication</code> 注解，我们看一下这个注解的体系</p>
<pre class="mermaid">flowchart LR
    A["@SpringBootApplication"]
    style A fill:#8ff600
    B["@ComponentScan"]
    C["@EnableAutoConfiguration"]
    style C fill:#8ff600
    D["@SpringBootConfiguration"]

    A--> B & C & D

    E["@Configuration"]
    D-->E

    F["@AutoConfigurationPackage"]
    G["@Import(AutoConfigurationPackages.Registrar.class)"]
    C-->F
    F-->G

    H["@Import(AutoConfigurationImportSelector.class)"]
    style H fill:#8ff600
    C-->H</pre>

<p>涉及到自动注入的注解使用绿色标记出来了，所以最终需要看的就是 <code>AutoConfigurationImportSelector</code>， 而这个类是一个 <code>DeferredImportSelector</code> 接口的实现类，具体参考 <ol><li><a href="/2023/55186be8f39b/index.html" title="Spring中DeferredImportSelector源码解析">Spring中DeferredImportSelector源码解析</a></li></ol>， 这里就直接给出 <code>DeferredImportSelector</code> 接口的执行流程</p>
<pre class="mermaid">flowchart TB
    A["Group实现类中的 process() 方法"]
    B["DeferredImportSelector实现类 
     中的 selectImports() 方法 
     （该方法不一定执行，当 Group 实现类中
     有调用的时候才会执行，没有调用就没有执行
     SpringBoot自动装配时就没有执行 selectImports() 方法）"]
    C["Group实现类中的 selectImports() 方法
    这个方法的返回值才是真正要被
    导入的类"]
    A-->B-->C</pre>

<p>先看一下 <code>AutoConfigurationImportSelector</code> 这个类中相关的代码（只展示主流程部分）</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">AutoConfigurationImportSelector</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">DeferredImportSelector</span>, BeanClassLoaderAware,<br>		ResourceLoaderAware, BeanFactoryAware, EnvironmentAware, Ordered &#123;<br><br>    <span class="hljs-comment">// 根据这个方法可以知道当前 DeferredImportSelector 实现类对应的 Group 类是 AutoConfigurationGroup</span><br>    <span class="hljs-comment">// 步骤1： 根据这个方法得到具体的 Group 实现类</span><br>	<span class="hljs-keyword">public</span> Class&lt;? <span class="hljs-keyword">extends</span> <span class="hljs-title class_">Group</span>&gt; getImportGroup() &#123;<br>		<span class="hljs-keyword">return</span> AutoConfigurationGroup.class;<br>	&#125;<br><br>    <span class="hljs-comment">// 步骤4： 这个方法是从 Group 实现类中的 process() 方法调用过来的</span><br>    <span class="hljs-keyword">protected</span> AutoConfigurationEntry <span class="hljs-title function_">getAutoConfigurationEntry</span><span class="hljs-params">(AnnotationMetadata annotationMetadata)</span> &#123;<br>		<span class="hljs-keyword">if</span> (!isEnabled(annotationMetadata)) &#123;<br>			<span class="hljs-keyword">return</span> EMPTY_ENTRY;<br>		&#125;<br>		<span class="hljs-type">AnnotationAttributes</span> <span class="hljs-variable">attributes</span> <span class="hljs-operator">=</span> getAttributes(annotationMetadata);<br>        <span class="hljs-comment">// 步骤5： getCandidateConfigurations 方法会从配置文件加载类</span><br>		List&lt;String&gt; configurations = getCandidateConfigurations(annotationMetadata, attributes);<br>		configurations = removeDuplicates(configurations);<br>        <span class="hljs-comment">// 获取手动排除掉的类，比如我们配置了 exclude 相关的配置</span><br>		Set&lt;String&gt; exclusions = getExclusions(annotationMetadata, attributes);<br>		checkExcludedClasses(configurations, exclusions);<br>		configurations.removeAll(exclusions);<br>        <span class="hljs-comment">// 步骤6： 这里就是根据条件判断，过滤掉不符合条件的类</span><br>		configurations = getConfigurationClassFilter().filter(configurations);<br>		fireAutoConfigurationImportEvents(configurations, exclusions);<br>		<span class="hljs-keyword">return</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">AutoConfigurationEntry</span>(configurations, exclusions);<br>	&#125;<br><br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">AutoConfigurationGroup</span><br>			<span class="hljs-keyword">implements</span> <span class="hljs-title class_">DeferredImportSelector</span>.Group, BeanClassLoaderAware, BeanFactoryAware, ResourceLoaderAware &#123;<br>        <br>        <span class="hljs-comment">// 步骤2： 调用 Group 实现类中的 process() 方法</span><br>        <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">process</span><span class="hljs-params">(AnnotationMetadata annotationMetadata, DeferredImportSelector deferredImportSelector)</span> &#123;<br>            <span class="hljs-comment">// 步骤3： 调用 AutoConfigurationImportSelector 类的 getAutoConfigurationEntry() 方法</span><br>            <span class="hljs-comment">// 所以这里并没有调用 AutoConfigurationImportSelector 类的 selectImports() 方法</span><br>			<span class="hljs-type">AutoConfigurationEntry</span> <span class="hljs-variable">autoConfigurationEntry</span> <span class="hljs-operator">=</span> ((AutoConfigurationImportSelector) deferredImportSelector)<br>				.getAutoConfigurationEntry(annotationMetadata);<br>			<span class="hljs-built_in">this</span>.autoConfigurationEntries.add(autoConfigurationEntry);<br>			<span class="hljs-keyword">for</span> (String importClassName : autoConfigurationEntry.getConfigurations()) &#123;<br>				<span class="hljs-built_in">this</span>.entries.putIfAbsent(importClassName, annotationMetadata);<br>			&#125;<br>		&#125;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<p><strong>步骤1</strong> 和 <strong>步骤2</strong> 的调用在讲解 <code>DeferredImportSelector</code> 接口的时候已经讲过了，这里简单贴一下调用过程</p>
<figure class="highlight leaf"><table><tr><td class="code"><pre><code class="hljs leaf">AbstractApplicationContext<span class="hljs-punctuation">#</span><span class="hljs-keyword">refresh</span><br>    AbstractApplicationContext<span class="hljs-punctuation">#</span><span class="hljs-keyword">invokeBeanFactoryPostProcessors</span><br>        public PostProcessorRegistrationDelegate<span class="hljs-punctuation">#</span><span class="hljs-keyword">invokeBeanFactoryPostProcessors</span><br>            private PostProcessorRegistrationDelegate<span class="hljs-punctuation">#</span><span class="hljs-keyword">invokeBeanDefinitionRegistryPostProcessors</span><br>                ConfigurationClassPostProcessor<span class="hljs-punctuation">#</span><span class="hljs-keyword">postProcessBeanDefinitionRegistry</span><br>                    ConfigurationClassPostProcessor<span class="hljs-punctuation">#</span><span class="hljs-keyword">processConfigBeanDefinitions</span><br>                        ConfigurationClassParser<span class="hljs-punctuation">#</span><span class="hljs-keyword">parse</span><br>                            for 循环 start[解析单个类]<br>                                ConfigurationClassParser<span class="hljs-punctuation">#</span><span class="hljs-keyword">parse</span><span class="hljs-params">(<span class="hljs-variable">AnnotationMetadata</span>, <span class="hljs-variable">java</span>.<span class="hljs-variable">lang</span>.<span class="hljs-variable">String</span>)</span><br>                                    ConfigurationClassParser<span class="hljs-punctuation">#</span><span class="hljs-keyword">processImports</span>    (1)<br>                            for 循环结束<br>                            DeferredImportSelectorHandler<span class="hljs-punctuation">#</span><span class="hljs-keyword">process</span>              (2)<br>                                DeferredImportSelectorGroupingHandler<span class="hljs-punctuation">#</span><span class="hljs-keyword">processGroupImports</span><br></code></pre></td></tr></table></figure>
<ol>
<li>在 <code>(1)</code> 位置会找到 <code>DeferredImportSelector</code> 类型的类，然后缓存起来，这个是在 <code>ConfigurationClassParser</code> 的 <code>processImports()</code> 方法中处理的</li>
<li>所以自定义的类都解析成 <code>BeanDefiniton</code> 之后(上面流程中的 <code>for</code> 循环之后)，开始执行 <code>DeferredImportSelectorHandler</code> 类的 <code>process()</code> 方法，这是因为 <code>(1)</code> 中缓存的 <code>DeferredImportSelector</code> 就是存放在 <code>DeferredImportSelectorHandler</code> 类中，也就是在 <code>(2)</code> 中调用到 <code>Group</code> 类的 <code>process()</code> 方法</li>
</ol>
<p>接下来直接从 <strong>步骤3</strong> 开始看,</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-comment">//  AutoConfigurationGroup类</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">process</span><span class="hljs-params">(AnnotationMetadata annotationMetadata, DeferredImportSelector deferredImportSelector)</span> &#123;<br>    <span class="hljs-comment">// 步骤3</span><br>	<span class="hljs-type">AutoConfigurationEntry</span> <span class="hljs-variable">autoConfigurationEntry</span> <span class="hljs-operator">=</span> ((AutoConfigurationImportSelector) deferredImportSelector)<br>	.getAutoConfigurationEntry(annotationMetadata);<br>&#125;<br><br><span class="hljs-comment">//  AutoConfigurationImportSelector类</span><br><span class="hljs-comment">// 步骤4： 这个方法是从 Group 实现类中的 process() 方法调用过来的</span><br><span class="hljs-keyword">protected</span> AutoConfigurationEntry <span class="hljs-title function_">getAutoConfigurationEntry</span><span class="hljs-params">(AnnotationMetadata annotationMetadata)</span> &#123;<br>    <span class="hljs-keyword">if</span> (!isEnabled(annotationMetadata)) &#123;<br>        <span class="hljs-keyword">return</span> EMPTY_ENTRY;<br>    &#125;<br>    <span class="hljs-type">AnnotationAttributes</span> <span class="hljs-variable">attributes</span> <span class="hljs-operator">=</span> getAttributes(annotationMetadata);<br>    <span class="hljs-comment">// 步骤5： getCandidateConfigurations 方法会从配置文件加载类</span><br>    List&lt;String&gt; configurations = getCandidateConfigurations(annotationMetadata, attributes);<br>    configurations = removeDuplicates(configurations);<br>    <span class="hljs-comment">// 获取手动排除掉的类，比如我们配置了 exclude 相关的配置</span><br>    Set&lt;String&gt; exclusions = getExclusions(annotationMetadata, attributes);<br>    checkExcludedClasses(configurations, exclusions);<br>    configurations.removeAll(exclusions);<br>    <span class="hljs-comment">// 步骤6： 这里就是根据条件判断，过滤掉不符合条件的类</span><br>    configurations = getConfigurationClassFilter().filter(configurations);<br>    fireAutoConfigurationImportEvents(configurations, exclusions);<br>    <span class="hljs-keyword">return</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">AutoConfigurationEntry</span>(configurations, exclusions);<br>&#125;<br></code></pre></td></tr></table></figure>

<p><strong>步骤5</strong> 完成的事情–从配置文件获取数据，得到的是配置的全限定类名</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-comment">//  AutoConfigurationImportSelector 类</span><br><span class="hljs-keyword">protected</span> List&lt;String&gt; <span class="hljs-title function_">getCandidateConfigurations</span><span class="hljs-params">(AnnotationMetadata metadata, AnnotationAttributes attributes)</span> &#123;<br>    List&lt;String&gt; configurations = ImportCandidates.load(AutoConfiguration.class, getBeanClassLoader())<br>        .getCandidates();<br>    <span class="hljs-keyword">return</span> configurations;<br>&#125;<br><br><span class="hljs-comment">// ImportCandidates 类</span><br><span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-type">String</span> <span class="hljs-variable">LOCATION</span> <span class="hljs-operator">=</span> <span class="hljs-string">&quot;META-INF/spring/%s.imports&quot;</span>;<br><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> ImportCandidates <span class="hljs-title function_">load</span><span class="hljs-params">(Class&lt;?&gt; annotation, ClassLoader classLoader)</span> &#123;<br>    <span class="hljs-type">ClassLoader</span> <span class="hljs-variable">classLoaderToUse</span> <span class="hljs-operator">=</span> decideClassloader(classLoader);<br>    <span class="hljs-comment">//  location = META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports</span><br>    <span class="hljs-type">String</span> <span class="hljs-variable">location</span> <span class="hljs-operator">=</span> String.format(LOCATION, annotation.getName());<br>    <span class="hljs-comment">//  通过 ClassLoader 读取 META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports</span><br>    <span class="hljs-comment">// 文件, 这个不是某个 jar 包，而是 classpath 下的所有jar 包对应路径的文件</span><br>    <span class="hljs-comment">// 所以返回的是 url 的集合</span><br>    Enumeration&lt;URL&gt; urls = findUrlsInClasspath(classLoaderToUse, location);<br>    List&lt;String&gt; importCandidates = <span class="hljs-keyword">new</span> <span class="hljs-title class_">ArrayList</span>&lt;&gt;();<br>    <span class="hljs-keyword">while</span> (urls.hasMoreElements()) &#123;<br>        <span class="hljs-type">URL</span> <span class="hljs-variable">url</span> <span class="hljs-operator">=</span> urls.nextElement();<br>        <span class="hljs-comment">// 遍历每个 url， 读取对应的文件内容， 然后将文件内容中的内容添加到 importCandidates 中</span><br>        importCandidates.addAll(readCandidateConfigurations(url));<br>    &#125;<br>    <span class="hljs-keyword">return</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">ImportCandidates</span>(importCandidates);<br>&#125;<br></code></pre></td></tr></table></figure>
<p>下面截取 <code>spring-boot-autoconfigure</code> 这个包下 <code>org.springframework.boot.autoconfigure.AutoConfiguration.imports</code> 文件的一部分内容看看</p>
<figure class="highlight stylus"><table><tr><td class="code"><pre><code class="hljs stylus">org<span class="hljs-selector-class">.springframework</span><span class="hljs-selector-class">.boot</span><span class="hljs-selector-class">.autoconfigure</span><span class="hljs-selector-class">.admin</span><span class="hljs-selector-class">.SpringApplicationAdminJmxAutoConfiguration</span><br>org<span class="hljs-selector-class">.springframework</span><span class="hljs-selector-class">.boot</span><span class="hljs-selector-class">.autoconfigure</span><span class="hljs-selector-class">.aop</span><span class="hljs-selector-class">.AopAutoConfiguration</span><br>org<span class="hljs-selector-class">.springframework</span><span class="hljs-selector-class">.boot</span><span class="hljs-selector-class">.autoconfigure</span><span class="hljs-selector-class">.amqp</span><span class="hljs-selector-class">.RabbitAutoConfiguration</span><br>org<span class="hljs-selector-class">.springframework</span><span class="hljs-selector-class">.boot</span><span class="hljs-selector-class">.autoconfigure</span><span class="hljs-selector-class">.batch</span><span class="hljs-selector-class">.BatchAutoConfiguration</span><br>……<br></code></pre></td></tr></table></figure>

<p>就是一行一个全限定类名而已，和 <code>SpringBoot2.7</code> 之前的 <code>spring.factories</code> 文件是不同的，这里也拿一个 <code>spring.factories</code> 文件内容对比一下</p>
<figure class="highlight mipsasm"><table><tr><td class="code"><pre><code class="hljs mipsasm"><span class="hljs-comment"># 这是一个 key</span><br><span class="hljs-keyword">org.springframework.boot.autoconfigure.AutoConfigurationImportFilter=\</span><br><span class="hljs-keyword"></span><span class="hljs-keyword">org.springframework.boot.autoconfigure.condition.OnBeanCondition,\</span><br><span class="hljs-keyword"></span><span class="hljs-keyword">org.springframework.boot.autoconfigure.condition.OnClassCondition,\</span><br><span class="hljs-keyword"></span><span class="hljs-keyword">org.springframework.boot.autoconfigure.condition.OnWebApplicationCondition</span><br><span class="hljs-keyword"></span><br><span class="hljs-comment"># Auto Configure</span><br><span class="hljs-comment"># 这是自动装配的 key</span><br><span class="hljs-keyword">org.springframework.boot.autoconfigure.EnableAutoConfiguration=\</span><br><span class="hljs-keyword"></span><span class="hljs-keyword">org.springframework.boot.autoconfigure.admin.SpringApplicationAdminJmxAutoConfiguration,\</span><br><span class="hljs-keyword"></span><span class="hljs-keyword">org.springframework.boot.autoconfigure.aop.AopAutoConfiguration,\</span><br><span class="hljs-keyword"></span>……<br></code></pre></td></tr></table></figure>

<p><strong>步骤6</strong>完成的事情，对 <strong>步骤5</strong>中获取的全限定类名进行过滤</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-comment">//  AutoConfigurationImportSelector类</span><br><span class="hljs-comment">// 步骤4： 这个方法是从 Group 实现类中的 process() 方法调用过来的</span><br><span class="hljs-keyword">protected</span> AutoConfigurationEntry <span class="hljs-title function_">getAutoConfigurationEntry</span><span class="hljs-params">(AnnotationMetadata annotationMetadata)</span> &#123;<br>    <span class="hljs-keyword">if</span> (!isEnabled(annotationMetadata)) &#123;<br>        <span class="hljs-keyword">return</span> EMPTY_ENTRY;<br>    &#125;<br>    <span class="hljs-type">AnnotationAttributes</span> <span class="hljs-variable">attributes</span> <span class="hljs-operator">=</span> getAttributes(annotationMetadata);<br>    <span class="hljs-comment">// 步骤5： getCandidateConfigurations 方法会从配置文件加载类</span><br>    List&lt;String&gt; configurations = getCandidateConfigurations(annotationMetadata, attributes);<br>    configurations = removeDuplicates(configurations);<br>    <span class="hljs-comment">// 获取手动排除掉的类，比如我们配置了 exclude 相关的配置</span><br>    Set&lt;String&gt; exclusions = getExclusions(annotationMetadata, attributes);<br>    checkExcludedClasses(configurations, exclusions);<br>    configurations.removeAll(exclusions);<br>    <span class="hljs-comment">// 步骤6： 这里就是根据条件判断，过滤掉不符合条件的类</span><br>    configurations = getConfigurationClassFilter().filter(configurations);<br>    fireAutoConfigurationImportEvents(configurations, exclusions);<br>    <span class="hljs-keyword">return</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">AutoConfigurationEntry</span>(configurations, exclusions);<br>&#125;<br></code></pre></td></tr></table></figure>

<ol>
<li>调用 <code>getConfigurationClassFilter()</code> 方法获取 <code>classFilter</code>, 这个不跟下去了，直接说结论，其实也是根据 <code>spi</code> 机制从 <code>spring.factories</code> 文件中获取的，从上面列出的 <code>spring.factories</code> 文件中可以看到，这个文件是 <code>key=values</code> 的形式，也就是一个 <code>key</code> 可以配置多个值，而获取 <code>classFilter</code> 的 <code>key</code> 是 <code>AutoConfigurationImportFilter</code>， 再看看 <code>spring.factories</code> 文件中关于 <code>AutoConfigurationImportFilter</code> 的配置</li>
</ol>
<figure class="highlight livescript"><table><tr><td class="code"><pre><code class="hljs livescript"><span class="hljs-comment"># 这个配置文件也是在 spring-boot-autoconfigure 包下面</span><br><span class="hljs-comment"># Auto Configuration Import Filters</span><br>org.springframework.boot.autoconfigure.AutoConfigurationImportFilter=<span class="hljs-string">\</span><br>org.springframework.boot.autoconfigure.condition.OnBeanCondition,<span class="hljs-string">\</span><br>org.springframework.boot.autoconfigure.condition.OnClassCondition,<span class="hljs-string">\</span><br>org.springframework.boot.autoconfigure.condition.OnWebApplicationCondition<br></code></pre></td></tr></table></figure>

<ol start="2">
<li><code>getConfigurationClassFilter()</code> 方法的返回值是 <code>ConfigurationClassFilter</code>， 看看这个类，具体的过滤逻辑这里就不再分析</li>
</ol>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">ConfigurationClassFilter</span> &#123;<br>	<span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> AutoConfigurationMetadata autoConfigurationMetadata;<br>    <span class="hljs-comment">// 上面讲到会从配置文件中加载到三个 AutoConfigurationImportFilter 实现类，会保存在这里然后调用过滤方法</span><br>	<span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> List&lt;AutoConfigurationImportFilter&gt; filters;<br>&#125;<br></code></pre></td></tr></table></figure>

<h3 id="解析文件和过滤后的处理"><a href="#解析文件和过滤后的处理" class="headerlink" title="解析文件和过滤后的处理"></a>解析文件和过滤后的处理</h3><p>配置文件也解析了，过滤也过滤完了，回过头再看看 <code>Group</code> 类的 <code>process()</code> 方法是从哪里调用过来的，前面有，这里再贴一下流程</p>
<figure class="highlight leaf"><table><tr><td class="code"><pre><code class="hljs leaf">AbstractApplicationContext<span class="hljs-punctuation">#</span><span class="hljs-keyword">refresh</span><br>    AbstractApplicationContext<span class="hljs-punctuation">#</span><span class="hljs-keyword">invokeBeanFactoryPostProcessors</span><br>        public PostProcessorRegistrationDelegate<span class="hljs-punctuation">#</span><span class="hljs-keyword">invokeBeanFactoryPostProcessors</span><br>            private PostProcessorRegistrationDelegate<span class="hljs-punctuation">#</span><span class="hljs-keyword">invokeBeanDefinitionRegistryPostProcessors</span><br>                ConfigurationClassPostProcessor<span class="hljs-punctuation">#</span><span class="hljs-keyword">postProcessBeanDefinitionRegistry</span><br>                    ConfigurationClassPostProcessor<span class="hljs-punctuation">#</span><span class="hljs-keyword">processConfigBeanDefinitions</span><br>                        ConfigurationClassParser<span class="hljs-punctuation">#</span><span class="hljs-keyword">parse</span><br>                            for 循环 start[解析单个类]<br>                                ConfigurationClassParser<span class="hljs-punctuation">#</span><span class="hljs-keyword">parse</span><span class="hljs-params">(<span class="hljs-variable">AnnotationMetadata</span>, <span class="hljs-variable">java</span>.<span class="hljs-variable">lang</span>.<span class="hljs-variable">String</span>)</span><br>                                    ConfigurationClassParser<span class="hljs-punctuation">#</span><span class="hljs-keyword">processImports</span><br>                            for 循环结束<br>                            DeferredImportSelectorHandler<span class="hljs-punctuation">#</span><span class="hljs-keyword">process</span><br>                                DeferredImportSelectorGroupingHandler<span class="hljs-punctuation">#</span><span class="hljs-keyword">processGroupImports</span><br></code></pre></td></tr></table></figure>

<p>所以是在 <strong>DeferredImportSelectorHandler</strong> 的类 <code>process()</code> 方法中处理的</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-comment">//  ConfigurationClassParser</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">processGroupImports</span><span class="hljs-params">()</span> &#123;<br>    <span class="hljs-keyword">for</span> (DeferredImportSelectorGrouping grouping : <span class="hljs-built_in">this</span>.groupings.values()) &#123;<br>        Predicate&lt;String&gt; exclusionFilter = grouping.getCandidateFilter();<br>        <span class="hljs-comment">// 就是在 getImport() 方法中调用到 Group 实现类的 process() 方法</span><br>        <span class="hljs-comment">// 返回值 Group 实现类的 selectImports 方法</span><br>        grouping.getImports().forEach(entry -&gt; &#123;<br>            <span class="hljs-type">ConfigurationClass</span> <span class="hljs-variable">configurationClass</span> <span class="hljs-operator">=</span> <span class="hljs-built_in">this</span>.configurationClasses.get(entry.getMetadata());<br>            <span class="hljs-comment">// 拿到结果之后调用的又是  processImports方法， 这个就是和</span><br>            <span class="hljs-comment">// @Import 导入的是一个普通类的流程一致的</span><br>            processImports(configurationClass, asSourceClass(configurationClass, exclusionFilter),<br>                        Collections.singleton(asSourceClass(entry.getImportClassName(), exclusionFilter)),<br>                        exclusionFilter, <span class="hljs-literal">false</span>);<br>        &#125;);<br>    &#125;<br>&#125;<br><br><span class="hljs-comment">//  ConfigurationClassParser</span><br><span class="hljs-keyword">public</span> Iterable&lt;Group.Entry&gt; getImports() &#123;<br>    <span class="hljs-keyword">for</span> (DeferredImportSelectorHolder deferredImport : <span class="hljs-built_in">this</span>.deferredImports) &#123;<br>        <span class="hljs-comment">//  就是在这里调用到 Group 实现类的 process() 方法</span><br>        <span class="hljs-built_in">this</span>.group.process(deferredImport.getConfigurationClass().getMetadata(),<br>                deferredImport.getImportSelector());<br>    &#125;<br>    <span class="hljs-keyword">return</span> <span class="hljs-built_in">this</span>.group.selectImports();<br>&#125;<br></code></pre></td></tr></table></figure>

<p>从上面代码看到，拿到解析和结果的结果之后，通过 <code>foreach</code> 调用 <code>processImports()</code> 方法，这个方法就是处理单个 <code>@Import</code> 导入的类的方法，至此整个流程就串起来了</p>
]]></content>
      <categories>
        <category>java</category>
        <category>SpringBoot</category>
      </categories>
      <tags>
        <tag>java</tag>
        <tag>SpringBoot</tag>
      </tags>
  </entry>
  <entry>
    <title>SpringBoot3自动装配案例</title>
    <url>/2023/ee343b448bf4/index.html</url>
    <content><![CDATA[<p>创建两个 <code>module</code></p>
<ol>
<li><code>demo-starter</code>, 这个 <code>module</code> 是为了让别的 <code>module</code> 引入的 <code>starter</code> 包</li>
<li><code>demo-main</code>, 这个 <code>module</code> 会引入 <code>demo-starter</code> 包</li>
</ol>
<p>两个 <code>module</code> 的包结构如下所示</p>
<figure class="highlight stylus"><table><tr><td class="code"><pre><code class="hljs stylus">demo-starter<br>│  ├─<span class="hljs-attribute">src</span><br>│  │  ├─<span class="hljs-selector-tag">main</span><br>│  │  │  ├─java<br>│  │  │  │  └─com<br>│  │  │  │      └─example<br>│  │  │  │          └─demostarter 包下有 StarterDemo 类<br>│  │  │  └─resources<br>│  │  │      └─META-INF<br>│  │  │          └─spring  文件夹下有 org<span class="hljs-selector-class">.springframework</span><span class="hljs-selector-class">.boot</span><span class="hljs-selector-class">.autoconfigure</span><span class="hljs-selector-class">.AutoConfiguration</span><span class="hljs-selector-class">.imports</span> 文件<br><br></code></pre></td></tr></table></figure>

<p><code>StarterDemo</code> 类如下</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">StarterDemo</span> &#123;<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">starterDemo</span><span class="hljs-params">()</span> &#123;<br>        System.out.println(<span class="hljs-string">&quot;StarterDemo&quot;</span>);<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<p><code>org.springframework.boot.autoconfigure.AutoConfiguration.imports</code> 文件内容如下</p>
<figure class="highlight stylus"><table><tr><td class="code"><pre><code class="hljs stylus">com<span class="hljs-selector-class">.example</span><span class="hljs-selector-class">.demostarter</span>.StarterDemo<br></code></pre></td></tr></table></figure>

<figure class="highlight pgsql"><table><tr><td class="code"><pre><code class="hljs pgsql">demo-main<br>│  ├─.mvn<br>│  │  └─<span class="hljs-keyword">wrapper</span><br>│  ├─src<br>│  │  ├─main<br>│  │  │  ├─java<br>│  │  │  │  └─com<br>│  │  │  │      └─example<br>│  │  │  │          └─demomain 包下有 DemoMainApplication 类<br>│  │  │  └─resources<br><br></code></pre></td></tr></table></figure>

<p><code>DemoMainApplication</code> 类内容如下</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-meta">@SpringBootApplication</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">DemoMainApplication</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">ApplicationContextAware</span> &#123;<br><br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> ApplicationContext context;<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">main</span><span class="hljs-params">(String[] args)</span> &#123;<br>        SpringApplication.run(DemoMainApplication.class, args);<br>        <span class="hljs-comment">// 只是引入了依赖而已，并没有手动注入 StarterDemo 类</span><br>        <span class="hljs-comment">// 但是这个类也在 spring 容器中，就是因为自动装配</span><br>        context.getBean(StarterDemo.class).starterDemo(); <span class="hljs-comment">// 会输出 “StarterDemo”</span><br>    &#125;<br><br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">setApplicationContext</span><span class="hljs-params">(ApplicationContext applicationContext)</span> <span class="hljs-keyword">throws</span> BeansException &#123;<br>        context = applicationContext;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<p><code>demo-main</code> 的 <code>pom.xml</code> 依赖如下</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><code class="hljs xml"><span class="hljs-tag">&lt;<span class="hljs-name">dependencies</span>&gt;</span><br>    <span class="hljs-tag">&lt;<span class="hljs-name">dependency</span>&gt;</span><br>        <span class="hljs-tag">&lt;<span class="hljs-name">groupId</span>&gt;</span>org.springframework.boot<span class="hljs-tag">&lt;/<span class="hljs-name">groupId</span>&gt;</span><br>        <span class="hljs-tag">&lt;<span class="hljs-name">artifactId</span>&gt;</span>spring-boot-starter<span class="hljs-tag">&lt;/<span class="hljs-name">artifactId</span>&gt;</span><br>    <span class="hljs-tag">&lt;/<span class="hljs-name">dependency</span>&gt;</span><br><br>    <span class="hljs-tag">&lt;<span class="hljs-name">dependency</span>&gt;</span><br>        <span class="hljs-tag">&lt;<span class="hljs-name">groupId</span>&gt;</span>com.example<span class="hljs-tag">&lt;/<span class="hljs-name">groupId</span>&gt;</span><br>        <span class="hljs-tag">&lt;<span class="hljs-name">artifactId</span>&gt;</span>demo-starter<span class="hljs-tag">&lt;/<span class="hljs-name">artifactId</span>&gt;</span><br>        <span class="hljs-tag">&lt;<span class="hljs-name">version</span>&gt;</span>0.0.1-SNAPSHOT<span class="hljs-tag">&lt;/<span class="hljs-name">version</span>&gt;</span><br>    <span class="hljs-tag">&lt;/<span class="hljs-name">dependency</span>&gt;</span><br><span class="hljs-tag">&lt;/<span class="hljs-name">dependencies</span>&gt;</span><br></code></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>java</category>
        <category>SpringBoot</category>
      </categories>
      <tags>
        <tag>java</tag>
        <tag>SpringBoot</tag>
      </tags>
  </entry>
  <entry>
    <title>SpringBoot实现文件的上传下载</title>
    <url>/2023/ca7a5b96a543/index.html</url>
    <content><![CDATA[<h2 id="文件上传"><a href="#文件上传" class="headerlink" title="文件上传"></a>文件上传</h2><p><code>Controller</code> 层代码</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-meta">@PostMapping(value = &quot;/db/execute&quot;)</span><br><span class="hljs-keyword">public</span> String <span class="hljs-title function_">uploadFile</span><span class="hljs-params">(<span class="hljs-meta">@RequestParam(&quot;files&quot;)</span> MultipartFile[] multipartFiles)</span> &#123;<br>    uploadService.uploadFile(multipartFiles);<br>    <span class="hljs-keyword">return</span> <span class="hljs-string">&quot;success&quot;</span>;<br>&#125;<br></code></pre></td></tr></table></figure>

<p><code>UploadService</code> 类代码</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">UploadService</span> &#123;<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">uploadFile</span><span class="hljs-params">(MultipartFile[] multipartFiles)</span> &#123;<br>        <span class="hljs-keyword">for</span> (MultipartFile multipartFile : multipartFiles) &#123;<br>            <span class="hljs-type">String</span> <span class="hljs-variable">folder</span> <span class="hljs-operator">=</span> <span class="hljs-string">&quot;/tmp/upload&quot;</span>;<br>            <span class="hljs-type">String</span> <span class="hljs-variable">filename</span> <span class="hljs-operator">=</span> multipartFile.getOriginalFilename();<br>            <span class="hljs-keyword">try</span> &#123;<br>                <span class="hljs-keyword">final</span> <span class="hljs-type">File</span> <span class="hljs-variable">file</span> <span class="hljs-operator">=</span> createFile(folderPath, filename);<br>                multipartFile.transferTo(file);<br>            &#125; <span class="hljs-keyword">catch</span> (IOException e) &#123;<br>                logger.error(<span class="hljs-string">&quot;store sql file error&quot;</span>, e);<br>                <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">RuntimeException</span>(e);<br>            &#125;<br>        &#125;<br>    &#125;<br><br>    <span class="hljs-keyword">public</span> File <span class="hljs-title function_">createFile</span><span class="hljs-params">(String folderPath, String filename)</span> <span class="hljs-keyword">throws</span> IOException &#123;<br>        <span class="hljs-keyword">final</span> <span class="hljs-type">File</span> <span class="hljs-variable">file</span> <span class="hljs-operator">=</span> FileUtils.getFile(folderPath);<br>        <span class="hljs-keyword">if</span> (!file.exists()) &#123;<br>            <span class="hljs-keyword">try</span> &#123;<br>                <span class="hljs-type">boolean</span> <span class="hljs-variable">folderCreated</span> <span class="hljs-operator">=</span> file.mkdirs();<br>                <span class="hljs-keyword">if</span> (!folderCreated &amp;&amp; !file.exists()) &#123;<br>                    <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">IOException</span>(<span class="hljs-string">&quot;Unable to create path&quot;</span>);<br>                &#125;<br>            &#125; <span class="hljs-keyword">catch</span> (SecurityException e) &#123;<br>                <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">IOException</span>(<span class="hljs-string">&quot;Unable to create path&quot;</span>, e);<br>            &#125;<br>        &#125;<br>        <span class="hljs-type">String</span> <span class="hljs-variable">fullFilePath</span> <span class="hljs-operator">=</span> folderPath + File.separator + filename;<br>        <span class="hljs-keyword">return</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">File</span>(fullFilePath);<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<h2 id="文件下载"><a href="#文件下载" class="headerlink" title="文件下载"></a>文件下载</h2><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">FileDownloadController</span> &#123;<br><br>    <span class="hljs-meta">@PostMapping(value = &quot;/package/batch/download&quot;)</span><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">batchDownloadPackage</span><span class="hljs-params">(HttpServletResponse response,  <span class="hljs-meta">@RequestParam</span> String storePath)</span> <span class="hljs-keyword">throws</span> IOException &#123;<br>        <span class="hljs-type">Resource</span> <span class="hljs-variable">resource</span> <span class="hljs-operator">=</span> fileDownloadService.buildResource(storePath);<br>        fileDownloadService.writeResponse(response, resource);<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<pre><code class="java">public class FileDownloadService &#123;
    // 根据资源地址构建 resource 对象或者 file对象
    public Resource buildResource(String storePath) &#123;
        Path filePath = Paths.get(storePath);
        Resource resource = null;
        try &#123;
            resource = new UrlResource(filePath.toUri());
        &#125; catch (MalformedURLException e) &#123;
            logger.warn(&quot;Could not read file&quot;);
            return null;
        &#125;
        if (resource.exists() &amp;&amp; resource.isReadable()) &#123;
            return resource;
        &#125; else &#123;
            logger.warn(&quot;Could not read file&quot;);
            return null;
        &#125;
    &#125;

    // 响应结果
    public void writeResponse(HttpServletResponse response, Resource resource) throws IOException &#123;
        if (Objects.isNull(resource)) &#123;
            buildResponseHeader(response, null);
            return;
        &#125;
        try (InputStream inputStream = new FileInputStream(resource.getFile())) &#123;
            buildResponseHeader(response, resource);
            ServletOutputStream outputStream = response.getOutputStream();
            byte[] b = new byte[1024];
            int len;
            //从输入流中读取一定数量的字节，并将其存储在缓冲区字节数组中，读到末尾返回-1
            while ((len = inputStream.read(b)) &gt; 0) &#123;
                outputStream.write(b, 0, len);
            &#125;
        &#125; catch (IOException ex) &#123;
            ex.printStackTrace();
        &#125;
    &#125;

    // 设置响应头
    private void buildResponseHeader(HttpServletResponse response, Resource resource) throws IOException &#123;
        // 读到流中
        response.reset();
        response.setContentType(&quot;application/octet-stream&quot;);
        response.addHeader(&quot;Content-Disposition&quot;, &quot;attachment; filename=&quot; + URLEncoder.encode(Objects.isNull(resource) ? &quot;文件不存在&quot; : resource.getFilename(), &quot;UTF-8&quot;));
        /**
         * CORS请求时，XMLHttpRequest对象的getResponseHeader()方法只能拿到6个基本字段：Cache-Control、Content-Language、
         * Content-Type、Expires、Last-Modified、Pragma。如果想拿到其他字段，就必须在Access-Control-Expose-Headers里面指定
         */
        response.setHeader(&quot;Access-Control-Expose-Headers&quot;, &quot;Content-disposition&quot;);
        response.addHeader(&quot;Content-Length&quot;, &quot;&quot; + (Objects.isNull(resource) ? 0 : resource.contentLength()));
    &#125;
&#125;
</code></pre>
]]></content>
      <categories>
        <category>java</category>
        <category>SpringBoot</category>
      </categories>
      <tags>
        <tag>java</tag>
        <tag>SpringBoot</tag>
      </tags>
  </entry>
  <entry>
    <title>SpringCloudAlibaba和SpringCloud和SpringBoot之间版本对应关系</title>
    <url>/2023/742b64bccf7c/index.html</url>
    <content><![CDATA[<h2 id="SpringClound-和-SpringBoot-的版本对应"><a href="#SpringClound-和-SpringBoot-的版本对应" class="headerlink" title="SpringClound 和 SpringBoot 的版本对应"></a>SpringClound 和 SpringBoot 的版本对应</h2><p><a href="https://spring.io/projects/spring-cloud">Spring官网之SpringCloud项目</a></p>
<h2 id="SpringCloundAlibaba-和-SpringCloud-的版本对应"><a href="#SpringCloundAlibaba-和-SpringCloud-的版本对应" class="headerlink" title="SpringCloundAlibaba 和 SpringCloud 的版本对应"></a>SpringCloundAlibaba 和 SpringCloud 的版本对应</h2><p><a href="https://sca.aliyun.com/zh-cn/">SpringCloundAlibaba官网</a></p>
<p><a href="https://sca.aliyun.com/zh-cn/docs/2022.0.0.0/overview/version-explain">SpringCloundAlibaba2022版本对应文档说明</a></p>
]]></content>
      <categories>
        <category>java</category>
        <category>SpringCloud</category>
      </categories>
      <tags>
        <tag>java</tag>
        <tag>SpringCloud</tag>
      </tags>
  </entry>
  <entry>
    <title>SpringBoot编写测试类</title>
    <url>/2023/9cd70dc15320/index.html</url>
    <content><![CDATA[<p>版本说明：</p>
<ol>
<li>SpringBoot3.x</li>
</ol>
<h2 id="引入依赖"><a href="#引入依赖" class="headerlink" title="引入依赖"></a>引入依赖</h2><figure class="highlight xml"><table><tr><td class="code"><pre><code class="hljs xml"><span class="hljs-tag">&lt;<span class="hljs-name">dependency</span>&gt;</span><br>    <span class="hljs-tag">&lt;<span class="hljs-name">groupId</span>&gt;</span>org.springframework.boot<span class="hljs-tag">&lt;/<span class="hljs-name">groupId</span>&gt;</span><br>    <span class="hljs-tag">&lt;<span class="hljs-name">artifactId</span>&gt;</span>spring-boot-starter-test<span class="hljs-tag">&lt;/<span class="hljs-name">artifactId</span>&gt;</span><br><span class="hljs-tag">&lt;/<span class="hljs-name">dependency</span>&gt;</span><br></code></pre></td></tr></table></figure>

<h2 id="编写测试类"><a href="#编写测试类" class="headerlink" title="编写测试类"></a>编写测试类</h2><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-meta">@SpringBootTest</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">TestRedis</span> &#123;<br>    <span class="hljs-meta">@Test</span><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">testOne</span><span class="hljs-params">()</span> &#123;<br>        System.out.println(<span class="hljs-string">&quot;测试类&quot;</span>);<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<h3 id="注意点"><a href="#注意点" class="headerlink" title="注意点"></a>注意点</h3><ol>
<li><code>SpringBoot2.5</code> 之后测试类中已经不需要添加 <code>RunWith</code> 注解，只需要一个 <code>SpringBootTest</code></li>
<li>需要保证测试类所在的包和项目主配置类包一致（项目 <code>main</code> 方法所在的包），如果不一致，则可以按照下面方式配置</li>
</ol>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-meta">@SpringBootTest(classes = TestApplication.class)</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">TestRedis</span> &#123;<br>    <span class="hljs-meta">@Test</span><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">testOne</span><span class="hljs-params">()</span> &#123;<br>        System.out.println(<span class="hljs-string">&quot;测试类&quot;</span>);<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>java</category>
        <category>SpringBoot</category>
      </categories>
      <tags>
        <tag>java</tag>
        <tag>SpringBoot</tag>
      </tags>
  </entry>
  <entry>
    <title>RestTemplate调用时添加请求头</title>
    <url>/2024/c0337b25d19d/index.html</url>
    <content><![CDATA[<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">import</span> org.springframework.http.HttpEntity;<br><span class="hljs-keyword">import</span> org.springframework.http.HttpHeaders;<br><span class="hljs-keyword">import</span> org.springframework.web.client.RestTemplate;<br><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">RestTemplateTest</span> &#123;<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">main</span><span class="hljs-params">(String[] args)</span> &#123;<br>        <span class="hljs-type">HttpHeaders</span> <span class="hljs-variable">httpHeaders</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">HttpHeaders</span>();<br>        <span class="hljs-comment">// 自定义请求头</span><br>        httpHeaders.add(<span class="hljs-string">&quot;key&quot;</span>, <span class="hljs-string">&quot;value&quot;</span>);<br>        <span class="hljs-type">Object</span> <span class="hljs-variable">requestBody</span> <span class="hljs-operator">=</span> <span class="hljs-literal">null</span>;<br>        <span class="hljs-type">HttpEntity</span> <span class="hljs-variable">httpEntity</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">HttpEntity</span>(requestBody, httpHeaders);<br>        <span class="hljs-type">RestTemplate</span> <span class="hljs-variable">restTemplate</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">RestTemplate</span>();<br>        restTemplate.postForObject(url, httpEntity, Boolean.class);<br>    &#125;<br></code></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>java</category>
        <category>SpringWeb</category>
      </categories>
      <tags>
        <tag>java</tag>
        <tag>SpringWeb</tag>
      </tags>
  </entry>
  <entry>
    <title>解决SpringBoot中Request.getInputStream只能调用一次的问题</title>
    <url>/2023/ded68009e52a/index.html</url>
    <content><![CDATA[<p><a href="https://www.cnblogs.com/spll/p/9685686.html">从Request域获取参数的几种方式</a></p>
<h2 id="调用-Request-getInputStream-的问题"><a href="#调用-Request-getInputStream-的问题" class="headerlink" title="调用 Request.getInputStream 的问题"></a>调用 Request.getInputStream 的问题</h2><p>获取 <code>post</code> 方法参数 <code>content-type</code> 为 <code>application/json</code> 时 <code>HttpServletRequest.getInputStream</code> 方法只能被调用一次， 如果在拦截器里面已经调用了来获取参数，比如获取 <code>token</code> 信息等，在 <code>Controller</code> 中解析参数时就会报错</p>
<h2 id="解决方法"><a href="#解决方法" class="headerlink" title="解决方法"></a>解决方法</h2><ul>
<li>对 <code>HttpServletRequest</code> 包装一层</li>
<li>使用 <code>filterChain.doFilter(request, response)</code> 向下传递 <code>request</code> 的时候使用我们包装的 <code>request</code></li>
</ul>
<h2 id="自定义类实现-HttpServletRequestWrapper"><a href="#自定义类实现-HttpServletRequestWrapper" class="headerlink" title="自定义类实现 HttpServletRequestWrapper"></a>自定义类实现 HttpServletRequestWrapper</h2><p>自定义继承 <code>HttpServletRequestWrapper</code> 类的作用是先调用一次 <code>request.getInputStream()</code>，将流中的信息读取后缓存起来，后续使用的时候就直接从缓存中读取就好</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">import</span> org.slf4j.Logger;<br><span class="hljs-keyword">import</span> org.slf4j.LoggerFactory;<br><br><span class="hljs-keyword">import</span> javax.servlet.ReadListener;<br><span class="hljs-keyword">import</span> javax.servlet.ServletInputStream;<br><span class="hljs-keyword">import</span> javax.servlet.http.HttpServletRequest;<br><span class="hljs-keyword">import</span> javax.servlet.http.HttpServletRequestWrapper;<br><span class="hljs-keyword">import</span> java.io.*;<br><span class="hljs-keyword">import</span> java.nio.charset.StandardCharsets;<br><br><span class="hljs-comment">/**</span><br><span class="hljs-comment"> * 扩展 HttpServletRequestWrapper， 解决 request.getInputStream() 方法只能读取一次的问题</span><br><span class="hljs-comment"> */</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">RequestWrapper</span> <span class="hljs-keyword">extends</span> <span class="hljs-title class_">HttpServletRequestWrapper</span> &#123;<br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-type">Logger</span> <span class="hljs-variable">logger</span> <span class="hljs-operator">=</span> LoggerFactory.getLogger(RequestWrapper.class);<br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> String body;<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-title function_">RequestWrapper</span><span class="hljs-params">(HttpServletRequest request)</span> &#123;<br>        <span class="hljs-built_in">super</span>(request);<br>        <span class="hljs-comment">//多次读取</span><br>        <span class="hljs-type">StringBuilder</span> <span class="hljs-variable">stringBuilder</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">StringBuilder</span>();<br>        <span class="hljs-type">BufferedReader</span> <span class="hljs-variable">bufferedReader</span> <span class="hljs-operator">=</span> <span class="hljs-literal">null</span>;<br>        <span class="hljs-type">InputStream</span> <span class="hljs-variable">inputStream</span> <span class="hljs-operator">=</span> <span class="hljs-literal">null</span>;<br>        <span class="hljs-keyword">try</span> &#123;<br>            inputStream = request.getInputStream();<br>            <span class="hljs-keyword">if</span> (inputStream != <span class="hljs-literal">null</span>) &#123;<br>                bufferedReader = <span class="hljs-keyword">new</span> <span class="hljs-title class_">BufferedReader</span>(<span class="hljs-keyword">new</span> <span class="hljs-title class_">InputStreamReader</span>(inputStream));<br>                <span class="hljs-type">char</span>[] charBuffer = <span class="hljs-keyword">new</span> <span class="hljs-title class_">char</span>[<span class="hljs-number">128</span>];<br>                <span class="hljs-type">int</span> <span class="hljs-variable">bytesRead</span> <span class="hljs-operator">=</span> -<span class="hljs-number">1</span>;<br>                <span class="hljs-keyword">while</span> ((bytesRead = bufferedReader.read(charBuffer)) &gt; <span class="hljs-number">0</span>) &#123;<br>                    stringBuilder.append(charBuffer, <span class="hljs-number">0</span>, bytesRead);<br>                &#125;<br>            &#125; <span class="hljs-keyword">else</span> &#123;<br>                stringBuilder.append(<span class="hljs-string">&quot;&quot;</span>);<br>            &#125;<br>        &#125; <span class="hljs-keyword">catch</span> (IOException ex) &#123;<br>            logger.error(<span class="hljs-string">&quot;解析 request.getInputStream 流出错:&#123;&#125;&quot;</span>, ex);<br>        &#125; <span class="hljs-keyword">finally</span> &#123;<br>            <span class="hljs-keyword">if</span> (inputStream != <span class="hljs-literal">null</span>) &#123;<br>                <span class="hljs-keyword">try</span> &#123;<br>                    inputStream.close();<br>                &#125; <span class="hljs-keyword">catch</span> (IOException e) &#123;<br>                    logger.error(<span class="hljs-string">&quot;关闭流出错：&#123;&#125;&quot;</span>, e);<br>                &#125;<br>            &#125;<br>            <span class="hljs-keyword">if</span> (bufferedReader != <span class="hljs-literal">null</span>) &#123;<br>                <span class="hljs-keyword">try</span> &#123;<br>                    bufferedReader.close();<br>                &#125; <span class="hljs-keyword">catch</span> (IOException e) &#123;<br>                    logger.error(<span class="hljs-string">&quot;关闭流出错：&#123;&#125;&quot;</span>, e);<br>                &#125;<br>            &#125;<br>        &#125;<br>        body = stringBuilder.toString();<br>    &#125;<br><br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> ServletInputStream <span class="hljs-title function_">getInputStream</span><span class="hljs-params">()</span> <span class="hljs-keyword">throws</span> IOException &#123;<br>        <span class="hljs-keyword">final</span> <span class="hljs-type">ByteArrayInputStream</span> <span class="hljs-variable">byteArrayInputStream</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">ByteArrayInputStream</span>(body.getBytes(StandardCharsets.UTF_8));<br>        <span class="hljs-type">ServletInputStream</span> <span class="hljs-variable">servletInputStream</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">ServletInputStream</span>() &#123;<br><br>            <span class="hljs-meta">@Override</span><br>            <span class="hljs-keyword">public</span> <span class="hljs-type">boolean</span> <span class="hljs-title function_">isFinished</span><span class="hljs-params">()</span> &#123;<br>                <span class="hljs-keyword">return</span> <span class="hljs-literal">false</span>;<br>            &#125;<br><br>            <span class="hljs-meta">@Override</span><br>            <span class="hljs-keyword">public</span> <span class="hljs-type">boolean</span> <span class="hljs-title function_">isReady</span><span class="hljs-params">()</span> &#123;<br>                <span class="hljs-keyword">return</span> <span class="hljs-literal">false</span>;<br>            &#125;<br><br>            <span class="hljs-meta">@Override</span><br>            <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">setReadListener</span><span class="hljs-params">(ReadListener readListener)</span> &#123;<br>            &#125;<br><br>            <span class="hljs-meta">@Override</span><br>            <span class="hljs-keyword">public</span> <span class="hljs-type">int</span> <span class="hljs-title function_">read</span><span class="hljs-params">()</span> <span class="hljs-keyword">throws</span> IOException &#123;<br>                <span class="hljs-keyword">return</span> byteArrayInputStream.read();<br>            &#125;<br>        &#125;;<br>        <span class="hljs-keyword">return</span> servletInputStream;<br><br>    &#125;<br><br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> BufferedReader <span class="hljs-title function_">getReader</span><span class="hljs-params">()</span> <span class="hljs-keyword">throws</span> IOException &#123;<br>        <span class="hljs-keyword">return</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">BufferedReader</span>(<span class="hljs-keyword">new</span> <span class="hljs-title class_">InputStreamReader</span>(<span class="hljs-built_in">this</span>.getInputStream()));<br>    &#125;<br><br>    <span class="hljs-keyword">public</span> String <span class="hljs-title function_">getBody</span><span class="hljs-params">()</span> &#123;<br>        <span class="hljs-keyword">return</span> <span class="hljs-built_in">this</span>.body;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<h2 id="自定义-Filter"><a href="#自定义-Filter" class="headerlink" title="自定义 Filter"></a>自定义 Filter</h2><p>上面已经自定义了 <code>HttpServletRequestWrapper</code> 实现类，接下来就是要使用这个类来替换掉 <code>HttpServletRequest</code> 了，替换的操作是在 <code>Filter</code> 类中完成</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-meta">@Component</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">AuthFilter</span> <span class="hljs-keyword">extends</span> <span class="hljs-title class_">OncePerRequestFilter</span> &#123;<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">doFilterInternal</span><span class="hljs-params">(HttpServletRequest request, HttpServletResponse response, FilterChain filterChain)</span><br>    <span class="hljs-keyword">throws</span> ServletException, IOException &#123;<br>        <span class="hljs-comment">// 将 HttpServletRequest 转换成自定义的实现了 HttpServletRequestWrapper 的 RequestWrapper</span><br>        <span class="hljs-type">RequestWrapper</span> <span class="hljs-variable">requestWrapper</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">RequestWrapper</span>(request);<br>        <span class="hljs-comment">// 获取参数</span><br>        Map&lt;String, Object&gt; parameters = getParameters(requestWrapper);<br>        filterChain.doFilter(requestWrapper, response);<br>    &#125;<br><br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> Map&lt;String, Object&gt; <span class="hljs-title function_">getParameters</span><span class="hljs-params">(RequestWrapper request)</span> <span class="hljs-keyword">throws</span> IOException &#123;<br>        <span class="hljs-type">ObjectMapper</span> <span class="hljs-variable">objectMapper</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">ObjectMapper</span>();<br>        <span class="hljs-type">String</span> <span class="hljs-variable">body</span> <span class="hljs-operator">=</span> request.getBody();<br>        <span class="hljs-keyword">if</span> (StringUtils.isEmpty(body)) &#123;<br>            <span class="hljs-keyword">return</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">HashMap</span>&lt;&gt;();<br>        &#125;<br>        Map&lt;String, Object&gt; resultMap = objectMapper.readValue(body,<br>             <span class="hljs-keyword">new</span> <span class="hljs-title class_">TypeReference</span>&lt;Map&lt;String, Object&gt;&gt;() &#123;&#125;);<br>        <span class="hljs-keyword">return</span> resultMap;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>java</category>
        <category>SpringBoot</category>
      </categories>
      <tags>
        <tag>java</tag>
        <tag>SpringBoot</tag>
      </tags>
  </entry>
  <entry>
    <title>Future的使用</title>
    <url>/2023/9f417cc2ea5e/index.html</url>
    <content><![CDATA[<span class="hide_first_mermaid">
<div class="mermaid-wrap"><pre class="mermaid-src" hidden>
  flowchart LR
    A
  </pre></div>
</span>

<h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><p><code>java</code> 中异步执行任务可以让任务类实现 <code>Runnable</code> 接口即可，但是使用 <code>Runnable</code> 接口执行的任务无法获取返回值，所以 <code>java</code> 在 <code>jdk1.5</code> 版本中增加了 <code>Future</code> 接口和 <code>Callable</code></p>
<span id="more"></span>
<h2 id="先给结论"><a href="#先给结论" class="headerlink" title="先给结论"></a>先给结论</h2><ol>
<li><code>Future</code> 可以使用 <code>get()</code> 方法获取线程调用的结果，该方法会阻塞主线程</li>
<li><code>Future</code> 类有提供 <code>isDone()</code> 方法来判断线程是否执行完成，该方法不会阻塞</li>
<li><code>Future</code> 可以拿到线程结果是因为在线程任务类（<code>FutureTask</code>）中执行完任务之后将结果设置到了 <code>Future</code> 中【先有个概念，下文会分析】</li>
</ol>
<h2 id="Future-的使用示例"><a href="#Future-的使用示例" class="headerlink" title="Future 的使用示例"></a>Future 的使用示例</h2><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">TestThread</span> &#123;<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">main</span><span class="hljs-params">(String[] args)</span> <span class="hljs-keyword">throws</span> ExecutionException, InterruptedException &#123;<br>        <span class="hljs-type">ExecutorService</span> <span class="hljs-variable">executorService</span> <span class="hljs-operator">=</span> Executors.newSingleThreadExecutor();<br>        <span class="hljs-comment">// 这里submit 方法是 &lt;T&gt; Future&lt;T&gt; submit(Callable&lt;T&gt; task);</span><br>        Future&lt;String&gt; future = executorService.submit(() -&gt; &#123;<br>            System.out.println(<span class="hljs-string">&quot;start execute:&quot;</span> + DateTimeFormatter.ofPattern(<span class="hljs-string">&quot;yyyy-MM-dd HH:mm:ss&quot;</span>).format(LocalDateTime.now()));<br>            Thread.sleep(<span class="hljs-number">3000</span>);<br>            <span class="hljs-keyword">return</span> <span class="hljs-string">&quot;hello&quot;</span>;<br>        &#125;);<br>        <span class="hljs-comment">// 非阻塞调用，不断查询结果是否完成</span><br>        <span class="hljs-keyword">while</span> (!future.isDone()) &#123;<br>            System.out.println(<span class="hljs-string">&quot;线程未执行完成&quot;</span>);<br>            Thread.sleep(<span class="hljs-number">1000</span>);<br>        &#125;<br>        <span class="hljs-comment">// 若任务未执行完成，get 方法会阻塞主线程</span><br>        <span class="hljs-type">String</span> <span class="hljs-variable">result</span> <span class="hljs-operator">=</span> future.get();<br>        System.out.println(<span class="hljs-string">&quot;after execute Time:&quot;</span> +  DateTimeFormatter.ofPattern(<span class="hljs-string">&quot;yyyy-MM-dd HH:mm:ss&quot;</span>).format(LocalDateTime.now()));<br>        System.out.println(result);<br>        <span class="hljs-comment">// 关闭线程池，否则jvm进程会一直在运行</span><br>        executorService.shutdown();<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>
<p>执行结果</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java">线程未执行完成<br>start execute:<span class="hljs-number">2023</span>-<span class="hljs-number">11</span>-<span class="hljs-number">17</span> <span class="hljs-number">15</span>:<span class="hljs-number">14</span>:<span class="hljs-number">35</span><br>线程未执行完成<br>线程未执行完成<br>线程未执行完成<br>after execute Time:<span class="hljs-number">2023</span>-<span class="hljs-number">11</span>-<span class="hljs-number">17</span> <span class="hljs-number">15</span>:<span class="hljs-number">14</span>:<span class="hljs-number">39</span><br>hello<br></code></pre></td></tr></table></figure>
<h2 id="核心类"><a href="#核心类" class="headerlink" title="核心类"></a>核心类</h2><p><img src="https://blog-1308475102.cos.ap-guangzhou.myqcloud.com/picture/202311172311584.png" alt="FutureTask核心类"></p>
<h3 id="说明"><a href="#说明" class="headerlink" title="说明"></a>说明</h3><p>从上面类图看到核心类是 <code>FutureTask</code>, 这个类实现了 <code>Future</code> 和 <code>Runnable</code>, 并且在构造函数中需要传入 <code>Callable</code></p>
<ol>
<li><code>FutureTask</code> 和 <code>Runnable</code> 接口的关系是实现，目的是便于向上转型传参，线程池中接收任务的顶层接口就是 <code>Runnable</code></li>
<li><code>FutureTask</code> 类和 <code>Callable</code> 实现类的关系是 <strong>组合</strong>，下面是 <code>FutureTask</code> 的构造方法</li>
</ol>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-title function_">FutureTask</span><span class="hljs-params">(Callable&lt;V&gt; callable)</span> &#123;<br>    <span class="hljs-keyword">if</span> (callable == <span class="hljs-literal">null</span>)<br>        <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">NullPointerException</span>();<br>    <span class="hljs-built_in">this</span>.callable = callable;<br>    <span class="hljs-built_in">this</span>.state = NEW;       <span class="hljs-comment">// ensure visibility of callable</span><br>&#125;<br></code></pre></td></tr></table></figure>
<p>描述上面两点是因为先弄清楚 <code>FutureTask</code> 和 <code>Callable</code> 和 <code>Runnable</code> 三者之间的关系，这样才能知道最终的任务类是哪个（任务类的 <code>run()</code> 方法就是重点了）</p>
<pre class="mermaid">sequenceDiagram
    participant A as ExecutorService
    participant B as AbstractExecutorService
    participant C as ThreadPoolExecutor
    A->>B: submit(Callable)
    Note over A,B: 将Callable转换成FutureTask
    B->>B: RunnableFuture = new FutureTask<T>(callable)
    Note over B,C: 将FutureTask转换成RunnableFuture
    B->>C: execute(Runnable command) {
    B->>A: return RunnableFuture</T></pre>
<p>下面再跟着代码看一遍这个过程</p>
<ol>
<li><code>TestThread#main</code> 中创建线程池并调用 <code>submit()</code> 方法<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-type">ExecutorService</span> <span class="hljs-variable">executorService</span> <span class="hljs-operator">=</span> Executors.newSingleThreadExecutor();<br><span class="hljs-comment">// 接下来看submit(Callable) 方法</span><br>Future&lt;String&gt; future = executorService.submit(() -&gt; &#123;<br>    System.out.println(<span class="hljs-string">&quot;start execute:&quot;</span> + DateTimeFormatter.ofPattern(<span class="hljs-string">&quot;yyyy-MM-dd HH:mm:ss&quot;</span>).format(LocalDateTime.now()));<br>    Thread.sleep(<span class="hljs-number">3000</span>);<br>    <span class="hljs-keyword">return</span> <span class="hljs-string">&quot;hello&quot;</span>;<br>&#125;);<br></code></pre></td></tr></table></figure></li>
<li><code>submit()</code> 方法最终会调用到 <code>java.util.concurrent.AbstractExecutorService#submit(java.util.concurrent.Callable&lt;T&gt;)</code></li>
</ol>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> &lt;T&gt; Future&lt;T&gt; <span class="hljs-title function_">submit</span><span class="hljs-params">(Callable&lt;T&gt; task)</span> &#123;<br>    <span class="hljs-keyword">if</span> (task == <span class="hljs-literal">null</span>) <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">NullPointerException</span>();<br>    <span class="hljs-comment">// 这里就是根据 Callable 生成 FutureTask 类</span><br>    RunnableFuture&lt;T&gt; ftask = newTaskFor(task);<br>    <span class="hljs-comment">// 接下来看 execute 方法，这个就是 ThreadPoolExecutor 中的 execute() 方法</span><br>    execute(ftask);<br>    <span class="hljs-keyword">return</span> ftask;<br>&#125;<br><br><span class="hljs-keyword">protected</span> &lt;T&gt; RunnableFuture&lt;T&gt; <span class="hljs-title function_">newTaskFor</span><span class="hljs-params">(Callable&lt;T&gt; callable)</span> &#123;<br>    <span class="hljs-keyword">return</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">FutureTask</span>&lt;T&gt;(callable);<br>&#125;<br></code></pre></td></tr></table></figure>
<ol start="3">
<li><code>java.util.concurrent.ThreadPoolExecutor#execute(Runnable)</code> 这个就是线程池的执行逻辑，参考</li>
</ol>
<p><code>&#123;% post_path 线程池实现原理 %&#125;</code><br>通过上面的步骤已经知道线程池中使用的 <strong>任务类</strong> 是 <code>FutureTask</code>，那么就一定会调用 <code>FutureTask</code> 类中的 <code>run()</code> 方法</p>
<h2 id="FutureTask-类中重要属性"><a href="#FutureTask-类中重要属性" class="headerlink" title="FutureTask 类中重要属性"></a>FutureTask 类中重要属性</h2><p>但是在看 <code>run()</code> 方法之前先看一下 <code>FutureTask</code> 类中比较重要的两个属性，在后面的方法中会遇到</p>
<ol>
<li>描述任务状态的字段 <code>state</code></li>
<li>结果保存字段 <code>Object outcome</code></li>
<li>等待队列 <code>volatile WaitNode waiters;</code></li>
</ol>
<h3 id="FutureTask-任务状态字段-state"><a href="#FutureTask-任务状态字段-state" class="headerlink" title="FutureTask 任务状态字段 state"></a>FutureTask 任务状态字段 state</h3><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">private</span> <span class="hljs-keyword">volatile</span> <span class="hljs-type">int</span> state;<br><span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-type">int</span> <span class="hljs-variable">NEW</span>          <span class="hljs-operator">=</span> <span class="hljs-number">0</span>;<br><span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-type">int</span> <span class="hljs-variable">COMPLETING</span>   <span class="hljs-operator">=</span> <span class="hljs-number">1</span>;<br><span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-type">int</span> <span class="hljs-variable">NORMAL</span>       <span class="hljs-operator">=</span> <span class="hljs-number">2</span>;<br><span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-type">int</span> <span class="hljs-variable">EXCEPTIONAL</span>  <span class="hljs-operator">=</span> <span class="hljs-number">3</span>;<br><span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-type">int</span> <span class="hljs-variable">CANCELLED</span>    <span class="hljs-operator">=</span> <span class="hljs-number">4</span>;<br><span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-type">int</span> <span class="hljs-variable">INTERRUPTING</span> <span class="hljs-operator">=</span> <span class="hljs-number">5</span>;<br><span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-type">int</span> <span class="hljs-variable">INTERRUPTED</span>  <span class="hljs-operator">=</span> <span class="hljs-number">6</span>;<br></code></pre></td></tr></table></figure>
<h4 id="state-状态转换流程"><a href="#state-状态转换流程" class="headerlink" title="state 状态转换流程"></a>state 状态转换流程</h4><pre class="mermaid">graph LR
    A[new]
    B[COMPLETING]
    C[NORMAL]
    D[EXCEPTIONAL]
    E[CANCELLED]
    F[INTERRUPTING]
    G[INTERRUPTED]
    A--"set() or setException()"-->B-->C & D
    A--"cancel(false)"-->E
    A--"cancel(true)"-->F-->G</pre>
<h3 id="Object-outcome"><a href="#Object-outcome" class="headerlink" title="Object outcome"></a><code>Object outcome</code></h3><p><code>outcome</code> 字段就是用来保存线程执行的结果的</p>
<h3 id="等待队列-WaitNode-waiters"><a href="#等待队列-WaitNode-waiters" class="headerlink" title="等待队列 WaitNode waiters"></a>等待队列 <code>WaitNode waiters</code></h3><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">WaitNode</span> &#123;<br>    <span class="hljs-keyword">volatile</span> Thread thread;<br>    <span class="hljs-keyword">volatile</span> WaitNode next;<br>    WaitNode() &#123; thread = Thread.currentThread(); &#125;<br>&#125;<br></code></pre></td></tr></table></figure>
<ol>
<li>waiters 是一个链表，waiters 字段就是这个链表的头部</li>
<li>因为可以有多个线程去调用 <code>Future.get()</code> 方法，当没有获取到结果时，所有调用 <code>get()</code> 方法的线程都是需要阻塞的</li>
</ol>
<h2 id="FutureTask-的-run-方法"><a href="#FutureTask-的-run-方法" class="headerlink" title="FutureTask 的 run() 方法"></a>FutureTask 的 run() 方法</h2><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">run</span><span class="hljs-params">()</span> &#123;<br>    <span class="hljs-comment">// 1. state 记录的是当前任务的状态</span><br>    <span class="hljs-keyword">if</span> (state != NEW ||<br>        !RUNNER.compareAndSet(<span class="hljs-built_in">this</span>, <span class="hljs-literal">null</span>, Thread.currentThread()))<br>        <span class="hljs-keyword">return</span>;<br>    <span class="hljs-keyword">try</span> &#123;<br>        Callable&lt;V&gt; c = callable;<br>        <span class="hljs-keyword">if</span> (c != <span class="hljs-literal">null</span> &amp;&amp; state == NEW) &#123;<br>            V result;<br>            <span class="hljs-type">boolean</span> ran;<br>            <span class="hljs-keyword">try</span> &#123;<br>                <span class="hljs-comment">// 2. 构造 FutureTask 时将 Callable 传递进来了，在这里进行调用</span><br>                result = c.call();<br>                <span class="hljs-comment">// 设置调用完成的标识</span><br>                ran = <span class="hljs-literal">true</span>;<br>            &#125; <span class="hljs-keyword">catch</span> (Throwable ex) &#123;<br>                result = <span class="hljs-literal">null</span>;<br>                ran = <span class="hljs-literal">false</span>;<br>                setException(ex);<br>            &#125;<br>            <span class="hljs-keyword">if</span> (ran)<br>                <span class="hljs-comment">// 3. 调用完成之后设置结果</span><br>                set(result);<br>        &#125;<br>    &#125; <span class="hljs-keyword">finally</span> &#123;<br>        runner = <span class="hljs-literal">null</span>;<br>        <span class="hljs-type">int</span> <span class="hljs-variable">s</span> <span class="hljs-operator">=</span> state;<br>        <span class="hljs-keyword">if</span> (s &gt;= INTERRUPTING)<br>            handlePossibleCancellationInterrupt(s);<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>
<p>接下来看看注释中的第 <code>3</code> 步， 调用完成之后设置结果</p>
<h3 id="set-V-v-方法"><a href="#set-V-v-方法" class="headerlink" title="set(V v) 方法"></a>set(V v) 方法</h3><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">protected</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">set</span><span class="hljs-params">(V v)</span> &#123;<br>    <span class="hljs-keyword">if</span> (STATE.compareAndSet(<span class="hljs-built_in">this</span>, NEW, COMPLETING)) &#123;<br>        <span class="hljs-comment">// 1. 将执行的结果赋值给 outcome</span><br>        outcome = v;<br>        STATE.setRelease(<span class="hljs-built_in">this</span>, NORMAL); <span class="hljs-comment">// final state</span><br>        <span class="hljs-comment">// 2. 通知阻塞的线程（唤醒）</span><br>        finishCompletion();<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>
<p>将结果赋值给 <code>outcome</code> 属性之后，还调用了 <code>finishCompletion()</code> 方法，这个方法的作用是什么呢？这个其实就是 <strong>生产者消费者</strong> 模型的应用</p>
<h2 id="Future-中生产者消费者模型"><a href="#Future-中生产者消费者模型" class="headerlink" title="Future 中生产者消费者模型"></a>Future 中生产者消费者模型</h2><ol>
<li>调用 <code>Future.get()</code> 方法若线程还没有执行完成，主线程会 <strong>阻塞</strong>，当任务完成后会任务类中会 <strong>唤醒</strong> 阻塞的线程</li>
<li>阻塞是阻塞在 <code>get()</code> 方法，那唤醒呢？ 其实就是上面的 <code>finishCompletion()</code> 知道了方法的作用之后，我们再看看看 <code>get()</code> 方法和 <code>finishCompletion()</code> 方法</li>
</ol>
<h3 id="get-方法"><a href="#get-方法" class="headerlink" title="get() 方法"></a>get() 方法</h3><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> V <span class="hljs-title function_">get</span><span class="hljs-params">()</span> <span class="hljs-keyword">throws</span> InterruptedException, ExecutionException &#123;<br>    <span class="hljs-type">int</span> <span class="hljs-variable">s</span> <span class="hljs-operator">=</span> state;<br>    <span class="hljs-comment">// 先获取任务状态，任务没有完成，就进行阻塞等待</span><br>    <span class="hljs-keyword">if</span> (s &lt;= COMPLETING)<br>        s = awaitDone(<span class="hljs-literal">false</span>, <span class="hljs-number">0L</span>);<br>    <span class="hljs-keyword">return</span> report(s);<br>&#125;<br></code></pre></td></tr></table></figure>
<p>阻塞等待是调用的 <code>awaitDone()</code> 方法, 还是先总结一下这个方法的作用再看源码</p>
<ol>
<li>任务未完成时，将当前线程封装成一个 <code>WaitNode</code> 对象，添加到 <code>WaitNode waiters</code> 数属性的后面</li>
<li>保存到等待链表之后，就使用 <code>LockSupport.park(this)</code> 来阻塞当前线程</li>
</ol>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">private</span> <span class="hljs-type">int</span> <span class="hljs-title function_">awaitDone</span><span class="hljs-params">(<span class="hljs-type">boolean</span> timed, <span class="hljs-type">long</span> nanos)</span> <span class="hljs-keyword">throws</span> InterruptedException &#123;<br>    <span class="hljs-type">long</span> <span class="hljs-variable">startTime</span> <span class="hljs-operator">=</span> <span class="hljs-number">0L</span>;    <span class="hljs-comment">// Special value 0L means not yet parked</span><br>    <span class="hljs-type">WaitNode</span> <span class="hljs-variable">q</span> <span class="hljs-operator">=</span> <span class="hljs-literal">null</span>;<br>    <span class="hljs-type">boolean</span> <span class="hljs-variable">queued</span> <span class="hljs-operator">=</span> <span class="hljs-literal">false</span>;<br>    <span class="hljs-keyword">for</span> (;;) &#123;<br>        <span class="hljs-type">int</span> <span class="hljs-variable">s</span> <span class="hljs-operator">=</span> state;<br>        <span class="hljs-keyword">if</span> (s &gt; COMPLETING) &#123;<br>            <span class="hljs-keyword">if</span> (q != <span class="hljs-literal">null</span>)<br>                q.thread = <span class="hljs-literal">null</span>;<br>            <span class="hljs-keyword">return</span> s;<br>        &#125;<br>        <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (s == COMPLETING)<br>            Thread.<span class="hljs-keyword">yield</span>();<br>        <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (Thread.interrupted()) &#123;<br>            removeWaiter(q);<br>            <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">InterruptedException</span>();<br>        &#125;<br>        <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (q == <span class="hljs-literal">null</span>) &#123;<br>            <span class="hljs-keyword">if</span> (timed &amp;&amp; nanos &lt;= <span class="hljs-number">0L</span>)<br>                <span class="hljs-keyword">return</span> s;<br>            <span class="hljs-comment">// 由当前线程构成一个 WaitNode 对象</span><br>            q = <span class="hljs-keyword">new</span> <span class="hljs-title class_">WaitNode</span>();<br>        &#125;<br>        <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (!queued)<br>            <span class="hljs-comment">// 将构造的 waitNode 对象添加到 waiters 链表中【可能有人有点疑惑，这两个逻辑处在不同 else if 条件中，为什么会同时执行到，其实这里不是同时执行到，而是串行的，因为这整块代码在一个for循环中，前面进行一个else分支后，下一次循环条件就改变了，导致进入到了不同的else分支中】</span><br>            queued = WAITERS.weakCompareAndSet(<span class="hljs-built_in">this</span>, q.next = waiters, q);<br>        <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (timed) &#123;<br>            <span class="hljs-keyword">final</span> <span class="hljs-type">long</span> parkNanos;<br>            <span class="hljs-keyword">if</span> (startTime == <span class="hljs-number">0L</span>) &#123; <span class="hljs-comment">// first time</span><br>                startTime = System.nanoTime();<br>                <span class="hljs-keyword">if</span> (startTime == <span class="hljs-number">0L</span>)<br>                    startTime = <span class="hljs-number">1L</span>;<br>                parkNanos = nanos;<br>            &#125; <span class="hljs-keyword">else</span> &#123;<br>                <span class="hljs-type">long</span> <span class="hljs-variable">elapsed</span> <span class="hljs-operator">=</span> System.nanoTime() - startTime;<br>                <span class="hljs-keyword">if</span> (elapsed &gt;= nanos) &#123;<br>                    removeWaiter(q);<br>                    <span class="hljs-keyword">return</span> state;<br>                &#125;<br>                parkNanos = nanos - elapsed;<br>            &#125;<br>            <span class="hljs-keyword">if</span> (state &lt; COMPLETING)<br>                LockSupport.parkNanos(<span class="hljs-built_in">this</span>, parkNanos);<br>        &#125;<br>        <span class="hljs-keyword">else</span><br>            <span class="hljs-comment">// 阻塞当前线程</span><br>            LockSupport.park(<span class="hljs-built_in">this</span>);<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<h3 id="finishCompletion-方法"><a href="#finishCompletion-方法" class="headerlink" title="finishCompletion() 方法"></a><code>finishCompletion()</code> 方法</h3><p>看完了 <code>get()</code> 阻塞方法，就可以来看看唤醒阻塞的线程方法 <code>finishCompletion()</code> 了, 同样的先简单总结一下该方法做的事情</p>
<ol>
<li>遍历 <code>waiters</code> 从里面获取阻塞的线程</li>
<li>获取到阻塞的线程之后，调用 <code>LockSupport.unpark(thread)</code> 来唤醒对应的线程<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">private</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">finishCompletion</span><span class="hljs-params">()</span> &#123;<br>    <span class="hljs-keyword">for</span> (WaitNode q; (q = waiters) != <span class="hljs-literal">null</span>;) &#123;<br>        <span class="hljs-keyword">if</span> (WAITERS.weakCompareAndSet(<span class="hljs-built_in">this</span>, q, <span class="hljs-literal">null</span>)) &#123;<br>            <span class="hljs-keyword">for</span> (;;) &#123;<br>                <span class="hljs-type">Thread</span> <span class="hljs-variable">t</span> <span class="hljs-operator">=</span> q.thread;<br>                <span class="hljs-keyword">if</span> (t != <span class="hljs-literal">null</span>) &#123;<br>                    q.thread = <span class="hljs-literal">null</span>;<br>                    <span class="hljs-comment">// 唤醒线程</span><br>                    LockSupport.unpark(t);<br>                &#125;<br>                <span class="hljs-type">WaitNode</span> <span class="hljs-variable">next</span> <span class="hljs-operator">=</span> q.next;<br>                <span class="hljs-keyword">if</span> (next == <span class="hljs-literal">null</span>)<br>                    <span class="hljs-keyword">break</span>;<br>                q.next = <span class="hljs-literal">null</span>; <span class="hljs-comment">// unlink to help gc</span><br>                q = next;<br>            &#125;<br>            <span class="hljs-keyword">break</span>;<br>        &#125;<br>    &#125;<br>    done();<br>    callable = <span class="hljs-literal">null</span>;        <span class="hljs-comment">// to reduce footprint</span><br>&#125;<br></code></pre></td></tr></table></figure></li>
</ol>
]]></content>
      <categories>
        <category>java</category>
        <category>多线程</category>
      </categories>
      <tags>
        <tag>java</tag>
        <tag>多线程</tag>
      </tags>
  </entry>
  <entry>
    <title>CompletableFuture的使用</title>
    <url>/2023/f31e3a7691d6/index.html</url>
    <content><![CDATA[<span class="hide_first_mermaid">
<div class="mermaid-wrap"><pre class="mermaid-src" hidden>
  flowchart LR
    A
  </pre></div>
</span>

<p>以下内容参考《Java并发编程深度解析与实战》谭锋（第11章）</p>
<h2 id="为什么要用-CompletableFuture"><a href="#为什么要用-CompletableFuture" class="headerlink" title="为什么要用 CompletableFuture"></a>为什么要用 <code>CompletableFuture</code></h2><p>使用 <code>CompletableFuture</code> 是因为 <code>Future</code> 虽然可以获取线程执行结果，但是无法使用回调函数，只能依靠阻塞方法 <code>get()</code> 来获取线程结果，<code>CompletableFuture</code> 就是对 <code>Future</code> 的优化和增强，具体可以实现以下功能</p>
<ol>
<li>提供了类似 <code>Future</code> 的阻塞式获取结果和状态的方法</li>
<li>提供 <code>CompletionStage</code> 任务执行之后回调</li>
<li>多个异步任务的聚合，串行，并行等功能</li>
</ol>
<h2 id="构造异步方法"><a href="#构造异步方法" class="headerlink" title="构造异步方法"></a>构造异步方法</h2><p><code>CompletableFuture</code> 提供了 4 个静态方法来构造一个异步事件</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-comment">// 含有返回值的异步方法（自定义线程池和默认线程池）</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> &lt;U&gt; CompletableFuture&lt;U&gt; <span class="hljs-title function_">supplyAsync</span><span class="hljs-params">(Supplier&lt;U&gt; supplier)</span>;<br><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> &lt;U&gt; CompletableFuture&lt;U&gt; <span class="hljs-title function_">supplyAsync</span><span class="hljs-params">(Supplier&lt;U&gt; supplier, Executor executor)</span>;<br><span class="hljs-comment">// 不含有返回值的异步方法（自定义线程池和默认线程池）</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> CompletableFuture&lt;Void&gt; <span class="hljs-title function_">runAsync</span><span class="hljs-params">(Runnable runnable)</span>;<br><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> CompletableFuture&lt;Void&gt; <span class="hljs-title function_">runAsync</span><span class="hljs-params">(Runnable runnable,Executor executor)</span><br></code></pre></td></tr></table></figure>
<div class="tabs" id="代码示例"><ul class="nav-tabs"><button type="button" class="tab  active" data-href="代码示例-1">supplyAsync</button><button type="button" class="tab " data-href="代码示例-2">runAsync示例</button></ul><div class="tab-contents"><div class="tab-item-content active" id="代码示例-1"><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">main</span><span class="hljs-params">(String[] args)</span> <span class="hljs-keyword">throws</span> ExecutionException, InterruptedException &#123;<br>      CompletableFuture&lt;Integer&gt; future = CompletableFuture.supplyAsync(() -&gt; &#123;<br>          System.out.println(<span class="hljs-string">&quot;当前线程：&quot;</span> + Thread.currentThread().getName());<br>          <span class="hljs-keyword">return</span> <span class="hljs-number">1</span>;<br>      &#125;);<br>      System.out.println(<span class="hljs-string">&quot;线程执行结果：&quot;</span> + future.get());<br>      System.out.println(<span class="hljs-string">&quot;主线程:&quot;</span> + Thread.currentThread().getName());<br>  &#125;<br></code></pre></td></tr></table></figure>
<figure class="highlight java"><figcaption><span>执行结果</span></figcaption><table><tr><td class="code"><pre><code class="hljs java">当前线程：ForkJoinPool.commonPool-worker-<span class="hljs-number">1</span><br>线程执行结果：<span class="hljs-number">1</span><br>主线程:main<br></code></pre></td></tr></table></figure></div><div class="tab-item-content" id="代码示例-2"><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">main</span><span class="hljs-params">(String[] args)</span> <span class="hljs-keyword">throws</span> ExecutionException, InterruptedException &#123;<br>      CompletableFuture&lt;Void&gt; future = CompletableFuture.runAsync(() -&gt; &#123;<br>          System.out.println(<span class="hljs-string">&quot;当前线程：&quot;</span> + Thread.currentThread().getName());<br>      &#125;);<br>      System.out.println(<span class="hljs-string">&quot;线程执行结果：&quot;</span> + future.get());<br>      System.out.println(<span class="hljs-string">&quot;主线程:&quot;</span> + Thread.currentThread().getName());<br>  &#125;<br></code></pre></td></tr></table></figure>
<figure class="highlight java"><figcaption><span>执行结果</span></figcaption><table><tr><td class="code"><pre><code class="hljs java">当前线程：ForkJoinPool.commonPool-worker-<span class="hljs-number">1</span><br>线程执行结果：<span class="hljs-literal">null</span><br>主线程:main<br></code></pre></td></tr></table></figure></div></div><div class="tab-to-top"><button type="button" aria-label="scroll to top"><i class="fas fa-arrow-up"></i></button></div></div>

<h2 id="任务与和或的静态方法"><a href="#任务与和或的静态方法" class="headerlink" title="任务与和或的静态方法"></a>任务与和或的静态方法</h2><p><code>CompletableFuture</code> 中还有另外两个特殊的静态方法</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-comment">// 接收多个CompletableFuture无返回值任务，当所有的CompletableFuture任务执行结束后，返回一个新的CompletableFuture对象</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> CompletableFuture&lt;Void&gt; <span class="hljs-title function_">allOf</span><span class="hljs-params">(CompletableFuture&lt;?&gt;... cfs)</span>;<br><span class="hljs-comment">//接收多个CompletableFuture带有返回值任务，当任何一个CompletableFuture任务执行完成后，返回一个新的CompletableFuture对象</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> CompletableFuture&lt;Object&gt; <span class="hljs-title function_">anyOf</span><span class="hljs-params">(CompletableFuture&lt;?&gt;... cfs)</span>;<br></code></pre></td></tr></table></figure>
<div class="tabs" id="allof()和anyof()"><ul class="nav-tabs"><button type="button" class="tab  active" data-href="allof()和anyof()-1">allOf()</button><button type="button" class="tab " data-href="allof()和anyof()-2">anyOf()</button></ul><div class="tab-contents"><div class="tab-item-content active" id="allof()和anyof()-1"><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">main</span><span class="hljs-params">(String[] args)</span> <span class="hljs-keyword">throws</span> ExecutionException, InterruptedException &#123;<br>    CompletableFuture&lt;Void&gt; future1 = CompletableFuture.runAsync(() -&gt; &#123;<br>        <span class="hljs-keyword">try</span> &#123;<br>            Thread.sleep(<span class="hljs-number">3000</span>);<br>        &#125; <span class="hljs-keyword">catch</span> (InterruptedException e) &#123;<br>            <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">RuntimeException</span>(e);<br>        &#125;<br>        System.out.println(<span class="hljs-string">&quot;当前线程：&quot;</span> + Thread.currentThread().getName());<br>    &#125;);<br>    CompletableFuture&lt;Void&gt; future2 = CompletableFuture.runAsync(() -&gt; &#123;<br>        <span class="hljs-keyword">try</span> &#123;<br>            Thread.sleep(<span class="hljs-number">5000</span>);<br>        &#125; <span class="hljs-keyword">catch</span> (InterruptedException e) &#123;<br>            <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">RuntimeException</span>(e);<br>        &#125;<br>        System.out.println(<span class="hljs-string">&quot;当前线程：&quot;</span> + Thread.currentThread().getName());<br>    &#125;);<br>    <span class="hljs-type">LocalTime</span> <span class="hljs-variable">now</span> <span class="hljs-operator">=</span> LocalTime.now();<br>    System.out.println(<span class="hljs-string">&quot;before time:&quot;</span> + now.getMinute() + <span class="hljs-string">&quot;:&quot;</span> + now.getSecond());<br>    CompletableFuture.allOf(future1, future2).join();<br>    now = LocalTime.now();<br>    System.out.println(<span class="hljs-string">&quot;after time:&quot;</span> + now.getMinute() + <span class="hljs-string">&quot;:&quot;</span> + now.getSecond());<br>&#125;<br></code></pre></td></tr></table></figure>
<p>执行结果，用了5s才返回，因为要所有都执行完成才返回，就需要看耗时最长的那个线程</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java">before time:<span class="hljs-number">50</span>:<span class="hljs-number">33</span><br>当前线程：ForkJoinPool.commonPool-worker-<span class="hljs-number">1</span><br>当前线程：ForkJoinPool.commonPool-worker-<span class="hljs-number">2</span><br>after time:<span class="hljs-number">50</span>:<span class="hljs-number">38</span><br></code></pre></td></tr></table></figure></div><div class="tab-item-content" id="allof()和anyof()-2"><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">main</span><span class="hljs-params">(String[] args)</span> <span class="hljs-keyword">throws</span> ExecutionException, InterruptedException &#123;<br>    CompletableFuture&lt;Void&gt; future1 = CompletableFuture.runAsync(() -&gt; &#123;<br>        <span class="hljs-keyword">try</span> &#123;<br>            Thread.sleep(<span class="hljs-number">3000</span>);<br>        &#125; <span class="hljs-keyword">catch</span> (InterruptedException e) &#123;<br>            <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">RuntimeException</span>(e);<br>        &#125;<br>        System.out.println(<span class="hljs-string">&quot;当前线程：&quot;</span> + Thread.currentThread().getName());<br>    &#125;);<br>    CompletableFuture&lt;Void&gt; future2 = CompletableFuture.runAsync(() -&gt; &#123;<br>        <span class="hljs-keyword">try</span> &#123;<br>            Thread.sleep(<span class="hljs-number">5000</span>);<br>        &#125; <span class="hljs-keyword">catch</span> (InterruptedException e) &#123;<br>            <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">RuntimeException</span>(e);<br>        &#125;<br>        System.out.println(<span class="hljs-string">&quot;当前线程：&quot;</span> + Thread.currentThread().getName());<br>    &#125;);<br>    <span class="hljs-type">LocalTime</span> <span class="hljs-variable">now</span> <span class="hljs-operator">=</span> LocalTime.now();<br>    System.out.println(<span class="hljs-string">&quot;before time:&quot;</span> + now.getMinute() + <span class="hljs-string">&quot;:&quot;</span> + now.getSecond());<br>    CompletableFuture.anyOf(future1, future2).join();<br>    now = LocalTime.now();<br>    System.out.println(<span class="hljs-string">&quot;after time:&quot;</span> + now.getMinute() + <span class="hljs-string">&quot;:&quot;</span> + now.getSecond());<br>&#125;<br></code></pre></td></tr></table></figure>
<p>执行结果，只用了3s就返回了，因为是其中一个执行完成就行，所以看耗时最短的那个线程</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java">before time:<span class="hljs-number">53</span>:<span class="hljs-number">43</span><br>当前线程：ForkJoinPool.commonPool-worker-<span class="hljs-number">1</span><br>after time:<span class="hljs-number">53</span>:<span class="hljs-number">46</span><br></code></pre></td></tr></table></figure></div></div><div class="tab-to-top"><button type="button" aria-label="scroll to top"><i class="fas fa-arrow-up"></i></button></div></div>

<h2 id="CompletionStage-详解"><a href="#CompletionStage-详解" class="headerlink" title="CompletionStage 详解"></a><code>CompletionStage</code> 详解</h2><ol>
<li><code>CompletionStage</code> 表示任务执行的一个阶段，每个异步任务都会返回一个新的 <code>CompletionStage</code> 对象</li>
<li>可以针对多个 <code>CompletionStage</code> 对象进行串行、并行或者聚合来进行下一阶段的操作</li>
</ol>
<p><code>CompletionStage</code> 类中提供了很多方法来实现多个 <code>CompletionStage</code> 的串行，并行等功能，可以按照如下方式进行分类</p>
<pre class="mermaid">flowchart LR
    A[CompletionStage]
    B["纯消费性方法 \n (上一个异步结果 \n 作为当前方法的参数进行计算 \n 都含有 Accept 关键字)"]
        B1[依赖单个 CompletionStage任务完成]
            B11["thenAccept(Consumer)"]
            B12["thenAcceptAsync(Consumer)"]
            B13["thenAcceptAsync(Consumer,Executor)"]
            B1-->B11 & B12 & B13
        B2[依赖两个 CompletionStage任务完成]
            B21["thenAcceptBoth(CompletionStage,Consumer)"]
            B22["thenAcceptBothAsync(CompletionStage,Consumer)"]
            B23["thenAcceptBothAsync(CompletionStage,Consumer,Executor)"]
            B2-->B21 & B22 & B23
        B3[依赖两个 CompletionStage任务中任何一个完成]
            B31["acceptEither(CompletionStage,Consumer)"]
            B32["acceptEitherAsync(CompletionStage,Consumer)"]
            B33["acceptEitherAsync(CompletionStage,Consumer,Executor)"]
            B3-->B31 & B32 & B33
        B-->B1 & B2 & B3

    C["有返回值类型的方法 \n (上一个异步结果 \n 作为当前方法的参数进行计算 \n 并且会产生新的有返回值\n的CompletionStage对象)"]
        C1[依赖单个 CompletionStage任务完成]
            C11["thenApply(Function)"]
            C12["thenApplyAsync(Function)"]
            C13["thenApplyAsync(Function,Executor)"]
            C1-->C11 & C12 & C13
        C2[依赖两个 CompletionStage任务完成]
            C21["thenCombine(CompletionStage,BiFunction)"]
            C22["thenCombineAsync(CompletionStage,BiFunction)"]
            C23["thenCombineAsync(CompletionStage,BiFunction,Executor)"]
            C2-->C21 & C22 & C23
        C3[依赖两个 CompletionStage任务中任何一个完成]
            C31["acceptToEither(CompletionStage,Function)"]
            C32["acceptToEitherAsync(CompletionStage,Function)"]
            C33["acceptToEitherAsync(CompletionStage,Function,Executor)"]
            C3-->C31 & C32 & C33
        C-->C1 & C2 & C3
    D["不消费也没有返回值类型的方法\n不依赖上个阶段的结果\n上一个阶段完成就执行指定的任务\n这类方法都包含 run 关键字"]
        D1[依赖单个 CompletionStage任务完成]
            D11["thenRun(Runnable)"]
            D12["thenRunAsync(Runnable)"]
            D13["thenRunAsync(FuncRunnabletion,Executor)"]
            D1-->D11 & D12 & D13
        D2[依赖两个 CompletionStage任务完成]
            D21["runAfterBoth(CompletionStage,Runnable)"]
            D22["runAfterBothAsync(CompletionStage,Runnable)"]
            D23["runAfterBothAsync(CompletionStage,Runnable,Executor)"]
            D2-->D21 & D22 & D23
        D3[依赖两个 CompletionStage任务中任何一个完成]
            D31["runAfterEither(CompletionStage,Runnable)"]
            D32["runAfterEitherAsync(CompletionStage,Runnable)"]
            D33["runAfterEitherAsync(CompletionStage,Runnable,Executor)"]
            D3-->D31 & D32 & D33
        D-->D1 & D2 & D3
    E[组合类型的方法]
        E1["thenCompose(Function,CompletionStage)"]
        E2["thenComposeAsync(Function,CompletionStage)"]
        E3["thenComposeAsync(Function,CompletionStage,Executor)"]
        E-->E1 & E2 & E3
    A-->B & C & D & E</pre>

<div class="tabs" id="代码示例"><ul class="nav-tabs"><button type="button" class="tab  active" data-href="代码示例-1">纯消费型</button><button type="button" class="tab " data-href="代码示例-2">有返回值类型</button><button type="button" class="tab " data-href="代码示例-3">不消费也没有返回值类型</button><button type="button" class="tab " data-href="代码示例-4">组合类型</button></ul><div class="tab-contents"><div class="tab-item-content active" id="代码示例-1"><figure class="highlight java"><figcaption><span>thenAccept</span></figcaption><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">main</span><span class="hljs-params">(String[] args)</span> <span class="hljs-keyword">throws</span> ExecutionException, InterruptedException &#123;<br>    System.out.println(<span class="hljs-string">&quot;thread main:&quot;</span> + Thread.currentThread().getName());<br>    CompletableFuture.supplyAsync(()-&gt;&#123;<br>        System.out.println(<span class="hljs-string">&quot;first stage:&quot;</span> + Thread.currentThread().getName());<br>        <span class="hljs-keyword">return</span> <span class="hljs-number">1</span>;<br>    &#125;).thenAccept((Integer v)-&gt; &#123;<br>        <span class="hljs-comment">// v 就是第一步返回的结果</span><br>        System.out.println(<span class="hljs-string">&quot;then accept thread:&quot;</span> + Thread.currentThread().getName());<br>        System.out.println(v + <span class="hljs-number">1</span>); <span class="hljs-comment">// 2</span><br>    &#125;).join();<br>&#125;<br></code></pre></td></tr></table></figure>
<figure class="highlight java"><figcaption><span>执行结果</span></figcaption><table><tr><td class="code"><pre><code class="hljs java">thread main:main<br>first stage:ForkJoinPool.commonPool-worker-<span class="hljs-number">1</span><br>then accept thread:main<br><span class="hljs-number">2</span><br></code></pre></td></tr></table></figure>
<p>上面代码调用 <code>thenAccept()</code> 使用的是 <code>main</code> 线程，如果是使用 <code>thenAcceptAsync()</code>, 调用结果如下</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java">thread main:main<br>first stage:ForkJoinPool.commonPool-worker-<span class="hljs-number">1</span><br>then accept thread:ForkJoinPool.commonPool-worker-<span class="hljs-number">1</span><br><span class="hljs-number">2</span><br></code></pre></td></tr></table></figure></div><div class="tab-item-content" id="代码示例-2"><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">main</span><span class="hljs-params">(String[] args)</span> <span class="hljs-keyword">throws</span> ExecutionException, InterruptedException &#123;<br>    System.out.println(<span class="hljs-string">&quot;thread main:&quot;</span> + Thread.currentThread().getName());<br>    CompletableFuture&lt;String&gt; completableFuture = CompletableFuture.supplyAsync(() -&gt; &#123;<br>        <span class="hljs-comment">// 第一个阶段任务返回的是 int 类型</span><br>        <span class="hljs-keyword">return</span> <span class="hljs-number">1</span>;<br>    &#125;).thenCombineAsync(CompletableFuture.supplyAsync(() -&gt; &#123;<br>        <span class="hljs-comment">// 第二个阶段任务返回的是 String 类型</span><br>        <span class="hljs-keyword">return</span> <span class="hljs-string">&quot;2&quot;</span>;<br>    &#125;), (Integer i1, String s1) -&gt; &#123;<br>        <span class="hljs-keyword">return</span> i1 + s1;<br>    &#125;);<br>    System.out.println(completableFuture.get()); <span class="hljs-comment">// 12</span><br>&#125;<br></code></pre></td></tr></table></figure></div><div class="tab-item-content" id="代码示例-3"><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">main</span><span class="hljs-params">(String[] args)</span> <span class="hljs-keyword">throws</span> Exception &#123;<br>    <span class="hljs-type">DateTimeFormatter</span> <span class="hljs-variable">formatter</span> <span class="hljs-operator">=</span> DateTimeFormatter.ofPattern(<span class="hljs-string">&quot;yyyy-MM-dd HH:mm:ss&quot;</span>);<br>    CompletableFuture.supplyAsync(() -&gt; &#123;<br>        <span class="hljs-keyword">try</span> &#123;<br>            <span class="hljs-comment">// 第一个阶段任务等待4s</span><br>            Thread.sleep(<span class="hljs-number">4000</span>);<br>            System.out.println(<span class="hljs-string">&quot;第一个阶段:&quot;</span> + LocalDateTime.now().format(formatter));<br>        &#125; <span class="hljs-keyword">catch</span> (InterruptedException e) &#123;<br>            <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">RuntimeException</span>(e);<br>        &#125;<br>        <span class="hljs-keyword">return</span> <span class="hljs-number">1</span>;<br>    &#125;).runAfterEitherAsync(CompletableFuture.supplyAsync(() -&gt; &#123;<br>        <span class="hljs-comment">// 第二个阶段任务等待2s</span><br>        <span class="hljs-keyword">try</span> &#123;<br>            Thread.sleep(<span class="hljs-number">2000</span>);<br>            System.out.println(<span class="hljs-string">&quot;第二个阶段：&quot;</span> + LocalDateTime.now().format(formatter));<br>        &#125; <span class="hljs-keyword">catch</span> (InterruptedException e) &#123;<br>            <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">RuntimeException</span>(e);<br>        &#125;<br>        <span class="hljs-keyword">return</span> <span class="hljs-string">&quot;2&quot;</span>;<br>    &#125;), () -&gt; &#123;<br>        System.out.println(<span class="hljs-string">&quot;最终:&quot;</span> + LocalDateTime.now().format(formatter));<br>        System.out.println(<span class="hljs-string">&quot;最终执行的任务&quot;</span>);<br>    &#125;).join();<br>&#125;<br></code></pre></td></tr></table></figure>
<p>执行结果</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java">第二个阶段：<span class="hljs-number">2023</span>-<span class="hljs-number">11</span>-<span class="hljs-number">20</span> <span class="hljs-number">23</span>:<span class="hljs-number">56</span>:<span class="hljs-number">32</span><br>最终:<span class="hljs-number">2023</span>-<span class="hljs-number">11</span>-<span class="hljs-number">20</span> <span class="hljs-number">23</span>:<span class="hljs-number">56</span>:<span class="hljs-number">32</span><br>最终执行的任务<br></code></pre></td></tr></table></figure></div><div class="tab-item-content" id="代码示例-4"><ol>
<li><code>thenCompose()</code>是多任务组合方法，它的作用是把两个<code>CompletionStage</code>任务进行组合达到串行执行的目的，也就是把第一个任务的执行结果作为参数传递给第二个任务执行，它有点类似于前面提到的<code>thenCombine()</code>方法，最大的不同在于<code>thenCompose()</code>方法中的任务存在先后关系，而<code>thenCombine()</code>中两个任务是并行执行的</li>
</ol></div></div><div class="tab-to-top"><button type="button" aria-label="scroll to top"><i class="fas fa-arrow-up"></i></button></div></div>

<h2 id="异常处理"><a href="#异常处理" class="headerlink" title="异常处理"></a>异常处理</h2><p><code>CompletionStage</code> 是链式处理，当前面的任务出现异常的时候，会导致后面的任务无法处理</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">main</span><span class="hljs-params">(String[] args)</span> <span class="hljs-keyword">throws</span> Exception &#123;<br>    CompletableFuture.supplyAsync(() -&gt; &#123;<br>        <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">RuntimeException</span>(<span class="hljs-string">&quot;第一个任务出现异常&quot;</span>);<br>    &#125;).thenRun(()-&gt;&#123;<br>        <span class="hljs-comment">// 这里并不会打印，因为前面一个任务出现了异常，导致该任务无法执行</span><br>        System.out.println(<span class="hljs-string">&quot;第二个任务&quot;</span>);<br>    &#125;).join();<br>&#125;<br></code></pre></td></tr></table></figure>
<p>所以在 <code>CompletionStage</code> 中也提供了异常处理的相关方法，主要有以下三类</p>
<pre class="mermaid">flowchart LR
    A[CompletionStage异常处理]
    B["以whenComplete前缀开头的方法\n不论前置的CompletionStage任务是正常执行结束\n还是出现异常，都能够触发特定的action\n这些方法都接收两个参数，\n一个是正常的结果，一个是异常\n没有异常时第二个就是null"]
        B1["whenComplete(BiConsumer)"]
        B2["whenCompleteAsync(BiConsumer)"]
        B3["whenCompleteAsync(BiConsumer,Executor)"]
        B-->B1 & B2 & B3
    C["以handle前缀开头的方法\n表示前置任务执行完成后，\n不管前置任务执行状态是正常还是异常，\n都会执行其中的函数fn，\n它和whenComplete类方法的\n作用几乎一致，不同点在于，\n这类方法是有返回值类型的方法"]
        C1["handle(BiFunction)"]
        C2["handleAsync(BiFunction)"]
        C3["handleAsync(BiFunction,Executor)"]
        C-->C1 & C2 & C3
    D["exceptionally()"]
        D1["exceptionally()方法接收一个函数fn，\n当上一个CompletionStage出现异常时，\n会把该异常作为参数传递给函数fn。\n该方法有一个CompletionStage的返回值，\n说明当前方法可以在接收到\n上一个阶段的异常时进行进一步处理，\n返回一个新的CompletionStage对象实例"]
        D-->D1
    A-->B & C & D</pre>

<h3 id="whenComplete类型方法"><a href="#whenComplete类型方法" class="headerlink" title="whenComplete类型方法"></a>whenComplete类型方法</h3><div class="tabs" id="whencomplete类型异常"><ul class="nav-tabs"><button type="button" class="tab  active" data-href="whencomplete类型异常-1">没有异常的情况</button><button type="button" class="tab " data-href="whencomplete类型异常-2">有异常的情况</button></ul><div class="tab-contents"><div class="tab-item-content active" id="whencomplete类型异常-1"><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">main</span><span class="hljs-params">(String[] args)</span> <span class="hljs-keyword">throws</span> Exception &#123;<br>    CompletableFuture.supplyAsync(() -&gt; &#123;<br>        <span class="hljs-keyword">return</span> <span class="hljs-number">1</span>;<br>    &#125;).whenCompleteAsync((result, excep)-&gt;&#123;<br>        System.out.println(<span class="hljs-string">&quot;任务结果:&quot;</span> + result);<br>        System.out.println(<span class="hljs-string">&quot;第一个任务的异常:&quot;</span> + excep);<br>    &#125;).join();<br>&#125;<br></code></pre></td></tr></table></figure>
<p>执行结果</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java">任务结果:<span class="hljs-number">1</span><br>第一个任务的异常:<span class="hljs-literal">null</span><br></code></pre></td></tr></table></figure></div><div class="tab-item-content" id="whencomplete类型异常-2"><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">main</span><span class="hljs-params">(String[] args)</span> <span class="hljs-keyword">throws</span> Exception &#123;<br>    CompletableFuture.supplyAsync(() -&gt; &#123;<br>        <span class="hljs-keyword">try</span> &#123;<br>            <span class="hljs-type">int</span> <span class="hljs-variable">i</span> <span class="hljs-operator">=</span> <span class="hljs-number">1</span> / <span class="hljs-number">0</span>;<br>        &#125; <span class="hljs-keyword">catch</span> (Exception e) &#123;<br>            <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">RuntimeException</span>(<span class="hljs-string">&quot;第一个任务出现异常&quot;</span>);<br>        &#125;<br>        <span class="hljs-keyword">return</span> <span class="hljs-number">1</span>;<br>    &#125;).whenCompleteAsync((result, excep)-&gt;&#123;<br>        System.out.println(<span class="hljs-string">&quot;任务结果:&quot;</span> + result);<br>        System.out.println(<span class="hljs-string">&quot;第一个任务的异常:&quot;</span> + excep);<br>    &#125;).join();<br>&#125;<br></code></pre></td></tr></table></figure>
<p>执行结果</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java">任务结果:<span class="hljs-literal">null</span><br>第一个任务的异常:java.util.concurrent.CompletionException: java.lang.RuntimeException: 第一个任务出现异常<br>Exception in thread <span class="hljs-string">&quot;main&quot;</span> java.util.concurrent.CompletionException: java.lang.RuntimeException: 第一个任务出现异常<br></code></pre></td></tr></table></figure></div></div><div class="tab-to-top"><button type="button" aria-label="scroll to top"><i class="fas fa-arrow-up"></i></button></div></div>

<h3 id="handle-类型方法"><a href="#handle-类型方法" class="headerlink" title="handle 类型方法"></a>handle 类型方法</h3><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">main</span><span class="hljs-params">(String[] args)</span> <span class="hljs-keyword">throws</span> Exception &#123;<br>    CompletableFuture&lt;String&gt; completableFuture = CompletableFuture.supplyAsync(() -&gt; &#123;<br>        <span class="hljs-keyword">try</span> &#123;<br>            <span class="hljs-type">int</span> <span class="hljs-variable">i</span> <span class="hljs-operator">=</span> <span class="hljs-number">1</span> / <span class="hljs-number">0</span>;<br>        &#125; <span class="hljs-keyword">catch</span> (Exception e) &#123;<br>            <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">RuntimeException</span>(<span class="hljs-string">&quot;第一个任务出现异常&quot;</span>);<br>        &#125;<br>        <span class="hljs-keyword">return</span> <span class="hljs-number">1</span>;<br>    &#125;).handle((result, excep) -&gt; &#123;<br>        System.out.println(<span class="hljs-string">&quot;任务结果:&quot;</span> + result);<br>        System.out.println(<span class="hljs-string">&quot;第一个任务的异常:&quot;</span> + excep.toString().substring(<span class="hljs-number">1</span>, <span class="hljs-number">70</span>));<br>        <span class="hljs-keyword">return</span> <span class="hljs-string">&quot;第二个任务的结果&quot;</span>;<br>    &#125;);<br>    <span class="hljs-comment">// 最终的执行结果</span><br>    System.out.println(completableFuture.get());<br>&#125;<br></code></pre></td></tr></table></figure>
<p>执行结果</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java">任务结果:<span class="hljs-literal">null</span><br>第一个任务的异常:ava.util.concurrent.CompletionException: java.lang.RuntimeException: <br>第二个任务的结果<br></code></pre></td></tr></table></figure>
<h3 id="exceptionally-方法"><a href="#exceptionally-方法" class="headerlink" title="exceptionally()方法"></a>exceptionally()方法</h3><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">main</span><span class="hljs-params">(String[] args)</span> <span class="hljs-keyword">throws</span> Exception &#123;<br>    CompletableFuture&lt;Integer&gt; completableFuture = CompletableFuture.supplyAsync(() -&gt; &#123;<br>        <span class="hljs-keyword">try</span> &#123;<br>            <span class="hljs-type">int</span> <span class="hljs-variable">i</span> <span class="hljs-operator">=</span> <span class="hljs-number">1</span> / <span class="hljs-number">0</span>;<br>        &#125; <span class="hljs-keyword">catch</span> (Exception e) &#123;<br>            <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">RuntimeException</span>(<span class="hljs-string">&quot;第一个任务出现异常&quot;</span>);<br>        &#125;<br>        <span class="hljs-keyword">return</span> <span class="hljs-number">1</span>;<br>    &#125;).exceptionally(exec -&gt; &#123;<br>        <span class="hljs-comment">// exceptionally 里面的返回值需要和前面的 stage 返回值保持一致</span><br>        <span class="hljs-keyword">return</span> Integer.valueOf(<span class="hljs-string">&quot;2&quot;</span>);<br>    &#125;);<br>    <span class="hljs-comment">// 最终的执行结果</span><br>    System.out.println(completableFuture.get());<br>&#125;<br></code></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>java</category>
        <category>多线程</category>
      </categories>
      <tags>
        <tag>java</tag>
        <tag>多线程</tag>
      </tags>
  </entry>
  <entry>
    <title>Semaphore使用和原理</title>
    <url>/2023/1dbf4f386c66/index.html</url>
    <content><![CDATA[<span class="hide_first_mermaid">
<div class="mermaid-wrap"><pre class="mermaid-src" hidden>
  flowchart LR
    A
  </pre></div>
</span>

<p><code>Semaphore</code> 的主要功能是用来限制对某个资源同时访问的线程数量，它有两个核心方法</p>
<ol>
<li><code>acquire()</code>：获取一个许可，如果没有就阻塞</li>
<li><code>release()</code>：释放一个许可 然后唤醒一个等待的线程</li>
</ol>
<h2 id="Semaphore-常用方法"><a href="#Semaphore-常用方法" class="headerlink" title="Semaphore 常用方法"></a>Semaphore 常用方法</h2><ol>
<li><code>Semaphore(permits,fair)</code>，<code>permits</code> 表示令牌数，<code>fair</code> 表示公平性</li>
<li><code>acquire(permits)</code>，获取指定 <code>permits</code> 数量的令牌，如果许可证数量不足，则会阻塞当前线程</li>
<li><code>tryAcquire(permits)</code>，尝试获取指定 <code>permits</code> 数量的令牌，此过程是非阻塞的，如果令牌数不够，则返回 <code>false</code>，否则返回 <code>true</code></li>
<li><code>release(permits)</code>，释放指定 <code>permits</code> 数量的令牌</li>
<li><code>drainPermits()</code>，当前线程获得剩下的所有可用令牌。</li>
<li><code>hasQueuedThread()</code>，判断当前 <code>Semaphore</code> 实例上是否存在正在等待令牌的线程。</li>
</ol>
<h2 id="Semaphore的使用原理图"><a href="#Semaphore的使用原理图" class="headerlink" title="Semaphore的使用原理图"></a>Semaphore的使用原理图</h2><pre class="mermaid">flowchart TB
    subgraph A["Threads"]
        direction LR
        A1["thread"]
        A2["thread"]
        A3["......"]
        A4["thread"]
        A1~~~A2~~~A3~~~A4
    end
    subgraph B["Semaphore"]
        B1["1 (permits=2)"]
        B2["2 (permits=2)"]
        B1~~~B2
    end
    C[resources]
    A--"acquire()"-->B
    A--"acquire()"-->B
    A--"......"-->B
    A--"acquire()"-->B
    B--"同时只允许两个线程访问"-->C
    B--"同时只允许两个线程访问"-->C</pre>

<h2 id="Semaphore的使用场景"><a href="#Semaphore的使用场景" class="headerlink" title="Semaphore的使用场景"></a>Semaphore的使用场景</h2><ol>
<li>限流，限制同时访问某个资源的线程数量</li>
<li>并发控制，比如数据库连接池，限制同时访问数据库的线程数量</li>
<li>信号量，用来控制同时访问某个资源的线程数量</li>
</ol>
<h2 id="Semaphore-使用案例"><a href="#Semaphore-使用案例" class="headerlink" title="Semaphore 使用案例"></a>Semaphore 使用案例</h2><p>如下代码，创建了一个线程池，提交了20个任务，每个任务中都等待1s，模拟方法调用耗时，正常来说可以20个任务同时执行，但是现在只允许2个任务同时执行，可以看到打印的结果是2个任务同时执行，剩下的任务都被阻塞了。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">TestSemaphore</span> &#123;<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">main</span><span class="hljs-params">(String[] args)</span> <span class="hljs-keyword">throws</span> InterruptedException &#123;<br>        <span class="hljs-type">ExecutorService</span> <span class="hljs-variable">executorService</span> <span class="hljs-operator">=</span> Executors.newCachedThreadPool();<br>        <span class="hljs-type">Semaphore</span> <span class="hljs-variable">semaphore</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Semaphore</span>(<span class="hljs-number">2</span>);<br>        <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> <span class="hljs-variable">i</span> <span class="hljs-operator">=</span> <span class="hljs-number">0</span>; i &lt; <span class="hljs-number">20</span>; i++) &#123;<br>            executorService.submit(<span class="hljs-keyword">new</span> <span class="hljs-title class_">SemaphoreThread</span>(semaphore));<br>        &#125;<br>    &#125;<br><br>    <span class="hljs-keyword">static</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">SemaphoreThread</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">Runnable</span> &#123;<br><br>        <span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> Semaphore semaphore;<br><br>        <span class="hljs-keyword">public</span> <span class="hljs-title function_">SemaphoreThread</span><span class="hljs-params">(Semaphore semaphore)</span> &#123;<br>            <span class="hljs-built_in">this</span>.semaphore = semaphore;<br>        &#125;<br><br>        <span class="hljs-meta">@Override</span><br>        <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">run</span><span class="hljs-params">()</span> &#123;<br>            <span class="hljs-keyword">try</span> &#123;<br>                semaphore.acquire();<br>                System.out.println(Thread.currentThread().getName() + <span class="hljs-string">&quot; 进入&quot;</span>);<br>                Thread.sleep(<span class="hljs-number">1000</span>);<br>                System.out.println(Thread.currentThread().getName() + <span class="hljs-string">&quot; 离开&quot;</span>);<br>            &#125; <span class="hljs-keyword">catch</span> (InterruptedException e) &#123;<br>                e.printStackTrace();<br>            &#125; <span class="hljs-keyword">finally</span> &#123;<br>                semaphore.release();<br>            &#125;<br>        &#125;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<p>执行结果：</p>
<figure class="highlight subunit"><table><tr><td class="code"><pre><code class="hljs subunit">pool<span class="hljs-string">-1</span>-thread<span class="hljs-string">-4</span> 进入<br>pool<span class="hljs-string">-1</span>-thread<span class="hljs-string">-1</span> 进入<br>pool<span class="hljs-string">-1</span>-thread<span class="hljs-string">-1</span> 离开<br>pool<span class="hljs-string">-1</span>-thread<span class="hljs-string">-4</span> 离开<br>pool<span class="hljs-string">-1</span>-thread<span class="hljs-string">-3</span> 进入<br>pool<span class="hljs-string">-1</span>-thread<span class="hljs-string">-8</span> 进入<br>pool<span class="hljs-string">-1</span>-thread<span class="hljs-string">-3</span> 离开<br>pool<span class="hljs-string">-1</span>-thread<span class="hljs-string">-7</span> 进入<br>pool<span class="hljs-string">-1</span>-thread<span class="hljs-string">-8</span> 离开<br>……<br></code></pre></td></tr></table></figure>
<p>可以看到最多允许两个线程进入执行，剩下的线程都被阻塞了。</p>
<h2 id="Semaphore-实现原理"><a href="#Semaphore-实现原理" class="headerlink" title="Semaphore 实现原理"></a>Semaphore 实现原理</h2><ol>
<li><code>Semaphore</code> 实际上也是基于 <code>AQS</code> 中的共享锁来实现的</li>
<li>在 <code>Semaphore</code> 中允许多个线程获得令牌被唤醒<br>以下面代码为例</li>
</ol>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-comment">//  使用部分</span><br><span class="hljs-type">Semaphore</span> <span class="hljs-variable">semaphore</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Semaphore</span>(<span class="hljs-number">2</span>);<br>semaphore.acquire();<br><br><span class="hljs-comment">// 源码部分， new Semaphore 实际创建了一个 NonfairSync 类</span><br><span class="hljs-keyword">public</span> <span class="hljs-title function_">Semaphore</span><span class="hljs-params">(<span class="hljs-type">int</span> <span class="hljs-keyword">permits</span>)</span> &#123;<br>    sync = <span class="hljs-keyword">new</span> <span class="hljs-title class_">NonfairSync</span>(<span class="hljs-keyword">permits</span>);<br>&#125;<br><br><span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">NonfairSync</span> <span class="hljs-keyword">extends</span> <span class="hljs-title class_">Sync</span> &#123;<br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-type">long</span> <span class="hljs-variable">serialVersionUID</span> <span class="hljs-operator">=</span> -<span class="hljs-number">2694183684443567898L</span>;<br><br>    NonfairSync(<span class="hljs-type">int</span> <span class="hljs-keyword">permits</span>) &#123;<br>        <span class="hljs-comment">// 这里实际是设置 AQS 的 state 变量的值</span><br>        <span class="hljs-built_in">super</span>(<span class="hljs-keyword">permits</span>);<br>    &#125;<br><br>    <span class="hljs-comment">// 这里是重写了获取非共享锁的逻辑</span><br>    <span class="hljs-keyword">protected</span> <span class="hljs-type">int</span> <span class="hljs-title function_">tryAcquireShared</span><span class="hljs-params">(<span class="hljs-type">int</span> acquires)</span> &#123;<br>        <span class="hljs-keyword">return</span> nonfairTryAcquireShared(acquires);<br>    &#125;<br>&#125;<br><br><span class="hljs-keyword">abstract</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">Sync</span> <span class="hljs-keyword">extends</span> <span class="hljs-title class_">AbstractQueuedSynchronizer</span> &#123;<br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-type">long</span> <span class="hljs-variable">serialVersionUID</span> <span class="hljs-operator">=</span> <span class="hljs-number">1192457210091910933L</span>;<br><br>    Sync(<span class="hljs-type">int</span> <span class="hljs-keyword">permits</span>) &#123;<br>        setState(<span class="hljs-keyword">permits</span>);<br>    &#125;<br><br>    <span class="hljs-keyword">final</span> <span class="hljs-type">int</span> <span class="hljs-title function_">getPermits</span><span class="hljs-params">()</span> &#123;<br>        <span class="hljs-keyword">return</span> getState();<br>    &#125;<br><br>    <span class="hljs-comment">//  获取非共享锁实际就是获取 AQS 中变量的值，然后减去此次需要获取到  permits 数量， 如果这个数量&lt;0 代表获取令牌失败</span><br>    <span class="hljs-comment">//  否则就是设置令牌成功，同时将剩余令牌数量使用 cas 方式更新的 state 变量中</span><br>    <span class="hljs-keyword">final</span> <span class="hljs-type">int</span> <span class="hljs-title function_">nonfairTryAcquireShared</span><span class="hljs-params">(<span class="hljs-type">int</span> acquires)</span> &#123;<br>        <span class="hljs-keyword">for</span> (;;) &#123;<br>            <span class="hljs-type">int</span> <span class="hljs-variable">available</span> <span class="hljs-operator">=</span> getState();<br>            <span class="hljs-type">int</span> <span class="hljs-variable">remaining</span> <span class="hljs-operator">=</span> available - acquires;<br>            <span class="hljs-keyword">if</span> (remaining &lt; <span class="hljs-number">0</span> ||<br>                compareAndSetState(available, remaining))<br>                <span class="hljs-keyword">return</span> remaining;<br>        &#125;<br>    &#125;<br><br>    <span class="hljs-comment">//  该方法是释放令牌的逻辑，和获取锁是类似的，也是先获取 AQS 中 state 变量的值，然后加上释放的令牌数量</span><br>    <span class="hljs-comment">//  然后再使用 cas 方式更新 state 变量的值即可</span><br>    <span class="hljs-keyword">protected</span> <span class="hljs-keyword">final</span> <span class="hljs-type">boolean</span> <span class="hljs-title function_">tryReleaseShared</span><span class="hljs-params">(<span class="hljs-type">int</span> releases)</span> &#123;<br>        <span class="hljs-keyword">for</span> (;;) &#123;<br>            <span class="hljs-type">int</span> <span class="hljs-variable">current</span> <span class="hljs-operator">=</span> getState();<br>            <span class="hljs-type">int</span> <span class="hljs-variable">next</span> <span class="hljs-operator">=</span> current + releases;<br>            <span class="hljs-keyword">if</span> (next &lt; current) <span class="hljs-comment">// overflow</span><br>                <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Error</span>(<span class="hljs-string">&quot;Maximum permit count exceeded&quot;</span>);<br>            <span class="hljs-keyword">if</span> (compareAndSetState(current, next))<br>                <span class="hljs-keyword">return</span> <span class="hljs-literal">true</span>;<br>        &#125;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>


]]></content>
      <categories>
        <category>java</category>
        <category>多线程</category>
      </categories>
      <tags>
        <tag>java</tag>
        <tag>多线程</tag>
      </tags>
  </entry>
  <entry>
    <title>ThreadLocal的使用和原理</title>
    <url>/2023/15c7fbcaf228/index.html</url>
    <content><![CDATA[<span class="hide_first_mermaid">
<div class="mermaid-wrap"><pre class="mermaid-src" hidden>
  flowchart LR
    A
  </pre></div>
</span>

<h2 id="ThreadLocal的使用场景"><a href="#ThreadLocal的使用场景" class="headerlink" title="ThreadLocal的使用场景"></a>ThreadLocal的使用场景</h2><ul>
<li>解决线程安全问题，因为 <code>ThreadLocal</code> 内部维护了一个 <code>ThreadLocalMap</code>，每个线程都可以将自己的变量存储到 <code>ThreadLocalMap</code> 中，从而实现了线程间的变量共享。</li>
<li>解决资源共享问题</li>
<li>解决线程间数据传递问题</li>
</ul>
<h2 id="2-ThreadLocal的使用"><a href="#2-ThreadLocal的使用" class="headerlink" title="2. ThreadLocal的使用"></a>2. ThreadLocal的使用</h2><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">ThreadLocalDemo</span> &#123;<br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> ThreadLocal&lt;Integer&gt; threadLocal = <span class="hljs-keyword">new</span> <span class="hljs-title class_">ThreadLocal</span>&lt;&gt;();<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">main</span><span class="hljs-params">(String[] args)</span> &#123;<br>        <span class="hljs-keyword">new</span> <span class="hljs-title class_">Thread</span>(() -&gt; &#123;<br>            threadLocal.set(<span class="hljs-number">100</span>);<br>            System.out.println(threadLocal.get());<br>        &#125;).start();<br><br>        <span class="hljs-keyword">new</span> <span class="hljs-title class_">Thread</span>(() -&gt; &#123;<br>            threadLocal.set(<span class="hljs-number">200</span>);<br>            System.out.println(threadLocal.get());<br>        &#125;).start();<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<p>上述代码中定义的 <code>threadLocal</code> 是一个静态变量，并且两个线程都使用了，但是不同线程中使用的 <code>threadLocal</code> 进行 <code>set</code> 和 <code>get</code> 的都是对应线程的值，其他线程操作 <code>threadLocal</code> 不会影响到当前线程的值。</p>
<h2 id="ThreadLocal-相关对象的关联关系"><a href="#ThreadLocal-相关对象的关联关系" class="headerlink" title="ThreadLocal 相关对象的关联关系"></a>ThreadLocal 相关对象的关联关系</h2><pre class="mermaid">graph LR
    subgraph A[" Thread 对象"]
        subgraph B1["ThreadLocalMap 成员变量"]
            subgraph C["Entry 对象"]
                C1["key 引用"]
                C2["value 引用"]
            end
        end
    end
    B["ThreadLocal 对象"]
    C1-. "虚引用" .->B
    C2--"强引用"-->D
    D["value 值"]</pre>

<p>根据上述对象关联图我们再来看看 <code>ThreadLocal</code> 源码</p>
<h2 id="ThreadLocal-源码"><a href="#ThreadLocal-源码" class="headerlink" title="ThreadLocal 源码"></a>ThreadLocal 源码</h2><p>从上面 ThreadLocal 的使用示例中可以看到，涉及的几个重要的方法如下</p>
<ol>
<li>new ThreadLocal()</li>
<li>ThreadLocal.set()</li>
<li>ThreadLocal.get()</li>
<li>ThreadLocal.remove()</li>
</ol>
<p>先看创建 set 方法</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">ThreadLocal</span>&lt;T&gt; &#123;<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">set</span><span class="hljs-params">(T value)</span> &#123;<br>        <span class="hljs-type">Thread</span> <span class="hljs-variable">t</span> <span class="hljs-operator">=</span> Thread.currentThread();<br>        <span class="hljs-type">ThreadLocalMap</span> <span class="hljs-variable">map</span> <span class="hljs-operator">=</span> getMap(t);<br>        <span class="hljs-keyword">if</span> (map != <span class="hljs-literal">null</span>) &#123;<br>            map.set(<span class="hljs-built_in">this</span>, value);<br>        &#125; <span class="hljs-keyword">else</span> &#123;<br>            <span class="hljs-comment">// 第一次使用是走这个分支，创建 ThreadLocalMap 对象</span><br>            createMap(t, value);<br>        &#125;<br>    &#125;<br><br>    <span class="hljs-comment">//  第一个参数就是 Thread 对象，这里是将 创建的 ThreadLocalMap 对象赋值给 Thread 对象中的 threadLocals 属性</span><br>    <span class="hljs-keyword">void</span> <span class="hljs-title function_">createMap</span><span class="hljs-params">(Thread t, T firstValue)</span> &#123;<br>        t.threadLocals = <span class="hljs-keyword">new</span> <span class="hljs-title class_">ThreadLocalMap</span>(<span class="hljs-built_in">this</span>, firstValue);<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>
<p>接下来再看看创建 ThreadLocalMap 方法, 从上面对象关联图可知，ThreadLocalMap 类是 ThreadLocal 的一个内部类，Entry 又是一个 ThreadLocalMap 的一个内部类</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">ThreadLocal</span>&lt;T&gt; &#123;<br><br>    <span class="hljs-keyword">static</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">ThreadLocalMap</span> &#123;<br>        <span class="hljs-keyword">private</span> Entry[] table;<br><br>        <span class="hljs-comment">/*</span><br><span class="hljs-comment">         Entry 有两个属性，一个用来存储 key, 一个用来存储 value,这个key就是 ThreadLocal 对象，</span><br><span class="hljs-comment">         value 就是 ThreadLocal 对象中存储的值, 从下面看 Entry 中只有一个 value 属性，</span><br><span class="hljs-comment">         是因为 Entry 继承了 WeakReference， 所以在父类中最终还有一个 private T referent;  属性，key 就是存在这个属性中</span><br><span class="hljs-comment">        */</span><br>        <span class="hljs-keyword">static</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">Entry</span> <span class="hljs-keyword">extends</span> <span class="hljs-title class_">WeakReference</span>&lt;ThreadLocal&lt;?&gt;&gt; &#123;<br>            Object value;<br><br>            Entry(ThreadLocal&lt;?&gt; k, Object v) &#123;<br>                <span class="hljs-built_in">super</span>(k);<br>                value = v;<br>            &#125;<br>        &#125;<br><br><br>        <span class="hljs-comment">/* 创建 ThreadLocalMap 相关的逻辑， 其实就是将 key 和 value 组合起来变成一个 Entry 对象,</span><br><span class="hljs-comment">         然后将这个 Entry 对象放到一个 ThreadLocalMap 的数组中</span><br><span class="hljs-comment">        */</span><br>        ThreadLocalMap(ThreadLocal&lt;?&gt; firstKey, Object firstValue) &#123;<br>            table = <span class="hljs-keyword">new</span> <span class="hljs-title class_">Entry</span>[INITIAL_CAPACITY];<br>            <span class="hljs-type">int</span> <span class="hljs-variable">i</span> <span class="hljs-operator">=</span> firstKey.threadLocalHashCode &amp; (INITIAL_CAPACITY - <span class="hljs-number">1</span>);<br>            table[i] = <span class="hljs-keyword">new</span> <span class="hljs-title class_">Entry</span>(firstKey, firstValue);<br>            size = <span class="hljs-number">1</span>;<br>            setThreshold(INITIAL_CAPACITY);<br>        &#125;<br>    <br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<h2 id="ThreadLocal-的内存泄露问题"><a href="#ThreadLocal-的内存泄露问题" class="headerlink" title="ThreadLocal 的内存泄露问题"></a>ThreadLocal 的内存泄露问题</h2><p>执行完下面的代码之后有堆中的 ThreadLocal 有几个引用呢？</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">private</span> ThreadLocal&lt;Integer&gt; tl = <span class="hljs-keyword">new</span> <span class="hljs-title class_">ThreadLocal</span>&lt;&gt;();<br>tl.set(<span class="hljs-number">1</span>);<br></code></pre></td></tr></table></figure>
<p>表面看只有栈中的 <code>tl</code>变量指向了 <code>ThreadLocal</code> 实例，其实不是的，从上面对象关联图中我们知道，<code>Entry</code> 对象的 <code>key</code> 就是一个 <code>ThreadLocal</code> 实例的引用，所以即使栈中的引用断开了，但是 <code>Entry</code> 中的引用还在，这也是为什么 <code>Entry</code> 中的 <code>key</code> 要使用弱引用的原因。</p>
<p>即使 <code>Entry</code> 中的 <code>key</code> 使用了弱引用，可以让 <code>Key</code> 被回收，但是 <code>Entry</code> 中的 <code>value</code> 是一个强引用，依然不会被回收，这个应该如何解决呢？</p>
<p>在 <code>ThreadLocal</code> 中提供了 <code>remove()</code> 方法，所以需要我们在使用完 <code>ThreadLocal</code> 之后调用 <code>remove()</code> 方法</p>
<h2 id="线程池使用-ThreadLocal-的坑"><a href="#线程池使用-ThreadLocal-的坑" class="headerlink" title="线程池使用 ThreadLocal 的坑"></a>线程池使用 ThreadLocal 的坑</h2><p>如果在线程中使用了 <code>ThreadLocal</code> 那么一定要在使用完之后（也就是在此次处理不再需要从 <code>ThreadLocal</code> 中调用 <code>get()</code> 方法获取值了）调用 <code>remove()</code> 方法， 因为线程池中线程是复用的，如果没有调用 <code>remove()</code> 方法当下次复用到之前的线程时从 <code>ThreadLocal</code> 中获取的就可能是之前设置的值</p>
]]></content>
      <categories>
        <category>java</category>
        <category>多线程</category>
      </categories>
      <tags>
        <tag>java</tag>
        <tag>多线程</tag>
      </tags>
  </entry>
  <entry>
    <title>synchronized的作用和实现原理</title>
    <url>/2023/138a9fb1d57d/index.html</url>
    <content><![CDATA[<p>参考 《Java并发编程深度解析与实战》</p>
<h2 id="什么是线程不安全"><a href="#什么是线程不安全" class="headerlink" title="什么是线程不安全"></a>什么是线程不安全</h2><p>就是当多个线程同时访问某个方法时，这个方法无法按照我们预期的行为来执行，那么我们认为这个方法是线程不安全的</p>
<h2 id="导致线程不安全的原因"><a href="#导致线程不安全的原因" class="headerlink" title="导致线程不安全的原因"></a>导致线程不安全的原因</h2><ol>
<li>原子性</li>
<li>有序性</li>
<li>可见性</li>
</ol>
<h2 id="原子性问题"><a href="#原子性问题" class="headerlink" title="原子性问题"></a>原子性问题</h2><blockquote>
<p>原子性是指一个或多个指令操作在CPU执行过程中不允许被中断</p>
</blockquote>
<h3 id="原子性问题的本质"><a href="#原子性问题的本质" class="headerlink" title="原子性问题的本质"></a>原子性问题的本质</h3><ol>
<li><code>cpu</code> 时间片切换</li>
<li>执行指令的原子性，即线程运行的程序或指令是否具备原子性</li>
</ol>
<h3 id="原子性问题的解决"><a href="#原子性问题的解决" class="headerlink" title="原子性问题的解决"></a>原子性问题的解决</h3><ol>
<li>不允许当前非原子指令在执行过程中被中断。比如保证 <code>i++</code> 操作在执行过程中不存在上下文切换</li>
<li>多线程并行执行导致的原子性问题可以通过互斥条件来实现串行执行</li>
</ol>
<h2 id="同步锁之-synchronized"><a href="#同步锁之-synchronized" class="headerlink" title="同步锁之 synchronized"></a>同步锁之 synchronized</h2><h3 id="synchronized-关键字的作用"><a href="#synchronized-关键字的作用" class="headerlink" title="synchronized 关键字的作用"></a>synchronized 关键字的作用</h3><ol>
<li>作用在方法级别：锁定的是当前 <code>class</code></li>
<li>作用在代码块：锁定某个对象实例</li>
</ol>
<h3 id="synchronized-需要实现的功能"><a href="#synchronized-需要实现的功能" class="headerlink" title="synchronized 需要实现的功能"></a>synchronized 需要实现的功能</h3><ol>
<li><code>synchronized</code> 是同步排他锁，要想达到排他，就需要多个线程抢夺同一个资源</li>
<li>同一个时刻只能有一个线程抢到锁，其他没有抢到锁的线程就需要等待</li>
<li>处于等待状态的线程不能一直占用 <code>cpu</code>, 所以需要阻塞起来，并且释放 <code>cpu</code> 资源</li>
<li>如果有多个线程被阻塞，就还需要一个容器来存储这些被阻塞的线程，当获得锁的线程执行完任务并释放锁之后，就需要从容器中唤醒阻塞的线程，被唤醒的线程再次尝试获取锁</li>
</ol>
<h3 id="synchronized-之锁资源"><a href="#synchronized-之锁资源" class="headerlink" title="synchronized 之锁资源"></a>synchronized 之锁资源</h3><p>前面讲到 <code>synchronized</code> 可以修饰方法和修饰代码块</p>
<ol>
<li>修饰方法时，对应的资源就是当前 <code>class</code> （静态方法）或当前对象实例</li>
<li>修饰代码块时，是 <code>synchronized(lock)</code> 中的 <code>lock</code> 就是资源</li>
</ol>
<p><strong>需要注意的是如果多个线程访问多个锁资源，就不存在竞争关系，也就达不到互斥的效果</strong></p>
<h3 id="抢到锁的标记之-Mark-Word"><a href="#抢到锁的标记之-Mark-Word" class="headerlink" title="抢到锁的标记之 Mark Word"></a>抢到锁的标记之 Mark Word</h3><p>前面讲到 <strong>同一个时刻只能有一个线程抢到锁</strong>， 对于抢到锁的线程就需要进行标记到底是那个线程抢到了锁，这个标记就是存储在<strong>对象头</strong>中的</p>
<h4 id="java-对象存储结构"><a href="#java-对象存储结构" class="headerlink" title="java 对象存储结构"></a>java 对象存储结构</h4><p><code>java</code> 对象存储结构分为三个部分</p>
<ol>
<li>对象头</li>
<li>实例数据</li>
<li>对其填充</li>
</ol>
<p>比如创建一个对象 <code>Object lock = new Object();</code>， 在堆中 <code>lock</code> 实例最终的存储结构如下：</p>
<p><img src="https://blog-1308475102.cos.ap-guangzhou.myqcloud.com/picture/202311282221986.webp" alt="对象存储结构"></p>
<h4 id="对象头"><a href="#对象头" class="headerlink" title="对象头"></a>对象头</h4><p>对象头包含三个部分</p>
<ol>
<li><code>Mark Word</code></li>
<li><code>Klass Pointer</code></li>
<li><code>Lenght</code></li>
</ol>
<p><strong>Mark Word</strong><br><code>Mark Word</code> 记录了与对象和锁相关的信息，当这个对象作为锁对象来实现<code>synchronized</code> 的同步操作时，锁标记和相关信息都是存储在 <code>Mark Word</code>中的</p>
<div class="tabs" id="mark-word存储结构"><ul class="nav-tabs"><button type="button" class="tab  active" data-href="mark-word存储结构-1">32位Mark</button><button type="button" class="tab " data-href="mark-word存储结构-2">64位Mark Word存储结构</button></ul><div class="tab-contents"><div class="tab-item-content active" id="mark-word存储结构-1"><p><img src="https://blog-1308475102.cos.ap-guangzhou.myqcloud.com/picture/202311282227201.webp" alt="32位Mark Word存储结构"></p></div><div class="tab-item-content" id="mark-word存储结构-2"><p><img src="https://blog-1308475102.cos.ap-guangzhou.myqcloud.com/picture/202311282225221.webp" alt="64位Mark Word存储结构"></p></div></div><div class="tab-to-top"><button type="button" aria-label="scroll to top"><i class="fas fa-arrow-up"></i></button></div></div>

<ol>
<li>不管在32位还是64位系统中，<code>Mark Word</code>中都会包含<code>GC</code>分代年龄、锁状态标记</li>
<li>锁状态的字段，它包含五种状态分别是无锁、偏向锁、轻量级锁、重量级锁、GC标记</li>
<li>无锁和偏向锁的 锁标记位都是 01，但是有一个单独的 <code>bit</code> 存储 <strong>是否偏向锁</strong></li>
</ol>
<p><strong>Klass Pointer</strong><br><code>Klass Pointer</code> 记录了对象所属的 <code>class</code> 信息，当对象被创建时，会将 <code>Klass Pointer</code> 指向 <code>class</code> 信息</p>
<p><strong>Length</strong><br>表示数组长度，只有构建对象数组时才会有数组长度属性</p>
<h4 id="内存中对象存储示例"><a href="#内存中对象存储示例" class="headerlink" title="内存中对象存储示例"></a>内存中对象存储示例</h4><p>比如下面代码</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">MarkWordExample</span> &#123;<br>    <span class="hljs-keyword">private</span> <span class="hljs-type">int</span> id;<br>    <span class="hljs-keyword">private</span> String name;<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">main</span><span class="hljs-params">(String[] args)</span> &#123;<br>        <span class="hljs-type">MarkWordExample</span> <span class="hljs-variable">example</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">MarkWordExample</span>();<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>
<p>在 <code>jvm</code> 运行时内存中结构如下图所示<br><img src="https://blog-1308475102.cos.ap-guangzhou.myqcloud.com/picture/202311282236608.webp" alt="对象结构示例"></p>
<h2 id="锁升级过程"><a href="#锁升级过程" class="headerlink" title="锁升级过程"></a>锁升级过程</h2><p><img src="https://blog-1308475102.cos.ap-guangzhou.myqcloud.com/picture/202311282252724.webp" alt="锁升级"></p>
<div class="tabs" id="锁升级过程"><ul class="nav-tabs"><button type="button" class="tab  active" data-href="锁升级过程-1">偏向锁</button><button type="button" class="tab " data-href="锁升级过程-2">偏向锁的批量重偏向</button><button type="button" class="tab " data-href="锁升级过程-3">轻量级锁</button><button type="button" class="tab " data-href="锁升级过程-4">重量级锁</button></ul><div class="tab-contents"><div class="tab-item-content active" id="锁升级过程-1"><p>偏向锁其实可以认为是在没有多线程竞争的情况下访问<code>synchronized</code>修饰的代码块的加锁场景，也就是在单线程执行的情况下</p>
<p>线程在没有线程竞争的情况下去访问synchronized同步代码块时，会尝试先通过偏向锁来抢占访问资格，这个抢占过程是基于CAS来完成的，如果抢占锁成功，则直接修改对象头中的锁标记。其中，偏向锁标记为1，锁标记为01，以及存储当前获得锁的线程ID。而偏向的意思就是，如果线程X获得了偏向锁，那么当线程X后续再访问这个同步方法时，只需要判断对象头中的线程ID和线程X是否相等即可。如果相等，就不需要再次去抢占锁，直接获得访问资格即可</p></div><div class="tab-item-content" id="锁升级过程-2"><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">BiasedLock</span> &#123;<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">main</span><span class="hljs-params">(String[] args)</span> &#123;<br>        List&lt;BiasedLock&gt; locks = <span class="hljs-keyword">new</span> <span class="hljs-title class_">ArrayList</span>&lt;&gt;();<br>        <span class="hljs-type">BiasedLock</span> <span class="hljs-variable">lock</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">BiasedLock</span>();<br>        <span class="hljs-type">Thread</span> <span class="hljs-variable">thread</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Thread</span>(() -&gt; &#123;<br>            <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> <span class="hljs-variable">i</span> <span class="hljs-operator">=</span> <span class="hljs-number">0</span>; i &lt; <span class="hljs-number">100</span>; i++) &#123;<br>                <span class="hljs-type">BiasedLock</span> <span class="hljs-variable">lock</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">BiasedLock</span>();<br>                <span class="hljs-keyword">synchronized</span> (lock) &#123;<br>                    locks.add(lock);<br>                &#125;<br>            &#125;<br>        &#125;);<br>        thread.start();<br>        thead.join();<br><br>        <span class="hljs-type">Thread</span> <span class="hljs-variable">thread2</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Thread</span>(() -&gt; &#123;<br>            <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> <span class="hljs-variable">i</span> <span class="hljs-operator">=</span> <span class="hljs-number">0</span>; i &lt; <span class="hljs-number">20</span>; i++) &#123;<br>                <span class="hljs-type">BiasedLock</span> <span class="hljs-variable">lock2</span> <span class="hljs-operator">=</span> locks.get(i);<br>                <span class="hljs-keyword">synchronized</span> (lock2) &#123;<br>                    <br>                &#125;<br>            &#125;<br>        &#125;);<br>    &#125;<br>&#125;<br>上面代码创建了两个线程，第一个线程创建了 <span class="hljs-number">100</span>个锁对象，第一个线程执行完成之后，第二个线程获取 <span class="hljs-number">100</span>个锁对象中的前面<span class="hljs-number">20</span>个进行加锁，这时会触发这些锁对象的升级过程，会从偏向锁升级到轻量级锁， 但是当执行到<span class="hljs-number">20</span>循环后，这个锁对象又会变成偏向锁<br><br>在`JVM`中，以`class`（这里指 `BiasedLock`）为单位，为每个`class`维护了一个偏向锁撤销的计数器，当这个`class`的对象发生偏向撤销操作时，计数器会进行累加，当累加的值达到重偏向的阈值时，`JVM`会认为这个`class`的偏向锁有问题，需要重新偏向<br><br>偏向锁撤销并批量重偏向的触发阈值可以通过`XX:BiasedLockingBulkRebiasThreshold = <span class="hljs-number">20</span>`来配置，默认是<span class="hljs-number">20</span><br></code></pre></td></tr></table></figure></div><div class="tab-item-content" id="锁升级过程-3"><p>轻量级锁就是没有抢占到锁的线程，进行一定次数的重试（自旋）。比如线程第一次没抢到锁则重试几次，如果在重试的过程中抢占到了锁，那么这个线程就不需要阻塞，这种实现方式我们称为自旋锁</p>
<p><img src="https://blog-1308475102.cos.ap-guangzhou.myqcloud.com/picture/202311282243489.webp" alt="轻量级锁"></p>
<p>获得轻量级锁的过程中可能需要不断自旋操作，这也是有代价的，所以也不可能一直自旋下去</p>
<p>在<code>JDK 1.6</code>中默认的自旋次数是10次，我们可以通过<code>-XX:PreBlockSpin</code>参数来调整自旋次数。同时开发者在<code>JDK 1.6</code>中还对自旋锁做了优化，引入了自适应自旋锁，自适应自旋锁的自旋次数不是固定的，而是根据前一次在同一个锁上的自旋次数及锁持有者的状态来决定的。如果在同一个锁对象上，通过自旋等待成功获得过锁，并且持有锁的线程正在运行中，那么<code>JVM</code>会认为此次自旋也有很大的机会获得锁，因此会将这个线程的自旋时间相对延长。反之，如果在一个锁对象中，通过自旋锁获得锁很少成功，那么<code>JVM</code>会缩短自旋次数</p></div><div class="tab-item-content" id="锁升级过程-4"><p>轻量级锁能够通过一定次数的重试让没有获得锁的线程有可能抢占到锁资源，但是轻量级锁只有在获得锁的线程持有锁的时间较短的情况下才能起到提升同步锁性能的效果，如果没抢占到锁资源的线程通过一定次数的自旋后，发现仍然没有获得锁，就只能阻塞等待了，所以最终会升级到重量级锁，通过系统层面的互斥量来抢占锁资源</p></div></div><div class="tab-to-top"><button type="button" aria-label="scroll to top"><i class="fas fa-arrow-up"></i></button></div></div>

<h2 id="CAS原理"><a href="#CAS原理" class="headerlink" title="CAS原理"></a>CAS原理</h2><p>前面介绍 <code>synchronized</code> 的 轻量级锁就是通过 <code>cas</code> 实现的，<code>cas</code> 是一种无锁算法，它是通过 CPU 指令实现的，<code>cas</code> 指令是一条 CPU 指令，它包含三个操作数，分别是内存地址、预期的值、更新的值，当且仅当内存地址的值与预期的值相同时，将该值更新为更新值，否则不做任何操作。</p>
<p><img src="https://blog-1308475102.cos.ap-guangzhou.myqcloud.com/picture/202311282249030.webp" alt="cas操作过程"></p>
]]></content>
      <categories>
        <category>java</category>
        <category>多线程</category>
      </categories>
      <tags>
        <tag>java</tag>
        <tag>多线程</tag>
      </tags>
  </entry>
  <entry>
    <title>volatile解决可见性和有序性</title>
    <url>/2023/d41d7b790ebc/index.html</url>
    <content><![CDATA[<span class="hide_first_mermaid">
<div class="mermaid-wrap"><pre class="mermaid-src" hidden>
  flowchart LR
    A
  </pre></div>
</span>

<h2 id="什么是可见性"><a href="#什么是可见性" class="headerlink" title="什么是可见性"></a>什么是可见性</h2><blockquote>
<p>如果一个线程对一个共享变量进行了修改，而其他线程不能及时地读取修改之后的值，那么我们认为在多线程环境下该共享变量存在可见性问题</p>
</blockquote>
<h2 id="可见性问题的根源"><a href="#可见性问题的根源" class="headerlink" title="可见性问题的根源"></a>可见性问题的根源</h2><p><code>cpu</code> 的速度远远高于内存，但是<code>cpu</code> 执行的指令和数据都是来源于内存，所以在 <code>cpu</code> 在等待内存的数据时，一直处于空闲状态，这个过程很显然会导致<code>CPU</code>资源的浪费，为了解决这个问题，开发者在硬件设备、操作系统及编译器层面做了很多优化</p>
<ul>
<li>在<code>CPU</code>层面增加了寄存器，来保存一些关键变量和临时数据，还增加了<code>CPU</code>高速缓存，以减少<code>CPU</code>和内存的I&#x2F;O等待时间</li>
<li>在操作系统层面引入了进程和线程，在当前进程或线程处于阻塞状态时，<code>CPU</code>会把自己的时间片分配给其他线程或进程使用，从而减少<code>CPU</code>的空闲时间，最大化地提升<code>CPU</code>的使用率</li>
<li>在编译器层面增加指令优化，减少与内存的交互次数</li>
</ul>
<p>上述优化都是为了提升 <code>cpu</code> 利用率，但是这些优化也会导致可见性问题</p>
<h2 id="cpu-缓存"><a href="#cpu-缓存" class="headerlink" title="cpu 缓存"></a>cpu 缓存</h2><h3 id="cpu-缓存伪共享问题"><a href="#cpu-缓存伪共享问题" class="headerlink" title="cpu 缓存伪共享问题"></a>cpu 缓存伪共享问题</h3><ol>
<li><code>cpu</code> 的缓存是以缓存行为单位进行缓存的，在 32 位和 64 位架构中 <code>cpu</code> 缓存行的大小都是 64 字节</li>
<li>假设有两个 字段 <code>int x = 0, int y =0</code> 现在有两个线程分别修改 <code>x</code> 和 修改 <code>y</code>，假设这两个线程是在不同的 <code>cpu</code> 执行，那么每个 <code>cpu</code> 都会加载对应的变量到自己的 <code>cpu</code> 缓存中，但是 <code>x</code> 和 <code>y</code> 加起来才 8 个字节，所以这两个变量一定是在同一个缓存行中， 当线程1（修改 <code>x</code> 的线程）修改了<code>x</code> 后，因为缓存一致性原理，导致第二个 <code>cpu</code> 中的 <code>x</code> 需要失效，间接导致 第二个 <code>cpu</code> 中的 <code>y</code> 的缓存也失效了，这就是缓存伪共享问题</li>
<li>在 <code>java</code> 中可以对字段或类添加 <code>@Contented</code> 注解让每个字段独自占用一个缓存行</li>
</ol>
<h3 id="cpu-高速缓存"><a href="#cpu-高速缓存" class="headerlink" title="cpu 高速缓存"></a>cpu 高速缓存</h3><p>现代 <code>cpu</code> 一般都有三级缓存</p>
<ol>
<li>1，2 级缓存是在 <code>cpu</code> 内部</li>
<li>三级缓存是所有 <code>cpu</code> 共享</li>
<li>一级缓存包含 <code>L1D</code>（数据缓存）和 <code>L1I</code>(指令缓存)</li>
</ol>
<h3 id="缓存一致性的解决方案"><a href="#缓存一致性的解决方案" class="headerlink" title="缓存一致性的解决方案"></a>缓存一致性的解决方案</h3><ol>
<li>所谓的缓存一致性是指不同的线程加载同一个共享变量到不同的 <code>cpu</code> 进行修改，这就会导致不同 <code>cpu</code> 缓存中相同变量的缓存值内容不同</li>
<li>为了解决缓存一致性问题 在 <code>cpu</code> 层面引入了 <strong>总线锁</strong> 和 <strong>缓存锁机制</strong></li>
</ol>
<blockquote>
<p>为了解决缓存一致性问题，开发者在 CPU 层面引入了总线锁和缓存锁机制。</p>
<p>在了解锁之前，我们先介绍一下总线。所谓的总线，就是 CPU 与内存、输入&#x2F;输出设备传专递信息的公共通道（也叫前端总线），当CPU访问内存进行数据交互时，必须经过总线来传输，那么什么是总线锁呢？</p>
<p>简单来说，总线锁就是在总线上声明一个 L0ck# 信号，这个信号能够确保共享内存只有当前 CPU 可以访问，其他的处理器请求会被阻塞，这就使得同一时刻只有一个处理能够访问共享内存，从而解决了缓存不一致的问题。但是这种做法产生的代价是，CPU 的利用率直线下降，很显然这是无法让人接受的，于是从 P6 系列的处理器开始增加了缓存锁的机制。</p>
<p>缓存锁指的是，如果当前 CPU 访问的数据已经缓存在其他 CPU 的高速缓存中，那么 CPU 不会在总线上发出 Lock# 信号，而是采用<strong>缓存一致性协议</strong>来保证多个 CPU 的缓存一致性。</p>
<p>CPU 最终用哪种锁来解决缓存一致性问题，取决于当前 CPU 是否支持缓存锁，如果不支持，就会采用总线锁。还有一种情况是，当前操作的数据不能被缓存在处理器内部，或者操作的数据跨多个缓存行时，也会使用总线锁。</p>
</blockquote>
<h3 id="缓存一致性协议"><a href="#缓存一致性协议" class="headerlink" title="缓存一致性协议"></a>缓存一致性协议</h3><ol>
<li>缓存锁通过缓存一致性协议来保证缓存的一致性，那么什么是缓存一致性协议呢</li>
<li>不同的 <code>CPU</code> 类型支持的缓存一致性协议也有区别，比如<code>MSI、MESI、MOSI、MESIF</code>协议等，比较常见的是MESI（<code>Modified Exclusive Shared Or Invalid</code>）协议</li>
</ol>
<h4 id="MESI-缓存一致性协议定义的-cpu-缓存行的四种状态"><a href="#MESI-缓存一致性协议定义的-cpu-缓存行的四种状态" class="headerlink" title="MESI 缓存一致性协议定义的 cpu 缓存行的四种状态"></a>MESI 缓存一致性协议定义的 cpu 缓存行的四种状态</h4><ol>
<li><code>M (Modified)</code>: 只有当前 <code>cpu</code> 含有数据的缓存，并且缓存数据已经被修改了，和主内存数据不一致 </li>
<li><code>E (Exclusive)</code>: 只有当前 <code>cpu</code> 含有数据的缓存, 缓存还没有被修改，和主内存数据一致</li>
<li><code>S (Shared)</code>: 多个 <code>cpu</code> 都含有同一份数据的缓存，所有 <code>cpu</code> 上的缓存都没有被修改</li>
<li><code>I (Invalid)</code>: 缓存已失效</li>
</ol>
<h4 id="MESI-协议针对缓存行的事件监听"><a href="#MESI-协议针对缓存行的事件监听" class="headerlink" title="MESI 协议针对缓存行的事件监听"></a>MESI 协议针对缓存行的事件监听</h4><p>随着 <code>cpu</code> 对缓存行的操作，缓存行会发生状态转移，也就是从一种状态到另外一种状态，所以 <code>MESI</code> 协议对缓存行的不同状态有不同的事件监听</p>
<ol>
<li><code>M</code> 状态的监听： 如果一个缓存行处于 <code>M</code> 状态，则必须监听所有试图读取该缓存行对应的主内存地址的操作，如果监听到有这类操作的发生，则必须在该操作执行之前把缓存行中的数据写回主内存</li>
<li><code>S</code> 状态的监听： 如果一个缓存行处于 <code>S</code> 状态，那么它必须要监听使该缓存行状态设置为 <code>Invalid</code> 或者对缓存行执行 <code>Exclusive</code> 操作的请求，如果存在，则必须要把当前缓存行状态设置为 <code>Invalid</code></li>
<li><code>E</code> 状态的监听： 如果一个缓存行处于 <code>E</code> 状态，那么它必须要监听其他试图读取该缓存行对应的主内存地址的操作，一旦有这种操作，那么该缓存行需要设置为 <code>Shared</code></li>
</ol>
<h4 id="缓存行事件监听的实现原理"><a href="#缓存行事件监听的实现原理" class="headerlink" title="缓存行事件监听的实现原理"></a>缓存行事件监听的实现原理</h4><ol>
<li>监听过程是基于 <code>CPU</code> 中的 <code>Snoopy</code> 嗅探协议来完成的，该协议要求每个 <code>CPU</code> 缓存都可以监听到总线上的数据事件并做出相应的反应</li>
<li>所有 <code>CPU</code> 都会监听地址总线上的事件，当某个处理器发出请求时，其他 <code>CPU</code> 会监听到地址总线的请求，根据当前缓存行的状态及监听的请求类型对缓存行状态进行更新</li>
</ol>
<h4 id="MESI-状态变化示例"><a href="#MESI-状态变化示例" class="headerlink" title="MESI 状态变化示例"></a>MESI 状态变化示例</h4><p>参考 《Java并发编程深度解析与实战》– 谭峰 3.2 节（深度理解可见性问题的本质）</p>
<h2 id="volatile-实现原理"><a href="#volatile-实现原理" class="headerlink" title="volatile 实现原理"></a>volatile 实现原理</h2><ol>
<li><code>volatile</code> 可以解决内存可见性问题就是基于<strong>缓存锁&#x2F;总线锁</strong>的方式达到的一致性</li>
<li>总线锁和缓存锁通过 <code>Lock#</code> 信号触发，如果当前<code>CPU</code>支持缓存锁，则不会在总线上声明<code>Lock#</code>信号，而是基于缓存一致性协议来保证缓存的一致性。如果<code>CPU</code>不支持缓存锁，则会在总线上声明<code>Lock#</code>信号锁定总线，从而保证同一时刻只允许一个<code>CPU</code>对共享内存的读写操作</li>
</ol>
<h2 id="内存屏障-解决cpu层面的指令重排"><a href="#内存屏障-解决cpu层面的指令重排" class="headerlink" title="内存屏障-解决cpu层面的指令重排"></a>内存屏障-解决cpu层面的指令重排</h2><blockquote>
<p>CPU 本身只是一个工具，它主要用于接收和执行指令，并不清楚什么时候应该优化，什么时候不应该优化，因此 CPU 设计者们提供了一个内存屏障指令，开发者可以在合适的位置插入内存屏障指令，相当于告诉 CPU指令之间的关系，避免 CPU 内存系统重排序问题的发生。</p>
</blockquote>
<h3 id="什么是指令重排"><a href="#什么是指令重排" class="headerlink" title="什么是指令重排"></a>什么是指令重排</h3><p>指令重排序是指编译器或<code>CPU</code>为了优化程序的执行性能而对指令进行重新排序的一种手段，重排序会带来可见性问题，所以在多线程开发中必须要关注并规避重排序，<code>java</code> 从源代码到运行会经过两个阶段的重排</p>
<ol>
<li>编译器重排序，就是在编译过程中，编译器根据上下文分析对指令进行重排序，目的是减少<code>CPU</code>和内存的交互，重排序之后尽可能保证<code>CPU</code>从寄存器或缓存行中读取数据</li>
<li>处理器重排序，处理器重排序分为两个部分</li>
<li>并行指令集重排序，这是处理器优化的一种，处理器可以改变指令的执行顺序</li>
<li>内存系统重排序，这是处理器引入<code>Store Buffer</code>缓冲区延时写入产生的指令执行顺序不一致的问题</li>
</ol>
<h3 id="指令重排需要遵循-as-if-serial语义"><a href="#指令重排需要遵循-as-if-serial语义" class="headerlink" title="指令重排需要遵循 as-if-serial语义"></a>指令重排需要遵循 as-if-serial语义</h3><p><code>as-if-serial</code> 表示所有的程序指令都可以因为优化而被重排序，但是在优化的过程中必须要保证是在单线程环境下，重排序之后的运行结果和程序代码本身预期的执行结果一致，<code>Java</code> 编译器、<code>CPU</code> 指令重排序都需要保证在单线程环境下的 <code>as-if-serial</code> 语义是正确的</p>
<p><code>as-if-serial</code>语义允许重排序，<code>CPU</code>层面的指令优化依然存在。在单线程中，这些优化并不会影响整体的执行结果，在多线程中，重排序会带来可见性问题</p>
<h3 id="重谈-MESI-过程"><a href="#重谈-MESI-过程" class="headerlink" title="重谈 MESI 过程"></a>重谈 MESI 过程</h3><p>假设存在一个 <code>S</code> 状态的缓存行（就是说 <code>CPU0</code> 和 <code>CPU1</code> 共享同一个缓存行），如果 <code>CPU0</code> 对这个缓存进行修改，那么 <code>CPU0</code> 需要发送一个 <code>Invalidate</code> 消息到 <code>CPU1</code>，在等待 <code>CPU1</code> 返回<code>Acknowledgement</code> 消息之前，<code>CPU0</code> 一直处于空闲状态</p>
<pre class="mermaid">sequenceDiagram
    participant A as CPU0
    participant B as CPU1
    A->>A: "加载变量, 缓存行状态S"
    B->>B: "加载变量，缓存行状态S"
    A->>A: "修改共享s变量"
    A->>B: "Invalidate"
    A->>A: "空闲等待"
    B->>A: "Acknowledgement"</pre>

<h4 id="Store-Buffers"><a href="#Store-Buffers" class="headerlink" title="Store Buffers"></a>Store Buffers</h4><ol>
<li><p>从上图可以看到，当一个 <code>cpu</code> 从发出 <code>Invalidate</code> 指令到收到 <code>Acknowledgement</code> 指令之间，这个 <code>cpu</code> 是需要阻塞的</p>
</li>
<li><p><code>Store Buffer</code> 是一个存储器，它存储着那些被修改过的缓存行。当一个 <code>cpu</code> 要修改一个缓存行，它会将这个缓存行的副本放到 <code>Store Buffer</code> 中，然后发出一个 <code>Invalidate</code> 指令，让其他 <code>cpu</code> 知道这个缓存行</p>
</li>
</ol>
<p><img src="https://blog-1308475102.cos.ap-guangzhou.myqcloud.com/picture/202311292320073.webp" alt="Store Buffers"></p>
<p>在 <code>CPU</code> 中引入<code>Store Buffers</code>的设计后，<code>CPU0</code>会先发送一个<code>Invalidate</code>消息给其他包含该缓存行的<code>CPU1</code>，并把当前修改的数据写入<code>Store Buffers</code>中，然后继续执行后续的指令。等收到<code>CPU1</code>的<code>Acknowledgement</code>消息后，<code>CPU0</code>再把<code>Store Buffers</code>移到缓存行中, 因为加入了 <code>Store Buffers</code>，就会导致内存可见性问题</p>
<h4 id="使用-Store-Forwarding-优化-Store-Buffers-问题"><a href="#使用-Store-Forwarding-优化-Store-Buffers-问题" class="headerlink" title="使用 Store Forwarding 优化 Store Buffers 问题"></a>使用 Store Forwarding 优化 Store Buffers 问题</h4><ol>
<li><p><code>Store Buffers</code> 之所以存在问题是因为，在 其他 <code>cpu</code> 返回 <code>Acknowledgement</code> 之前，数据是存放在 <code>Store Buffer</code> 中的，<code>cpu</code> 缓存行中并没有最新的数据，<code>cpu</code> 执行指令时就会用到缓存行中的旧数据</p>
</li>
<li><p><code>Store Forwarding</code> 是指每个 <code>CPU</code> 在加载数据之前，会先引用当前 <code>CPU</code> 的 <code>Store Buffers</code>，也就是说支持将 <code>CPU</code> 存入<code>Store Buffers</code> 的数据传递给后续的加载操作，而不需要经过 <code>Cache</code></p>
</li>
</ol>
<p><img src="https://blog-1308475102.cos.ap-guangzhou.myqcloud.com/picture/202311292327246.webp" alt="Store Forwarding"></p>
<h4 id="使用-Invalidate-Queues-优化-Store-Buffers-问题"><a href="#使用-Invalidate-Queues-优化-Store-Buffers-问题" class="headerlink" title="使用 Invalidate Queues 优化 Store Buffers 问题"></a>使用 Invalidate Queues 优化 Store Buffers 问题</h4><ol>
<li><p>前面讲到的 <code>Store Forwarding</code> 针对的时发出 <code>Invalidate</code> 指令的 <code>cpu</code>，而接下来要讲解的 <code>Invalidate Queues</code> 则是针对接收到 <code>Invalidate</code> 指令并且要响应 <code>Acknowledgement</code> 指令的 <code>cpu</code>， 具体原因如下</p>
</li>
<li><p><code>Store Buffers</code>本身的存储容量是有限的，在当前<code>CPU</code>的所有写入操作都存在缓存未命中的情况时，就会导致<code>Store Buffers</code>很容易被填充满。被填满之后，必须要等到<code>CPU</code>返回<code>Invalidate Acknowledge</code>消息，<code>Store Buffers</code>中对应的指令才能被清理，而这个过程<code>CPU</code>必须要等待，无论该<code>CPU</code>中后续指令是否存在缓存未命中的情况</p>
</li>
<li><p>如果收到<code>Invalidate</code>消息的<code>CPU</code>此时处于繁忙状态，那么会导致<code>Invalidate Acknowledge</code>消息返回延迟</p>
</li>
<li><p>增加一个<code>Invalidate Queues</code>，用于存储让缓存行失效的消息。也就是说，<code>CPU</code>收到<code>Invalidate</code>消息时，把让该缓存行失效的消息放入<code>Invalidate Queues</code>，然后同步返回一个<code>Invalidate Acknowledge</code>消息。这样就大大缩短了响应的时间</p>
</li>
</ol>
<p><img src="https://blog-1308475102.cos.ap-guangzhou.myqcloud.com/picture/202311292336827.webp" alt="Invalidate Queues"></p>
<h3 id="内存屏障指令"><a href="#内存屏障指令" class="headerlink" title="内存屏障指令"></a>内存屏障指令</h3><ol>
<li>读屏障指令(Ifence)</li>
<li>将 <code>Invalidate Queues</code>中的指令立即处理，并且强制读取<code>CPU</code>的缓存行, 执行<code>lfence</code>指令之后的读操作不会被重排序到执行<code>lfence</code>指令之前，这意味着其他<code>CPU</code>暴露出来的缓存行状态对当前<code>CPU</code>可见</li>
<li>写屏障指令(sfence)</li>
<li>它会把<code>Store Buffers</code>中的修改刷新到本地缓存中，使得其他<code>CPU</code>能够看到这些修改，而且在执行<code>sfence</code>指令之后的写操作不会被重排序到执行<code>sfence</code>指令之前，这意味着执行<code>sfence</code>指令之前的写操作一定要全局可见（内存可见性及禁止重排序）</li>
<li>读写屏障指令(mfence）</li>
<li>相当于<code>lfence</code>和<code>sfence</code>的混合体，保证<code>mfence</code>指令执行前后的读写操作的顺序，同时要求执行<code>mfence</code>指令之后的写操作的结果全局可见，执行<code>mfence</code>指令之前的写操作结果全局可见</li>
</ol>
<h3 id="JVM-内存屏障指令"><a href="#JVM-内存屏障指令" class="headerlink" title="JVM 内存屏障指令"></a>JVM 内存屏障指令</h3><p>前面讲到的内存屏障指令是 <code>cpu</code> 可以执行的指令，但是这些指令是需要应用来调用的，同样的应用也需要提供相应的指令给开发者调用，对应到 <code>JVM</code> 中，提供了如下几种指令</p>
<ol>
<li>loadload</li>
<li>storestore</li>
<li>loadstore</li>
<li>storeload</li>
</ol>
<p><img src="https://blog-1308475102.cos.ap-guangzhou.myqcloud.com/picture/202311292348303.webp" alt="内存屏障"></p>
]]></content>
      <categories>
        <category>java</category>
        <category>多线程</category>
      </categories>
      <tags>
        <tag>java</tag>
        <tag>多线程</tag>
      </tags>
  </entry>
  <entry>
    <title>多线程上下文切换包含哪些内容</title>
    <url>/2023/c51368ba5d47/index.html</url>
    <content><![CDATA[<h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><p><code>cpu</code> 不知道什么是进程和线程，<code>cpu</code> 只知道两件事情</p>
<ol>
<li>从内存中取出指令</li>
<li>执行指令</li>
<li>一直重复上面两个步骤，所以 <strong>取指令</strong>-&gt; <strong>执行指令</strong> 就是 <code>cpu</code> 所能做的所有事情</li>
</ol>
<h2 id="cpu-获取指令执行的流程"><a href="#cpu-获取指令执行的流程" class="headerlink" title="cpu 获取指令执行的流程"></a>cpu 获取指令执行的流程</h2><ol>
<li>编写源代码，源代码中都会有 main 函数，这个 main 函数就是执行入口</li>
<li>程序启动后，会将 main 函数的第一条机器指令放入 pc 寄存器中（程序计数器）</li>
<li>pc 寄存器的地址默认是自动 +1， 初始地址就是 main 函数的指令地址</li>
<li>当遇到 if else 等语句时程序会根据计算结果或者指令中指定的跳转地址来动态改变 pc 寄存器中的值</li>
</ol>
<h2 id="进程和线程的区别"><a href="#进程和线程的区别" class="headerlink" title="进程和线程的区别"></a>进程和线程的区别</h2><ol>
<li>进程是操作系统分配资源的最小单位</li>
<li>线程是调用的基本单位，线程之间共享进程资源<ol>
<li>线程之间共享了哪些资源<ol>
<li>堆区</li>
<li>代码区</li>
<li>数据区：存放的就是全局变量</li>
</ol>
</li>
</ol>
</li>
</ol>
<h2 id="什么是线程上下文"><a href="#什么是线程上下文" class="headerlink" title="什么是线程上下文"></a>什么是线程上下文</h2><ol>
<li>线程上下文就是指线程在某个时刻的状态，或者说某个时刻的数据，这个数据是线程私有的才能称为是线程的上下文</li>
<li>要看线程上下文包含哪些信息，就需要看哪些数据都是线程私有的，这些线程私有的数据整体就是线程上下文</li>
</ol>
<h3 id="线程的私有数据有哪些"><a href="#线程的私有数据有哪些" class="headerlink" title="线程的私有数据有哪些"></a>线程的私有数据有哪些</h3><p>线程本质就是函数的执行，函数执行的信息都是放在栈帧中，栈帧主要包含如下内容</p>
<ol>
<li>当前函数的返回值</li>
<li>调用其他函数的参数（寄存器不够时参数是会放在栈帧中的）</li>
<li>函数使用的局部变量</li>
<li>函数使用的寄存器信息</li>
<li>程序计数器是线程私有的</li>
<li>栈指针（该线程栈区的栈顶位置）</li>
<li>执行函数时所使用的寄存器也是线程私有的</li>
</ol>
<p>所有上面这些信息包含在一起就是线程上下文</p>
<h2 id="常用的几种寄存器"><a href="#常用的几种寄存器" class="headerlink" title="常用的几种寄存器"></a>常用的几种寄存器</h2><ol>
<li>栈寄存器<ol>
<li>函数运行时都有一个栈帧，对于栈来说重要的信息之一就是栈顶，栈寄存器就是保存栈顶信息的</li>
</ol>
</li>
<li>指令寄存器（pc寄存器）</li>
<li>状态寄存器，线程是运行在内核态还是用户态的信息保存在状态寄存器中</li>
</ol>
]]></content>
      <categories>
        <category>java</category>
        <category>多线程</category>
      </categories>
      <tags>
        <tag>java</tag>
        <tag>多线程</tag>
      </tags>
  </entry>
  <entry>
    <title>线程池实现原理</title>
    <url>/2023/63b6d22fbd8f/index.html</url>
    <content><![CDATA[<span class="hide_first_mermaid">
<div class="mermaid-wrap"><pre class="mermaid-src" hidden>
  flowchart LR
    A
  </pre></div>
</span>

<h2 id="1-为什么要使用线程池"><a href="#1-为什么要使用线程池" class="headerlink" title="1. 为什么要使用线程池"></a>1. 为什么要使用线程池</h2><ol>
<li>线程的创建和销毁都需要消耗资源（cpu，内存）</li>
<li>使用线程池可以预先创建好线程，开始使用时速度更快<span id="more"></span></li>
</ol>
<h2 id="2-线程池的设计思想"><a href="#2-线程池的设计思想" class="headerlink" title="2. 线程池的设计思想"></a>2. 线程池的设计思想</h2><h3 id="2-1-如何做到线程在执行完任务后不回收"><a href="#2-1-如何做到线程在执行完任务后不回收" class="headerlink" title="2.1. 如何做到线程在执行完任务后不回收"></a>2.1. 如何做到线程在执行完任务后不回收</h3><p>线程池就是要缓存线程，当一个任务执行完成之后，执行任务的线程可以不用回收，当有新的任务来到时复用当前线程来执行，平时线程执行的代码如下</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">Thread</span> &#123;<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">run</span><span class="hljs-params">()</span> &#123;<br>    <br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>
<p>线程的创建和销毁实际是底层的操作系统来实现的，当启动一个线程执行任务时，<code>run()</code> 方法就会执行，当 <code>run()</code> 执行完成之后当前的线程会自动销毁<br>那么如何做到线程不被销毁呢？其实只要让 <code>run()</code> 方法一直在执行就好了，比如加个 <code>while</code> 循环</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">Thread</span> &#123;<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">run</span><span class="hljs-params">()</span> &#123;<br>        <span class="hljs-keyword">while</span>(<span class="hljs-literal">true</span>) &#123;<br><br>        &#125;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<h3 id="2-2-线程空闲时也消耗资源怎么处理"><a href="#2-2-线程空闲时也消耗资源怎么处理" class="headerlink" title="2.2. 线程空闲时也消耗资源怎么处理"></a>2.2. 线程空闲时也消耗资源怎么处理</h3><ol>
<li>为了做到将线程不回收，一直执行，所以使用将任务代码放在 <code>while(true)</code> 里面，但是这样做会出现即使没有任务时，这个线程也会一直消耗 cpu 资源，所以需要进一步优化</li>
<li>优化方式也很简单，就是没有任务时就阻塞当前线程就好了，当有任务来到时再唤起当前线程进行执行</li>
<li>所以线程池可以认为是一个 <code>生产者--消费者</code> 模型，提交任务的线程就是生产者，线程池自身就是消费者，线程池从阻塞队列中不断获取任务，有任务就执行，没有任务就阻塞自己</li>
</ol>
<h2 id="3-线程池创建方式"><a href="#3-线程池创建方式" class="headerlink" title="3. 线程池创建方式"></a>3. 线程池创建方式</h2><h3 id="3-1-Executors-类"><a href="#3-1-Executors-类" class="headerlink" title="3.1. Executors 类"></a>3.1. Executors 类</h3><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">Executors</span> &#123;<br>    <span class="hljs-comment">// 创建固定数量线程的线程池</span><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> ExecutorService <span class="hljs-title function_">newFixedThreadPool</span><span class="hljs-params">(<span class="hljs-type">int</span> nThreads)</span> &#123;<br>        <span class="hljs-keyword">return</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">ThreadPoolExecutor</span>(nThreads, nThreads,<br>                                      <span class="hljs-number">0L</span>, TimeUnit.MILLISECONDS,<br>                                      <span class="hljs-keyword">new</span> <span class="hljs-title class_">LinkedBlockingQueue</span>&lt;Runnable&gt;());<br>    &#125;<br><br>    <span class="hljs-comment">// 创建单个线程的线程池 </span><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> ExecutorService <span class="hljs-title function_">newSingleThreadExecutor</span><span class="hljs-params">(ThreadFactory threadFactory)</span> &#123;<br>        <span class="hljs-keyword">return</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">FinalizableDelegatedExecutorService</span><br>            (<span class="hljs-keyword">new</span> <span class="hljs-title class_">ThreadPoolExecutor</span>(<span class="hljs-number">1</span>, <span class="hljs-number">1</span>,<br>                                    <span class="hljs-number">0L</span>, TimeUnit.MILLISECONDS,<br>                                    <span class="hljs-keyword">new</span> <span class="hljs-title class_">LinkedBlockingQueue</span>&lt;Runnable&gt;(),<br>                                    threadFactory));<br>    &#125;<br>    ……<br>&#125;<br></code></pre></td></tr></table></figure>

<h3 id="3-2-ThreadPoolExecutor"><a href="#3-2-ThreadPoolExecutor" class="headerlink" title="3.2. ThreadPoolExecutor"></a>3.2. ThreadPoolExecutor</h3><p>通过上面 <code>Executors</code> 类创建的线程池可以看到最终还是 <code>new ThreadPoolExecutor</code>, 但是直接使用 <code>Executors</code> 创建的线程池存在的问题时，要么线程数量没有限制，要么是队列的大小没有限制，所以一般情况下都不推荐使用 <code>Executors</code> 来直接创建线程池，而是手动通过 <code>ThreadPoolExecutor</code> 来创建， 比如下面这种方式</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-type">ThreadPoolExecutor</span> <span class="hljs-variable">threadPoolExecutor</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">ThreadPoolExecutor</span>(<br>        <span class="hljs-number">5</span>,  <span class="hljs-comment">// 线程池核心线程数</span><br>        <span class="hljs-number">10</span>,  <span class="hljs-comment">// 线程池最大线程数</span><br>        <span class="hljs-number">1000</span>,  <span class="hljs-comment">// 线程池中超过corePoolSize数目的空闲线程最大存活时间</span><br>        TimeUnit.MILLISECONDS,  <span class="hljs-comment">// 时间单位，毫秒</span><br>        <span class="hljs-keyword">new</span> <span class="hljs-title class_">LinkedBlockingQueue</span>&lt;&gt;(<span class="hljs-number">50</span>),  <span class="hljs-comment">// 工作线程等待队列</span><br>        Executors.defaultThreadFactory(),  <span class="hljs-comment">// 自定义线程工厂</span><br>        <span class="hljs-keyword">new</span> <span class="hljs-title class_">ThreadPoolExecutor</span>.AbortPolicy());  <span class="hljs-comment">// 线程池满时的拒绝策略</span><br></code></pre></td></tr></table></figure>

<h2 id="线程池的生命周期"><a href="#线程池的生命周期" class="headerlink" title="线程池的生命周期"></a>线程池的生命周期</h2><pre class="mermaid">flowchart LR
    A[running]-- "执行shutdown()方法"--> B[Shutdown]
    A-- "执行shutdonwNow() 方法"--> C[Stop]
    B--"任务列表为空，所有任务都执行完成"-->D[TIDYING]
    B--"shutdownNow()"-->C
    C--所有任务都执行完成-->D
    D--"执行 terminated() 方法"-->E[TERMINATED]</pre>
<ul>
<li>RUNNING：能接受新任务，并处理阻塞队列中的任务</li>
<li>SHUTDOWN：不接受新任务，但是可以处理阻塞队列中的任务</li>
<li>STOP：所有任务都不处理（正在运行的线程准备中断，阻塞队列中的任务不再处理，新的任务不会再接收）</li>
<li>TIDYING：所有任务都终止，并且工作线程也为0，处于关闭之前的状态</li>
<li>TERMINATED：已关闭。</li>
</ul>
<h2 id="线程池的处理流程"><a href="#线程池的处理流程" class="headerlink" title="线程池的处理流程"></a>线程池的处理流程</h2><pre class="mermaid">flowchart LR
    A[提交任务] --> B{核心线程池是否已满}
    B --否--> C[创建线程执行任务]
    B --是--> D{队列是否已满}
    D --否--> E[将任务存储在队列中]
    D --是--> F{是否达到最大线程池}
    F --否--> G[创建线程执行]
    F --是--> J[执行拒绝策略]</pre>

<h3 id="线程池相关核心变量"><a href="#线程池相关核心变量" class="headerlink" title="线程池相关核心变量"></a>线程池相关核心变量</h3><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">ThreadPoolExecutor</span> <span class="hljs-keyword">extends</span> <span class="hljs-title class_">AbstractExecutorService</span> &#123;<br>    <span class="hljs-comment">// ctl 变量保存了两个信息，线程池的状态（高3位）和线程数量（低29位）</span><br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> <span class="hljs-type">AtomicInteger</span> <span class="hljs-variable">ctl</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">AtomicInteger</span>(ctlOf(RUNNING, <span class="hljs-number">0</span>));<br>    <span class="hljs-comment">// 统计线程数量的位数，这里表示使用32-3 = 29 位来表示线程的数量</span><br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-type">int</span> <span class="hljs-variable">COUNT_BITS</span> <span class="hljs-operator">=</span> Integer.SIZE - <span class="hljs-number">3</span>;<br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-type">int</span> <span class="hljs-variable">COUNT_MASK</span> <span class="hljs-operator">=</span> (<span class="hljs-number">1</span> &lt;&lt; COUNT_BITS) - <span class="hljs-number">1</span>;<br><br>    <span class="hljs-comment">// 线程池的几种状态</span><br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-type">int</span> <span class="hljs-variable">RUNNING</span>    <span class="hljs-operator">=</span> -<span class="hljs-number">1</span> &lt;&lt; COUNT_BITS;<br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-type">int</span> <span class="hljs-variable">SHUTDOWN</span>   <span class="hljs-operator">=</span>  <span class="hljs-number">0</span> &lt;&lt; COUNT_BITS;<br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-type">int</span> <span class="hljs-variable">STOP</span>       <span class="hljs-operator">=</span>  <span class="hljs-number">1</span> &lt;&lt; COUNT_BITS;<br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-type">int</span> <span class="hljs-variable">TIDYING</span>    <span class="hljs-operator">=</span>  <span class="hljs-number">2</span> &lt;&lt; COUNT_BITS;<br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-type">int</span> <span class="hljs-variable">TERMINATED</span> <span class="hljs-operator">=</span>  <span class="hljs-number">3</span> &lt;&lt; COUNT_BITS;<br>&#125;<br></code></pre></td></tr></table></figure>
<p>下面重点看一下 ctl 变量是如何通过一个变量存储两种信息的</p>
<h3 id="线程数量"><a href="#线程数量" class="headerlink" title="线程数量"></a>线程数量</h3><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-type">int</span> <span class="hljs-variable">COUNT_BITS</span> <span class="hljs-operator">=</span> Integer.SIZE - <span class="hljs-number">3</span>;<br></code></pre></td></tr></table></figure>
<ol>
<li>其中 <code>Integer.SIZE</code> &#x3D; 32, 所以 <code>COUNT_BITS</code> 就是29，也就是使用 29 位来表示线程的数量<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-type">int</span> <span class="hljs-variable">COUNT_MASK</span> <span class="hljs-operator">=</span> (<span class="hljs-number">1</span> &lt;&lt; COUNT_BITS) - <span class="hljs-number">1</span>;<br><br>     <span class="hljs-number">1</span> &lt;&lt; COUNT_BITS<br>      ​<br>      <span class="hljs-number">1</span>的<span class="hljs-number">32</span>位<span class="hljs-number">2</span>进制是<br>      <span class="hljs-number">0000</span> <span class="hljs-number">0000</span> <span class="hljs-number">0000</span> <span class="hljs-number">0000</span> <span class="hljs-number">0000</span> <span class="hljs-number">0000</span> <span class="hljs-number">0000</span> <span class="hljs-number">0001</span><br>      ​<br>      左移<span class="hljs-number">29</span>位<br>      <span class="hljs-number">0010</span> <span class="hljs-number">0000</span> <span class="hljs-number">0000</span> <span class="hljs-number">0000</span> <span class="hljs-number">0000</span> <span class="hljs-number">0000</span> <span class="hljs-number">0000</span> <span class="hljs-number">0000</span><br>      ​<br>      再进行减一<br>      <span class="hljs-number">000</span> <span class="hljs-number">11111</span> <span class="hljs-number">1111</span> <span class="hljs-number">1111</span> <span class="hljs-number">1111</span> <span class="hljs-number">1111</span> <span class="hljs-number">1111</span> <span class="hljs-number">1111</span><br>      ​<br>      所以线程池最大数目就是<br>      <span class="hljs-number">000</span> <span class="hljs-number">11111</span> <span class="hljs-number">11111111</span> <span class="hljs-number">11111111</span> <span class="hljs-number">11111111</span><br></code></pre></td></tr></table></figure></li>
<li>那怎么通过这 29 位来获取线程的数量呢，通过 <code>workderCountOf(int c)</code> 方法可以看到实际是通过位运算中的 <code>与</code> 操作来去除最高位来进行获取的</li>
</ol>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-type">int</span> <span class="hljs-title function_">workerCountOf</span><span class="hljs-params">(<span class="hljs-type">int</span> c)</span>  &#123; <br>    <span class="hljs-keyword">return</span> c &amp; COUNT_MASK; <br>&#125;<br></code></pre></td></tr></table></figure>

<h3 id="线程池的状态"><a href="#线程池的状态" class="headerlink" title="线程池的状态"></a>线程池的状态</h3><p>获取线程池状态的方法是 <code>runStateOf()</code></p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-type">int</span> <span class="hljs-title function_">runStateOf</span><span class="hljs-params">(<span class="hljs-type">int</span> c)</span>     &#123;<br>     <span class="hljs-keyword">return</span> c &amp; ~COUNT_MASK; <br>&#125;<br></code></pre></td></tr></table></figure>
<p>可以看到分成了两步</p>
<ol>
<li>对 <code>COUNT_MASK</code> 取反，前面已经知道 <code>COUNT_MASK</code> 前面三位是0，最后29位是1， 取反则是前面三位是1，最后29位是0</li>
<li><code>与</code> 操作和计算线程数量时是一样的，是为了去除后面29位数，只需要计算前面三位的数即可</li>
</ol>
<h2 id="线程池核心问题"><a href="#线程池核心问题" class="headerlink" title="线程池核心问题"></a>线程池核心问题</h2><h3 id="线程池核心线程什么时候创建"><a href="#线程池核心线程什么时候创建" class="headerlink" title="线程池核心线程什么时候创建"></a>线程池核心线程什么时候创建</h3><ol>
<li>默认情况下不会预先创建线程，所以核心线程也是在有任务来的时候才会创建</li>
<li>在 <code>ThreadPoolExecutor</code> 类中提供了两个方法</li>
</ol>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">ThreadPoolExecutor</span> <span class="hljs-keyword">extends</span> <span class="hljs-title class_">AbstractExecutorService</span> &#123;<br>    <span class="hljs-comment">// 预先启动一个核心线程</span><br>    <span class="hljs-keyword">public</span> <span class="hljs-type">boolean</span> <span class="hljs-title function_">prestartCoreThread</span><span class="hljs-params">()</span> &#123;<br>        <span class="hljs-keyword">return</span> workerCountOf(ctl.get()) &lt; corePoolSize &amp;&amp;<br>            addWorker(<span class="hljs-literal">null</span>, <span class="hljs-literal">true</span>);<br>    &#125;<br><br>    <span class="hljs-comment">// 预先启动所有核心线程</span><br>    <span class="hljs-keyword">public</span> <span class="hljs-type">int</span> <span class="hljs-title function_">prestartAllCoreThreads</span><span class="hljs-params">()</span> &#123;<br>        <span class="hljs-type">int</span> <span class="hljs-variable">n</span> <span class="hljs-operator">=</span> <span class="hljs-number">0</span>;<br>        <span class="hljs-keyword">while</span> (addWorker(<span class="hljs-literal">null</span>, <span class="hljs-literal">true</span>))<br>            ++n;<br>        <span class="hljs-keyword">return</span> n;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>
<p>通过上面两个方法可知，在创建线程池之后（提交任务之前，也就是调用 execute 之前）是可以调用这两个方法来预先创建核心线程的</p>
<h3 id="线程池的核心线程是否会被回收"><a href="#线程池的核心线程是否会被回收" class="headerlink" title="线程池的核心线程是否会被回收"></a>线程池的核心线程是否会被回收</h3><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">ThreadPoolExecutor</span> <span class="hljs-keyword">extends</span> <span class="hljs-title class_">AbstractExecutorService</span> &#123;<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">allowCoreThreadTimeOut</span><span class="hljs-params">(<span class="hljs-type">boolean</span> value)</span> &#123;<br>        <span class="hljs-keyword">if</span> (value &amp;&amp; keepAliveTime &lt;= <span class="hljs-number">0</span>)<br>            <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">IllegalArgumentException</span>(<span class="hljs-string">&quot;Core threads must have nonzero keep alive times&quot;</span>);<br>        <span class="hljs-keyword">if</span> (value != allowCoreThreadTimeOut) &#123;<br>            allowCoreThreadTimeOut = value;<br>            <span class="hljs-keyword">if</span> (value)<br>                interruptIdleWorkers();<br>        &#125;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>
<p>在 <code>ThreadPoolExecutor</code> 类中有提供 <code>allowCoreThreadTimeOut(boolean)</code> 方法来设置当达到超时时间时，核心线程也可以被回收掉</p>
<h3 id="核心线程数量和最大先吃数量动态修改"><a href="#核心线程数量和最大先吃数量动态修改" class="headerlink" title="核心线程数量和最大先吃数量动态修改"></a>核心线程数量和最大先吃数量动态修改</h3><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-comment">// 动态修改核心线程数量</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">setCorePoolSize</span><span class="hljs-params">(<span class="hljs-type">int</span> corePoolSize)</span> &#123;<br>    ……<br>&#125;<br><br><span class="hljs-comment">// 动态修改最大线程数量</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">setMaximumPoolSize</span><span class="hljs-params">(<span class="hljs-type">int</span> maximumPoolSize)</span> &#123;<br>    ……<br>&#125;<br></code></pre></td></tr></table></figure>
<p>上面有写到可以动态修改线程池的核心线程和最大线程数量，那么这么设计的原因是什么呢？说到这个问题就牵扯到另外一个问题，如何合理设置线程池的线程数量大小？对于线程池数量大小的设置问题有多种建议，比如</p>
<ol>
<li>CPU密集型，线程池大小设置为N+1， IO密集型，线程池大小设置为2N+1（N指的是cpu核心数）</li>
<li>线程数&#x3D;CPU核数 *（1+线程等待时间 &#x2F; 线程时间运行时间）</li>
</ol>
<p>对于方式2是结合了实际的业务，但是线程等待时间，线程运行时间都是需要 <code>监控</code> 线程环境才可以得知的，既然涉及到 <code>监控</code> 就说明这个值是需要动态去修改的，所以才会需要提供这样两个修改线程数量的方法</p>
<h3 id="相同的任务如何舍弃"><a href="#相同的任务如何舍弃" class="headerlink" title="相同的任务如何舍弃"></a>相同的任务如何舍弃</h3><ol>
<li>在某些场景下会有任务不断的往线程池中添加，但是对于添加的相同的任务如果之前还没有执行完成，后续添加的任务可以直接舍弃，这里涉及到的知识点是 <code>队列</code> 和 <code>对象相等的判断</code></li>
<li>如果每次添加新任务之前旧的任务已经执行完成，就不存在相同的任务舍弃这个问题，所以这里的场景是提交的任务太多，线程没有执行完成的任务保留在队列中</li>
</ol>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">MyTask</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">Runnable</span>&#123;<br>    <span class="hljs-keyword">private</span> String account;<br><br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">run</span><span class="hljs-params">()</span> &#123;<br>        doSomething();<br>    &#125;<br><br>    <span class="hljs-comment">// 重写 equals 方法，这个就是判断任务相同的关键</span><br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> <span class="hljs-type">boolean</span> <span class="hljs-title function_">equals</span><span class="hljs-params">(Object o)</span> &#123;<br>        <span class="hljs-keyword">if</span> (<span class="hljs-built_in">this</span> == o) &#123;<br>            <span class="hljs-keyword">return</span> <span class="hljs-literal">true</span>;<br>        &#125;<br>        <span class="hljs-keyword">if</span> (o == <span class="hljs-literal">null</span> || getClass() != o.getClass()) &#123;<br>            <span class="hljs-keyword">return</span> <span class="hljs-literal">false</span>;<br>        &#125;<br>        <span class="hljs-type">MyTask</span> <span class="hljs-variable">that</span> <span class="hljs-operator">=</span> (MyTask) o;<br>        <span class="hljs-comment">// 这里是用账号来判断任务是否相同</span><br>        <span class="hljs-keyword">return</span> account.equals(that.account);<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>
<ol start="3">
<li>提交任务之前先获取队列中的任务，看是否有正在排队的相同账号的任务</li>
</ol>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-comment">// 获取队列中的任务</span><br>BlockingQueue&lt;Runnable&gt; queue = threadPoolExecutor.getQueue();<br><span class="hljs-comment">// 构建当前任务</span><br><span class="hljs-type">MyTask</span> <span class="hljs-variable">task</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">MyTask</span>(<span class="hljs-string">&quot;currentTaskAccount&quot;</span>);<br><span class="hljs-keyword">if</span> (queue.size() &gt; <span class="hljs-number">0</span> &amp;&amp; queue.contains(authSymbolTask)) &#123;<br>    logger.info(<span class="hljs-string">&quot;队列中含有:&#123;&#125;的任务,当前任务被丢弃&quot;</span>, task.getAccount());<br>&#125; <span class="hljs-keyword">else</span> &#123;<br>    <span class="hljs-comment">// 队列中不包含当前账号的任务就提交当前任务</span><br>    threadPoolExecutor.execute(task);<br>&#125;<br></code></pre></td></tr></table></figure>
<h2 id="源码分析"><a href="#源码分析" class="headerlink" title="源码分析"></a>源码分析</h2><h3 id="线程池提交任务流程"><a href="#线程池提交任务流程" class="headerlink" title="线程池提交任务流程"></a>线程池提交任务流程</h3><p>下面代码是 <code>ThreadPoolExecutor</code> 类</p>
<figure class="highlight java"><figcaption><span>execute方法</span></figcaption><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">execute</span><span class="hljs-params">(Runnable command)</span> &#123;<br>    <span class="hljs-keyword">if</span> (command == <span class="hljs-literal">null</span>)<br>        <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">NullPointerException</span>();<br>    <span class="hljs-type">int</span> <span class="hljs-variable">c</span> <span class="hljs-operator">=</span> ctl.get();<br>    <span class="hljs-comment">// 线程数量小于核心线程数</span><br>    <span class="hljs-keyword">if</span> (workerCountOf(c) &lt; corePoolSize) &#123;<br>        <span class="hljs-comment">// 1. 创建线程的核心方法</span><br>        <span class="hljs-keyword">if</span> (addWorker(command, <span class="hljs-literal">true</span>))<br>            <span class="hljs-keyword">return</span>;<br>        c = ctl.get();<br>    &#125;<br>    <span class="hljs-comment">// 线程数量大于核心数量，则将任务添加到队列中</span><br>    <span class="hljs-keyword">if</span> (isRunning(c) &amp;&amp; workQueue.offer(command)) &#123;<br>        <span class="hljs-type">int</span> <span class="hljs-variable">recheck</span> <span class="hljs-operator">=</span> ctl.get();<br>        <span class="hljs-comment">// 再次检查如果线程池不是运行状态，则将添加到队列的任务移除掉，并且执行拒绝策略</span><br>        <span class="hljs-keyword">if</span> (! isRunning(recheck) &amp;&amp; remove(command))<br>            reject(command);<br>        <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (workerCountOf(recheck) == <span class="hljs-number">0</span>)<br>            addWorker(<span class="hljs-literal">null</span>, <span class="hljs-literal">false</span>);<br>    &#125;<br>    <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (!addWorker(command, <span class="hljs-literal">false</span>))<br>        reject(command);<br>&#125;<br></code></pre></td></tr></table></figure>
<p>可以看到添加线程进行执行主要是在 <code>addWorker()</code> 方法中，所以继续看 <code>addWorkder()</code></p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">private</span> <span class="hljs-type">boolean</span> <span class="hljs-title function_">addWorker</span><span class="hljs-params">(Runnable firstTask, <span class="hljs-type">boolean</span> core)</span> &#123;<br>    retry:<br>    <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> <span class="hljs-variable">c</span> <span class="hljs-operator">=</span> ctl.get();;) &#123;<br>        <span class="hljs-comment">// 线程池正在关闭或已经关闭时就不运行添加新任务</span><br>        <span class="hljs-keyword">if</span> (runStateAtLeast(c, SHUTDOWN)<br>            &amp;&amp; (runStateAtLeast(c, STOP)<br>                || firstTask != <span class="hljs-literal">null</span><br>                || workQueue.isEmpty()))<br>            <span class="hljs-keyword">return</span> <span class="hljs-literal">false</span>;<br><br>        <span class="hljs-keyword">for</span> (;;) &#123;<br>            <span class="hljs-keyword">if</span> (workerCountOf(c)<br>                &gt;= ((core ? corePoolSize : maximumPoolSize) &amp; COUNT_MASK))<br>                <span class="hljs-keyword">return</span> <span class="hljs-literal">false</span>;<br>            <span class="hljs-keyword">if</span> (compareAndIncrementWorkerCount(c))<br>                <span class="hljs-keyword">break</span> retry;<br>            c = ctl.get();  <span class="hljs-comment">// Re-read ctl</span><br>            <span class="hljs-keyword">if</span> (runStateAtLeast(c, SHUTDOWN))<br>                <span class="hljs-keyword">continue</span> retry;<br>            <span class="hljs-comment">// else CAS failed due to workerCount change; retry inner loop</span><br>        &#125;<br>    &#125;<br><br>    <span class="hljs-type">boolean</span> <span class="hljs-variable">workerStarted</span> <span class="hljs-operator">=</span> <span class="hljs-literal">false</span>;<br>    <span class="hljs-type">boolean</span> <span class="hljs-variable">workerAdded</span> <span class="hljs-operator">=</span> <span class="hljs-literal">false</span>;<br>    <span class="hljs-type">Worker</span> <span class="hljs-variable">w</span> <span class="hljs-operator">=</span> <span class="hljs-literal">null</span>;<br>    <span class="hljs-keyword">try</span> &#123;<br>        <span class="hljs-comment">// worker类包含Thread 和 Runnable Task</span><br>        w = <span class="hljs-keyword">new</span> <span class="hljs-title class_">Worker</span>(firstTask);<br>        <span class="hljs-keyword">final</span> <span class="hljs-type">Thread</span> <span class="hljs-variable">t</span> <span class="hljs-operator">=</span> w.thread;<br>        <span class="hljs-keyword">if</span> (t != <span class="hljs-literal">null</span>) &#123;<br>            <span class="hljs-keyword">final</span> <span class="hljs-type">ReentrantLock</span> <span class="hljs-variable">mainLock</span> <span class="hljs-operator">=</span> <span class="hljs-built_in">this</span>.mainLock;<br>            mainLock.lock();<br>            <span class="hljs-keyword">try</span> &#123;<br>                <span class="hljs-type">int</span> <span class="hljs-variable">c</span> <span class="hljs-operator">=</span> ctl.get();<br>                <span class="hljs-comment">// 线程池还在运行状态</span><br>                <span class="hljs-keyword">if</span> (isRunning(c) ||<br>                    (runStateLessThan(c, STOP) &amp;&amp; firstTask == <span class="hljs-literal">null</span>)) &#123;<br>                    <span class="hljs-keyword">if</span> (t.getState() != Thread.State.NEW)<br>                        <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">IllegalThreadStateException</span>();<br>                    workers.add(w);<br>                    workerAdded = <span class="hljs-literal">true</span>;<br>                    <span class="hljs-type">int</span> <span class="hljs-variable">s</span> <span class="hljs-operator">=</span> workers.size();<br>                    <span class="hljs-keyword">if</span> (s &gt; largestPoolSize)<br>                        largestPoolSize = s;<br>                &#125;<br>            &#125; <span class="hljs-keyword">finally</span> &#123;<br>                mainLock.unlock();<br>            &#125;<br>            <span class="hljs-keyword">if</span> (workerAdded) &#123;<br>                <span class="hljs-comment">// 添加成功就启动线程开始执行</span><br>                t.start();<br>                workerStarted = <span class="hljs-literal">true</span>;<br>            &#125;<br>        &#125;<br>    &#125; <span class="hljs-keyword">finally</span> &#123;<br>        <span class="hljs-keyword">if</span> (! workerStarted)<br>            addWorkerFailed(w);<br>    &#125;<br>    <span class="hljs-keyword">return</span> workerStarted;<br>&#125;<br></code></pre></td></tr></table></figure>
<p>Worker 类代码如下</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java">Worker(Runnable firstTask) &#123;<br>    setState(-<span class="hljs-number">1</span>); <span class="hljs-comment">// inhibit interrupts until runWorker</span><br>    <span class="hljs-built_in">this</span>.firstTask = firstTask;<br>    <span class="hljs-built_in">this</span>.thread = getThreadFactory().newThread(<span class="hljs-built_in">this</span>);<br>&#125;<br><br><span class="hljs-comment">/** Delegates main run loop to outer runWorker. */</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">run</span><span class="hljs-params">()</span> &#123;<br>    runWorker(<span class="hljs-built_in">this</span>);<br>&#125;<br><br><span class="hljs-keyword">final</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">runWorker</span><span class="hljs-params">(Worker w)</span> &#123;<br>    <span class="hljs-type">Thread</span> <span class="hljs-variable">wt</span> <span class="hljs-operator">=</span> Thread.currentThread();<br>    <span class="hljs-type">Runnable</span> <span class="hljs-variable">task</span> <span class="hljs-operator">=</span> w.firstTask;<br>    w.firstTask = <span class="hljs-literal">null</span>;<br>    w.unlock(); <span class="hljs-comment">// allow interrupts</span><br>    <span class="hljs-type">boolean</span> <span class="hljs-variable">completedAbruptly</span> <span class="hljs-operator">=</span> <span class="hljs-literal">true</span>;<br>    <span class="hljs-keyword">try</span> &#123;<br>        <span class="hljs-comment">// 如何保证线程不会被回收呢？就是通过一个 while 循环，不让线程退出 run 方法</span><br>        <span class="hljs-comment">// 如何保证线程在空闲时不占用cpu呢，实现逻辑就是在 getTask() 方法里面，如果没有获取到任务就阻塞当前线程</span><br>        <span class="hljs-comment">// 如何实现线程的回收呢？这个关键点就是 while 条件的判断，当条件不满足时就会退出 while 循环，当前线程执行完了，线程就会被jvm回收掉</span><br>        <span class="hljs-keyword">while</span> (task != <span class="hljs-literal">null</span> || (task = getTask()) != <span class="hljs-literal">null</span>) &#123;<br>            w.lock();<br>            <span class="hljs-keyword">if</span> ((runStateAtLeast(ctl.get(), STOP) ||<br>                    (Thread.interrupted() &amp;&amp;<br>                    runStateAtLeast(ctl.get(), STOP))) &amp;&amp;<br>                !wt.isInterrupted())<br>                wt.interrupt();<br>            <span class="hljs-keyword">try</span> &#123;<br>                beforeExecute(wt, task);<br>                <span class="hljs-keyword">try</span> &#123;<br>                    task.run();<br>                    afterExecute(task, <span class="hljs-literal">null</span>);<br>                &#125; <span class="hljs-keyword">catch</span> (Throwable ex) &#123;<br>                    afterExecute(task, ex);<br>                    <span class="hljs-keyword">throw</span> ex;<br>                &#125;<br>            &#125; <span class="hljs-keyword">finally</span> &#123;<br>                task = <span class="hljs-literal">null</span>;<br>                w.completedTasks++;<br>                w.unlock();<br>            &#125;<br>        &#125;<br>        completedAbruptly = <span class="hljs-literal">false</span>;<br>    &#125; <span class="hljs-keyword">finally</span> &#123;<br>        <span class="hljs-comment">// 线程池中线程被回收的逻辑</span><br>        processWorkerExit(w, completedAbruptly);<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>
<h3 id="线程池中的线程是如何被回收的"><a href="#线程池中的线程是如何被回收的" class="headerlink" title="线程池中的线程是如何被回收的"></a>线程池中的线程是如何被回收的</h3><pre class="mermaid">sequenceDiagram
    participant A as ThreadPoolExecutor[A]
    participant B as ThreadPoolExecutor[B]
    participant C as Worker[C]
    participant D as Worker[D]
    participant E as Worker[E]
    A->>B: execute()
    B->>C: addWorker(Runnable firstTask, boolean core)
    Note over B,C: Worker类包含Thread和Task
    C->>D: runWorker(Worker)
    Note over C,D: runWorker()中有while，保证线程的复用
    D->>E: getTask()
    Note over D,E: 获取不到任务会阻塞</pre>
<p>在上面时序图中可以看到，在 <code>Worker.runWorker()</code>  方法中会不断的循环，保证线程可以复用，同时在 <code>getTask()</code> 中做是否要退出循环的条件的判断</p>
<ol>
<li>阻塞队列中没有任务了</li>
<li>已经超过了设置的空闲时间</li>
</ol>
<p>这样就可以退出让 Thread 的run 方法流程走完，退出 run 方法之后相当于这个线程就被回收了</p>
]]></content>
      <categories>
        <category>java</category>
        <category>多线程</category>
      </categories>
      <tags>
        <tag>java</tag>
        <tag>多线程</tag>
      </tags>
  </entry>
  <entry>
    <title>解决vscode找不到Python自定义模块报错no module named</title>
    <url>/2023/05b28e23ee5b/index.html</url>
    <content><![CDATA[<p>在 <code>.vscode</code> 目录下（若没有则新建）的 <code>launch.json</code> 中添加 <code>env</code> 相关内容：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><code class="hljs python">&#123;<br>    <span class="hljs-string">&quot;version&quot;</span>: <span class="hljs-string">&quot;0.2.0&quot;</span>,<br>    <span class="hljs-string">&quot;configurations&quot;</span>: [<br>        &#123;<br>            <span class="hljs-string">&quot;name&quot;</span>: <span class="hljs-string">&quot;update_package&quot;</span>,<br>            <span class="hljs-string">&quot;type&quot;</span>: <span class="hljs-string">&quot;python&quot;</span>,<br>            <span class="hljs-string">&quot;request&quot;</span>: <span class="hljs-string">&quot;launch&quot;</span>,<br>            <span class="hljs-string">&quot;program&quot;</span>: <span class="hljs-string">&quot;timeline\\ssh\\win_command.py&quot;</span>,<br>            <span class="hljs-string">&quot;console&quot;</span>: <span class="hljs-string">&quot;integratedTerminal&quot;</span>,<br>            <span class="hljs-string">&quot;justMyCode&quot;</span>: true,<br>            <span class="hljs-comment"># 下面这部分是新增的内容</span><br>            <span class="hljs-string">&quot;env&quot;</span>: &#123;<br>                <span class="hljs-string">&quot;PYTHONPATH&quot;</span>: <span class="hljs-string">&quot;$&#123;workspaceRoot&#125;&quot;</span><br>            &#125;<br>        &#125;<br>    ]<br>&#125;<br></code></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>python</category>
      </categories>
      <tags>
        <tag>python</tag>
      </tags>
  </entry>
  <entry>
    <title>dubbo的spi机制使用案例</title>
    <url>/2023/30c57ac8b6c7/index.html</url>
    <content><![CDATA[<h2 id="dubbo为什么需要spi机制"><a href="#dubbo为什么需要spi机制" class="headerlink" title="dubbo为什么需要spi机制"></a>dubbo为什么需要spi机制</h2><p>一个好的程序是要遵循一定的设计规范比如设计模式中的开闭原则</p>
<ol>
<li><strong>对扩展开放</strong>：指的是我们系统中的模块、类、方法对它们的提供者（开发者）应该是开放的，提供者可以对系统进行扩展（新增）新的功能。</li>
<li><strong>对修改关闭</strong>：指的是系统中的模块、类、方法对它们的使用者（调用者）应该是• 关闭的。使用者使用这些功能时，不会因为提供方新增了功能而导致使用者也进行相应修改<blockquote>
<p>Apache Dubbo 是一款微服务开发框架，它提供了 RPC 通信与微服务治理两大关键能力。这意味着，使用 Dubbo 开发的微服务，将具备相互之间的远程发现与通信能力，同时利用 Dubbo 提供的丰富服务治理能力，可以实现诸如服务发现、负载均衡、流量调度等服务治理诉求。同时 Dubbo 是高度可扩展的，用户几乎可以在任意功能点去定制自己的实现，以改变框架的默认行为来满足自己的业务需求</p>
<p>对于 Apache Dubbo 来说不变的是 RPC 调用流程，微服务治理相关的概念和流程，但是在流程中每个环节的具体实现方式可以是动态改变的，Dubbo 使用微内核的架构，将具体的实现开放出来，让使用者可以根据自己的需求来选择</p>
</blockquote>
</li>
</ol>
<h3 id="微内核架构"><a href="#微内核架构" class="headerlink" title="微内核架构"></a>微内核架构</h3><p>微内核架构由两大架构模块组成：核心系统与插件模块</p>
<ol>
<li><strong>核心系统</strong>： 负责和具体业务功能无关的通用功能，例如模块加载、模块间通信等，Dubbo 内核的工作原理由四部分组成<ol>
<li>服务发现机制 <code>spi</code></li>
<li>自适应机制 <code>Adaptive</code></li>
<li>包装机制 <code>Wrapper</code></li>
<li>激活机制 <code>Activate</code><br><code>Dubbo</code> 通过上面四种机制实现了对插件的 <code>IOC</code>, <code>AOP</code> 实现了对自动生成类的动态编译 <code>Compile</code></li>
</ol>
</li>
<li><strong>插件模块</strong>： 负责实现具体的业务逻辑，在 <code>Dubbo</code> 中就是 <code>Dubbo SPI</code> 接口与实现</li>
</ol>
<h2 id="Dubbo-中的-spi-规范"><a href="#Dubbo-中的-spi-规范" class="headerlink" title="Dubbo 中的 spi 规范"></a>Dubbo 中的 spi 规范</h2><ol>
<li>接口名： 可以随意定义，但是需要被 <code>@SPI</code> 注解修饰</li>
<li>实现类名: 在接口名前面添加一个用于表示自身功能的 “标识前缀” 字符串</li>
<li>提供者配置文件路径，依次查找目录为<ol>
<li><code>META-INF/dubbo/internal</code></li>
<li><code>META-INF/dubbo</code></li>
<li><code>META-INF/services</code></li>
</ol>
</li>
<li>提供者配置文件名称：接口的全限定性类名</li>
<li>提供者配置文件内容：文件的内容为 <code>key=value</code> 形式，<code>value</code> 为该接口的实现类的全限类性类名，<code>key</code> 可以随意，但一般为该实现类的“标识前辍”（首字母小写）。一个类名占一行</li>
<li><code>提供者加载：ExtensionLoader</code> 类相当于 <code>JDK SP</code>I 中 <code>ServiceLoader</code> 类，用于加载提供者配置文件中所有的实现类，并创建相应的实例</li>
</ol>
<p>dubbo spi 机制都是生成代理类，一般使用 <code>javaassist</code> 方法生成，可以参考</p>
<ol><li><a href="/2023/d513f5c497bd/index.html" title="java输出运行过程中的代理类">java输出运行过程中的代理类</a></li></ol> 来获取代理类

<h2 id="dubbo-spi-使用案例"><a href="#dubbo-spi-使用案例" class="headerlink" title="dubbo spi 使用案例"></a>dubbo spi 使用案例</h2><figure class="highlight java"><figcaption><span>接口和对应实现类</span></figcaption><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-meta">@SPI</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">interface</span> <span class="hljs-title class_">Hello</span> &#123;<br>    String <span class="hljs-title function_">sayHello</span><span class="hljs-params">()</span>;<br>&#125;<br><br><span class="hljs-comment">//  第一个实现类</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">FirstHello</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">Hello</span>&#123;<br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> String <span class="hljs-title function_">sayHello</span><span class="hljs-params">()</span> &#123;<br>        <span class="hljs-keyword">return</span> <span class="hljs-string">&quot;first&quot;</span>;<br>    &#125;<br>&#125;<br><br><span class="hljs-comment">// 第二个实现类</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">SecondHello</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">Hello</span>&#123;<br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> String <span class="hljs-title function_">sayHello</span><span class="hljs-params">()</span> &#123;<br>        <span class="hljs-keyword">return</span> <span class="hljs-string">&quot;second&quot;</span>;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<p>添加配置文件，在 <code>resource</code> 下创建目录 <code>META-INF/services</code>, 然后创建文件</p>
<p>文件名为接口全限定性类名 ： <code>com.example.dubbodemo.spi.Hello</code> </p>
<p>文件内容如下：</p>
<figure class="highlight stylus"><table><tr><td class="code"><pre><code class="hljs stylus">first=com<span class="hljs-selector-class">.example</span><span class="hljs-selector-class">.dubbodemo</span><span class="hljs-selector-class">.spi</span><span class="hljs-selector-class">.FirstHello</span><br>second=com<span class="hljs-selector-class">.example</span><span class="hljs-selector-class">.dubbodemo</span><span class="hljs-selector-class">.spi</span>.SecondHello<br></code></pre></td></tr></table></figure>



<figure class="highlight java"><figcaption><span>代码测试</span></figcaption><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-meta">@Test</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">testSpi</span><span class="hljs-params">()</span> &#123;<br>    ExtensionLoader&lt;Hello&gt; loader = ExtensionLoader.getExtensionLoader(Hello.class);<br>    <span class="hljs-type">Hello</span> <span class="hljs-variable">firstHello</span> <span class="hljs-operator">=</span> loader.getExtension(<span class="hljs-string">&quot;first&quot;</span>);<br>    System.out.println(firstHello.sayHello()); <span class="hljs-comment">// first</span><br>    <span class="hljs-type">Hello</span> <span class="hljs-variable">secondHello</span> <span class="hljs-operator">=</span> loader.getExtension(<span class="hljs-string">&quot;second&quot;</span>);<br>    System.out.println(secondHello.sayHello()); <span class="hljs-comment">// second</span><br>&#125;<br></code></pre></td></tr></table></figure>

<h2 id="dubbo-Adaptive-机制"><a href="#dubbo-Adaptive-机制" class="headerlink" title="dubbo Adaptive 机制"></a>dubbo Adaptive 机制</h2><ol>
<li>从上面 <code>spi</code> 代码示例中可以看到当有多个实现类的时候，我们可以手动根据 <code>key</code> 来选择具体是使用哪个实现类</li>
<li><code>dubbo</code> 中是以 <code>url</code> 来贯穿整个调用路径的，所谓的 <code>adaptive</code> 机制就是根据 <code>url</code> 中的参数来自动帮我们判断应该使用哪个实现类，而不是手动指定 <code>key</code></li>
<li><code>@Adaptive</code> 可以用来修饰类和修饰方法， 如果是修饰方法，那么 <code>@Adaptive</code> 修饰的方法就必须包含 <code>url</code> 参数，如果是修饰方法，则没有要求接口中的方法必须有 <code>url</code> 参数</li>
</ol>
<p>下面示例中包含一个 打招呼的接口 <code>Hello</code>， 然后还有两种打招呼的实现，分别是 <code>FirstHello</code> 和 <code>SecondHello</code></p>
<div class="tabs" id="dubbo的adaptive机制示例"><ul class="nav-tabs"><button type="button" class="tab  active" data-href="dubbo的adaptive机制示例-1">修饰方法示例</button><button type="button" class="tab " data-href="dubbo的adaptive机制示例-2">adaptive修饰类示例</button></ul><div class="tab-contents"><div class="tab-item-content active" id="dubbo的adaptive机制示例-1"><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-comment">//  接口定义</span><br><span class="hljs-meta">@SPI</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">interface</span> <span class="hljs-title class_">Hello</span> &#123;<br>    String <span class="hljs-title function_">sayHello</span><span class="hljs-params">()</span>;<br><br>    <span class="hljs-meta">@Adaptive</span><br>    String <span class="hljs-title function_">adaptiveHello</span><span class="hljs-params">(URL url)</span>;<br>&#125;<br><br><span class="hljs-comment">//  第一个实现类</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">FirstHello</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">Hello</span> &#123;<br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> String <span class="hljs-title function_">sayHello</span><span class="hljs-params">()</span> &#123;<br>        <span class="hljs-keyword">return</span> <span class="hljs-string">&quot;first normal&quot;</span>;<br>    &#125;<br><br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> String <span class="hljs-title function_">adaptiveHello</span><span class="hljs-params">(URL url)</span> &#123;<br>        <span class="hljs-keyword">return</span> <span class="hljs-string">&quot;first adaptive&quot;</span>;<br>    &#125;<br>&#125;<br><br><span class="hljs-comment">// 第二个实现类</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">SecondHello</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">Hello</span> &#123;<br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> String <span class="hljs-title function_">sayHello</span><span class="hljs-params">()</span> &#123;<br>        <span class="hljs-keyword">return</span> <span class="hljs-string">&quot;second normal&quot;</span>;<br>    &#125;<br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> String <span class="hljs-title function_">adaptiveHello</span><span class="hljs-params">(URL url)</span> &#123;<br>        <span class="hljs-keyword">return</span> <span class="hljs-string">&quot;second adaptive&quot;</span>;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<p>添加配置文件，在 <code>resource</code> 下创建目录 <code>META-INF/services</code>, 然后创建文件</p>
<p>文件名为接口全限定性类名 ： <code>com.example.dubbodemo.spi.Hello</code> </p>
<p>文件内容如下：</p>
<figure class="highlight stylus"><table><tr><td class="code"><pre><code class="hljs stylus">first=com<span class="hljs-selector-class">.example</span><span class="hljs-selector-class">.dubbodemo</span><span class="hljs-selector-class">.spi</span><span class="hljs-selector-class">.FirstHello</span><br>second=com<span class="hljs-selector-class">.example</span><span class="hljs-selector-class">.dubbodemo</span><span class="hljs-selector-class">.spi</span>.SecondHello<br></code></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-comment">//  测试类</span><br><span class="hljs-meta">@Test</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">testAdaptive</span><span class="hljs-params">()</span> &#123;<br>    ExtensionLoader&lt;Hello&gt; loader = ExtensionLoader.getExtensionLoader(Hello.class);<br>    <span class="hljs-type">Hello</span> <span class="hljs-variable">iHello</span> <span class="hljs-operator">=</span> loader.getAdaptiveExtension();<br>    <span class="hljs-comment">// 这个相当于在url中设置了 key=hello(也就是接口名称)， value=second, 这里的second是和配置文件中的key对应的，表示</span><br>    <span class="hljs-comment">// 具体选择的是哪个实现类</span><br>    <span class="hljs-type">URL</span> <span class="hljs-variable">url</span> <span class="hljs-operator">=</span> URL.valueOf(<span class="hljs-string">&quot;http://localhost?hello=second&quot;</span>);<br>    <span class="hljs-type">String</span> <span class="hljs-variable">s</span> <span class="hljs-operator">=</span> iHello.adaptiveHello(url);<br>    System.out.println(s); <span class="hljs-comment">// 会打印 second adaptive</span><br>&#125;<br><br><span class="hljs-meta">@Test</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">testAdaptive2</span><span class="hljs-params">()</span> &#123;<br>    ExtensionLoader&lt;Hello&gt; loader = ExtensionLoader.getExtensionLoader(Hello.class);<br>    <span class="hljs-type">Hello</span> <span class="hljs-variable">iHello</span> <span class="hljs-operator">=</span> loader.getAdaptiveExtension();<br>    <span class="hljs-type">String</span> <span class="hljs-variable">s</span> <span class="hljs-operator">=</span> iHello.sayHello(); <span class="hljs-comment">// 这里会报错，因为这个方法并没有使用 @Adaptive 注解，所以无法使用 adaptive 方法来进行调用</span><br>    System.out.println(s);<br>&#125;<br></code></pre></td></tr></table></figure></div><div class="tab-item-content" id="dubbo的adaptive机制示例-2"><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-comment">//  接口定义, 其中有默认值</span><br><span class="hljs-meta">@SPI(&quot;first&quot;)</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">interface</span> <span class="hljs-title class_">Hello</span> &#123;<br>    String <span class="hljs-title function_">sayHello</span><span class="hljs-params">()</span>;<br>&#125;<br><br><span class="hljs-comment">// 第一个实现类</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">FirstHello</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">Hello</span> &#123;<br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> String <span class="hljs-title function_">sayHello</span><span class="hljs-params">()</span> &#123;<br>        <span class="hljs-keyword">return</span> <span class="hljs-string">&quot;first hello&quot;</span>;<br>    &#125;<br>&#125;<br><br><span class="hljs-comment">// 第二个实现类</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">SecondHello</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">Hello</span>&#123;<br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> String <span class="hljs-title function_">sayHello</span><span class="hljs-params">()</span> &#123;<br>        <span class="hljs-keyword">return</span> <span class="hljs-string">&quot;second hello&quot;</span>;<br>    &#125;<br>&#125;<br><br><span class="hljs-comment">// 自适应类，这个类需要使用 @Adaptive 注解修饰</span><br><span class="hljs-meta">@Adaptive</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">AdaptiveHello</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">Hello</span> &#123;<br><br>    <span class="hljs-keyword">private</span> String type;<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">setType</span><span class="hljs-params">(String type)</span> &#123;<br>        <span class="hljs-built_in">this</span>.type = type;<br>    &#125;<br><br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> String <span class="hljs-title function_">sayHello</span><span class="hljs-params">()</span> &#123;<br>        ExtensionLoader&lt;Hello&gt; loader = ExtensionLoader.getExtensionLoader(Hello.class);<br>        Hello hello;<br>        <span class="hljs-keyword">if</span> (StringUtils.hasText(type)) &#123;<br>            hello = loader.getExtension(type);<br>        &#125; <span class="hljs-keyword">else</span> &#123;<br>            <span class="hljs-comment">// 没有指定的类型，则使用默认的类型，就是接口上 @SPI 注解中的值</span><br>            hello = loader.getDefaultExtension();<br>        &#125;<br>        <span class="hljs-keyword">return</span> hello.sayHello();<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<p>添加配置文件，在 <code>resource</code> 下创建目录 <code>META-INF/services</code>, 然后创建文件</p>
<p>文件名为接口全限定性类名 ： <code>com.example.dubbodemo.spi.Hello</code> </p>
<p>文件内容如下：</p>
<figure class="highlight stylus"><table><tr><td class="code"><pre><code class="hljs stylus">first=com<span class="hljs-selector-class">.example</span><span class="hljs-selector-class">.dubbodemo</span><span class="hljs-selector-class">.spi</span><span class="hljs-selector-class">.FirstHello</span><br>second=com<span class="hljs-selector-class">.example</span><span class="hljs-selector-class">.dubbodemo</span><span class="hljs-selector-class">.spi</span><span class="hljs-selector-class">.SecondHello</span><br>adaptive=com<span class="hljs-selector-class">.example</span><span class="hljs-selector-class">.dubbodemo</span><span class="hljs-selector-class">.spi</span>.AdaptiveHello<br></code></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-comment">// 测试类</span><br><span class="hljs-meta">@Test</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">testAdaptiveClass</span><span class="hljs-params">()</span> &#123;<br>    <span class="hljs-type">Hello</span> <span class="hljs-variable">hello</span> <span class="hljs-operator">=</span> ExtensionLoader.getExtensionLoader(Hello.class).getAdaptiveExtension();<br>    System.out.println(hello.sayHello());<span class="hljs-comment">// 输出 first hello, 默认值就是 接口上的 @SPI 注解中的值</span><br><br>    ((AdaptiveHello)hello).setType(<span class="hljs-string">&quot;second&quot;</span>);<br>    System.out.println(hello.sayHello());  <span class="hljs-comment">// 输出 second hello, 因为通过 type 设置了想要的类型</span><br>&#125;<br></code></pre></td></tr></table></figure></div></div><div class="tab-to-top"><button type="button" aria-label="scroll to top"><i class="fas fa-arrow-up"></i></button></div></div>

<h2 id="dubbo-中-wrapper-机制案例"><a href="#dubbo-中-wrapper-机制案例" class="headerlink" title="dubbo 中 wrapper 机制案例"></a>dubbo 中 wrapper 机制案例</h2><h3 id="wrapper-是什么"><a href="#wrapper-是什么" class="headerlink" title="wrapper 是什么"></a>wrapper 是什么</h3><ol>
<li>作用上 <code>wrapper</code> 和 <code>aop</code> 是类似的，也是对一个类的功能进行增强，但是 <code>wrapper</code> 不是通过 <code>aop</code> 的形式实现的，而是通过一系列规范来定义一个 <code>wrapper</code> 类</li>
<li>第一点说的是 <code>wrapper</code> 类可以增强，但是没说增强的是什么？ 其实 <code>wrapper</code> 类增强的就是 <code>spi</code> 接口的实现类</li>
</ol>
<h3 id="wrapper-类规范"><a href="#wrapper-类规范" class="headerlink" title="wrapper 类规范"></a>wrapper 类规范</h3><ol>
<li>定义的 <code>wrapper</code> 类要实现 <code>spi</code> 接口</li>
<li>定义的 <code>wrapper</code> 类必须要包含 <code>spi</code> 接口的引用</li>
<li><code>wrapper</code> 类中 <code>spi</code> 接口的实现是通过仅包含一个 <code>spi</code> 接口参数的带参构造器传入的</li>
<li>在接口实现方法中所需要调用 <code>spi</code> 接口引用对象的相应方法</li>
<li><code>wrapper</code> 类名称需要以 <code>Wrapper</code> 结尾</li>
</ol>
<h3 id="wrapper-类示例"><a href="#wrapper-类示例" class="headerlink" title="wrapper 类示例"></a>wrapper 类示例</h3><p>下面是 <code>wrapper</code> 案例代码，依然使用的是前面的 <code>Hello</code> 接口和两个实现类以及对这两个实现类进行增强的两个 <code>wrapper</code> 类</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-comment">// SPI 接口定义，默认实现类是 FirstHello</span><br><span class="hljs-meta">@SPI(&quot;first&quot;)</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">interface</span> <span class="hljs-title class_">Hello</span> &#123;<br>    String <span class="hljs-title function_">sayHello</span><span class="hljs-params">()</span>;<br>&#125;<br><br><span class="hljs-comment">// 第一个实现类</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">FirstHello</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">Hello</span> &#123;<br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> String <span class="hljs-title function_">sayHello</span><span class="hljs-params">()</span> &#123;<br>        <span class="hljs-keyword">return</span> <span class="hljs-string">&quot;first hello&quot;</span>;<br>    &#125;<br>&#125;<br><br><span class="hljs-comment">// 第二个实现类</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">SecondHello</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">Hello</span>&#123;<br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> String <span class="hljs-title function_">sayHello</span><span class="hljs-params">()</span> &#123;<br>        <span class="hljs-keyword">return</span> <span class="hljs-string">&quot;second hello&quot;</span>;<br>    &#125;<br>&#125;<br><br><span class="hljs-comment">// 第一个增强类</span><br><span class="hljs-comment">// 1. 满足第一点要求，Wrapper 类需要实现 SPI 接口</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">HelloWrapper</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">Hello</span>&#123;<br><br>    <span class="hljs-comment">// 2. 满足第二点 定义的 `wrapper` 类必须要包含 `spi` 接口的引用</span><br>    <span class="hljs-keyword">private</span> Hello hello;<br><br>    <span class="hljs-comment">// 3. 满足第三点 `wrapper` 类中 `spi` 接口的实现是通过仅包含一个 `spi` 接口参数的带参构造器传入的</span><br>    <span class="hljs-keyword">public</span> <span class="hljs-title function_">HelloWrapper</span><span class="hljs-params">(Hello hello)</span> &#123;<br>        <span class="hljs-built_in">this</span>.hello = hello;<br>    &#125;<br><br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> String <span class="hljs-title function_">sayHello</span><span class="hljs-params">()</span> &#123;<br>        System.out.println(<span class="hljs-string">&quot;wrapper1的方法增强开始&quot;</span>);<br>        <span class="hljs-type">String</span> <span class="hljs-variable">result</span> <span class="hljs-operator">=</span> hello.sayHello();<br>        System.out.println(<span class="hljs-string">&quot;Wrapper1的方法增强结束&quot;</span>);<br>        <span class="hljs-keyword">return</span> result;<br>    &#125;<br>&#125;<br><br><br><span class="hljs-comment">// 第二个增强类</span><br><span class="hljs-comment">// 1. 满足第一点要求，Wrapper 类需要实现 SPI 接口</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">HelloWrapper2</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">Hello</span>&#123;<br><br>    <span class="hljs-comment">// 2. 满足第二点 定义的 `wrapper` 类必须要包含 `spi` 接口的引用</span><br>    <span class="hljs-keyword">private</span> Hello hello;<br><br>    <span class="hljs-comment">// 3. 满足第三点 `wrapper` 类中 `spi` 接口的实现是通过仅包含一个 `spi` 接口参数的带参构造器传入的</span><br>    <span class="hljs-keyword">public</span> <span class="hljs-title function_">HelloWrapper2</span><span class="hljs-params">(Hello hello)</span> &#123;<br>        <span class="hljs-built_in">this</span>.hello = hello;<br>    &#125;<br><br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> String <span class="hljs-title function_">sayHello</span><span class="hljs-params">()</span> &#123;<br>        System.out.println(<span class="hljs-string">&quot;wrapper2的方法增强开始&quot;</span>);<br>        <span class="hljs-type">String</span> <span class="hljs-variable">result</span> <span class="hljs-operator">=</span> hello.sayHello();<br>        System.out.println(<span class="hljs-string">&quot;Wrapper2的方法增强结束&quot;</span>);<br>        <span class="hljs-keyword">return</span> result;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>
<p><code>com.example.dubbodemo.spi.Hello</code> 配置文件内容</p>
<figure class="highlight stylus"><table><tr><td class="code"><pre><code class="hljs stylus">first=com<span class="hljs-selector-class">.example</span><span class="hljs-selector-class">.dubbodemo</span><span class="hljs-selector-class">.spi</span><span class="hljs-selector-class">.FirstHello</span><br>second=com<span class="hljs-selector-class">.example</span><span class="hljs-selector-class">.dubbodemo</span><span class="hljs-selector-class">.spi</span><span class="hljs-selector-class">.SecondHello</span><br>wrapper=com<span class="hljs-selector-class">.example</span><span class="hljs-selector-class">.dubbodemo</span><span class="hljs-selector-class">.spi</span><span class="hljs-selector-class">.HelloWrapper</span><br>wrapper2=com<span class="hljs-selector-class">.example</span><span class="hljs-selector-class">.dubbodemo</span><span class="hljs-selector-class">.spi</span>.HelloWrapper2<br></code></pre></td></tr></table></figure>

<p>测试类</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-meta">@Test</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">testWrapper</span><span class="hljs-params">()</span> &#123;<br>    ExtensionLoader&lt;Hello&gt; loader = ExtensionLoader.getExtensionLoader(Hello.class);<br>    <span class="hljs-type">Hello</span> <span class="hljs-variable">firstHello</span> <span class="hljs-operator">=</span> loader.getExtension(<span class="hljs-string">&quot;first&quot;</span>);<br>    System.out.println(firstHello.sayHello());<br>&#125;<br></code></pre></td></tr></table></figure>

<p>执行结果</p>
<figure class="highlight applescript"><table><tr><td class="code"><pre><code class="hljs applescript">wrapper2的方法增强开始<br>wrapper1的方法增强开始<br>Wrapper1的方法增强结束<br>Wrapper2的方法增强结束<br><span class="hljs-keyword">first</span> hello<br></code></pre></td></tr></table></figure>

<h2 id="dubbo-spi-机制源码解析"><a href="#dubbo-spi-机制源码解析" class="headerlink" title="dubbo spi 机制源码解析"></a>dubbo spi 机制源码解析</h2><p><a href="https://cn.dubbo.apache.org/zh-cn/overview/mannual/java-sdk/reference-manual/spi/description/dubbo-spi/">dubbo官网spi源码解析</a></p>
<h2 id="dubbo3-模块领域模型"><a href="#dubbo3-模块领域模型" class="headerlink" title="dubbo3 模块领域模型"></a>dubbo3 模块领域模型</h2><p>前面在讲解加载 <code>spi</code> 扩展点时用到的是如下代码</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java">ExtensionLoader&lt;Hello&gt; loader = ExtensionLoader.getExtensionLoader(Hello.class);<br></code></pre></td></tr></table></figure>
<p>但是在 <code>dubbo3</code> 中已经将 <code>ExtensionLoader.getExtensionLoader</code> 标记为 <strong>弃用</strong>，这是因为在 <code>dubbo3</code> 中增加了模块领域模型，简单理解就是 <strong>每一个扩展点都是有范围的，而之前的方式相当于所有扩展点都在一个范围中</strong></p>
<blockquote>
<p>主要有如下原因 之前dubbo 都是只有一个作用域的，通过静态类 属性共享 增加域模型是为了:</p>
<ol>
<li>让Dubbo支持多应用的部署，这块一些大企业有诉求  </li>
<li>从架构设计上，解决静态属性资源共享、清理的问题</li>
<li>分层模型将应用的管理和服务的管理分开</li>
</ol>
<p>可能比较抽象，可以具体点来看。Dubbo3中在启动时候需要启动配置中心、元数据中心，这个配置中心和元数据中心可以归应用模型来管理。Dubbo作为RPC框架又需要启动服务和引用服务，服务级别的管理就交给了这个模块模型来管理。分层次的管理方便我们理解和处理逻辑，父子级别的模型又方便了数据传递。</p>
</blockquote>
<p><a href="https://cn.dubbo.apache.org/zh-cn/blog/2022/08/03/03-%E6%A1%86%E6%9E%B6%E5%BA%94%E7%94%A8%E7%A8%8B%E5%BA%8F%E6%A8%A1%E5%9D%97%E9%A2%86%E5%9F%9F%E6%A8%A1%E5%9E%8Bmodel%E5%AF%B9%E8%B1%A1%E7%9A%84%E5%88%9D%E5%A7%8B%E5%8C%96/">官网dubbo3模块领域模型</a></p>
]]></content>
      <categories>
        <category>中间件</category>
        <category>dubbo</category>
      </categories>
      <tags>
        <tag>dubbo</tag>
      </tags>
  </entry>
  <entry>
    <title>nacos2.2.3通过插件机制支持postgres</title>
    <url>/2023/4c25a01a2388/index.html</url>
    <content><![CDATA[<h2 id="使用-nacos2-2-3-版本"><a href="#使用-nacos2-2-3-版本" class="headerlink" title="使用 nacos2.2.3 版本"></a>使用 nacos2.2.3 版本</h2><ol>
<li>从 <a href="https://github.com/alibaba/nacos/releases">github下载nacos源码</a></li>
<li>切换到 2.2.3 版本<ol>
<li><code>git checkout -b 2.2.3 2.2.3</code>， <code>nacos</code> 源码中 2.2.3 是 <code>tag</code>, 该命令是从对应 <code>tag</code> 创建分支</li>
</ol>
</li>
</ol>
<h2 id="添加-postgres-依赖"><a href="#添加-postgres-依赖" class="headerlink" title="添加 postgres 依赖"></a>添加 postgres 依赖</h2><p>在 <code>nacos</code> 的根目录下的 <code>pom.xml</code> 中添加如下内容</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><code class="hljs xml"><span class="hljs-tag">&lt;<span class="hljs-name">properties</span>&gt;</span><br>    <span class="hljs-comment">&lt;!-- 添加 postgres 版本约束 --&gt;</span><br>    <span class="hljs-tag">&lt;<span class="hljs-name">postgresql-version</span>&gt;</span>42.3.8<span class="hljs-tag">&lt;/<span class="hljs-name">postgresql-version</span>&gt;</span><br>    <span class="hljs-comment">&lt;!-- 默认是1.8，目前使用 jdk11 编译 --&gt;</span><br>    <span class="hljs-tag">&lt;<span class="hljs-name">java.version</span>&gt;</span>11<span class="hljs-tag">&lt;/<span class="hljs-name">java.version</span>&gt;</span><br><span class="hljs-tag">&lt;/<span class="hljs-name">properties</span>&gt;</span><br></code></pre></td></tr></table></figure>

<p>在 <code>config</code> 模块下的 <code>pom.xml</code> 中添加如下内容</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><code class="hljs xml"><span class="hljs-tag">&lt;<span class="hljs-name">dependencies</span>&gt;</span><br>    <span class="hljs-comment">&lt;!-- 添加 postgresql 依赖 --&gt;</span><br>    <span class="hljs-tag">&lt;<span class="hljs-name">dependency</span>&gt;</span><br>        <span class="hljs-tag">&lt;<span class="hljs-name">groupId</span>&gt;</span>org.postgresql<span class="hljs-tag">&lt;/<span class="hljs-name">groupId</span>&gt;</span><br>        <span class="hljs-tag">&lt;<span class="hljs-name">artifactId</span>&gt;</span>postgresql<span class="hljs-tag">&lt;/<span class="hljs-name">artifactId</span>&gt;</span><br>        <span class="hljs-tag">&lt;<span class="hljs-name">version</span>&gt;</span>$&#123;postgresql-version&#125;<span class="hljs-tag">&lt;/<span class="hljs-name">version</span>&gt;</span><br>    <span class="hljs-tag">&lt;/<span class="hljs-name">dependency</span>&gt;</span><br><span class="hljs-tag">&lt;/<span class="hljs-name">dependencies</span>&gt;</span><br></code></pre></td></tr></table></figure>

<h2 id="通过-spi-机制添加-postgres-相关支持"><a href="#通过-spi-机制添加-postgres-相关支持" class="headerlink" title="通过 spi 机制添加 postgres 相关支持"></a>通过 spi 机制添加 postgres 相关支持</h2><p>找到 <code>plugin</code> 模块，里面还包含一个 <code>datasource</code> 模块，目录结构如下<br>【datasource】模块的目录路径为： <code>nacos源码所在的目录位置\nacos\plugin\datasource</code></p>
<figure class="highlight stata"><table><tr><td class="code"><pre><code class="hljs stata">├─datasource<br>│  ├─src<br>│  │  ├─main<br>│  │  │  ├─java<br>│  │  │  │  └─com<br>│  │  │  │      └─alibaba<br>│  │  │  │          └─nacos<br>│  │  │  │              └─<span class="hljs-keyword">plugin</span><br>│  │  │  │                  └─datasource<br>│  │  │  │                      ├─constants<br>│  │  │  │                      ├─impl<br>│  │  │  │                      │  ├─derby<br>│  │  │  │                      │  ├─mysql<br>│  │  │  │                      │  └─postgresql【这个目录是新增的】<br>│  │  │  │                      ├─mapper<br>│  │  │  │                      └─proxy<br>│  │  │  └─resources<br>│  │  │      └─<span class="hljs-keyword">META</span>-<span class="hljs-keyword">INF</span><br>│  │  │          └─services<br><br></code></pre></td></tr></table></figure>

<p>在 <code>com.alibaba.nacos.plugin.datasource.impl</code> 包下已经有的两个实现包是 <code>mysql</code> 和 <code>derby</code>, 可以拷贝一下 <code>mysql</code> 整个包复制到 <code>mysql</code> 同级目录下并且改名为 <code>postgres</code>, 包里面的每个类的后缀改为 <code>Postgres</code>, 改完后 <code>postgres</code> 目录下的类有如下 9 个</p>
<figure class="highlight ebnf"><table><tr><td class="code"><pre><code class="hljs ebnf"><span class="hljs-attribute">ConfigInfoAggrMapperByPostgreSql</span><br><span class="hljs-attribute">ConfigInfoBetaMapperByPostgreSql</span><br><span class="hljs-attribute">ConfigInfoMapperByPostgreSql</span><br><span class="hljs-attribute">ConfigInfoTagMapperByPostgreSql</span><br><span class="hljs-attribute">ConfigTagsRelationMapperByPostgreSql</span><br><span class="hljs-attribute">GroupCapacityMapperByPostgreSql</span><br><span class="hljs-attribute">HistoryConfigInfoMapperByPostgreSql</span><br><span class="hljs-attribute">TenantCapacityMapperByPostgreSql</span><br><span class="hljs-attribute">TenantInfoMapperByPostgreSql</span><br></code></pre></td></tr></table></figure>

<p>进行上述操作后只是将 <code>mysql</code> 目录整个拷贝了一份修改目录名和类名，但是里面的内容其实没有变化，我们以 <code>ConfigInfoMapperByPostgreSql</code>（mysql 目录下） 为例，看看需要修改什么内容</p>
<figure class="highlight java"><figcaption><span>ConfigInfoMapperByPostgreSql类</span></figcaption><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">ConfigInfoAggrMapperByMySql</span> <span class="hljs-keyword">extends</span> <span class="hljs-title class_">AbstractMapper</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">ConfigInfoAggrMapper</span> &#123;<br>    <br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> String <span class="hljs-title function_">findConfigInfoAggrByPageFetchRows</span><span class="hljs-params">(<span class="hljs-type">int</span> startRow, <span class="hljs-type">int</span> pageSize)</span> &#123;<br>        <span class="hljs-keyword">return</span> <span class="hljs-string">&quot;SELECT data_id,group_id,tenant_id,datum_id,app_name,content FROM config_info_aggr WHERE data_id= ? AND &quot;</span><br>                + <span class="hljs-string">&quot;group_id= ? AND tenant_id= ? ORDER BY datum_id LIMIT &quot;</span> + startRow + <span class="hljs-string">&quot;,&quot;</span> + pageSize;  <span class="hljs-comment">// 修改点1</span><br>    &#125;<br>    <br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> String <span class="hljs-title function_">getDataSource</span><span class="hljs-params">()</span> &#123;<br>        <span class="hljs-keyword">return</span> DataSourceConstant.MYSQL;  <span class="hljs-comment">// 修改点2</span><br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<p><strong>修改点1</strong><br>这里的修改是因为 <code>mysql</code> 和 <code>postgres</code> 的分页语法不同<br><code>mysql</code> 分页：<code>LIMIT startRow,pageSize</code><br><code>postgres</code> 分页：<code>LIMIT pageSize OFFSET startRow</code></p>
<p><strong>修改点2</strong><br><code>getDataSource()</code> 方法返回的是数据源类型，<code>mysql</code> 中返回的是 <code>DataSourceConstant.MYSQL</code>，看一下 <code>DataSourceConstant</code>类代码</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">DataSourceConstant</span> &#123;<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-type">String</span> <span class="hljs-variable">MYSQL</span> <span class="hljs-operator">=</span> <span class="hljs-string">&quot;mysql&quot;</span>;<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-type">String</span> <span class="hljs-variable">DERBY</span> <span class="hljs-operator">=</span> <span class="hljs-string">&quot;derby&quot;</span>;<br>&#125;<br></code></pre></td></tr></table></figure>
<p>默认只有两个，现在新增一个 <code>postgres</code> 类型，变成</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">DataSourceConstant</span> &#123;<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-type">String</span> <span class="hljs-variable">MYSQL</span> <span class="hljs-operator">=</span> <span class="hljs-string">&quot;mysql&quot;</span>;<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-type">String</span> <span class="hljs-variable">DERBY</span> <span class="hljs-operator">=</span> <span class="hljs-string">&quot;derby&quot;</span>;<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-type">String</span> <span class="hljs-variable">POSTGRESQL</span>  <span class="hljs-operator">=</span> <span class="hljs-string">&quot;postgresql&quot;</span>;<br>&#125;<br></code></pre></td></tr></table></figure>

<p>两处都修改完成之后，<code>postgres</code> 目录下对应的 <code>ConfigInfoAggrMapperByPostgreSql</code> 类经过修改后变成如下形式</p>
<figure class="highlight java"><figcaption><span>ConfigInfoAggrMapperByPostgreSql类</span></figcaption><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">ConfigInfoAggrMapperByPostgreSql</span> <span class="hljs-keyword">extends</span> <span class="hljs-title class_">AbstractMapper</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">ConfigInfoAggrMapper</span> &#123;<br>    <br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> String <span class="hljs-title function_">findConfigInfoAggrByPageFetchRows</span><span class="hljs-params">(<span class="hljs-type">int</span> startRow, <span class="hljs-type">int</span> pageSize)</span> &#123;<br>        <span class="hljs-keyword">return</span> <span class="hljs-string">&quot;SELECT data_id,group_id,tenant_id,datum_id,app_name,content FROM config_info_aggr WHERE data_id= ? AND &quot;</span><br>                + <span class="hljs-string">&quot;group_id= ? AND tenant_id= ? ORDER BY datum_id LIMIT &quot;</span> + pageSize + <span class="hljs-string">&quot; offset &quot;</span> + startRow;<br>    &#125;<br>    <br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> String <span class="hljs-title function_">getDataSource</span><span class="hljs-params">()</span> &#123;<br>        <span class="hljs-keyword">return</span> DataSourceConstant.POSTGRESQL;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<h2 id="添加spi类配置"><a href="#添加spi类配置" class="headerlink" title="添加spi类配置"></a>添加spi类配置</h2><p>下面会列出修改后所有 <code>postgres</code> 目录下所有类代码，不过这里先将其他需要修改的配置改完</p>
<p>找到 <code>plugin/datasource</code> 模块下 <code>resources/META-INF/services</code> 目录，该目录下有一个配置文件名为 <code>com.alibaba.nacos.plugin.datasource.mapper.Mapper</code><br>将如下内容添加到这个文件中</p>
<figure class="highlight stylus"><table><tr><td class="code"><pre><code class="hljs stylus">com<span class="hljs-selector-class">.alibaba</span><span class="hljs-selector-class">.nacos</span><span class="hljs-selector-class">.plugin</span><span class="hljs-selector-class">.datasource</span><span class="hljs-selector-class">.impl</span><span class="hljs-selector-class">.postgresql</span><span class="hljs-selector-class">.ConfigInfoAggrMapperByPostgreSql</span><br>com<span class="hljs-selector-class">.alibaba</span><span class="hljs-selector-class">.nacos</span><span class="hljs-selector-class">.plugin</span><span class="hljs-selector-class">.datasource</span><span class="hljs-selector-class">.impl</span><span class="hljs-selector-class">.postgresql</span><span class="hljs-selector-class">.ConfigInfoBetaMapperByPostgreSql</span><br>com<span class="hljs-selector-class">.alibaba</span><span class="hljs-selector-class">.nacos</span><span class="hljs-selector-class">.plugin</span><span class="hljs-selector-class">.datasource</span><span class="hljs-selector-class">.impl</span><span class="hljs-selector-class">.postgresql</span><span class="hljs-selector-class">.ConfigInfoMapperByPostgreSql</span><br>com<span class="hljs-selector-class">.alibaba</span><span class="hljs-selector-class">.nacos</span><span class="hljs-selector-class">.plugin</span><span class="hljs-selector-class">.datasource</span><span class="hljs-selector-class">.impl</span><span class="hljs-selector-class">.postgresql</span><span class="hljs-selector-class">.ConfigInfoTagMapperByPostgreSql</span><br>com<span class="hljs-selector-class">.alibaba</span><span class="hljs-selector-class">.nacos</span><span class="hljs-selector-class">.plugin</span><span class="hljs-selector-class">.datasource</span><span class="hljs-selector-class">.impl</span><span class="hljs-selector-class">.postgresql</span><span class="hljs-selector-class">.ConfigTagsRelationMapperByPostgreSql</span><br>com<span class="hljs-selector-class">.alibaba</span><span class="hljs-selector-class">.nacos</span><span class="hljs-selector-class">.plugin</span><span class="hljs-selector-class">.datasource</span><span class="hljs-selector-class">.impl</span><span class="hljs-selector-class">.postgresql</span><span class="hljs-selector-class">.HistoryConfigInfoMapperByPostgreSql</span><br>com<span class="hljs-selector-class">.alibaba</span><span class="hljs-selector-class">.nacos</span><span class="hljs-selector-class">.plugin</span><span class="hljs-selector-class">.datasource</span><span class="hljs-selector-class">.impl</span><span class="hljs-selector-class">.postgresql</span><span class="hljs-selector-class">.TenantInfoMapperByPostgreSql</span><br>com<span class="hljs-selector-class">.alibaba</span><span class="hljs-selector-class">.nacos</span><span class="hljs-selector-class">.plugin</span><span class="hljs-selector-class">.datasource</span><span class="hljs-selector-class">.impl</span><span class="hljs-selector-class">.postgresql</span><span class="hljs-selector-class">.TenantCapacityMapperByPostgreSql</span><br>com<span class="hljs-selector-class">.alibaba</span><span class="hljs-selector-class">.nacos</span><span class="hljs-selector-class">.plugin</span><span class="hljs-selector-class">.datasource</span><span class="hljs-selector-class">.impl</span><span class="hljs-selector-class">.postgresql</span>.GroupCapacityMapperByPostgreSql<br></code></pre></td></tr></table></figure>

<h2 id="修改-db-配置"><a href="#修改-db-配置" class="headerlink" title="修改 db 配置"></a>修改 db 配置</h2><p>在 <code>console</code> 模块下的 <code>application.properties</code> 文件中添加如下配置</p>
<figure class="highlight properties"><table><tr><td class="code"><pre><code class="hljs properties"><span class="hljs-attr">spring.sql.init.platform</span>=<span class="hljs-string">postgresql</span><br><span class="hljs-attr">db.num</span>=<span class="hljs-string">1</span><br><span class="hljs-attr">&lt;!--</span> <span class="hljs-string">地址和用户名密码根据实际情况修改 --&gt;</span><br><span class="hljs-attr">db.url.0</span>=<span class="hljs-string">jdbc:postgresql://127.0.0.1:5432/nacos</span><br><span class="hljs-attr">db.user.0</span>=<span class="hljs-string">postgres</span><br><span class="hljs-attr">db.password.0</span>=<span class="hljs-string">postgres</span><br><span class="hljs-attr">db.pool.config.driverClassName</span>=<span class="hljs-string">org.postgresql.Driver</span><br></code></pre></td></tr></table></figure>

<h2 id="postgres-目录下所有类"><a href="#postgres-目录下所有类" class="headerlink" title="postgres 目录下所有类"></a>postgres 目录下所有类</h2><p>下面已经说明了每个类需要修改的点，下面就给出修改后所有 <code>postgres</code> 目录下所有类的内容<br>ConfigInfoAggrMapperByPostgreSql类</p>
<figure class="highlight java"><figcaption><span>ConfigInfoAggrMapperByPostgreSql类</span></figcaption><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">ConfigInfoAggrMapperByPostgreSql</span> <span class="hljs-keyword">extends</span> <span class="hljs-title class_">AbstractMapper</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">ConfigInfoAggrMapper</span> &#123;<br>    <br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> String <span class="hljs-title function_">findConfigInfoAggrByPageFetchRows</span><span class="hljs-params">(<span class="hljs-type">int</span> startRow, <span class="hljs-type">int</span> pageSize)</span> &#123;<br>        <span class="hljs-keyword">return</span> <span class="hljs-string">&quot;SELECT data_id,group_id,tenant_id,datum_id,app_name,content FROM config_info_aggr WHERE data_id= ? AND &quot;</span><br>                + <span class="hljs-string">&quot;group_id= ? AND tenant_id= ? ORDER BY datum_id LIMIT &quot;</span> + pageSize + <span class="hljs-string">&quot; offset &quot;</span> + startRow;<br>    &#125;<br>    <br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> String <span class="hljs-title function_">getDataSource</span><span class="hljs-params">()</span> &#123;<br>        <span class="hljs-keyword">return</span> DataSourceConstant.POSTGRESQL;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<p>ConfigInfoBetaMapperByPostgreSql类</p>
<figure class="highlight java"><figcaption><span>ConfigInfoBetaMapperByPostgreSql类</span></figcaption><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">ConfigInfoBetaMapperByPostgreSql</span> <span class="hljs-keyword">extends</span> <span class="hljs-title class_">AbstractMapper</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">ConfigInfoBetaMapper</span> &#123;<br><br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> String <span class="hljs-title function_">findAllConfigInfoBetaForDumpAllFetchRows</span><span class="hljs-params">(<span class="hljs-type">int</span> startRow, <span class="hljs-type">int</span> pageSize)</span> &#123;<br>        <span class="hljs-keyword">return</span> <span class="hljs-string">&quot; SELECT t.id,data_id,group_id,tenant_id,app_name,content,md5,gmt_modified,beta_ips,encrypted_data_key &quot;</span><br>                + <span class="hljs-string">&quot; FROM ( SELECT id FROM config_info_beta  ORDER BY id LIMIT &quot;</span> + pageSize + <span class="hljs-string">&quot; offset &quot;</span> + startRow + <span class="hljs-string">&quot; )&quot;</span><br>                + <span class="hljs-string">&quot;  g, config_info_beta t WHERE g.id = t.id &quot;</span>;<br>    &#125;<br><br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> String <span class="hljs-title function_">getDataSource</span><span class="hljs-params">()</span> &#123;<br>        <span class="hljs-keyword">return</span> DataSourceConstant.POSTGRESQL;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<p>ConfigInfoMapperByPostgreSql类</p>
<figure class="highlight java"><figcaption><span>ConfigInfoMapperByPostgreSql类</span></figcaption><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">ConfigInfoMapperByPostgreSql</span> <span class="hljs-keyword">extends</span> <span class="hljs-title class_">AbstractMapper</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">ConfigInfoMapper</span> &#123;<br>    <br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-type">String</span> <span class="hljs-variable">DATA_ID</span> <span class="hljs-operator">=</span> <span class="hljs-string">&quot;dataId&quot;</span>;<br>    <br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-type">String</span> <span class="hljs-variable">GROUP</span> <span class="hljs-operator">=</span> <span class="hljs-string">&quot;group&quot;</span>;<br>    <br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-type">String</span> <span class="hljs-variable">APP_NAME</span> <span class="hljs-operator">=</span> <span class="hljs-string">&quot;appName&quot;</span>;<br>    <br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-type">String</span> <span class="hljs-variable">CONTENT</span> <span class="hljs-operator">=</span> <span class="hljs-string">&quot;content&quot;</span>;<br>    <br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-type">String</span> <span class="hljs-variable">TENANT</span> <span class="hljs-operator">=</span> <span class="hljs-string">&quot;tenant&quot;</span>;<br><br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> String <span class="hljs-title function_">findConfigInfoByAppFetchRows</span><span class="hljs-params">(<span class="hljs-type">int</span> startRow, <span class="hljs-type">int</span> pageSize)</span> &#123;<br>        <span class="hljs-keyword">return</span> <span class="hljs-string">&quot;SELECT id,data_id,group_id,tenant_id,app_name,content FROM config_info&quot;</span><br>                + <span class="hljs-string">&quot; WHERE tenant_id LIKE ? AND app_name= ?&quot;</span> + <span class="hljs-string">&quot; LIMIT &quot;</span> + pageSize + <span class="hljs-string">&quot; offset &quot;</span> + startRow;<br>    &#125;<br><br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> String <span class="hljs-title function_">getTenantIdList</span><span class="hljs-params">(<span class="hljs-type">int</span> startRow, <span class="hljs-type">int</span> pageSize)</span> &#123;<br>        <span class="hljs-keyword">return</span> <span class="hljs-string">&quot;SELECT tenant_id FROM config_info WHERE tenant_id != &#x27;&quot;</span> + NamespaceUtil.getNamespaceDefaultId()<br>                + <span class="hljs-string">&quot;&#x27; GROUP BY tenant_id LIMIT &quot;</span> + pageSize + <span class="hljs-string">&quot; offset &quot;</span> + startRow;<br>    &#125;<br>    <br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> String <span class="hljs-title function_">getGroupIdList</span><span class="hljs-params">(<span class="hljs-type">int</span> startRow, <span class="hljs-type">int</span> pageSize)</span> &#123;<br>        <span class="hljs-keyword">return</span> <span class="hljs-string">&quot;SELECT group_id FROM config_info WHERE tenant_id =&#x27;&quot;</span> + NamespaceUtil.getNamespaceDefaultId()<br>                + <span class="hljs-string">&quot;&#x27; GROUP BY group_id LIMIT &quot;</span> + pageSize + <span class="hljs-string">&quot; offset &quot;</span> + startRow;<br>    &#125;<br>    <br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> String <span class="hljs-title function_">findAllConfigKey</span><span class="hljs-params">(<span class="hljs-type">int</span> startRow, <span class="hljs-type">int</span> pageSize)</span> &#123;<br>        <span class="hljs-keyword">return</span> <span class="hljs-string">&quot; SELECT data_id,group_id,app_name  FROM ( &quot;</span><br>                + <span class="hljs-string">&quot; SELECT id FROM config_info WHERE tenant_id LIKE ? ORDER BY id LIMIT &quot;</span> + pageSize + <span class="hljs-string">&quot; offset &quot;</span> + startRow<br>                + <span class="hljs-string">&quot; )&quot;</span> + <span class="hljs-string">&quot; g, config_info t WHERE g.id = t.id  &quot;</span>;<br>    &#125;<br>    <br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> String <span class="hljs-title function_">findAllConfigInfoBaseFetchRows</span><span class="hljs-params">(<span class="hljs-type">int</span> startRow, <span class="hljs-type">int</span> pageSize)</span> &#123;<br>        <span class="hljs-keyword">return</span> <span class="hljs-string">&quot;SELECT t.id,data_id,group_id,content,md5&quot;</span><br>                + <span class="hljs-string">&quot; FROM ( SELECT id FROM config_info ORDER BY id LIMIT &quot;</span> + pageSize + <span class="hljs-string">&quot; offset &quot;</span> + startRow + <span class="hljs-string">&quot;  ) &quot;</span><br>                + <span class="hljs-string">&quot; g, config_info t  WHERE g.id = t.id &quot;</span>;<br>    &#125;<br>    <br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> String <span class="hljs-title function_">findAllConfigInfoFragment</span><span class="hljs-params">(<span class="hljs-type">int</span> startRow, <span class="hljs-type">int</span> pageSize)</span> &#123;<br>        <span class="hljs-keyword">return</span> <span class="hljs-string">&quot;SELECT id,data_id,group_id,tenant_id,app_name,content,md5,gmt_modified,type,encrypted_data_key &quot;</span><br>                + <span class="hljs-string">&quot;FROM config_info WHERE id &gt; ? ORDER BY id ASC LIMIT &quot;</span> + pageSize + <span class="hljs-string">&quot; offset &quot;</span> + startRow;<br>    &#125;<br><br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> String <span class="hljs-title function_">findChangeConfigFetchRows</span><span class="hljs-params">(Map&lt;String, String&gt; params, <span class="hljs-keyword">final</span> Timestamp startTime,</span><br><span class="hljs-params">            <span class="hljs-keyword">final</span> Timestamp endTime, <span class="hljs-type">int</span> startRow, <span class="hljs-type">int</span> pageSize, <span class="hljs-type">long</span> lastMaxId)</span> &#123;<br>        <span class="hljs-keyword">final</span> <span class="hljs-type">String</span> <span class="hljs-variable">tenant</span> <span class="hljs-operator">=</span> params.get(TENANT);<br>        <span class="hljs-keyword">final</span> <span class="hljs-type">String</span> <span class="hljs-variable">dataId</span> <span class="hljs-operator">=</span> params.get(DATA_ID);<br>        <span class="hljs-keyword">final</span> <span class="hljs-type">String</span> <span class="hljs-variable">group</span> <span class="hljs-operator">=</span> params.get(GROUP);<br>        <span class="hljs-keyword">final</span> <span class="hljs-type">String</span> <span class="hljs-variable">appName</span> <span class="hljs-operator">=</span> params.get(APP_NAME);<br>        <span class="hljs-keyword">final</span> <span class="hljs-type">String</span> <span class="hljs-variable">tenantTmp</span> <span class="hljs-operator">=</span> StringUtils.isBlank(tenant) ? StringUtils.EMPTY : tenant;<br>        <span class="hljs-keyword">final</span> <span class="hljs-type">String</span> <span class="hljs-variable">sqlFetchRows</span> <span class="hljs-operator">=</span> <span class="hljs-string">&quot;SELECT id,data_id,group_id,tenant_id,app_name,content,type,md5,gmt_modified FROM config_info WHERE &quot;</span>;<br>        <span class="hljs-type">String</span> <span class="hljs-variable">where</span> <span class="hljs-operator">=</span> <span class="hljs-string">&quot; 1=1 &quot;</span>;<br>        <span class="hljs-keyword">if</span> (!StringUtils.isBlank(dataId)) &#123;<br>            where += <span class="hljs-string">&quot; AND data_id LIKE ? &quot;</span>;<br>        &#125;<br>        <span class="hljs-keyword">if</span> (!StringUtils.isBlank(group)) &#123;<br>            where += <span class="hljs-string">&quot; AND group_id LIKE ? &quot;</span>;<br>        &#125;<br>        <br>        <span class="hljs-keyword">if</span> (!StringUtils.isBlank(tenantTmp)) &#123;<br>            where += <span class="hljs-string">&quot; AND tenant_id = ? &quot;</span>;<br>        &#125;<br>        <br>        <span class="hljs-keyword">if</span> (!StringUtils.isBlank(appName)) &#123;<br>            where += <span class="hljs-string">&quot; AND app_name = ? &quot;</span>;<br>        &#125;<br>        <span class="hljs-keyword">if</span> (startTime != <span class="hljs-literal">null</span>) &#123;<br>            where += <span class="hljs-string">&quot; AND gmt_modified &gt;=? &quot;</span>;<br>        &#125;<br>        <span class="hljs-keyword">if</span> (endTime != <span class="hljs-literal">null</span>) &#123;<br>            where += <span class="hljs-string">&quot; AND gmt_modified &lt;=? &quot;</span>;<br>        &#125;<br>        <span class="hljs-keyword">return</span> sqlFetchRows + where + <span class="hljs-string">&quot; AND id &gt; &quot;</span> + lastMaxId + <span class="hljs-string">&quot; ORDER BY id ASC&quot;</span> + <span class="hljs-string">&quot; LIMIT &quot;</span> + pageSize + <span class="hljs-string">&quot; offset 0 &quot;</span>;<br>    &#125;<br>    <br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> String <span class="hljs-title function_">listGroupKeyMd5ByPageFetchRows</span><span class="hljs-params">(<span class="hljs-type">int</span> startRow, <span class="hljs-type">int</span> pageSize)</span> &#123;<br>        <span class="hljs-keyword">return</span> <span class="hljs-string">&quot;SELECT t.id,data_id,group_id,tenant_id,app_name,md5,type,gmt_modified,encrypted_data_key FROM &quot;</span><br>                + <span class="hljs-string">&quot;( SELECT id FROM config_info ORDER BY id LIMIT &quot;</span> + pageSize + <span class="hljs-string">&quot; offset &quot;</span> + startRow<br>                + <span class="hljs-string">&quot; ) g, config_info t WHERE g.id = t.id&quot;</span>;<br>    &#125;<br><br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> String <span class="hljs-title function_">findConfigInfoBaseLikeFetchRows</span><span class="hljs-params">(Map&lt;String, String&gt; params, <span class="hljs-type">int</span> startRow, <span class="hljs-type">int</span> pageSize)</span> &#123;<br>        <span class="hljs-keyword">final</span> <span class="hljs-type">String</span> <span class="hljs-variable">sqlFetchRows</span> <span class="hljs-operator">=</span> <span class="hljs-string">&quot;SELECT id,data_id,group_id,tenant_id,content FROM config_info WHERE &quot;</span>;<br>        <span class="hljs-type">String</span> <span class="hljs-variable">where</span> <span class="hljs-operator">=</span> <span class="hljs-string">&quot; 1=1 AND tenant_id=&#x27;&quot;</span> + NamespaceUtil.getNamespaceDefaultId() + <span class="hljs-string">&quot;&#x27; &quot;</span>;<br>        <span class="hljs-keyword">if</span> (!StringUtils.isBlank(params.get(DATA_ID))) &#123;<br>            where += <span class="hljs-string">&quot; AND data_id LIKE ? &quot;</span>;<br>        &#125;<br>        <span class="hljs-keyword">if</span> (!StringUtils.isBlank(params.get(GROUP))) &#123;<br>            where += <span class="hljs-string">&quot; AND group_id LIKE &quot;</span>;<br>        &#125;<br>        <span class="hljs-keyword">if</span> (!StringUtils.isBlank(params.get(CONTENT))) &#123;<br>            where += <span class="hljs-string">&quot; AND content LIKE ? &quot;</span>;<br>        &#125;<br>        <span class="hljs-keyword">return</span> sqlFetchRows + where + <span class="hljs-string">&quot; LIMIT &quot;</span> + pageSize + <span class="hljs-string">&quot; offset &quot;</span> + startRow;<br>    &#125;<br><br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> String <span class="hljs-title function_">findConfigInfo4PageFetchRows</span><span class="hljs-params">(Map&lt;String, String&gt; params, <span class="hljs-type">int</span> startRow, <span class="hljs-type">int</span> pageSize)</span> &#123;<br>        <span class="hljs-keyword">final</span> <span class="hljs-type">String</span> <span class="hljs-variable">appName</span> <span class="hljs-operator">=</span> params.get(APP_NAME);<br>        <span class="hljs-keyword">final</span> <span class="hljs-type">String</span> <span class="hljs-variable">dataId</span> <span class="hljs-operator">=</span> params.get(DATA_ID);<br>        <span class="hljs-keyword">final</span> <span class="hljs-type">String</span> <span class="hljs-variable">group</span> <span class="hljs-operator">=</span> params.get(GROUP);<br>        <span class="hljs-keyword">final</span> <span class="hljs-type">String</span> <span class="hljs-variable">content</span> <span class="hljs-operator">=</span> params.get(CONTENT);<br>        <span class="hljs-keyword">final</span> <span class="hljs-type">String</span> <span class="hljs-variable">sql</span> <span class="hljs-operator">=</span> <span class="hljs-string">&quot;SELECT id,data_id,group_id,tenant_id,app_name,content,type,encrypted_data_key FROM config_info&quot;</span>;<br>        <span class="hljs-type">StringBuilder</span> <span class="hljs-variable">where</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">StringBuilder</span>(<span class="hljs-string">&quot; WHERE &quot;</span>);<br>        where.append(<span class="hljs-string">&quot; tenant_id=? &quot;</span>);<br>        <span class="hljs-keyword">if</span> (StringUtils.isNotBlank(dataId)) &#123;<br>            where.append(<span class="hljs-string">&quot; AND data_id=? &quot;</span>);<br>        &#125;<br>        <span class="hljs-keyword">if</span> (StringUtils.isNotBlank(group)) &#123;<br>            where.append(<span class="hljs-string">&quot; AND group_id=? &quot;</span>);<br>        &#125;<br>        <span class="hljs-keyword">if</span> (StringUtils.isNotBlank(appName)) &#123;<br>            where.append(<span class="hljs-string">&quot; AND app_name=? &quot;</span>);<br>        &#125;<br>        <span class="hljs-keyword">if</span> (!StringUtils.isBlank(content)) &#123;<br>            where.append(<span class="hljs-string">&quot; AND content LIKE ? &quot;</span>);<br>        &#125;<br>        <span class="hljs-keyword">return</span> sql + where + <span class="hljs-string">&quot; LIMIT &quot;</span> + pageSize + <span class="hljs-string">&quot; offset &quot;</span> + startRow;<br>    &#125;<br>    <br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> String <span class="hljs-title function_">findConfigInfoBaseByGroupFetchRows</span><span class="hljs-params">(<span class="hljs-type">int</span> startRow, <span class="hljs-type">int</span> pageSize)</span> &#123;<br>        <span class="hljs-keyword">return</span> <span class="hljs-string">&quot;SELECT id,data_id,group_id,content FROM config_info WHERE group_id=? AND tenant_id=?&quot;</span> + <span class="hljs-string">&quot; LIMIT &quot;</span><br>                + pageSize + <span class="hljs-string">&quot; offset &quot;</span> + startRow;<br>    &#125;<br>    <br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> String <span class="hljs-title function_">findConfigInfoLike4PageFetchRows</span><span class="hljs-params">(Map&lt;String, String&gt; params, <span class="hljs-type">int</span> startRow, <span class="hljs-type">int</span> pageSize)</span> &#123;<br>        <span class="hljs-type">String</span> <span class="hljs-variable">dataId</span> <span class="hljs-operator">=</span> params.get(DATA_ID);<br>        <span class="hljs-type">String</span> <span class="hljs-variable">group</span> <span class="hljs-operator">=</span> params.get(GROUP);<br>        <span class="hljs-keyword">final</span> <span class="hljs-type">String</span> <span class="hljs-variable">appName</span> <span class="hljs-operator">=</span> params.get(APP_NAME);<br>        <span class="hljs-keyword">final</span> <span class="hljs-type">String</span> <span class="hljs-variable">content</span> <span class="hljs-operator">=</span> params.get(CONTENT);<br>        <span class="hljs-keyword">final</span> <span class="hljs-type">String</span> <span class="hljs-variable">sqlFetchRows</span> <span class="hljs-operator">=</span> <span class="hljs-string">&quot;SELECT id,data_id,group_id,tenant_id,app_name,content,encrypted_data_key FROM config_info&quot;</span>;<br>        <span class="hljs-type">StringBuilder</span> <span class="hljs-variable">where</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">StringBuilder</span>(<span class="hljs-string">&quot; WHERE &quot;</span>);<br>        where.append(<span class="hljs-string">&quot; tenant_id LIKE ? &quot;</span>);<br>        <span class="hljs-keyword">if</span> (!StringUtils.isBlank(dataId)) &#123;<br>            where.append(<span class="hljs-string">&quot; AND data_id LIKE ? &quot;</span>);<br>        &#125;<br>        <span class="hljs-keyword">if</span> (!StringUtils.isBlank(group)) &#123;<br>            where.append(<span class="hljs-string">&quot; AND group_id LIKE ? &quot;</span>);<br>        &#125;<br>        <span class="hljs-keyword">if</span> (!StringUtils.isBlank(appName)) &#123;<br>            where.append(<span class="hljs-string">&quot; AND app_name = ? &quot;</span>);<br>        &#125;<br>        <span class="hljs-keyword">if</span> (!StringUtils.isBlank(content)) &#123;<br>            where.append(<span class="hljs-string">&quot; AND content LIKE ? &quot;</span>);<br>        &#125;<br>        <span class="hljs-keyword">return</span> sqlFetchRows + where + <span class="hljs-string">&quot; LIMIT &quot;</span> + pageSize + <span class="hljs-string">&quot; offset &quot;</span> + startRow;<br>    &#125;<br>    <br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> String <span class="hljs-title function_">findAllConfigInfoFetchRows</span><span class="hljs-params">(<span class="hljs-type">int</span> startRow, <span class="hljs-type">int</span> pageSize)</span> &#123;<br>        <span class="hljs-keyword">return</span> <span class="hljs-string">&quot;SELECT t.id,data_id,group_id,tenant_id,app_name,content,md5 &quot;</span><br>                + <span class="hljs-string">&quot; FROM (  SELECT id FROM config_info WHERE tenant_id LIKE ? ORDER BY id LIMIT &quot;</span> + pageSize + <span class="hljs-string">&quot; offset &quot;</span> + startRow + <span class="hljs-string">&quot; )&quot;</span><br>                + <span class="hljs-string">&quot; g, config_info t  WHERE g.id = t.id &quot;</span>;<br>    &#125;<br><br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> String <span class="hljs-title function_">getDataSource</span><span class="hljs-params">()</span> &#123;<br>        <span class="hljs-keyword">return</span> DataSourceConstant.POSTGRESQL;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<p>ConfigInfoTagMapperByPostgreSql类</p>
<figure class="highlight java"><figcaption><span>ConfigInfoTagMapperByPostgreSql类</span></figcaption><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">ConfigInfoTagMapperByPostgreSql</span> <span class="hljs-keyword">extends</span> <span class="hljs-title class_">AbstractMapper</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">ConfigInfoTagMapper</span> &#123;<br><br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> String <span class="hljs-title function_">findAllConfigInfoTagForDumpAllFetchRows</span><span class="hljs-params">(<span class="hljs-type">int</span> startRow, <span class="hljs-type">int</span> pageSize)</span> &#123;<br>        <span class="hljs-keyword">return</span> <span class="hljs-string">&quot; SELECT t.id,data_id,group_id,tenant_id,tag_id,app_name,content,md5,gmt_modified &quot;</span><br>                + <span class="hljs-string">&quot; FROM (  SELECT id FROM config_info_tag  ORDER BY id LIMIT &quot;</span> + pageSize + <span class="hljs-string">&quot; offset &quot;</span> + startRow + <span class="hljs-string">&quot; ) &quot;</span><br>                + <span class="hljs-string">&quot;g, config_info_tag t  WHERE g.id = t.id  &quot;</span>;<br>    &#125;<br><br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> String <span class="hljs-title function_">getDataSource</span><span class="hljs-params">()</span> &#123;<br>        <span class="hljs-keyword">return</span> DataSourceConstant.POSTGRESQL;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<p>ConfigTagsRelationMapperByPostgreSql类</p>
<figure class="highlight java"><figcaption><span>ConfigTagsRelationMapperByPostgreSql类</span></figcaption><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">ConfigTagsRelationMapperByPostgreSql</span> <span class="hljs-keyword">extends</span> <span class="hljs-title class_">AbstractMapper</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">ConfigTagsRelationMapper</span> &#123;<br>    <br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> String <span class="hljs-title function_">findConfigInfo4PageFetchRows</span><span class="hljs-params">(Map&lt;String, String&gt; params, <span class="hljs-type">int</span> tagSize, <span class="hljs-type">int</span> startRow, <span class="hljs-type">int</span> pageSize)</span> &#123;<br>        <span class="hljs-keyword">final</span> <span class="hljs-type">String</span> <span class="hljs-variable">appName</span> <span class="hljs-operator">=</span> params.get(<span class="hljs-string">&quot;appName&quot;</span>);<br>        <span class="hljs-keyword">final</span> <span class="hljs-type">String</span> <span class="hljs-variable">dataId</span> <span class="hljs-operator">=</span> params.get(<span class="hljs-string">&quot;dataId&quot;</span>);<br>        <span class="hljs-keyword">final</span> <span class="hljs-type">String</span> <span class="hljs-variable">group</span> <span class="hljs-operator">=</span> params.get(<span class="hljs-string">&quot;group&quot;</span>);<br>        <span class="hljs-keyword">final</span> <span class="hljs-type">String</span> <span class="hljs-variable">content</span> <span class="hljs-operator">=</span> params.get(<span class="hljs-string">&quot;content&quot;</span>);<br>        <span class="hljs-type">StringBuilder</span> <span class="hljs-variable">where</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">StringBuilder</span>(<span class="hljs-string">&quot; WHERE &quot;</span>);<br>        <span class="hljs-keyword">final</span> <span class="hljs-type">String</span> <span class="hljs-variable">sql</span> <span class="hljs-operator">=</span><br>                <span class="hljs-string">&quot;SELECT a.id,a.data_id,a.group_id,a.tenant_id,a.app_name,a.content FROM config_info  a LEFT JOIN &quot;</span><br>                        + <span class="hljs-string">&quot;config_tags_relation b ON a.id=b.id&quot;</span>;<br>        <br>        where.append(<span class="hljs-string">&quot; a.tenant_id=? &quot;</span>);<br>        <br>        <span class="hljs-keyword">if</span> (StringUtils.isNotBlank(dataId)) &#123;<br>            where.append(<span class="hljs-string">&quot; AND a.data_id=? &quot;</span>);<br>        &#125;<br>        <span class="hljs-keyword">if</span> (StringUtils.isNotBlank(group)) &#123;<br>            where.append(<span class="hljs-string">&quot; AND a.group_id=? &quot;</span>);<br>        &#125;<br>        <span class="hljs-keyword">if</span> (StringUtils.isNotBlank(appName)) &#123;<br>            where.append(<span class="hljs-string">&quot; AND a.app_name=? &quot;</span>);<br>        &#125;<br>        <span class="hljs-keyword">if</span> (!StringUtils.isBlank(content)) &#123;<br>            where.append(<span class="hljs-string">&quot; AND a.content LIKE ? &quot;</span>);<br>        &#125;<br>        where.append(<span class="hljs-string">&quot; AND b.tag_name IN (&quot;</span>);<br>        <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> <span class="hljs-variable">i</span> <span class="hljs-operator">=</span> <span class="hljs-number">0</span>; i &lt; tagSize; i++) &#123;<br>            <span class="hljs-keyword">if</span> (i != <span class="hljs-number">0</span>) &#123;<br>                where.append(<span class="hljs-string">&quot;, &quot;</span>);<br>            &#125;<br>            where.append(<span class="hljs-string">&#x27;?&#x27;</span>);<br>        &#125;<br>        where.append(<span class="hljs-string">&quot;) &quot;</span>);<br>        <span class="hljs-keyword">return</span> sql + where + <span class="hljs-string">&quot; LIMIT &quot;</span> + pageSize + <span class="hljs-string">&quot; offset &quot;</span> + startRow;<br>    &#125;<br>    <br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> String <span class="hljs-title function_">findConfigInfoLike4PageFetchRows</span><span class="hljs-params">(<span class="hljs-keyword">final</span> Map&lt;String, String&gt; params, <span class="hljs-type">int</span> tagSize, <span class="hljs-type">int</span> startRow,</span><br><span class="hljs-params">            <span class="hljs-type">int</span> pageSize)</span> &#123;<br>        <span class="hljs-keyword">final</span> <span class="hljs-type">String</span> <span class="hljs-variable">appName</span> <span class="hljs-operator">=</span> params.get(<span class="hljs-string">&quot;appName&quot;</span>);<br>        <span class="hljs-keyword">final</span> <span class="hljs-type">String</span> <span class="hljs-variable">content</span> <span class="hljs-operator">=</span> params.get(<span class="hljs-string">&quot;content&quot;</span>);<br>        <span class="hljs-keyword">final</span> <span class="hljs-type">String</span> <span class="hljs-variable">dataId</span> <span class="hljs-operator">=</span> params.get(<span class="hljs-string">&quot;dataId&quot;</span>);<br>        <span class="hljs-keyword">final</span> <span class="hljs-type">String</span> <span class="hljs-variable">group</span> <span class="hljs-operator">=</span> params.get(<span class="hljs-string">&quot;group&quot;</span>);<br>        <span class="hljs-type">StringBuilder</span> <span class="hljs-variable">where</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">StringBuilder</span>(<span class="hljs-string">&quot; WHERE &quot;</span>);<br>        <span class="hljs-keyword">final</span> <span class="hljs-type">String</span> <span class="hljs-variable">sqlFetchRows</span> <span class="hljs-operator">=</span> <span class="hljs-string">&quot;SELECT a.id,a.data_id,a.group_id,a.tenant_id,a.app_name,a.content &quot;</span><br>                + <span class="hljs-string">&quot;FROM config_info a LEFT JOIN config_tags_relation b ON a.id=b.id &quot;</span>;<br>        <br>        where.append(<span class="hljs-string">&quot; a.tenant_id LIKE ? &quot;</span>);<br>        <span class="hljs-keyword">if</span> (!StringUtils.isBlank(dataId)) &#123;<br>            where.append(<span class="hljs-string">&quot; AND a.data_id LIKE ? &quot;</span>);<br>        &#125;<br>        <span class="hljs-keyword">if</span> (!StringUtils.isBlank(group)) &#123;<br>            where.append(<span class="hljs-string">&quot; AND a.group_id LIKE ? &quot;</span>);<br>        &#125;<br>        <span class="hljs-keyword">if</span> (!StringUtils.isBlank(appName)) &#123;<br>            where.append(<span class="hljs-string">&quot; AND a.app_name = ? &quot;</span>);<br>        &#125;<br>        <span class="hljs-keyword">if</span> (!StringUtils.isBlank(content)) &#123;<br>            where.append(<span class="hljs-string">&quot; AND a.content LIKE ? &quot;</span>);<br>        &#125;<br>        <br>        where.append(<span class="hljs-string">&quot; AND b.tag_name IN (&quot;</span>);<br>        <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> <span class="hljs-variable">i</span> <span class="hljs-operator">=</span> <span class="hljs-number">0</span>; i &lt; tagSize; i++) &#123;<br>            <span class="hljs-keyword">if</span> (i != <span class="hljs-number">0</span>) &#123;<br>                where.append(<span class="hljs-string">&quot;, &quot;</span>);<br>            &#125;<br>            where.append(<span class="hljs-string">&#x27;?&#x27;</span>);<br>        &#125;<br>        where.append(<span class="hljs-string">&quot;) &quot;</span>);<br>        <span class="hljs-keyword">return</span> sqlFetchRows + where + <span class="hljs-string">&quot; LIMIT &quot;</span> + pageSize + <span class="hljs-string">&quot; offset &quot;</span> + startRow;<br>    &#125;<br>    <br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> String <span class="hljs-title function_">getDataSource</span><span class="hljs-params">()</span> &#123;<br>        <span class="hljs-keyword">return</span> DataSourceConstant.POSTGRESQL;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<p>GroupCapacityMapperByPostgreSql类</p>
<figure class="highlight java"><figcaption><span>GroupCapacityMapperByPostgreSql类</span></figcaption><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">GroupCapacityMapperByPostgreSql</span> <span class="hljs-keyword">extends</span> <span class="hljs-title class_">AbstractMapper</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">GroupCapacityMapper</span> &#123;<br><br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> String <span class="hljs-title function_">getDataSource</span><span class="hljs-params">()</span> &#123;<br>        <span class="hljs-keyword">return</span> DataSourceConstant.POSTGRESQL;<br>    &#125;<br><br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> String <span class="hljs-title function_">selectGroupInfoBySize</span><span class="hljs-params">()</span> &#123;<br>        <span class="hljs-keyword">return</span> <span class="hljs-string">&quot;SELECT id, group_id FROM group_capacity WHERE id &gt; ? LIMIT ?&quot;</span>;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<p>HistoryConfigInfoMapperByPostgreSql类</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">HistoryConfigInfoMapperByPostgreSql</span> <span class="hljs-keyword">extends</span> <span class="hljs-title class_">AbstractMapper</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">HistoryConfigInfoMapper</span> &#123;<br>    <br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> String <span class="hljs-title function_">removeConfigHistory</span><span class="hljs-params">()</span> &#123;<br>        <span class="hljs-keyword">return</span> <span class="hljs-string">&quot;DELETE FROM his_config_info WHERE gmt_modified &lt; ? LIMIT ?&quot;</span>;<br>    &#125;<br><br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> String <span class="hljs-title function_">pageFindConfigHistoryFetchRows</span><span class="hljs-params">(<span class="hljs-type">int</span> pageNo, <span class="hljs-type">int</span> pageSize)</span> &#123;<br>        <span class="hljs-keyword">final</span> <span class="hljs-type">int</span> <span class="hljs-variable">offset</span> <span class="hljs-operator">=</span> (pageNo - <span class="hljs-number">1</span>) * pageSize;<br>        <span class="hljs-keyword">final</span> <span class="hljs-type">int</span> <span class="hljs-variable">limit</span> <span class="hljs-operator">=</span> pageSize;<br>        <span class="hljs-keyword">return</span>  <span class="hljs-string">&quot;SELECT nid,data_id,group_id,tenant_id,app_name,src_ip,src_user,op_type,gmt_create,gmt_modified FROM his_config_info &quot;</span><br>                + <span class="hljs-string">&quot;WHERE data_id = ? AND group_id = ? AND tenant_id = ? ORDER BY nid DESC  LIMIT &quot;</span> + limit + <span class="hljs-string">&quot; offset &quot;</span> + offset;<br>    &#125;<br><br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> String <span class="hljs-title function_">getDataSource</span><span class="hljs-params">()</span> &#123;<br>        <span class="hljs-keyword">return</span> DataSourceConstant.POSTGRESQL;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<p>TenantCapacityMapperByPostgreSql类</p>
<figure class="highlight java"><figcaption><span>TenantCapacityMapperByPostgreSql类</span></figcaption><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">TenantCapacityMapperByPostgreSql</span> <span class="hljs-keyword">extends</span> <span class="hljs-title class_">AbstractMapper</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">TenantCapacityMapper</span> &#123;<br><br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> String <span class="hljs-title function_">getDataSource</span><span class="hljs-params">()</span> &#123;<br>        <span class="hljs-keyword">return</span> DataSourceConstant.POSTGRESQL;<br>    &#125;<br><br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> String <span class="hljs-title function_">getCapacityList4CorrectUsage</span><span class="hljs-params">()</span> &#123;<br>        <span class="hljs-keyword">return</span> <span class="hljs-string">&quot;SELECT id, tenant_id FROM tenant_capacity WHERE id&gt;? LIMIT ?&quot;</span>;<br>    &#125;<br><br>&#125;<br></code></pre></td></tr></table></figure>

<p>TenantInfoMapperByPostgreSql类</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">TenantInfoMapperByPostgreSql</span> <span class="hljs-keyword">extends</span> <span class="hljs-title class_">AbstractMapper</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">TenantInfoMapper</span> &#123;<br><br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> String <span class="hljs-title function_">getDataSource</span><span class="hljs-params">()</span> &#123;<br>        <span class="hljs-keyword">return</span> DataSourceConstant.POSTGRESQL;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>中间件</category>
        <category>nacos</category>
      </categories>
      <tags>
        <tag>nacos</tag>
      </tags>
  </entry>
  <entry>
    <title>使用maven编译nacos2.2.3源码</title>
    <url>/2023/235410d42872/index.html</url>
    <content><![CDATA[<p>使用 <code>maven</code> 命令进入 <code>nacos</code> 根目录编译</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">mvn clean install -DskipTests<br></code></pre></td></tr></table></figure>

<p>出现</p>
<figure class="highlight stylus"><table><tr><td class="code"><pre><code class="hljs stylus">Execution pmd of goal org<span class="hljs-selector-class">.apache</span><span class="hljs-selector-class">.maven</span><span class="hljs-selector-class">.plugins</span>:maven-pmd-plugin:<span class="hljs-number">3.8</span>:pmd failed: org<span class="hljs-selector-class">.apache</span><span class="hljs-selector-class">.maven</span><span class="hljs-selector-class">.reporting</span><span class="hljs-selector-class">.MavenReportException</span>:<br> Unsupported targetJdk value <span class="hljs-string">&#x27;11&#x27;</span>. <br></code></pre></td></tr></table></figure>
<p>错误，这是因为增加了 <code>pmd</code> 校验，可以使用下面命令跳过校验</p>
<figure class="highlight ada"><table><tr><td class="code"><pre><code class="hljs ada">mvn clean install &#x27;-DskipTests<span class="hljs-string">&#x27; &#x27;</span>-Dpmd.skip=<span class="hljs-literal">true</span><span class="hljs-string">&#x27; &#x27;</span>-Dcheckstyle.skip=<span class="hljs-literal">true</span>&#x27;<br></code></pre></td></tr></table></figure>

<p>出现 </p>
<figure class="highlight smali"><table><tr><td class="code"><pre><code class="hljs smali">[ERROR] Failed to<span class="hljs-built_in"> execute </span>goal org.apache.maven.plugins:maven-compiler-plugin:3.5.1:testCompile (default-testCompile) on project nacos-config: Compilation failure<br>[ERROR] /D:/code/nacos/config/src/test/java/com/alibaba/nacos/config/server/utils/ResponseUtilTest.java:[28,29] 程序包 sun.security.action 不可见<br>[ERROR]   (程序包 sun.security.action 已在模块 java.base 中声明, 但该模块未将它导出到未命名模块)<br></code></pre></td></tr></table></figure>
<p>错误，解决方式是在 <code>maven-compiler-plugin</code> 配置中添加 <code>--add-exports=java.base/sun.security.action=ALL-UNNAMED</code> 参数，具体配置为在根目录下的 <code>pom.xml</code> 中添加下面代码</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><code class="hljs xml"> <span class="hljs-tag">&lt;<span class="hljs-name">plugin</span>&gt;</span><br>    <span class="hljs-tag">&lt;<span class="hljs-name">artifactId</span>&gt;</span>maven-compiler-plugin<span class="hljs-tag">&lt;/<span class="hljs-name">artifactId</span>&gt;</span><br>    <span class="hljs-tag">&lt;<span class="hljs-name">version</span>&gt;</span>$&#123;maven-compiler-plugin.version&#125;<span class="hljs-tag">&lt;/<span class="hljs-name">version</span>&gt;</span><br>    <span class="hljs-tag">&lt;<span class="hljs-name">configuration</span>&gt;</span><br>        <span class="hljs-tag">&lt;<span class="hljs-name">source</span>&gt;</span>$&#123;maven.compiler.source&#125;<span class="hljs-tag">&lt;/<span class="hljs-name">source</span>&gt;</span><br>        <span class="hljs-tag">&lt;<span class="hljs-name">target</span>&gt;</span>$&#123;maven.compiler.target&#125;<span class="hljs-tag">&lt;/<span class="hljs-name">target</span>&gt;</span><br>        <span class="hljs-tag">&lt;<span class="hljs-name">compilerVersion</span>&gt;</span>$&#123;maven.compiler.source&#125;<span class="hljs-tag">&lt;/<span class="hljs-name">compilerVersion</span>&gt;</span><br>        <span class="hljs-tag">&lt;<span class="hljs-name">showDeprecation</span>&gt;</span>true<span class="hljs-tag">&lt;/<span class="hljs-name">showDeprecation</span>&gt;</span><br>        <span class="hljs-tag">&lt;<span class="hljs-name">showWarnings</span>&gt;</span>true<span class="hljs-tag">&lt;/<span class="hljs-name">showWarnings</span>&gt;</span><br>        <span class="hljs-comment">&lt;!-- 下面这一段新添加的用于解决编译错误的关键 --&gt;</span><br>        <span class="hljs-tag">&lt;<span class="hljs-name">compilerArgs</span>&gt;</span><br>            <span class="hljs-tag">&lt;<span class="hljs-name">arg</span>&gt;</span>--add-exports=java.base/sun.security.action=ALL-UNNAMED<span class="hljs-tag">&lt;/<span class="hljs-name">arg</span>&gt;</span><br>        <span class="hljs-tag">&lt;/<span class="hljs-name">compilerArgs</span>&gt;</span><br>    <span class="hljs-tag">&lt;/<span class="hljs-name">configuration</span>&gt;</span><br><span class="hljs-tag">&lt;/<span class="hljs-name">plugin</span>&gt;</span><br></code></pre></td></tr></table></figure>

<p>注意： 因为提示出现错误的包是 <code>sun.security.action</code>， 所以添加的时候才添加 <code>java.base/sun.security.action</code>， 如果提示的是其他包，则需要对应修改</p>
<h2 id="最终打包命令"><a href="#最终打包命令" class="headerlink" title="最终打包命令"></a>最终打包命令</h2><p>最终打包需要选择 profile， 这个 profile 在 <code>nacos-console</code> 模块的 <code>pom.xml</code> 文件中已经写了是  <code>release-nacos</code>， 所以最终打包命令是</p>
<figure class="highlight ada"><table><tr><td class="code"><pre><code class="hljs ada">mvn clean install &#x27;-DskipTests=<span class="hljs-literal">true</span><span class="hljs-string">&#x27; &#x27;</span>-Dpmd.skip=<span class="hljs-literal">true</span><span class="hljs-string">&#x27; &#x27;</span>-Dcheckstyle.skip=<span class="hljs-literal">true</span>&#x27; -Prelease-nacos<br></code></pre></td></tr></table></figure>


]]></content>
      <categories>
        <category>中间件</category>
        <category>nacos</category>
      </categories>
      <tags>
        <tag>nacos</tag>
      </tags>
  </entry>
  <entry>
    <title>RocketMQ4中Nameserver详解</title>
    <url>/2023/e17cf930f952/index.html</url>
    <content><![CDATA[<span class="hide_first_mermaid">
<div class="mermaid-wrap"><pre class="mermaid-src" hidden>
  flowchart LR
    A
  </pre></div>
</span>

<blockquote>
<p>RocketMQ版本： 4.9.7<br>JDK版本： 17</p>
</blockquote>
<h2 id="Nameserver-在-RocketMQ-整体架构中的作用"><a href="#Nameserver-在-RocketMQ-整体架构中的作用" class="headerlink" title="Nameserver 在 RocketMQ 整体架构中的作用"></a>Nameserver 在 RocketMQ 整体架构中的作用</h2><p>一句话描述：<code>Nameserver</code> 就相当于是 <code>RocketMQ</code> 中的注册中心， <code>Broker</code> 会将自身信息注册到 <code>Nameserver</code> 中，<code>Producer</code> 和 <code>Consumer</code> 会从 <code>Nameserver</code> 中不断获取 <code>Broker</code> 相关信息</p>
<h2 id="从代码层面来看-Nameserver-的功能"><a href="#从代码层面来看-Nameserver-的功能" class="headerlink" title="从代码层面来看 Nameserver 的功能"></a>从代码层面来看 Nameserver 的功能</h2><p>总结：<code>Nameserver</code> 主要完成两件事情</p>
<ol>
<li>作为 <code>NettyServer</code> 接收网络请求, 因为对于 <code>Produer</code>, <code>Consumer</code>, <code>Broker</code> 而言，<code>Nameserver</code> 就是服务端，它们之间的通信是通过 <code>Netty</code> 实现</li>
<li>处理接收到的请求，比如更新 <code>Broker</code> 信息，获取 <code>Broker</code> 信息等</li>
</ol>
<h2 id="Nameserver-的启动流程"><a href="#Nameserver-的启动流程" class="headerlink" title="Nameserver 的启动流程"></a>Nameserver 的启动流程</h2><pre class="mermaid">graph TB
    subgraph ONE["createNamesrvControlle 创建 NamesrvController"]
        A1["解析命令行参数
        变成 CommandLine"]
        A2["根据解析的参数
        设置 NamesrvConfig 和
        NettyServerConfig"]
        A3["实例化 NamesrvController "]
        A1-->A2-->A3
    end
    subgraph TWO["NamesrvController.initialize 初始化 NamesrvController"]
        B1["加载 KVConfig 配置"]
        B2["初始化 NettyRemotingServer "]
        B3["注册 DefaultRequestProcessor 处理器"]
        B4["启动定时任务
        清理下线的 Broker "]
        B1-->B2-->B3-->B4
    end
    subgraph THREE["NamesrvController.start 启动 NamesrvController"]
        C1["启动 Nettry 服务
        NettyRemotingServer.start()"]
    end
    ONE-->TWO-->THREE</pre>

<p><code>Nameserver</code> 整体代码(只展示部分重点代码)</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">NamesrvStartup</span> &#123;<br><br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> InternalLogger log;<br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-type">Properties</span> <span class="hljs-variable">properties</span> <span class="hljs-operator">=</span> <span class="hljs-literal">null</span>;<br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-type">CommandLine</span> <span class="hljs-variable">commandLine</span> <span class="hljs-operator">=</span> <span class="hljs-literal">null</span>;<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">main</span><span class="hljs-params">(String[] args)</span> &#123;<br>        main0(args);<br>    &#125;<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> NamesrvController <span class="hljs-title function_">main0</span><span class="hljs-params">(String[] args)</span> &#123;<br>        <span class="hljs-comment">// 创建 NamesrvController</span><br>        <span class="hljs-type">NamesrvController</span> <span class="hljs-variable">controller</span> <span class="hljs-operator">=</span> createNamesrvController(args);<br>        start(controller);<br>        <span class="hljs-keyword">return</span> controller;<br>    &#125;<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> NamesrvController <span class="hljs-title function_">start</span><span class="hljs-params">(<span class="hljs-keyword">final</span> NamesrvController controller)</span> <span class="hljs-keyword">throws</span> Exception &#123;<br>        <span class="hljs-comment">// 初始化 NamesrvController</span><br>        <span class="hljs-type">boolean</span> <span class="hljs-variable">initResult</span> <span class="hljs-operator">=</span> controller.initialize();<br>        <span class="hljs-keyword">if</span> (!initResult) &#123;<br>            controller.shutdown();<br>            System.exit(-<span class="hljs-number">3</span>);<br>        &#125;<br><br>        Runtime.getRuntime().addShutdownHook(<span class="hljs-keyword">new</span> <span class="hljs-title class_">ShutdownHookThread</span>(log, (Callable&lt;Void&gt;) () -&gt; &#123;<br>            controller.shutdown();<br>            <span class="hljs-keyword">return</span> <span class="hljs-literal">null</span>;<br>        &#125;));<br>        <span class="hljs-comment">// 启动 NamesrvController</span><br>        controller.start();<br>        <span class="hljs-keyword">return</span> controller;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<h3 id="配置解析"><a href="#配置解析" class="headerlink" title="配置解析"></a>配置解析</h3><ol>
<li>配置解析主要逻辑就是将 命令行参数读取出来解析成 <code>CommandLine</code> 对象</li>
<li>判断选项中是否包含 <code>-c</code> 选项( <code>-c</code> 是用来指定配置文件的 )， 若包含，则需要读取文件内容到 <code>Properties</code> 中</li>
<li>将所有配置信息设置到 <code>NamesrvConfig</code> 和 <code>NettyServerConfig</code> 中，设置的方式就是通过反射获取这两个对象的 <code>set</code> 方法，然后将获取的属性和配置中的 <code>key</code> 进行对比，相同则设置值</li>
</ol>
<h3 id="注册-DefaultRequestProcessor-处理器（重点）"><a href="#注册-DefaultRequestProcessor-处理器（重点）" class="headerlink" title="注册 DefaultRequestProcessor 处理器（重点）"></a>注册 DefaultRequestProcessor 处理器（重点）</h3><p><code>Nameserver</code> 需要和 <code>RocketMQ</code> 中其他组件通信，也就是需要处理各种请求，这些请求最终都是通过 <code>DefaultRequestProcessor</code> 进行处理的， 接下来就是看 <code>DefaultRequestProcessor</code> 的代码</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">DefaultRequestProcessor</span> <span class="hljs-keyword">extends</span> <span class="hljs-title class_">AsyncNettyRequestProcessor</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">NettyRequestProcessor</span> &#123;<br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-type">InternalLogger</span> <span class="hljs-variable">log</span> <span class="hljs-operator">=</span> InternalLoggerFactory.getLogger(LoggerName.NAMESRV_LOGGER_NAME);<br><br>    <span class="hljs-keyword">protected</span> <span class="hljs-keyword">final</span> NamesrvController namesrvController;<br><br>    <span class="hljs-comment">// 创建 DefaultRequestProcessor 的时候会将 NamesrvController 传入</span><br>    <span class="hljs-keyword">public</span> <span class="hljs-title function_">DefaultRequestProcessor</span><span class="hljs-params">(NamesrvController namesrvController)</span> &#123;<br>        <span class="hljs-built_in">this</span>.namesrvController = namesrvController;<br>    &#125;<br><br>    <span class="hljs-comment">// 这里就是核心的逻辑，Nameserver 可以处理哪些请求这里都有列举出来</span><br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> RemotingCommand <span class="hljs-title function_">processRequest</span><span class="hljs-params">(ChannelHandlerContext ctx,</span><br><span class="hljs-params">        RemotingCommand request)</span> <span class="hljs-keyword">throws</span> RemotingCommandException &#123;<br>        <span class="hljs-keyword">switch</span> (request.getCode()) &#123;<br>            <span class="hljs-keyword">case</span> RequestCode.PUT_KV_CONFIG:<br>                <span class="hljs-keyword">return</span> <span class="hljs-built_in">this</span>.putKVConfig(ctx, request);<br>            <span class="hljs-keyword">case</span> RequestCode.GET_KV_CONFIG:<br>                <span class="hljs-keyword">return</span> <span class="hljs-built_in">this</span>.getKVConfig(ctx, request);<br>            <span class="hljs-keyword">case</span> RequestCode.DELETE_KV_CONFIG:<br>                <span class="hljs-keyword">return</span> <span class="hljs-built_in">this</span>.deleteKVConfig(ctx, request);<br>            <span class="hljs-keyword">case</span> RequestCode.QUERY_DATA_VERSION:<br>                <span class="hljs-keyword">return</span> queryBrokerTopicConfig(ctx, request);<br>            <span class="hljs-keyword">case</span> RequestCode.REGISTER_BROKER:<br>                <span class="hljs-type">Version</span> <span class="hljs-variable">brokerVersion</span> <span class="hljs-operator">=</span> MQVersion.value2Version(request.getVersion());<br>                <span class="hljs-keyword">if</span> (brokerVersion.ordinal() &gt;= MQVersion.Version.V3_0_11.ordinal()) &#123;<br>                    <span class="hljs-keyword">return</span> <span class="hljs-built_in">this</span>.registerBrokerWithFilterServer(ctx, request);<br>                &#125; <span class="hljs-keyword">else</span> &#123;<br>                    <span class="hljs-keyword">return</span> <span class="hljs-built_in">this</span>.registerBroker(ctx, request);<br>                &#125;<br>            <span class="hljs-keyword">case</span> RequestCode.UNREGISTER_BROKER:<br>                <span class="hljs-keyword">return</span> <span class="hljs-built_in">this</span>.unregisterBroker(ctx, request);<br>            <span class="hljs-keyword">case</span> RequestCode.GET_ROUTEINFO_BY_TOPIC:<br>                <span class="hljs-keyword">return</span> <span class="hljs-built_in">this</span>.getRouteInfoByTopic(ctx, request);<br>            <span class="hljs-keyword">case</span> RequestCode.GET_BROKER_CLUSTER_INFO:<br>                <span class="hljs-keyword">return</span> <span class="hljs-built_in">this</span>.getBrokerClusterInfo(ctx, request);<br>            <span class="hljs-keyword">case</span> RequestCode.WIPE_WRITE_PERM_OF_BROKER:<br>                <span class="hljs-keyword">return</span> <span class="hljs-built_in">this</span>.wipeWritePermOfBroker(ctx, request);<br>            <span class="hljs-keyword">case</span> RequestCode.ADD_WRITE_PERM_OF_BROKER:<br>                <span class="hljs-keyword">return</span> <span class="hljs-built_in">this</span>.addWritePermOfBroker(ctx, request);<br>            <span class="hljs-keyword">case</span> RequestCode.GET_ALL_TOPIC_LIST_FROM_NAMESERVER:<br>                <span class="hljs-keyword">return</span> getAllTopicListFromNameserver(ctx, request);<br>            <span class="hljs-keyword">case</span> RequestCode.DELETE_TOPIC_IN_NAMESRV:<br>                <span class="hljs-keyword">return</span> deleteTopicInNamesrv(ctx, request);<br>            <span class="hljs-keyword">case</span> RequestCode.GET_KVLIST_BY_NAMESPACE:<br>                <span class="hljs-keyword">return</span> <span class="hljs-built_in">this</span>.getKVListByNamespace(ctx, request);<br>            <span class="hljs-keyword">case</span> RequestCode.GET_TOPICS_BY_CLUSTER:<br>                <span class="hljs-keyword">return</span> <span class="hljs-built_in">this</span>.getTopicsByCluster(ctx, request);<br>            <span class="hljs-keyword">case</span> RequestCode.GET_SYSTEM_TOPIC_LIST_FROM_NS:<br>                <span class="hljs-keyword">return</span> <span class="hljs-built_in">this</span>.getSystemTopicListFromNs(ctx, request);<br>            <span class="hljs-keyword">case</span> RequestCode.GET_UNIT_TOPIC_LIST:<br>                <span class="hljs-keyword">return</span> <span class="hljs-built_in">this</span>.getUnitTopicList(ctx, request);<br>            <span class="hljs-keyword">case</span> RequestCode.GET_HAS_UNIT_SUB_TOPIC_LIST:<br>                <span class="hljs-keyword">return</span> <span class="hljs-built_in">this</span>.getHasUnitSubTopicList(ctx, request);<br>            <span class="hljs-keyword">case</span> RequestCode.GET_HAS_UNIT_SUB_UNUNIT_TOPIC_LIST:<br>                <span class="hljs-keyword">return</span> <span class="hljs-built_in">this</span>.getHasUnitSubUnUnitTopicList(ctx, request);<br>            <span class="hljs-keyword">case</span> RequestCode.UPDATE_NAMESRV_CONFIG:<br>                <span class="hljs-keyword">return</span> <span class="hljs-built_in">this</span>.updateConfig(ctx, request);<br>            <span class="hljs-keyword">case</span> RequestCode.GET_NAMESRV_CONFIG:<br>                <span class="hljs-keyword">return</span> <span class="hljs-built_in">this</span>.getConfig(ctx, request);<br>            <span class="hljs-keyword">default</span>:<br>                <span class="hljs-keyword">break</span>;<br>        &#125;<br>        <span class="hljs-keyword">return</span> <span class="hljs-literal">null</span>;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>
<p>接下来根据实际场景看下特定请求的实现逻辑</p>
<h4 id="场景1：Broker-注册到-NameServer-RequestCode-REGISTER-BROKER"><a href="#场景1：Broker-注册到-NameServer-RequestCode-REGISTER-BROKER" class="headerlink" title="场景1：Broker 注册到 NameServer (RequestCode.REGISTER_BROKER)"></a>场景1：Broker 注册到 NameServer (RequestCode.REGISTER_BROKER)</h4><p>在 <code>NameServer</code> 中处理 <code>Broker</code> 注册和保存 <code>Broker</code>，<code>Topic</code>, <code>Queue</code> 相关数据的类是 <code>RouteInfoManager</code>, 看一下这个类的几个属性</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">RouteInfoManager</span> &#123;<br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> <span class="hljs-keyword">static</span> <span class="hljs-type">long</span> <span class="hljs-variable">BROKER_CHANNEL_EXPIRED_TIME</span> <span class="hljs-operator">=</span> <span class="hljs-number">1000</span> * <span class="hljs-number">60</span> * <span class="hljs-number">2</span>;<br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> <span class="hljs-type">ReadWriteLock</span> <span class="hljs-variable">lock</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">ReentrantReadWriteLock</span>();<br>    <span class="hljs-comment">// 记录 topic，broker, queue 的关系</span><br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> HashMap&lt;String<span class="hljs-comment">/* topic */</span>, Map&lt;String <span class="hljs-comment">/* brokerName */</span> , QueueData&gt;&gt; topicQueueTable;<br>    <span class="hljs-comment">// 记录 broker 的地址</span><br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> HashMap&lt;String<span class="hljs-comment">/* brokerName */</span>, BrokerData&gt; brokerAddrTable;<br>    <span class="hljs-comment">// 记录每个集群中 broker 的名称</span><br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> HashMap&lt;String<span class="hljs-comment">/* clusterName */</span>, Set&lt;String<span class="hljs-comment">/* brokerName */</span>&gt;&gt; clusterAddrTable;<br>    <span class="hljs-comment">// 记录存活的broker 列表， brokerAddr 是类似 192.168.0.100:10911 这样的值</span><br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> HashMap&lt;String<span class="hljs-comment">/* brokerAddr */</span>, BrokerLiveInfo&gt; brokerLiveTable;<br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> HashMap&lt;String<span class="hljs-comment">/* brokerAddr */</span>, List&lt;String&gt;<span class="hljs-comment">/* Filter Server */</span>&gt; filterServerTable;<br>&#125;<br></code></pre></td></tr></table></figure>
<p>知道了 <code>RouteInfoManager</code> 类重要的属性，接下来先给出注册的结论然后再去看注册的逻辑<br><strong>注册流程总结</strong></p>
<pre class="mermaid">graph LR
    A["解析请求头，获取
    Broker 相关数据"]
    B["解析请求体，获取
    Topic 相关数据"]
    C["注册 Broker 相关数据"]
    D["注册 Topic 相关数据"]
    A-->B-->C-->D</pre>


<ul>
<li>根据 <code>RemotingCommand</code> 请求指令解析出 <code>RegisterBrokerRequestHeader</code>， 这个主要是从请求中获取 <code>BrokerName</code>, <code>BrokerAddr</code>, <code>BrokerId</code> 等信息，这个是获取<strong>请求头</strong>的数据</li>
<li>根据 <code>RemotingCommand</code> 请求指令解析出 <code>RegisterBrokerBody</code>， 这个就是要获取 <strong>请求体</strong> 的数据了，主要是获取 <code>ConcurrentMap&lt;String, TopicConfig&gt; topicConfigTable =new ConcurrentHashMap&lt;String, TopicConfig&gt;()</code>， 也就是 <code>Topic</code> 相关的数据</li>
<li>请求中的数据都拿出来了，就需要调用 <code>RouteInfoManager.registerBroker()</code> 方法将这些数据放入 <code>RouteInfoManager</code> 类的相关属性中了，注册也是分成两个部分<ul>
<li>注册 <code>Broker</code> 相关数据</li>
<li>注册 <code>Topic</code> 相关数据</li>
</ul>
</li>
</ul>
<p><strong>注册流程代码（下面代码只展示了方法中与注册相关的代码）</strong><br><code>DefaultRequestProcessor</code> 类</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> RemotingCommand <span class="hljs-title function_">processRequest</span><span class="hljs-params">(ChannelHandlerContext ctx,</span><br><span class="hljs-params">    RemotingCommand request)</span> <span class="hljs-keyword">throws</span> RemotingCommandException &#123;<br>    <span class="hljs-keyword">switch</span> (request.getCode()) &#123;<br>        <span class="hljs-keyword">case</span> RequestCode.REGISTER_BROKER:<br>            <span class="hljs-type">Version</span> <span class="hljs-variable">brokerVersion</span> <span class="hljs-operator">=</span> MQVersion.value2Version(request.getVersion());<br>            <span class="hljs-keyword">if</span> (brokerVersion.ordinal() &gt;= MQVersion.Version.V3_0_11.ordinal()) &#123;<br>                <span class="hljs-comment">// 使用的 RocketMQ4.9.7 版本会是走这个逻辑</span><br>                <span class="hljs-keyword">return</span> <span class="hljs-built_in">this</span>.registerBrokerWithFilterServer(ctx, request);<br>            &#125; <span class="hljs-keyword">else</span> &#123;<br>                <span class="hljs-keyword">return</span> <span class="hljs-built_in">this</span>.registerBroker(ctx, request);<br>            &#125;<br>        <span class="hljs-keyword">default</span>:<br>            <span class="hljs-keyword">break</span>;<br>    &#125;<br>    <span class="hljs-keyword">return</span> <span class="hljs-literal">null</span>;<br>&#125;<br><br><span class="hljs-keyword">public</span> RemotingCommand <span class="hljs-title function_">registerBrokerWithFilterServer</span><span class="hljs-params">(ChannelHandlerContext ctx, RemotingCommand request)</span><br>        <span class="hljs-keyword">throws</span> RemotingCommandException &#123;<br>    <span class="hljs-keyword">final</span> <span class="hljs-type">RemotingCommand</span> <span class="hljs-variable">response</span> <span class="hljs-operator">=</span> RemotingCommand.createResponseCommand(RegisterBrokerResponseHeader.class);<br>    <span class="hljs-keyword">final</span> <span class="hljs-type">RegisterBrokerResponseHeader</span> <span class="hljs-variable">responseHeader</span> <span class="hljs-operator">=</span> (RegisterBrokerResponseHeader) response.readCustomHeader();<br>    <span class="hljs-comment">// 获取请求头的数据，主要是获取 BrokerName, BrokerAddr, BrokerId 等数据</span><br>    <span class="hljs-keyword">final</span> <span class="hljs-type">RegisterBrokerRequestHeader</span> <span class="hljs-variable">requestHeader</span> <span class="hljs-operator">=</span><br>        (RegisterBrokerRequestHeader) request.decodeCommandCustomHeader(RegisterBrokerRequestHeader.class);<br><br>    <span class="hljs-keyword">if</span> (!checksum(ctx, request, requestHeader)) &#123;<br>        response.setCode(ResponseCode.SYSTEM_ERROR);<br>        response.setRemark(<span class="hljs-string">&quot;crc32 not match&quot;</span>);<br>        <span class="hljs-keyword">return</span> response;<br>    &#125;<br><br>    <span class="hljs-type">RegisterBrokerBody</span> <span class="hljs-variable">registerBrokerBody</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">RegisterBrokerBody</span>();<br><br>    <span class="hljs-keyword">if</span> (request.getBody() != <span class="hljs-literal">null</span>) &#123;<br>        <span class="hljs-keyword">try</span> &#123;<br>            <span class="hljs-comment">// 获取请求体的数据，主要是获取 Topic 相关的数据</span><br>            registerBrokerBody = RegisterBrokerBody.decode(request.getBody(), requestHeader.isCompressed());<br>        &#125; <span class="hljs-keyword">catch</span> (Exception e) &#123;<br>            <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">RemotingCommandException</span>(<span class="hljs-string">&quot;Failed to decode RegisterBrokerBody&quot;</span>, e);<br>        &#125;<br>    &#125; <span class="hljs-keyword">else</span> &#123;<br>        registerBrokerBody.getTopicConfigSerializeWrapper().getDataVersion().setCounter(<span class="hljs-keyword">new</span> <span class="hljs-title class_">AtomicLong</span>(<span class="hljs-number">0</span>));<br>        registerBrokerBody.getTopicConfigSerializeWrapper().getDataVersion().setTimestamp(<span class="hljs-number">0</span>);<br>    &#125;<br><br>    <span class="hljs-comment">// 这里就是将请求中的数据获取出来后进行注册(所谓的注册就是将这些信息保存在内存中)</span><br>    <span class="hljs-comment">// 【重点代码1】</span><br>    <span class="hljs-type">RegisterBrokerResult</span> <span class="hljs-variable">result</span> <span class="hljs-operator">=</span> <span class="hljs-built_in">this</span>.namesrvController.getRouteInfoManager().registerBroker(<br>        requestHeader.getClusterName(),<br>        requestHeader.getBrokerAddr(),<br>        requestHeader.getBrokerName(),<br>        requestHeader.getBrokerId(),<br>        requestHeader.getHaServerAddr(),<br>        registerBrokerBody.getTopicConfigSerializeWrapper(),<br>        registerBrokerBody.getFilterServerList(),<br>        ctx.channel());<br><br>    responseHeader.setHaServerAddr(result.getHaServerAddr());<br>    responseHeader.setMasterAddr(result.getMasterAddr());<br><br>    <span class="hljs-type">byte</span>[] jsonValue = <span class="hljs-built_in">this</span>.namesrvController.getKvConfigManager().getKVListByNamespace(NamesrvUtil.NAMESPACE_ORDER_TOPIC_CONFIG);<br>    response.setBody(jsonValue);<br><br>    response.setCode(ResponseCode.SUCCESS);<br>    response.setRemark(<span class="hljs-literal">null</span>);<br>    <span class="hljs-keyword">return</span> response;<br>&#125;<br></code></pre></td></tr></table></figure>

<p>接下来就 <strong>【重点代码1】</strong>， 即将解析出来的请求中的数据保存在 <code>RouteInfoManager</code> 类的相关属性中的逻辑</p>
<p><code>RouteInfoManager</code> 类</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> RegisterBrokerResult <span class="hljs-title function_">registerBroker</span><span class="hljs-params">(</span><br><span class="hljs-params">        <span class="hljs-keyword">final</span> String clusterName,</span><br><span class="hljs-params">        <span class="hljs-keyword">final</span> String brokerAddr,</span><br><span class="hljs-params">        <span class="hljs-keyword">final</span> String brokerName,</span><br><span class="hljs-params">        <span class="hljs-keyword">final</span> <span class="hljs-type">long</span> brokerId,</span><br><span class="hljs-params">        <span class="hljs-keyword">final</span> String haServerAddr,</span><br><span class="hljs-params">        <span class="hljs-keyword">final</span> TopicConfigSerializeWrapper topicConfigWrapper,</span><br><span class="hljs-params">        <span class="hljs-keyword">final</span> List&lt;String&gt; filterServerList,</span><br><span class="hljs-params">        <span class="hljs-keyword">final</span> Channel channel)</span> &#123;<br>    <span class="hljs-type">RegisterBrokerResult</span> <span class="hljs-variable">result</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">RegisterBrokerResult</span>();<br>    <span class="hljs-keyword">try</span> &#123;<br>        <span class="hljs-keyword">try</span> &#123;<br>            <span class="hljs-built_in">this</span>.lock.writeLock().lockInterruptibly();<br><br>            Set&lt;String&gt; brokerNames = <span class="hljs-built_in">this</span>.clusterAddrTable.computeIfAbsent(clusterName, k -&gt; <span class="hljs-keyword">new</span> <span class="hljs-title class_">HashSet</span>&lt;&gt;());<br>            brokerNames.add(brokerName);<br><br>            <span class="hljs-type">boolean</span> <span class="hljs-variable">registerFirst</span> <span class="hljs-operator">=</span> <span class="hljs-literal">false</span>;<br><br>            <span class="hljs-type">BrokerData</span> <span class="hljs-variable">brokerData</span> <span class="hljs-operator">=</span> <span class="hljs-built_in">this</span>.brokerAddrTable.get(brokerName);<br>            <span class="hljs-keyword">if</span> (<span class="hljs-literal">null</span> == brokerData) &#123;<br>                registerFirst = <span class="hljs-literal">true</span>;<br>                <span class="hljs-comment">// 如果是第一次注册，则构造 BrokerData 对象，保存在 brokerAddrTable 中</span><br>                brokerData = <span class="hljs-keyword">new</span> <span class="hljs-title class_">BrokerData</span>(clusterName, brokerName, <span class="hljs-keyword">new</span> <span class="hljs-title class_">HashMap</span>&lt;&gt;());<br>                <span class="hljs-built_in">this</span>.brokerAddrTable.put(brokerName, brokerData);<br>            &#125;<br>            Map&lt;Long, String&gt; brokerAddrsMap = brokerData.getBrokerAddrs();<br>            <span class="hljs-comment">//Switch slave to master: first remove &lt;1, IP:PORT&gt; in namesrv, then add &lt;0, IP:PORT&gt;</span><br>            <span class="hljs-comment">//The same IP:PORT must only have one record in brokerAddrTable</span><br>            Iterator&lt;Entry&lt;Long, String&gt;&gt; it = brokerAddrsMap.entrySet().iterator();<br>            <span class="hljs-keyword">while</span> (it.hasNext()) &#123;<br>                Entry&lt;Long, String&gt; item = it.next();<br>                <span class="hljs-keyword">if</span> (<span class="hljs-literal">null</span> != brokerAddr &amp;&amp; brokerAddr.equals(item.getValue()) &amp;&amp; brokerId != item.getKey()) &#123;<br>                    log.debug(<span class="hljs-string">&quot;remove entry &#123;&#125; from brokerData&quot;</span>, item);<br>                    it.remove();<br>                &#125;<br>            &#125;<br><br>            <span class="hljs-type">String</span> <span class="hljs-variable">oldAddr</span> <span class="hljs-operator">=</span> brokerData.getBrokerAddrs().put(brokerId, brokerAddr);<br>            <span class="hljs-keyword">if</span> (MixAll.MASTER_ID == brokerId) &#123;<br>                log.info(<span class="hljs-string">&quot;cluster [&#123;&#125;] brokerName [&#123;&#125;] master address change from &#123;&#125; to &#123;&#125;&quot;</span>,<br>                        brokerData.getCluster(), brokerData.getBrokerName(), oldAddr, brokerAddr);<br>            &#125;<br><br>            registerFirst = registerFirst || (<span class="hljs-literal">null</span> == oldAddr);<br><br>            <span class="hljs-comment">// 前面是注册 Broker 相关的逻辑（即保存 Broker相关数据）</span><br>            <span class="hljs-comment">// 接下来就是处理 Topic 相关的注册</span><br>            <span class="hljs-keyword">if</span> (<span class="hljs-literal">null</span> != topicConfigWrapper &amp;&amp; MixAll.MASTER_ID == brokerId) &#123;<br>                <span class="hljs-comment">// 只有 Topic 数据变化了，或者是第一次注册才需要处理</span><br>                <span class="hljs-keyword">if</span> (<span class="hljs-built_in">this</span>.isBrokerTopicConfigChanged(brokerAddr, topicConfigWrapper.getDataVersion())<br>                        || registerFirst) &#123;<br>                    ConcurrentMap&lt;String, TopicConfig&gt; tcTable =<br>                            topicConfigWrapper.getTopicConfigTable();<br>                    <span class="hljs-keyword">if</span> (tcTable != <span class="hljs-literal">null</span>) &#123;<br>                        <span class="hljs-keyword">for</span> (Map.Entry&lt;String, TopicConfig&gt; entry : tcTable.entrySet()) &#123;<br>                            <span class="hljs-comment">// 注册 Topic , 实际就是将数据存储 RouteInfoManager 类的</span><br>                            <span class="hljs-comment">// HashMap&lt;String/* topic */, Map&lt;String /* brokerName */ , QueueData&gt;&gt; topicQueueTable; 属性中</span><br>                            <span class="hljs-built_in">this</span>.createAndUpdateQueueData(brokerName, entry.getValue());<br>                        &#125;<br>                    &#125;<br>                &#125;<br>            &#125;<br><br>            <span class="hljs-type">BrokerLiveInfo</span> <span class="hljs-variable">prevBrokerLiveInfo</span> <span class="hljs-operator">=</span> <span class="hljs-built_in">this</span>.brokerLiveTable.put(brokerAddr,<br>                    <span class="hljs-keyword">new</span> <span class="hljs-title class_">BrokerLiveInfo</span>(<br>                            System.currentTimeMillis(),<br>                            topicConfigWrapper.getDataVersion(),<br>                            channel,<br>                            haServerAddr));<br>            <span class="hljs-keyword">if</span> (<span class="hljs-literal">null</span> == prevBrokerLiveInfo) &#123;<br>                log.info(<span class="hljs-string">&quot;new broker registered, &#123;&#125; HAServer: &#123;&#125;&quot;</span>, brokerAddr, haServerAddr);<br>            &#125;<br><br>            <span class="hljs-keyword">if</span> (filterServerList != <span class="hljs-literal">null</span>) &#123;<br>                <span class="hljs-keyword">if</span> (filterServerList.isEmpty()) &#123;<br>                    <span class="hljs-built_in">this</span>.filterServerTable.remove(brokerAddr);<br>                &#125; <span class="hljs-keyword">else</span> &#123;<br>                    <span class="hljs-built_in">this</span>.filterServerTable.put(brokerAddr, filterServerList);<br>                &#125;<br>            &#125;<br><br>            <span class="hljs-keyword">if</span> (MixAll.MASTER_ID != brokerId) &#123;<br>                <span class="hljs-type">String</span> <span class="hljs-variable">masterAddr</span> <span class="hljs-operator">=</span> brokerData.getBrokerAddrs().get(MixAll.MASTER_ID);<br>                <span class="hljs-keyword">if</span> (masterAddr != <span class="hljs-literal">null</span>) &#123;<br>                    <span class="hljs-type">BrokerLiveInfo</span> <span class="hljs-variable">brokerLiveInfo</span> <span class="hljs-operator">=</span> <span class="hljs-built_in">this</span>.brokerLiveTable.get(masterAddr);<br>                    <span class="hljs-keyword">if</span> (brokerLiveInfo != <span class="hljs-literal">null</span>) &#123;<br>                        result.setHaServerAddr(brokerLiveInfo.getHaServerAddr());<br>                        result.setMasterAddr(masterAddr);<br>                    &#125;<br>                &#125;<br>            &#125;<br>        &#125; <span class="hljs-keyword">finally</span> &#123;<br>            <span class="hljs-built_in">this</span>.lock.writeLock().unlock();<br>        &#125;<br>    &#125; <span class="hljs-keyword">catch</span> (Exception e) &#123;<br>        log.error(<span class="hljs-string">&quot;registerBroker Exception&quot;</span>, e);<br>    &#125;<br><br>    <span class="hljs-keyword">return</span> result;<br>&#125;<br></code></pre></td></tr></table></figure>

<h4 id="场景2：Producer-启动时获取-Broker-相关信息-RequestCode-GET-ROUTEINFO-BY-TOPIC"><a href="#场景2：Producer-启动时获取-Broker-相关信息-RequestCode-GET-ROUTEINFO-BY-TOPIC" class="headerlink" title="场景2：Producer 启动时获取 Broker 相关信息 (RequestCode.GET_ROUTEINFO_BY_TOPIC)"></a>场景2：Producer 启动时获取 Broker 相关信息 (RequestCode.GET_ROUTEINFO_BY_TOPIC)</h4><p>获取信息命令是 <code>RequestCode.GET_ROUTEINFO_BY_TOPIC</code>， 调用的是 <code>DefaultRequestProcessor#getRouteInfoByTopic</code> 方法</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> RemotingCommand <span class="hljs-title function_">getRouteInfoByTopic</span><span class="hljs-params">(ChannelHandlerContext ctx,</span><br><span class="hljs-params">    RemotingCommand request)</span> <span class="hljs-keyword">throws</span> RemotingCommandException &#123;<br>    <span class="hljs-keyword">final</span> <span class="hljs-type">RemotingCommand</span> <span class="hljs-variable">response</span> <span class="hljs-operator">=</span> RemotingCommand.createResponseCommand(<span class="hljs-literal">null</span>);<br>    <span class="hljs-comment">// 同样是解析请求头的数据，转换成 GetRouteInfoRequestHeader</span><br>    <span class="hljs-keyword">final</span> <span class="hljs-type">GetRouteInfoRequestHeader</span> <span class="hljs-variable">requestHeader</span> <span class="hljs-operator">=</span><br>        (GetRouteInfoRequestHeader) request.decodeCommandCustomHeader(GetRouteInfoRequestHeader.class);<br><br>    <span class="hljs-comment">// 根据从请求头中解析出来的 Topic 名称从 RouteInfoManager 中获取数据 【重点代码2】</span><br>    <span class="hljs-type">TopicRouteData</span> <span class="hljs-variable">topicRouteData</span> <span class="hljs-operator">=</span> <span class="hljs-built_in">this</span>.namesrvController.getRouteInfoManager().pickupTopicRouteData(requestHeader.getTopic());<br>    <span class="hljs-comment">// 省略构造相应的代码 ……</span><br>    response.setCode(ResponseCode.TOPIC_NOT_EXIST);<br>    response.setRemark(<span class="hljs-string">&quot;No topic route info in name server for the topic: &quot;</span> + requestHeader.getTopic()<br>        + FAQUrl.suggestTodo(FAQUrl.APPLY_TOPIC_URL));<br>    <span class="hljs-keyword">return</span> response;<br>&#125;<br></code></pre></td></tr></table></figure>

<p>接下来就看 <strong>【重点代码2】</strong>， 这是从 <code>RouteInfoManager</code> 中获取数据, 前面讲解 <strong>Broker注册</strong> 时已经讲到 <code>Topic</code> 相关的数据是放入 <code>RouteInfoManager</code> 类的 <code>private final HashMap&lt;String/* topic */, Map&lt;String /* brokerName */ , QueueData&gt;&gt; topicQueueTable;</code> 结构中，<code>Broker</code> 的地址信息是放入 <code>private final HashMap&lt;String/* brokerName */, BrokerData&gt; brokerAddrTable;</code> 结构中，所以获取 <code>Topic</code> 主要就是从这两个结构中获取数据。</p>
<h3 id="启动定时任务清理下线的-Broker"><a href="#启动定时任务清理下线的-Broker" class="headerlink" title="启动定时任务清理下线的 Broker"></a>启动定时任务清理下线的 Broker</h3><p>清理下线的 <code>Broker</code> 调用流程如下, 以下代码省略与此次分析无关的代码</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> NamesrvController <span class="hljs-title function_">start</span><span class="hljs-params">(<span class="hljs-keyword">final</span> NamesrvController controller)</span> <span class="hljs-keyword">throws</span> Exception &#123;<br>    <span class="hljs-type">boolean</span> <span class="hljs-variable">initResult</span> <span class="hljs-operator">=</span> controller.initialize();<br>&#125;<br><br><span class="hljs-keyword">public</span> <span class="hljs-type">boolean</span> <span class="hljs-title function_">initialize</span><span class="hljs-params">()</span> &#123;<br>    <span class="hljs-comment">//  定时清除的逻辑是在 RouteInfoManager#scanNotActiveBroker 方法中进行的</span><br>    <span class="hljs-built_in">this</span>.scheduledExecutorService.scheduleAtFixedRate(NamesrvController.<span class="hljs-built_in">this</span>.routeInfoManager::scanNotActiveBroker, <span class="hljs-number">5</span>, <span class="hljs-number">10</span>, TimeUnit.SECONDS);<br>    <span class="hljs-keyword">return</span> <span class="hljs-literal">true</span>;<br>&#125;<br></code></pre></td></tr></table></figure>

<p><code>RouteInfoManager</code> 类</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-comment">//  2分钟</span><br><span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> <span class="hljs-keyword">static</span> <span class="hljs-type">long</span> <span class="hljs-variable">BROKER_CHANNEL_EXPIRED_TIME</span> <span class="hljs-operator">=</span> <span class="hljs-number">1000</span> * <span class="hljs-number">60</span> * <span class="hljs-number">2</span>;<br><span class="hljs-comment">// brokerLiveTable 保存的是存活的 Broker 数据</span><br><span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> HashMap&lt;String<span class="hljs-comment">/* brokerAddr */</span>, BrokerLiveInfo&gt; brokerLiveTable;<br><br><span class="hljs-keyword">public</span> <span class="hljs-type">int</span> <span class="hljs-title function_">scanNotActiveBroker</span><span class="hljs-params">()</span> &#123;<br>    <span class="hljs-type">int</span> <span class="hljs-variable">removeCount</span> <span class="hljs-operator">=</span> <span class="hljs-number">0</span>;<br>    <span class="hljs-comment">//  首先将所有存活的 Broker 数据进行遍历</span><br>    Iterator&lt;Entry&lt;String, BrokerLiveInfo&gt;&gt; it = <span class="hljs-built_in">this</span>.brokerLiveTable.entrySet().iterator();<br>    <span class="hljs-keyword">while</span> (it.hasNext()) &#123;<br>        Entry&lt;String, BrokerLiveInfo&gt; next = it.next();<br>        <span class="hljs-type">long</span> <span class="hljs-variable">last</span> <span class="hljs-operator">=</span> next.getValue().getLastUpdateTimestamp();<br>        <span class="hljs-comment">// 判断最新更新时间是否已经超过 2 分钟，如果超过 2 分钟则移除</span><br>        <span class="hljs-keyword">if</span> ((last + BROKER_CHANNEL_EXPIRED_TIME) &lt; System.currentTimeMillis()) &#123;<br>            RemotingUtil.closeChannel(next.getValue().getChannel());<br>            it.remove();<br>            log.warn(<span class="hljs-string">&quot;The broker channel expired, &#123;&#125; &#123;&#125;ms&quot;</span>, next.getKey(), BROKER_CHANNEL_EXPIRED_TIME);<br>            <span class="hljs-built_in">this</span>.onChannelDestroy(next.getKey(), next.getValue().getChannel());<br>            removeCount++;<br>        &#125;<br>    &#125;<br>    <span class="hljs-keyword">return</span> removeCount;<br>&#125;<br></code></pre></td></tr></table></figure>

<p>至此 <code>Nameserver</code> 相关功能梳理完成，核心点在两个类</p>
<ul>
<li><code>DefaultRequestProcessor</code> 的 <code>processRequest</code> 方法，这是所有请求逻辑处理的入口</li>
<li><code>RouteInfoManager</code> 类，<code>Broker</code> 注册数据是放在该类中，<code>Produer</code> 和 <code>Consumer</code> 获取 <code>Broker</code> 数据时也是从该类获取</li>
</ul>
]]></content>
      <categories>
        <category>中间件</category>
        <category>RocketMQ</category>
      </categories>
      <tags>
        <tag>RocketMQ</tag>
      </tags>
  </entry>
  <entry>
    <title>RocketMQ4版本以集群模式启动Broker</title>
    <url>/2023/f0b30d177028/index.html</url>
    <content><![CDATA[<blockquote>
<p>RocketMQ版本: 4.9.7<br>JDK版本： 17</p>
</blockquote>
<p>对于单机版运行 <code>RocketMQ4</code> 或 <code>RocketMQ5</code> 版本都可以参考</p>
<ol><li><a href="/2023/49fb249febb6/index.html" title="RocketMQ安装使用和基本概念">RocketMQ安装使用和基本概念</a></li></ol>

<p>对于源码编译出现的问题可以参考</p>
<ol><li><a href="/2023/2adf4cc6108b/index.html" title="使用idea编译RocketMQ5源码">使用idea编译RocketMQ5源码</a></li></ol>

<p>对于 <code>RocketMQ4</code> 版本使用 <code>jdk17</code> 出现的问题解决方案可以参考</p>
<ol><li><a href="/2023/69cb00fc039e/index.html" title="RocketMQ4使用jdk17启动出现的问题">RocketMQ4使用jdk17启动出现的问题</a></li></ol>

<h2 id="创建各自-Broker-的-ROCKETMQ-HOME-目录"><a href="#创建各自-Broker-的-ROCKETMQ-HOME-目录" class="headerlink" title="创建各自 Broker 的 ROCKETMQ_HOME 目录"></a>创建各自 Broker 的 ROCKETMQ_HOME 目录</h2><p>这里主要讲解的是本地使用源码启动两个(两个以上也一样) <code>Broker</code>，这样在调试源码的时候可以看到 <code>Broker</code> 处于集群状态的一些处理方式</p>
<p>创建 <code>ROCKETMQ_HOME</code> 目录，因为要在同一台电脑上启动两个 <code>Broker</code>, 所以每一个 <code>Broker</code> 都给一个独立的目录和配置，这里创建了 <code>D:\data\rocketmq</code>(以下简称 <code>rocketmq</code> ) 和 <code>D:\data\rocketmq2</code>(以下简称 <code>rocketmq2</code>)，如下图所示</p>
<p><img src="https://s2.loli.net/2023/12/21/SQEnai8qe1ILJhp.png" alt="ROCKETMQ_HOME"><br>在 <code>conf</code> 子目录中都有三个配置文件</p>
<ul>
<li><code>broker.conf</code></li>
<li><code>logback_broker.xml</code> 这个文件可以直接从 <code>rokcetmq</code> 源码的 <code>distribution</code> 子模块的 <code>conf</code> 目录中拷贝</li>
<li><code>logback_namesrv.xml</code> 这个文件可以直接从 <code>rokcetmq</code> 源码的 <code>distribution</code> 子模块的 <code>conf</code> 目录中拷贝</li>
</ul>
<div class="tabs" id="broker.conf-文件内容"><ul class="nav-tabs"><button type="button" class="tab  active" data-href="broker.conf-文件内容-1">rocketmq下broker.conf</button><button type="button" class="tab " data-href="broker.conf-文件内容-2">rocketmq2下broker.conf</button></ul><div class="tab-contents"><div class="tab-item-content active" id="broker.conf-文件内容-1"><p><code>rocketmq</code> 下 <code>conf</code> 下的 <code>broker.conf</code> 内容如下</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs conf">brokerClusterName = DefaultCluster<br>brokerName = broker-a<br>brokerId = 0<br>deleteWhen = 04<br>fileReservedTime = 48<br>brokerRole = ASYNC_MASTER<br>flushDiskType = ASYNC_FLUSH<br><br>namesrvAddr = 127.0.0.1:9876<br>storePathRootDir = D:\\data\\rocketmq\\store<br>storePathCommitLog = D:\\data\\rocketmq\\store\\commitlog<br>storePathConsumeQueue = D:\\data\\rocketmq\\store\\consumequeue<br>storePathIndex = D:\\data\\rocketmq\\store\\index<br>storeCheckpoint = D:\\data\\rocketmq\\store\\checkpoint<br></code></pre></td></tr></table></figure></div><div class="tab-item-content" id="broker.conf-文件内容-2"><p><code>rocketmq2</code> 下 <code>conf</code> 下的 <code>broker.conf</code> 内容如下</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs conf">brokerClusterName = DefaultCluster<br>brokerName = broker-b<br>brokerId = 0<br>deleteWhen = 04<br>fileReservedTime = 48<br>brokerRole = ASYNC_MASTER<br>flushDiskType = ASYNC_FLUSH<br>listenPort = 10915<br><br>namesrvAddr = 127.0.0.1:9876<br>storePathRootDir = D:\\data\\rocketmq2\\store<br>storePathCommitLog = D:\\data\\rocketmq2\\store\\commitlog<br>storePathConsumeQueue = D:\\data\\rocketmq2\\store\\consumequeue<br>storePathIndex = D:\\data\\rocketmq2\\store\\index<br>storeCheckpoint = D:\\data\\rocketmq2\\store\\checkpoint<br></code></pre></td></tr></table></figure></div></div><div class="tab-to-top"><button type="button" aria-label="scroll to top"><i class="fas fa-arrow-up"></i></button></div></div>


<p><code>logback_broker.xml</code> 和 <code>logback_namesrv.xml</code> 文件的修改<br>对于这两个文件，只需要将 <code>$&#123;user.home&#125;</code> 修改成对应的 <code>ROCKETMQ_HOME</code> 目录即可，比如对于 <code>D:\data\rocketmq\conf</code> 下的日志配置文件，将 <code>$&#123;user.home&#125;</code> 都替换成 <code>D:\data\rocketmq</code>，对于<code>D:\data\rocketmq2\conf</code> 下的日志配置文件，将 <code>$&#123;user.home&#125;</code> 都替换成 <code>D:\data\rocketmq2</code></p>
<h2 id="修改-idea-中的配置"><a href="#修改-idea-中的配置" class="headerlink" title="修改 idea 中的配置"></a>修改 idea 中的配置</h2><p>在 <code>idea</code> 的 <code>run/debug configuration</code> 中，添加 <code>Application</code>, 模块选择 <code>rocketmq-broker</code>, 类选择 <code>org.apache.rocketmq.broker.BrokerStartup</code>，需要添加两次，最终配置如下图所示<br><img src="https://s2.loli.net/2023/12/21/vFZh5J3qxVTXCSo.png" alt="Broker1的配置"></p>
<p><img src="https://s2.loli.net/2023/12/21/9VhWeDbRQZjuHES.png" alt="Broker2的配置"></p>
<p>配置完成就可以启动了</p>
]]></content>
      <categories>
        <category>中间件</category>
        <category>RocketMQ</category>
      </categories>
      <tags>
        <tag>RocketMQ</tag>
      </tags>
  </entry>
  <entry>
    <title>RocketMQ4.x中顺序消息的发送和消费</title>
    <url>/2023/0479976f9bff/index.html</url>
    <content><![CDATA[<p>RocketMQ版本： 4.9.7</p>
<h3 id="全局有序"><a href="#全局有序" class="headerlink" title="全局有序"></a>全局有序</h3><ol>
<li>只有一个生产者，并且生产者也是单线程发送</li>
<li>消息队列只有一个</li>
<li>消费者只有一个并且是单线程消费</li>
</ol>
<h3 id="分区有序"><a href="#分区有序" class="headerlink" title="分区有序"></a>分区有序</h3><p>如果只是要求局部有序，就可以将同一类有序的消息发送到同一个 <code>Queue</code>, 所以对于发送端而言重要的就是如何选择 <code>Queue</code>, 可以参考源码中 <code>example</code> 子模块下的 <code>org.apache.rocketmq.example.ordermessage.Producer</code> 类，该类的代码就是测试分区有序消息</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">Producer</span> &#123;<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">main</span><span class="hljs-params">(String[] args)</span> <span class="hljs-keyword">throws</span> UnsupportedEncodingException &#123;<br>        <span class="hljs-keyword">try</span> &#123;<br>            <span class="hljs-type">DefaultMQProducer</span> <span class="hljs-variable">producer</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">DefaultMQProducer</span>(<span class="hljs-string">&quot;please_rename_unique_group_name&quot;</span>);<br>            producer.start();<br><br>            String[] tags = <span class="hljs-keyword">new</span> <span class="hljs-title class_">String</span>[] &#123;<span class="hljs-string">&quot;TagA&quot;</span>, <span class="hljs-string">&quot;TagB&quot;</span>, <span class="hljs-string">&quot;TagC&quot;</span>, <span class="hljs-string">&quot;TagD&quot;</span>, <span class="hljs-string">&quot;TagE&quot;</span>&#125;;<br>            <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> <span class="hljs-variable">i</span> <span class="hljs-operator">=</span> <span class="hljs-number">0</span>; i &lt; <span class="hljs-number">100</span>; i++) &#123;<br>                <span class="hljs-type">int</span> <span class="hljs-variable">orderId</span> <span class="hljs-operator">=</span> i % <span class="hljs-number">10</span>;<br>                <span class="hljs-type">Message</span> <span class="hljs-variable">msg</span> <span class="hljs-operator">=</span><br>                    <span class="hljs-keyword">new</span> <span class="hljs-title class_">Message</span>(<span class="hljs-string">&quot;TopicTestjjj&quot;</span>, tags[i % tags.length], <span class="hljs-string">&quot;KEY&quot;</span> + i,<br>                        (<span class="hljs-string">&quot;Hello RocketMQ &quot;</span> + i).getBytes(RemotingHelper.DEFAULT_CHARSET));<br>                <span class="hljs-comment">// 调用 send 方法时，设置一个 MessageQueueSelector 类型的回调函数，用于指定消息发送到哪个队列</span><br>                <span class="hljs-type">SendResult</span> <span class="hljs-variable">sendResult</span> <span class="hljs-operator">=</span> producer.send(msg, <span class="hljs-keyword">new</span> <span class="hljs-title class_">MessageQueueSelector</span>() &#123;<br>                    <span class="hljs-meta">@Override</span><br>                    <span class="hljs-keyword">public</span> MessageQueue <span class="hljs-title function_">select</span><span class="hljs-params">(List&lt;MessageQueue&gt; mqs, Message msg, Object arg)</span> &#123;<br>                        <span class="hljs-type">Integer</span> <span class="hljs-variable">id</span> <span class="hljs-operator">=</span> (Integer) arg;<br>                        <span class="hljs-type">int</span> <span class="hljs-variable">index</span> <span class="hljs-operator">=</span> id % mqs.size();<br>                        <span class="hljs-keyword">return</span> mqs.get(index);<br>                    &#125;<br>                &#125;, orderId);<br><br>                System.out.printf(<span class="hljs-string">&quot;%s%n&quot;</span>, sendResult);<br>            &#125;<br><br>            producer.shutdown();<br>        &#125; <span class="hljs-keyword">catch</span> (MQClientException | RemotingException | MQBrokerException | InterruptedException e) &#123;<br>            e.printStackTrace();<br>        &#125;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>中间件</category>
        <category>RocketMQ</category>
      </categories>
      <tags>
        <tag>RocketMQ</tag>
      </tags>
  </entry>
  <entry>
    <title>RocketMQ4使用jdk17启动出现的问题</title>
    <url>/2023/69cb00fc039e/index.html</url>
    <content><![CDATA[<blockquote>
<p>RocketMQ版本： 4.9.7<br>JDK版本： 17</p>
</blockquote>
<p>当使用超过 <code>jdk8</code> 的版本运行 <code>RocketMQ</code> 时，启动 <code>Broker</code> 可能会出现如下错误</p>
<figure class="highlight stylus"><table><tr><td class="code"><pre><code class="hljs stylus">ERROR <span class="hljs-selector-tag">main</span> - load exception<br>java<span class="hljs-selector-class">.lang</span><span class="hljs-selector-class">.IllegalStateException</span>: java<span class="hljs-selector-class">.lang</span><span class="hljs-selector-class">.reflect</span><span class="hljs-selector-class">.InaccessibleObjectException</span>: Unable to make public java<span class="hljs-selector-class">.lang</span><span class="hljs-selector-class">.Object</span> java<span class="hljs-selector-class">.nio</span><span class="hljs-selector-class">.DirectByteBuffer</span><span class="hljs-selector-class">.attachment</span>() accessible: module java<span class="hljs-selector-class">.base</span> does not <span class="hljs-string">&quot;opens java.nio&quot;</span> to unnamed module @<span class="hljs-number">276438</span>c9<br>	at org<span class="hljs-selector-class">.apache</span><span class="hljs-selector-class">.rocketmq</span><span class="hljs-selector-class">.store</span><span class="hljs-selector-class">.MappedFile</span>.lambda<span class="hljs-variable">$invoke</span>$<span class="hljs-number">0</span>(MappedFile<span class="hljs-selector-class">.java</span>:<span class="hljs-number">116</span>)<br>	at java.base/java<span class="hljs-selector-class">.security</span><span class="hljs-selector-class">.AccessController</span><span class="hljs-selector-class">.doPrivileged</span>(AccessController<span class="hljs-selector-class">.java</span>:<span class="hljs-number">318</span>)<br>	at org<span class="hljs-selector-class">.apache</span><span class="hljs-selector-class">.rocketmq</span><span class="hljs-selector-class">.store</span><span class="hljs-selector-class">.MappedFile</span><span class="hljs-selector-class">.invoke</span>(MappedFile<span class="hljs-selector-class">.java</span>:<span class="hljs-number">110</span>)<br>	at org<span class="hljs-selector-class">.apache</span><span class="hljs-selector-class">.rocketmq</span><span class="hljs-selector-class">.store</span><span class="hljs-selector-class">.MappedFile</span><span class="hljs-selector-class">.viewed</span>(MappedFile<span class="hljs-selector-class">.java</span>:<span class="hljs-number">140</span>)<br>	at org<span class="hljs-selector-class">.apache</span><span class="hljs-selector-class">.rocketmq</span><span class="hljs-selector-class">.store</span><span class="hljs-selector-class">.MappedFile</span><span class="hljs-selector-class">.clean</span>(MappedFile<span class="hljs-selector-class">.java</span>:<span class="hljs-number">106</span>)<br>	at org<span class="hljs-selector-class">.apache</span><span class="hljs-selector-class">.rocketmq</span><span class="hljs-selector-class">.store</span><span class="hljs-selector-class">.MappedFile</span><span class="hljs-selector-class">.cleanup</span>(MappedFile<span class="hljs-selector-class">.java</span>:<span class="hljs-number">449</span>)<br>	at org<span class="hljs-selector-class">.apache</span><span class="hljs-selector-class">.rocketmq</span><span class="hljs-selector-class">.store</span><span class="hljs-selector-class">.ReferenceResource</span><span class="hljs-selector-class">.release</span>(ReferenceResource<span class="hljs-selector-class">.java</span>:<span class="hljs-number">63</span>)<br>	at org<span class="hljs-selector-class">.apache</span><span class="hljs-selector-class">.rocketmq</span><span class="hljs-selector-class">.store</span><span class="hljs-selector-class">.ReferenceResource</span><span class="hljs-selector-class">.shutdown</span>(ReferenceResource<span class="hljs-selector-class">.java</span>:<span class="hljs-number">47</span>)<br>	at org<span class="hljs-selector-class">.apache</span><span class="hljs-selector-class">.rocketmq</span><span class="hljs-selector-class">.store</span><span class="hljs-selector-class">.MappedFile</span><span class="hljs-selector-class">.destroy</span>(MappedFile<span class="hljs-selector-class">.java</span>:<span class="hljs-number">457</span>)<br>	at org<span class="hljs-selector-class">.apache</span><span class="hljs-selector-class">.rocketmq</span><span class="hljs-selector-class">.store</span><span class="hljs-selector-class">.index</span><span class="hljs-selector-class">.IndexFile</span><span class="hljs-selector-class">.destroy</span>(IndexFile<span class="hljs-selector-class">.java</span>:<span class="hljs-number">85</span>)<br>	at org<span class="hljs-selector-class">.apache</span><span class="hljs-selector-class">.rocketmq</span><span class="hljs-selector-class">.store</span><span class="hljs-selector-class">.index</span><span class="hljs-selector-class">.IndexService</span><span class="hljs-selector-class">.load</span>(IndexService<span class="hljs-selector-class">.java</span>:<span class="hljs-number">71</span>)<br>	at org<span class="hljs-selector-class">.apache</span><span class="hljs-selector-class">.rocketmq</span><span class="hljs-selector-class">.store</span><span class="hljs-selector-class">.DefaultMessageStore</span><span class="hljs-selector-class">.load</span>(DefaultMessageStore<span class="hljs-selector-class">.java</span>:<span class="hljs-number">210</span>)<br>	at org<span class="hljs-selector-class">.apache</span><span class="hljs-selector-class">.rocketmq</span><span class="hljs-selector-class">.broker</span><span class="hljs-selector-class">.BrokerController</span><span class="hljs-selector-class">.initialize</span>(BrokerController<span class="hljs-selector-class">.java</span>:<span class="hljs-number">268</span>)<br>	at org<span class="hljs-selector-class">.apache</span><span class="hljs-selector-class">.rocketmq</span><span class="hljs-selector-class">.broker</span><span class="hljs-selector-class">.BrokerStartup</span><span class="hljs-selector-class">.createBrokerController</span>(BrokerStartup<span class="hljs-selector-class">.java</span>:<span class="hljs-number">219</span>)<br>	at org<span class="hljs-selector-class">.apache</span><span class="hljs-selector-class">.rocketmq</span><span class="hljs-selector-class">.broker</span><span class="hljs-selector-class">.BrokerStartup</span><span class="hljs-selector-class">.main</span>(BrokerStartup<span class="hljs-selector-class">.java</span>:<span class="hljs-number">57</span>)<br></code></pre></td></tr></table></figure>

<p>以及</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java">ERROR main - load exception<br>java.lang.IllegalStateException: java.lang.reflect.InaccessibleObjectException: Unable to make <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> jdk.internal.ref.Cleaner.clean() accessible: <span class="hljs-keyword">module</span> java.base does not <span class="hljs-string">&quot;exports jdk.internal.ref&quot;</span> to unnamed <span class="hljs-keyword">module</span> @4e4aea35<br>	at org.apache.rocketmq.store.MappedFile.lambda$invoke$<span class="hljs-number">0</span>(MappedFile.java:<span class="hljs-number">116</span>)<br>	at java.base/java.security.AccessController.doPrivileged(AccessController.java:<span class="hljs-number">318</span>)<br>	at org.apache.rocketmq.store.MappedFile.invoke(MappedFile.java:<span class="hljs-number">110</span>)<br>	at org.apache.rocketmq.store.MappedFile.clean(MappedFile.java:<span class="hljs-number">106</span>)<br>	at org.apache.rocketmq.store.MappedFile.cleanup(MappedFile.java:<span class="hljs-number">449</span>)<br>	at org.apache.rocketmq.store.ReferenceResource.release(ReferenceResource.java:<span class="hljs-number">63</span>)<br>	at org.apache.rocketmq.store.ReferenceResource.shutdown(ReferenceResource.java:<span class="hljs-number">47</span>)<br>	at org.apache.rocketmq.store.MappedFile.destroy(MappedFile.java:<span class="hljs-number">457</span>)<br>	at org.apache.rocketmq.store.index.IndexFile.destroy(IndexFile.java:<span class="hljs-number">85</span>)<br>	at org.apache.rocketmq.store.index.IndexService.load(IndexService.java:<span class="hljs-number">71</span>)<br>	at org.apache.rocketmq.store.DefaultMessageStore.load(DefaultMessageStore.java:<span class="hljs-number">210</span>)<br>	at org.apache.rocketmq.broker.BrokerController.initialize(BrokerController.java:<span class="hljs-number">268</span>)<br>	at org.apache.rocketmq.broker.BrokerStartup.createBrokerController(BrokerStartup.java:<span class="hljs-number">219</span>)<br>	at org.apache.rocketmq.broker.BrokerStartup.main(BrokerStartup.java:<span class="hljs-number">57</span>)<br>Caused by: java.lang.reflect.InaccessibleObjectException: Unable to make <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> jdk.internal.ref.Cleaner.clean() accessible: <span class="hljs-keyword">module</span> java.base does not <span class="hljs-string">&quot;exports jdk.internal.ref&quot;</span> to unnamed <span class="hljs-keyword">module</span> @4e4aea35<br>	at java.base/java.lang.reflect.AccessibleObject.checkCanSetAccessible(AccessibleObject.java:<span class="hljs-number">354</span>)<br>	at java.base/java.lang.reflect.AccessibleObject.checkCanSetAccessible(AccessibleObject.java:<span class="hljs-number">297</span>)<br>	at java.base/java.lang.reflect.Method.checkCanSetAccessible(Method.java:<span class="hljs-number">199</span>)<br>	at java.base/java.lang.reflect.Method.setAccessible(Method.java:<span class="hljs-number">193</span>)<br>	at org.apache.rocketmq.store.MappedFile.lambda$invoke$<span class="hljs-number">0</span>(MappedFile.java:<span class="hljs-number">113</span>)<br></code></pre></td></tr></table></figure>

<p>这是因为 <code>jdk8</code> 以上就使用模块化，所以需要添加 <code>jvm</code> 参数</p>
<p><img src="https://s2.loli.net/2023/12/21/H3dfjZbrRGDAsKP.png" alt="Broker添加vm参数"></p>
<figure class="highlight csharp"><table><tr><td class="code"><pre><code class="hljs csharp">--<span class="hljs-keyword">add</span>-opens java.<span class="hljs-keyword">base</span>/java.nio=ALL-UNNAMED --<span class="hljs-keyword">add</span>-exports java.<span class="hljs-keyword">base</span>/jdk.<span class="hljs-keyword">internal</span>.<span class="hljs-keyword">ref</span>=ALL-UNNAMED<br></code></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>中间件</category>
        <category>RocketMQ</category>
      </categories>
      <tags>
        <tag>RocketMQ</tag>
      </tags>
  </entry>
  <entry>
    <title>RocketMQ中Broker注册到Nameserver详解</title>
    <url>/2023/e44019fc2e8a/index.html</url>
    <content><![CDATA[<blockquote>
<p>RocketMQ版本：4.9.7<br>JDK版本： 17</p>
</blockquote>
<p>可以先参考</p>
<ol><li><a href="/2023/5398bb79bdb5/index.html" title="RocketMQ之Broker详解">RocketMQ之Broker详解</a></li></ol>
<ol><li><a href="/2023/e17cf930f952/index.html" title="RocketMQ4中Nameserver详解">RocketMQ4中Nameserver详解</a></li></ol>

<h2 id="注册的整体逻辑"><a href="#注册的整体逻辑" class="headerlink" title="注册的整体逻辑"></a>注册的整体逻辑</h2><p><code>Broker</code> 将信息注册到 <code>NameServer</code> 中的代码是在 <code>BrokerController#registerBrokerAll(boolean checkOrderConfig, boolean oneway, boolean forceRegister)</code> 方法中完成的</p>
<p>注册的逻辑中主要完成三件事情</p>
<ul>
<li>获取当前 <code>Broker</code> 中保存的 <code>Topic</code> 信息，将其变成 <code>ConcurrentHashMap&lt;String, TopicConfig&gt; topicConfigTable</code> 结果并放入 <code>TopicConfigSerializeWrapper</code> 类中</li>
<li>获取 <code>Broker</code> 相关信息，比如 <code>BrokerName</code>, <code>BrokerId</code>, <code>BrokerAddr</code></li>
<li>将前面两步获取的信息结合起来通过 <code>Netty</code> 发送到 <code>NameServer</code> 中</li>
</ul>
<p>接下来就看下实际的代码实现</p>
<p><code>BrokerController</code> 类</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">synchronized</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">registerBrokerAll</span><span class="hljs-params">(<span class="hljs-keyword">final</span> <span class="hljs-type">boolean</span> checkOrderConfig, <span class="hljs-type">boolean</span> oneway, <span class="hljs-type">boolean</span> forceRegister)</span> &#123;<br>    <span class="hljs-comment">// 【步骤1】获取 Topic 信息</span><br>    <span class="hljs-type">TopicConfigSerializeWrapper</span> <span class="hljs-variable">topicConfigWrapper</span> <span class="hljs-operator">=</span> <span class="hljs-built_in">this</span>.getTopicConfigManager().buildTopicConfigSerializeWrapper();<br><br>    <span class="hljs-keyword">if</span> (!PermName.isWriteable(<span class="hljs-built_in">this</span>.getBrokerConfig().getBrokerPermission())<br>        || !PermName.isReadable(<span class="hljs-built_in">this</span>.getBrokerConfig().getBrokerPermission())) &#123;<br>        ConcurrentHashMap&lt;String, TopicConfig&gt; topicConfigTable = <span class="hljs-keyword">new</span> <span class="hljs-title class_">ConcurrentHashMap</span>&lt;&gt;();<br>        <span class="hljs-keyword">for</span> (TopicConfig topicConfig : topicConfigWrapper.getTopicConfigTable().values()) &#123;<br>            <span class="hljs-type">TopicConfig</span> <span class="hljs-variable">tmp</span> <span class="hljs-operator">=</span><br>                <span class="hljs-keyword">new</span> <span class="hljs-title class_">TopicConfig</span>(topicConfig.getTopicName(), topicConfig.getReadQueueNums(), topicConfig.getWriteQueueNums(),<br>                    <span class="hljs-built_in">this</span>.brokerConfig.getBrokerPermission());<br>            topicConfigTable.put(topicConfig.getTopicName(), tmp);<br>        &#125;<br>        topicConfigWrapper.setTopicConfigTable(topicConfigTable);<br>    &#125;<br><br>    <span class="hljs-keyword">if</span> (forceRegister || needRegister(<span class="hljs-built_in">this</span>.brokerConfig.getBrokerClusterName(),<br>        <span class="hljs-built_in">this</span>.getBrokerAddr(),<br>        <span class="hljs-built_in">this</span>.brokerConfig.getBrokerName(),<br>        <span class="hljs-built_in">this</span>.brokerConfig.getBrokerId(),<br>        <span class="hljs-built_in">this</span>.brokerConfig.getRegisterBrokerTimeoutMills())) &#123;<br>        doRegisterBrokerAll(checkOrderConfig, oneway, topicConfigWrapper);<br>    &#125;<br>&#125;<br><br><span class="hljs-keyword">private</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">doRegisterBrokerAll</span><span class="hljs-params">(<span class="hljs-type">boolean</span> checkOrderConfig, <span class="hljs-type">boolean</span> oneway,</span><br><span class="hljs-params">    TopicConfigSerializeWrapper topicConfigWrapper)</span> &#123;<br>    <span class="hljs-comment">// 【步骤2】 this.brokerOuterAPI.registerBrokerAll 方法的参数</span><br>    <span class="hljs-comment">// 就包含了 Broker 信息和 Topic 信息， 组装命令和发送的逻辑</span><br>    <span class="hljs-comment">// 是在 brokerOuterAPI 中完成的</span><br>    List&lt;RegisterBrokerResult&gt; registerBrokerResultList = <span class="hljs-built_in">this</span>.brokerOuterAPI.registerBrokerAll(<br>        <span class="hljs-built_in">this</span>.brokerConfig.getBrokerClusterName(),<br>        <span class="hljs-built_in">this</span>.getBrokerAddr(),<br>        <span class="hljs-built_in">this</span>.brokerConfig.getBrokerName(),<br>        <span class="hljs-built_in">this</span>.brokerConfig.getBrokerId(),<br>        <span class="hljs-built_in">this</span>.getHAServerAddr(),<br>        topicConfigWrapper,<br>        <span class="hljs-built_in">this</span>.filterServerManager.buildNewFilterServerList(),<br>        oneway,<br>        <span class="hljs-built_in">this</span>.brokerConfig.getRegisterBrokerTimeoutMills(),<br>        <span class="hljs-built_in">this</span>.brokerConfig.isCompressedRegister());<br><br>    <span class="hljs-keyword">if</span> (registerBrokerResultList.size() &gt; <span class="hljs-number">0</span>) &#123;<br>        <span class="hljs-type">RegisterBrokerResult</span> <span class="hljs-variable">registerBrokerResult</span> <span class="hljs-operator">=</span> registerBrokerResultList.get(<span class="hljs-number">0</span>);<br>        <span class="hljs-keyword">if</span> (registerBrokerResult != <span class="hljs-literal">null</span>) &#123;<br>            <span class="hljs-keyword">if</span> (<span class="hljs-built_in">this</span>.updateMasterHAServerAddrPeriodically &amp;&amp; registerBrokerResult.getHaServerAddr() != <span class="hljs-literal">null</span>) &#123;<br>                <span class="hljs-built_in">this</span>.messageStore.updateHaMasterAddress(registerBrokerResult.getHaServerAddr());<br>            &#125;<br><br>            <span class="hljs-built_in">this</span>.slaveSynchronize.setMasterAddr(registerBrokerResult.getMasterAddr());<br><br>            <span class="hljs-keyword">if</span> (checkOrderConfig) &#123;<br>                <span class="hljs-built_in">this</span>.getTopicConfigManager().updateOrderTopicConfig(registerBrokerResult.getKvTable());<br>            &#125;<br>        &#125;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<p><code>BrokerOuterAPI</code> 类</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">BrokerOuterAPI</span> &#123;<br>    <span class="hljs-comment">// 【步骤3】这里面会实际组装 RemotingCommand 并执行发送的逻辑</span><br>    <span class="hljs-keyword">private</span> RegisterBrokerResult <span class="hljs-title function_">registerBroker</span><span class="hljs-params">(</span><br><span class="hljs-params">        <span class="hljs-keyword">final</span> String namesrvAddr,</span><br><span class="hljs-params">        <span class="hljs-keyword">final</span> <span class="hljs-type">boolean</span> oneway,</span><br><span class="hljs-params">        <span class="hljs-keyword">final</span> <span class="hljs-type">int</span> timeoutMills,</span><br><span class="hljs-params">        <span class="hljs-keyword">final</span> RegisterBrokerRequestHeader requestHeader,</span><br><span class="hljs-params">        <span class="hljs-keyword">final</span> <span class="hljs-type">byte</span>[] body</span><br><span class="hljs-params">    )</span> <span class="hljs-keyword">throws</span> RemotingCommandException, MQBrokerException, RemotingConnectException, RemotingSendRequestException, RemotingTimeoutException,<br>        InterruptedException &#123;<br>        <span class="hljs-comment">// 这里构建的 Command 指令是 RequestCode.REGISTER_BROKER</span><br>        <span class="hljs-type">RemotingCommand</span> <span class="hljs-variable">request</span> <span class="hljs-operator">=</span> RemotingCommand.createRequestCommand(RequestCode.REGISTER_BROKER, requestHeader);<br>        request.setBody(body);<br><br>        <span class="hljs-keyword">if</span> (oneway) &#123;<br>            <span class="hljs-keyword">try</span> &#123;<br>                <span class="hljs-built_in">this</span>.remotingClient.invokeOneway(namesrvAddr, request, timeoutMills);<br>            &#125; <span class="hljs-keyword">catch</span> (RemotingTooMuchRequestException e) &#123;<br>                <span class="hljs-comment">// Ignore</span><br>            &#125;<br>            <span class="hljs-keyword">return</span> <span class="hljs-literal">null</span>;<br>        &#125;<br><br>        <span class="hljs-type">RemotingCommand</span> <span class="hljs-variable">response</span> <span class="hljs-operator">=</span> <span class="hljs-built_in">this</span>.remotingClient.invokeSync(namesrvAddr, request, timeoutMills);<br>        <span class="hljs-keyword">assert</span> response != <span class="hljs-literal">null</span>;<br>        <span class="hljs-keyword">switch</span> (response.getCode()) &#123;<br>            <span class="hljs-keyword">case</span> ResponseCode.SUCCESS: &#123;<br>                <span class="hljs-type">RegisterBrokerResponseHeader</span> <span class="hljs-variable">responseHeader</span> <span class="hljs-operator">=</span><br>                    (RegisterBrokerResponseHeader) response.decodeCommandCustomHeader(RegisterBrokerResponseHeader.class);<br>                <span class="hljs-type">RegisterBrokerResult</span> <span class="hljs-variable">result</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">RegisterBrokerResult</span>();<br>                result.setMasterAddr(responseHeader.getMasterAddr());<br>                result.setHaServerAddr(responseHeader.getHaServerAddr());<br>                <span class="hljs-keyword">if</span> (response.getBody() != <span class="hljs-literal">null</span>) &#123;<br>                    result.setKvTable(KVTable.decode(response.getBody(), KVTable.class));<br>                &#125;<br>                <span class="hljs-keyword">return</span> result;<br>            &#125;<br>            <span class="hljs-keyword">default</span>:<br>                <span class="hljs-keyword">break</span>;<br>        &#125;<br><br>        <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">MQBrokerException</span>(response.getCode(), response.getRemark(), requestHeader == <span class="hljs-literal">null</span> ? <span class="hljs-literal">null</span> : requestHeader.getBrokerAddr());<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<h3 id="【步骤1】获取-Topic-信息"><a href="#【步骤1】获取-Topic-信息" class="headerlink" title="【步骤1】获取 Topic 信息"></a>【步骤1】获取 Topic 信息</h3><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-comment">// BrokerController 类</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">BrokerController</span> &#123;<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">synchronized</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">registerBrokerAll</span><span class="hljs-params">(<span class="hljs-keyword">final</span> <span class="hljs-type">boolean</span> checkOrderConfig, <span class="hljs-type">boolean</span> oneway, <span class="hljs-type">boolean</span> forceRegister)</span> &#123;<br>        <span class="hljs-type">TopicConfigSerializeWrapper</span> <span class="hljs-variable">topicConfigWrapper</span> <span class="hljs-operator">=</span> <span class="hljs-built_in">this</span>.getTopicConfigManager().buildTopicConfigSerializeWrapper();<br>    &#125;<br>&#125;<br><br><span class="hljs-comment">// TopicConfigManager 类</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">TopicConfigManager</span> &#123;<br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> ConcurrentMap&lt;String, TopicConfig&gt; topicConfigTable =<br>        <span class="hljs-keyword">new</span> <span class="hljs-title class_">ConcurrentHashMap</span>&lt;String, TopicConfig&gt;(<span class="hljs-number">1024</span>);<br><br>    <span class="hljs-keyword">public</span> TopicConfigSerializeWrapper <span class="hljs-title function_">buildTopicConfigSerializeWrapper</span><span class="hljs-params">()</span> &#123;<br>        <span class="hljs-type">TopicConfigSerializeWrapper</span> <span class="hljs-variable">topicConfigSerializeWrapper</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">TopicConfigSerializeWrapper</span>();<br>        topicConfigSerializeWrapper.setTopicConfigTable(<span class="hljs-built_in">this</span>.topicConfigTable);<br>        topicConfigSerializeWrapper.setDataVersion(<span class="hljs-built_in">this</span>.dataVersion);<br>        <span class="hljs-keyword">return</span> topicConfigSerializeWrapper;<br>    &#125;<br>&#125;<br><br></code></pre></td></tr></table></figure>
<p>从上面代码可以看到，获取 <code>Topic</code> 信息的代码很简单，就是获取 <code>TopicConfigManager</code> 类中的 <code>topicConfigTable</code> 属性，既然这里是直接获取的，就说明前面已经有地方设置值，那么是在哪里设置值的呢？</p>
<p>一个简单快速的方法就是直接在源码中找到 <code>TopicConfigManager</code>， 然后在类中搜索 <code>topicConfigTable.put</code>, 在含有添加的地方打上断点，这样就可以知道添加 <code>Topic</code> 的调用链，通过这种方式在 <code>Broker</code> 启动过程中 会有两个地方添加 <code>Topic</code> 数据<br><strong>第一个添加 <code>Toipc</code> 的地方</strong></p>
<figure class="highlight haxe"><table><tr><td class="code"><pre><code class="hljs haxe">BrokerStartup<span class="hljs-meta">#main</span><br>    BrokerStartup<span class="hljs-meta">#createBrokerController</span><br>        <span class="hljs-keyword">new</span> <span class="hljs-type">BrokerController</span>()<br>            <span class="hljs-keyword">new</span> <span class="hljs-type">TopicConfigManager</span>()<br></code></pre></td></tr></table></figure>
<p>在创建 <code>TopicConfigManager</code> 对象的时候就会加载一系列内部的 <code>Topic</code>， 代码如下</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-title function_">TopicConfigManager</span><span class="hljs-params">(BrokerController brokerController)</span> &#123;<br>    <span class="hljs-built_in">this</span>.brokerController = brokerController;<br>    &#123;<br>        <span class="hljs-type">String</span> <span class="hljs-variable">topic</span> <span class="hljs-operator">=</span> TopicValidator.RMQ_SYS_SELF_TEST_TOPIC;<br>        <span class="hljs-type">TopicConfig</span> <span class="hljs-variable">topicConfig</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">TopicConfig</span>(topic);<br>        TopicValidator.addSystemTopic(topic);<br>        topicConfig.setReadQueueNums(<span class="hljs-number">1</span>);<br>        topicConfig.setWriteQueueNums(<span class="hljs-number">1</span>);<br>        <span class="hljs-built_in">this</span>.topicConfigTable.put(topicConfig.getTopicName(), topicConfig);<br>    &#125;<br>    <span class="hljs-comment">// …… 省略其他内部 topic 创建过程</span><br>&#125;<br></code></pre></td></tr></table></figure>


<p><strong>第二个添加 <code>Topic</code> 的地方</strong></p>
<figure class="highlight 1c"><table><tr><td class="code"><pre><code class="hljs 1c">BrokerStartup<span class="hljs-meta">#main</span><br>    BrokerStartup<span class="hljs-meta">#createBrokerController</span><br>        BrokerController<span class="hljs-meta">#initialize</span><br>            ConfigManager<span class="hljs-meta">#load</span><br></code></pre></td></tr></table></figure>
<p>这里的 <code>load</code> 是从 <code>ROCKETMQ_HOME/store/config/topics.json</code> 中加载数据，如果不是第一次部署，而是已经创建有 <code>topic</code> 然后重启 <code>Broker</code> 的话就会从这里面加载 <code>Topic</code> 数据</p>
]]></content>
      <categories>
        <category>中间件</category>
        <category>RocketMQ</category>
      </categories>
      <tags>
        <tag>RocketMQ</tag>
      </tags>
  </entry>
  <entry>
    <title>RocketMQ中MessageQueue详解</title>
    <url>/2023/0137d652ba85/index.html</url>
    <content><![CDATA[<span class="hide_first_mermaid">
<div class="mermaid-wrap"><pre class="mermaid-src" hidden>
  flowchart LR
    A
  </pre></div>
</span>

<blockquote>
<p>RocketMQ版本： 4.9.7<br>JDK版本： 17</p>
</blockquote>
<h2 id="MessageQueue-简介"><a href="#MessageQueue-简介" class="headerlink" title="MessageQueue 简介"></a>MessageQueue 简介</h2><p>在 <code>RokcetMQ</code> 中有几个核心的概念，其中就包含 <code>Topic</code> 和 <code>Queue</code></p>
<ul>
<li><code>Topic</code> 是业务上的逻辑分类，一类消息创发往同一个 <code>Topic</code></li>
<li><code>Queue</code> 则是消息实际发送的地方，一个 <code>Topic</code> 对应多个 <code>Queue</code></li>
</ul>
<h2 id="MessageQueue-结构"><a href="#MessageQueue-结构" class="headerlink" title="MessageQueue 结构"></a>MessageQueue 结构</h2><p><code>MessageQueue</code> 结构如下：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">MessageQueue</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">Comparable</span>&lt;MessageQueue&gt;, Serializable &#123;<br>    <span class="hljs-keyword">private</span> String topic;<br>    <span class="hljs-keyword">private</span> String brokerName;<br>    <span class="hljs-keyword">private</span> <span class="hljs-type">int</span> queueId;<br>&#125;<br></code></pre></td></tr></table></figure>
<p>由上面 <code>MessageQueue</code> 属性可知，想要确定消息到底发送到那个 <code>Queue</code> 中需要知道三个信息</p>
<ul>
<li><code>topic</code>: 这个不用多说，发送肯定需要先知道发往哪个 <code>Topic</code></li>
<li><code>brokerName</code>: 为什么还需要知道 <code>BrokerName</code> 呢？主要是某个 <code>Topic</code> 可以创建在集群中的多个 <code>Broker</code> 中，比如集群中有 5 个 <code>Broker</code>, 我们可以将 <code>Topic</code> 创建在任意几个或全部的 <code>Broker</code> 中</li>
<li><code>queueId</code>：<code>Broker</code> 中创建的 <code>Topic</code> 默认是会有四个 <code>Queue</code>, 所以需要知道具体是发送到哪个 <code>Queue</code> 中</li>
</ul>
<p>接下来会需要在 <code>Broker</code> 集群中来查看 <code>MessageQueue</code> 结构的数据，所以可以先参考下面文章从源码启动 <code>Broker</code> 集群 【因为需要分析源码，所以从源码启动】</p>
<ol><li><a href="/2023/f0b30d177028/index.html" title="RocketMQ4版本以集群模式启动Broker">RocketMQ4版本以集群模式启动Broker</a></li></ol>

<h3 id="通过-Producer-发送数据查看-MessageQueue-内容"><a href="#通过-Producer-发送数据查看-MessageQueue-内容" class="headerlink" title="通过 Producer 发送数据查看 MessageQueue 内容"></a>通过 Producer 发送数据查看 MessageQueue 内容</h3><p>接下来就是在 <code>idea</code> 中通过源码启动 1 个 <code>NameServer</code>, 2 个 <code>Broker</code><br>然后找到源码下的 <code>exmaple</code> 子模块，这个模块主要是测试代码，找到 <code>org.apache.rocketmq.example.simple.Producer</code> 类，代码如下（有部分调整，比如循环次数改到了10）</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">Producer</span> &#123;<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">main</span><span class="hljs-params">(String[] args)</span> <span class="hljs-keyword">throws</span> MQClientException, InterruptedException &#123;<br><br>        <span class="hljs-type">DefaultMQProducer</span> <span class="hljs-variable">producer</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">DefaultMQProducer</span>(<span class="hljs-string">&quot;ProducerGroupName&quot;</span>);<br>        producer.setNamesrvAddr(<span class="hljs-string">&quot;127.0.0.1:9876&quot;</span>);<br>        producer.start();<br>        <span class="hljs-comment">// 这里循环默认是 128， 改成10 便于观察 MessageQueue 里面的内容</span><br>        <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> <span class="hljs-variable">i</span> <span class="hljs-operator">=</span> <span class="hljs-number">0</span>; i &lt; <span class="hljs-number">10</span>; i++) &#123;<br>            <span class="hljs-keyword">try</span> &#123;<br>                &#123;<br>                    <span class="hljs-type">Message</span> <span class="hljs-variable">msg</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Message</span>(<span class="hljs-string">&quot;TopicTest&quot;</span>,<br>                            <span class="hljs-string">&quot;TagA&quot;</span>,<br>                            <span class="hljs-string">&quot;OrderID188&quot;</span>,<br>                            <span class="hljs-string">&quot;Hello world&quot;</span>.getBytes(RemotingHelper.DEFAULT_CHARSET));<br>                    <span class="hljs-type">SendResult</span> <span class="hljs-variable">sendResult</span> <span class="hljs-operator">=</span> producer.send(msg);<br>                    System.out.printf(<span class="hljs-string">&quot;%s%n&quot;</span>, sendResult);<br>                &#125;<br><br>            &#125; <span class="hljs-keyword">catch</span> (Exception e) &#123;<br>                e.printStackTrace();<br>            &#125;<br>        &#125;<br>        producer.shutdown();<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>
<p><code>NameServer</code> 和 2 个 <code>Broker</code> 都启动之后就启动上面说的 <code>Producer</code> 类，然后观察控制台打印的结果，部分结果如下</p>
<figure class="highlight routeros"><table><tr><td class="code"><pre><code class="hljs routeros">SendResult [<span class="hljs-attribute">sendStatus</span>=SEND_OK, <span class="hljs-attribute">offsetMsgId</span>=C0A8EF0100002A9F0000000000000000, <span class="hljs-attribute">messageQueue</span>=MessageQueue [<span class="hljs-attribute">topic</span>=TopicTest, <span class="hljs-attribute">brokerName</span>=broker-a, <span class="hljs-attribute">queueId</span>=2], <span class="hljs-attribute">queueOffset</span>=0]<br>SendResult [<span class="hljs-attribute">sendStatus</span>=SEND_OK, <span class="hljs-attribute">offsetMsgId</span>=C0A8EF0100002A9F00000000000000C9, <span class="hljs-attribute">messageQueue</span>=MessageQueue [<span class="hljs-attribute">topic</span>=TopicTest, <span class="hljs-attribute">brokerName</span>=broker-a, <span class="hljs-attribute">queueId</span>=3], <span class="hljs-attribute">queueOffset</span>=0]<br>SendResult [<span class="hljs-attribute">sendStatus</span>=SEND_OK, <span class="hljs-attribute">offsetMsgId</span>=C0A8EF0100002AA30000000000000000, <span class="hljs-attribute">messageQueue</span>=MessageQueue [<span class="hljs-attribute">topic</span>=TopicTest, <span class="hljs-attribute">brokerName</span>=broker-b, <span class="hljs-attribute">queueId</span>=0], <span class="hljs-attribute">queueOffset</span>=0]<br>SendResult [<span class="hljs-attribute">sendStatus</span>=SEND_OK, <span class="hljs-attribute">offsetMsgId</span>=C0A8EF0100002AA300000000000000C9, <span class="hljs-attribute">messageQueue</span>=MessageQueue [<span class="hljs-attribute">topic</span>=TopicTest, <span class="hljs-attribute">brokerName</span>=broker-b, <span class="hljs-attribute">queueId</span>=1], <span class="hljs-attribute">queueOffset</span>=0]<br>SendResult [<span class="hljs-attribute">sendStatus</span>=SEND_OK, <span class="hljs-attribute">offsetMsgId</span>=C0A8EF0100002AA30000000000000192, <span class="hljs-attribute">messageQueue</span>=MessageQueue [<span class="hljs-attribute">topic</span>=TopicTest, <span class="hljs-attribute">brokerName</span>=broker-b, <span class="hljs-attribute">queueId</span>=2], <span class="hljs-attribute">queueOffset</span>=0]<br>SendResult [<span class="hljs-attribute">sendStatus</span>=SEND_OK, <span class="hljs-attribute">offsetMsgId</span>=C0A8EF0100002AA3000000000000025B, <span class="hljs-attribute">messageQueue</span>=MessageQueue [<span class="hljs-attribute">topic</span>=TopicTest, <span class="hljs-attribute">brokerName</span>=broker-b, <span class="hljs-attribute">queueId</span>=3], <span class="hljs-attribute">queueOffset</span>=0]<br>SendResult [<span class="hljs-attribute">sendStatus</span>=SEND_OK, <span class="hljs-attribute">offsetMsgId</span>=C0A8EF0100002A9F0000000000000192, <span class="hljs-attribute">messageQueue</span>=MessageQueue [<span class="hljs-attribute">topic</span>=TopicTest, <span class="hljs-attribute">brokerName</span>=broker-a, <span class="hljs-attribute">queueId</span>=0], <span class="hljs-attribute">queueOffset</span>=0]<br>SendResult [<span class="hljs-attribute">sendStatus</span>=SEND_OK, <span class="hljs-attribute">offsetMsgId</span>=C0A8EF0100002A9F000000000000025B, <span class="hljs-attribute">messageQueue</span>=MessageQueue [<span class="hljs-attribute">topic</span>=TopicTest, <span class="hljs-attribute">brokerName</span>=broker-a, <span class="hljs-attribute">queueId</span>=1], <span class="hljs-attribute">queueOffset</span>=0]<br>SendResult [<span class="hljs-attribute">sendStatus</span>=SEND_OK, <span class="hljs-attribute">offsetMsgId</span>=C0A8EF0100002A9F0000000000000324, <span class="hljs-attribute">messageQueue</span>=MessageQueue [<span class="hljs-attribute">topic</span>=TopicTest, <span class="hljs-attribute">brokerName</span>=broker-a, <span class="hljs-attribute">queueId</span>=2], <span class="hljs-attribute">queueOffset</span>=1]<br>SendResult [<span class="hljs-attribute">sendStatus</span>=SEND_OK, <span class="hljs-attribute">offsetMsgId</span>=C0A8EF0100002A9F00000000000003ED, <span class="hljs-attribute">messageQueue</span>=MessageQueue [<span class="hljs-attribute">topic</span>=TopicTest, <span class="hljs-attribute">brokerName</span>=broker-a, <span class="hljs-attribute">queueId</span>=3], <span class="hljs-attribute">queueOffset</span>=1]<br></code></pre></td></tr></table></figure>

<p>从上面输出的结果可以得到以下几个结论</p>
<ol>
<li>这个 <code>Topic</code> 在每个 <code>Broker</code> 中都有创建（默认 <code>Tppic</code> 是会自动创建的，可以在 <code>broker.conf</code> 配置文件中添加 <code>autoCreateTopicEnable=false</code> 来禁止自动创建 <code>topic</code>）</li>
<li>每一个 <code>Broker</code> 中默认有创建 4 个 <code>Queue</code>，分别是 <code>0</code>、<code>1</code>、<code>2</code>、<code>3</code></li>
</ol>
<h2 id="为什么不推荐在生产环境自动创建-Topic"><a href="#为什么不推荐在生产环境自动创建-Topic" class="headerlink" title="为什么不推荐在生产环境自动创建 Topic"></a>为什么不推荐在生产环境自动创建 Topic</h2><ul>
<li>安全性问题：自动创建 <code>Topic</code> 可能会导致未经授权的 <code>Topic</code> 被创建，从而增加了系统的安全风险。</li>
<li>风险控制问题：如果允许自动创建 <code>Topic</code>，可能会导致系统中出现大量不必要的 <code>Topic</code>，增加了管理和维护的难度。</li>
<li>配置管理问题：手动创建 <code>Topic</code> 可以更好地控制 <code>Topic</code> 的配置，包括分区数、副本数等，而自动创建 <code>Topic</code> 则可能会导致配置的混乱和不一致。</li>
<li>性能问题：大量的不必要的 <code>Topic</code> 会增加系统的负载和资源消耗，影响系统的性能表现</li>
</ul>
<h2 id="Producer-选择-MessageQueue-的逻辑"><a href="#Producer-选择-MessageQueue-的逻辑" class="headerlink" title="Producer 选择 MessageQueue 的逻辑"></a>Producer 选择 MessageQueue 的逻辑</h2><p><code>Producer</code> 发送消息的核心逻辑是在 <code>DefaultMQProducerImpl#sendDefaultImpl</code> 方法中， 接下来就看一下这个方法</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">DefaultMQProducerImpl</span> &#123;<br>    <span class="hljs-keyword">private</span> SendResult <span class="hljs-title function_">sendDefaultImpl</span><span class="hljs-params">(</span><br><span class="hljs-params">        Message msg,</span><br><span class="hljs-params">        <span class="hljs-keyword">final</span> CommunicationMode communicationMode,</span><br><span class="hljs-params">        <span class="hljs-keyword">final</span> SendCallback sendCallback,</span><br><span class="hljs-params">        <span class="hljs-keyword">final</span> <span class="hljs-type">long</span> timeout</span><br><span class="hljs-params">    )</span> <span class="hljs-keyword">throws</span> MQClientException, RemotingException, MQBrokerException, InterruptedException &#123;<br>        <span class="hljs-built_in">this</span>.makeSureStateOK();<br>        Validators.checkMessage(msg, <span class="hljs-built_in">this</span>.defaultMQProducer);<br>        <span class="hljs-keyword">final</span> <span class="hljs-type">long</span> <span class="hljs-variable">invokeID</span> <span class="hljs-operator">=</span> random.nextLong();<br>        <span class="hljs-type">long</span> <span class="hljs-variable">beginTimestampFirst</span> <span class="hljs-operator">=</span> System.currentTimeMillis();<br>        <span class="hljs-type">long</span> <span class="hljs-variable">beginTimestampPrev</span> <span class="hljs-operator">=</span> beginTimestampFirst;<br>        <span class="hljs-type">long</span> <span class="hljs-variable">endTimestamp</span> <span class="hljs-operator">=</span> beginTimestampFirst;<br>        <span class="hljs-comment">// 获取 Topic, MessageQueue 等数据</span><br>        <span class="hljs-type">TopicPublishInfo</span> <span class="hljs-variable">topicPublishInfo</span> <span class="hljs-operator">=</span> <span class="hljs-built_in">this</span>.tryToFindTopicPublishInfo(msg.getTopic());<br>        <span class="hljs-keyword">if</span> (topicPublishInfo != <span class="hljs-literal">null</span> &amp;&amp; topicPublishInfo.ok()) &#123;<br>            <span class="hljs-type">boolean</span> <span class="hljs-variable">callTimeout</span> <span class="hljs-operator">=</span> <span class="hljs-literal">false</span>;<br>            <span class="hljs-type">MessageQueue</span> <span class="hljs-variable">mq</span> <span class="hljs-operator">=</span> <span class="hljs-literal">null</span>;<br>            <span class="hljs-type">Exception</span> <span class="hljs-variable">exception</span> <span class="hljs-operator">=</span> <span class="hljs-literal">null</span>;<br>            <span class="hljs-type">SendResult</span> <span class="hljs-variable">sendResult</span> <span class="hljs-operator">=</span> <span class="hljs-literal">null</span>;<br>            <span class="hljs-type">int</span> <span class="hljs-variable">timesTotal</span> <span class="hljs-operator">=</span> communicationMode == CommunicationMode.SYNC ? <span class="hljs-number">1</span> + <span class="hljs-built_in">this</span>.defaultMQProducer.getRetryTimesWhenSendFailed() : <span class="hljs-number">1</span>;<br>            <span class="hljs-type">int</span> <span class="hljs-variable">times</span> <span class="hljs-operator">=</span> <span class="hljs-number">0</span>;<br>            String[] brokersSent = <span class="hljs-keyword">new</span> <span class="hljs-title class_">String</span>[timesTotal];<br>            <span class="hljs-keyword">for</span> (; times &lt; timesTotal; times++) &#123;<br>                <span class="hljs-type">String</span> <span class="hljs-variable">lastBrokerName</span> <span class="hljs-operator">=</span> <span class="hljs-literal">null</span> == mq ? <span class="hljs-literal">null</span> : mq.getBrokerName();<br>                <span class="hljs-comment">// selectOneMessageQueue 方法就是选择 一个 MessageQueue 进行发送</span><br>                <span class="hljs-type">MessageQueue</span> <span class="hljs-variable">mqSelected</span> <span class="hljs-operator">=</span> <span class="hljs-built_in">this</span>.selectOneMessageQueue(topicPublishInfo, lastBrokerName);<br>                <span class="hljs-comment">// 后续代码省略...</span><br>            &#125;<br>        &#125;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<h3 id="选择-MessageQueue-逻辑重点1-–-Topic和Queue数据的来源"><a href="#选择-MessageQueue-逻辑重点1-–-Topic和Queue数据的来源" class="headerlink" title="选择 MessageQueue 逻辑重点1 – Topic和Queue数据的来源"></a>选择 MessageQueue 逻辑重点1 – Topic和Queue数据的来源</h3><p>选择的 <code>MessageQueue</code> 数据来源是从  <code>this.tryToFindTopicPublishInfo(msg.getTopic());</code> 方法的返回值中获取的， 返回值是 <code>TopicPublishInfo</code>， 比如还是以前面两个 <code>Broker</code> 为例，获取的结果如下图所示<br><img src="https://s2.loli.net/2023/12/22/PCEi9JLVpn1KeZY.png" alt="Topic和Queue数据来源"><br>而 <code>tryToFindTopicPublishInfo</code> 方法也是从 <code>NameServer</code> 中获取的，发送的指令类型是 <code>RequestCode.GET_ROUTEINFO_BY_TOPIC</code>， 这里可以参考</p>
<ol><li><a href="/2023/e17cf930f952/index.html" title="RocketMQ4中Nameserver详解">RocketMQ4中Nameserver详解</a></li></ol>

<p>这里简单描述一下整体的逻辑</p>
<ol>
<li><code>Broker</code> 启动时会加载 <code>ROCKETMQ_HOME/store/config/topics.json</code> 文件，这里面保存的就是当前 <code>Broker</code> 中创建的 <code>Topic</code> 数据, 就像下面这种形式(省略了其他 <code>Topic</code> 的数据)</li>
</ol>
<figure class="highlight json"><table><tr><td class="code"><pre><code class="hljs json"><span class="hljs-punctuation">&#123;</span><br>	<span class="hljs-attr">&quot;dataVersion&quot;</span><span class="hljs-punctuation">:</span><span class="hljs-punctuation">&#123;</span><br>		<span class="hljs-attr">&quot;counter&quot;</span><span class="hljs-punctuation">:</span><span class="hljs-number">3</span><span class="hljs-punctuation">,</span><br>		<span class="hljs-attr">&quot;timestamp&quot;</span><span class="hljs-punctuation">:</span><span class="hljs-number">1703223338653</span><br>	<span class="hljs-punctuation">&#125;</span><span class="hljs-punctuation">,</span><br>	<span class="hljs-attr">&quot;topicConfigTable&quot;</span><span class="hljs-punctuation">:</span><span class="hljs-punctuation">&#123;</span><br>		<span class="hljs-attr">&quot;TopicTest&quot;</span><span class="hljs-punctuation">:</span><span class="hljs-punctuation">&#123;</span><br>			<span class="hljs-attr">&quot;order&quot;</span><span class="hljs-punctuation">:</span><span class="hljs-literal"><span class="hljs-keyword">false</span></span><span class="hljs-punctuation">,</span><br>			<span class="hljs-attr">&quot;perm&quot;</span><span class="hljs-punctuation">:</span><span class="hljs-number">6</span><span class="hljs-punctuation">,</span><br>			<span class="hljs-attr">&quot;readQueueNums&quot;</span><span class="hljs-punctuation">:</span><span class="hljs-number">4</span><span class="hljs-punctuation">,</span><br>			<span class="hljs-attr">&quot;topicFilterType&quot;</span><span class="hljs-punctuation">:</span><span class="hljs-string">&quot;SINGLE_TAG&quot;</span><span class="hljs-punctuation">,</span><br>			<span class="hljs-attr">&quot;topicName&quot;</span><span class="hljs-punctuation">:</span><span class="hljs-string">&quot;TopicTest&quot;</span><span class="hljs-punctuation">,</span><br>			<span class="hljs-attr">&quot;topicSysFlag&quot;</span><span class="hljs-punctuation">:</span><span class="hljs-number">0</span><span class="hljs-punctuation">,</span><br>			<span class="hljs-attr">&quot;writeQueueNums&quot;</span><span class="hljs-punctuation">:</span><span class="hljs-number">4</span><br>		<span class="hljs-punctuation">&#125;</span><br>	<span class="hljs-punctuation">&#125;</span><br><span class="hljs-punctuation">&#125;</span><br></code></pre></td></tr></table></figure>
<ol start="2">
<li><code>Broker</code> 加载完 <code>Topic</code> 数据后会发送 <code>RequestCode.REGISTER_BROKER</code> 指令，将自身的数据发送到 <code>NameServer</code> 中</li>
<li><code>NameServer</code> 接收到 <code>RequestCode.REGISTER_BROKER</code> 会解析出请求中的数据保存到 <code>RouteInfoManager</code> 类中</li>
</ol>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">RouteInfoManager</span> &#123;<br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-type">InternalLogger</span> <span class="hljs-variable">log</span> <span class="hljs-operator">=</span> InternalLoggerFactory.getLogger(LoggerName.NAMESRV_LOGGER_NAME);<br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> <span class="hljs-keyword">static</span> <span class="hljs-type">long</span> <span class="hljs-variable">BROKER_CHANNEL_EXPIRED_TIME</span> <span class="hljs-operator">=</span> <span class="hljs-number">1000</span> * <span class="hljs-number">60</span> * <span class="hljs-number">2</span>;<br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> <span class="hljs-type">ReadWriteLock</span> <span class="hljs-variable">lock</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">ReentrantReadWriteLock</span>();<br>    <span class="hljs-comment">//  保存 topic， broker, queue 相关的数据</span><br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> HashMap&lt;String<span class="hljs-comment">/* topic */</span>, Map&lt;String <span class="hljs-comment">/* brokerName */</span> , QueueData&gt;&gt; topicQueueTable;<br>    <span class="hljs-comment">// 保存 broker 地址相关数据</span><br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> HashMap&lt;String<span class="hljs-comment">/* brokerName */</span>, BrokerData&gt; brokerAddrTable;<br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> HashMap&lt;String<span class="hljs-comment">/* clusterName */</span>, Set&lt;String<span class="hljs-comment">/* brokerName */</span>&gt;&gt; clusterAddrTable;<br>    <span class="hljs-comment">// 保存存活的 broker 数据</span><br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> HashMap&lt;String<span class="hljs-comment">/* brokerAddr */</span>, BrokerLiveInfo&gt; brokerLiveTable;<br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> HashMap&lt;String<span class="hljs-comment">/* brokerAddr */</span>, List&lt;String&gt;<span class="hljs-comment">/* Filter Server */</span>&gt; filterServerTable;<br>&#125;<br></code></pre></td></tr></table></figure>
<ol start="4">
<li><code>Producer</code> 发送 <code>RequestCode.GET_ROUTEINFO_BY_TOPIC</code> 指令从 <code>NameServer</code> 查询数据其实就是获取 <code>Broker</code> 注册时给 <code>NameServer</code> 的数据，也就是从 <code>RouteInfoManager</code> 类中相关属性获取</li>
</ol>
<h3 id="选择-MessageQueue-逻辑重点2-–-Broker的避让"><a href="#选择-MessageQueue-逻辑重点2-–-Broker的避让" class="headerlink" title="选择 MessageQueue 逻辑重点2 – Broker的避让"></a>选择 MessageQueue 逻辑重点2 – Broker的避让</h3><p>对于选择 <code>MessageQueue</code> 还需要关注的就是有一个选择 <code>Broker</code> 时的 <strong>避让</strong> 逻辑，即当前面发送消息失败时，在重试的时候不会选择到上一次发送的 <code>Broker</code> ( 因为 <code>Queue</code> 是分配在 <code>Broker</code> 上的，所以选择 <code>Queue</code> 也可以认为就是选择 <code>Broker</code> )</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-comment">// org.apache.rocketmq.client.latency.MQFaultStrategy#selectOneMessageQueue 类</span><br><span class="hljs-keyword">public</span> MessageQueue <span class="hljs-title function_">selectOneMessageQueue</span><span class="hljs-params">(<span class="hljs-keyword">final</span> TopicPublishInfo tpInfo, <span class="hljs-keyword">final</span> String lastBrokerName, <span class="hljs-keyword">final</span> <span class="hljs-type">boolean</span> resetIndex)</span> &#123;<br>    <span class="hljs-comment">// 如果是重试的话，lastBrokerName就不为空，BrokerFilter 中就包含 lastBrokerName 字段</span><br>    <span class="hljs-type">BrokerFilter</span> <span class="hljs-variable">brokerFilter</span> <span class="hljs-operator">=</span> threadBrokerFilter.get();<br>    brokerFilter.setLastBrokerName(lastBrokerName);<br>    <span class="hljs-comment">// 省略部分代码</span><br><br>    <span class="hljs-comment">// 这里 BrokerFilter 就是用来过滤上一次发送失败的 Broker 的</span><br>    <span class="hljs-type">MessageQueue</span> <span class="hljs-variable">mq</span> <span class="hljs-operator">=</span> tpInfo.selectOneMessageQueue(brokerFilter);<br>    <span class="hljs-keyword">if</span> (mq != <span class="hljs-literal">null</span>) &#123;<br>        <span class="hljs-keyword">return</span> mq;<br>    &#125;<br>    <span class="hljs-keyword">return</span> tpInfo.selectOneMessageQueue();<br>&#125;<br><br><span class="hljs-comment">//  selectOneMessageQueue 类的内部类</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">BrokerFilter</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">QueueFilter</span> &#123;<br>    <span class="hljs-keyword">private</span> String lastBrokerName;<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">setLastBrokerName</span><span class="hljs-params">(String lastBrokerName)</span> &#123;<br>        <span class="hljs-built_in">this</span>.lastBrokerName = lastBrokerName;<br>    &#125;<br><br>    <span class="hljs-meta">@Override</span> <span class="hljs-keyword">public</span> <span class="hljs-type">boolean</span> <span class="hljs-title function_">filter</span><span class="hljs-params">(MessageQueue mq)</span> &#123;<br>        <span class="hljs-keyword">if</span> (lastBrokerName != <span class="hljs-literal">null</span>) &#123;<br>            <span class="hljs-keyword">return</span> !mq.getBrokerName().equals(lastBrokerName);<br>        &#125;<br>        <span class="hljs-keyword">return</span> <span class="hljs-literal">true</span>;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-comment">//  TopicPublishInfo 类</span><br><span class="hljs-keyword">private</span> MessageQueue <span class="hljs-title function_">selectOneMessageQueue</span><span class="hljs-params">(List&lt;MessageQueue&gt; messageQueueList, ThreadLocalIndex sendQueue, QueueFilter ...filter)</span> &#123;<br>    <span class="hljs-keyword">if</span> (messageQueueList == <span class="hljs-literal">null</span> || messageQueueList.isEmpty()) &#123;<br>        <span class="hljs-keyword">return</span> <span class="hljs-literal">null</span>;<br>    &#125;<br><br>    <span class="hljs-keyword">if</span> (filter != <span class="hljs-literal">null</span> &amp;&amp; filter.length != <span class="hljs-number">0</span>) &#123;<br>        <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> <span class="hljs-variable">i</span> <span class="hljs-operator">=</span> <span class="hljs-number">0</span>; i &lt; messageQueueList.size(); i++) &#123;<br>            <span class="hljs-comment">//  对队列的数量进行取模运算</span><br>            <span class="hljs-type">int</span> <span class="hljs-variable">index</span> <span class="hljs-operator">=</span> Math.abs(sendQueue.incrementAndGet() % messageQueueList.size());<br>            <span class="hljs-type">MessageQueue</span> <span class="hljs-variable">mq</span> <span class="hljs-operator">=</span> messageQueueList.get(index);<br>            <span class="hljs-type">boolean</span> <span class="hljs-variable">filterResult</span> <span class="hljs-operator">=</span> <span class="hljs-literal">true</span>;<br>            <span class="hljs-comment">// 进行过滤，调用 QueueFilter的filter 方法</span><br>            <span class="hljs-keyword">for</span> (QueueFilter f: filter) &#123;<br>                Preconditions.checkNotNull(f);<br>                filterResult &amp;= f.filter(mq);<br>            &#125;<br>            <span class="hljs-keyword">if</span> (filterResult) &#123;<br>                <span class="hljs-keyword">return</span> mq;<br>            &#125;<br>        &#125;<br>        <span class="hljs-keyword">return</span> <span class="hljs-literal">null</span>;<br>    &#125;<br><br>    <span class="hljs-type">int</span> <span class="hljs-variable">index</span> <span class="hljs-operator">=</span> Math.abs(sendQueue.incrementAndGet() % messageQueueList.size());<br>    <span class="hljs-keyword">return</span> messageQueueList.get(index);<br>&#125;<br></code></pre></td></tr></table></figure>
<h4 id="为什么需要避让逻辑而不是直接删除-Broker-信息"><a href="#为什么需要避让逻辑而不是直接删除-Broker-信息" class="headerlink" title="为什么需要避让逻辑而不是直接删除 Broker 信息"></a>为什么需要避让逻辑而不是直接删除 Broker 信息</h4><p>当选择一个 <code>Broker</code> 发送消息时若发送失败，说明 <code>Broker</code> 可能宕机了，对于此次消息发送而再次选择时最好时避免选到该 <code>Broker</code>，所以需要记录上一次发送的 <code>Broker</code>, 再次选择时避免选择到</p>
<p>那么为什么要记录上一次发送失败的 <code>Broker</code>，而不是直接删除失败的 <code>Broker</code> 信息呢？因为 <code>Producer</code> 会定期从 <code>NameServer</code> 获取 <code>Broker</code> 的状态，所以维护 <code>Broker</code> 状态的这件事情其实是有另外的线程在做，那么对于此次发送消息的线程来说只要简单排除一下发送失败的 <code>Broker</code> 即可</p>
<h2 id="readQueue-和-writeQueue-的作用"><a href="#readQueue-和-writeQueue-的作用" class="headerlink" title="readQueue 和 writeQueue 的作用"></a>readQueue 和 writeQueue 的作用</h2><p>在 <code>RocketMQ</code> 中 <code>Queue</code> 的数量分为 <code>readQueueNums</code> 和 <code>writeQueueNums</code>， 通过 <code>org.apache.rocketmq.common.protocol.route.QueueData</code> 的属性也能看出来</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">QueueData</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">Comparable</span>&lt;QueueData&gt; &#123;<br>    <span class="hljs-keyword">private</span> String brokerName;<br>    <span class="hljs-keyword">private</span> <span class="hljs-type">int</span> readQueueNums;<br>    <span class="hljs-keyword">private</span> <span class="hljs-type">int</span> writeQueueNums;<br>    <span class="hljs-keyword">private</span> <span class="hljs-type">int</span> perm;<br>    <span class="hljs-keyword">private</span> <span class="hljs-type">int</span> topicSysFlag;<br>&#125;<br></code></pre></td></tr></table></figure>

<p><strong>不过这里需要注意的是，这并不代表有两种类型的 Queue, 这里的 readQueueNums 和 writeQueueNums 只是单纯的代表数量</strong></p>
<p><strong>readQueueNums</strong><br><code>readQueueNums</code> 是对消费者起作用，因为消费者启动时需要读取 <code>Queue</code> 的数量（这个数量就是 <code>readQueueNums</code> ），然后将消费者与 <code>Queue</code> 进行绑定，也就是每个消费者需要消费哪些 <code>Queue</code> 的消息，物理上实际存在的 <code>Queue</code> 的数量</p>
<p><strong>writeQueueNums</strong><br><code>writeQueueNums</code> 是对生产者起作用，因为生产者启动时需要知道 <code>Queue</code> 的数量（这个数量就是 <code>writeQueueNums</code> ），然后选择一个 <code>Queue</code> 将消息写入，物理上会创建的 <code>Queue</code> 的数量是等于 <code>writeQueueNums</code> 的。</p>
<ul>
<li>一般情况下 <code>readQueueNums = writeQueueNums</code></li>
<li>当 <code>readQueueNums &gt; writeQueueNums</code>, 比如 <code>readQueueNums=5</code>， <code>writeQueueNums=3</code> 时，此时的场景如下图所示<pre class="mermaid">graph LR
  subgraph B["队列"]
      direction TB
          B1["Queue0 (真实存在)"]
          B2["Queue1 (真实存在)"]
          B3["Queue2 (真实存在)"]
          style B1 fill:#70b224
          style B2 fill:#70b224
          style B3 fill:#70b224
          B4["Queue3 (不存在)"]
          B5["Queue4 (不存在)"]
          style B4 fill:#f9f
          style B5 fill:#f9f
  end

  subgraph C["消费者组"]
      direction TB
      C1["消费者1"]
      C2["消费者2"]
      
  end
  C1-->B1
  C1-->B2
  C2-->B3
  C2-..->B4
  C2-..->B5</pre></li>
</ul>
<p>从上图可知，虽然 <code>Queue3</code> 和 <code>Queue4</code> 被分配给了 <strong>消费者2</strong>， 但是这两个 <code>Queue</code> 实际并不存在，所以这两个 <code>Queue</code> 中也不会有数据，如果极端一点，有五个消费者，那么每一个消费者都会分配到一个 <code>Queue</code>,这样就会有两个消费者根本没有数据消费</p>
<h3 id="readQueueNums-writeQueueNums-的场景"><a href="#readQueueNums-writeQueueNums-的场景" class="headerlink" title="readQueueNums &gt; writeQueueNums 的场景"></a><code>readQueueNums &gt; writeQueueNums</code> 的场景</h3><ul>
<li>这种场景是为了平滑<strong>扩容消费者</strong>的场景准备的， 消费者的并发量是不可能超过 <code>Queue</code> 的数量的，要想增加消费者，很可能就要增加 <code>Queue</code>, 假如只有一种类型的 <code>Queue</code>, 当增加 <code>Queue</code> 之后，原来每个消费者的压力可能会增大，这是有一定风险的</li>
<li><code>readQueue</code> 和 <code>writeQueue</code> 分开之后，就可以在不停机的情况下先增加 <code>readQueue</code> 的数量，这些增加的 <code>readQueue</code> 是没有数据的，然后再增加消费者，这样消费者压力并没有增大，最后再增加 <code>writeQueue</code> 数量和 <code>readQueue</code> 数量一致，就完成了扩容</li>
</ul>
<h3 id="writeQueueNums-readQueueNums-的场景"><a href="#writeQueueNums-readQueueNums-的场景" class="headerlink" title="writeQueueNums &gt; readQueueNums 的场景"></a><code>writeQueueNums &gt; readQueueNums</code> 的场景</h3><ul>
<li>这种场景是为了缩容的场景准备的， 先减少 <code>writeQueue</code> 的数量， 比如从 5 -&gt; 3, 当 4 号和 5 号 <code>Queue</code> 消费完成后，再将 <code>readQueue</code> 数量减少到 3， 最好停掉部分消费者，就完成了缩容</li>
</ul>
<h2 id="批量消息"><a href="#批量消息" class="headerlink" title="批量消息"></a>批量消息</h2><ul>
<li>批量消息发送是将同一主题的多条消息一起打包发送到消息服务端，减少网络调用次数，提高网络传输效率</li>
<li>单批次消息发送总长度不能超过 <code>MQProducer#maxMessageSize</code></li>
<li>批量消息发送要解决的是如何将这些消息编码以便服务端能够正确解码出每条消息的消息内容</li>
</ul>
<p>非批量消息使用的消息承载结构是 <code>Message</code> 类<br>批量消息使用的消息承载结构是 <code>MessageBatch</code></p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">Message</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">Serializable</span> &#123;<br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-type">long</span> <span class="hljs-variable">serialVersionUID</span> <span class="hljs-operator">=</span> <span class="hljs-number">8445773977080406428L</span>;<br><br>    <span class="hljs-keyword">private</span> String topic;<br>    <span class="hljs-keyword">private</span> <span class="hljs-type">int</span> flag;<br>    <span class="hljs-keyword">private</span> Map&lt;String, String&gt; properties;<br>    <span class="hljs-keyword">private</span> <span class="hljs-type">byte</span>[] body;<br>    <span class="hljs-keyword">private</span> String transactionId;<br>&#125;<br><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">MessageBatch</span> <span class="hljs-keyword">extends</span> <span class="hljs-title class_">Message</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">Iterable</span>&lt;Message&gt; &#123;<br><br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-type">long</span> <span class="hljs-variable">serialVersionUID</span> <span class="hljs-operator">=</span> <span class="hljs-number">621335151046335557L</span>;<br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> List&lt;Message&gt; messages;<br><br>    <span class="hljs-keyword">private</span> <span class="hljs-title function_">MessageBatch</span><span class="hljs-params">(List&lt;Message&gt; messages)</span> &#123;<br>        <span class="hljs-built_in">this</span>.messages = messages;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<p><code>MessageBatch</code> 继承了 <code>Message</code>, 同时含有 <code>List&lt;Message&gt; messages</code> 属性，所以消息发送和单挑消息发送逻辑没有任何区别（ <code>java</code> 多态的使用，方法中接收 <code>Message</code> 类型的参数也可以接收 <code>MessageBatch</code> ）</p>
<p>每一条消息传递的时候最终是要序列化成 <code>byte[]</code>, 批量消息的发送流程没有任何变化，但是消息编码和解析是不同的，在 <code>MessageBatch</code> 中有一个核心方法就是对消息进行编码的</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">MessageBatch</span> <span class="hljs-keyword">extends</span> <span class="hljs-title class_">Message</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">Iterable</span>&lt;Message&gt; &#123;<br><br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-type">long</span> <span class="hljs-variable">serialVersionUID</span> <span class="hljs-operator">=</span> <span class="hljs-number">621335151046335557L</span>;<br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> List&lt;Message&gt; messages;<br><br>    <span class="hljs-keyword">private</span> <span class="hljs-title function_">MessageBatch</span><span class="hljs-params">(List&lt;Message&gt; messages)</span> &#123;<br>        <span class="hljs-built_in">this</span>.messages = messages;<br>    &#125;<br><br>    <span class="hljs-comment">// 对消息进行编码，最终调用的是 MessageDecoder.encodeMessages 方法</span><br>    <span class="hljs-keyword">public</span> <span class="hljs-type">byte</span>[] encode() &#123;<br>        <span class="hljs-keyword">return</span> MessageDecoder.encodeMessages(messages);<br>    &#125;<br>&#125;<br><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">MessageDecoder</span> &#123;<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-type">byte</span>[] encodeMessages(List&lt;Message&gt; messages) &#123;<br>        <span class="hljs-comment">//TO DO refactor, accumulate in one buffer, avoid copies</span><br>        List&lt;<span class="hljs-type">byte</span>[]&gt; encodedMessages = <span class="hljs-keyword">new</span> <span class="hljs-title class_">ArrayList</span>&lt;<span class="hljs-type">byte</span>[]&gt;(messages.size());<br>        <span class="hljs-type">int</span> <span class="hljs-variable">allSize</span> <span class="hljs-operator">=</span> <span class="hljs-number">0</span>;<br>        <span class="hljs-keyword">for</span> (Message message : messages) &#123;<br>            <span class="hljs-type">byte</span>[] tmp = encodeMessage(message);<br>            encodedMessages.add(tmp);<br>            allSize += tmp.length;<br>        &#125;<br>        <span class="hljs-type">byte</span>[] allBytes = <span class="hljs-keyword">new</span> <span class="hljs-title class_">byte</span>[allSize];<br>        <span class="hljs-type">int</span> <span class="hljs-variable">pos</span> <span class="hljs-operator">=</span> <span class="hljs-number">0</span>;<br>        <span class="hljs-keyword">for</span> (<span class="hljs-type">byte</span>[] bytes : encodedMessages) &#123;<br>            System.arraycopy(bytes, <span class="hljs-number">0</span>, allBytes, pos, bytes.length);<br>            pos += bytes.length;<br>        &#125;<br>        <span class="hljs-keyword">return</span> allBytes;<br>    &#125;<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-type">byte</span>[] encodeMessage(Message message) &#123;<br>        <span class="hljs-comment">//only need flag, body, properties</span><br>        <span class="hljs-type">byte</span>[] body = message.getBody();<br>        <span class="hljs-type">int</span> <span class="hljs-variable">bodyLen</span> <span class="hljs-operator">=</span> body.length;<br>        <span class="hljs-type">String</span> <span class="hljs-variable">properties</span> <span class="hljs-operator">=</span> messageProperties2String(message.getProperties());<br>        <span class="hljs-type">byte</span>[] propertiesBytes = properties.getBytes(CHARSET_UTF8);<br>        <span class="hljs-comment">//note properties length must not more than Short.MAX</span><br>        <span class="hljs-type">int</span> <span class="hljs-variable">propsLen</span> <span class="hljs-operator">=</span> propertiesBytes.length;<br>        <span class="hljs-keyword">if</span> (propsLen &gt; Short.MAX_VALUE)<br>            <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">RuntimeException</span>(String.format(<span class="hljs-string">&quot;Properties size of message exceeded, properties size: &#123;&#125;, maxSize: &#123;&#125;.&quot;</span>, propsLen, Short.MAX_VALUE));<br>        <span class="hljs-type">short</span> <span class="hljs-variable">propertiesLength</span> <span class="hljs-operator">=</span> (<span class="hljs-type">short</span>) propsLen;<br>        <span class="hljs-type">int</span> <span class="hljs-variable">sysFlag</span> <span class="hljs-operator">=</span> message.getFlag();<br>        <span class="hljs-type">int</span> <span class="hljs-variable">storeSize</span> <span class="hljs-operator">=</span> <span class="hljs-number">4</span> <span class="hljs-comment">// 1 TOTALSIZE</span><br>            + <span class="hljs-number">4</span> <span class="hljs-comment">// 2 MAGICCOD</span><br>            + <span class="hljs-number">4</span> <span class="hljs-comment">// 3 BODYCRC</span><br>            + <span class="hljs-number">4</span> <span class="hljs-comment">// 4 FLAG</span><br>            + <span class="hljs-number">4</span> + bodyLen <span class="hljs-comment">// 4 BODY</span><br>            + <span class="hljs-number">2</span> + propertiesLength;<br>        <span class="hljs-type">ByteBuffer</span> <span class="hljs-variable">byteBuffer</span> <span class="hljs-operator">=</span> ByteBuffer.allocate(storeSize);<br>        <span class="hljs-comment">// 1 TOTALSIZE</span><br>        byteBuffer.putInt(storeSize);<br><br>        <span class="hljs-comment">// 2 MAGICCODE</span><br>        byteBuffer.putInt(<span class="hljs-number">0</span>);<br><br>        <span class="hljs-comment">// 3 BODYCRC</span><br>        byteBuffer.putInt(<span class="hljs-number">0</span>);<br><br>        <span class="hljs-comment">// 4 FLAG</span><br>        <span class="hljs-type">int</span> <span class="hljs-variable">flag</span> <span class="hljs-operator">=</span> message.getFlag();<br>        byteBuffer.putInt(flag);<br><br>        <span class="hljs-comment">// 5 BODY</span><br>        byteBuffer.putInt(bodyLen);<br>        byteBuffer.put(body);<br><br>        <span class="hljs-comment">// 6 properties</span><br>        byteBuffer.putShort(propertiesLength);<br>        byteBuffer.put(propertiesBytes);<br><br>        <span class="hljs-keyword">return</span> byteBuffer.array();<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<p>最终所有的消息内容存放在 <code>byte[]</code> 中，那么要怎么区分每一个消息占用的部分是多少呢？ 比如一共 80 字节，5个消息，要知道每个消息的内容不同，消息长度肯定不同，所以不可能是平分，所以必然是要设计出 <strong>不变的结构 + 可变的内容</strong></p>
<h3 id="不变的结构"><a href="#不变的结构" class="headerlink" title="不变的结构"></a>不变的结构</h3><p>从上面 <code>encodeMessage</code> 这个方法可以看出是如何对单挑消息进行编码的</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-type">ByteBuffer</span> <span class="hljs-variable">byteBuffer</span> <span class="hljs-operator">=</span> ByteBuffer.allocate(storeSize);<br><span class="hljs-comment">// 1 TOTALSIZE</span><br>byteBuffer.putInt(storeSize);<br><span class="hljs-comment">// 2 MAGICCODE</span><br>byteBuffer.putInt(<span class="hljs-number">0</span>);<br><span class="hljs-comment">// 3 BODYCRC</span><br>byteBuffer.putInt(<span class="hljs-number">0</span>);<br><span class="hljs-comment">// 4 FLAG</span><br><span class="hljs-type">int</span> <span class="hljs-variable">flag</span> <span class="hljs-operator">=</span> message.getFlag();<br>byteBuffer.putInt(flag);<br><span class="hljs-comment">// 5 BODY</span><br>byteBuffer.putInt(bodyLen);<br>byteBuffer.put(body);<br><span class="hljs-comment">// 6 properties</span><br>byteBuffer.putShort(propertiesLength);<br>byteBuffer.put(propertiesBytes);<br><span class="hljs-keyword">return</span> byteBuffer.array();<br></code></pre></td></tr></table></figure>
<p>那么那部分是不变的呢？ 其实看 <code>byteBuffer.putInt()</code>, <code>byteBuffer.putShort()</code> 等方法就知道这是不变的部分，因为这里是添加固定长度的字节，而对于 <code>byteBuffer.put（）</code> 方法就是可变的部分</p>
<pre class="mermaid">graph LR
    subgraph M1["第一条消息"]
        A["总长度(TOTALSIZE) 固定长度，4字节"]
        B["魔数（MAGICCODE）固定长度 4字节"]
        C["(BODYCRC) 固定长度 4字节"]
        D["(FLAG) 固定长度 4字节"]
        subgraph E["消息主体"]
            E1["消息长度（body_length）固定长度 4字节"]
            E2["具体消息内容，长度可变，具体长度
            由前面消息长度字段定义"]
        end
        subgraph F["properties 属性"]
            F1["属性长度（properties_length）固定长度 2字节"]
            F2["具体属性内容，长度可变，具体长度
            由前面属性长度字段定义"]
        end
    end
    subgraph M2["第一条消息"]
        A2["总长度(TOTALSIZE) 固定长度，4字节"]
        B2["魔数（MAGICCODE）固定长度 4字节"]
        C2["(BODYCRC) 固定长度 4字节"]
        D2["(FLAG) 固定长度 4字节"]
        subgraph EE["消息主体"]
            EE1["消息长度（body_length）固定长度 4字节"]
            EE2["具体消息内容，长度可变，具体长度
            由前面消息长度字段定义"]
        end
        subgraph FF["properties 属性"]
            FF1["属性长度（properties_length）固定长度 2字节"]
            FF2["具体属性内容，长度可变，具体长度
            由前面属性长度字段定义"]
        end
    end</pre>]]></content>
      <categories>
        <category>中间件</category>
        <category>RocketMQ</category>
      </categories>
      <tags>
        <tag>RocketMQ</tag>
      </tags>
  </entry>
  <entry>
    <title>RocketMQ中Message详解</title>
    <url>/2023/cd2da7828738/index.html</url>
    <content><![CDATA[<h2 id="Message-类结构"><a href="#Message-类结构" class="headerlink" title="Message 类结构"></a>Message 类结构</h2><p>在 <code>RocketMQ</code> 的子模块 <code>Common</code> 中有 <code>org.apache.rocketmq.common.message.Message</code> 类， 包含的属性有</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">Message</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">Serializable</span> &#123;<br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-type">long</span> <span class="hljs-variable">serialVersionUID</span> <span class="hljs-operator">=</span> <span class="hljs-number">8445773977080406428L</span>;<br><br>    <span class="hljs-keyword">private</span> String topic;<br>    <span class="hljs-keyword">private</span> <span class="hljs-type">int</span> flag;<br>    <span class="hljs-keyword">private</span> Map&lt;String, String&gt; properties;<br>    <span class="hljs-keyword">private</span> <span class="hljs-type">byte</span>[] body;<br>    <span class="hljs-keyword">private</span> String transactionId;<br>&#125;<br></code></pre></td></tr></table></figure>

<ul>
<li><code>topic</code>： 消息要发往到那个 <code>topic</code> 中</li>
<li><code>flag</code>: 使用时自己设置，<code>RocketMQ</code> 本身并不会使用这个字段，但是会帮我们从生产者端直接传递到消费者端</li>
<li><code>properties</code>： 这个属性是用来传递一些额外的信息，本质就是一个 <code>Map</code></li>
<li><code>body</code>： 消息体</li>
<li><code>transactionId</code>： 事务消息的唯一标识，如果是普通消息，则为空</li>
</ul>
<h2 id="Message-构造方法"><a href="#Message-构造方法" class="headerlink" title="Message 构造方法"></a>Message 构造方法</h2><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">Message</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">Serializable</span> &#123;<br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-type">long</span> <span class="hljs-variable">serialVersionUID</span> <span class="hljs-operator">=</span> <span class="hljs-number">8445773977080406428L</span>;<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-title function_">Message</span><span class="hljs-params">()</span> &#123;&#125;<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-title function_">Message</span><span class="hljs-params">(String topic, <span class="hljs-type">byte</span>[] body)</span> &#123;&#125;<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-title function_">Message</span><span class="hljs-params">(String topic, String tags, <span class="hljs-type">byte</span>[] body)</span> &#123;&#125;<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-title function_">Message</span><span class="hljs-params">(String topic, String tags, String keys, <span class="hljs-type">byte</span>[] body)</span> &#123;&#125;<br><br>    <span class="hljs-comment">// 前面所有构造方法最终都是调用这个构造方法，所以只列出来最终的这个构造方法代码</span><br>    <span class="hljs-comment">// waitStoreMsgOK 表示是否要在这条 Message 落到磁盘上之后才返回应答这里该变量的值默认为 true</span><br>    <span class="hljs-keyword">public</span> <span class="hljs-title function_">Message</span><span class="hljs-params">(String topic, String tags, String keys, <span class="hljs-type">int</span> flag, <span class="hljs-type">byte</span>[] body, <span class="hljs-type">boolean</span> waitStoreMsgOK)</span> &#123;<br>        <span class="hljs-built_in">this</span>.topic = topic;<br>        <span class="hljs-built_in">this</span>.flag = flag;<br>        <span class="hljs-built_in">this</span>.body = body;<br><br>        <span class="hljs-comment">// 针对 Tag 字段的处理实际是将其保存在 properties 中</span><br>        <span class="hljs-keyword">if</span> (tags != <span class="hljs-literal">null</span> &amp;&amp; tags.length() &gt; <span class="hljs-number">0</span>) &#123;<br>            <span class="hljs-built_in">this</span>.setTags(tags);<br>        &#125;<br><br>        <span class="hljs-keyword">if</span> (keys != <span class="hljs-literal">null</span> &amp;&amp; keys.length() &gt; <span class="hljs-number">0</span>) &#123;<br>            <span class="hljs-built_in">this</span>.setKeys(keys);<br>        &#125;<br><br>        <span class="hljs-built_in">this</span>.setWaitStoreMsgOK(waitStoreMsgOK);<br>    &#125;<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">setTags</span><span class="hljs-params">(String tags)</span> &#123;<br>        <span class="hljs-built_in">this</span>.putProperty(MessageConst.PROPERTY_TAGS, tags);<br>    &#125;<br><br>    <span class="hljs-comment">// 可以看到上面调用 setTags() 方法时实际是将其保存在 properties 中</span><br>    <span class="hljs-comment">// 只是这个 key 是内部使用的key，内部 key 一般都定义在 MessageConst 类中</span><br>    <span class="hljs-comment">// 所以如果我们要自定义字段，也是通过 properties传递，但是key 就不要使用内部key了</span><br>    <span class="hljs-keyword">void</span> <span class="hljs-title function_">putProperty</span><span class="hljs-params">(<span class="hljs-keyword">final</span> String name, <span class="hljs-keyword">final</span> String value)</span> &#123;<br>        <span class="hljs-keyword">if</span> (<span class="hljs-literal">null</span> == <span class="hljs-built_in">this</span>.properties) &#123;<br>            <span class="hljs-built_in">this</span>.properties = <span class="hljs-keyword">new</span> <span class="hljs-title class_">HashMap</span>&lt;String, String&gt;();<br>        &#125;<br><br>        <span class="hljs-built_in">this</span>.properties.put(name, value);<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>


]]></content>
      <categories>
        <category>中间件</category>
        <category>RocketMQ</category>
      </categories>
      <tags>
        <tag>RocketMQ</tag>
      </tags>
  </entry>
  <entry>
    <title>RocketMQ中Producer和ProducerGroup</title>
    <url>/2023/254af85a97af/index.html</url>
    <content><![CDATA[<p>RocketMQ版本: 5.1.4</p>
<span class="hide_first_mermaid">
<div class="mermaid-wrap"><pre class="mermaid-src" hidden>
  flowchart LR
    A
  </pre></div>
</span>

<h2 id="Producer-和-ProducerGroup"><a href="#Producer-和-ProducerGroup" class="headerlink" title="Producer 和 ProducerGroup"></a>Producer 和 ProducerGroup</h2><h3 id="Producer"><a href="#Producer" class="headerlink" title="Producer"></a>Producer</h3><ul>
<li>Producer 是 RocketMQ 客户端，负责向 RocketMQ 集群发送消息。</li>
</ul>
<h3 id="ProducerGroup"><a href="#ProducerGroup" class="headerlink" title="ProducerGroup"></a>ProducerGroup</h3><ul>
<li><code>ProducerGroup</code> 是一类 <code>Producer</code>, 比如现在大多都是微服务架构，而每一个服务都是多实例的， 如下所示</li>
</ul>
<pre class="mermaid">graph LR
    PA["ProducerGroupA"]
    PA--> A1 & A2 & A3 & A4
    subgraph A["服务A"]
        A1("实例1（ProducerGroup=GroupA）")
        A2("实例2（ProducerGroup=GroupA）")
        A3("实例 …… ")
        A4("实例M（ProducerGroup=GroupA）")
    end
    subgraph B["MessageQueue"]
        B1("MessageQueue1")
        B2("MessageQueue2")
        B3("……")
        B4("MessageQueue3")
    end
    A1-->B1
    A2-->B2
    A2-->B3
    A4-->B4</pre>

<ul>
<li><p>同一个服务的不同实例属于同一个 <code>ProducerGroup</code></p>
</li>
<li><p>每一个实例可以将消息发往对应  <code>Topic</code> 的多个 <code>Queue</code> 中</p>
</li>
<li><p><code>ProducerGroup</code> 的作用是什么？</p>
</li>
</ul>
<p>上面有提到 <code>ProducerGroup</code> 是为了将生产者归类，既然是归类，那就是为了方便找到同类生产者？那为什么要找到同类生产者呢？这里和 <strong>事务消息</strong> 有关，事务消息是分成了多个阶段，如果某个生产者在事务消息的其中一个阶段失败了，那就可以找到同类生产者（也就是同一个服务的其他实例）来进行后续的操作 <strong>这里要记住的重点是属于同一个组的生产者他们发送消息的topic和对应的操作都是相同的</strong></p>
<h2 id="生产者初始化流程"><a href="#生产者初始化流程" class="headerlink" title="生产者初始化流程"></a>生产者初始化流程</h2><p>在 <code>RocketMQ</code> 源码的 <code>example</code> 模块下可以找到 <code>org.apache.rocketmq.example.simple.Producer</code> 测试类，该类就是测试消息发送流程的代码</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">Producer</span> &#123;<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-type">String</span> <span class="hljs-variable">PRODUCER_GROUP</span> <span class="hljs-operator">=</span> <span class="hljs-string">&quot;ProducerGroupName&quot;</span>;<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-type">String</span> <span class="hljs-variable">DEFAULT_NAMESRVADDR</span> <span class="hljs-operator">=</span> <span class="hljs-string">&quot;127.0.0.1:9876&quot;</span>;<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-type">String</span> <span class="hljs-variable">TOPIC</span> <span class="hljs-operator">=</span> <span class="hljs-string">&quot;TopicTest&quot;</span>;<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-type">String</span> <span class="hljs-variable">TAG</span> <span class="hljs-operator">=</span> <span class="hljs-string">&quot;TagA&quot;</span>;<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">main</span><span class="hljs-params">(String[] args)</span> <span class="hljs-keyword">throws</span> MQClientException, InterruptedException &#123;<br>        <span class="hljs-comment">// 1. 实例化</span><br>        <span class="hljs-type">DefaultMQProducer</span> <span class="hljs-variable">producer</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">DefaultMQProducer</span>(PRODUCER_GROUP);<br>        producer.setNamesrvAddr(DEFAULT_NAMESRVADDR);<br><br>        <span class="hljs-comment">// 2. 初始化</span><br>        producer.start();<br>        <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> <span class="hljs-variable">i</span> <span class="hljs-operator">=</span> <span class="hljs-number">0</span>; i &lt; <span class="hljs-number">128</span>; i++) &#123;<br>            <span class="hljs-keyword">try</span> &#123;<br>                <span class="hljs-type">Message</span> <span class="hljs-variable">msg</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Message</span>(TOPIC, TAG, <span class="hljs-string">&quot;OrderID188&quot;</span>, <span class="hljs-string">&quot;Hello world&quot;</span>.getBytes(StandardCharsets.UTF_8));<br><br>                <span class="hljs-comment">// 3. 发送消息</span><br>                <span class="hljs-type">SendResult</span> <span class="hljs-variable">sendResult</span> <span class="hljs-operator">=</span> producer.send(msg);<br>                System.out.printf(<span class="hljs-string">&quot;%s%n&quot;</span>, sendResult);<br>            &#125; <span class="hljs-keyword">catch</span> (Exception e) &#123;<br>                e.printStackTrace();<br>            &#125;<br>        &#125;<br>        producer.shutdown();<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>
<p>从上面代码可以看到使用 生产者的使用主要如下三个步骤</p>
<ol>
<li>实例化 <code>Producer</code></li>
<li>初始化 <code>Producer</code></li>
<li>发送消息</li>
</ol>
<h3 id="实例化"><a href="#实例化" class="headerlink" title="实例化"></a>实例化</h3><p>实例化 <code>Producer</code>, 这里使用的是 <code>DefaultMQProducer</code>, 但是真正执行消息发送的还是 <code>DefaultMQProducerImpl</code></p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-title function_">DefaultMQProducer</span><span class="hljs-params">(<span class="hljs-keyword">final</span> String namespace, <span class="hljs-keyword">final</span> String producerGroup, RPCHook rpcHook)</span> &#123;<br>    <span class="hljs-built_in">this</span>.namespace = namespace;<br>    <span class="hljs-built_in">this</span>.producerGroup = producerGroup;<br>    defaultMQProducerImpl = <span class="hljs-keyword">new</span> <span class="hljs-title class_">DefaultMQProducerImpl</span>(<span class="hljs-built_in">this</span>, rpcHook);<br>    <span class="hljs-comment">// produceAccumulator 和批量消息有关</span><br>    produceAccumulator = MQClientManager.getInstance().getOrCreateProduceAccumulator(<span class="hljs-built_in">this</span>);<br>&#125;<br></code></pre></td></tr></table></figure>

<h3 id="初始化"><a href="#初始化" class="headerlink" title="初始化"></a>初始化</h3><p>在讲解初始化之前需要可以先了解下相关的类</p>
<ul>
<li><code>MQClientInstance</code>： 代表的是一个客户端实例（ <code>Producer</code> 和 <code>Consumer</code> 都是客户端实例），里面会保存有客户端相关的配置，比如 <code>namesrvAddr</code>， <code>clientIP</code>， 发送消息相关的 <code>Topic</code> 等信息</li>
<li><code>MQClientManager</code>： 代表的是客户端管理器，同一个进程可以启动多个 <code>Producer</code> 和 <code>Consumer</code> 实例，即可以认为有多个 <code>MQClientInstance</code>， <code>MQClientManager</code> 就是管理多个 <code>MQClientInstance</code></li>
</ul>
<p>初始化 <code>Producer</code>, 主要是调用 <code>DefaultMQProducer#start()</code> 方法</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">start</span><span class="hljs-params">()</span> <span class="hljs-keyword">throws</span> MQClientException &#123;<br>    <span class="hljs-built_in">this</span>.setProducerGroup(withNamespace(<span class="hljs-built_in">this</span>.producerGroup));<br>    <span class="hljs-comment">// 因为实际发送消息的是 DefaultMQProducerImpl， 所以重点看这个</span><br>    <span class="hljs-built_in">this</span>.defaultMQProducerImpl.start();<br>    <span class="hljs-keyword">if</span> (<span class="hljs-built_in">this</span>.produceAccumulator != <span class="hljs-literal">null</span>) &#123;<br>        <span class="hljs-built_in">this</span>.produceAccumulator.start();<br>    &#125;<br>    <span class="hljs-keyword">if</span> (<span class="hljs-literal">null</span> != traceDispatcher) &#123;<br>        <span class="hljs-keyword">try</span> &#123;<br>            traceDispatcher.start(<span class="hljs-built_in">this</span>.getNamesrvAddr(), <span class="hljs-built_in">this</span>.getAccessChannel());<br>        &#125; <span class="hljs-keyword">catch</span> (MQClientException e) &#123;<br>            logger.warn(<span class="hljs-string">&quot;trace dispatcher start failed &quot;</span>, e);<br>        &#125;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>
<p>接下来看 <code>this.defaultMQProducerImpl.start();</code>， 也就是 <code>DefaultMQProducerImpl#start()</code> 方法</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">start</span><span class="hljs-params">(<span class="hljs-keyword">final</span> <span class="hljs-type">boolean</span> startFactory)</span> <span class="hljs-keyword">throws</span> MQClientException &#123;<br>    <span class="hljs-keyword">switch</span> (<span class="hljs-built_in">this</span>.serviceState) &#123;<br>        <span class="hljs-keyword">case</span> CREATE_JUST:<br>            <span class="hljs-comment">// 设置默认状态时启动失败，如果启动成功再修改状态</span><br>            <span class="hljs-built_in">this</span>.serviceState = ServiceState.START_FAILED;<br>            <span class="hljs-comment">// 检查生产者组的配置</span><br>            <span class="hljs-built_in">this</span>.checkConfig();<br>            <span class="hljs-comment">// 修改 instanceName, 如果没有自己设置则将其修改为 “this.instanceName = UtilAll.getPid() + &quot;#&quot; + System.nanoTime()”</span><br>            <span class="hljs-keyword">if</span> (!<span class="hljs-built_in">this</span>.defaultMQProducer.getProducerGroup().equals(MixAll.CLIENT_INNER_PRODUCER_GROUP)) &#123;<br>                <span class="hljs-built_in">this</span>.defaultMQProducer.changeInstanceNameToPID();<br>            &#125;<br>            <span class="hljs-comment">// 这里就是创建 MQClientInstance，并且将其存入 MQClientManager</span><br>            <span class="hljs-built_in">this</span>.mQClientFactory = MQClientManager.getInstance().getOrCreateMQClientInstance(<span class="hljs-built_in">this</span>.defaultMQProducer, rpcHook);<br>            <span class="hljs-comment">// 这里的注册实际是将 Producer 存入 MQClientInstance 的 ConcurrentMap&lt;String, MQProducerInner&gt; producerTable 属性中</span><br>            <span class="hljs-type">boolean</span> <span class="hljs-variable">registerOK</span> <span class="hljs-operator">=</span> mQClientFactory.registerProducer(<span class="hljs-built_in">this</span>.defaultMQProducer.getProducerGroup(), <span class="hljs-built_in">this</span>);<br>            <span class="hljs-keyword">if</span> (!registerOK) &#123;<br>                <span class="hljs-built_in">this</span>.serviceState = ServiceState.CREATE_JUST;<br>                <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">MQClientException</span>(<span class="hljs-string">&quot;The producer group[&quot;</span> + <span class="hljs-built_in">this</span>.defaultMQProducer.getProducerGroup()<br>                    + <span class="hljs-string">&quot;] has been created before, specify another name please.&quot;</span> + FAQUrl.suggestTodo(FAQUrl.GROUP_NAME_DUPLICATE_URL),<br>                    <span class="hljs-literal">null</span>);<br>            &#125;<br><br>            <span class="hljs-keyword">if</span> (startFactory) &#123;<br>                <span class="hljs-comment">/**</span><br><span class="hljs-comment">                 *  【代码1】</span><br><span class="hljs-comment">                 * </span><br><span class="hljs-comment">                 *  这里的启动也是核心，主要包含</span><br><span class="hljs-comment">                 * 1. 启动底层 Netty 服务进行通信</span><br><span class="hljs-comment">                 * 2. 启动定时任务不断从 Nameserve 获取最新的数据</span><br><span class="hljs-comment">                 * 3. 启动拉取消息的定时任务服务</span><br><span class="hljs-comment">                 * 4. 启动 RebalanceService 服务</span><br><span class="hljs-comment">                 * 5. 启动清除下线的 broker 定时任务线程</span><br><span class="hljs-comment">                 * 6. 启动定时任务发送心跳到 broker 中</span><br><span class="hljs-comment">                 * 7. 如果是 consumer， 还会有定时持久化的操作</span><br><span class="hljs-comment">                 */</span><br>                mQClientFactory.start();<br>            &#125;<br><br>            <span class="hljs-built_in">this</span>.mqFaultStrategy.startDetector();<br><br>            log.info(<span class="hljs-string">&quot;the producer [&#123;&#125;] start OK. sendMessageWithVIPChannel=&#123;&#125;&quot;</span>, <span class="hljs-built_in">this</span>.defaultMQProducer.getProducerGroup(),<br>                <span class="hljs-built_in">this</span>.defaultMQProducer.isSendMessageWithVIPChannel());<br>            <span class="hljs-built_in">this</span>.serviceState = ServiceState.RUNNING;<br>            <span class="hljs-keyword">break</span>;<br>        <span class="hljs-keyword">case</span> RUNNING:<br>        <span class="hljs-keyword">case</span> START_FAILED:<br>        <span class="hljs-keyword">case</span> SHUTDOWN_ALREADY:<br>            <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">MQClientException</span>(<span class="hljs-string">&quot;The producer service state not OK, maybe started once, &quot;</span><br>                + <span class="hljs-built_in">this</span>.serviceState<br>                + FAQUrl.suggestTodo(FAQUrl.CLIENT_SERVICE_NOT_OK),<br>                <span class="hljs-literal">null</span>);<br>        <span class="hljs-keyword">default</span>:<br>            <span class="hljs-keyword">break</span>;<br>    &#125;<br>    <span class="hljs-comment">// 发送所有心跳到 Broker 中</span><br>    <span class="hljs-built_in">this</span>.mQClientFactory.sendHeartbeatToAllBrokerWithLock();<br><br>    RequestFutureHolder.getInstance().startScheduledTask(<span class="hljs-built_in">this</span>);<br><br>&#125;<br></code></pre></td></tr></table></figure>


<h2 id="生产者发送消息的流程"><a href="#生产者发送消息的流程" class="headerlink" title="生产者发送消息的流程"></a>生产者发送消息的流程</h2><ol><li><a href="/2023/22987494ce74/index.html" title="RocketMQ中同步和异步消息发送流程">RocketMQ中同步和异步消息发送流程</a></li></ol>

<h2 id="生产者的高可用"><a href="#生产者的高可用" class="headerlink" title="生产者的高可用"></a>生产者的高可用</h2><h3 id="客户端保证"><a href="#客户端保证" class="headerlink" title="客户端保证"></a>客户端保证</h3><ul>
<li>重试机制， 通过下面参数配置</li>
</ul>
<figure class="highlight properties"><table><tr><td class="code"><pre><code class="hljs properties"><span class="hljs-attr">rocketmq.producer.retry-times-when-send-failed</span>=<span class="hljs-string">2</span><br><span class="hljs-attr">rocketmq.producer.retry-times-when-send-async-failed</span>=<span class="hljs-string">2</span><br></code></pre></td></tr></table></figure>
<p><strong>同步发送重试</strong>的核心代码是在 <code>org.apache.rocketmq.client.impl.producer.DefaultMQProducerImpl#sendDefaultImpl()</code> 方法， 下面只展示该方法与重试相关的代码</p>
<p><code>DefaultMQProducerImpl</code> 类</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">private</span> SendResult <span class="hljs-title function_">sendDefaultImpl</span><span class="hljs-params">(</span><br><span class="hljs-params">        Message msg,</span><br><span class="hljs-params">        <span class="hljs-keyword">final</span> CommunicationMode communicationMode,</span><br><span class="hljs-params">        <span class="hljs-keyword">final</span> SendCallback sendCallback,</span><br><span class="hljs-params">        <span class="hljs-keyword">final</span> <span class="hljs-type">long</span> timeout</span><br><span class="hljs-params">    )</span> <span class="hljs-keyword">throws</span> MQClientException, RemotingException, MQBrokerException, InterruptedException &#123;<br>    <span class="hljs-keyword">if</span> (topicPublishInfo != <span class="hljs-literal">null</span> &amp;&amp; topicPublishInfo.ok()) &#123;<br>        <span class="hljs-type">boolean</span> <span class="hljs-variable">callTimeout</span> <span class="hljs-operator">=</span> <span class="hljs-literal">false</span>;<br>        <span class="hljs-type">MessageQueue</span> <span class="hljs-variable">mq</span> <span class="hljs-operator">=</span> <span class="hljs-literal">null</span>;<br>        <span class="hljs-type">Exception</span> <span class="hljs-variable">exception</span> <span class="hljs-operator">=</span> <span class="hljs-literal">null</span>;<br>        <span class="hljs-type">SendResult</span> <span class="hljs-variable">sendResult</span> <span class="hljs-operator">=</span> <span class="hljs-literal">null</span>;<br>        <span class="hljs-comment">// 获取调用的总次数</span><br>        <span class="hljs-type">int</span> <span class="hljs-variable">timesTotal</span> <span class="hljs-operator">=</span> communicationMode == CommunicationMode.SYNC ? <span class="hljs-number">1</span> + <span class="hljs-built_in">this</span>.defaultMQProducer.getRetryTimesWhenSendFailed() : <span class="hljs-number">1</span>;<br>        <span class="hljs-type">int</span> <span class="hljs-variable">times</span> <span class="hljs-operator">=</span> <span class="hljs-number">0</span>;<br>        String[] brokersSent = <span class="hljs-keyword">new</span> <span class="hljs-title class_">String</span>[timesTotal];<br>        <span class="hljs-keyword">for</span> (; times &lt; timesTotal; times++) &#123;<br>            <span class="hljs-type">String</span> <span class="hljs-variable">lastBrokerName</span> <span class="hljs-operator">=</span> <span class="hljs-literal">null</span> == mq ? <span class="hljs-literal">null</span> : mq.getBrokerName();<br>            <span class="hljs-type">MessageQueue</span> <span class="hljs-variable">mqSelected</span> <span class="hljs-operator">=</span> <span class="hljs-built_in">this</span>.selectOneMessageQueue(topicPublishInfo, lastBrokerName);<br>            <span class="hljs-keyword">if</span> (mqSelected != <span class="hljs-literal">null</span>) &#123;<br>                mq = mqSelected;<br>                brokersSent[times] = mq.getBrokerName();<br>                <span class="hljs-keyword">try</span> &#123;<br>                    <span class="hljs-comment">// 发送消息并获取返回结果</span><br>                    sendResult = <span class="hljs-built_in">this</span>.sendKernelImpl(msg, mq, communicationMode, sendCallback, topicPublishInfo, timeout - costTime);<br>                    endTimestamp = System.currentTimeMillis();<br>                    <span class="hljs-built_in">this</span>.updateFaultItem(mq.getBrokerName(), endTimestamp - beginTimestampPrev, <span class="hljs-literal">false</span>);<br>                    <span class="hljs-keyword">switch</span> (communicationMode) &#123;<br>                        <span class="hljs-comment">// 异步发送的结果不是在这里获取,所以直接返回 null</span><br>                        <span class="hljs-keyword">case</span> ASYNC:<br>                            <span class="hljs-keyword">return</span> <span class="hljs-literal">null</span>;<br>                        <span class="hljs-comment">// 只发送一次，所以也不需要重试</span><br>                        <span class="hljs-keyword">case</span> ONEWAY:<br>                            <span class="hljs-keyword">return</span> <span class="hljs-literal">null</span>;<br>                        <span class="hljs-comment">// 同步发送，结果在这里获取</span><br>                        <span class="hljs-keyword">case</span> SYNC:<br>                            <span class="hljs-keyword">if</span> (sendResult.getSendStatus() != SendStatus.SEND_OK) &#123;<br>                                <span class="hljs-comment">// 发送不成功，并且开启了重试，则进行重试，也就是进入 for 循环的下一次循环</span><br>                                <span class="hljs-keyword">if</span> (<span class="hljs-built_in">this</span>.defaultMQProducer.isRetryAnotherBrokerWhenNotStoreOK()) &#123;<br>                                    <span class="hljs-keyword">continue</span>;<br>                                &#125;<br>                            &#125;<br><br>                            <span class="hljs-keyword">return</span> sendResult;<br>                        <span class="hljs-keyword">default</span>:<br>                            <span class="hljs-keyword">break</span>;<br>                    &#125;<br>                &#125;<br>            &#125; <span class="hljs-keyword">else</span> &#123;<br>                <span class="hljs-keyword">break</span>;<br>            &#125;<br>        &#125;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<p><strong>异步发送重试</strong>的核心代码是在 <code>org.apache.rocketmq.client.impl.MQClientAPIImpl#sendMessageAsync()</code> 方法中<br><code>MQClientAPIImpl</code> 类</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">private</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">sendMessageAsync</span><span class="hljs-params">(</span><br><span class="hljs-params">        <span class="hljs-keyword">final</span> String addr,</span><br><span class="hljs-params">        <span class="hljs-keyword">final</span> String brokerName,</span><br><span class="hljs-params">        <span class="hljs-keyword">final</span> Message msg,</span><br><span class="hljs-params">        <span class="hljs-keyword">final</span> <span class="hljs-type">long</span> timeoutMillis,</span><br><span class="hljs-params">        <span class="hljs-keyword">final</span> RemotingCommand request,</span><br><span class="hljs-params">        <span class="hljs-keyword">final</span> SendCallback sendCallback,</span><br><span class="hljs-params">        <span class="hljs-keyword">final</span> TopicPublishInfo topicPublishInfo,</span><br><span class="hljs-params">        <span class="hljs-keyword">final</span> MQClientInstance instance,</span><br><span class="hljs-params">        <span class="hljs-keyword">final</span> <span class="hljs-type">int</span> retryTimesWhenSendFailed,</span><br><span class="hljs-params">        <span class="hljs-keyword">final</span> AtomicInteger times,</span><br><span class="hljs-params">        <span class="hljs-keyword">final</span> SendMessageContext context,</span><br><span class="hljs-params">        <span class="hljs-keyword">final</span> DefaultMQProducerImpl producer</span><br><span class="hljs-params">    )</span> &#123;<br>    <span class="hljs-keyword">final</span> <span class="hljs-type">long</span> <span class="hljs-variable">beginStartTime</span> <span class="hljs-operator">=</span> System.currentTimeMillis();<br>    <span class="hljs-keyword">try</span> &#123;<br>        <span class="hljs-comment">// new InvokeCallback() 就是执行发送消息的回调</span><br>        <span class="hljs-built_in">this</span>.remotingClient.invokeAsync(addr, request, timeoutMillis, <span class="hljs-keyword">new</span> <span class="hljs-title class_">InvokeCallback</span>() &#123;<br>            <span class="hljs-meta">@Override</span><br>            <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">operationComplete</span><span class="hljs-params">(ResponseFuture responseFuture)</span> &#123;<br>                <span class="hljs-type">long</span> <span class="hljs-variable">cost</span> <span class="hljs-operator">=</span> System.currentTimeMillis() - beginStartTime;<br>                <span class="hljs-type">RemotingCommand</span> <span class="hljs-variable">response</span> <span class="hljs-operator">=</span> responseFuture.getResponseCommand();<br>                <span class="hljs-comment">// response != null 代表消息发送成功</span><br>                <span class="hljs-keyword">if</span> (response != <span class="hljs-literal">null</span>) &#123;<br>                &#125; <span class="hljs-keyword">else</span> &#123;<br>                    <span class="hljs-comment">// response == null 代表消息发送失败, 在 onExceptionImpl 里面就会进行发送方法的重试</span><br>                    producer.updateFaultItem(brokerName, System.currentTimeMillis() - responseFuture.getBeginTimestamp(), <span class="hljs-literal">true</span>);<br>                    <span class="hljs-keyword">if</span> (!responseFuture.isSendRequestOK()) &#123;<br>                        <span class="hljs-type">MQClientException</span> <span class="hljs-variable">ex</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">MQClientException</span>(<span class="hljs-string">&quot;send request failed&quot;</span>, responseFuture.getCause());<br>                        onExceptionImpl(brokerName, msg, timeoutMillis - cost, request, sendCallback, topicPublishInfo, instance,<br>                            retryTimesWhenSendFailed, times, ex, context, <span class="hljs-literal">true</span>, producer);<br>                    &#125; <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (responseFuture.isTimeout()) &#123;<br>                        <span class="hljs-type">MQClientException</span> <span class="hljs-variable">ex</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">MQClientException</span>(<span class="hljs-string">&quot;wait response timeout &quot;</span> + responseFuture.getTimeoutMillis() + <span class="hljs-string">&quot;ms&quot;</span>,<br>                            responseFuture.getCause());<br>                        onExceptionImpl(brokerName, msg, timeoutMillis - cost, request, sendCallback, topicPublishInfo, instance,<br>                            retryTimesWhenSendFailed, times, ex, context, <span class="hljs-literal">true</span>, producer);<br>                    &#125; <span class="hljs-keyword">else</span> &#123;<br>                        <span class="hljs-type">MQClientException</span> <span class="hljs-variable">ex</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">MQClientException</span>(<span class="hljs-string">&quot;unknow reseaon&quot;</span>, responseFuture.getCause());<br>                        onExceptionImpl(brokerName, msg, timeoutMillis - cost, request, sendCallback, topicPublishInfo, instance,<br>                            retryTimesWhenSendFailed, times, ex, context, <span class="hljs-literal">true</span>, producer);<br>                    &#125;<br>                &#125;<br>            &#125;<br>        &#125;);<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<h3 id="客户端容错"><a href="#客户端容错" class="headerlink" title="客户端容错"></a>客户端容错</h3><p><code>RocketMQ Client</code> 会维护一个 “Broker-发送延迟” 关系，根据这个关系选择一个发送延迟级别较低的 <code>Broker</code> 来发送消息，这样能最大限度地利用 <code>Broker</code> 的能力，剔除已经宕机、不可用或者发送延迟级别较高的 <code>Broker</code>，尽量保证消息的正常发送</p>
<p>选择 <code>Broker</code> 其实就是选择 <code>MessageQueue</code> 发送消息, 具体实现在 <code>org.apache.rocketmq.client.latency.MQFaultStrategy#selectOneMessageQueue()</code> 方法中，前面讲到同步发送消息的方法默认是 <code>org.apache.rocketmq.client.impl.producer.DefaultMQProducerImpl#sendDefaultImpl()</code>， 而 <code>selectOneMessageQueue</code> 方法就是在 <code>sendDefaultImpl</code> 方法中进行调用的</p>
<p><code>selectOneMessageQueue</code> 类</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> MessageQueue <span class="hljs-title function_">selectOneMessageQueue</span><span class="hljs-params">(<span class="hljs-keyword">final</span> TopicPublishInfo tpInfo, <span class="hljs-keyword">final</span> String lastBrokerName)</span> &#123;<br>    <span class="hljs-comment">// 1. 如果开启了发送延迟容错，才会调用容错相关的逻辑</span><br>    <span class="hljs-keyword">if</span> (<span class="hljs-built_in">this</span>.sendLatencyFaultEnable) &#123;<br>        <span class="hljs-keyword">try</span> &#123;<br>            <span class="hljs-type">int</span> <span class="hljs-variable">index</span> <span class="hljs-operator">=</span> tpInfo.getSendWhichQueue().incrementAndGet();<br>            <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> <span class="hljs-variable">i</span> <span class="hljs-operator">=</span> <span class="hljs-number">0</span>; i &lt; tpInfo.getMessageQueueList().size(); i++) &#123;<br>                <span class="hljs-type">int</span> <span class="hljs-variable">pos</span> <span class="hljs-operator">=</span> Math.abs(index++) % tpInfo.getMessageQueueList().size();<br>                <span class="hljs-keyword">if</span> (pos &lt; <span class="hljs-number">0</span>)<br>                    pos = <span class="hljs-number">0</span>;<br>                <span class="hljs-type">MessageQueue</span> <span class="hljs-variable">mq</span> <span class="hljs-operator">=</span> tpInfo.getMessageQueueList().get(pos);<br>                <span class="hljs-keyword">if</span> (latencyFaultTolerance.isAvailable(mq.getBrokerName()))<br>                    <span class="hljs-keyword">return</span> mq;<br>            &#125;<br><br>            <span class="hljs-keyword">final</span> <span class="hljs-type">String</span> <span class="hljs-variable">notBestBroker</span> <span class="hljs-operator">=</span> latencyFaultTolerance.pickOneAtLeast();<br>            <span class="hljs-type">int</span> <span class="hljs-variable">writeQueueNums</span> <span class="hljs-operator">=</span> tpInfo.getQueueIdByBroker(notBestBroker);<br>            <span class="hljs-keyword">if</span> (writeQueueNums &gt; <span class="hljs-number">0</span>) &#123;<br>                <span class="hljs-keyword">final</span> <span class="hljs-type">MessageQueue</span> <span class="hljs-variable">mq</span> <span class="hljs-operator">=</span> tpInfo.selectOneMessageQueue();<br>                <span class="hljs-keyword">if</span> (notBestBroker != <span class="hljs-literal">null</span>) &#123;<br>                    mq.setBrokerName(notBestBroker);<br>                    mq.setQueueId(tpInfo.getSendWhichQueue().incrementAndGet() % writeQueueNums);<br>                &#125;<br>                <span class="hljs-keyword">return</span> mq;<br>            &#125; <span class="hljs-keyword">else</span> &#123;<br>                latencyFaultTolerance.remove(notBestBroker);<br>            &#125;<br>        &#125; <span class="hljs-keyword">catch</span> (Exception e) &#123;<br>            log.error(<span class="hljs-string">&quot;Error occurred when selecting message queue&quot;</span>, e);<br>        &#125;<br><br>        <span class="hljs-keyword">return</span> tpInfo.selectOneMessageQueue();<br>    &#125;<br><br>    <span class="hljs-keyword">return</span> tpInfo.selectOneMessageQueue(lastBrokerName);<br>&#125;<br></code></pre></td></tr></table></figure>
<ul>
<li>获取一个在延迟上可以接受，并且和上次发送相同的 <code>Broker</code>。首先获取一个自增序号<code>index</code>，通过取模获取<code>Queue</code>的位置下标 <code>Pos</code>。如果 <code>Pos</code>对应的 <code>Broker</code>的延迟时间是可以接受的，并且是第一次发送，或者和上次发送的<code>Broker</code>相同，则将<code>Queue</code>返回</li>
<li>如果第一步没有选中一个<code>Broker</code>，则选择一个延迟较低的<code>Broker</code></li>
<li>如果第一、二步都没有选中一个<code>Broker</code>，则随机选择一个<code>Broker</code></li>
</ul>
<h3 id="Broker-端保证"><a href="#Broker-端保证" class="headerlink" title="Broker 端保证"></a>Broker 端保证</h3><ul>
<li><code>Broker</code> 端包含 <code>Master</code> 和 <code>Slave</code></li>
<li><code>Master</code> 和 <code>Slave</code> 之间有<strong>同步复制</strong> 和 <strong>异步复制</strong>，如果要想做到高可用，可以使用 <strong>同步复制</strong></li>
</ul>
<h2 id="生产者-DefaultMQProducer-核心方法"><a href="#生产者-DefaultMQProducer-核心方法" class="headerlink" title="生产者 (DefaultMQProducer) 核心方法"></a>生产者 (DefaultMQProducer) 核心方法</h2><ul>
<li><code>long searchOffset（final MessageQueue mq, final long timestamp）</code> 根据时间戳从队列中查找其偏移量</li>
<li><code>List&lt;MessageQueue&gt; fetchPublishMessageQueues(String topic)</code> 查找该主题下所有的消息队列</li>
<li></li>
</ul>
]]></content>
      <categories>
        <category>中间件</category>
        <category>RocketMQ</category>
      </categories>
      <tags>
        <tag>RocketMQ</tag>
      </tags>
  </entry>
  <entry>
    <title>RocketMQ中同步和异步消息发送流程</title>
    <url>/2023/22987494ce74/index.html</url>
    <content><![CDATA[<span class="hide_first_mermaid">
<div class="mermaid-wrap"><pre class="mermaid-src" hidden>
  flowchart LR
    A
  </pre></div>
</span>

<h2 id="Producer-初始化流程"><a href="#Producer-初始化流程" class="headerlink" title="Producer 初始化流程"></a>Producer 初始化流程</h2><p>消息在发送之前需要先初始化 <code>Producer</code>, 具体可以参考</p>
<ol><li><a href="/2023/254af85a97af/index.html" title="RocketMQ中Producer和ProducerGroup">RocketMQ中Producer和ProducerGroup</a></li></ol>

<h2 id="消息类型"><a href="#消息类型" class="headerlink" title="消息类型"></a>消息类型</h2><pre class="mermaid">mindmap
Root("Producer(生产者)")
    A("普通消息")
        A1("同步发送")
        A2("异步发送")
        A3("单向发送")
    B("顺序消息")
    C("延迟消息")
    D("批量消息")
    E("事务消息")</pre>


<h2 id="普通-同步消息发送流程"><a href="#普通-同步消息发送流程" class="headerlink" title="普通-同步消息发送流程"></a>普通-同步消息发送流程</h2><p>普通消息发送代码示例：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">Producer</span> &#123;<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-type">String</span> <span class="hljs-variable">PRODUCER_GROUP</span> <span class="hljs-operator">=</span> <span class="hljs-string">&quot;ProducerGroupName&quot;</span>;<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-type">String</span> <span class="hljs-variable">DEFAULT_NAMESRVADDR</span> <span class="hljs-operator">=</span> <span class="hljs-string">&quot;192.168.208.130:9876&quot;</span>;<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-type">String</span> <span class="hljs-variable">TOPIC</span> <span class="hljs-operator">=</span> <span class="hljs-string">&quot;TopicTest&quot;</span>;<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-type">String</span> <span class="hljs-variable">TAG</span> <span class="hljs-operator">=</span> <span class="hljs-string">&quot;TagA&quot;</span>;<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">main</span><span class="hljs-params">(String[] args)</span> <span class="hljs-keyword">throws</span> MQClientException, InterruptedException &#123;<br>        <span class="hljs-comment">// 1. 实例化</span><br>        <span class="hljs-type">DefaultMQProducer</span> <span class="hljs-variable">producer</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">DefaultMQProducer</span>(PRODUCER_GROUP);<br>        producer.setNamesrvAddr(DEFAULT_NAMESRVADDR);<br><br>        <span class="hljs-comment">// 2. 初始化</span><br>        producer.start();<br>        <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> <span class="hljs-variable">i</span> <span class="hljs-operator">=</span> <span class="hljs-number">0</span>; i &lt; <span class="hljs-number">128</span>; i++) &#123;<br>            <span class="hljs-keyword">try</span> &#123;<br>                <span class="hljs-comment">// 3. 构造消息</span><br>                <span class="hljs-type">Message</span> <span class="hljs-variable">msg</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Message</span>(TOPIC, TAG, <span class="hljs-string">&quot;OrderID188&quot;</span>, <span class="hljs-string">&quot;Hello world&quot;</span>.getBytes(StandardCharsets.UTF_8));<br>                <span class="hljs-comment">// 4. 发送消息</span><br>                <span class="hljs-type">SendResult</span> <span class="hljs-variable">sendResult</span> <span class="hljs-operator">=</span> producer.send(msg);<br>                System.out.printf(<span class="hljs-string">&quot;%s%n&quot;</span>, sendResult);<br>            &#125; <span class="hljs-keyword">catch</span> (Exception e) &#123;<br>                e.printStackTrace();<br>            &#125;<br>        &#125;<br>        producer.shutdown();<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<p>发送消息的核心流程</p>
<figure class="highlight leaf"><table><tr><td class="code"><pre><code class="hljs leaf">DefaultMQProducer<span class="hljs-punctuation">#</span><span class="hljs-keyword">send</span><span class="hljs-params">(<span class="hljs-variable">Message</span>)</span><br>    DefaultMQProducer<span class="hljs-punctuation">#</span><span class="hljs-keyword">sendDirect</span><span class="hljs-params">()</span><br>        DefaultMQProducerImpl<span class="hljs-punctuation">#</span><span class="hljs-keyword">send</span><span class="hljs-params">(<span class="hljs-variable">org</span>.<span class="hljs-variable">apache</span>.<span class="hljs-variable">rocketmq</span>.<span class="hljs-variable">common</span>.<span class="hljs-variable">message</span>.<span class="hljs-variable">Message</span>, <span class="hljs-variable">long</span>)</span><br>            DefaultMQProducerImpl<span class="hljs-punctuation">#</span><span class="hljs-keyword">sendDefaultImpl</span><span class="hljs-params">()</span><br></code></pre></td></tr></table></figure>

<p>对于普通同步消息，最终的主流程代码是在 <code>DefaultMQProducerImpl#sendDefaultImpl()</code> 中，接下来就看下这个方法（只展示了主要相关代码）</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">private</span> SendResult <span class="hljs-title function_">sendDefaultImpl</span><span class="hljs-params">(</span><br><span class="hljs-params">        Message msg,</span><br><span class="hljs-params">        <span class="hljs-keyword">final</span> CommunicationMode communicationMode,</span><br><span class="hljs-params">        <span class="hljs-keyword">final</span> SendCallback sendCallback,</span><br><span class="hljs-params">        <span class="hljs-keyword">final</span> <span class="hljs-type">long</span> timeout</span><br><span class="hljs-params">    )</span> <span class="hljs-keyword">throws</span> MQClientException, RemotingException, MQBrokerException, InterruptedException &#123;<br>        <span class="hljs-built_in">this</span>.makeSureStateOK();<br>        Validators.checkMessage(msg, <span class="hljs-built_in">this</span>.defaultMQProducer);<br>        <span class="hljs-keyword">final</span> <span class="hljs-type">long</span> <span class="hljs-variable">invokeID</span> <span class="hljs-operator">=</span> random.nextLong();<br>        <span class="hljs-type">long</span> <span class="hljs-variable">beginTimestampFirst</span> <span class="hljs-operator">=</span> System.currentTimeMillis();<br>        <span class="hljs-type">long</span> <span class="hljs-variable">beginTimestampPrev</span> <span class="hljs-operator">=</span> beginTimestampFirst;<br>        <span class="hljs-type">long</span> <span class="hljs-variable">endTimestamp</span> <span class="hljs-operator">=</span> beginTimestampFirst;<br>        <span class="hljs-comment">// 核心步骤1：构造 TopicPublishInfo</span><br>        <span class="hljs-type">TopicPublishInfo</span> <span class="hljs-variable">topicPublishInfo</span> <span class="hljs-operator">=</span> <span class="hljs-built_in">this</span>.tryToFindTopicPublishInfo(msg.getTopic());<br>        <span class="hljs-keyword">if</span> (topicPublishInfo != <span class="hljs-literal">null</span> &amp;&amp; topicPublishInfo.ok()) &#123;<br>            <span class="hljs-type">boolean</span> <span class="hljs-variable">callTimeout</span> <span class="hljs-operator">=</span> <span class="hljs-literal">false</span>;<br>            <span class="hljs-type">MessageQueue</span> <span class="hljs-variable">mq</span> <span class="hljs-operator">=</span> <span class="hljs-literal">null</span>;<br>            <span class="hljs-type">SendResult</span> <span class="hljs-variable">sendResult</span> <span class="hljs-operator">=</span> <span class="hljs-literal">null</span>;<br>            <span class="hljs-comment">// 获取重试此时，总次数 = 1+重试次数（同步的时候才会重试）</span><br>            <span class="hljs-type">int</span> <span class="hljs-variable">timesTotal</span> <span class="hljs-operator">=</span> communicationMode == CommunicationMode.SYNC ? <span class="hljs-number">1</span> + <span class="hljs-built_in">this</span>.defaultMQProducer.getRetryTimesWhenSendFailed() : <span class="hljs-number">1</span>;<br>            <span class="hljs-type">int</span> <span class="hljs-variable">times</span> <span class="hljs-operator">=</span> <span class="hljs-number">0</span>;<br>            String[] brokersSent = <span class="hljs-keyword">new</span> <span class="hljs-title class_">String</span>[timesTotal];<br>            <span class="hljs-type">boolean</span> <span class="hljs-variable">resetIndex</span> <span class="hljs-operator">=</span> <span class="hljs-literal">false</span>;<br>            <span class="hljs-keyword">for</span> (; times &lt; timesTotal; times++) &#123;<br>                <span class="hljs-type">String</span> <span class="hljs-variable">lastBrokerName</span> <span class="hljs-operator">=</span> <span class="hljs-literal">null</span> == mq ? <span class="hljs-literal">null</span> : mq.getBrokerName();<br>                <span class="hljs-keyword">if</span> (times &gt; <span class="hljs-number">0</span>) &#123;<br>                    resetIndex = <span class="hljs-literal">true</span>;<br>                &#125;<br>                <span class="hljs-comment">// 核心步骤2：选择一个 MessageQueue</span><br>                <span class="hljs-type">MessageQueue</span> <span class="hljs-variable">mqSelected</span> <span class="hljs-operator">=</span> <span class="hljs-built_in">this</span>.selectOneMessageQueue(topicPublishInfo, lastBrokerName, resetIndex);<br>                <span class="hljs-keyword">if</span> (mqSelected != <span class="hljs-literal">null</span>) &#123;<br>                    mq = mqSelected;<br>                    brokersSent[times] = mq.getBrokerName();<br>                    <span class="hljs-keyword">try</span> &#123;<br>                        <span class="hljs-comment">// 核心步骤3： 执行消息的发送逻辑</span><br>                        sendResult = <span class="hljs-built_in">this</span>.sendKernelImpl(msg, mq, communicationMode, sendCallback, topicPublishInfo, timeout - costTime);<br>                        endTimestamp = System.currentTimeMillis();<br>                        <span class="hljs-built_in">this</span>.updateFaultItem(mq.getBrokerName(), endTimestamp - beginTimestampPrev, <span class="hljs-literal">false</span>, <span class="hljs-literal">true</span>);<br>                        <span class="hljs-keyword">switch</span> (communicationMode) &#123;<br>                            <span class="hljs-keyword">case</span> ASYNC:<br>                                <span class="hljs-keyword">return</span> <span class="hljs-literal">null</span>;<br>                            <span class="hljs-keyword">case</span> ONEWAY:<br>                                <span class="hljs-keyword">return</span> <span class="hljs-literal">null</span>;<br>                            <span class="hljs-keyword">case</span> SYNC:<br>                                <span class="hljs-comment">// 如果发送不成功，使用continue就会进入下一次循环，也就是重试</span><br>                                <span class="hljs-keyword">if</span> (sendResult.getSendStatus() != SendStatus.SEND_OK) &#123;<br>                                    <span class="hljs-keyword">if</span> (<span class="hljs-built_in">this</span>.defaultMQProducer.isRetryAnotherBrokerWhenNotStoreOK()) &#123;<br>                                        <span class="hljs-keyword">continue</span>;<br>                                    &#125;<br>                                &#125;<br>                                <span class="hljs-comment">// 如果发送成功，就直接返回结果</span><br>                                <span class="hljs-keyword">return</span> sendResult;<br>                            <span class="hljs-keyword">default</span>:<br>                                <span class="hljs-keyword">break</span>;<br>                        &#125;<br>                    &#125; <br>                &#125; <span class="hljs-keyword">else</span> &#123;<br>                    <span class="hljs-keyword">break</span>;<br>                &#125;<br>            &#125;<br>            <span class="hljs-keyword">if</span> (sendResult != <span class="hljs-literal">null</span>) &#123;<br>                <span class="hljs-keyword">return</span> sendResult;<br>            &#125;<br>        &#125;<br>    &#125;<br></code></pre></td></tr></table></figure>
<p>核心流程的逻辑如下</p>
<ol>
<li>构造 <code>TopicPublishInfo</code>, 该结构包含队列(<code>Queue</code>, <code>Broker</code>)</li>
<li>选择一个队列（<code>MessageQueue</code>）进行发送，因为消息实际是发往队列的，<code>Topic</code> 是一个逻辑上的概念</li>
<li>根据 <code>MessageQueue</code> 获取对应的 <code>BrokerName</code> 和 <code>BrokerAddr</code></li>
<li>判断消息是否需要压缩（消息大于 4K 则压缩）</li>
<li>执行消息发送，若发送失败，则进行重试</li>
</ol>
<p>下面看一下这几个核心步骤</p>
<p><strong>构造 TopicPublishInfo</strong><br>先看一下 <code>TopicPublishInfo</code> 结构， <code>TopicPublishInfo</code> 包含了 <code>Lis&lt;MessageQueue&gt;</code> 和 <code>TopicRouteData</code><br><code>TopicPublishInfo</code> 类</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">TopicPublishInfo</span> &#123;<br>    <span class="hljs-keyword">private</span> <span class="hljs-type">boolean</span> <span class="hljs-variable">orderTopic</span> <span class="hljs-operator">=</span> <span class="hljs-literal">false</span>;<br>    <span class="hljs-keyword">private</span> <span class="hljs-type">boolean</span> <span class="hljs-variable">haveTopicRouterInfo</span> <span class="hljs-operator">=</span> <span class="hljs-literal">false</span>;<br>    <span class="hljs-keyword">private</span> List&lt;MessageQueue&gt; messageQueueList = <span class="hljs-keyword">new</span> <span class="hljs-title class_">ArrayList</span>&lt;&gt;();<br>    <span class="hljs-keyword">private</span> TopicRouteData topicRouteData;<br>&#125;<br></code></pre></td></tr></table></figure>

<p><code>TopicRouteData</code> 类</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">TopicRouteData</span> <span class="hljs-keyword">extends</span> <span class="hljs-title class_">RemotingSerializable</span> &#123;<br>    <span class="hljs-keyword">private</span> List&lt;QueueData&gt; queueDatas;<br>    <span class="hljs-keyword">private</span> List&lt;BrokerData&gt; brokerDatas;<br>    <span class="hljs-keyword">private</span> Map&lt;String<span class="hljs-comment">/*brokerName*/</span>, TopicQueueMappingInfo&gt; topicQueueMappingByBroker;<br>&#125;<br></code></pre></td></tr></table></figure>

<p><code>MessageQueue</code> 类</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">MessageQueue</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">Comparable</span>&lt;MessageQueue&gt;, Serializable &#123;<br>    <span class="hljs-keyword">private</span> String topic;<br>    <span class="hljs-keyword">private</span> String brokerName;<br>    <span class="hljs-keyword">private</span> <span class="hljs-type">int</span> queueId;<br>&#125;<br></code></pre></td></tr></table></figure>
<p><strong>核心步骤1</strong> 调用的代码是 <code>TopicPublishInfo topicPublishInfo = this.tryToFindTopicPublishInfo(msg.getTopic());</code> 所以需要看 <code>tryToFindTopicPublishInfo</code> 方法</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">private</span> TopicPublishInfo <span class="hljs-title function_">tryToFindTopicPublishInfo</span><span class="hljs-params">(<span class="hljs-keyword">final</span> String topic)</span> &#123;<br>    <span class="hljs-comment">// 第一次发送消息肯定没有值，所以是走下面的流程</span><br>    <span class="hljs-type">TopicPublishInfo</span> <span class="hljs-variable">topicPublishInfo</span> <span class="hljs-operator">=</span> <span class="hljs-built_in">this</span>.topicPublishInfoTable.get(topic);<br>    <span class="hljs-keyword">if</span> (<span class="hljs-literal">null</span> == topicPublishInfo || !topicPublishInfo.ok()) &#123;<br>        <span class="hljs-built_in">this</span>.topicPublishInfoTable.putIfAbsent(topic, <span class="hljs-keyword">new</span> <span class="hljs-title class_">TopicPublishInfo</span>());<br>        <span class="hljs-built_in">this</span>.mQClientFactory.updateTopicRouteInfoFromNameServer(topic);<br>        topicPublishInfo = <span class="hljs-built_in">this</span>.topicPublishInfoTable.get(topic);<br>    &#125;<br><br>    <span class="hljs-keyword">if</span> (topicPublishInfo.isHaveTopicRouterInfo() || topicPublishInfo.ok()) &#123;<br>        <span class="hljs-keyword">return</span> topicPublishInfo;<br>    &#125; <span class="hljs-keyword">else</span> &#123;<br>        <span class="hljs-comment">// 第一次往某个 topic 中发送数据时走这个逻辑，注意第二个参数传递的是 true</span><br>        <span class="hljs-comment">// 这个参数代表的是否是默认的 topic</span><br>        <span class="hljs-built_in">this</span>.mQClientFactory.updateTopicRouteInfoFromNameServer(topic, <span class="hljs-literal">true</span>, <span class="hljs-built_in">this</span>.defaultMQProducer);<br>        topicPublishInfo = <span class="hljs-built_in">this</span>.topicPublishInfoTable.get(topic);<br>        <span class="hljs-keyword">return</span> topicPublishInfo;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<p>构建 <code>TopicPublishInfo</code> 类的过程实际是先从 <code>Nameserver</code> 获取数据，<code>updateTopicRouteInfoFromNameServer()</code> 方法的第二个参数是 <code>true</code>, 所以最终是调用到下面方法</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-type">String</span> <span class="hljs-variable">AUTO_CREATE_TOPIC_KEY_TOPIC</span> <span class="hljs-operator">=</span> <span class="hljs-string">&quot;TBW102&quot;</span>; <br><span class="hljs-keyword">public</span> TopicRouteData <span class="hljs-title function_">getDefaultTopicRouteInfoFromNameServer</span><span class="hljs-params">(<span class="hljs-keyword">final</span> <span class="hljs-type">long</span> timeoutMillis)</span><br>        <span class="hljs-keyword">throws</span> RemotingException, MQClientException, InterruptedException &#123;<br>    <span class="hljs-keyword">return</span> getTopicRouteInfoFromNameServer(TopicValidator.AUTO_CREATE_TOPIC_KEY_TOPIC, timeoutMillis, <span class="hljs-literal">false</span>);<br>&#125;<br></code></pre></td></tr></table></figure>
<p>因此实际获取的是 <code>TBW102</code> 这个内部 <code>Topic</code> 的相关数据，所以我们自定义的 <code>Topic</code> 的相关数据实际是从 <code>TBW102</code> 中拷贝过来的， <code>TBW102</code> 的 <code>ReadQueue</code> 和 <code>WriteQueue</code> 都是 8，那么我们自定义的 <code>Topic</code> 读写队列也是 8 么？ 实际不是的，处理读写队列的代码如下</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-comment">// MQClientInstance 类</span><br><span class="hljs-keyword">public</span> <span class="hljs-type">boolean</span> <span class="hljs-title function_">updateTopicRouteInfoFromNameServer</span><span class="hljs-params">(<span class="hljs-keyword">final</span> String topic, <span class="hljs-type">boolean</span> isDefault,</span><br><span class="hljs-params">        DefaultMQProducer defaultMQProducer)</span> &#123;<br><span class="hljs-keyword">try</span> &#123;<br>    <span class="hljs-keyword">if</span> (<span class="hljs-built_in">this</span>.lockNamesrv.tryLock(LOCK_TIMEOUT_MILLIS, TimeUnit.MILLISECONDS)) &#123;<br>        <span class="hljs-keyword">try</span> &#123;<br>            TopicRouteData topicRouteData;<br>            <span class="hljs-keyword">if</span> (isDefault &amp;&amp; defaultMQProducer != <span class="hljs-literal">null</span>) &#123;<br>                <span class="hljs-comment">// default=true 时会走下面逻辑，也就是会查询 TBW102 topic 的数据</span><br>                topicRouteData = <span class="hljs-built_in">this</span>.mQClientAPIImpl.getDefaultTopicRouteInfoFromNameServer(clientConfig.getMqClientApiTimeout());<br>                <span class="hljs-keyword">if</span> (topicRouteData != <span class="hljs-literal">null</span>) &#123;<br>                    <span class="hljs-keyword">for</span> (QueueData data : topicRouteData.getQueueDatas()) &#123;<br>                        <span class="hljs-comment">// 这里获取读写队列的逻辑是最配置的 值和 TBW102 topic readQueueNum 的最小值</span><br>                        <span class="hljs-comment">// 前面说 TBW102 topic readQueueNum 是8， 但是 getDefaultTopicQueueNums 默认是 4</span><br>                        <span class="hljs-comment">// private volatile int defaultTopicQueueNums = 4; （DefaultMQProducer 类）</span><br>                        <span class="hljs-type">int</span> <span class="hljs-variable">queueNums</span> <span class="hljs-operator">=</span> Math.min(defaultMQProducer.getDefaultTopicQueueNums(), data.getReadQueueNums());<br>                        data.setReadQueueNums(queueNums);<br>                        data.setWriteQueueNums(queueNums);<br>                    &#125;<br>                &#125;<br>            &#125;<br>            <span class="hljs-comment">// 其他代码省略</span><br>        &#125;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<p>后面代码就不分析了，基本会是从 <code>TopicRouteData</code> 中获取数据填充到 <code>TopicPublishInfo</code> 中</p>
<p><strong>选择 MessageQueue</strong><br>对于 <code>MessageQueue</code> 的内容可以参考</p>
<ol><li><a href="/2023/0137d652ba85/index.html" title="RocketMQ中MessageQueue详解">RocketMQ中MessageQueue详解</a></li></ol>

<h3 id="消息发送整体流程总结"><a href="#消息发送整体流程总结" class="headerlink" title="消息发送整体流程总结"></a>消息发送整体流程总结</h3><pre class="mermaid">sequenceDiagram
    participant A as DefaultMQProducer
    participant B as DefaultMQProducerImpl
    participant C as MQClientAPIImpl
    participant D as RemotingClient
    A->>B: 调用发送消息方法send()
    B->>B: sendDefaultImpl()
    B->>B: sendKernelImpl()
    B->>B: 发送request封装
    B->>C: sendMessage()
    C->>D: invokeSync()
    Note over D: 发送网络请求</pre>


<h2 id="普通-单向消息发送"><a href="#普通-单向消息发送" class="headerlink" title="普通-单向消息发送"></a>普通-单向消息发送</h2><p>单向发送和同步发送的差异是：单向发送消息不需要处理发送结果，不用管是发送成功还是发送失败，自然也就没有重试的逻辑</p>
<p><strong>单向发送的使用场景</strong><br>什么情况下可以使用单向发送呢？ 因为单向发送可能会丢失数据，所以允许丢失数据的场景就可以使用，一般来说像日志收集之类的可以使用单向发送</p>
<h2 id="普通-异步消息发送"><a href="#普通-异步消息发送" class="headerlink" title="普通-异步消息发送"></a>普通-异步消息发送</h2><p>可以从 <code>RocketMQ</code> 源码的 <code>example</code> 模块下找到 <code>org.apache.rocketmq.example.simple.AsyncProducer</code> 类，这是异步发送的示例代码</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">AsyncProducer</span> &#123;<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">main</span><span class="hljs-params">(</span><br><span class="hljs-params">        String[] args)</span> <span class="hljs-keyword">throws</span> MQClientException, InterruptedException, UnsupportedEncodingException &#123;<br><br>        <span class="hljs-type">DefaultMQProducer</span> <span class="hljs-variable">producer</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">DefaultMQProducer</span>(<span class="hljs-string">&quot;Jodie_Daily_test&quot;</span>);<br>        producer.start();<br>        <span class="hljs-comment">// suggest to on enableBackpressureForAsyncMode in heavy traffic, default is false</span><br>        producer.setEnableBackpressureForAsyncMode(<span class="hljs-literal">true</span>);<br>        producer.setRetryTimesWhenSendAsyncFailed(<span class="hljs-number">0</span>);<br><br>        <span class="hljs-type">int</span> <span class="hljs-variable">messageCount</span> <span class="hljs-operator">=</span> <span class="hljs-number">100</span>;<br>        <span class="hljs-keyword">final</span> <span class="hljs-type">CountDownLatch</span> <span class="hljs-variable">countDownLatch</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">CountDownLatch</span>(messageCount);<br>        <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> <span class="hljs-variable">i</span> <span class="hljs-operator">=</span> <span class="hljs-number">0</span>; i &lt; messageCount; i++) &#123;<br>            <span class="hljs-keyword">try</span> &#123;<br>                <span class="hljs-keyword">final</span> <span class="hljs-type">int</span> <span class="hljs-variable">index</span> <span class="hljs-operator">=</span> i;<br>                <span class="hljs-type">Message</span> <span class="hljs-variable">msg</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Message</span>(<span class="hljs-string">&quot;Jodie_topic_1023&quot;</span>,<br>                    <span class="hljs-string">&quot;TagA&quot;</span>,<br>                    <span class="hljs-string">&quot;OrderID188&quot;</span>,<br>                    <span class="hljs-string">&quot;Hello world&quot;</span>.getBytes(RemotingHelper.DEFAULT_CHARSET));<br>                producer.send(msg, <span class="hljs-keyword">new</span> <span class="hljs-title class_">SendCallback</span>() &#123;<br>                    <span class="hljs-meta">@Override</span><br>                    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">onSuccess</span><span class="hljs-params">(SendResult sendResult)</span> &#123;<br>                        countDownLatch.countDown();<br>                        System.out.printf(<span class="hljs-string">&quot;%-10d OK %s %n&quot;</span>, index, sendResult.getMsgId());<br>                    &#125;<br><br>                    <span class="hljs-meta">@Override</span><br>                    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">onException</span><span class="hljs-params">(Throwable e)</span> &#123;<br>                        countDownLatch.countDown();<br>                        System.out.printf(<span class="hljs-string">&quot;%-10d Exception %s %n&quot;</span>, index, e);<br>                        e.printStackTrace();<br>                    &#125;<br>                &#125;);<br>            &#125; <span class="hljs-keyword">catch</span> (Exception e) &#123;<br>                e.printStackTrace();<br>            &#125;<br>        &#125;<br>        countDownLatch.await(<span class="hljs-number">5</span>, TimeUnit.SECONDS);<br>        producer.shutdown();<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<p>同步消息使用的 <code>public SendResult send(Message msg)</code> 方法<br>异步消息使用的 <code>public void send(Message msg,SendCallback sendCallback)</code> 方法，也就是有回调函数<br>那么异步消息是如何实现的呢？ 接下来就看异步消息的核心实现原理, 下面先猜想一下，然后看源码去验证</p>
<ol>
<li>既然是异步，那么业务线程和实际发送消息的线程肯定不是同一个，基本上就可以肯定发送线程是使用另外一个线程池来实现的</li>
<li>异步调用时传入了 <code>SendCallback</code>， 那么在发送线程得到发送结果后就需要调用这个 <code>SendCallback</code>，这样就可以拿到返回结果</li>
</ol>
<p>异步调用流程</p>
<figure class="highlight leaf"><table><tr><td class="code"><pre><code class="hljs leaf">DefaultMQProducer<span class="hljs-punctuation">#</span><span class="hljs-keyword">send</span><span class="hljs-params">(<span class="hljs-variable">Message</span>, <span class="hljs-variable">SendCallback</span>)</span><br>    DefaultMQProducer<span class="hljs-punctuation">#</span><span class="hljs-keyword">sendDirect</span><span class="hljs-params">(<span class="hljs-variable">Message</span>, <span class="hljs-variable">MessageQueue</span>,<span class="hljs-variable">SendCallback</span>)</span><br>        DefaultMQProducerImpl<span class="hljs-punctuation">#</span><span class="hljs-keyword">send</span><span class="hljs-params">(<span class="hljs-variable">Message</span>, <span class="hljs-variable">SendCallback</span>)</span><br>            DefaultMQProducerImpl<span class="hljs-punctuation">#</span><span class="hljs-keyword">send</span><span class="hljs-params">(<span class="hljs-variable">Message</span>, <span class="hljs-variable">SendCallback</span>, <span class="hljs-variable">long</span>)</span>  【重点代码1】<br>                DefaultMQProducerImpl<span class="hljs-punctuation">#</span><span class="hljs-keyword">executeAsyncMessageSend</span><span class="hljs-params">(<span class="hljs-variable">Runnable</span>, <span class="hljs-variable">Message</span>, <span class="hljs-variable">BackpressureSendCallBack</span>,<span class="hljs-variable">long</span>, <span class="hljs-variable">long</span>)</span> 【重点代码2】<br>                    DefaultMQProducerImpl<span class="hljs-punctuation">#</span><span class="hljs-keyword">sendDefaultImpl</span><br></code></pre></td></tr></table></figure>

<p>根据上面的调用流程，直接看 <strong>重点代码1</strong> 部分， 即 <code>DefaultMQProducerImpl#send(Message, SendCallback, long)</code> 方法</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-comment">//  DefaultMQProducerImpl 类</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">send</span><span class="hljs-params">(<span class="hljs-keyword">final</span> Message msg, <span class="hljs-keyword">final</span> MessageQueue mq, <span class="hljs-keyword">final</span> SendCallback sendCallback, <span class="hljs-keyword">final</span> <span class="hljs-type">long</span> timeout)</span><br>        <span class="hljs-keyword">throws</span> MQClientException, RemotingException, InterruptedException &#123;<br>    <span class="hljs-type">BackpressureSendCallBack</span> <span class="hljs-variable">newCallBack</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">BackpressureSendCallBack</span>(sendCallback);<br>    <span class="hljs-keyword">final</span> <span class="hljs-type">long</span> <span class="hljs-variable">beginStartTime</span> <span class="hljs-operator">=</span> System.currentTimeMillis();<br>    <span class="hljs-comment">// 构造任务，这里只是创建了一个 Runnable 对象，并没有调用 start 方法</span><br>    <span class="hljs-comment">// 实际会将这个任务交给 线程池 去执行</span><br>    <span class="hljs-type">Runnable</span> <span class="hljs-variable">runnable</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Runnable</span>() &#123;<br>        <span class="hljs-meta">@Override</span><br>        <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">run</span><span class="hljs-params">()</span> &#123;<br>            <span class="hljs-keyword">try</span> &#123;<br>                makeSureStateOK();<br>                Validators.checkMessage(msg, defaultMQProducer);<br><br>                <span class="hljs-keyword">if</span> (!msg.getTopic().equals(mq.getTopic())) &#123;<br>                    <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">MQClientException</span>(<span class="hljs-string">&quot;Topic of the message does not match its target message queue&quot;</span>, <span class="hljs-literal">null</span>);<br>                &#125;<br>                <span class="hljs-type">long</span> <span class="hljs-variable">costTime</span> <span class="hljs-operator">=</span> System.currentTimeMillis() - beginStartTime;<br>                <span class="hljs-keyword">if</span> (timeout &gt; costTime) &#123;<br>                    <span class="hljs-keyword">try</span> &#123;<br>                        <span class="hljs-comment">// 在任务里面执行消息的发送，同时将 callback 回调函数传入，这样将收到消息的结果</span><br>                        <span class="hljs-comment">// 之后就可以调用回调函数</span><br>                        sendKernelImpl(msg, mq, CommunicationMode.ASYNC, newCallBack, <span class="hljs-literal">null</span>,<br>                            timeout - costTime);<br>                    &#125; <span class="hljs-keyword">catch</span> (MQBrokerException e) &#123;<br>                        <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">MQClientException</span>(<span class="hljs-string">&quot;unknown exception&quot;</span>, e);<br>                    &#125;<br>                &#125; <span class="hljs-keyword">else</span> &#123;<br>                    newCallBack.onException(<span class="hljs-keyword">new</span> <span class="hljs-title class_">RemotingTooMuchRequestException</span>(<span class="hljs-string">&quot;call timeout&quot;</span>));<br>                &#125;<br>            &#125; <span class="hljs-keyword">catch</span> (Exception e) &#123;<br>                newCallBack.onException(e);<br>            &#125;<br>        &#125;<br><br>    &#125;;<br>    <span class="hljs-comment">// 这里面就是将任务交给 线程池 去执行，也就是上面列出的 **重点代码2**</span><br>    executeAsyncMessageSend(runnable, msg, newCallBack, timeout, beginStartTime);<br>&#125;<br></code></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">executeAsyncMessageSend</span><span class="hljs-params">(Runnable runnable, <span class="hljs-keyword">final</span> Message msg, <span class="hljs-keyword">final</span> BackpressureSendCallBack sendCallback,</span><br><span class="hljs-params">        <span class="hljs-keyword">final</span> <span class="hljs-type">long</span> timeout, <span class="hljs-keyword">final</span> <span class="hljs-type">long</span> beginStartTime)</span><br>    <span class="hljs-keyword">throws</span> MQClientException, InterruptedException &#123;<br>    <span class="hljs-type">ExecutorService</span> <span class="hljs-variable">executor</span> <span class="hljs-operator">=</span> <span class="hljs-built_in">this</span>.getAsyncSenderExecutor();<br>    <span class="hljs-type">boolean</span> <span class="hljs-variable">isEnableBackpressureForAsyncMode</span> <span class="hljs-operator">=</span> <span class="hljs-built_in">this</span>.getDefaultMQProducer().isEnableBackpressureForAsyncMode();<br>    <span class="hljs-type">boolean</span> <span class="hljs-variable">isSemaphoreAsyncNumAquired</span> <span class="hljs-operator">=</span> <span class="hljs-literal">false</span>;<br>    <span class="hljs-type">boolean</span> <span class="hljs-variable">isSemaphoreAsyncSizeAquired</span> <span class="hljs-operator">=</span> <span class="hljs-literal">false</span>;<br>    <span class="hljs-type">int</span> <span class="hljs-variable">msgLen</span> <span class="hljs-operator">=</span> msg.getBody() == <span class="hljs-literal">null</span> ? <span class="hljs-number">1</span> : msg.getBody().length;<br><br>    <span class="hljs-keyword">try</span> &#123;<br>        <span class="hljs-keyword">if</span> (isEnableBackpressureForAsyncMode) &#123;<br>            <span class="hljs-comment">// 省略……</span><br>        &#125;<br>        sendCallback.isSemaphoreAsyncSizeAquired = isSemaphoreAsyncSizeAquired;<br>        sendCallback.isSemaphoreAsyncNumAquired = isSemaphoreAsyncNumAquired;<br>        sendCallback.msgLen = msgLen;<br>        <span class="hljs-comment">//  将发送消息的任务提交到线程池中</span><br>        executor.submit(runnable);<br>    &#125; <span class="hljs-keyword">catch</span> (RejectedExecutionException e) &#123;<br>        <span class="hljs-keyword">if</span> (isEnableBackpressureForAsyncMode) &#123;<br>            runnable.run();<br>        &#125; <span class="hljs-keyword">else</span> &#123;<br>            <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">MQClientException</span>(<span class="hljs-string">&quot;executor rejected &quot;</span>, e);<br>        &#125;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>
<p>至此异步消息任务提交就完成了，至于发送逻辑其实和同步就没什么区别了，只是在结果处理的地方会有区别，同步的话会需要等待结果返回，异步的话直接返回 <code>null</code>, 当收到响应后直接调用回调函数</p>
]]></content>
      <categories>
        <category>中间件</category>
        <category>RocketMQ</category>
      </categories>
      <tags>
        <tag>RocketMQ</tag>
      </tags>
  </entry>
  <entry>
    <title>RocketMQ之Broker详解</title>
    <url>/2023/5398bb79bdb5/index.html</url>
    <content><![CDATA[<blockquote>
<p>RocketMQ版本： 4.9.7<br>JDK版本： 17</p>
</blockquote>
<p>以下代码在 <code>broker</code> 子模块中</p>
<h2 id="获取启动文件"><a href="#获取启动文件" class="headerlink" title="获取启动文件"></a>获取启动文件</h2><p>启动 <code>broker</code> 的脚本是 <code>bin/mqbroker</code>, 这个执行文件的最后一行是 <code>sh $&#123;ROCKETMQ_HOME&#125;/bin/runbroker.sh org.apache.rocketmq.broker.BrokerStartup $@</code>， 所以在源码中启动 <code>broker</code> 的文件其实是 <code>BrokerStartup</code></p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">BrokerStartup</span> &#123;<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">main</span><span class="hljs-params">(String[] args)</span> &#123;<br>        start(createBrokerController(args));<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>
<p>分为两个部分</p>
<ul>
<li>创建 <code>BrokerController</code></li>
<li>启动 <code>BrokerController</code></li>
</ul>
<h2 id="BrokerController-创建流程"><a href="#BrokerController-创建流程" class="headerlink" title="BrokerController 创建流程"></a>BrokerController 创建流程</h2><h2 id="BrokerControlelr-启动流程"><a href="#BrokerControlelr-启动流程" class="headerlink" title="BrokerControlelr 启动流程"></a>BrokerControlelr 启动流程</h2>]]></content>
      <categories>
        <category>中间件</category>
        <category>RocketMQ</category>
      </categories>
      <tags>
        <tag>RocketMQ</tag>
      </tags>
  </entry>
  <entry>
    <title>RocketMQ安装使用和基本概念</title>
    <url>/2023/49fb249febb6/index.html</url>
    <content><![CDATA[<p>jdk版本：17<br>rocketmq版本：4.9.7 和 5.1.4</p>
<span class="hide_first_mermaid">
<div class="mermaid-wrap"><pre class="mermaid-src" hidden>
  flowchart LR
    A
  </pre></div>
</span>

<h2 id="基本概念"><a href="#基本概念" class="headerlink" title="基本概念"></a>基本概念</h2><p>如下图所示，出现了如下几个名词</p>
<ul>
<li><code>Broker</code></li>
<li><code>Producer</code></li>
<li><code>Consumer</code></li>
<li><code>Topic</code></li>
<li><code>Queue</code></li>
<li><code>NameServer</code></li>
</ul>
<pre class="mermaid">flowchart TB
    MP["Producer"]
    MC["Consumer"]
    style MP fill:#ca108c
    style MC fill:#ca108c
    subgraph A["消息队列集群"]
        style A fill:#8a5dca
        subgraph AA["Master"]
            subgraph A1["BrokerA"]
                style A1 fill:#7bed9f
                A11["queue 0 \n (TopicA)"]
                style A11 fill:#5dcac1
                A12["queue 0 \n (TopicB)"]
                style A12 fill:#ca8a5d
                A13["queue 0 \n (TopicC)"]
                style A13 fill:#f9d3e3
            end
            subgraph A2["BrokerB"]
                style A2 fill:#7bed9f
                A21["queue 1 \n (TopicA)"]
                style A21 fill:#5dcac1
                A22["queue 1 \n (TopicB)"]
                style A22 fill:#ca8a5d
                A23["queue 1 \n (TopicC)"]
                style A23 fill:#f9d3e3
            end
            subgraph A3["BrokerC"]
                style A3 fill:#7bed9f
                A31["queue 2 \n (TopicA)"]
                style A31 fill:#5dcac1
                A32["queue 2 \n (TopicB)"]
                style A32 fill:#ca8a5d
                A33["queue 2 \n (TopicC)"]
                style A33 fill:#f9d3e3
            end
            
        end
        subgraph BB["Slave"]
            subgraph B1["BrokerA"]
                B11["Message"]
            end
            subgraph B2["BrokerB"]
                B21["Message"]
            end
            subgraph B3["BrokerC"]
                B31["Message"]
            end
        end
        A1--同步-->B1
        A2--同步-->B2
        A3--同步-->B3
    end
    subgraph C["NameServer集群"]
        style C fill:#c0d695
        direction LR
        C1["NameServerA"]
        C2["NameServerB"]
        C3["NameServerC"]
        C1~~~C2~~~C3
    end
    A--"BrokerA注册"-->C
    A--"BrokerB注册"-->C
    A--"BrokerC注册"-->C
    MP--"投递消息"-->A
    A--"获取消息"-->MC
    MP--"获取元数据"-->C
    MC--"获取元数据"-->C</pre>

<p>下面就针对这些概念来进行讲解</p>
<h3 id="Broker"><a href="#Broker" class="headerlink" title="Broker"></a>Broker</h3><p><code>Broker</code> 就是 <code>RocketMq</code> 服务端程序，名称叫做 <code>Broker</code> 而已，<code>Broker</code> 是 <code>RocketMQ</code> 中的核心，简单来说它负责接收 <code>Producer</code> 发送过来的 <code>Message</code> 、并将其持久化起来，同时负责处理 <code>Consumer</code> 的消费请求</p>
<pre class="mermaid">graph LR
    A["生产者(Producer)"]
    B["消息队列(Broker)"]
    C["消费者(Consumer)"]
    A--"投递消息"-->B--"拉取消息"-->C</pre>

<h3 id="Producer-和-Consumer"><a href="#Producer-和-Consumer" class="headerlink" title="Producer 和 Consumer"></a>Producer 和 Consumer</h3><ul>
<li><code>Producer</code> 就是发送消息的程序，一般都是我们的业务程序作为 <code>Producer</code></li>
<li><code>Consuemr</code> 就是消费消息的程序，一般都是我们的业务程序作为 <code>Consumer</code></li>
</ul>
<h3 id="Topic"><a href="#Topic" class="headerlink" title="Topic"></a>Topic</h3><p><code>Topic</code> 是 <code>RocketMQ</code> 中消息传输和存储的顶层容器，用于标识同一类业务逻辑的消息。 主题的作用主要如下：</p>
<ul>
<li>定义数据的分类隔离： 在 <code>RocketMQ</code> 的方案设计中，建议将不同业务类型的数据拆分到不同的主题中管理，通过主题实现存储的隔离性和订阅隔离性</li>
<li>定义数据的身份和权限： <code>RocketMQ</code> 的消息本身是匿名无身份的，同一分类的消息使用相同的主题来做身份识别和权限管理</li>
<li><code>Topic</code> 实际是一个逻辑上的概念，消息实际是发送到 <code>MessageQueue</code> 中</li>
</ul>
<h3 id="MessageQueue"><a href="#MessageQueue" class="headerlink" title="MessageQueue"></a>MessageQueue</h3><p>直接使用 <code>Topic</code> 带来的问题负载不平衡，比如有的 <code>Topic</code> 数据量很大，有的 <code>Topic</code> 数据量很小，就会导致有的 <code>Broker</code> 压力大，有的 <code>Broker</code> 压力小，为了解决这个问题，消息实际上不是直接发往 <code>Topic</code> 的，而是发往 <code>MessageQueue</code>, 对应的关系图如下</p>
<pre class="mermaid">graph TB
    A["TopicA"]
    subgraph B["Broker集群"]
        subgraph B1["Broker1"]
            B11["MessageQueue-1"]
        end
        subgraph B2["Broker2"]
            B21["MessageQueue-2"]
        end
        subgraph B3["Broker3"]
            B31["MessageQueue-3"]
        end
    end
    style B11 fill:#7bed9f
    style B21 fill:#7bed9f
    style B31 fill:#7bed9f
    A-->B11
    A-->B21
    A-->B31</pre>

<p>上面有提到 <code>Topic</code> 的作用是对消息进行分类，<code>MessageQueue</code> 是消息存储和传输的实际容器，也是 <code>RocketMQ</code> 消息的最小存储单元， 根据上图就能看出来，同一类消息就可以分散存储在不同的 <code>Broker</code> 中，机器压力就分散了</p>
<h3 id="NameServer"><a href="#NameServer" class="headerlink" title="NameServer"></a>NameServer</h3><ul>
<li><code>NameServer</code> 用于存储整个 <code>RocketMQ</code> 集群的 元数据</li>
<li><code>NameServer</code> 中的元数据大概包含如下几类<ul>
<li>集群里都包含哪些 <code>Topic</code></li>
<li>这些 <code>Topic</code> 的 <code>MessageQueue</code> 分别在哪些 <code>Broker</code> 上</li>
<li>集群里都有哪些活跃的 <code>Broker</code></li>
</ul>
</li>
<li>为了避免 <code>NameServer</code> 单点故障，所以也可以将 <code>NameServer</code> 部署成集群，但是 <code>NameServer</code> 之间并没有数据传递， 每一个 <code>NameServer</code> 都包含完整的数据</li>
<li><code>Producer</code>,<code>Consumer</code>,<code>Broker</code> 都需要自行将数据注册到 <code>NameServer</code> 上，如果是 <code>NameServer</code> 是集群，则需要将数据注册到每一个 <code>NameServer</code> 中</li>
</ul>
<pre class="mermaid">graph TB
    subgraph A["生产者/消费者"]
        A1["生产者(Producer)"]
        A2["消费者(Consumer)"]
    end
    style A1 fill:#7bed9f
    style A2 fill:#7bed9f
    subgraph B["Broker集群"]
        B1["Broker1"]
        B2["Broker2"]
    end
    style B1 fill:#5dcac1
    style B2 fill:#5dcac1
    subgraph D["NameServer集群"]
        D1["NameServer1"]
        D2["NameServer2"]
    end
    style D1 fill:#c0d695
    style D2 fill:#c0d695
    A1-->D1
    A1-->D2
    A2-->D1
    A2-->D2
    B1-->D1
    B1-->D2
    B2-->D1
    B2-->D2</pre>


<h2 id="RocketMQ4-安装和使用"><a href="#RocketMQ4-安装和使用" class="headerlink" title="RocketMQ4 安装和使用"></a>RocketMQ4 安装和使用</h2><p>安装 <code>RocketMQ</code> 之前需要下安装 <code>JDK</code> 环境</p>
<ol><li><a href="/2023/e6e83576f25e/index.html" title="Ubuntu23.04安装jdk和maven">Ubuntu23.04安装jdk和maven</a></li></ol>

<p><a href="https://rocketmq.apache.org/download/">RocketMQ官方下载地址</a>，选择 <code>binary</code> 类型，本次使用的版本是 <code>4.9.7</code></p>
<p><a href="https://mirrors.aliyun.com/apache/rocketmq/">RocketMQ阿里云镜像站点</a></p>
<p>这里下载的是 <code>rocketmq-all-4.9.7-bin-release.zip</code> 然后将其上传到 <code>Ubuntu</code> 服务器中， 这里上传的位置是 <code>/opt/software</code></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">cd</span> /opt/software<br>unzip rocketmq-all-4.9.7-bin-release.zip<br><br><span class="hljs-built_in">cd</span> rocketmq-all-4.9.7-bin-release/<br><span class="hljs-comment"># 启动 namesrv</span><br><span class="hljs-built_in">nohup</span> bash bin/mqnamesrv &amp;<br><br><span class="hljs-comment"># 启动 broker</span><br><span class="hljs-built_in">nohup</span> bash bin/mqbroker -n localhost:9876 &amp;<br><br><span class="hljs-comment"># namesrv 和 broker 启动的日志都是存在 ~/logs/rocketmq-logs/ 下面</span><br><br><span class="hljs-comment"># 停止 broker</span><br><span class="hljs-built_in">cd</span> /opt/software/rocketmq-all-4.9.7-bin-release/<br>bash bin/mqshutdown broker<br><br><span class="hljs-comment"># 停止 namesrv</span><br><span class="hljs-built_in">cd</span> /opt/software/rocketmq-all-4.9.7-bin-release/<br>bash bin/mqshutdown namesrv<br></code></pre></td></tr></table></figure>
<h3 id="启动错误问题收集"><a href="#启动错误问题收集" class="headerlink" title="启动错误问题收集"></a>启动错误问题收集</h3><p><strong>内存不足</strong></p>
<p>启动 <code>namesrv</code> 或 <code>broker</code> 出现</p>
<figure class="highlight apache"><table><tr><td class="code"><pre><code class="hljs apache"><span class="hljs-attribute">os</span>::commit_memory(<span class="hljs-number">0</span>x0000000600000000, <span class="hljs-number">8589934592</span>, <span class="hljs-number">0</span>) failed; error=&#x27;Not enough space&#x27; (errno=<span class="hljs-number">12</span>)<br><span class="hljs-comment">#</span><br><span class="hljs-comment"># There is insufficient memory for the Java Runtime Environment to continue.</span><br><span class="hljs-comment"># Native memory allocation (mmap) failed to map 8589934592 bytes for committing reserved memory.</span><br></code></pre></td></tr></table></figure>

<p>这个是说明内存不足，可以调小 <code>jvm</code> 内存， 找到 <code>/opt/software/rocketmq-all-4.9.7-bin-release/bin/runserver.sh</code> 和 <code>/opt/software/rocketmq-all-4.9.7-bin-release/bin/runbroker.sh</code>， 找到类似 <code> -Xms8g -Xmx8g -Xmn4g</code> 的配置， 然后将 <code>8g</code> 改成 <code>2g</code> 即可</p>
<p><strong>参数不对</strong><br>启动 <code>broker</code> 出现</p>
<figure class="highlight subunit"><table><tr><td class="code"><pre><code class="hljs subunit">Unrecognized VM option &#x27;UseBiasedLocking&#x27;<br><span class="hljs-keyword">Error: </span>Could not create the Java Virtual Machine.<br><span class="hljs-keyword">Error: </span>A fatal exception has occurred. Program will exit.<br></code></pre></td></tr></table></figure>
<p>错误中已经有提示无法识别 <code>jvm</code> 参数  <code>UseBiasedLocking</code>， 这个基本上都是和使用的 <code>jdk</code> 版本有关，<code>broker</code> 启动脚本是 <code>bin</code> 目录下的 <code>runbroker.sh</code>， 可以找到这个文件将无法识别的参数去掉</p>
<h3 id="基于配置文件启动-Broker"><a href="#基于配置文件启动-Broker" class="headerlink" title="基于配置文件启动 Broker"></a>基于配置文件启动 Broker</h3><p>前面讲解的时候使用 <code>nohup bash bin/mqbroker -n localhost:9876 &amp;</code> 的方式来启动 <code>Broker</code>, 如果是使用 <code>Broker</code> 集群，这种启动方式比较麻烦，所以可以启动参数放入配置文件中， 默认在 <code>RocketMQ</code> 安装目录下的 <code>conf</code> 目录下， 启动配置文件为 <code>broker.conf</code></p>
<figure class="highlight properties"><table><tr><td class="code"><pre><code class="hljs properties"><span class="hljs-comment">#broker.conf</span><br><span class="hljs-attr">brokerClusterName</span> = <span class="hljs-string">DefaultCluster</span><br><span class="hljs-attr">brokerName</span> = <span class="hljs-string">broker-a</span><br><span class="hljs-attr">brokerId</span> = <span class="hljs-string">0</span><br><span class="hljs-attr">deleteWhen</span> = <span class="hljs-string">04</span><br><span class="hljs-attr">fileReservedTime</span> = <span class="hljs-string">48</span><br><span class="hljs-attr">brokerRole</span> = <span class="hljs-string">ASYNC_MASTER</span><br><span class="hljs-attr">flushDiskType</span> = <span class="hljs-string">ASYNC_FLUSH</span><br></code></pre></td></tr></table></figure>
<p>这个默认的配置文件并没有指定 <code>namesrv</code> 的地址，所以可以修改成如下形式</p>
<figure class="highlight properties"><table><tr><td class="code"><pre><code class="hljs properties"><span class="hljs-attr">&lt;!--</span> <span class="hljs-string">配置namesrv地址，可以是多个 --&gt;</span><br><span class="hljs-attr">namesrvAddr</span>=<span class="hljs-string">192.168.100.131:9876; 192.168.100.132:9876</span><br><span class="hljs-attr">&lt;!--</span> <span class="hljs-string">集群的名称，同一个集群中的多个 broker配置文件的集群名称要相同 --&gt;</span><br><span class="hljs-attr">brokerClusterName</span>=<span class="hljs-string">DefaultCluster</span><br><span class="hljs-attr">&lt;!--</span> <span class="hljs-string">当前 broker 的名称 --&gt;</span><br><span class="hljs-attr">brokerName</span>=<span class="hljs-string">broker-a</span><br><span class="hljs-attr">&lt;!--</span> <span class="hljs-string">一个Master Borker可以有多个Slave,0表示Master，大于0表示不同Slave的ID --&gt;</span><br><span class="hljs-attr">brokerId</span>=<span class="hljs-string">0</span><br><span class="hljs-attr">&lt;!--</span> <span class="hljs-string">与fileReservedTime参数呼应，表明在几点做消息删除动作，默认值04表示凌晨4点 --&gt;</span><br><span class="hljs-attr">deleteWhen</span>=<span class="hljs-string">04</span><br><span class="hljs-attr">&lt;!--</span> <span class="hljs-string">在磁盘上保存消息的时长，单位是小时，自动删除超时的消息 --&gt;</span><br><span class="hljs-attr">fileReservedTime</span>=<span class="hljs-string">48</span><br><span class="hljs-attr">&lt;!--</span> <span class="hljs-string">brokerRole有3种：SYNC_MASTER、ASYNC_MASTER、SLAVE。关键词SYNC和ASYNC表示Master和Slave</span><br><span class="hljs-attr">之间同步消息的机制，SYNC的意思是当Slave和Master消息同步完成后，再返回发送成功的状态</span> <span class="hljs-string">--&gt;</span><br><span class="hljs-attr">brokerRole</span>=<span class="hljs-string">SYNC_MASTER</span><br><span class="hljs-attr">&lt;!--</span> <span class="hljs-string">flushDiskType表示刷盘策略，分为SYNC_FLUSH和ASYNC_FLUSH两种，</span><br><span class="hljs-attr">分别代表同步刷盘和异步刷盘。同步刷盘情况下，消息真正写入磁盘后再返回成功状态；</span><br><span class="hljs-attr">异步刷盘情况下，消息写入page_cache后就返回成功状态</span> <span class="hljs-string">--&gt;</span><br><span class="hljs-attr">flushDiskType</span>=<span class="hljs-string">ASYNC_FLUSH</span><br><span class="hljs-attr">&lt;!--</span> <span class="hljs-string">Broker监听的端口号，如果一台机器上启动了多个Broker，则要设置不同的端口号，避免冲突 --&gt;</span><br><span class="hljs-attr">listenPort</span>=<span class="hljs-string">10911</span><br><span class="hljs-attr">&lt;!--</span> <span class="hljs-string">存储消息以及一些配置信息的根目录 --&gt;</span><br><span class="hljs-attr">storePathRootDir</span>=<span class="hljs-string">/home/rocketmq/store-a</span><br></code></pre></td></tr></table></figure>
<p>然后在启动多个 <code>namesrv</code> 之后可以使用下面的方式来启动 <code>broker</code></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">cd</span> /opt/software/rocketmq-all-4.9.7-bin-release/<br><span class="hljs-built_in">nohup</span> bash ./bin/mqbroker -c  ./conf/broker.conf &amp;<br></code></pre></td></tr></table></figure>

<h2 id="使用脚本快速启动RocketMQ-单机版本"><a href="#使用脚本快速启动RocketMQ-单机版本" class="headerlink" title="使用脚本快速启动RocketMQ 单机版本"></a>使用脚本快速启动RocketMQ 单机版本</h2><p>前面已经写了怎么通过命令来启动&#x2F;停止 <code>RocketMQ</code>， 但是每次需要进入特定的目录，而且启动和停止的命令也不一样，所以可以写一个脚本来简化操作（当前 <code>RocketMQ</code> 根目录是 <code>/opt/software/rocketmq-all-4.9.7-bin-release/</code> ）</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">cd</span> /opt/software/rocketmq-all-4.9.7-bin-release/<br>vim rocketmq.sh<br></code></pre></td></tr></table></figure>
<p><code>rocketmq.sh</code> 脚本内容如下</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash"><span class="hljs-meta">#!/bin/bash</span><br><br>ROCKETMQ_HOME=/opt/software/rocketmq-all-4.9.7-bin-release<br><br><span class="hljs-keyword">if</span> [ <span class="hljs-string">&quot;<span class="hljs-variable">$1</span>&quot;</span> = <span class="hljs-string">&quot;start&quot;</span> ]; <span class="hljs-keyword">then</span><br>  <span class="hljs-built_in">nohup</span> sh <span class="hljs-variable">$ROCKETMQ_HOME</span>/bin/mqnamesrv &gt;  /dev/null 2&gt;&amp;1 &amp;<br>  <span class="hljs-built_in">sleep</span> 3<br>  <span class="hljs-built_in">nohup</span> sh <span class="hljs-variable">$ROCKETMQ_HOME</span>/bin/mqbroker -n localhost:9876 &gt; /dev/null 2&gt;&amp;1 &amp;<br>  <span class="hljs-built_in">echo</span> <span class="hljs-string">&quot;RocketMQ nameserver and broker started&quot;</span><br><span class="hljs-keyword">elif</span> [ <span class="hljs-string">&quot;<span class="hljs-variable">$1</span>&quot;</span> = <span class="hljs-string">&quot;stop&quot;</span> ]; <span class="hljs-keyword">then</span><br>  sh <span class="hljs-variable">$ROCKETMQ_HOME</span>/bin/mqshutdown broker<br>  <span class="hljs-built_in">sleep</span> 3<br>  sh <span class="hljs-variable">$ROCKETMQ_HOME</span>/bin/mqshutdown namesrv<br>  <span class="hljs-built_in">echo</span> <span class="hljs-string">&quot;RocketMQ broker and nameserver stopped&quot;</span><br><span class="hljs-keyword">else</span><br>  <span class="hljs-built_in">echo</span> <span class="hljs-string">&quot;Usage: <span class="hljs-variable">$0</span> start|stop&quot;</span><br>  <span class="hljs-built_in">exit</span> 1<br><span class="hljs-keyword">fi</span><br></code></pre></td></tr></table></figure>

<p>然后将脚本的权限设置为可执行</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">chmod</span> +x rocketmq.sh<br></code></pre></td></tr></table></figure>

<p>创建软连接，在任意位置都可以执行这个脚本</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">sudo <span class="hljs-built_in">ln</span> -s /opt/software/rocketmq-all-4.9.7-bin-release/rocketmq.sh /usr/local/bin/rocketmq.sh<br><span class="hljs-comment"># 接下来的命令是可以在任意位置执行</span><br><span class="hljs-comment"># 启动 rocketmq</span><br>rocketmq.sh start<br><br><span class="hljs-comment"># 停止 rocketmq</span><br>rocketmq.sh stop<br></code></pre></td></tr></table></figure>

<h2 id="安装-RocketMQ-dashboard（rocketmq-console）"><a href="#安装-RocketMQ-dashboard（rocketmq-console）" class="headerlink" title="安装 RocketMQ dashboard（rocketmq-console）"></a>安装 RocketMQ dashboard（rocketmq-console）</h2><p>以前 <code>RokcetMQ</code> 界面监控服务叫做 <code>rocketmq-console</code>， 现在已经改成 <code>rocketmq-dashboard</code>， 所以需要安装 <code>rocketmq-dashboard</code></p>
<p><a href="https://rocketmq.apache.org/zh/docs/4.x/deployment/03Dashboard">rocketmq-dashboard官方安装说明</a></p>
<p>使用 <code>docker</code> 安装更简单，所以就使用 <code>docker</code> 安装的方式</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">docker pull apacherocketmq/rocketmq-dashboard:latest<br>docker run -d --name rocketmq-dashboard -e <span class="hljs-string">&quot;JAVA_OPTS=-Drocketmq.namesrv.addr=192.168.208.130:9876&quot;</span> -p 8080:8080 -t apacherocketmq/rocketmq-dashboard:latest<br></code></pre></td></tr></table></figure>
<p>因为 <code>RocketMQ</code> 是安装在 <code>Ubuntu</code> 虚拟机中，但是访问是在 <code>Window</code> 中，所以这里 <code>rocketmq.namesrv.addr</code> 填写的就是虚拟机的 <code>ip</code></p>
<p>然后在 <code>Window</code> 中访问 <code>http://192.168.208.130:8080</code> 即可看到 <code>RocketMQ</code> 的监控页面</p>
<h3 id="修改脚本一次性启动-RocketMQ-和-Dashboard"><a href="#修改脚本一次性启动-RocketMQ-和-Dashboard" class="headerlink" title="修改脚本一次性启动 RocketMQ 和 Dashboard"></a>修改脚本一次性启动 <code>RocketMQ</code> 和 <code>Dashboard</code></h3><p>前提条件是已经使用上面 <code>docker</code> 的方式启动过一次 <code>rocketq-dashboard</code>, 这样对应容器的名称就是 <code>rocketmq-dashboard</code>， 也就可以使用下面的脚本一次性启动和关闭</p>
<p><code>rocketmq.sh</code> 脚本内容修改成如下形式</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash"><span class="hljs-meta">#!/bin/bash</span><br><br>ROCKETMQ_HOME=/opt/software/rocketmq-all-4.9.7-bin-release<br><br><span class="hljs-keyword">if</span> [ <span class="hljs-string">&quot;<span class="hljs-variable">$1</span>&quot;</span> = <span class="hljs-string">&quot;start&quot;</span> ]; <span class="hljs-keyword">then</span><br>  <span class="hljs-built_in">nohup</span> sh <span class="hljs-variable">$ROCKETMQ_HOME</span>/bin/mqnamesrv &gt;  /dev/null 2&gt;&amp;1 &amp;<br>  ecgi <span class="hljs-string">&quot;RocketMQ namesrv started&quot;</span><br>  <span class="hljs-built_in">sleep</span> 3<br>  <span class="hljs-built_in">nohup</span> sh <span class="hljs-variable">$ROCKETMQ_HOME</span>/bin/mqbroker -n localhost:9876 &gt; /dev/null 2&gt;&amp;1 &amp;<br>  <span class="hljs-built_in">echo</span> <span class="hljs-string">&quot;RocketMQ broker started&quot;</span><br>  <span class="hljs-built_in">sleep</span> 3<br>  docker start rocketmq-dashboard<br>  <span class="hljs-built_in">echo</span> <span class="hljs-string">&quot;rocketmq-dashboard started&quot;</span><br><span class="hljs-keyword">elif</span> [ <span class="hljs-string">&quot;<span class="hljs-variable">$1</span>&quot;</span> = <span class="hljs-string">&quot;stop&quot;</span> ]; <span class="hljs-keyword">then</span><br>  sh <span class="hljs-variable">$ROCKETMQ_HOME</span>/bin/mqshutdown broker<br>  <span class="hljs-built_in">sleep</span> 3<br>  <span class="hljs-built_in">echo</span> <span class="hljs-string">&quot;rocketmq broker stopped&quot;</span><br>  sh <span class="hljs-variable">$ROCKETMQ_HOME</span>/bin/mqshutdown namesrv<br>  <span class="hljs-built_in">echo</span> <span class="hljs-string">&quot;RocketMQ nameserver stopped&quot;</span><br>  <span class="hljs-built_in">sleep</span> 2<br>  docker stop rocketmq-dashboard<br>  <span class="hljs-built_in">echo</span> <span class="hljs-string">&quot;rocketmq-dashboard stopped&quot;</span><br><span class="hljs-keyword">else</span><br>  <span class="hljs-built_in">echo</span> <span class="hljs-string">&quot;Usage: <span class="hljs-variable">$0</span> start|stop&quot;</span><br>  <span class="hljs-built_in">exit</span> 1<br><span class="hljs-keyword">fi</span><br></code></pre></td></tr></table></figure>
<ul>
<li>使用 <code>rocketmq.sh start</code> 启动 <code>Namesrv</code> 和 <code>Broker</code> 和 <code>Dashboard</code></li>
<li>使用 <code>rocketmq.sh stop</code> 停止 <code>Namesrv</code> 和 <code>Broker</code> 和 <code>Dashboard</code></li>
</ul>
<h2 id="下载-RocketMQ-源码测试"><a href="#下载-RocketMQ-源码测试" class="headerlink" title="下载 RocketMQ 源码测试"></a>下载 RocketMQ 源码测试</h2><p>前面下载和安装的都是 <code>binary</code> 类型，可以认为是 <code>RocketMQ</code> 的服务端程序( <code>Namesrv</code> 和 <code>Broker</code> )， 接下来使用源码是作为 <code>Producer</code> 和 <code>Consumer</code> 进行消息的发送和接收的</p>
<blockquote>
<p>RocketMQ 源码也是可以原来启动 Namesrv 和 Broker 的，不过如果不是要看对应的源码，还是使用 Binary 方式启动 Namesrv 和 broker 比较方便</p>
</blockquote>
<p><a href="https://rocketmq.apache.org/download/">RocketMQ官方下载地址</a>，选择 <code>Source</code> 类型，本次使用的版本是 <code>4.9.7</code>， 解压后直接用 <code>idea</code> 导入即可</p>
<h3 id="发送消息测试"><a href="#发送消息测试" class="headerlink" title="发送消息测试"></a>发送消息测试</h3><ul>
<li>打开 <code>rocket</code> 源码后找到 <code>example</code> 子项目，在该子项目下找到 <code>org.apache.rocketmq.example.simple.Producer</code> 类</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">Producer</span> &#123;<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">main</span><span class="hljs-params">(String[] args)</span> <span class="hljs-keyword">throws</span> MQClientException, InterruptedException &#123;<br><br>        <span class="hljs-type">DefaultMQProducer</span> <span class="hljs-variable">producer</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">DefaultMQProducer</span>(<span class="hljs-string">&quot;ProducerGroupName&quot;</span>);<br>        <span class="hljs-comment">// 这一行是自己添加的，指定 Namesrv 的地址</span><br>        producer.setNamesrvAddr(<span class="hljs-string">&quot;192.168.208.130:9876&quot;</span>);<br>        producer.start();<br><br>        <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> <span class="hljs-variable">i</span> <span class="hljs-operator">=</span> <span class="hljs-number">0</span>; i &lt; <span class="hljs-number">128</span>; i++)<br>            <span class="hljs-keyword">try</span> &#123;<br>                &#123;<br>                    <span class="hljs-type">Message</span> <span class="hljs-variable">msg</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Message</span>(<span class="hljs-string">&quot;TopicTest&quot;</span>,<br>                        <span class="hljs-string">&quot;TagA&quot;</span>,<br>                        <span class="hljs-string">&quot;OrderID188&quot;</span>,<br>                        <span class="hljs-string">&quot;Hello world&quot;</span>.getBytes(RemotingHelper.DEFAULT_CHARSET));<br>                    <span class="hljs-type">SendResult</span> <span class="hljs-variable">sendResult</span> <span class="hljs-operator">=</span> producer.send(msg);<br>                    System.out.printf(<span class="hljs-string">&quot;%s%n&quot;</span>, sendResult);<br>                &#125;<br><br>            &#125; <span class="hljs-keyword">catch</span> (Exception e) &#123;<br>                e.printStackTrace();<br>            &#125;<br>        producer.shutdown();<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>
<p>直接启动就可以发送消息到 <code>RocketMQ</code> 中， 可以打开 <code>RocketMQ dashboard</code> 进行查看</p>
<h3 id="接收消息测试"><a href="#接收消息测试" class="headerlink" title="接收消息测试"></a>接收消息测试</h3><ul>
<li>打开 <code>rocket</code> 源码后找到 <code>example</code> 子项目，在该子项目下找到 <code>org.apache.rocketmq.example.simple.PullConsumer</code> 类</li>
</ul>
<p>主要是修改连接 <code>Namesrv</code> 的地址和增加消息接收后的打印信息</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">PullConsumer</span> &#123;<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">main</span><span class="hljs-params">(String[] args)</span> <span class="hljs-keyword">throws</span> MQClientException &#123;<br><br>        <span class="hljs-type">DefaultMQPullConsumer</span> <span class="hljs-variable">consumer</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">DefaultMQPullConsumer</span>(<span class="hljs-string">&quot;please_rename_unique_group_name_5&quot;</span>);<br>        <span class="hljs-comment">// 修改 namesrv 的地址</span><br>        consumer.setNamesrvAddr(<span class="hljs-string">&quot;192.168.208.130:9876&quot;</span>);<br>        Set&lt;String&gt; topics = <span class="hljs-keyword">new</span> <span class="hljs-title class_">HashSet</span>&lt;&gt;();<br>        <span class="hljs-comment">//You would better to register topics,It will use in rebalance when starting</span><br>        topics.add(<span class="hljs-string">&quot;TopicTest&quot;</span>);<br>        consumer.setRegisterTopics(topics);<br>        consumer.start();<br><br>        <span class="hljs-type">ExecutorService</span> <span class="hljs-variable">executors</span> <span class="hljs-operator">=</span> Executors.newFixedThreadPool(topics.size(), <span class="hljs-keyword">new</span> <span class="hljs-title class_">ThreadFactory</span>() &#123;<br>            <span class="hljs-meta">@Override</span><br>            <span class="hljs-keyword">public</span> Thread <span class="hljs-title function_">newThread</span><span class="hljs-params">(Runnable r)</span> &#123;<br>                <span class="hljs-keyword">return</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Thread</span>(r, <span class="hljs-string">&quot;PullConsumerThread&quot;</span>);<br>            &#125;<br>        &#125;);<br>        <span class="hljs-keyword">for</span> (String topic : consumer.getRegisterTopics()) &#123;<br><br>            executors.execute(<span class="hljs-keyword">new</span> <span class="hljs-title class_">Runnable</span>() &#123;<br><br>                <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">doSomething</span><span class="hljs-params">(List&lt;MessageExt&gt; msgs)</span> &#123;<br>                    <span class="hljs-comment">// 打印接收到的消息</span><br>                    System.out.println(<span class="hljs-string">&quot;接收到消息&quot;</span> + msgs.toString());<br><br>                &#125;<br><br>                <span class="hljs-meta">@Override</span><br>                <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">run</span><span class="hljs-params">()</span> &#123;<br>                    <span class="hljs-keyword">while</span> (<span class="hljs-literal">true</span>) &#123;<br>                        <span class="hljs-keyword">try</span> &#123;<br>                            Set&lt;MessageQueue&gt; messageQueues = consumer.fetchMessageQueuesInBalance(topic);<br>                            <span class="hljs-keyword">if</span> (messageQueues == <span class="hljs-literal">null</span> || messageQueues.isEmpty()) &#123;<br>                                Thread.sleep(<span class="hljs-number">1000</span>);<br>                                <span class="hljs-keyword">continue</span>;<br>                            &#125;<br>                            <span class="hljs-type">PullResult</span> <span class="hljs-variable">pullResult</span> <span class="hljs-operator">=</span> <span class="hljs-literal">null</span>;<br>                            <span class="hljs-keyword">for</span> (MessageQueue messageQueue : messageQueues) &#123;<br>                                <span class="hljs-keyword">try</span> &#123;<br>                                    <span class="hljs-type">long</span> <span class="hljs-variable">offset</span> <span class="hljs-operator">=</span> <span class="hljs-built_in">this</span>.consumeFromOffset(messageQueue);<br>                                    pullResult = consumer.pull(messageQueue, <span class="hljs-string">&quot;*&quot;</span>, offset, <span class="hljs-number">32</span>);<br>                                    <span class="hljs-keyword">switch</span> (pullResult.getPullStatus()) &#123;<br>                                        <span class="hljs-keyword">case</span> FOUND:<br>                                            List&lt;MessageExt&gt; msgs = pullResult.getMsgFoundList();<br><br>                                            <span class="hljs-keyword">if</span> (msgs != <span class="hljs-literal">null</span> &amp;&amp; !msgs.isEmpty()) &#123;<br>                                                <span class="hljs-built_in">this</span>.doSomething(msgs);<br>                                                <span class="hljs-comment">//update offset to broker</span><br>                                                consumer.updateConsumeOffset(messageQueue, pullResult.getNextBeginOffset());<br>                                                <span class="hljs-comment">//print pull tps</span><br>                                                <span class="hljs-built_in">this</span>.incPullTPS(topic, pullResult.getMsgFoundList().size());<br>                                            &#125;<br>                                            <span class="hljs-keyword">break</span>;<br>                                        <span class="hljs-keyword">case</span> OFFSET_ILLEGAL:<br>                                            consumer.updateConsumeOffset(messageQueue, pullResult.getNextBeginOffset());<br>                                            <span class="hljs-keyword">break</span>;<br>                                        <span class="hljs-keyword">case</span> NO_NEW_MSG:<br>                                            Thread.sleep(<span class="hljs-number">1</span>);<br>                                            consumer.updateConsumeOffset(messageQueue, pullResult.getNextBeginOffset());<br>                                            <span class="hljs-keyword">break</span>;<br>                                        <span class="hljs-keyword">case</span> NO_MATCHED_MSG:<br>                                            consumer.updateConsumeOffset(messageQueue, pullResult.getNextBeginOffset());<br>                                            <span class="hljs-keyword">break</span>;<br>                                        <span class="hljs-keyword">default</span>:<br>                                    &#125;<br>                                &#125; <span class="hljs-keyword">catch</span> (RemotingException e) &#123;<br>                                    e.printStackTrace();<br>                                &#125; <span class="hljs-keyword">catch</span> (MQBrokerException e) &#123;<br>                                    e.printStackTrace();<br>                                &#125; <span class="hljs-keyword">catch</span> (Exception e) &#123;<br>                                    e.printStackTrace();<br>                                &#125;<br>                            &#125;<br>                        &#125; <span class="hljs-keyword">catch</span> (MQClientException e) &#123;<br>                            <span class="hljs-comment">//reblance error</span><br>                            e.printStackTrace();<br>                        &#125; <span class="hljs-keyword">catch</span> (InterruptedException e) &#123;<br>                            e.printStackTrace();<br>                        &#125; <span class="hljs-keyword">catch</span> (Exception e) &#123;<br>                            e.printStackTrace();<br>                        &#125;<br>                    &#125;<br>                &#125;<br><br>                <span class="hljs-keyword">public</span> <span class="hljs-type">long</span> <span class="hljs-title function_">consumeFromOffset</span><span class="hljs-params">(MessageQueue messageQueue)</span> <span class="hljs-keyword">throws</span> MQClientException &#123;<br>                    <span class="hljs-comment">//-1 when started</span><br>                    <span class="hljs-type">long</span> <span class="hljs-variable">offset</span> <span class="hljs-operator">=</span> consumer.getOffsetStore().readOffset(messageQueue, ReadOffsetType.READ_FROM_MEMORY);<br>                    <span class="hljs-keyword">if</span> (offset &lt; <span class="hljs-number">0</span>) &#123;<br>                        <span class="hljs-comment">//query from broker</span><br>                        offset = consumer.getOffsetStore().readOffset(messageQueue, ReadOffsetType.READ_FROM_STORE);<br>                    &#125;<br>                    <span class="hljs-keyword">if</span> (offset &lt; <span class="hljs-number">0</span>) &#123;<br>                        <span class="hljs-comment">//first time start from last offset</span><br>                        offset = consumer.maxOffset(messageQueue);<br>                    &#125;<br>                    <span class="hljs-comment">//make sure</span><br>                    <span class="hljs-keyword">if</span> (offset &lt; <span class="hljs-number">0</span>) &#123;<br>                        offset = <span class="hljs-number">0</span>;<br>                    &#125;<br>                    <span class="hljs-keyword">return</span> offset;<br>                &#125;<br><br>                <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">incPullTPS</span><span class="hljs-params">(String topic, <span class="hljs-type">int</span> pullSize)</span> &#123;<br>                    consumer.getDefaultMQPullConsumerImpl().getRebalanceImpl().getmQClientFactory()<br>                            .getConsumerStatsManager().incPullTPS(consumer.getConsumerGroup(), topic, pullSize);<br>                &#125;<br>            &#125;);<br><br>        &#125;<br><span class="hljs-comment">//        executors.shutdown();</span><br><span class="hljs-comment">//        consumer.shutdown();</span><br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<h2 id="安装-RocketMQ5-版本"><a href="#安装-RocketMQ5-版本" class="headerlink" title="安装 RocketMQ5 版本"></a>安装 RocketMQ5 版本</h2><p>和 <code>RocketMQ</code> 流程一样，当启动 <code>broker</code> 的时候会出现如下错误</p>
<figure class="highlight stylus"><table><tr><td class="code"><pre><code class="hljs stylus">java<span class="hljs-selector-class">.lang</span><span class="hljs-selector-class">.IllegalAccessError</span>: class org<span class="hljs-selector-class">.apache</span><span class="hljs-selector-class">.rocketmq</span><span class="hljs-selector-class">.common</span><span class="hljs-selector-class">.UtilAll</span> (<span class="hljs-keyword">in</span> unnamed module @<span class="hljs-number">0</span>x74582ff6) cannot access class sun<span class="hljs-selector-class">.nio</span><span class="hljs-selector-class">.ch</span><span class="hljs-selector-class">.DirectBuffer</span> (<span class="hljs-keyword">in</span> module java.base) because module java<span class="hljs-selector-class">.base</span> does not export sun<span class="hljs-selector-class">.nio</span><span class="hljs-selector-class">.ch</span> to unnamed module @<span class="hljs-number">0</span>x74582ff6<br>        at org<span class="hljs-selector-class">.apache</span><span class="hljs-selector-class">.rocketmq</span><span class="hljs-selector-class">.common</span><span class="hljs-selector-class">.UtilAll</span><span class="hljs-selector-class">.viewed</span>(UtilAll<span class="hljs-selector-class">.java</span>:<span class="hljs-number">741</span>)<br>        at org<span class="hljs-selector-class">.apache</span><span class="hljs-selector-class">.rocketmq</span><span class="hljs-selector-class">.common</span><span class="hljs-selector-class">.UtilAll</span><span class="hljs-selector-class">.cleanBuffer</span>(UtilAll<span class="hljs-selector-class">.java</span>:<span class="hljs-number">705</span>)<br>        at org<span class="hljs-selector-class">.apache</span><span class="hljs-selector-class">.rocketmq</span><span class="hljs-selector-class">.store</span><span class="hljs-selector-class">.logfile</span><span class="hljs-selector-class">.DefaultMappedFile</span><span class="hljs-selector-class">.cleanup</span>(DefaultMappedFile<span class="hljs-selector-class">.java</span>:<span class="hljs-number">540</span>)<br>        at org<span class="hljs-selector-class">.apache</span><span class="hljs-selector-class">.rocketmq</span><span class="hljs-selector-class">.store</span><span class="hljs-selector-class">.ReferenceResource</span><span class="hljs-selector-class">.release</span>(ReferenceResource<span class="hljs-selector-class">.java</span>:<span class="hljs-number">63</span>)<br>        at org<span class="hljs-selector-class">.apache</span><span class="hljs-selector-class">.rocketmq</span><span class="hljs-selector-class">.store</span><span class="hljs-selector-class">.ReferenceResource</span><span class="hljs-selector-class">.shutdown</span>(ReferenceResource<span class="hljs-selector-class">.java</span>:<span class="hljs-number">47</span>)<br>        at org<span class="hljs-selector-class">.apache</span><span class="hljs-selector-class">.rocketmq</span><span class="hljs-selector-class">.store</span><span class="hljs-selector-class">.logfile</span><span class="hljs-selector-class">.DefaultMappedFile</span><span class="hljs-selector-class">.destroy</span>(DefaultMappedFile<span class="hljs-selector-class">.java</span>:<span class="hljs-number">551</span>)<br>        at org<span class="hljs-selector-class">.apache</span><span class="hljs-selector-class">.rocketmq</span><span class="hljs-selector-class">.store</span><span class="hljs-selector-class">.index</span><span class="hljs-selector-class">.IndexFile</span><span class="hljs-selector-class">.destroy</span>(IndexFile<span class="hljs-selector-class">.java</span>:<span class="hljs-number">110</span>)<br>        at org<span class="hljs-selector-class">.apache</span><span class="hljs-selector-class">.rocketmq</span><span class="hljs-selector-class">.store</span><span class="hljs-selector-class">.index</span><span class="hljs-selector-class">.IndexService</span><span class="hljs-selector-class">.load</span>(IndexService<span class="hljs-selector-class">.java</span>:<span class="hljs-number">72</span>)<br>        at org<span class="hljs-selector-class">.apache</span><span class="hljs-selector-class">.rocketmq</span><span class="hljs-selector-class">.store</span><span class="hljs-selector-class">.DefaultMessageStore</span><span class="hljs-selector-class">.load</span>(DefaultMessageStore<span class="hljs-selector-class">.java</span>:<span class="hljs-number">341</span>)<br>        at org<span class="hljs-selector-class">.apache</span><span class="hljs-selector-class">.rocketmq</span><span class="hljs-selector-class">.broker</span><span class="hljs-selector-class">.BrokerController</span><span class="hljs-selector-class">.recoverAndInitService</span>(BrokerController<span class="hljs-selector-class">.java</span>:<span class="hljs-number">806</span>)<br>        at org<span class="hljs-selector-class">.apache</span><span class="hljs-selector-class">.rocketmq</span><span class="hljs-selector-class">.broker</span><span class="hljs-selector-class">.BrokerController</span><span class="hljs-selector-class">.initialize</span>(BrokerController<span class="hljs-selector-class">.java</span>:<span class="hljs-number">792</span>)<br>        at org<span class="hljs-selector-class">.apache</span><span class="hljs-selector-class">.rocketmq</span><span class="hljs-selector-class">.broker</span><span class="hljs-selector-class">.BrokerStartup</span><span class="hljs-selector-class">.createBrokerController</span>(BrokerStartup<span class="hljs-selector-class">.java</span>:<span class="hljs-number">240</span>)<br>        at org<span class="hljs-selector-class">.apache</span><span class="hljs-selector-class">.rocketmq</span><span class="hljs-selector-class">.broker</span><span class="hljs-selector-class">.BrokerStartup</span><span class="hljs-selector-class">.main</span>(BrokerStartup<span class="hljs-selector-class">.java</span>:<span class="hljs-number">50</span>)<br></code></pre></td></tr></table></figure>
<p>这个是和使用的 <code>jdk</code> 版本高于 8 的原因导致，这里使用的 <code>jdk</code> 版本是 17</p>
<p>解决方案如下：打开 <code>bin</code> 下面的 <code>runbroker.sh</code></p>
<p>原来的 <code>runbroker.sh</code> 是这样</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash"><span class="hljs-keyword">if</span> [ $? -eq 0 ]<br><span class="hljs-keyword">then</span><br>        <span class="hljs-keyword">if</span> [ -z <span class="hljs-string">&quot;<span class="hljs-variable">$RMQ_NUMA_NODE</span>&quot;</span> ] ; <span class="hljs-keyword">then</span><br>                numactl --interleave=all <span class="hljs-variable">$JAVA</span> <span class="hljs-variable">$&#123;JAVA_OPT&#125;</span> <span class="hljs-variable">$@</span><br>        <span class="hljs-keyword">else</span><br>                numactl --cpunodebind=<span class="hljs-variable">$RMQ_NUMA_NODE</span> --membind=<span class="hljs-variable">$RMQ_NUMA_NODE</span> <span class="hljs-variable">$JAVA</span> <span class="hljs-variable">$&#123;JAVA_OPT&#125;</span> <span class="hljs-variable">$@</span><br>        <span class="hljs-keyword">fi</span><br><span class="hljs-keyword">else</span><br>        <span class="hljs-string">&quot;<span class="hljs-variable">$JAVA</span>&quot;</span> <span class="hljs-variable">$&#123;JAVA_OPT&#125;</span>   <span class="hljs-variable">$@</span><br><span class="hljs-keyword">fi</span><br></code></pre></td></tr></table></figure>
<p>修改后变成如下形式，主要是在启动命令中增加 <code>--add-exports=java.base/sun.nio.ch=ALL-UNNAMED</code></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash"><span class="hljs-keyword">if</span> [ $? -eq 0 ]<br><span class="hljs-keyword">then</span><br>        <span class="hljs-keyword">if</span> [ -z <span class="hljs-string">&quot;<span class="hljs-variable">$RMQ_NUMA_NODE</span>&quot;</span> ] ; <span class="hljs-keyword">then</span><br>                numactl --interleave=all <span class="hljs-variable">$JAVA</span> <span class="hljs-variable">$&#123;JAVA_OPT&#125;</span> --add-exports=java.base/sun.nio.ch=ALL-UNNAMED <span class="hljs-variable">$@</span><br>        <span class="hljs-keyword">else</span><br>                numactl --cpunodebind=<span class="hljs-variable">$RMQ_NUMA_NODE</span> --membind=<span class="hljs-variable">$RMQ_NUMA_NODE</span> <span class="hljs-variable">$JAVA</span> <span class="hljs-variable">$&#123;JAVA_OPT&#125;</span> --add-exports=java.base/sun.nio.ch=ALL-UNNAMED <span class="hljs-variable">$@</span><br>        <span class="hljs-keyword">fi</span><br><span class="hljs-keyword">else</span><br>        <span class="hljs-string">&quot;<span class="hljs-variable">$JAVA</span>&quot;</span> <span class="hljs-variable">$&#123;JAVA_OPT&#125;</span> --add-exports=java.base/sun.nio.ch=ALL-UNNAMED  <span class="hljs-variable">$@</span><br><span class="hljs-keyword">fi</span><br></code></pre></td></tr></table></figure>


<h2 id="通过RocketMQ5源码启动单机版-Nameserver-和-Broker"><a href="#通过RocketMQ5源码启动单机版-Nameserver-和-Broker" class="headerlink" title="通过RocketMQ5源码启动单机版 Nameserver 和 Broker"></a>通过RocketMQ5源码启动单机版 Nameserver 和 Broker</h2><p>使用的系统是 <code>win11</code>, 启动前需要先编译，参考</p>
<ol><li><a href="/2023/2adf4cc6108b/index.html" title="使用idea编译RocketMQ5源码">使用idea编译RocketMQ5源码</a></li></ol>

<p>找一个位置创建一个目录作为 <code>ROCKETMQ_HOME</code>, 比如创建目录 <code>D:\data\rocketmq</code>， 现在 <code>ROCKETMQ_HOME=D:\data\rocketmq</code><br>在 <code>ROCKETMQ_HOME</code> 目录下创建 <code>conf</code> 和 <code>store</code> 目录， 在 <code>conf</code> 目录下创建 <code>broker.conf</code> 文件，现在 <code>ROCKETMQ_HOME</code> 目录结构如下</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">D:\data\rocketmq<br>.<br>├── conf<br>│   ├── broker.conf<br>└── store<br></code></pre></td></tr></table></figure>
<p><code>broker.conf</code> 文件内容如下</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs conf"># 集群名称<br>brokerClusterName = DefaultCluster<br># broker 名称<br>brokerName = broker-a<br># brokerId=0表示master，其他值表示 slave<br>brokerId = 0<br># 与fileReservedTime参数呼应，表明在几点做消息删除动作，默认值04表示凌晨4点<br>deleteWhen = 04<br># 在磁盘上保存消息的时长，单位是小时，自动删除超时的消息<br>fileReservedTime = 48<br># brokerRole有3种：SYNC_MASTER、ASYNC_MASTER、SLAVE。关键词SYNC和ASYNC表示Master和Slave<br># 之间同步消息的机制，SYNC的意思是当Slave和Master消息同步完成后，再返回发送成功的状态<br>brokerRole = ASYNC_MASTER<br># flushDiskType表示刷盘策略，分为SYNC_FLUSH和ASYNC_FLUSH两种，<br># 分别代表同步刷盘和异步刷盘。同步刷盘情况下，消息真正写入磁盘后再返回成功状态；<br># 异步刷盘情况下，消息写入page_cache后就返回成功状态<br>flushDiskType = ASYNC_FLUSH<br><br>namesrvAddr = 127.0.0.1:9876<br>storePathRootDir = D:\\data\\rocketmq\\store<br>storePathCommitLog = D:\\data\\rocketmq\\store\\commitlog<br>storePathConsumeQueue = D:\\data\\rocketmq\\store\\consumequeue<br>storePathIndex = D:\\data\\rocketmq\\store\\index<br>storeCheckpoint = D:\\data\\rocketmq\\store\\checkpoint<br></code></pre></td></tr></table></figure>

<h3 id="源码启动-nameserver"><a href="#源码启动-nameserver" class="headerlink" title="源码启动 nameserver"></a>源码启动 nameserver</h3><p>最终是要找到 <code>rocketmq-namesrv</code> 子模块下的 <code>org.apache.rocketmq.namesrv.NamesrvStartup</code>， 执行这个类的 <code>main</code> 方法，不过还需要一些配置<br>在 <code>idea</code> 中配置 <code>NamesrvStartup</code> 启动类的 <code>ROCKETMQ_HOME</code> 环境变量，如下图所示  <code>ROCKETMQ_HOME=D:\\data\\rocketmq</code><br><img src="https://s2.loli.net/2023/12/20/8NsrKgcuALHDBbf.png" alt="Namesrv配置"></p>
<h3 id="源码启动-Broker"><a href="#源码启动-Broker" class="headerlink" title="源码启动 Broker"></a>源码启动 Broker</h3><p>最终是要找到 <code>rocketmq-broker</code> 子模块下的 <code>org.apache.rocketmq.broker.BrokerStartup</code> 类的 <code>main</code> 方法，同样也是需要设置一些参数，如下图所示<br><img src="https://s2.loli.net/2023/12/20/G3Ql5mBXafALdIt.png" alt="Broker配置"></p>
<p><code>program arguments</code>：<code>-c D:\\data\\rocketmq\\conf\\broker.conf</code><br><code>Environment variables</code>: <code>ROCKETMQ_HOME=D:\\data\\rocketmq</code></p>
<h2 id="通过-RocketMQ4源码启动-Nameserver-和-broker"><a href="#通过-RocketMQ4源码启动-Nameserver-和-broker" class="headerlink" title="通过 RocketMQ4源码启动 Nameserver 和 broker"></a>通过 RocketMQ4源码启动 Nameserver 和 broker</h2><p><code>RocketMQ4</code> 版本也是和 5 版本一样需要创建 <code>ROCKETMQ_HOME</code> 目录，只是在 <code>conf</code> 目录下会多两个文件，分别是</p>
<ul>
<li><code>logback_broker.xml</code></li>
<li><code>logback_namesrv.xml</code></li>
</ul>
<p>这两个文件可以从 <code>rocketmq-distribution</code> 模块下的 <code>conf</code> 目录中复制过来， 然后将这两个文件中的 <code>$&#123;user.home&#125;</code> 修改成对应的 <code>ROCKETMQ_HOME</code> 地址就好了，在上面示例中 <code>ROCKETMQ_HOME=D:\data\rocketmq</code></p>
]]></content>
      <categories>
        <category>中间件</category>
        <category>RocketMQ</category>
      </categories>
      <tags>
        <tag>RocketMQ</tag>
      </tags>
  </entry>
  <entry>
    <title>RocketMQ整合SpringBoot3</title>
    <url>/2023/1a12a33976fc/index.html</url>
    <content><![CDATA[<p>SpringBoot版本： 3.2.0</p>
<p><a href="https://github.com/apache/rocketmq-spring/wiki">官方文档</a></p>
<blockquote>
<p>SpringBoot3 版本直接使用示例会提示 RocketMQTemplate 注入失败，解决方式在文末</p>
</blockquote>
<p>使用 <code>idea</code> 创建 <code>SpringBoot</code> 项目, 项目名称是 <code>rocket-spring-boot</code></p>
<figure class="highlight mipsasm"><table><tr><td class="code"><pre><code class="hljs mipsasm">rocket-spring-<span class="hljs-keyword">boot</span><br><span class="hljs-keyword"></span>├─src<br>│  ├─main<br>│  │  ├─<span class="hljs-keyword">java</span><br><span class="hljs-keyword"></span>│  │  │  └─<span class="hljs-keyword">org</span><br><span class="hljs-keyword"></span>│  │  │      └─example<br>│  │  │          └─rocketmqspringboot<br>│  │  │              └─RocketmqSpringBootApplication.<span class="hljs-keyword">java</span><br><span class="hljs-keyword"></span>│  │  └─resources<br>│  │      └─application.properties<br>└─pom.xml<br></code></pre></td></tr></table></figure>

<p>主要有三个文件</p>
<ul>
<li><code>pom.xml</code> 依赖文件</li>
</ul>
<figure class="highlight xml"><table><tr><td class="code"><pre><code class="hljs xml"><span class="hljs-meta">&lt;?xml version=<span class="hljs-string">&quot;1.0&quot;</span> encoding=<span class="hljs-string">&quot;UTF-8&quot;</span>?&gt;</span><br><span class="hljs-tag">&lt;<span class="hljs-name">project</span> <span class="hljs-attr">xmlns</span>=<span class="hljs-string">&quot;http://maven.apache.org/POM/4.0.0&quot;</span> <span class="hljs-attr">xmlns:xsi</span>=<span class="hljs-string">&quot;http://www.w3.org/2001/XMLSchema-instance&quot;</span></span><br><span class="hljs-tag">         <span class="hljs-attr">xsi:schemaLocation</span>=<span class="hljs-string">&quot;http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd&quot;</span>&gt;</span><br>    <span class="hljs-tag">&lt;<span class="hljs-name">modelVersion</span>&gt;</span>4.0.0<span class="hljs-tag">&lt;/<span class="hljs-name">modelVersion</span>&gt;</span><br>    <span class="hljs-tag">&lt;<span class="hljs-name">parent</span>&gt;</span><br>        <span class="hljs-tag">&lt;<span class="hljs-name">groupId</span>&gt;</span>org.springframework.boot<span class="hljs-tag">&lt;/<span class="hljs-name">groupId</span>&gt;</span><br>        <span class="hljs-tag">&lt;<span class="hljs-name">artifactId</span>&gt;</span>spring-boot-starter-parent<span class="hljs-tag">&lt;/<span class="hljs-name">artifactId</span>&gt;</span><br>        <span class="hljs-tag">&lt;<span class="hljs-name">version</span>&gt;</span>3.2.0<span class="hljs-tag">&lt;/<span class="hljs-name">version</span>&gt;</span><br>        <span class="hljs-tag">&lt;<span class="hljs-name">relativePath</span>/&gt;</span> <span class="hljs-comment">&lt;!-- lookup parent from repository --&gt;</span><br>    <span class="hljs-tag">&lt;/<span class="hljs-name">parent</span>&gt;</span><br>    <span class="hljs-tag">&lt;<span class="hljs-name">groupId</span>&gt;</span>org.example<span class="hljs-tag">&lt;/<span class="hljs-name">groupId</span>&gt;</span><br>    <span class="hljs-tag">&lt;<span class="hljs-name">artifactId</span>&gt;</span>rocketmq-spring-boot<span class="hljs-tag">&lt;/<span class="hljs-name">artifactId</span>&gt;</span><br>    <span class="hljs-tag">&lt;<span class="hljs-name">version</span>&gt;</span>0.0.1-SNAPSHOT<span class="hljs-tag">&lt;/<span class="hljs-name">version</span>&gt;</span><br>    <span class="hljs-tag">&lt;<span class="hljs-name">name</span>&gt;</span>rocketmq-spring-boot<span class="hljs-tag">&lt;/<span class="hljs-name">name</span>&gt;</span><br>    <span class="hljs-tag">&lt;<span class="hljs-name">description</span>&gt;</span>rocketmq-spring-boot<span class="hljs-tag">&lt;/<span class="hljs-name">description</span>&gt;</span><br>    <span class="hljs-tag">&lt;<span class="hljs-name">properties</span>&gt;</span><br>        <span class="hljs-tag">&lt;<span class="hljs-name">java.version</span>&gt;</span>17<span class="hljs-tag">&lt;/<span class="hljs-name">java.version</span>&gt;</span><br>    <span class="hljs-tag">&lt;/<span class="hljs-name">properties</span>&gt;</span><br>    <span class="hljs-tag">&lt;<span class="hljs-name">dependencies</span>&gt;</span><br>        <span class="hljs-tag">&lt;<span class="hljs-name">dependency</span>&gt;</span><br>            <span class="hljs-tag">&lt;<span class="hljs-name">groupId</span>&gt;</span>org.springframework.boot<span class="hljs-tag">&lt;/<span class="hljs-name">groupId</span>&gt;</span><br>            <span class="hljs-tag">&lt;<span class="hljs-name">artifactId</span>&gt;</span>spring-boot-starter<span class="hljs-tag">&lt;/<span class="hljs-name">artifactId</span>&gt;</span><br>        <span class="hljs-tag">&lt;/<span class="hljs-name">dependency</span>&gt;</span><br><br>        <span class="hljs-comment">&lt;!-- 这里添加 rocketmq-spring-boot-starter 依赖 --&gt;</span><br>        <span class="hljs-tag">&lt;<span class="hljs-name">dependency</span>&gt;</span><br>            <span class="hljs-tag">&lt;<span class="hljs-name">groupId</span>&gt;</span>org.apache.rocketmq<span class="hljs-tag">&lt;/<span class="hljs-name">groupId</span>&gt;</span><br>            <span class="hljs-tag">&lt;<span class="hljs-name">artifactId</span>&gt;</span>rocketmq-spring-boot-starter<span class="hljs-tag">&lt;/<span class="hljs-name">artifactId</span>&gt;</span><br>            <span class="hljs-tag">&lt;<span class="hljs-name">version</span>&gt;</span>2.2.3<span class="hljs-tag">&lt;/<span class="hljs-name">version</span>&gt;</span><br>        <span class="hljs-tag">&lt;/<span class="hljs-name">dependency</span>&gt;</span><br><br>        <span class="hljs-tag">&lt;<span class="hljs-name">dependency</span>&gt;</span><br>            <span class="hljs-tag">&lt;<span class="hljs-name">groupId</span>&gt;</span>org.springframework.boot<span class="hljs-tag">&lt;/<span class="hljs-name">groupId</span>&gt;</span><br>            <span class="hljs-tag">&lt;<span class="hljs-name">artifactId</span>&gt;</span>spring-boot-starter-test<span class="hljs-tag">&lt;/<span class="hljs-name">artifactId</span>&gt;</span><br>            <span class="hljs-tag">&lt;<span class="hljs-name">scope</span>&gt;</span>test<span class="hljs-tag">&lt;/<span class="hljs-name">scope</span>&gt;</span><br>        <span class="hljs-tag">&lt;/<span class="hljs-name">dependency</span>&gt;</span><br>        <span class="hljs-tag">&lt;<span class="hljs-name">dependency</span>&gt;</span><br>            <span class="hljs-tag">&lt;<span class="hljs-name">groupId</span>&gt;</span>org.projectlombok<span class="hljs-tag">&lt;/<span class="hljs-name">groupId</span>&gt;</span><br>            <span class="hljs-tag">&lt;<span class="hljs-name">artifactId</span>&gt;</span>lombok<span class="hljs-tag">&lt;/<span class="hljs-name">artifactId</span>&gt;</span><br>        <span class="hljs-tag">&lt;/<span class="hljs-name">dependency</span>&gt;</span><br>    <span class="hljs-tag">&lt;/<span class="hljs-name">dependencies</span>&gt;</span><br><br>    <span class="hljs-tag">&lt;<span class="hljs-name">build</span>&gt;</span><br>        <span class="hljs-tag">&lt;<span class="hljs-name">plugins</span>&gt;</span><br>            <span class="hljs-tag">&lt;<span class="hljs-name">plugin</span>&gt;</span><br>                <span class="hljs-tag">&lt;<span class="hljs-name">groupId</span>&gt;</span>org.springframework.boot<span class="hljs-tag">&lt;/<span class="hljs-name">groupId</span>&gt;</span><br>                <span class="hljs-tag">&lt;<span class="hljs-name">artifactId</span>&gt;</span>spring-boot-maven-plugin<span class="hljs-tag">&lt;/<span class="hljs-name">artifactId</span>&gt;</span><br>            <span class="hljs-tag">&lt;/<span class="hljs-name">plugin</span>&gt;</span><br>        <span class="hljs-tag">&lt;/<span class="hljs-name">plugins</span>&gt;</span><br>    <span class="hljs-tag">&lt;/<span class="hljs-name">build</span>&gt;</span><br><br><span class="hljs-tag">&lt;/<span class="hljs-name">project</span>&gt;</span><br></code></pre></td></tr></table></figure>
<ul>
<li><p><code>RocketmqSpringBootApplication.java</code>, 默认只有 <code>main</code> 方法，这里按照官方文档示例，实现 <code>CommandLineRunner</code> 接口，在项目启动之后就进行消息的发送</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-meta">@SpringBootApplication</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">RocketmqSpringBootApplication</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">CommandLineRunner</span> &#123;<br><br>    <span class="hljs-meta">@Autowired</span><br>    <span class="hljs-keyword">private</span> RocketMQTemplate rocketMQTemplate;<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">main</span><span class="hljs-params">(String[] args)</span> &#123;<br>        SpringApplication.run(RocketmqSpringBootApplication.class, args);<br>    &#125;<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">run</span><span class="hljs-params">(String... args)</span> <span class="hljs-keyword">throws</span> Exception &#123;<br>        <span class="hljs-comment">//send message synchronously</span><br>        rocketMQTemplate.convertAndSend(<span class="hljs-string">&quot;test-topic-1&quot;</span>, <span class="hljs-string">&quot;Hello, World!&quot;</span>);<br>        <span class="hljs-comment">//send spring message</span><br>        rocketMQTemplate.send(<span class="hljs-string">&quot;test-topic-1&quot;</span>, MessageBuilder.withPayload(<span class="hljs-string">&quot;Hello, World! I&#x27;m from spring message&quot;</span>).build());<br>        <span class="hljs-comment">//send messgae asynchronously</span><br>        rocketMQTemplate.asyncSend(<span class="hljs-string">&quot;test-topic-2&quot;</span>, <span class="hljs-keyword">new</span> <span class="hljs-title class_">OrderPaidEvent</span>(<span class="hljs-string">&quot;T_001&quot;</span>, <span class="hljs-keyword">new</span> <span class="hljs-title class_">BigDecimal</span>(<span class="hljs-string">&quot;88.00&quot;</span>)), <span class="hljs-keyword">new</span> <span class="hljs-title class_">SendCallback</span>() &#123;<br>            <span class="hljs-meta">@Override</span><br>            <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">onSuccess</span><span class="hljs-params">(SendResult var1)</span> &#123;<br>                System.out.printf(<span class="hljs-string">&quot;async onSucess SendResult=%s %n&quot;</span>, var1);<br>            &#125;<br><br>            <span class="hljs-meta">@Override</span><br>            <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">onException</span><span class="hljs-params">(Throwable var1)</span> &#123;<br>                System.out.printf(<span class="hljs-string">&quot;async onException Throwable=%s %n&quot;</span>, var1);<br>            &#125;<br><br>        &#125;);<br>        <span class="hljs-comment">//Send messages orderly</span><br>        rocketMQTemplate.syncSendOrderly(<span class="hljs-string">&quot;orderly_topic&quot;</span>, MessageBuilder.withPayload(<span class="hljs-string">&quot;Hello, World&quot;</span>).build(),<span class="hljs-string">&quot;hashkey&quot;</span>);<br><br>        <span class="hljs-comment">//rocketMQTemplate.destroy(); // notes:  once rocketMQTemplate be destroyed, you can not send any message again with this rocketMQTemplate</span><br>    &#125;<br><br>    <span class="hljs-meta">@Data</span><br>    <span class="hljs-meta">@AllArgsConstructor</span><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">OrderPaidEvent</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">Serializable</span> &#123;<br>        <span class="hljs-keyword">private</span> String orderId;<br><br>        <span class="hljs-keyword">private</span> BigDecimal paidMoney;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>
</li>
<li><p>添加 <code>application.properties</code> 配置文件</p>
<figure class="highlight properties"><table><tr><td class="code"><pre><code class="hljs properties"><span class="hljs-attr">rocketmq.name-server</span>=<span class="hljs-string">192.168.119.129:9876</span><br><span class="hljs-attr">rocketmq.producer.group</span>=<span class="hljs-string">my-group</span><br></code></pre></td></tr></table></figure></li>
</ul>
<h2 id="解决-SpringBoot3-中-RocketMQTemplate-注入失败问题"><a href="#解决-SpringBoot3-中-RocketMQTemplate-注入失败问题" class="headerlink" title="解决 SpringBoot3 中 RocketMQTemplate 注入失败问题"></a>解决 SpringBoot3 中 RocketMQTemplate 注入失败问题</h2><p><code>SpringBoot3</code> 自动装配文件不是使用 <code>spring.factories</code> 文件，而是需要在 <code>META-INF/spring</code> 目录下创建 <code>org.springframework.boot.autoconfigure.AutoConfiguration.imports</code> 文件，然后将需要自动装配的类全名放入这个文件，一个类一行即可， 所以需要在 <code>resources</code> 目录下创建 <code>META-INF/spring</code> 目录，然后创建 <code>org.springframework.boot.autoconfigure.AutoConfiguration.imports</code> 文件，内容如下</p>
<figure class="highlight stylus"><table><tr><td class="code"><pre><code class="hljs stylus">org<span class="hljs-selector-class">.apache</span><span class="hljs-selector-class">.rocketmq</span><span class="hljs-selector-class">.spring</span><span class="hljs-selector-class">.autoconfigure</span>.RocketMQAutoConfiguration<br></code></pre></td></tr></table></figure>
<p>至此就可以按照示例使用了， 直接启动 <code>SpringBoot</code> 项目就可以看到消息发送成功的日志了</p>
]]></content>
      <categories>
        <category>中间件</category>
        <category>RocketMQ</category>
      </categories>
      <tags>
        <tag>RocketMQ</tag>
      </tags>
  </entry>
  <entry>
    <title>RocketMQ消息存储机制</title>
    <url>/2023/c3210ca00e5d/index.html</url>
    <content><![CDATA[<span class="hide_first_mermaid">
<div class="mermaid-wrap"><pre class="mermaid-src" hidden>
  flowchart LR
    A
  </pre></div>
</span>

<blockquote>
<p>RocketMQ版本： 4.9.7<br>JDK版本： 17</p>
</blockquote>
<h2 id="存储相关基础知识"><a href="#存储相关基础知识" class="headerlink" title="存储相关基础知识"></a>存储相关基础知识</h2><ul>
<li><code>RocketMQ</code> 主要存储的文件包括 <code>Comitlog</code> 文件、<code>ConsumeQueue</code>文件、<code>IndexFile</code>文件</li>
<li><code>RocketMQ</code> 将所有主题的消息存储在同一个文件中，即一个 <code>Commitlog</code> 文件，<strong>这么做的优势是确保消息发送时顺序写文件，提高消息发送的性能和吞吐量</strong>, <code>Commitlog</code> 文件存储目录默认为 <code>$&#123;ROCKET_HOME&#125;/store/commitlog</code> 目录下</li>
<li><code>ConsumerQueue</code> 则是为了提高消费者的消费能力而设计的一个文件，<code>Commitlog</code> 包含所有 <code>Topic</code> 的消息，但是消费者是基于某个 <code>Topic</code> 的订阅机制，所以就增加了 <code>ConsumerQueue</code> 消息队列文件，消息存储在 <code>Commitlog</code> 文件后，将<strong>异步</strong>转发到消息消费队列</li>
<li><code>IndexFile</code> 索引文件则是为了加速消息的检索性能，根据消息的属性快速从 <code>Commitlog</code> 文件夹中检索消息</li>
<li><code>$&#123;ROCKET_HOME&#125;/store/config</code> 目录主要存储运行期间的配置信息<ul>
<li><code>consumerFilter.json</code>：主题消息过滤信息</li>
<li><code>consumerOffset.json</code>：集群消费模式消息消费进度</li>
<li><code>delayOffset.json</code>：延时消息队列拉取进度</li>
<li><code>subscriptionGroup.json</code>：消息消费组配置信息</li>
<li><code>topics.json</code>: <code>topic</code> 配置属性</li>
</ul>
</li>
<li><code>$&#123;ROCKET_HOME&#125;/store/abort</code> ： 如果存在 <code>abort</code> 文件说明 <code>Broker</code> 非正常关闭，该文件在默认启动时创建，正常退出之前删除</li>
<li><code>$&#123;ROCKET_HOME&#125;/store/checkpoint</code>: 文件检测点，存储<code>commitlog</code>文件最后一次刷盘时间戳、<code>consumequeue</code>最后一次刷盘时间、<code>index</code>索引文件最后一次刷盘时间戳</li>
<li>存储目录相关的参数可以通过 <code>broker.conf</code> 进行配置，示例如下</li>
</ul>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs conf">storePathRootDir = E:\\config\\rocketmq\\store<br>storePathCommitLog = E:\\config\\rocketmq\\store\\commitlog<br>storePathConsumeQueue = E:\\config\\rocketmq\\store\\consumequeue<br>storePathIndex = E:\\config\\rocketmq\\store\\index<br>storeCheckpoint = E:\\config\\rocketmq\\store\\checkpoint<br></code></pre></td></tr></table></figure>

<p><strong>事务状态服务</strong><br>存储每条消息的事务状态</p>
<p><strong>定时消息服务</strong><br>每一个延迟级别对应一个消息消费队列，存储延迟队列的消息拉取进度</p>
<h2 id="Commitlog-文件"><a href="#Commitlog-文件" class="headerlink" title="Commitlog 文件"></a>Commitlog 文件</h2><p><code>Commitlog</code> 目录下的文件主要用于存储消息，每一条信息的长度都不相同，但是每一条消息的长度都会使用 4 个字节存储下来，逻辑图如下</p>
<pre class="mermaid">graph LR
    subgraph A["TopicA:Msg1"]
        A1["TotalSize(4字节存储)"]
        A2["消息内容"]
    end
    subgraph B["TopicB:Msg2"]
        B1["TotalSize(4字节存储)"]
        B2["消息内容"]
    end
    subgraph C["TopicA:Msg3"]
        C1["TotalSize(4字节存储)"]
        C2["消息内容"]
    end
    A-->B-->C</pre>

<h2 id="ConsumerQueue-文件"><a href="#ConsumerQueue-文件" class="headerlink" title="ConsumerQueue 文件"></a>ConsumerQueue 文件</h2><ul>
<li><code>Commitlog</code> 文件是此时的所有消息的内容，<code>ConsumerQueue</code> 文件可以认为是按照 <code>Topic</code> 对 <code>Commitlog</code> 进行分类和索引(因为 <code>ConsumerQueue</code> 并不会存储完整的消息内容，只是会存储消息在 <code>Commitlog</code> 文件中的偏移量，想要获取完整的消息还是需要到 <code>Commitlog</code>文件中获取)</li>
</ul>
<p><img src="https://blog-1308475102.cos.ap-guangzhou.myqcloud.com/picture/202312252303487.webp" alt="ConsumerQueue文件"></p>
<ul>
<li><code>ConsumerQueue</code> 文件存储的内容也是结构性的，该文件中的每一条数据称为 <code>ConsumerQueue</code> 条目，每一个 <code>ConsumerQueue</code> 条目的结构如下<br><img src="https://blog-1308475102.cos.ap-guangzhou.myqcloud.com/picture/202312252308083.webp" alt="ConsumerQueue条目结构"></li>
<li>单个 <code>ConsumerQueue</code> 文件默认包含 30万个条目，单个文件的大小就是 30w * 20字节</li>
<li>因为 <code>ConsumerQueue</code> 条目大小是固定的(8+4+8 字节)，所以可以将 <code>ConsumerQueue</code> 文件看成是 <code>ConsumerQueue</code> 条目的数组，下标就是 <code>ConsumerQueue</code> 的逻辑偏移量，消息消费进度存储的偏移量也是逻辑偏移量</li>
<li><code>ConsumerQueue</code> 文件新增的时机是当消息到达 <code>Commitlog</code> 文件后，会有专门发线程产生消息转发任务，此时会添加数据到 <code>ConsumerQueue</code> 中</li>
</ul>
<h2 id="Index-索引文件"><a href="#Index-索引文件" class="headerlink" title="Index 索引文件"></a>Index 索引文件</h2><p>索引文件的目的是为了方便根据 <code>MessageKey</code> 来查询消息，看一下下面的 <code>Producer</code> 发送消息的代码</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">main</span><span class="hljs-params">(String[] args)</span> <span class="hljs-keyword">throws</span> MQClientException, InterruptedException &#123;<br>    <span class="hljs-type">DefaultMQProducer</span> <span class="hljs-variable">producer</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">DefaultMQProducer</span>(<span class="hljs-string">&quot;ProducerGroupName&quot;</span>);<br>    producer.setNamesrvAddr(<span class="hljs-string">&quot;127.0.0.1:9876&quot;</span>);<br>    producer.start();<br>    <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> <span class="hljs-variable">i</span> <span class="hljs-operator">=</span> <span class="hljs-number">0</span>; i &lt; <span class="hljs-number">5</span>; i++) &#123;<br>        <span class="hljs-keyword">try</span> &#123;<br>            &#123;<br>                <span class="hljs-type">Message</span> <span class="hljs-variable">msg</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Message</span>(<span class="hljs-string">&quot;TopicTest&quot;</span>,<br>                        <span class="hljs-string">&quot;TagA&quot;</span>,<br>                        <span class="hljs-string">&quot;OrderID188&quot;</span>, <span class="hljs-comment">// 这个参数就是 keys</span><br>                        <span class="hljs-string">&quot;Hello world&quot;</span>.getBytes(RemotingHelper.DEFAULT_CHARSET));<br>                <span class="hljs-type">SendResult</span> <span class="hljs-variable">sendResult</span> <span class="hljs-operator">=</span> producer.send(msg);<br>                System.out.printf(<span class="hljs-string">&quot;%s%n&quot;</span>, sendResult);<br>            &#125;<br>        &#125; <span class="hljs-keyword">catch</span> (Exception e) &#123;<br>            e.printStackTrace();<br>        &#125;<br>    &#125;<br>    producer.shutdown();<br>&#125;<br></code></pre></td></tr></table></figure>
<p>下面截图就是在 <code>RokcetMQ-dashboard</code> 中按照 <code>Keys</code> 来查询消息<br><img src="https://s2.loli.net/2023/12/26/CjOyVWS38BvEmhL.png" alt="根据Key查询消息"></p>
<h3 id="索引文件结构"><a href="#索引文件结构" class="headerlink" title="索引文件结构"></a>索引文件结构</h3><p>接下来就看一下索引文件结果，下图来自 《RocketMQ技术内幕:RocketMQ架构设计与实现原理》 一书<br><img src="https://s2.loli.net/2023/12/26/ign4Iwm8Lv3ftlZ.png" alt="IndexFile结构"></p>
<p>整体看 <code>Index</code> 索引文件包含三个部分</p>
<ul>
<li><code>head</code>： 文件头</li>
<li><code>slot</code>： 哈希槽</li>
<li><code>index content</code>： 索引内容</li>
</ul>
<h4 id="文件头-40字节"><a href="#文件头-40字节" class="headerlink" title="文件头(40字节)"></a>文件头(40字节)</h4><p>文件头一共包含 40 字节，从前往后代表的含义如下</p>
<ol>
<li>开始时间( <code>beginTimestamp</code> ), 占用 8 字节</li>
<li>结束时间( <code>endTimestamp</code> ), 占用 8 字节</li>
<li>消息最小物理偏移量( <code>beginPhyoffset</code> ), 占用 8 字节，最小物理偏移量在往索引文件添加第一个条目的时候就可以确定，因为消息是顺序添加的，第一个添加的消息就是物理偏移量最小的</li>
<li>消息最大物理偏移量( <code>endPhyoffset</code> ), 占用 8 字节，最大物理偏移量是随着往索引文件不断添加条目而递增的</li>
<li>有效 <code>hash slot count</code>, 占用 4 字节，最坏的情况下有效的哈希槽只有一个（所有消息的 <code>Keys</code> 字段都相同，导致 <code>key</code> 的 <code>hash</code> 冲突），最理想的情况下每个 索引文件可以包含 500w 个 <code>hash slot</code></li>
<li><code>index</code> 索引数量，占用 4 字节，默认一个索引文件可以包含 2000万个 <code>index content</code>, <code>index count</code> 就是记录的已经使用的 <code>index content</code> 数量</li>
</ol>
<h4 id="slot"><a href="#slot" class="headerlink" title="slot"></a>slot</h4><ul>
<li><code>slot</code> 区域默认可以存放 500万个，可以认为是长度为 500w 的数组</li>
<li><code>slot</code> 中存放的是当前 <code>index content</code> 的位置，这个位置是逻辑偏移量，为什么可以记录逻辑偏移量呢？先给出计算公式再进行说明</li>
</ul>
<figure class="highlight axapta"><table><tr><td class="code"><pre><code class="hljs axapta">当前 slot 记录的 <span class="hljs-keyword">index</span> 实际位置 = 头部字节长度(<span class="hljs-number">40</span>) + (hash slot数量(默认<span class="hljs-number">500</span>w)) * 单个hash slot大小(默认<span class="hljs-number">4</span>字节) + 当前slot存储的<span class="hljs-keyword">index</span>数量 * 单个<span class="hljs-keyword">index</span> content大小(默认<span class="hljs-number">20</span>字节)<br></code></pre></td></tr></table></figure>
<p>上面公式可以成立的原因就是 <strong>头部长度固定</strong>， <strong>slot区域总长度也固定</strong>，<strong>每一个index content长度固定</strong>，所以就可以根据索引下标来计算实际位置</p>
<h5 id="如何根据key确定slot位置"><a href="#如何根据key确定slot位置" class="headerlink" title="如何根据key确定slot位置"></a>如何根据key确定slot位置</h5><ol>
<li>根据 <code>Message</code> 中的 <code>Keys</code> 计算 <code>hashcode</code></li>
<li>使用 <code>hashcode</code> 对 <code>slot</code> 总数取模</li>
</ol>
<h5 id="keys-hash-冲突如何解决"><a href="#keys-hash-冲突如何解决" class="headerlink" title="keys hash 冲突如何解决"></a>keys hash 冲突如何解决</h5><p>在如下两种情况下都可能存在 <code>keys</code> 的哈希冲突</p>
<ol>
<li>不同消息的 <code>Keys</code> 字段相同，此时计算出来的 <code>hashcode</code> 相同</li>
<li>根据 <code>keys</code> 计算出来的 <code>hashcode</code> 不同，但是对 <code>slot</code> 长度取模后得出的结果相同</li>
</ol>
<p>当出现这种冲突时，<code>slot</code> 中存放的是最后一次 <code>index content</code> 的位置？也就是说存放的之前的 <code>index content</code> 位置被覆盖了，为什么可以这样呢？这是因为 <code>index content</code> 中会有字段来存储之前的 <code>index content</code> 的位置</p>
<h4 id="index-content-一共20字节"><a href="#index-content-一共20字节" class="headerlink" title="index content (一共20字节)"></a>index content (一共20字节)</h4><ul>
<li><code>hashcode</code> 值： 4字节</li>
<li>消息的物理地址(<code>phyoffset</code>): 8字节，消息的物理地址指向的是 <code>Commitlog</code> 文件中的偏移量</li>
<li>当前消息与最早消息的时间差（<code>timedif</code>）: 4字节，这个是为了时间的范围查询</li>
<li>上一条 <code>index content</code> 的位置（<code>pre index no</code>）:4字节，前面讲 <code>slot</code> 冲突时，<code>slot</code> 中记录的是最后一个 <code>index content</code> 的位置，然后再由这个 <code>index content</code> 指向之前那个 <code>index conent</code></li>
</ul>
<p>下面以两次往 <code>Index</code> 索引文件添加内容为例，看一下当 <code>key</code> 相同时是如何处理冲突的<br><strong>第一次</strong><br>假设根据 <code>keys</code> 计算出来的 <code>slot</code> 下标是5，因为是第一次添加，所以 <code>index content</code> 就放在第一个位置(<code>index content</code> 和 <code>slot</code> 不同，<code>slot</code> 需要计算下标位置，<code>index content</code> 直接依次递增存放)，此时结构图如下所示<br><img src="https://s2.loli.net/2023/12/26/mWP2QeVNpgoGBEy.png" alt="第一次添加索引文件内容"></p>
<p><strong>第二次</strong><br>第二个发送 <code>Message</code> 时还是使用相同的 <code>keys</code>, 那么计算出来的 <code>slot</code> 位置肯定还是 5<br><img src="https://s2.loli.net/2023/12/26/jNigTfp7ECZVosS.png" alt="第二次发送"><br>首先，<code>slot</code> 区域中下标为5的位置存储的内容改变了，变成当前新增的 <code>index content</code> 的逻辑偏移量(即 <code>index content</code> 区域的下标)<br>其次，当前 <code>index content</code> 中的 <code>pre index no</code> 区域中会存储之前的 <code>index content</code> 的逻辑偏移量(因为有哈希冲突了，如果没有冲突就不用存储)</p>
<h2 id="MappedFile-和-MappedFileQueue"><a href="#MappedFile-和-MappedFileQueue" class="headerlink" title="MappedFile 和 MappedFileQueue"></a>MappedFile 和 MappedFileQueue</h2><ul>
<li><code>Commitlog</code> 文件存储目录为 <code>$&#123;ROCKET_HOME&#125;/store/commitlog</code>，每一个文件默认都是 1G, 如下图所示<br><img src="https://blog-1308475102.cos.ap-guangzhou.myqcloud.com/picture/202312252113697.webp" alt="Commitlog文件"></li>
<li><code>Commitlog</code> 文件名就是文件中第一条消息的物理量偏移量</li>
<li><code>MappedFile</code> 就是对应这一个个的 <code>Commitlog</code> 文件，<strong>是内存映射文件的具体实现</strong></li>
<li><code>MappedFileQueue</code> 是 <code>MappedFile</code>的管理容器，对应 <code>$&#123;ROCKET_HOME&#125;/store/commitlog</code> 文件夹</li>
</ul>
]]></content>
      <categories>
        <category>中间件</category>
        <category>RocketMQ</category>
      </categories>
      <tags>
        <tag>RocketMQ</tag>
      </tags>
  </entry>
  <entry>
    <title>使用idea编译RocketMQ5源码</title>
    <url>/2023/2adf4cc6108b/index.html</url>
    <content><![CDATA[<blockquote>
<p>Jdk版本： 17<br>RocketMQ版本： 5.1.4<br>idea版本： 2023.2</p>
</blockquote>
<p>参考 <ol><li><a href="/2023/49fb249febb6/index.html" title="RocketMQ安装使用和基本概念">RocketMQ安装使用和基本概念</a></li></ol></p>
<p>直接在 <code>idea</code> 导入 <code>RocketMQ5</code> 源码并编译会类似下面的错误</p>
<figure class="highlight stylus"><table><tr><td class="code"><pre><code class="hljs stylus">java: 程序包sun<span class="hljs-selector-class">.nio</span><span class="hljs-selector-class">.ch</span> 不存在<br></code></pre></td></tr></table></figure>

<p>这是因为使用 <code>jdk8</code> 以上的版本进行编译，8 以上的 <code>jdk</code> 开始使用模块化，所以有些模块需要自己导入，解决方式如下</p>
<ul>
<li>打开 <code>settings -&gt; Build,Execution,Deployment -&gt; Compile -&gt; java compile</code></li>
</ul>
<p><img src="https://s2.loli.net/2023/12/19/9z8Tkuhes7FR1wy.png" alt="添加编译参数"></p>
<p>给 <code>rocketmq-common</code> 和 <code>rocketmq-store</code> 模块添加如下参数</p>
<figure class="highlight csharp"><table><tr><td class="code"><pre><code class="hljs csharp">--<span class="hljs-keyword">add</span>-exports java.<span class="hljs-keyword">base</span>/sun.nio.ch=ALL-UNNAMED<br></code></pre></td></tr></table></figure>

<p>给 <code>rocketmq-test</code> 模块添加如下参数</p>
<figure class="highlight csharp"><table><tr><td class="code"><pre><code class="hljs csharp">--<span class="hljs-keyword">add</span>-exports java.<span class="hljs-keyword">base</span>/sun.util.locale=ALL-UNNAMED<br></code></pre></td></tr></table></figure>
<p>然后重新编译即可</p>
<p>Tips: 为什么要去掉 <code>use --release</code> 的选项呢？<br>使用 <code>javac --help</code> 看看支持的参数说明</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">javac --<span class="hljs-built_in">help</span><br>--release &lt;release&gt;<br>    Compile <span class="hljs-keyword">for</span> a specific VM version. Supported targets: 6, 7, 8, 9, 10, 11<br>-<span class="hljs-built_in">source</span> &lt;release&gt;<br>    Provide <span class="hljs-built_in">source</span> compatibility with specified release<br>-target &lt;release&gt;            Generate class files <span class="hljs-keyword">for</span> specific VM version<br></code></pre></td></tr></table></figure>
<p>所以 <code>--release</code> 主要是为了向下兼容，比如使用 <code>jdk13</code> 开发，但是运行环境使用的是 <code>jdk11</code>, 使用 <code>--release</code> 编译就不会有问题</p>
<p>不过 <code>--release</code> 和 添加的 编译参数有冲突，所以就先取消了 <code>--release</code> 选项</p>
]]></content>
      <categories>
        <category>中间件</category>
        <category>RocketMQ</category>
      </categories>
      <tags>
        <tag>RocketMQ</tag>
      </tags>
  </entry>
  <entry>
    <title>SpringBoot中使用Redis</title>
    <url>/2023/5ef02c37afab/index.html</url>
    <content><![CDATA[<p>版本说明：</p>
<ol>
<li>SpringBoot 3.x</li>
</ol>
<h2 id="引入依赖"><a href="#引入依赖" class="headerlink" title="引入依赖"></a>引入依赖</h2><figure class="highlight xml"><table><tr><td class="code"><pre><code class="hljs xml"><span class="hljs-comment">&lt;!-- springboot 版本 --&gt;</span><br><span class="hljs-tag">&lt;<span class="hljs-name">parent</span>&gt;</span><br>    <span class="hljs-tag">&lt;<span class="hljs-name">groupId</span>&gt;</span>org.springframework.boot<span class="hljs-tag">&lt;/<span class="hljs-name">groupId</span>&gt;</span><br>    <span class="hljs-tag">&lt;<span class="hljs-name">artifactId</span>&gt;</span>spring-boot-starter-parent<span class="hljs-tag">&lt;/<span class="hljs-name">artifactId</span>&gt;</span><br>    <span class="hljs-tag">&lt;<span class="hljs-name">version</span>&gt;</span>3.0.4<span class="hljs-tag">&lt;/<span class="hljs-name">version</span>&gt;</span><br>    <span class="hljs-tag">&lt;<span class="hljs-name">relativePath</span>/&gt;</span> <span class="hljs-comment">&lt;!-- lookup parent from repository --&gt;</span><br><span class="hljs-tag">&lt;/<span class="hljs-name">parent</span>&gt;</span><br><span class="hljs-comment">&lt;!-- 引入redis依赖 --&gt;</span><br><span class="hljs-tag">&lt;<span class="hljs-name">dependency</span>&gt;</span><br>    <span class="hljs-tag">&lt;<span class="hljs-name">groupId</span>&gt;</span>org.springframework.boot<span class="hljs-tag">&lt;/<span class="hljs-name">groupId</span>&gt;</span><br>    <span class="hljs-tag">&lt;<span class="hljs-name">artifactId</span>&gt;</span>spring-boot-starter-data-redis<span class="hljs-tag">&lt;/<span class="hljs-name">artifactId</span>&gt;</span><br><span class="hljs-tag">&lt;/<span class="hljs-name">dependency</span>&gt;</span><br><span class="hljs-tag">&lt;<span class="hljs-name">dependency</span>&gt;</span><br>    <span class="hljs-tag">&lt;<span class="hljs-name">groupId</span>&gt;</span>org.apache.commons<span class="hljs-tag">&lt;/<span class="hljs-name">groupId</span>&gt;</span><br>    <span class="hljs-tag">&lt;<span class="hljs-name">artifactId</span>&gt;</span>commons-pool2<span class="hljs-tag">&lt;/<span class="hljs-name">artifactId</span>&gt;</span><br><span class="hljs-tag">&lt;/<span class="hljs-name">dependency</span>&gt;</span><br></code></pre></td></tr></table></figure>

<h2 id="redis-配置"><a href="#redis-配置" class="headerlink" title="redis 配置"></a>redis 配置</h2><ol>
<li>下面 <code>redis</code> 配置的前缀都是 <code>spring.data.redis</code>, 如果使用的是 <code>SpringBoot2.x</code> 版本，前缀是 <code>spring.redis</code><figure class="highlight properties"><table><tr><td class="code"><pre><code class="hljs properties"><span class="hljs-comment">#默认是0号库</span><br><span class="hljs-attr">spring.data.redis.database</span>=<span class="hljs-string">3</span><br><span class="hljs-comment"># Redis服务器地址</span><br><span class="hljs-attr">spring.data.redis.host</span>=<span class="hljs-string">192.168.0.236</span><br><span class="hljs-comment"># Redis服务器连接端口</span><br><span class="hljs-attr">spring.data.redis.port</span>=<span class="hljs-string">6379</span><br><span class="hljs-attr">spring.data.redis.username</span>=<span class="hljs-string">default</span><br><span class="hljs-comment"># Redis服务器连接密码,默认为空</span><br><span class="hljs-attr">spring.data.redis.password</span>=<span class="hljs-string">123456</span><br><span class="hljs-comment"># 连接池最大连接数</span><br><span class="hljs-attr">spring.data.redis.lettuce.pool.max-active</span>=<span class="hljs-string">10</span><br><span class="hljs-comment"># 连接池最大阻塞等待时间,默认-1表示没有限制</span><br><span class="hljs-attr">spring.data.redis.lettuce.pool.max-wait</span>=<span class="hljs-string">-1</span><br><span class="hljs-comment"># 连接池中的最大空闲连接</span><br><span class="hljs-attr">spring.data.redis.lettuce.pool.max-idle</span>=<span class="hljs-string">10</span><br><span class="hljs-comment"># 连接池中的最小空闲连接,默认是0</span><br><span class="hljs-attr">spring.data.redis.lettuce.pool.min-idle</span>=<span class="hljs-string">0</span><br></code></pre></td></tr></table></figure></li>
</ol>
<h2 id="编写测试类"><a href="#编写测试类" class="headerlink" title="编写测试类"></a>编写测试类</h2><ol><li><a href="/2023/9cd70dc15320/index.html" title="SpringBoot编写测试类">SpringBoot编写测试类</a></li></ol>

<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-meta">@SpringBootTest(classes = TestApplication.class)</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">TestRedis</span> &#123;<br>    <span class="hljs-meta">@Autowired</span><br>    <span class="hljs-keyword">private</span> RedisTemplate redisTemplate;<br><br>    <span class="hljs-meta">@Test</span><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">testRedis</span><span class="hljs-params">()</span>&#123;<br>        redisTemplate.opsForValue().set(<span class="hljs-string">&quot;key1&quot;</span>, <span class="hljs-string">&quot;value1&quot;</span>);<br>        <span class="hljs-type">Object</span> <span class="hljs-variable">o</span> <span class="hljs-operator">=</span> redisTemplate.opsForValue().get(<span class="hljs-string">&quot;key1&quot;</span>);<br>        System.out.println(<span class="hljs-string">&quot;获取redis key1 的结果:&quot;</span> + o);<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>
<p>使用 <code>redis</code> 客户端看一下这个 <code>key</code> 和 <code>value</code></p>
<figure class="highlight wren"><table><tr><td class="code"><pre><code class="hljs wren">[<span class="hljs-variable">db3</span>] <span class="hljs-operator">&gt;</span> <span class="hljs-variable">get</span> <span class="hljs-title function_">key1</span><br>(<span class="hljs-variable">nil</span>)<br><br>[<span class="hljs-variable">db3</span>] <span class="hljs-operator">&gt;</span> <span class="hljs-variable">keys</span> <span class="hljs-operator">*</span><span class="hljs-variable">key1</span><span class="hljs-operator">*</span><br><span class="hljs-number">1</span>) <span class="hljs-string">&quot;\xac\xed<span class="hljs-char escape_">\x00</span><span class="hljs-char escape_">\x05</span>t<span class="hljs-char escape_">\x00</span><span class="hljs-char escape_">\x04</span>key1&quot;</span><br><br>[<span class="hljs-variable">db3</span>] <span class="hljs-operator">&gt;</span> <span class="hljs-variable">keys</span> <span class="hljs-operator">*</span><span class="hljs-variable">key1</span><span class="hljs-operator">*</span><br><span class="hljs-number">1</span>) <span class="hljs-string">&quot;\xac\xed<span class="hljs-char escape_">\x00</span><span class="hljs-char escape_">\x05</span>t<span class="hljs-char escape_">\x00</span><span class="hljs-char escape_">\x04</span>key1&quot;</span><br></code></pre></td></tr></table></figure>
<p>可以看到直接使用 <code>key1</code> 竟然获取不到数据，使用模拟匹配后能看到，发现都是进行了编码，这是因为 <code>RedisTemplate</code> 默认使用 <code>jdk</code> 的序列化方式，可以通过自定义 <code>RedisTemplate</code> 方式来修改序列化方式</p>
<h2 id="自定义-RedisTemplate"><a href="#自定义-RedisTemplate" class="headerlink" title="自定义 RedisTemplate"></a>自定义 RedisTemplate</h2><p>下面代码将 <code>RedisTemplate</code> 修改成使用 <code>json</code> 方式进行序列化</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-meta">@Configuration</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">CusConfig</span> &#123;<br><br>    <span class="hljs-meta">@Bean</span><br>    <span class="hljs-meta">@SuppressWarnings(&quot;all&quot;)</span><br>    <span class="hljs-keyword">public</span> RedisTemplate&lt;String, Object&gt; <span class="hljs-title function_">redisTemplate</span><span class="hljs-params">(RedisConnectionFactory factory)</span> &#123;<br>        RedisTemplate&lt;String, Object&gt; template = <span class="hljs-keyword">new</span> <span class="hljs-title class_">RedisTemplate</span>&lt;&gt;();<br>        template.setConnectionFactory(factory);<br>        <span class="hljs-type">Jackson2JsonRedisSerializer</span> <span class="hljs-variable">jackson2JsonRedisSerializer</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Jackson2JsonRedisSerializer</span>(Object.class);<br>        <span class="hljs-type">ObjectMapper</span> <span class="hljs-variable">om</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">ObjectMapper</span>();<br>        om.setVisibility(PropertyAccessor.ALL, JsonAutoDetect.Visibility.ANY);<br>        om.activateDefaultTyping(LaissezFaireSubTypeValidator.instance , ObjectMapper.DefaultTyping.NON_FINAL);<br>        om.configure(DeserializationFeature.FAIL_ON_UNKNOWN_PROPERTIES, <span class="hljs-literal">false</span>);<br>        jackson2JsonRedisSerializer.setObjectMapper(om);<br>        <span class="hljs-type">StringRedisSerializer</span> <span class="hljs-variable">stringRedisSerializer</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">StringRedisSerializer</span>();<br>        <span class="hljs-comment">// key采用String的序列化方式</span><br>        template.setKeySerializer(stringRedisSerializer);<br>        <span class="hljs-comment">// hash的key也采用String的序列化方式</span><br>        template.setHashKeySerializer(stringRedisSerializer);<br>        <span class="hljs-comment">// value序列化方式采用jackson</span><br>        template.setValueSerializer(jackson2JsonRedisSerializer);<br>        <span class="hljs-comment">// hash的value序列化方式采用jackson</span><br>        template.setHashValueSerializer(jackson2JsonRedisSerializer);<br>        template.afterPropertiesSet();<br>        <span class="hljs-keyword">return</span> template;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>
<p>执行测试代码后再次查看结果</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><code class="hljs swift">[db3] <span class="hljs-operator">&gt;</span> <span class="hljs-keyword">get</span> key1<br><span class="hljs-string">&quot;<span class="hljs-subst">\&quot;</span>value1<span class="hljs-subst">\&quot;</span>&quot;</span><br></code></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>存储</category>
        <category>Redis</category>
      </categories>
      <tags>
        <tag>Redis</tag>
      </tags>
  </entry>
  <entry>
    <title>redis分布式锁</title>
    <url>/2023/c21b41047d75/index.html</url>
    <content><![CDATA[<h2 id="redis-分布式锁要做到的事情"><a href="#redis-分布式锁要做到的事情" class="headerlink" title="redis 分布式锁要做到的事情"></a>redis 分布式锁要做到的事情</h2><ol>
<li>独占性： 任何时刻只能有一个线程持有</li>
<li>高可用： 若在 <code>redis</code> 集群环境下，不能因为某一个节点挂了而出现获取锁和释放锁失败的情况</li>
<li>防止死锁： 要有超时控制机制或者撤销操作</li>
<li>正确性： 不能 <code>unlock</code> 别人的锁，自己的锁只能自己释放</li>
<li>可重入</li>
</ol>
<h2 id="分布式锁的实现"><a href="#分布式锁的实现" class="headerlink" title="分布式锁的实现"></a>分布式锁的实现</h2><p><a href="https://redis.io/docs/manual/patterns/distributed-locks/">redis分布式锁官方文档</a><br>下面一段是官网对 <code>redis</code> 分布式锁的穆描述</p>
<ol>
<li>使用的算法是 <code>Redlock</code></li>
<li><code>Java</code> 版本对 <code>Redlock</code> 的实现是 <code>Redisson</code><blockquote>
<p>This page describes a more canonical algorithm to implement distributed locks with Redis. We propose an algorithm, called Redlock, which implements a DLM which we believe to be safer than the vanilla single instance approach. We hope that the community will analyze it, provide feedback, and use it as a starting point for the implementations or more complex or alternative designs.</p>
</blockquote>
</li>
</ol>
<p><a href="https://github.com/redisson/redisson">官网描述的redisson分布式实现</a><br>这里面有很多实现，比如 <code>Spring Data Redis</code>, <code>Spring Boot Starter</code> 等，我们选择<a href="https://github.com/redisson/redisson/tree/master/redisson-spring-boot-starter">Spring Boot Starter</a> 这个</p>
<h3 id="添加依赖"><a href="#添加依赖" class="headerlink" title="添加依赖"></a>添加依赖</h3><figure class="highlight xml"><table><tr><td class="code"><pre><code class="hljs xml"><span class="hljs-tag">&lt;<span class="hljs-name">dependency</span>&gt;</span><br>     <span class="hljs-tag">&lt;<span class="hljs-name">groupId</span>&gt;</span>org.redisson<span class="hljs-tag">&lt;/<span class="hljs-name">groupId</span>&gt;</span><br>     <span class="hljs-tag">&lt;<span class="hljs-name">artifactId</span>&gt;</span>redisson-spring-boot-starter<span class="hljs-tag">&lt;/<span class="hljs-name">artifactId</span>&gt;</span><br>     <span class="hljs-tag">&lt;<span class="hljs-name">version</span>&gt;</span>3.24.3<span class="hljs-tag">&lt;/<span class="hljs-name">version</span>&gt;</span><br><span class="hljs-tag">&lt;/<span class="hljs-name">dependency</span>&gt;</span><br></code></pre></td></tr></table></figure>

<h3 id="redis参数配置"><a href="#redis参数配置" class="headerlink" title="redis参数配置"></a>redis参数配置</h3><div class="tabs" id="redis参数配置"><ul class="nav-tabs"><button type="button" class="tab  active" data-href="redis参数配置-1">SpringBoot3.x版本</button><button type="button" class="tab " data-href="redis参数配置-2">SpringBoot2.x版本</button></ul><div class="tab-contents"><div class="tab-item-content active" id="redis参数配置-1"><ol>
<li><p>redis 参数配置</p>
<figure class="highlight properties"><table><tr><td class="code"><pre><code class="hljs properties"><span class="hljs-attr">spring.data.redis.database</span>=<span class="hljs-string">3</span><br><span class="hljs-attr">spring.data.redis.host</span>=<span class="hljs-string">127.0.0.1</span><br><span class="hljs-attr">spring.data.redis.port</span>=<span class="hljs-string">6379</span><br><span class="hljs-attr">spring.data.redis.username</span>=<span class="hljs-string">default</span><br><span class="hljs-attr">spring.data.redis.password</span>=<span class="hljs-string">123456</span><br><span class="hljs-attr">spring.data.redis.lettuce.pool.max-active</span>=<span class="hljs-string">10</span><br><span class="hljs-attr">spring.data.redis.lettuce.pool.max-wait</span>=<span class="hljs-string">-1</span><br><span class="hljs-attr">spring.data.redis.lettuce.pool.max-idle</span>=<span class="hljs-string">10</span><br><span class="hljs-attr">spring.data.redis.lettuce.pool.min-idle</span>=<span class="hljs-string">0</span><br></code></pre></td></tr></table></figure>
</li>
<li><p>redisson 配置</p>
<figure class="highlight properties"><table><tr><td class="code"><pre><code class="hljs properties"><span class="hljs-attr">spring.data.redis.redisson.singleServerConfig.pool.max-active</span>=<span class="hljs-string">10</span><br><span class="hljs-attr">spring.data.redis.redisson.singleServerConfig.pool.max-wait</span>=<span class="hljs-string">-1</span><br><span class="hljs-attr">spring.data.redis.redisson.singleServerConfig.pool.max-idle</span>=<span class="hljs-string">10</span><br><span class="hljs-attr">spring.data.redis.redisson.singleServerConfig.pool.min-idle</span>=<span class="hljs-string">0</span><br><span class="hljs-attr">spring.data.redis.redisson.singleServerConfig.address</span>=<span class="hljs-string">&quot;redis://127.0.0.1:6379&quot;</span><br><span class="hljs-attr">spring.data.redis.redisson.singleServerConfig.databse</span>=<span class="hljs-string">3</span><br><span class="hljs-attr">spring.data.redis.redisson.singleServerConfig.password</span>=<span class="hljs-string">123456</span><br></code></pre></td></tr></table></figure></li>
</ol></div><div class="tab-item-content" id="redis参数配置-2"><ol>
<li><p>redis 参数配置</p>
<figure class="highlight properties"><table><tr><td class="code"><pre><code class="hljs properties"><span class="hljs-attr">spring.data.redis.database</span>=<span class="hljs-string">3</span><br><span class="hljs-attr">spring.redis.host</span>=<span class="hljs-string">127.0.0.1</span><br><span class="hljs-attr">spring.redis.port</span>=<span class="hljs-string">6379</span><br><span class="hljs-attr">spring.redis.username</span>=<span class="hljs-string">default</span><br><span class="hljs-attr">spring.redis.password</span>=<span class="hljs-string">123456</span><br><span class="hljs-attr">spring.redis.lettuce.pool.max-active</span>=<span class="hljs-string">10</span><br><span class="hljs-attr">spring.redis.lettuce.pool.max-wait</span>=<span class="hljs-string">-1</span><br><span class="hljs-attr">spring.redis.lettuce.pool.max-idle</span>=<span class="hljs-string">10</span><br><span class="hljs-attr">spring.redis.lettuce.pool.min-idle</span>=<span class="hljs-string">0</span><br></code></pre></td></tr></table></figure>
</li>
<li><p>redisson 配置</p>
<figure class="highlight properties"><table><tr><td class="code"><pre><code class="hljs properties"><span class="hljs-attr">spring.redis.redisson.singleServerConfig.pool.max-active</span>=<span class="hljs-string">10</span><br><span class="hljs-attr">spring.redis.redisson.singleServerConfig.pool.max-wait</span>=<span class="hljs-string">-1</span><br><span class="hljs-attr">spring.redis.redisson.singleServerConfig.pool.max-idle</span>=<span class="hljs-string">10</span><br><span class="hljs-attr">spring.redis.redisson.singleServerConfig.pool.min-idle</span>=<span class="hljs-string">0</span><br><span class="hljs-attr">spring.redis.redisson.singleServerConfig.address</span>=<span class="hljs-string">&quot;redis://127.0.0.1:6379&quot;</span><br><span class="hljs-attr">spring.redis.redisson.singleServerConfig.databse</span>=<span class="hljs-string">3</span><br><span class="hljs-attr">spring.redis.redisson.singleServerConfig.password</span>=<span class="hljs-string">123456</span><br></code></pre></td></tr></table></figure></li>
</ol></div></div><div class="tab-to-top"><button type="button" aria-label="scroll to top"><i class="fas fa-arrow-up"></i></button></div></div>

<p>使用上述配置之后，在 <code>Spring Boot</code> 环境中可以直接使用如下的几个 <code>Bean</code> </p>
<ul>
<li><code>RedissonClient</code></li>
<li><code>RedissonRxClient</code></li>
<li><code>RedissonReactiveClient</code></li>
<li><code>RedisTemplate</code></li>
<li><code>ReactiveRedisTemplate</code></li>
</ul>
<h3 id="分布式锁代码示例"><a href="#分布式锁代码示例" class="headerlink" title="分布式锁代码示例"></a>分布式锁代码示例</h3><p>下面代码实际使用的还是单机中的程序，不过用来测试分布式锁的使用已经足够了</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-meta">@SpringBootTest</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">TestRedis</span> &#123;<br>    <span class="hljs-meta">@Autowired</span><br>    <span class="hljs-keyword">private</span> RedissonClient redisson;<br><br>    <span class="hljs-keyword">private</span> <span class="hljs-type">int</span> <span class="hljs-variable">i</span> <span class="hljs-operator">=</span> <span class="hljs-number">0</span>;<br><br>    <span class="hljs-meta">@Test</span><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">testRedisLock</span><span class="hljs-params">()</span> <span class="hljs-keyword">throws</span> InterruptedException &#123;<br>        List&lt;Thread&gt; threads = <span class="hljs-keyword">new</span> <span class="hljs-title class_">ArrayList</span>&lt;&gt;();<br>        <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> <span class="hljs-variable">j</span> <span class="hljs-operator">=</span> <span class="hljs-number">0</span>; j &lt; <span class="hljs-number">10</span>; j++) &#123;<br>            <span class="hljs-type">Thread</span> <span class="hljs-variable">thread</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Thread</span>(() -&gt; &#123;<br>                <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> <span class="hljs-variable">k</span> <span class="hljs-operator">=</span> <span class="hljs-number">0</span>; k &lt; <span class="hljs-number">100</span>; k++) &#123;<br>                  <span class="hljs-type">RLock</span> <span class="hljs-variable">lock</span> <span class="hljs-operator">=</span> <span class="hljs-literal">null</span>;<br>                    <span class="hljs-keyword">try</span> &#123;<br>                        lock = redisson.getLock(<span class="hljs-string">&quot;lock&quot;</span>);<br>                        Thread.sleep(<span class="hljs-number">10</span>);<br>                        lock.lock();<br>                        i++;<br>                    &#125; <span class="hljs-keyword">catch</span> (InterruptedException e) &#123;<br>                        <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">RuntimeException</span>(e);<br>                    &#125; <span class="hljs-keyword">finally</span> &#123;<br>                        <span class="hljs-keyword">if</span> (lock!= <span class="hljs-literal">null</span>) &#123;<br>                            lock.unlock();<br>                        &#125;<br>                    &#125;<br>                &#125;<br>            &#125;, <span class="hljs-string">&quot;t1&quot;</span>);<br>            thread.start();<br>            threads.add(thread);<br>        &#125;<br>        <span class="hljs-keyword">for</span> (Thread thread : threads) &#123;<br>            thread.join();<br>        &#125;<br>        System.out.println(<span class="hljs-string">&quot;最终结果:&quot;</span> + i);<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>




]]></content>
      <categories>
        <category>存储</category>
        <category>Redis</category>
      </categories>
      <tags>
        <tag>Redis</tag>
      </tags>
  </entry>
  <entry>
    <title>redis发布订阅</title>
    <url>/2023/2d9e56a00163/index.html</url>
    <content><![CDATA[<p>参考 <a href="https://docs.spring.io/spring-data/redis/reference/redis/pubsub.html">Spring官网对redis发布订阅的代码实现</a><br>说明：</p>
<ol>
<li><code>SpringBoot3.x</code> 使用 <code>Redis</code> 可以先参考下面的文章</li>
</ol>
<ol><li><a href="/2023/5ef02c37afab/index.html" title="SpringBoot中使用Redis">SpringBoot中使用Redis</a></li></ol>
<ol start="2">
<li>整体代码分为三部分</li>
<li>消息监听者 (这部分是业务代码中需要编写的消息接收的逻辑)</li>
<li>注册消息监听者 （这部分是将自己编写的消息监听者注册到 redis 中，便于 redis 可以调用到我们的消息监听类）</li>
<li>消息发送者</li>
</ol>
<h2 id="消息监听者"><a href="#消息监听者" class="headerlink" title="消息监听者"></a>消息监听者</h2><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-meta">@Component</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">RedisMessageListener</span> &#123;<br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">onMessage</span><span class="hljs-params">(String message)</span> &#123;<br>        System.out.println(<span class="hljs-string">&quot;监听到的消息:&quot;</span> + message);<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<h2 id="注册消息监听"><a href="#注册消息监听" class="headerlink" title="注册消息监听"></a>注册消息监听</h2><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-meta">@Component</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">RedisMessageListener</span> &#123;<br><span class="hljs-meta">@Configuration</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">RedisConfig</span> &#123;<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-type">String</span> <span class="hljs-variable">REDIS_SUBSCRIBE_CHANNEL</span> <span class="hljs-operator">=</span> <span class="hljs-string">&quot;redis.subscribe.channel&quot;</span>;<br><br>    <span class="hljs-meta">@Bean</span><br>    <span class="hljs-keyword">public</span> RedisMessageListenerContainer <span class="hljs-title function_">listenerContainer</span><span class="hljs-params">(RedisConnectionFactory factory,</span><br><span class="hljs-params">                                                           MessageListenerAdapter messageListenerAdapter)</span>&#123;<br>        <span class="hljs-type">RedisMessageListenerContainer</span> <span class="hljs-variable">container</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">RedisMessageListenerContainer</span>();<br>        container.setConnectionFactory(factory);<br>        <span class="hljs-comment">//redis.subscribe.channel 是配置的订阅的 channel, 发送时也需要往这个 channel 中发送</span><br>        container.addMessageListener(messageListenerAdapter, <span class="hljs-keyword">new</span> <span class="hljs-title class_">PatternTopic</span>(REDIS_SUBSCRIBE_CHANNEL));<br>        <span class="hljs-keyword">return</span> container;<br>    &#125;<br><br>    <span class="hljs-meta">@Bean</span><br>    <span class="hljs-keyword">public</span> MessageListenerAdapter <span class="hljs-title function_">messageListenerAdapter</span><span class="hljs-params">(RedisMessageListener listener)</span>&#123;<br>        <span class="hljs-type">MessageListenerAdapter</span> <span class="hljs-variable">messageListenerAdapter</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">MessageListenerAdapter</span>(listener,<br>                <span class="hljs-string">&quot;onMessage&quot;</span>);<br>        <span class="hljs-keyword">return</span> messageListenerAdapter;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>
<p>从上面代码中看到往 <code>RedisMessageListenerContainer</code> 类中注册的并不是我们自己编写的 <code>RedisMessageListener</code>， 而是 <code>MessageListenerAdapter</code>（<code>MessageListenerAdapter</code> 包裹了 <code>RedisMessageListener</code>），这是因为需要通过 <code>MessageListenerAdapter</code> 来做消息适配，比如我们编写的 <code>RedisMessageListener</code> 类中 <code>onMessage(String)</code> 方法接收 <code>String</code> 类型参数，为什么可以直接接收 <code>String</code> 类型参数呢？ 就是 <code>MessageListenerAdapter</code> 做了转换</p>
<h2 id="消息发送者"><a href="#消息发送者" class="headerlink" title="消息发送者"></a>消息发送者</h2><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-meta">@Component</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">RedisPublish</span> &#123;<br><br>    <span class="hljs-meta">@Autowired</span><br>    <span class="hljs-keyword">private</span> RedisTemplate&lt;String, Object&gt; stringRedisTemplate;<br><br>    <span class="hljs-comment">//这里使用convertAndSend方法，参数为 channel 通道和内容</span><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">sendMsg</span><span class="hljs-params">(String channel, String msg)</span> &#123;<br>        stringRedisTemplate.convertAndSend(channel, msg);<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<h2 id="主类启动并测试"><a href="#主类启动并测试" class="headerlink" title="主类启动并测试"></a>主类启动并测试</h2><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-meta">@SpringBootApplication</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">TestApplication</span> &#123;<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">main</span><span class="hljs-params">(String[] args)</span> <span class="hljs-keyword">throws</span> InterruptedException &#123;<br>        <span class="hljs-type">ConfigurableApplicationContext</span> <span class="hljs-variable">applicationContext</span> <span class="hljs-operator">=</span> SpringApplication.run(TestApplication.class, args);<br>        <span class="hljs-comment">// 为了让日志在最后打印，便于展示所以延迟5s再发送</span><br>        Thread.sleep(<span class="hljs-number">5000</span>);<br>        <span class="hljs-type">RedisPublish</span> <span class="hljs-variable">redisPublish</span> <span class="hljs-operator">=</span> applicationContext.getBean(RedisPublish.class);<br>        <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> <span class="hljs-variable">i</span> <span class="hljs-operator">=</span> <span class="hljs-number">0</span>; i &lt; <span class="hljs-number">5</span>; i++) &#123;<br>            redisPublish.sendMsg(RedisConfig.REDIS_SUBSCRIBE_CHANNEL, <span class="hljs-string">&quot;测试发送数据&quot;</span> + i);<br>        &#125;<br><br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<p>测试结果：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java">监听到的消息:<span class="hljs-string">&quot;测试发送数据1&quot;</span><br>监听到的消息:<span class="hljs-string">&quot;测试发送数据2&quot;</span><br>监听到的消息:<span class="hljs-string">&quot;测试发送数据3&quot;</span><br>监听到的消息:<span class="hljs-string">&quot;测试发送数据4&quot;</span><br></code></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>存储</category>
        <category>Redis</category>
      </categories>
      <tags>
        <tag>Redis</tag>
      </tags>
  </entry>
  <entry>
    <title>redis和mysql双写一致性问题</title>
    <url>/2023/68d7307921af/index.html</url>
    <content><![CDATA[<p>当数据有更新时，是先更新缓存还是先更新数据库？或者是其他的操作？对于缓存和数据库的更新组合有以下几种方式</p>
<h2 id="先更新缓存，再更新数据库"><a href="#先更新缓存，再更新数据库" class="headerlink" title="先更新缓存，再更新数据库"></a>先更新缓存，再更新数据库</h2><p>假设更新缓存成功，但是更新数据库失败，缓存中数据就是错误，而且 redis 是不支持事务回滚，所以这种方案有比较大的缺陷</p>
<h2 id="先删除缓存，再更新数据库"><a href="#先删除缓存，再更新数据库" class="headerlink" title="先删除缓存，再更新数据库"></a>先删除缓存，再更新数据库</h2><ol>
<li>使用这种方案，即使更新数据库失败了也不需要回滚缓存。这种做法虽然巧妙规避了失败回滚的问题，但是会有其他的问题</li>
<li>假设线程A先删除缓存，再更新数据库。在线程A完成更新数据库之前，后执行的线程B反而超前完成了操作，读取Key发现没有数据后，将数据库中的旧值存放到了缓存中。线程A在线程B都完成后再更新数据库，这样就会出现缓存（旧值）与数据库的值（新值）不一致的问题</li>
</ol>
<h2 id="先更新数据库，再更新缓存"><a href="#先更新数据库，再更新缓存" class="headerlink" title="先更新数据库，再更新缓存"></a>先更新数据库，再更新缓存</h2><ol>
<li>当更新数据库成功，更新缓存失败时，可以采取重试的策略，但重试机制如果存在延时还是会出现数据库与缓存不一致的情况，不好处理</li>
<li>假设两个线程同时更新同一个数据，线程A先完成了第一步，线程B先完成了第二步怎么办？线程A把值更新成a，线程B把值更新成b，此时数据库中的最新值是b，因为线程A先完成了第一步，后完成第二步，所以缓存中的最新值是a，数据库与缓存的值还是不一致，这个逻辑还是有问题的</li>
</ol>
<h2 id="先更新数据库，再删除缓存"><a href="#先更新数据库，再删除缓存" class="headerlink" title="先更新数据库，再删除缓存"></a>先更新数据库，再删除缓存</h2><ol>
<li>因为更新数据库之后是删除缓存，所以可以解决 <strong>先更新数据库，再更新缓存</strong> 带来的缓存不一致的问题</li>
<li>假设线程A要更新数据，先完成第一步更新数据库，在线程A删除缓存之前，线程B要访问缓存，那么取得的就是旧数据， 不过这种情况出现的概率比较低</li>
</ol>
<h2 id="先删除缓存，更新数据库，再删除缓存"><a href="#先删除缓存，更新数据库，再删除缓存" class="headerlink" title="先删除缓存，更新数据库，再删除缓存"></a>先删除缓存，更新数据库，再删除缓存</h2><ol>
<li>和 <strong>先更新数据库，再删除缓存</strong> 处理方式类似，所以还是会出现类似的问题，假设线程A要更新数据库，先删除了缓存，这一瞬间线程C要读缓存，先把数据迁移到缓存；然后线程A完成了更新数据库的操作，这一瞬间线程B也要访问缓存，此时它访问到的就是线程C放到缓存里面的旧数据， 不过这种情况出现的概率更低，这是需要三个线程配合才可能会出现的问题</li>
</ol>
<p>综上所述，每一种方案都是有一定的缺陷的，只是最后一种方式可以让缺陷发生的机率更低而已</p>
]]></content>
      <categories>
        <category>存储</category>
        <category>Redis</category>
      </categories>
      <tags>
        <tag>Redis</tag>
      </tags>
  </entry>
  <entry>
    <title>redis底层数据结构</title>
    <url>/2023/19e21a188fd0/index.html</url>
    <content><![CDATA[<span class="hide_first_mermaid">
<div class="mermaid-wrap"><pre class="mermaid-src" hidden>
  flowchart LR
    A
  </pre></div>
</span>

<h2 id="redis-对象"><a href="#redis-对象" class="headerlink" title="redis 对象"></a>redis 对象</h2><p><code>redis</code> 中所有数据类型都是使用 <code>RedisObject</code> 对象形式来表示，<code>RedisObject</code> 主要包含三个字段</p>
<span id="more"></span>

<pre class="mermaid">graph LR
    A[RedisObject]
    A-->B["type (记录对象的类型) \n 常用5种基本类型，实际还有BitMap（2.2 版新增）\n、HyperLogLog（2.8 版新增）、\n GEO（3.2 版新增）、Stream（5.0 版新增）"]

    B1[String 字符串对象]
    B2[List 列表集合]
    B3[Hash 哈希对象]
    B4[Set 集合对象]
    B5[Zset 有序集合对象]
    B--> B1 & B2 & B3 & B4 & B5

    A-->C["encoding (记录对象的编码)"]
    C1[Intset 整数集合]
    C2[embstr embstr编码的简单动态字符串]
    C3[raw 简单动态字符串]
    C4[HT 字典]
    C5[Linkedlist 双端列表]
    C6[ziplist 压缩列表]
    C7[skiplist 跳跃表和字典]
    C8[quicklist 快速列表]
    C-->C1 & C2 & C3 & C4 & C5 & C6 & C7 & C8

    A-->D["*ptr (指向具体的类型)"]</pre>

<h3 id="为什么一个对象需要包含-type-和-encoding"><a href="#为什么一个对象需要包含-type-和-encoding" class="headerlink" title="为什么一个对象需要包含 type 和 encoding"></a>为什么一个对象需要包含 <code>type</code> 和 <code>encoding</code></h3><ol>
<li>因为 <code>type</code> 只是记录对象的类型</li>
<li>每一个 <code>type</code> 可以使用不同的底层数据结构来实现，所以还需要具体的 <code>encoding</code> 来指明这个 <code>type</code> 的实现是什么， 下图就列出每一个 <code>type</code> 有哪几种底层数据结构</li>
</ol>
<h2 id="5-种基本数据类型对应的底层实现"><a href="#5-种基本数据类型对应的底层实现" class="headerlink" title="5 种基本数据类型对应的底层实现"></a>5 种基本数据类型对应的底层实现</h2><h3 id="String"><a href="#String" class="headerlink" title="String"></a>String</h3><ol>
<li><code>String</code> 类型的底层的数据结构实现主要是 <code>int</code> 和 <code>SDS</code>（简单动态字符串）<ol>
<li>如果存储的是数字，则编码使用 <code>int</code> 类型</li>
</ol>
</li>
<li><code>sds</code> 的实际编码又包含 <code>raw</code> 和 <code>embstr</code><ol>
<li>字符串长度小于 32位时使用 <code>embstr</code>(<code>embstr</code> 的空间挨着 <code>RedisObject，RedisObject</code> 和 <code>embstr</code> 的内存是一次性分配的)</li>
<li>字符串长度大于 32位时使用 <code>raw</code> 编码(<code>RedisObject</code> 的内存分区和 <code>raw</code> 编码类型的空间分配是分开的，也就是需要两次内存分配)</li>
</ol>
</li>
</ol>
<h4 id="SDS-和-C-原生字符串的区别"><a href="#SDS-和-C-原生字符串的区别" class="headerlink" title="SDS 和 C 原生字符串的区别"></a>SDS 和 C 原生字符串的区别</h4><ol>
<li><code>sds</code> 可以保存文本和二进制数据，因为 <code>sds</code> 有 <code>length</code> 字段来判断字符串是否结束，c 使用 ‘\0’ 作为结束符</li>
<li><code>sds</code> 的 <code>api</code> 安全，不会出现缓冲区溢出，因为 <code>sds</code> 拼接字符串前有做容量检查</li>
</ol>
<h3 id="List"><a href="#List" class="headerlink" title="List"></a>List</h3><ol>
<li><code>List</code> 类型的底层数据结构是由双向链表或压缩列表实现的<ol>
<li>元素个数小于 512 个（默认值，可由 <code>list-max-ziplist-entries</code> 配置），列表每个元素的值都小于 64 字节（默认值，可由 <code>list-max-ziplist-value</code> <code>配置），Redis</code> 会使用压缩列表作为 <code>List</code> 类型的底层数据结构；</li>
</ol>
</li>
<li>在 <code>Redis3.2</code> 版本之后，<code>List</code> 类型底层数据结构只有 <code>quicklist</code> 一种</li>
</ol>
<h3 id="Hash"><a href="#Hash" class="headerlink" title="Hash"></a>Hash</h3><ol>
<li><code>Hash</code> 类型的底层数据结构是由压缩列表或哈希表实现的<ol>
<li>元素个数小于 512 个（可通过 <code>hash-max-ziplist-entries</code> 修改），所有值小于 64 字节（可通过 <code>hash-max-ziplist-value</code> 修改）时，<code>Redis</code> 会使用压缩列表作为 <code>Hash</code> 类型的底层数据结构</li>
</ol>
</li>
<li>在 <code>Redis7.0</code> 中，压缩列表数据结构被废弃，由 <code>listpack</code> 数据结构来实现</li>
</ol>
<h3 id="Set"><a href="#Set" class="headerlink" title="Set"></a>Set</h3><ol>
<li><code>Set</code> 类型的底层数据结构是由哈希表或整数集合实现的<ol>
<li>如果集合中的元素都是整数且元素个数小于 512 （可通过 <code>set-maxintset-entries</code> 修改）个，<code>Redis</code> 会使用整数集合作为 Set 类型的底层数据结构</li>
</ol>
</li>
</ol>
<h3 id="Zset"><a href="#Zset" class="headerlink" title="Zset"></a>Zset</h3><ol>
<li><p>Zset 类型底层数据结构是由压缩列表或跳表实现</p>
<ol>
<li>若有序集合的元素个数小于 128 个且每个元素的值小于 64 字节，<code>Redis</code> 会使用压缩列表作为 <code>Zset</code> 类型的底层数据结构</li>
</ol>
</li>
<li><p><code>Redis 7.0</code> 中，压缩列表数据结构被废弃，交由 <code>listpack</code> 数据结构来实现</p>
</li>
</ol>
]]></content>
      <categories>
        <category>存储</category>
        <category>Redis</category>
      </categories>
      <tags>
        <tag>Redis</tag>
      </tags>
  </entry>
  <entry>
    <title>redis线程模型</title>
    <url>/2023/e4db3f95ceb5/index.html</url>
    <content><![CDATA[<span class="hide_first_mermaid">
<div class="mermaid-wrap"><pre class="mermaid-src" hidden>
  flowchart LR
    A
  </pre></div>
</span>

<p>总结：<code>redis</code> 整体是多线程的，至于平时所说的 <code>redis</code> 单线程是指 <strong>redis的网络io 和 键值对读写</strong> 是在同一个线程中完成</p>
<h2 id="一次-redis-请求过程"><a href="#一次-redis-请求过程" class="headerlink" title="一次 redis 请求过程"></a>一次 redis 请求过程</h2><ol>
<li>监听客户端请求（<code>bind/listen</code>）</li>
<li>等待客户端的连接（<code>accept</code>）</li>
<li>从 <code>socket</code> 中读取数据（<code>recv</code>）</li>
<li>解析客户端发送的内容（<code>parse</code>）</li>
<li>根据解析出来的内容进行内存操作（<code>get/set</code>）</li>
<li>执行完成命令之后给客户端响应结果，即向 <code>socket</code> 中写数据（<code>send</code>）</li>
</ol>
<pre class="mermaid">graph TB
    subgraph A[redis线程]
        subgraph B[网络IO处理]
            B1[bind/listen]
            B2[accept]
            B3[recv]
            B4[parse]
        end
        subgraph C[键值对读写]
            C1[get/set]
        end
        subgraph D[网络IO处理]
            D1[send]
        end
    end
    B-->C-->D</pre>
<ul>
<li>网络 io 处理过程中， <code>listen</code> 和 <code>recv</code> 这两个步骤是可能阻塞的，即当没有链接或客户端没有发送命令时，服务器处理这两个环节时会阻塞，<strong>但是socket网络模型自身就支持非阻塞的形式</strong></li>
</ul>
<h2 id="redis4-版本中的多线程"><a href="#redis4-版本中的多线程" class="headerlink" title="redis4 版本中的多线程"></a>redis4 版本中的多线程</h2><ol>
<li>在 <code>redis4</code> 中主要是增加了异步删除机制，比如 <code>UNLINK、FLUSHALL ASYNC、FLUSHDB ASYNC</code> 等非阻塞的删除操作。</li>
<li>引入异步删除的原因是现在服务器的内存原来越大，<code>redis</code> 中可能会存在 <code>bigKey</code>, 对于这些 <code>bigKey</code> 的删除如果使用阻塞的方式，会导致服务器的响应时间变长，所以引入了异步删除的机制，即当删除 <code>bigKey</code> 的时候，会将删除操作放入一个异步队列中，由一个单独的线程来负责异步删除，这样就可以提高服务器的响应时间</li>
</ol>
<h2 id="redis6-版本的多线程"><a href="#redis6-版本的多线程" class="headerlink" title="redis6 版本的多线程"></a>redis6 版本的多线程</h2><p>在 <code>redis6</code> 中有几个重要特性，比如</p>
<ol>
<li>面向<strong>网络处理</strong>的多 <code>IO</code> 线程</li>
<li>客户端缓存</li>
<li>细粒度的权限控制</li>
</ol>
<p>本节主要讲解 <code>redis6</code> 中的多线程模型，在 <code>redis6</code> 之前，<strong>从网络io处理到键值对读写都是在同一个线程中完成的</strong>， 随着网络硬件的性能提升，<code>redis</code> 的性能瓶颈有时会出现在网络<code>io</code>的处理上，即 <strong>单个主线程处理网络请求的速度跟不上底层网络硬件的速度</strong>，所以在 <code>redis6</code> 中使用多个网络<code>io</code>线程来处理网络请求，提高网络请求处理并行的能力, 需要注意的是 <strong>redis6的多线程模型只是针对网络io部分，redis的数据读写依然是在一个线程中完成的</strong></p>
<h3 id="redis6中主线程和io线程协作流程"><a href="#redis6中主线程和io线程协作流程" class="headerlink" title="redis6中主线程和io线程协作流程"></a>redis6中主线程和io线程协作流程</h3><pre class="mermaid">graph TB
    subgraph A["redis主线程(接收请求)"]
        direction TB
        style A fill:#f9f
        A1["接收建立连接请求，获取socket"]
        A2["将socket放入全局等待队列"]
        A3["以轮询方式将socket连接分配给IO线程, 主线程开始阻塞-----"]
        A1-->A2-->A3
    end
    subgraph B["redis网络IO线程(解析请求)"]
        direction TB
        B1["将socket和线程绑定"]
        B2["读取socket中的请求并解析"]
        B3["请求解析完成"]
        B1-->B2-->B3
    end
    subgraph BB["redis网络IO线程(解析请求)"]
        direction TB
        BB1["将socket和线程绑定"]
        BB2["读取socket中的请求并解析"]
        BB3["请求解析完成"]
        BB1-->BB2-->BB3
    end
    A--"io线程开始执行"-->B & BB
    subgraph C["redis主线程(命令读写)"]
        direction TB
        style C fill:#f9f
        C1["执行请求的命令操作"]
        C2["请求的命令操作执行完成"]
        C3["将结果数据写入缓冲区"]
        C4["等待io线程完成数据回写socket，主线程阻塞---"]
        C1-->C2-->C3-->C4
    end
    B-->C
    BB-->C
    subgraph D["redis网络IO线程(结果响应)"]
        direction TB
        D1["将结果数据回写socket(这一步是并行处理)"]
        D2["socket回写完成"]
        D1-->D2
    end
    subgraph DD["redis网络IO线程(结果响应)"]
        direction TB
        DD1["将结果数据回写socket(这一步是并行处理)"]
        DD2["socket回写完成"]
        DD1-->DD2
    end
    C-->D & DD
    D-->E["主线程清空等待队列，等待后续请求"]
    DD-->E["主线程清空等待队列，等待后续请求"]
    style E fill:#f9f</pre>
<h3 id="阶段1"><a href="#阶段1" class="headerlink" title="阶段1"></a>阶段1</h3><ol>
<li>服务端和客户端连接 <code>socket</code> 连接，并将 <code>socket</code> 放入全局等待队列</li>
<li>主线程通过轮询的方式，将 <code>socket</code> 连接分配给 <code>io</code> 线程</li>
<li>主线程进入阻塞状态</li>
</ol>
<h3 id="阶段2"><a href="#阶段2" class="headerlink" title="阶段2"></a>阶段2</h3><ol>
<li><code>io</code> 线程完成客户端请求读取和解析</li>
<li><code>io</code> 线程有多个，是并发操作的, 所以会很快</li>
</ol>
<h3 id="阶段3"><a href="#阶段3" class="headerlink" title="阶段3"></a>阶段3</h3><ol>
<li>等待 <code>io</code> x线程将命令解析完成后，<strong>主线程会以单线程的方式执行这个命令操作</strong></li>
</ol>
<h3 id="阶段4"><a href="#阶段4" class="headerlink" title="阶段4"></a>阶段4</h3><ol>
<li>主线程执行完请求操作后，将需要返回的结果写入缓冲区，然会，主线程会阻塞等待 <code>io</code> 线程把这些结果写回到 <code>socket</code> 中，并返回给客户端</li>
<li><code>io</code> 线程回写 <code>socket</code> 之后，主线程会清空全局队列，等待客户端的后续请求</li>
</ol>
<h2 id="redis6-多线程模型的启用"><a href="#redis6-多线程模型的启用" class="headerlink" title="redis6 多线程模型的启用"></a>redis6 多线程模型的启用</h2><ol>
<li>在 <code>redis6</code> 中，多线程模块默认是关闭的</li>
<li>可以在 <code>redis.conf</code> 中设置 <code>io-threads-do-reads yes</code> 来开启多线程</li>
<li>可以在 <code>redis.conf</code> 中设置线程个数，比如 <code>io-threads 4</code>, 一般建议线程个数小于 <code>redis</code> 实例所在机器的 <code>cpu</code> 核个数</li>
</ol>
<h2 id="疑问解答"><a href="#疑问解答" class="headerlink" title="疑问解答"></a>疑问解答</h2><ol>
<li><code>Redis6.0</code>增加了<code>IO</code>线程来处理网络请求，如果客户端先发送了一个<code>set key1 val1</code>写命令，紧接着发送一个<code>get key1</code>读命令。由于IO线程是多线程处理的，是否会导致<code>get key1</code>读命令 先于 <code>set key1 val1</code>写命令执行呢？结果客户端读到了<code>key1</code>的旧值的情况发生？</li>
</ol>
<blockquote>
<p>如果这两条命令是同一个客户端发送的，有明显的先后顺序，就不会出现这种情况，因为接收请求的还是主线程，主线程接收请求后会将命令入队列，这个是可以保证顺序的，虽然后续是交给多线程来解析请求，但是命令读写的顺序还是会按照队列中命令先后出现的顺序，而且命令读写是通过主线程单线程操作的。</p>
<p>如果这里说的是多个客户端发送的命令，那就需要看哪个客户端发送的请求先到达服务端，先到达的命令会先执行</p>
</blockquote>
]]></content>
      <categories>
        <category>存储</category>
        <category>Redis</category>
      </categories>
      <tags>
        <tag>Redis</tag>
      </tags>
  </entry>
  <entry>
    <title>缓存雪崩缓存击穿缓存穿透</title>
    <url>/2023/90567821a93b/index.html</url>
    <content><![CDATA[<h2 id="缓存雪崩"><a href="#缓存雪崩" class="headerlink" title="缓存雪崩"></a>缓存雪崩</h2><h3 id="概念"><a href="#概念" class="headerlink" title="概念"></a>概念</h3><p>缓存雪崩是指在缓存服务器重启或者大量 <code>key</code> 集中在某一时刻同时失效，而这些缓存在同一时刻被大量的请求所访问，从而引起大量的缓存集中失效，引起请求的性能急剧下降。</p>
<h3 id="解决方案"><a href="#解决方案" class="headerlink" title="解决方案"></a>解决方案</h3><ol>
<li>避免缓存设置相近的有效期</li>
<li>对数据进行预热</li>
</ol>
<h2 id="缓存击穿"><a href="#缓存击穿" class="headerlink" title="缓存击穿"></a>缓存击穿</h2><h3 id="概念-1"><a href="#概念-1" class="headerlink" title="概念"></a>概念</h3><p>缓存击穿是指一个热点 <code>key</code>在缓存失效的时候，同时大量的请求都在访问这个<code>key</code>，而这些请求都在同时失效，从而引起大量的请求集中到达数据库，从而引起数据库的压力。</p>
<h3 id="解决方案-1"><a href="#解决方案-1" class="headerlink" title="解决方案"></a>解决方案</h3><ol>
<li>热点数据永远不过期，即在数据过期之间可以先进行更新</li>
<li>使用分布式锁，只让一个线程去查询 <code>db</code> 并返回结果，不过这种方式在高并发下比较影响性能</li>
</ol>
<h2 id="缓存穿透"><a href="#缓存穿透" class="headerlink" title="缓存穿透"></a>缓存穿透</h2><h3 id="概念-2"><a href="#概念-2" class="headerlink" title="概念"></a>概念</h3><p>缓存和数据库中都没有的数据，还是有源源不断的请求到达，导致每次请求都会到数据库，从而压垮数据库</p>
<h3 id="解决方案-2"><a href="#解决方案-2" class="headerlink" title="解决方案"></a>解决方案</h3><ol>
<li>对于查询数据库也不存在的数据，依然可以存放在 <code>redis</code> 中，并且设置一个较短的过期时间</li>
<li>使用布隆过滤器</li>
</ol>
]]></content>
      <categories>
        <category>存储</category>
        <category>Redis</category>
      </categories>
      <tags>
        <tag>Redis</tag>
      </tags>
  </entry>
  <entry>
    <title>InnoDB存储引擎详解</title>
    <url>/2023/a421bfc0bf81/index.html</url>
    <content><![CDATA[<span class="hide_first_mermaid">
<div class="mermaid-wrap"><pre class="mermaid-src" hidden>
  flowchart LR
    A
  </pre></div>
</span>

<h2 id="InnoDB-存储引擎数据操作过程"><a href="#InnoDB-存储引擎数据操作过程" class="headerlink" title="InnoDB 存储引擎数据操作过程"></a>InnoDB 存储引擎数据操作过程</h2><ol>
<li><code>InnoDB</code> 存储引擎是基于磁盘存储的，并将其中的记录按照页的方式进行管理, 页大小默认时 <code>16KB</code></li>
<li>从数据库中读取数据，是会先从磁盘读取到也放入缓冲池中，该过程称为将页 <code>Fix</code> 在缓冲池中</li>
<li>对于数据库中页的修改操作，是先修改缓冲区中的页(如果没有则使用步骤2进行加载)，然后使用一定的频率刷新到磁盘中，刷新数据到磁盘是通过 <code>Checkpoint</code> 机制实现</li>
</ol>
<h2 id="缓冲池内存数据对象"><a href="#缓冲池内存数据对象" class="headerlink" title="缓冲池内存数据对象"></a>缓冲池内存数据对象</h2><pre class="mermaid">flowchart TB
  subgraph A["InnoDB内存结构"]
    B["重做日志缓冲 \n(redo log buffer)"]
    C["额外内存"]
    subgraph D["缓冲池(innodb_buffer_pool)"]
      style D fill:green
      D1["数据页"]
      D2["索引页"]
      D3["插入缓冲"]
      D4["锁信息"]
      D5["自适应hash索引"]
      D6["数据字典信息"]
    end
    subgraph E["缓冲池(innodb_buffer_pool)"]
      style E fill:green
      E1["数据页"]
      E2["索引页"]
      E3["插入缓冲"]
      E4["锁信息"]
      E5["自适应hash索引"]
      E6["数据字典信息"]
    end
  end</pre>
<ol>
<li><code>InnoDB</code> 引擎允许有多个 <code>Buffer Pool</code> 对象， 每个页可以根据哈希值平均分配到不算的缓冲池中，这样可以检测数据库内部的资源竞争，增加数据库的并发能力， 可以通过 <code>innodb_buffer_pool_instances</code> 来进行设置，默认是1</li>
</ol>
<h2 id="LRU-List，Free-List-和-Flush-List"><a href="#LRU-List，Free-List-和-Flush-List" class="headerlink" title="LRU List，Free List 和 Flush List"></a>LRU List，Free List 和 Flush List</h2><h3 id="Free-List-空闲链表"><a href="#Free-List-空闲链表" class="headerlink" title="Free List 空闲链表"></a>Free List 空闲链表</h3><ol>
<li><code>Free List</code> 是 <code>InnoDB</code> 存储引擎中一个重要的数据结构，它用于管理 <code>Buffer Pool</code> 中的空闲页，当需要使用一个新的页的时候，会从空闲链表中获取一个页，在数据库刚运行时，所有的页都是空闲页</li>
</ol>
<h3 id="LRU-List-Latest-Recent-Used-最近最少使用"><a href="#LRU-List-Latest-Recent-Used-最近最少使用" class="headerlink" title="LRU List(Latest Recent Used) 最近最少使用"></a>LRU List(Latest Recent Used) 最近最少使用</h3><ol>
<li>数据库中的缓冲池是通过<code>LRU</code>（<code>Latest Recent Used</code>，最近最少使用）算法来管理缓存页的。即最频繁使用的页在 <code>LRU</code> 列表的前端，而最少使用的页在 <code>LRU</code> 列表的尾端</li>
<li>对于 <code>InnoDB</code> 而言，数据放在磁盘中就是数据页，当数据页加载到缓冲区中后就变成了缓存页，二者大小是相同的</li>
<li><code>InnoDB</code> 中根据 <strong>数据冷热分离思想</strong> 对 <code>LRU</code> 链表进行了优化，从磁盘中加载的数据页默认先放在 <code>LRU</code> 的冷数据区域，满足一定条件后才会将移动到热取数据区域（冷数据区域默认占 3&#x2F;8），可以通过 <code>innodb_old_blocks_pct</code> 参数修改</li>
<li><code>Mysql</code> 的预读机制可能会加载实际不会使用到的数据到缓冲池中，会占用缓冲池的空间</li>
<li>遇到类似 <code>select * from table</code> 这种全表扫描的语句，会加载大量的磁盘中的数据页到缓冲区中，但是这些数据可能只用一次就不用了，如果直接放在 <code>LRU</code> 链表头部可能会将其他常用的数据挤出缓冲区</li>
</ol>
<h3 id="Flush-列表"><a href="#Flush-列表" class="headerlink" title="Flush 列表"></a>Flush 列表</h3><ol>
<li><code>LRU</code> 链表中管理的缓存页被修改之后就会变成 <strong>脏页</strong>， 这些脏页最终是需要将其刷新到磁盘中的，所以需要将所有的脏页放在一起进行管理，于是 <code>Flush List</code> 就诞生了</li>
<li><code>LRU</code> 列表用来管理缓冲池中页的可用性，<code>Flush</code>列表用来管理将页刷新回磁盘，二者互不影响</li>
</ol>
<h2 id="重做日志缓冲"><a href="#重做日志缓冲" class="headerlink" title="重做日志缓冲"></a>重做日志缓冲</h2><blockquote>
<p>InnoDB 存储引擎的内存区域除了有缓冲池外，还有重做日志缓冲（redo log buffer）。InnoDB存储引擎首先将重做日志信息先放入到这个缓冲区，然后按一定频率将其刷新到重做日志文件。重做日志缓冲一般不需要设置得很大，因为一般情况下每一秒钟会将重做日志缓冲刷新到日志文件，因此用户只需要保证每秒产生的事务量在这个缓冲大小之内即可。该值可由配置参数innodb_log_buffer_size控制，默认为8MB</p>
</blockquote>
<h3 id="重做日志缓冲刷盘时机"><a href="#重做日志缓冲刷盘时机" class="headerlink" title="重做日志缓冲刷盘时机"></a>重做日志缓冲刷盘时机</h3><ol>
<li><code>Master Thread</code> 每一秒将重做日志缓冲刷新到重做日志文件；</li>
<li>每个事务提交时会将重做日志缓冲刷新到重做日志文件；</li>
<li>当重做日志缓冲池剩余空间小于1&#x2F;2时，重做日志缓冲刷新到重做日志文件。</li>
</ol>
<h2 id="Checkpoint-技术"><a href="#Checkpoint-技术" class="headerlink" title="Checkpoint 技术"></a>Checkpoint 技术</h2><h3 id="redo-产生背景"><a href="#redo-产生背景" class="headerlink" title="redo 产生背景"></a>redo 产生背景</h3><ol>
<li>数据页加载到缓冲区变成缓存页，缓存页被修改后就变成脏页，脏页最终是要刷回磁盘，如果缓存页有变化就刷盘那么性能很差（磁盘随机读写），如果很久都不刷盘，可能会有数据丢失的情况</li>
<li>为了避免数据丢失，很多系统普遍 <code>Write Ahead Log</code> 策略，即当事务提交时，先写重做日志，再修改页。当由于发生宕机而导致数据丢失时，通过重做日志来完成数据的恢复（日志是磁盘顺序写）</li>
</ol>
<h3 id="checkpoint-作用"><a href="#checkpoint-作用" class="headerlink" title="checkpoint 作用"></a>checkpoint 作用</h3><p>从上面两点可以知道 <code>redo</code> 日志的作用，但是 <code>redo</code> 日志文件也不可能无限大，即使可以无限大，最终数据恢复时间也很漫长，所以才引入 <code>checkpoint</code> 技术，主要解决以下几个问题</p>
<ol>
<li>缩短数据库的恢复时间</li>
<li>缓冲区不够用时，将脏页刷新到磁盘</li>
<li>重做日志不可用时，刷新脏页</li>
<li>因为重做日志不是无限大，而是循环使用，所以当设置的大小都被占用了，就需要将脏页数据刷盘</li>
</ol>
<p>可以认为 <code>checkpoint</code> 就是一个临界点，在 <code>checkpoint</code> 之前的数据都已经存在磁盘上了，在 <code>checkpoint</code> 之后的数据存在 <code>redo</code> 日志和缓存页中</p>
<h3 id="checkpoint-实现"><a href="#checkpoint-实现" class="headerlink" title="checkpoint 实现"></a>checkpoint 实现</h3><ol>
<li>对于 <code>InnoDB</code> 存储引擎而言，其是通过<code>LSN（Log Sequence Number）</code>来标记版本的， <code>LSN</code> 是8字节的数字， 每个数据页都有 <code>LSN</code></li>
<li>重做日志和 <code>Checkpoint</code> 都会 <code>LSN</code>, 可以通过命令 <code>show engine innodb status\G</code> 查看 <code>innodb</code> 引擎信息，其中就包含 <code>LSN</code>， 比如如下所示<figure class="highlight pgsql"><table><tr><td class="code"><pre><code class="hljs pgsql"><span class="hljs-keyword">Log</span> <span class="hljs-keyword">sequence</span> number <span class="hljs-number">92561351052</span> <br><span class="hljs-keyword">Log</span> flushed up <span class="hljs-keyword">to</span> <span class="hljs-number">92561351052</span> <br>Last <span class="hljs-keyword">checkpoint</span> at <span class="hljs-number">92561351052</span><br></code></pre></td></tr></table></figure></li>
<li>在 <code>InnoDB</code> 存储引擎内部，有两种 <code>Checkpoint</code></li>
<li><strong>Sharp Checkpoint</strong>: 发生在数据库关闭时，需要将所有的脏页都刷新会磁盘，对应的是 <code>innodb_fast_shutdown=1</code>, 也是默认的行为</li>
<li><strong>Fuzzy Checkpoint</strong>： <code>InnoDB</code> 存储引擎内部使用 <code>Fuzzy Checkpoint</code>进行页的刷新，即只刷新一部分脏页，而不是刷新所有的脏页回磁盘</li>
</ol>
<h4 id="Fuzzy-Checkpoint-刷盘时机"><a href="#Fuzzy-Checkpoint-刷盘时机" class="headerlink" title="Fuzzy Checkpoint 刷盘时机"></a>Fuzzy Checkpoint 刷盘时机</h4><ol>
<li>Master Thread Checkpoint</li>
<li>差不多以每秒或每十秒的速度从缓冲池的脏页列表中刷新一定比例的页回磁盘。这个过程是异步的</li>
<li>FLUSH_LRU_LIST Checkpoint</li>
<li><code>InnoDB</code> 存储引擎需要保证 <code>LRU</code> 列表中需要有差不多100个空闲页可供使用, 具体多少个可以通过参数 <code>innodb_lru_scan_depth</code> 控制</li>
<li>当发现 <code>LRU</code> 列表中没有那么多空闲页时，就需要将 <code>LRU</code> 尾端的页移除，如果这些页有脏页，就需要进行 <code>checkpoint</code></li>
<li>Async&#x2F;Sync Flush Checkpoint</li>
<li>当重做日志不可用时，需要从脏页中强制将一些页进行刷盘</li>
<li>Dirty Page too much Checkpoint</li>
<li>当脏页的数量太多时，为了保证缓冲区仍有空间可用，也会强制将一些脏页刷盘， 可以通过参数 <code>innodb_max_dirty_pages_pct</code> 控制脏页占比，默认是 <code>75%</code>， 即当缓冲池中脏页数量占到了 <code>75%</code> 会强制 <code>checkpoint</code></li>
</ol>
<h2 id="Master-线程"><a href="#Master-线程" class="headerlink" title="Master 线程"></a>Master 线程</h2><ol>
<li><p><code>Master Thread</code> 具有最高的线程优先级别。内部由多个循环（<code>loop</code>）组成</p>
</li>
<li><p><code>Master Thread</code> 会根据数据库运行的状态在 <code>loop</code>、<code>background loop</code>、<code>flush loop</code>和<code>suspend loop</code>中进行切换</p>
</li>
</ol>
<p><strong>主循环（loop）</strong></p>
<ul>
<li>每 <code>1s</code> 的操作<ol>
<li>日志缓冲刷新到磁盘，即使这个事务还没有提（总是）</li>
<li>合并插入缓冲（可能）</li>
<li>至多刷新100个 <code>InnoDB</code> 的缓冲池中的脏页磁盘（可能）</li>
<li>如果当前没有用户活动，则切换到<code>background loop</code>（可能）</li>
</ol>
</li>
<li>每 <code>10s</code> 的操作<ol>
<li>刷新100个脏页到磁盘（可能）</li>
<li>合并至多5个插入缓冲（总是）</li>
<li>将日志缓冲刷新到磁盘（总是）</li>
<li>删除无用的 <code>Undo</code> 页（总是）</li>
</ol>
</li>
</ul>
<p><strong>后台循环（backgroup loop）</strong></p>
<p>若当前没有用户活动（数据库空闲时）或者数据库关闭（<code>shutdown</code>），就会切换到这个循环, 会存在以下操作</p>
<ul>
<li>删除无用的 <code>Undo</code> 页（总是）</li>
<li>合并20个插入缓冲（总是）</li>
<li>跳回到主循环（总是）</li>
<li>不断刷新100个页直到符合条件（可能，跳转到<code>flush loop</code>中完成）</li>
</ul>
<p><strong>刷新循环（flush loop）</strong> </p>
<p>主要是将缓冲区脏页数据刷新到磁盘</p>
<p><strong>暂停循环（suspend loop）</strong><br>若 <code>flush loop</code> 中也没有什么事情可以做了，<code>InnoDB</code>存储引擎会切换到<code>suspend loop</code>，将<code>Master Thread</code>挂起，等待事件的发生</p>
<p>在 <code>InnoDB1.2.x</code>版本中从 <code>Master Thread</code> 线程分离到一个单独的<code> Page Cleaner Thread</code>，从而减轻了<code>Master Thread</code>的工作，同时进一步提高了系统的并发性</p>
<h2 id="插入缓冲-Insert-Buffer"><a href="#插入缓冲-Insert-Buffer" class="headerlink" title="插入缓冲 Insert Buffer"></a>插入缓冲 Insert Buffer</h2><h3 id="Insert-Buffer-在哪里"><a href="#Insert-Buffer-在哪里" class="headerlink" title="Insert Buffer 在哪里"></a>Insert Buffer 在哪里</h3><p><code>Insert Buffer</code>和数据页一样，也是物理页的一个组成部分（缓冲区中也有 <code>Insert Buffer</code>, 含义不同）</p>
<h3 id="为什么要有-Insert-Buffer"><a href="#为什么要有-Insert-Buffer" class="headerlink" title="为什么要有 Insert Buffer"></a>为什么要有 Insert Buffer</h3><ol>
<li>每个表都有一个主键，如果是自增的那么数据是按照主键顺序存储的，这种情况下数据新增是很快的，因为是有顺序的</li>
<li>如果主键不是自增的，或者其他非聚集索引（先理解成非主键索引）都可能不是自增的，在这种情况下，数据页的存放还是按主键进行顺序存放的，但是对于非聚集索引叶子节点的插入不再是顺序的了，这时就需要离散地访问非聚集索引页，由于随机读取的存在而导致了插入操作性能下降</li>
<li><code>Insert Buffer</code> 的作用就是对于非聚集索引的插入或更新操作，不是每一次直接插入到索引页中，而是先判断插入的非聚集索引页是否在缓冲池中，若在，则直接插入；若不在，则先放入到一个<code>Insert Buffer</code>对象中， 然后再以一定的频率和情况进行<code>Insert Buffer</code>和辅助索引页子节点的<code>merge</code>（合并）操作，这时通常能将多个插入合并到一个操作中（因为在一个索引页中），这就大大提高了对于非聚集索引插入的性能</li>
</ol>
<h3 id="使用-Insert-Buffer-需要满足的条件"><a href="#使用-Insert-Buffer-需要满足的条件" class="headerlink" title="使用 Insert Buffer 需要满足的条件"></a>使用 Insert Buffer 需要满足的条件</h3><ol>
<li>索引是辅助索引</li>
<li>索引不是唯一的： 如果索引唯一，就还需要查找判断操作，此时使用 <code>Insert Buffer</code> 就不能提升性能了</li>
</ol>
<p>所以 <code>Insert Buffer</code> 主要是为了提高索引页数据插入的性能</p>
]]></content>
      <categories>
        <category>存储</category>
        <category>Mysql</category>
      </categories>
      <tags>
        <tag>Mysql</tag>
      </tags>
  </entry>
  <entry>
    <title>InnoDB存储引擎之MVCC详解</title>
    <url>/2023/7aa9bac70e9b/index.html</url>
    <content><![CDATA[<span class="hide_first_mermaid">
<div class="mermaid-wrap"><pre class="mermaid-src" hidden>
  flowchart LR
    A
  </pre></div>
</span>


<h2 id="mysql8-查看事务的隔离级别"><a href="#mysql8-查看事务的隔离级别" class="headerlink" title="mysql8 查看事务的隔离级别"></a>mysql8 查看事务的隔离级别</h2><figure class="highlight sql"><table><tr><td class="code"><pre><code class="hljs sql"><span class="hljs-comment">-- 查看当前会话的隔离级别</span><br><span class="hljs-keyword">SELECT</span> @<span class="hljs-variable">@transaction_isolation</span>;<br><span class="hljs-comment">-- 查看全局的隔离级别</span><br><span class="hljs-keyword">SELECT</span> @<span class="hljs-variable">@global</span>.transaction_isolation;<br></code></pre></td></tr></table></figure>

<h2 id="undo-log-版本链"><a href="#undo-log-版本链" class="headerlink" title="undo log 版本链"></a>undo log 版本链</h2><ul>
<li>每一条数据都有两个隐藏字段<ul>
<li><code>trx_id</code>: 事务id</li>
<li><code>roll_pointer</code>: 指向了更新这个事务之前的 <code>undo log</code></li>
</ul>
</li>
</ul>
<h3 id="trx-id-的作用"><a href="#trx-id-的作用" class="headerlink" title="trx_id 的作用"></a>trx_id 的作用</h3><p>在 <code>InnoDB</code> 存储引擎中，<code>trx_id</code> 是一个用于<strong>标识事务的唯一标识符</strong>。<code>trx_id</code> 的生成时机是在事务开始时生成的</p>
<p>下面举例说明，比如新增一条数据，事务 id 为10，因为之前没有数据，所以 <code>roll_pointer</code> 指向 <code>null</code></p>
<pre class="mermaid">graph LR
    subgraph A["事务A"]
        direction LR
        A1["值A"]
        A2["trx_id=10"]
        A3["roll_pointer"]
        A1~~~A2~~~A3
    end</pre>

<p>接下来另外一个事务启动，更新同一条数据，将值从 A 改为 B, 此时对应数据状态为</p>
<pre class="mermaid">graph LR
    subgraph A["事务A"]
        direction LR
        A1["值A"]
        A2["trx_id=10"]
        A3["roll_pointer"]
        A1~~~A2~~~A3
    end
    subgraph B["事务B"]
        direction LR
        B1["值B"]
        B2["trx_id=15"]
        B3["roll_pointer"]
        B1~~~B2~~~B3
    end
    B3-.->A3
    style B3 fill:#8ff600
    style A3 fill:#8ff600</pre>

<p>后启动的事务的 <code>roll_pointer</code> 指向了前面的数据</p>
<p>当还有其他事务来修改这条数据时，对应的数据状态为</p>
<pre class="mermaid">graph LR
    subgraph A["事务A"]
        direction LR
        A1["值A"]
        A2["trx_id=10"]
        A3["roll_pointer"]
        A1~~~A2~~~A3
    end
    subgraph B["事务B"]
        direction LR
        B1["值B"]
        B2["trx_id=15"]
        B3["roll_pointer"]
        B1~~~B2~~~B3
    end
    subgraph C["事务C"]
        direction LR
        C1["值C"]
        C2["trx_id=20"]
        C3["roll_pointer"]
        C1~~~C2~~~C3
    end
    C3-.->B3-.->A3
    style B3 fill:#8ff600
    style A3 fill:#8ff600
    style C3 fill:#8ff600</pre>

<p>这就是 <code>undo log</code> 版本链，也是 <code>MVCC</code> 的实现基础， 另外一个实现基础是 <code>ReadView</code> 机制</p>
<h2 id="ReadView-机制"><a href="#ReadView-机制" class="headerlink" title="ReadView 机制"></a>ReadView 机制</h2><p>当执行一个事务的时候，就会生成一个 <code>ReadView</code>， <code>ReadView</code> 中比较重要的有四个字段</p>
<ol>
<li><code>m_ids</code>: 当前事务开始时，还有哪些事务也正在执行（即还提交的事务）</li>
<li><code>min_trx_id</code>: <code>m_ids</code> 中最小的值</li>
<li><code>max_trx_id</code>: <code>Mysql</code> 下一个要生成的事务id</li>
<li><code>creator_trx_id</code>: 当前事务的id</li>
</ol>
<h3 id="ReadView-生成时机"><a href="#ReadView-生成时机" class="headerlink" title="ReadView 生成时机"></a>ReadView 生成时机</h3><p>前面有提到 <code>trx_id</code> 的生成时机是在事务开始时生成的，而 <code>ReadView</code> 并不是在事务开始时生成的，而是在事务中执行读操作操作时才会生成 <code>ReadView</code></p>
<p>这也是为什么在当前事务的 <code>ReadView</code> 中可能会存在事务id 比当前事务id 大的事务存在</p>
<h2 id="RC（Read-Commited）隔离级别实现原理"><a href="#RC（Read-Commited）隔离级别实现原理" class="headerlink" title="RC（Read Commited）隔离级别实现原理"></a>RC（Read Commited）隔离级别实现原理</h2><p><code>RC</code> 隔离级别指的是一个事务中可以读取到其他事务提交的数据，其他事务未提交数据无法读取到</p>
<p>下面举例说明如何基于 <code>undo log</code> 和 <code>ReadView</code> 机制实现 <code>RC</code> 隔离级别</p>
<p>db 中某条数据的初始值 <code>trx_id=10</code><br>在某个时刻有事务 A 和 事务 B 同时操作这条数据，事务A 是查询，事务B是修改，我们的观察角度是事务A</p>
<p><strong>事务B修改了数据但未提交</strong><br>因为事务B 修改了数据，此时 <code>undo log</code> 版本链如下所示</p>
<pre class="mermaid">graph LR
    subgraph A["初始数据"]
        direction LR
        A1["初始值"]
        A2["trx_id=10"]
        A3["roll_pointer"]
        A1~~~A2~~~A3
    end
    subgraph B["事务B"]
        direction LR
        B1["值B"]
        B2["trx_id=15"]
        B3["roll_pointer"]
        B1~~~B2~~~B3
    end
    B3-.->A3
    style B3 fill:#8ff600
    style A3 fill:#8ff600</pre>

<p><strong>事务A读取数据</strong><br>事务A 执行 <code>select</code> 操作时，就会生成 <code>ReadView</code>， 此时 <code>ReadView</code> 涉及到的值如下所示<br>事务A(trx_id&#x3D;15), 事务B(trx_id&#x3D;20)<br><code>m_ids</code>: [15, 20]<br><code>min_trx_id</code>: 15<br><code>max_trx_id</code>: 21<br><code>creator_trx_id</code>: 15</p>
<p>读取到数据B, 这条数据的 <code>trx_id=20</code>, <code>trx_id</code> 比自己的事务id大，但是<strong>在</strong> <code>m_ids</code> 中，说明这条数据是和事务A 差不多同时启动的事务操作的，并且还没有提交，那么事务A 就不能进行读取，需要顺着 <code>undo log</code> 版本链继续寻找，<code>undo log</code> 的下一条数据的 <code>trx_id=10</code>, 比 <code>creator_trx_id=15</code> 小，说明是在事务A 开启之前就已经提交的，可以读取，所以读取到的是 <strong>初始值</strong> 而不是 <strong>数据B</strong></p>
<p><strong>事务B提交数据后事务A再读取</strong><br>对于 <code>RC</code> 隔离级别，是每次读取数据时都会生成 <code>ReadView</code>， 所以事务B 提交数据后，事务A 再读取时，就会生成一个新的 <code>ReadView</code>，此时 <code>ReadView</code> 涉及到的值如下所示<br>事务A(trx_id&#x3D;15)<br><code>m_ids</code>: [15]<br><code>min_trx_id</code>: 15<br><code>max_trx_id</code>: 21<br><code>creator_trx_id</code>: 15</p>
<p>读取到数据B, 这条数据的 <code>trx_id=20</code>, <code>trx_id</code> 比自己的事务id大，但是<strong>不在</strong> <code>m_ids</code> 中，说明这条数据是和事务A 差不多同时启动的事务操作的，并且已经提交，那么事务A 就可以进行读取，读取到的就是 <strong>数据B</strong></p>
<p><strong>总结：RC 隔离级别可以实现读取到其他事务提交的数据的核心是在当前事务中每次执行 <code>select</code> 中都会生成一个新的 <code>ReadView</code></strong></p>
<h2 id="RR（Repeatable-Read）隔离级别实现原理"><a href="#RR（Repeatable-Read）隔离级别实现原理" class="headerlink" title="RR（Repeatable Read）隔离级别实现原理"></a>RR（Repeatable Read）隔离级别实现原理</h2><p>在 <code>Mysql</code> 中 <code>RR</code>  隔离级别可以防止 幻读，不可重复读和幻读</p>
<p><code>RR</code> 和 <code>RC</code> 隔离级别实现上的区别是： <code>RR</code> 隔离级别在一个事务中只会在第一次查询时生成 <code>ReadView</code>，之后的查询只会使用当前事务的 <code>ReadView</code></p>
<p>下面举例说明如何基于 <code>undo log</code> 和 <code>ReadView</code> 机制实现 <code>RR</code> 隔离级别, 依然使用 <code>RC</code> 隔离级别的例子</p>
<p>db 中某条数据的初始值 <code>trx_id=10</code><br>在某个时刻有事务A 和 事务B 同时操作这条数据，事务A 是查询，事务B是修改，我们的观察角度是事务A</p>
<p><strong>事务B修改了数据但未提交</strong><br>因为事务 B 修改了数据，此时 <code>undo log</code> 版本链如下所示</p>
<pre class="mermaid">graph LR
    subgraph A["初始数据"]
        direction LR
        A1["初始值"]
        A2["trx_id=10"]
        A3["roll_pointer"]
        A1~~~A2~~~A3
    end
    subgraph B["事务B"]
        direction LR
        B1["值B"]
        B2["trx_id=15"]
        B3["roll_pointer"]
        B1~~~B2~~~B3
    end
    B3-.->A3
    style B3 fill:#8ff600
    style A3 fill:#8ff600</pre>

<p><strong>事务A读取数据</strong><br>事务 A 执行 <code>select</code> 操作时，就会生成 <code>ReadView</code>， 此时 <code>ReadView</code> 涉及到的值如下所示<br>事务A(trx_id&#x3D;15), 事务B(trx_id&#x3D;20)<br><code>m_ids</code>: [15, 20]<br><code>min_trx_id</code>: 15<br><code>max_trx_id</code>: 21<br><code>creator_trx_id</code>: 15</p>
<p>读取到数据B, 这条数据的 <code>trx_id=20</code>, <code>trx_id</code> 比自己的事务id大，但是<strong>在</strong> <code>m_ids</code> 中，说明这条数据是和事务 A 差不多同时启动的事务操作的，并且还没有提交，那么事务 A 就不能进行读取，需要顺着 <code>undo log</code> 版本链继续寻找，<code>undo log</code> 的下一条数据的 <code>trx_id=10</code>, 比 <code>creator_trx_id=15</code> 小，说明是在事务 A 开启之前就已经提交的，可以读取，所以读取到的是 <strong>初始值</strong> 而不是 <strong>数据B</strong></p>
<p><strong>事务B提交数据后事务A再读取</strong><br>对于 <code>RR</code> 隔离级别，只会在第一次 <code>select</code> 时生成 <code>ReadView</code>， 所以事务 B 提交数据后，事务 A 再读取时，使用的还是之前那个 <code>ReadView</code>，此时 <code>ReadView</code> 涉及到的值如下所示<br>事务A(trx_id&#x3D;15)<br><code>m_ids</code>: [15, 20]<br><code>min_trx_id</code>: 15<br><code>max_trx_id</code>: 21<br><code>creator_trx_id</code>: 15</p>
<p>读取到数据B, 这条数据的 <code>trx_id=20</code>, <code>trx_id</code> 比自己的事务id大，并且在 <code>min_trx_id</code> 和 <code>max_trx_id</code> 之间，同时还在 <code>m_ids</code> 中，说明这条数据是和事务 A 差不多同时启动的事务操作的，事务 A 不可以读取，所以顺着 <code>undo log</code> 版本链继续往前找，找到 <code>trx_id=10</code>, 说明是在事务 A 开启之前就已经提交的，可以读取，所以读取到的是 <strong>初始值</strong></p>
<h3 id="为什么-Mysql-的-RR-隔离级别可以防止幻读"><a href="#为什么-Mysql-的-RR-隔离级别可以防止幻读" class="headerlink" title="为什么 Mysql 的 RR 隔离级别可以防止幻读"></a>为什么 Mysql 的 RR 隔离级别可以防止幻读</h3><p>在 <ol><li><a href="/2023/ba87d5c56893/index.html" title="Mysql事务隔离级别">Mysql事务隔离级别</a></li></ol> 中有提到在 <code>Sql</code> 标准中，<code>RR</code> 隔离级别下不能防止幻读，但是在 <code>Mysql</code> 中，<code>RR</code> 隔离级别下可以访问幻读，下面举例说明</p>
<p>比如下面的 <code>sql</code> 语句可以查询出两条数据</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><code class="hljs Sql"><span class="hljs-keyword">select</span> <span class="hljs-operator">*</span> <span class="hljs-keyword">from</span> student <span class="hljs-keyword">where</span> id <span class="hljs-operator">&gt;</span> <span class="hljs-number">10</span>;<br></code></pre></td></tr></table></figure>
<p>数据初始值为</p>
<table>
<thead>
<tr>
<th>trx_id（隐藏字段）</th>
<th>id</th>
</tr>
</thead>
<tbody><tr>
<td>10</td>
<td>11</td>
</tr>
<tr>
<td>15</td>
<td>12</td>
</tr>
</tbody></table>
<p>现在有事务 A， B 同时操作，事务 A 是查询，事务 B 是新增数据</p>
<p><strong>事务A 第一次查询</strong><br>此时假设事务 A 的 <code>ReadView</code> 如下<br>事务A(trx_id&#x3D;16), 事务B(trx_id&#x3D;17)<br><code>m_ids</code>: [16, 17]<br><code>min_trx_id</code>: 16<br><code>max_trx_id</code>: 18<br><code>creator_trx_id</code>: 16</p>
<p>因为事务 B 还没有真正插入数据，所以事务 A 查询的就是上面展示的两条数据</p>
<p><strong>事务B 新增数据并提交</strong></p>
<figure class="highlight sql"><table><tr><td class="code"><pre><code class="hljs SQL"><span class="hljs-keyword">insert</span> <span class="hljs-keyword">into</span> student(id) <span class="hljs-keyword">values</span>(<span class="hljs-number">13</span>);<br><span class="hljs-keyword">commit</span>;<br></code></pre></td></tr></table></figure>
<p>此时数据库中有三条数据</p>
<table>
<thead>
<tr>
<th>trx_id（隐藏字段）</th>
<th>id</th>
</tr>
</thead>
<tbody><tr>
<td>10</td>
<td>11</td>
</tr>
<tr>
<td>15</td>
<td>12</td>
</tr>
<tr>
<td>17</td>
<td>13</td>
</tr>
</tbody></table>
<p><strong>事务A 第二次查询</strong><br>此时事务 A 的 <code>ReadVew</code> 还是和之前一样，并不会改变<br><code>m_ids</code>: [16, 17]<br><code>min_trx_id</code>: 16<br><code>max_trx_id</code>: 18<br><code>creator_trx_id</code>: 16<br>当执行下面的 <code>sql</code> 时可以查询出 3 条数据</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><code class="hljs Sql"><span class="hljs-keyword">select</span> <span class="hljs-operator">*</span> <span class="hljs-keyword">from</span> student <span class="hljs-keyword">where</span> id <span class="hljs-operator">&gt;</span> <span class="hljs-number">10</span>;<br></code></pre></td></tr></table></figure>
<p>但是其中有一条数据的 <code>trx_id=17</code>, 17 在当前 <code>ReadView</code> 的 <code>min_trx_id</code> 和 <code>max_trx_id</code> 之间，说明是在事务 A 开启之后才新增的数据，这条数据就不能展示出来</p>
<p><strong>总结：<code>RR</code> 隔离级别在一个事务中只会在第一次查询时生成 <code>ReadView</code>，之后的查询只会使用当前事务的 <code>ReadView</code></strong></p>
]]></content>
      <categories>
        <category>存储</category>
        <category>Mysql</category>
      </categories>
      <tags>
        <tag>Mysql</tag>
      </tags>
  </entry>
  <entry>
    <title>Mysql事务隔离级别</title>
    <url>/2023/ba87d5c56893/index.html</url>
    <content><![CDATA[<h2 id="事务的四个特点"><a href="#事务的四个特点" class="headerlink" title="事务的四个特点"></a>事务的四个特点</h2><ol>
<li>原子性（Atomicity）：事务是一个原子操作单元，要么全部执行，要么全部不执行，不会出现部分执行的情况。</li>
<li>一致性（Consistency）：事务执行前后，数据库的完整性约束没有被破坏，即数据库从一个一致性状态转换到另一个一致性状态。</li>
<li>隔离性（Isolation）：多个事务并发执行时，每个事务的操作应该与其他事务的操作相互隔离，不会相互干扰。</li>
<li>持久性（Durability）：一旦事务提交，其所做的修改会永久保存在数据库中，即使系统发生故障也不会丢失。</li>
</ol>
<h2 id="并发读写数据带来的问题"><a href="#并发读写数据带来的问题" class="headerlink" title="并发读写数据带来的问题"></a>并发读写数据带来的问题</h2><ol>
<li>脏写</li>
<li>脏读</li>
<li>不可重复读</li>
<li>幻读</li>
</ol>
<p><strong>脏写</strong></p>
<ol>
<li>事务 A，B 同时开始准备更新一条数据</li>
<li>事务 A 更新</li>
<li>事务 B 更新</li>
<li>事务 A 回滚，将事务 B 更新的数据也回滚了</li>
</ol>
<p>对于事务 B 来说，自己更新了，但是看到的是没有更新的数据，这就是脏写。</p>
<p><strong>脏读</strong></p>
<ol>
<li>事务 A，B 同时开启准备处理数据</li>
<li>事务 B 更新了数据，但是没有提交</li>
<li>事务 A 拿到事务 B 更新的数据</li>
<li>事务 B 回滚</li>
<li>事务 A 再次查询，发现数据变了</li>
</ol>
<p>对于事务 A 来说，因为读取到了事务 B 还没有提交的数据，导致自己先后两次读取的结果不同，这就是脏读。</p>
<p><strong>不可重复读</strong></p>
<ol>
<li>事务 A，B 同时开始准备处理数据</li>
<li>事务 A 读取数据 A（事务 A 一直开启着）</li>
<li>事务 B 将数据 A 更新成数据 B</li>
<li>事务 A 再次读取，结果变成了 数据B, 在同一个事务中，事务 A 两次读取的结果不一致，这就是不可重复读</li>
</ol>
<p>总结：一个事务读取到了另外一个事务中<strong>提交</strong>的 <code>update</code> 的数据</p>
<p><strong>幻读</strong><br>指一个事务读取到了另外一个事务中<strong>提交</strong>的 <code>insert</code> 的数据</p>
<h2 id="Sql-隔离级别"><a href="#Sql-隔离级别" class="headerlink" title="Sql 隔离级别"></a>Sql 隔离级别</h2><p>针对以上在多事务并发时可能出现的问题，在 <code>Sql</code> 标准中规定了几种隔离级别，用来解决这些问题</p>
<table>
<thead>
<tr>
<th>隔离级别</th>
<th>脏读</th>
<th>不可重复读</th>
<th>幻读</th>
</tr>
</thead>
<tbody><tr>
<td>读未提交（Read uncommitted）</td>
<td>可能</td>
<td>可能</td>
<td>可能</td>
</tr>
<tr>
<td>读已提交（Read committed）</td>
<td>不可能</td>
<td>可能</td>
<td>可能</td>
</tr>
<tr>
<td>可重复读（Repeatable read）</td>
<td>不可能</td>
<td>不可能</td>
<td>可能</td>
</tr>
<tr>
<td>串行化</td>
<td>不可能</td>
<td>不可能</td>
<td>不可能</td>
</tr>
</tbody></table>
<p>在 <code>Sql</code> 标准的隔离级别中，<code>RR</code> 隔离级别是用来保证同一个事务中对同一行数据的多次查询，不会读取到不一样的数据，但是针对范围查询来说，如果在查询范围内新增了数据并且还提交了，依然是可以被读取到的，所以 <code>RR</code> 隔离级别还是可能发生 <strong>幻读</strong></p>
<p>在 <code>Mysql</code> 的 <code>RR</code> 隔离级别下不会发生 <strong>幻读</strong>，具体原因可以参考</p>
<ol><li><a href="/2023/7aa9bac70e9b/index.html" title="InnoDB存储引擎之MVCC详解">InnoDB存储引擎之MVCC详解</a></li></ol>

<h2 id="查看-Mysql8-隔离级别"><a href="#查看-Mysql8-隔离级别" class="headerlink" title="查看 Mysql8 隔离级别"></a>查看 Mysql8 隔离级别</h2><figure class="highlight sql"><table><tr><td class="code"><pre><code class="hljs sql"><span class="hljs-keyword">SELECT</span> @<span class="hljs-variable">@transaction_isolation</span>;<br></code></pre></td></tr></table></figure>


]]></content>
      <categories>
        <category>存储</category>
        <category>Mysql</category>
      </categories>
      <tags>
        <tag>Mysql</tag>
      </tags>
  </entry>
  <entry>
    <title>Mysql整体架构</title>
    <url>/2023/fd974035a1b5/index.html</url>
    <content><![CDATA[<span class="hide_first_mermaid">
<div class="mermaid-wrap"><pre class="mermaid-src" hidden>
  flowchart LR
    A
  </pre></div>
</span>

<pre class="mermaid">flowchart LR
    K1["客户端连接"]
    K2["客户端连接"]
    K1 & K2--1--->A
    subgraph A["Mysql服务器"]
        direction LR
        subgraph A1["连接池"]
            direction TB
            A11[连接]
            A12[连接]
            A13[连接]
            A11~~~A12~~~A13
        end
        subgraph A3["线程"]
        end
        
        A1--"2(获取sql语句)"-->A3

        subgraph A2["sql相关处理"]
            direction TB
            A23["3(sql接口)"]
            A24["4(sql解析器)"]
            A25["5(查询优化器)"]
            A26["6(执行器)"]
            A23-->A24-->A25-->A26
        end

        A3-->A2
        subgraph A4["7(存储引擎)"]
        end
        A2-->A4
    end</pre>
<p>下面按照上图中标的数字的顺序进行梳理</p>
<h2 id="1-连接池"><a href="#1-连接池" class="headerlink" title="1 连接池"></a>1 连接池</h2><ol>
<li>我们平时所说的连接池一般都是指应用端的连接池，比如一个 <code>Java</code> 应用连接数据库时，<code>Java</code> 应用会有一个连接池去连接数据库</li>
<li><code>Mysql</code> 服务器自身也是有一个连接池来和客户端进行连接，这个连接池管理了各种系统跟这台数据库服务器建立的所有连接</li>
</ol>
<h2 id="2-线程处理连接"><a href="#2-线程处理连接" class="headerlink" title="2 线程处理连接"></a>2 线程处理连接</h2><p>对于网络连接一般都是需要单独的线程进行处理，这个线程来监听请求以及获取请求数据</p>
<h2 id="3-sql接口"><a href="#3-sql接口" class="headerlink" title="3 sql接口"></a>3 sql接口</h2><ol>
<li><code>MySQL</code> 的工作线程接收到<code>SQL</code>语句之后，就会转交给<code>SQL</code>接口去执行</li>
<li>这种设计和 <code>java</code> 类似，也是面向接口编程，外部使用只需要和接口打交道，屏蔽了内部复杂的实现逻辑</li>
</ol>
<h2 id="4-sql解析器"><a href="#4-sql解析器" class="headerlink" title="4 sql解析器"></a>4 sql解析器</h2><ol>
<li>查询解析器<code>(Parser)</code>就是负责对<code>SQL</code>语句进行解析, 这里的解析的含义其实就是按照 <code>SQL</code> 语法对 <code>SQL</code> 进行拆解， 比如下面的一个 <code>SQL</code></li>
</ol>
<figure class="highlight sql"><table><tr><td class="code"><pre><code class="hljs sql"><span class="hljs-keyword">select</span> <span class="hljs-operator">*</span> <span class="hljs-keyword">from</span> <span class="hljs-keyword">user</span> <span class="hljs-keyword">where</span> id <span class="hljs-operator">=</span> <span class="hljs-number">1</span>;<br></code></pre></td></tr></table></figure>
<p>上面这个 <code>SQL</code> 语句只是一个字符串而已，但是通过 <strong>查询解析器</strong> 后，就可以知道 <code>user</code> 对应的是表，<code>id</code> 对应的是表里面的一个字段</p>
<h2 id="5-查询优化器"><a href="#5-查询优化器" class="headerlink" title="5 查询优化器"></a>5 查询优化器</h2><ol>
<li>查询优化器的作用是 <strong>选择最优的查询路径</strong></li>
<li>一条 <code>SQL</code> 语句可以有多个方式达到最终的效果，还是已 <code>select * from user where id = 1;</code> 这个 <code>SQL</code> 语句举例<ol>
<li>第一种方式是可以先获取到所有数据，然后根据 <code>id=1</code> 这个条件去过滤，再将结果返回</li>
<li>第二种方式也可以是先拿到 <code>id=1</code> 这个条件，然后直接定位 <code>id=1</code> 的数据，再将结果返回</li>
</ol>
</li>
</ol>
<h2 id="6-执行器"><a href="#6-执行器" class="headerlink" title="6 执行器"></a>6 执行器</h2><ol>
<li>执行器是根据执行计划调用存储引擎的接口</li>
<li>执行器会根据优化器选择的执行方案，按照一定的顺序去调用存储引擎的接口，把<code>SQL</code>语句的逻辑给执行</li>
</ol>
<h2 id="7-存储引擎"><a href="#7-存储引擎" class="headerlink" title="7 存储引擎"></a>7 存储引擎</h2><p>存储引擎其实就是真正执行<code>SQL</code>语句的，他会按照一定的步骤去查询内存缓存数据，更新磁盘数据，查询磁盘数据等操作</p>
]]></content>
      <categories>
        <category>存储</category>
        <category>Mysql</category>
      </categories>
      <tags>
        <tag>Mysql</tag>
      </tags>
  </entry>
  <entry>
    <title>Mysql锁机制详解</title>
    <url>/2023/fefcb42b5b82/index.html</url>
    <content><![CDATA[<h2 id="Mysql-中锁的概念"><a href="#Mysql-中锁的概念" class="headerlink" title="Mysql 中锁的概念"></a>Mysql 中锁的概念</h2><p>锁是数据库系统区别于文件系统的一个关键特性。锁机制用于管理对共享资源的并发访问</p>
<h2 id="Mysql-中与锁相关的表"><a href="#Mysql-中与锁相关的表" class="headerlink" title="Mysql 中与锁相关的表"></a>Mysql 中与锁相关的表</h2><h3 id="Mysql5-版本"><a href="#Mysql5-版本" class="headerlink" title="Mysql5.* 版本"></a>Mysql5.* 版本</h3><ol>
<li><code>information_schema.INNODB_TRX</code></li>
<li><code>information_schema.INNODB_LOCKS</code></li>
<li><code>information_schema.INNODB_LOCK_WAITS</code></li>
</ol>
<h3 id="Mysql8-版本"><a href="#Mysql8-版本" class="headerlink" title="Mysql8 版本"></a>Mysql8 版本</h3><ol>
<li><code>information_schema.INNODB_TRX</code></li>
<li><code>performance_schema.INNODB_LOCKS</code></li>
<li><code>performance_schema.INNODB_LOCK_WAITS</code></li>
</ol>
<h2 id="共享锁和互斥锁"><a href="#共享锁和互斥锁" class="headerlink" title="共享锁和互斥锁"></a>共享锁和互斥锁</h2><p>共享锁简写为      S<br>意向共享锁简写为  IS<br>排他锁简写为      X<br>意向排他锁简写为  IX</p>
<table>
<thead>
<tr>
<th>锁类型</th>
<th>独占锁</th>
<th>共享锁</th>
</tr>
</thead>
<tbody><tr>
<td>独占锁</td>
<td>互斥</td>
<td>互斥</td>
</tr>
<tr>
<td>共享锁</td>
<td>互斥</td>
<td>不互斥</td>
</tr>
</tbody></table>
<ul>
<li><code>Mysql</code> 查询操作默认默认不会加锁</li>
<li><code>Mysql</code> 在读和写之间不会互斥，因为对于读操作是直接使用 <code>MVCC</code> 来实现</li>
<li>手动给 <code>select</code> 增加共享锁的方式是  <code>select * from table_name lock in share mode;</code></li>
<li>手动给 <code>select</code> 增加独占锁的方式是   <code>select * from table_name for update;</code></li>
</ul>
<h2 id="表级锁"><a href="#表级锁" class="headerlink" title="表级锁"></a>表级锁</h2><p>表级锁分为两种，分别是 <strong>表锁</strong> 和 <strong>表级意向锁</strong></p>
<p><strong>表锁</strong></p>
<ol>
<li>表级共享锁： <code>LOCK TABLES xxx READ</code></li>
<li>表级独占锁： <code>LOCK TABLES xxx WRITE</code></li>
</ol>
<p>可以使用 <code>unlock tables;</code> 来释放所有的表级锁</p>
<p><strong>表级意向锁</strong></p>
<ul>
<li>更新数据时，除了会增加行级锁外，还会增加一个 表级意向独占锁</li>
<li>在一个事务中，如果执行了查询操作，那么会在对应的表级加一个意向共享锁</li>
</ul>
<h3 id="两种表级锁的互斥关系"><a href="#两种表级锁的互斥关系" class="headerlink" title="两种表级锁的互斥关系"></a>两种表级锁的互斥关系</h3><p><strong>手动添加的表锁</strong>和表级意向锁是存在互斥关系的，对应的互斥关系如下</p>
<table>
<thead>
<tr>
<th align="center">锁类型(都是表级)</th>
<th align="center">独占锁</th>
<th align="center">意向独占锁</th>
<th align="center">共享锁</th>
<th align="center">意向共享锁</th>
</tr>
</thead>
<tbody><tr>
<td align="center">独占锁</td>
<td align="center">互斥</td>
<td align="center">互斥</td>
<td align="center">互斥</td>
<td align="center">互斥</td>
</tr>
<tr>
<td align="center">意向独占锁</td>
<td align="center">互斥</td>
<td align="center">不互斥</td>
<td align="center">互斥</td>
<td align="center">不互斥</td>
</tr>
<tr>
<td align="center">共享锁</td>
<td align="center">互斥</td>
<td align="center">互斥</td>
<td align="center">不互斥</td>
<td align="center">不互斥</td>
</tr>
<tr>
<td align="center">意向共享锁</td>
<td align="center">互斥</td>
<td align="center">不互斥</td>
<td align="center">不互斥</td>
<td align="center">不互斥</td>
</tr>
</tbody></table>
<h3 id="表级意向锁的作用"><a href="#表级意向锁的作用" class="headerlink" title="表级意向锁的作用"></a>表级意向锁的作用</h3><ul>
<li><code>InnoDB</code> 存储引擎对标的行记录添加 <code>S</code> 锁之前，会先在表级别添加一个 <code>IS</code> 锁</li>
<li><code>InnoDB</code> 存储引擎对标的行记录添加 <code>X</code> 锁之前，会先在表级别添加一个 <code>IX</code> 锁 </li>
<li><code>IS</code> 锁和 <code>IX</code> 锁的作用是为了后续在加表级别的 <code>S</code> 锁和 <code>X</code> 锁时判断表中是否有已经被加锁的记录，以避免用遍历的方式来查看表中有没有上锁的记录，下面举例说明</li>
</ul>
<p>事务 A 修改一条数据，所以事务 A 会给这条数据增加 <strong>行锁</strong>(下面会讲解)</p>
<p>事务 B 想要添加表级排他锁，因为是表级，所以可以理解成会给所有行都上锁，因为事务 A 已经上锁成功，事务 B 肯定是无法加锁成功的，这点没有疑问，问题就是判断的过程，事务 B 怎么知道哪一行加锁了呢? 那只能一行一行去判断了，这样效率就很低了，所以才有了意向锁，也就是说实际情况会是下面这样的</p>
<p>事务 A 修改一条数据，所以事务 A 会给这条数据增加 <strong>行锁</strong>(下面会讲解)，同时会给这张表加一个意向排他锁</p>
<p>事务 B 想要添加表级排他锁，一看发现这个表上面已经存在了 意向排他锁，根据上面的互斥关系表，可以知道，表级排他锁和表级意向排他锁是互斥的，那事务 B 就不能加锁成功了，这样只需要判断一次，效率明显提高</p>
<h2 id="行级锁"><a href="#行级锁" class="headerlink" title="行级锁"></a>行级锁</h2><ul>
<li>前面讲解的是表级锁，表级意向锁是 <code>InnoDB</code> 自动帮我们添加的，修改数据时会自动帮我们添加 <strong>表级意向独占锁</strong>，至于手动添加的情况比较少，接下来需要讲解的就是行级锁了</li>
<li>行级锁锁住的是索引</li>
</ul>
<h3 id="行级锁的分类"><a href="#行级锁的分类" class="headerlink" title="行级锁的分类"></a>行级锁的分类</h3><ul>
<li><code>Record Locks</code>： 记录锁， 官方正式的名称是 <code>LOCK_REC_NOT_GAP</code>，对对应的索引记录项加锁， <strong>表中展示的是 <code>X,REC_NOT_GAP</code></strong></li>
<li><code>Gap Locks</code>： 间隙锁，锁住的是索引之间的间隙，** 表中展示的是 <code>X,GAP</code>**</li>
<li><code>Next-key Lock</code>： 邻键锁 <code>Next-key Lock</code> &#x3D; <code>Gap Locks</code> + <code>Record Locks</code>, <strong>表中展示的是 <code>X</code></strong></li>
<li><code>Insert Intention Locks</code>： 插入数据时需要插入的位置是否被别的事务添加了 <code>Gap</code> 锁（也就是 <code>Gap Locks</code> 或 <code>Next-key Lock</code> ），如果有的话，当前事务的插入操作需要等待，直到拥有 <code>Gap</code> 锁的事务提交。此时对于等待的那个事务就会添加一个 <code>Insert Intention Locks</code></li>
</ul>
<h2 id="Mysql8-REPEATABLE-READ-隔离级别下锁示例"><a href="#Mysql8-REPEATABLE-READ-隔离级别下锁示例" class="headerlink" title="Mysql8 REPEATABLE-READ 隔离级别下锁示例"></a>Mysql8 REPEATABLE-READ 隔离级别下锁示例</h2><p>创建测试表</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><code class="hljs sql"><span class="hljs-keyword">create</span> <span class="hljs-keyword">table</span> student<br>(<br>    id   <span class="hljs-type">int</span>          <span class="hljs-keyword">not</span> <span class="hljs-keyword">null</span><br>        <span class="hljs-keyword">primary</span> key,<br>    name <span class="hljs-type">varchar</span>(<span class="hljs-number">100</span>) <span class="hljs-keyword">null</span>,<br>    id_card <span class="hljs-type">varchar</span>(<span class="hljs-number">50</span>) <span class="hljs-keyword">not</span> <span class="hljs-keyword">null</span><br>) engine <span class="hljs-operator">=</span> InnoDB;<br><br><span class="hljs-keyword">create</span> index s_name <span class="hljs-keyword">on</span> student (name);<br><span class="hljs-keyword">create</span> <span class="hljs-keyword">unique</span> index s_id_card <span class="hljs-keyword">on</span> student(id_card);<br></code></pre></td></tr></table></figure>
<ul>
<li>表中有三个字段，一个是主键 <code>id</code></li>
<li>针对 <code>name</code> 字段创建了一个普通索引</li>
<li>针对 <code>id_card</code> 字段创建了一个唯一索引</li>
</ul>
<p>插入测试数据</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><code class="hljs Sql"><span class="hljs-keyword">insert</span> <span class="hljs-keyword">into</span> student(id, name, id_card) <span class="hljs-keyword">values</span> (<span class="hljs-number">1</span>, <span class="hljs-string">&#x27;name1&#x27;</span>, <span class="hljs-string">&#x27;1_card&#x27;</span>);<br><span class="hljs-keyword">insert</span> <span class="hljs-keyword">into</span> student(id, name, id_card) <span class="hljs-keyword">values</span> (<span class="hljs-number">5</span>, <span class="hljs-string">&#x27;name5&#x27;</span>, <span class="hljs-string">&#x27;5_card&#x27;</span>);<br><span class="hljs-keyword">insert</span> <span class="hljs-keyword">into</span> student(id, name, id_card) <span class="hljs-keyword">values</span> (<span class="hljs-number">10</span>, <span class="hljs-string">&#x27;name10&#x27;</span>, <span class="hljs-string">&#x27;10_card&#x27;</span>);<br></code></pre></td></tr></table></figure>

<p>先在 <code>student</code> 表针对 <code>id</code> 字段的邻键锁为</p>
<ul>
<li>（-∞， 1]</li>
<li>（1， 5]</li>
<li>（5， 10]</li>
<li>（10， +∞]  +∞ 使用的是 <code>supremum</code> 代替</li>
</ul>
<h3 id="测试前关闭事务的自动提交"><a href="#测试前关闭事务的自动提交" class="headerlink" title="测试前关闭事务的自动提交"></a>测试前关闭事务的自动提交</h3><p>查看事务自动提交状态</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><code class="hljs Sql"><span class="hljs-keyword">SHOW</span> VARIABLES <span class="hljs-keyword">LIKE</span> <span class="hljs-string">&#x27;autocommit&#x27;</span>;<br></code></pre></td></tr></table></figure>

<p>关闭事务自动提交</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><code class="hljs Sql"><span class="hljs-keyword">SET</span> autocommit <span class="hljs-operator">=</span> OFF;<br></code></pre></td></tr></table></figure>
<p>也可以显式开启事务，即使用 <code>begin</code> 开启事务，然后使用 <code>commit</code> 提交事务，使用 <code>rollback</code> 回滚事务</p>
<h3 id="测试前修改锁等待超时时间"><a href="#测试前修改锁等待超时时间" class="headerlink" title="测试前修改锁等待超时时间"></a>测试前修改锁等待超时时间</h3><figure class="highlight sql"><table><tr><td class="code"><pre><code class="hljs Sql"><span class="hljs-comment">-- 查看锁等待超时时间</span><br><span class="hljs-keyword">SHOW</span> VARIABLES <span class="hljs-keyword">LIKE</span> <span class="hljs-string">&#x27;innodb_lock_wait_timeout&#x27;</span>;<br><span class="hljs-comment">-- 设置锁等待超时时间，默认是50，单位是s，这里设置 600s, 没有添加 global 就是当前 会话有效</span><br><span class="hljs-keyword">set</span> <span class="hljs-keyword">global</span> innodb_lock_wait_timeout<span class="hljs-operator">=</span><span class="hljs-number">600</span>;<br></code></pre></td></tr></table></figure>

<h3 id="使用主键进行操作的场景"><a href="#使用主键进行操作的场景" class="headerlink" title="使用主键进行操作的场景"></a>使用主键进行操作的场景</h3><p>需要开启三个窗口，以下简称为 <strong>窗口1</strong>，<strong>窗口2</strong>，<strong>窗口3</strong>，其中第三个窗口可以不用关闭自动提交，这个窗口主要是来查询 <code>information_schema.INNODB_TRX</code> 和 <code>performance_schema.data_locks</code> 观察锁状态</p>
<ol>
<li><p><strong>窗口1</strong> 执行</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><code class="hljs Sql"><span class="hljs-keyword">update</span> student <span class="hljs-keyword">set</span> name <span class="hljs-operator">=</span> <span class="hljs-string">&#x27;name5_1&#x27;</span> <span class="hljs-keyword">where</span> id <span class="hljs-operator">=</span> <span class="hljs-number">5</span>;<br></code></pre></td></tr></table></figure>
</li>
<li><p><strong>窗口3</strong> 执行下面语句查看锁状态</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><code class="hljs Sql"><span class="hljs-keyword">select</span> ENGINE_LOCK_ID, ENGINE_TRANSACTION_ID, INDEX_NAME,LOCK_TYPE, LOCK_MODE, LOCK_DATA <span class="hljs-keyword">from</span> performance_schema.data_locks;<br></code></pre></td></tr></table></figure>
<p>结果如下</p>
<table>
<thead>
<tr>
<th align="left">ENGINE_LOCK_ID</th>
<th align="left">ENGINE_TRANSACTION_ID</th>
<th align="left">INDEX_NAME</th>
<th align="left">LOCK_TYPE</th>
<th align="left">LOCK_MODE</th>
<th align="left">LOCK_DATA</th>
</tr>
</thead>
<tbody><tr>
<td align="left">1853609022608:1067:1853614658072</td>
<td align="left">4188</td>
<td align="left">null</td>
<td align="left">TABLE</td>
<td align="left">IX</td>
<td align="left">null</td>
</tr>
<tr>
<td align="left">1853609022608:5:4:5:1853598537752</td>
<td align="left">4188</td>
<td align="left">PRIMARY</td>
<td align="left">RECORD</td>
<td align="left">X,REC_NOT_GAP</td>
<td align="left">5</td>
</tr>
</tbody></table>
</li>
</ol>
<p>可以看到，使用 <code>id</code> 条件更新，会给表增加 <code>IX</code> 锁，给 <code>id=5</code> 这条记录增加 <code>X,REC_NOT_GAP</code> ，这就是 <code>Record Lock</code></p>
<ol start="3">
<li><p><strong>窗口2</strong> 执行下面语句（在这里窗口1的事务并没有提交）</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><code class="hljs Sql"><span class="hljs-keyword">update</span> student <span class="hljs-keyword">set</span> name <span class="hljs-operator">=</span> <span class="hljs-string">&#x27;name5_2&#x27;</span> <span class="hljs-keyword">where</span> id <span class="hljs-operator">=</span> <span class="hljs-number">5</span>;<br></code></pre></td></tr></table></figure>
</li>
<li><p><strong>窗口3</strong> 执行下面语句查看锁状态</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><code class="hljs Sql"><span class="hljs-keyword">select</span> ENGINE_LOCK_ID, ENGINE_TRANSACTION_ID, INDEX_NAME,LOCK_TYPE, LOCK_MODE, LOCK_DATA <span class="hljs-keyword">from</span> performance_schema.data_locks;<br></code></pre></td></tr></table></figure>
<table>
<thead>
<tr>
<th align="left">ENGINE_LOCK_ID</th>
<th align="left">ENGINE_TRANSACTION_ID</th>
<th align="left">INDEX_NAME</th>
<th align="left">LOCK_TYPE</th>
<th align="left">LOCK_MODE</th>
<th align="left">LOCK_DATA</th>
</tr>
</thead>
<tbody><tr>
<td align="left">1853609023384:1067:1853614658840</td>
<td align="left">4193</td>
<td align="left">null</td>
<td align="left">TABLE</td>
<td align="left">IX</td>
<td align="left">null</td>
</tr>
<tr>
<td align="left">1853609023384:5:4:5:1853598540824</td>
<td align="left">4193</td>
<td align="left">PRIMARY</td>
<td align="left">RECORD</td>
<td align="left">X,REC_NOT_GAP</td>
<td align="left">5</td>
</tr>
<tr>
<td align="left">1853609022608:1067:1853614658072</td>
<td align="left">4188</td>
<td align="left">null</td>
<td align="left">TABLE</td>
<td align="left">IX</td>
<td align="left">null</td>
</tr>
<tr>
<td align="left">1853609022608:5:4:5:1853598537752</td>
<td align="left">4188</td>
<td align="left">PRIMARY</td>
<td align="left">RECORD</td>
<td align="left">X,REC_NOT_GAP</td>
<td align="left">5</td>
</tr>
</tbody></table>
</li>
</ol>
<p>可以看到，第二个事务 <code>4193</code>(窗口1的那个事务 <code>id</code> 是 <code>4188</code>)， 也是给表增加了 <code>IX</code> 锁，给 <code>id=5</code> 那一行增加了 <code>X,REC_NOT_GAP</code></p>
<p>接下来还是在窗口3执行下面两条语句，看下事务等待状态<br>先执行</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><code class="hljs Sql"><span class="hljs-keyword">select</span> trx_id, trx_state, trx_requested_lock_id, trx_wait_started <span class="hljs-keyword">from</span> information_schema.INNODB_TRX;<br></code></pre></td></tr></table></figure>
<table>
<thead>
<tr>
<th align="left">trx_id</th>
<th align="left">trx_state</th>
<th align="left">trx_requested_lock_id</th>
<th align="left">trx_wait_started</th>
</tr>
</thead>
<tbody><tr>
<td align="left">4193</td>
<td align="left">LOCK WAIT</td>
<td align="left">1853609023384:5:4:5:1853598540824</td>
<td align="left">2023-12-14 08:58:05</td>
</tr>
<tr>
<td align="left">4188</td>
<td align="left">RUNNING</td>
<td align="left">null</td>
<td align="left">null</td>
</tr>
<tr>
<td align="left">可以看到 <code>4188</code> 这个事务在运行中，<code>4193</code>（第二个窗口的事务）是 <code>lock wait</code> 状态， 同时可以看到 <code>4193</code> 这个事务的 <code>trx_requested_lock_id=1853609023384:5:4:5:1853598540824</code></td>
<td align="left"></td>
<td align="left"></td>
<td align="left"></td>
</tr>
<tr>
<td align="left">接下来执行如下 sql</td>
<td align="left"></td>
<td align="left"></td>
<td align="left"></td>
</tr>
</tbody></table>
<figure class="highlight sql"><table><tr><td class="code"><pre><code class="hljs Sql"><span class="hljs-keyword">select</span> REQUESTING_ENGINE_LOCK_ID,  BLOCKING_ENGINE_TRANSACTION_ID, BLOCKING_THREAD_ID <span class="hljs-keyword">from</span> performance_schema.data_lock_waits;<br></code></pre></td></tr></table></figure>
<table>
<thead>
<tr>
<th align="left">REQUESTING_ENGINE_LOCK_ID</th>
<th align="left">BLOCKING_ENGINE_TRANSACTION_ID</th>
<th align="left">BLOCKING_THREAD_ID</th>
</tr>
</thead>
<tbody><tr>
<td align="left">1853609023384:5:4:5:1853598540824</td>
<td align="left">4188</td>
<td align="left">68</td>
</tr>
</tbody></table>
<p>可以看到 <code>trx_requested_lock_id=1853609023384:5:4:5:1853598540824</code> 就是被 <code>4188</code> 这个事务所阻塞的</p>
<p><strong>接下来总结一下：使用主键进行等值更新，添加的是 <code>Record Lock</code></strong></p>
]]></content>
      <categories>
        <category>存储</category>
        <category>Mysql</category>
      </categories>
      <tags>
        <tag>Mysql</tag>
      </tags>
  </entry>
  <entry>
    <title>Mysql配置项和配置文件</title>
    <url>/2023/59d19215016d/index.html</url>
    <content><![CDATA[<p>以下基于 <code>Mysql8</code> 版本</p>
<h2 id="Mysql-程序选项"><a href="#Mysql-程序选项" class="headerlink" title="Mysql 程序选项"></a>Mysql 程序选项</h2><p><code>Mysql</code> 是一个 <strong>客户端-服务端</strong> 程序，安装 <code>Mysql</code> 后 <code>bin</code> 目录下有很多可执行程序，比如</p>
<ul>
<li>启动服务端的 <code>Mysqld</code> 程序</li>
<li>启动服务端的 <code>mysqld_safe</code> 脚本程序，这个脚本也会启动 <code>Mysqld</code>, 但会包含更多功能</li>
<li>启动客户端的 <code>Mysql</code> 程序</li>
</ul>
<p>还有很多可执行程序，具体可以参考 <a href="https://dev.mysql.com/doc/refman/8.0/en/programs-overview.html">Mysql programs</a></p>
<p>这些可执行程序在启动时都可以指定参数，这些参数有两种形式</p>
<ul>
<li>长格式<ul>
<li>以 <code>--</code> 开头</li>
<li>如果对应的参数有值，则是 <code>--key=value</code> 形式，比如 <code>--port=3306</code>， 注意 对应的 <code>key</code>, <code>=</code> 和 <code>value</code> 之间不要有空格， 比如 <code>--port = 3306</code> 形式是错误的</li>
</ul>
</li>
<li>短格式<ul>
<li>以 <code>-</code> 开头</li>
<li>如果对应的参数有值，则是 <code>-key value</code> 形式，比如 <code>-p 3306</code>， 参数和值之间不需要 <code>=</code></li>
</ul>
</li>
</ul>
<h3 id="可执行程序支持的选项"><a href="#可执行程序支持的选项" class="headerlink" title="可执行程序支持的选项"></a>可执行程序支持的选项</h3><p>可以通过 <code>--help</code> 来查看对应可执行命令的帮助信息，一般会在帮助信息中列出支持的选项，比如</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">$ mysql --<span class="hljs-built_in">help</span><br>mysql  Ver 8.0.35 <span class="hljs-keyword">for</span> Linux on x86_64 (MySQL Community Server - GPL)<br>Copyright (c) 2000, 2023, Oracle and/or its affiliates.<br><br>Oracle is a registered trademark of Oracle Corporation and/or its<br>affiliates. Other names may be trademarks of their respective<br>owners.<br><br>Usage: mysql [OPTIONS] [database]<br>  -?, --<span class="hljs-built_in">help</span>          Display this <span class="hljs-built_in">help</span> and <span class="hljs-built_in">exit</span>.<br>  -I, --<span class="hljs-built_in">help</span>          Synonym <span class="hljs-keyword">for</span> -?<br>  --auto-rehash       Enable automatic rehashing. One doesn<span class="hljs-string">&#x27;t need to use</span><br><span class="hljs-string">                      &#x27;</span><span class="hljs-built_in">rehash</span><span class="hljs-string">&#x27; to get table and field completion, but startup</span><br><span class="hljs-string">                      and reconnecting may take a longer time. Disable with</span><br><span class="hljs-string">                      --disable-auto-rehash.</span><br><span class="hljs-string">                      (Defaults to on; use --skip-auto-rehash to disable.)</span><br><span class="hljs-string">    …… 省略</span><br></code></pre></td></tr></table></figure>

<h2 id="配置文件"><a href="#配置文件" class="headerlink" title="配置文件"></a>配置文件</h2><p>通过命令行执行可执行文件并且在命令后面增加参数是一次性有效的，如果停止了 <code>Mysql</code> 服务，再次启动需要重新指定参数，所以可以将参数写在配置文件，这样就可以不用每次都指定参数了。</p>
<p><a href="https://dev.mysql.com/doc/refman/8.0/en/option-files.html">Mysql配置文件官方文档说明</a></p>
<h3 id="Unix-和-类-Unix-下配置文件读取位置"><a href="#Unix-和-类-Unix-下配置文件读取位置" class="headerlink" title="Unix 和 类 Unix 下配置文件读取位置"></a>Unix 和 类 Unix 下配置文件读取位置</h3><table>
<thead>
<tr>
<th>File Name</th>
<th>Purpose</th>
</tr>
</thead>
<tbody><tr>
<td>&#x2F;etc&#x2F;my.cnf</td>
<td>Global options</td>
</tr>
<tr>
<td>&#x2F;etc&#x2F;mysql&#x2F;my.cnf</td>
<td>Global options</td>
</tr>
<tr>
<td>SYSCONFDIR&#x2F;my.cnf</td>
<td>Global options</td>
</tr>
<tr>
<td>$MYSQL_HOME&#x2F;my.cnf</td>
<td>Server-specific options (server only)</td>
</tr>
<tr>
<td>defaults-extra-file</td>
<td>The file specified with –defaults-extra-file, if any</td>
</tr>
<tr>
<td>~&#x2F;.my.cn</td>
<td>User-specific options</td>
</tr>
<tr>
<td>~&#x2F;.mylogin.cnf</td>
<td>User-specific login path options (clients only)</td>
</tr>
<tr>
<td>DATADIR&#x2F;mysqld-auto.cnf</td>
<td>System variables persisted with SET PERSIST or SET PERSIST_ONLY (server only)</td>
</tr>
</tbody></table>
<ul>
<li><code>$MYSQL_HOME</code> 是环境变量，可以自己设置<ol><li><a href="/2023/1f7648c2ff11/index.html" title="Ubuntu中设置环境变量的几种方式">Ubuntu中设置环境变量的几种方式</a></li></ol></li>
<li>以 <code>~</code> 开头的则是用户目录下的配置文件</li>
<li><code>defaults-extra-file</code> 是指通过命令行指定的配置文件，比如 <code>mysqld --defaults-extra-file=/usr/loca/mysql.cnf</code></li>
</ul>
<h3 id="配置文件包含的内容"><a href="#配置文件包含的内容" class="headerlink" title="配置文件包含的内容"></a>配置文件包含的内容</h3><p>下面是一个 <code>my.cnf</code> 配置文件的示例</p>
<figure class="highlight clean"><table><tr><td class="code"><pre><code class="hljs clean">###### [mysql]配置模块 ######<br>[mysql]<br># 设置MySQL客户端默认字符集<br>default-character-set=utf8mb4<br>socket=/var/lib/mysql/mysql.sock<br><br><br>###### [mysqld]配置模块 ######<br>[mysqld]<br>port=<span class="hljs-number">3306</span><br>user=root<br>#skip-grant-tables<br>sql_mode=STRICT_TRANS_TABLES,NO_ZERO_IN_DATE,NO_ZERO_DATE,ERROR_FOR_DIVISION_BY_ZERO,NO_ENGINE_SUBSTITUTION<br><br>datadir=/var/lib/mysql<br>socket=/var/lib/mysql/mysql.sock<br></code></pre></td></tr></table></figure>

<p>主要包含以下几部分内容</p>
<ul>
<li>注释和空行</li>
<li>[mysql]， [mysqld] 这种在官方文档中称为  <code>group</code>, 也就是配置文件被分组了，组里面的内容就是可执行文件名<ul>
<li>不同组下的配置项是给不同的启动命令使用的，如果选项组名称与程序名称相同，则组中的选项将应用于该程序， 比如 <code>[mysql]</code> 组下面的选项会在使用 <code>mysql</code> 命令时读取</li>
<li><code>[server]</code> 组下配置项将作用于所有的服务器程序</li>
<li><code>[client]</code> 组下配置项将作用于所有的客户端程序</li>
<li><code>[mysqld_safe]</code> 和 <code>[mysql.server]</code> 这两个程序在启动时除了读取自己组下的配置还会读取 <code>[mysqld]</code> 下的配置</li>
</ul>
</li>
<li>组下面的具体配置<ul>
<li>配置文件中的配置项只能是长格式，而且不需要 <code>--</code> 开头</li>
</ul>
</li>
</ul>
<h3 id="配置文件优先级"><a href="#配置文件优先级" class="headerlink" title="配置文件优先级"></a>配置文件优先级</h3><p><strong>不同配置文件的优先级</strong></p>
<p>不同目录下都有配置文件，并且有相同的配置项的情况下，以后读取的为准，上面已经列出了读取的目录</p>
<table>
<thead>
<tr>
<th>File Name</th>
<th>Purpose</th>
</tr>
</thead>
<tbody><tr>
<td>&#x2F;etc&#x2F;my.cnf</td>
<td>Global options</td>
</tr>
<tr>
<td>&#x2F;etc&#x2F;mysql&#x2F;my.cnf</td>
<td>Global options</td>
</tr>
<tr>
<td>SYSCONFDIR&#x2F;my.cnf</td>
<td>Global options</td>
</tr>
<tr>
<td>$MYSQL_HOME&#x2F;my.cnf</td>
<td>Server-specific options (server only)</td>
</tr>
<tr>
<td>defaults-extra-file</td>
<td>The file specified with –defaults-extra-file, if any</td>
</tr>
<tr>
<td>~&#x2F;.my.cn</td>
<td>User-specific options</td>
</tr>
<tr>
<td>~&#x2F;.mylogin.cnf</td>
<td>User-specific login path options (clients only)</td>
</tr>
<tr>
<td>DATADIR&#x2F;mysqld-auto.cnf</td>
<td>System variables persisted with SET PERSIST or SET PERSIST_ONLY (server only)</td>
</tr>
</tbody></table>
<p>如果 <code>/etc/my.cnf </code> 和 <code>~/.my.cn</code> 下都有相同的配置，则使用的是 <code> ~/.my.cn</code> 配置文件中的内容</p>
<p><strong>相同配置文件的优先级</strong></p>
<ol>
<li><code>mysql</code> 配置文件有多个 <code>group</code></li>
<li>若是在不同的 <code>group</code> 中出现相同的配置则以最后一个 <code>group</code> 中出现的配置为准（前提是对应的程序会读取这个 <code>group</code> 的配置）</li>
</ol>
]]></content>
      <categories>
        <category>存储</category>
        <category>Mysql</category>
      </categories>
      <tags>
        <tag>Mysql</tag>
      </tags>
  </entry>
  <entry>
    <title>Window下安装mysql8解压版</title>
    <url>/2023/ff432902bea0/index.html</url>
    <content><![CDATA[<ul>
<li><a href="https://dev.mysql.com/downloads/mysql/">下载Mysql8</a>， 选择 <code>Windows (x86, 64-bit), ZIP Archive</code> 这个</li>
<li>解压到某个目录，比如 <code>E:\config\mysql-8.2.0-winx64</code>， 这就是 <code>Mysql</code> 安装目录</li>
<li>在 <code>Mysql</code> 安装目录下新建 <code>data</code> 目录用来存放数据，即 <code>data</code> 目录全路径为 <code>E:\config\mysql-8.2.0-winx64\data</code></li>
<li>在 <code>Mysql</code> 安装目录下新建配置文件 <code>my.cnf</code>, 内容如下</li>
</ul>
<figure class="highlight ini"><table><tr><td class="code"><pre><code class="hljs ini"><span class="hljs-section">[mysqld]</span><br><span class="hljs-comment">#设置时区为东八区，此项设置后，在连接MySQL的时候可以不用每次都手动设置时区</span><br><span class="hljs-attr">default-time-zone</span> = <span class="hljs-string">&#x27;+8:00&#x27;</span><br><span class="hljs-comment"># 设置3306端口</span><br><span class="hljs-attr">port</span>=<span class="hljs-number">3306</span><br><span class="hljs-comment"># 设置mysql的安装目录，替换为自己的路径</span><br><span class="hljs-attr">basedir</span>=E:\config\mysql-<span class="hljs-number">8.2</span>.<span class="hljs-number">0</span>-winx64<br><span class="hljs-comment"># 设置mysql数据库的数据的存放目录， 替换为自己的路径</span><br><span class="hljs-attr">datadir</span>=E:\config\mysql-<span class="hljs-number">8.2</span>.<span class="hljs-number">0</span>-winx64\data<br><span class="hljs-comment"># 允许最大连接数</span><br><span class="hljs-attr">max_connections</span>=<span class="hljs-number">200</span><br><span class="hljs-comment"># 允许连接失败的次数</span><br><span class="hljs-attr">max_connect_errors</span>=<span class="hljs-number">10</span><br><span class="hljs-comment"># 服务端使用的字符集默认为UTF8</span><br><span class="hljs-attr">character-set-server</span>=utf8<br><span class="hljs-comment"># 创建新表时将使用的默认存储引擎</span><br><span class="hljs-attr">default-storage-engine</span>=INNODB<br><span class="hljs-comment"># 默认使用“mysql_native_password”插件认证</span><br><span class="hljs-attr">default_authentication_plugin</span>=mysql_native_password<br><br><br><span class="hljs-section">[mysql]</span><br><span class="hljs-comment"># 设置mysql客户端默认字符集</span><br><span class="hljs-attr">default-character-set</span>=utf8<br><span class="hljs-section">[client]</span><br><span class="hljs-comment"># 设置mysql客户端连接服务端时默认使用的端口</span><br><span class="hljs-attr">port</span>=<span class="hljs-number">3306</span><br><span class="hljs-attr">default-character-set</span>=utf8<br></code></pre></td></tr></table></figure>

<ul>
<li>使用管理员身份打开 <code>cmd</code>, 进入 <code>Mysql</code> 安装目录下的 <code>bin</code> 目录</li>
</ul>
<figure class="highlight cmd"><table><tr><td class="code"><pre><code class="hljs cmd"><span class="hljs-built_in">cd</span> E:\config\mysql-<span class="hljs-number">8</span>.<span class="hljs-number">2</span>.<span class="hljs-number">0</span>-winx64\bin<br>mysqld --initialize --console<br></code></pre></td></tr></table></figure>
<p>初始化完成后在 <code>cmd</code> 窗口会出现 <code> A temporary password is generated for root@localhost: ,mDQ&gt;kFHI65w</code> 字样(密码生成的不同)， 复制保存这个密码</p>
<ul>
<li>继续在当前 <code>cmd</code> 窗口执行 <code>mysqld --install</code> 命令，这个命令是将 <code>Mysqld</code> 程序作为安装为 <code>Windows</code> 服务， 这样就可以在系统服务中看到 <code>Mysqld</code> 了</li>
<li>执行 <code>net start MySQL</code> 就可以启动 <code>Mysql</code> 服务了</li>
<li>执行 <code>mysql -u root -p</code> 命令，然后复制上面拷贝出来的 <code>,mDQ&gt;kFHI65w</code> 密码，直接用这个密码去登录就行了</li>
<li>修改密码为 <code>admin</code>, 后续就可以使用 <code>root</code> 账号和 <code>admin</code> 密码登录了</li>
</ul>
<figure class="highlight sql"><table><tr><td class="code"><pre><code class="hljs sql"><span class="hljs-keyword">ALTER</span> <span class="hljs-keyword">USER</span> <span class="hljs-string">&#x27;root&#x27;</span>@<span class="hljs-string">&#x27;localhost&#x27;</span> IDENTIFIED <span class="hljs-keyword">BY</span> <span class="hljs-string">&#x27;admin&#x27;</span>;<br></code></pre></td></tr></table></figure>

<p><strong>问题</strong><br>执行 <code>net start mysql</code> 时出现 <strong>无法启动服务，原因可能是已被禁用或与其相关联的设备没有启动。</strong><br>此时可以先卸载 <code>Mysql</code> 服务再重试安装</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><code class="hljs xml">mysqld -remove<br><span class="hljs-comment">&lt;!-- 如果上面执行不成功的话，可以使用下面命令卸载试试 --&gt;</span><br>mysqld -nt -remove<br><span class="hljs-comment">&lt;!-- 然后再次安装 --&gt;</span><br>mysqld -install<br></code></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>存储</category>
        <category>Mysql</category>
      </categories>
      <tags>
        <tag>Mysql</tag>
      </tags>
  </entry>
  <entry>
    <title>Postgres清除idle类型的连接</title>
    <url>/2023/d2329b029733/index.html</url>
    <content><![CDATA[<figure class="highlight sql"><table><tr><td class="code"><pre><code class="hljs sql"><span class="hljs-comment">-- 查询空闲连接的数量</span><br><span class="hljs-keyword">select</span> <span class="hljs-built_in">count</span>(<span class="hljs-number">1</span>) <span class="hljs-keyword">FROM</span> pg_stat_activity <span class="hljs-keyword">WHERE</span> state<span class="hljs-operator">=</span><span class="hljs-string">&#x27;idle&#x27;</span>;<br><br><span class="hljs-comment">-- 关闭掉所有的空闲连接</span><br><span class="hljs-keyword">SELECT</span> pg_terminate_backend(pid) <span class="hljs-keyword">FROM</span> pg_stat_activity <span class="hljs-keyword">WHERE</span> state<span class="hljs-operator">=</span><span class="hljs-string">&#x27;idle&#x27;</span>;<br></code></pre></td></tr></table></figure>

<p>如果线上环境所有连接已经被占用，无法进行控制台操作，则可以考虑如下方式</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">pkill -f idle; psql -Upostgres -d db_name<br></code></pre></td></tr></table></figure>
<p><code>pkill -f</code> 可以根据关键字来关掉进程，这里使用的关键字是 <code>idle</code><br><code>psql -Upostgres -d db_name</code> 该命令是登录 <code>psql</code> 客户端，进入特定的数据库</p>
]]></content>
      <categories>
        <category>存储</category>
        <category>PostgreSQL</category>
      </categories>
      <tags>
        <tag>PostgreSQL</tag>
      </tags>
  </entry>
  <entry>
    <title>postgres将毫秒值转换成字符串</title>
    <url>/2023/3a8e444a3e04/index.html</url>
    <content><![CDATA[<h2 id="基本信息"><a href="#基本信息" class="headerlink" title="基本信息"></a>基本信息</h2><p>毫秒值类型:  <code>numeric(13)</code></p>
<p>字段名称: <code>check_time</code></p>
<figure class="highlight sql"><table><tr><td class="code"><pre><code class="hljs sql"><span class="hljs-keyword">SELECT</span> to_char(to_timestamp(check_time <span class="hljs-operator">/</span> <span class="hljs-number">1000</span>) <span class="hljs-keyword">AT</span> <span class="hljs-type">TIME</span> ZONE <span class="hljs-string">&#x27;Asia/Shanghai&#x27;</span>, <span class="hljs-string">&#x27;YYYY-MM-DD HH24:MI:SS.MS&#x27;</span>) <span class="hljs-keyword">AS</span> check_time <span class="hljs-keyword">from</span> table_neme;<br></code></pre></td></tr></table></figure>

<h2 id="使用到的函数"><a href="#使用到的函数" class="headerlink" title="使用到的函数"></a>使用到的函数</h2><p><a href="http://www.postgres.cn/docs/12/functions-formatting.html">postgres12版本的日期函数参考</a></p>
<ol>
<li><code>to_timestamp</code>： 将 <strong>秒</strong> 值转换成 <code>timestamp</code> 类型， 所以如果是毫秒值的话还需要 <code>毫秒值/1000</code><ol>
<li><code>AT TIME ZONE &#39;Asia/Shanghai</code> 代表将返回的时间戳指定时区</li>
</ol>
</li>
<li><code>to_char</code> 将 <code>timestamp</code> 类型转换成字符串， 其中 <code>YYYY-MM-DD HH24:MI:SS.MS</code> 代表格式化的字符串<ol>
<li><code>HH24</code> 代表 24 小时制</li>
<li><code>MI</code> 表示分钟</li>
</ol>
</li>
</ol>
]]></content>
      <categories>
        <category>存储</category>
        <category>PostgreSQL</category>
      </categories>
      <tags>
        <tag>PostgreSQL</tag>
      </tags>
  </entry>
  <entry>
    <title>通过idea查找注解被使用的地方</title>
    <url>/2023/4bed69252515/index.html</url>
    <content><![CDATA[<blockquote>
<p>idea版本: 2023.3</p>
</blockquote>
<p>在 <code>Mybatis</code> 整合 <code>SpringBoot</code> 的时候，会使用 <code>MapperScan</code> 注解，如果我们想快速知道这个注解是在哪里被解析(被使用的)，可以通过 <code>idea</code> 的 <code>Find Usages</code> 功能, 具体步骤如下</p>
<ul>
<li>先进入 <code>MapperScan</code> 注解类，因为这个是 <code>jar</code> 包，所以可以先 <code>download resource</code></li>
<li>光标放在 <code>MapperScan</code> 注解上 -&gt; 右键 -&gt; <code>Find Usages</code></li>
</ul>
<p><img src="https://s2.loli.net/2023/12/28/RA4pDHQVjZgt15m.png" alt="Find Usages功能"></p>
<p> <code>Scope</code> 选择那个界面是点击最左边的 <code>设置图标</code> 按钮即可</p>
]]></content>
      <categories>
        <category>tools</category>
        <category>idea</category>
      </categories>
      <tags>
        <tag>idea</tag>
      </tags>
  </entry>
  <entry>
    <title>maven编译跳过test</title>
    <url>/2023/5614a2f5fdca/index.html</url>
    <content><![CDATA[<h2 id="方式一"><a href="#方式一" class="headerlink" title="方式一"></a>方式一</h2><figure class="highlight armasm"><table><tr><td class="code"><pre><code class="hljs armasm"><span class="hljs-keyword">mvn</span> clean install -DskipTests<br></code></pre></td></tr></table></figure>

<h2 id="方式二"><a href="#方式二" class="headerlink" title="方式二"></a>方式二</h2><figure class="highlight cmake"><table><tr><td class="code"><pre><code class="hljs cmake">mvn clean <span class="hljs-keyword">install</span> -Dmaven.<span class="hljs-keyword">test</span>.skip=<span class="hljs-keyword">true</span><br></code></pre></td></tr></table></figure>
<p>如果在 <code>window</code> 中使用 <code>powershell</code> 或在 <code>idea</code> 的终端下使用这种方式可能会提示 <code>Unknown lifecycle phase &quot;.test.skip=true&quot;</code>，此时需要增加引号，比如</p>
<figure class="highlight cmake"><table><tr><td class="code"><pre><code class="hljs cmake">mvn clean <span class="hljs-keyword">install</span> &#x27;-Dmaven.<span class="hljs-keyword">test</span>.skip=<span class="hljs-keyword">true</span>&#x27;<br></code></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>tools</category>
        <category>maven</category>
      </categories>
      <tags>
        <tag>maven</tag>
      </tags>
  </entry>
  <entry>
    <title>docker中运行nacos</title>
    <url>/2023/f67204379054/index.html</url>
    <content><![CDATA[<p>使用的系统为 <code>ubuntu22.04</code></p>
<ol><li><a href="/2023/71e6b223e45b/index.html" title="ubuntu22.04安装docker-compose">ubuntu22.04安装docker-compose</a></li></ol>

<p>参考 <a href="https://nacos.io/zh-cn/docs/quick-start-docker.html">nacos官方文档之使用docker启动</a></p>
<p>这里使用的是 <code>mysql8</code> 单机版启动</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><code class="hljs xml"><span class="hljs-comment">&lt;!-- 克隆项目 --&gt;</span><br>git clone https://github.com/nacos-group/nacos-docker.git<br>cd nacos-docker<br><br><span class="hljs-comment">&lt;!-- 以后台模式启动 nacos 服务 --&gt;</span><br>docker-compose -f example/standalone-mysql-8.yaml up -d<br><br><span class="hljs-comment">&lt;!-- 查看启动的docker服务 --&gt;</span><br>docker container ps<br><br><span class="hljs-comment">&lt;!-- 关闭服务 --&gt;</span><br>docker-compose -f example/standalone-mysql-8.yaml down<br><br><span class="hljs-comment">&lt;!-- 启动后，访问 nacos  --&gt;</span><br>http://localhost:8848/nacos/index.html<br></code></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>云原生</category>
        <category>docker</category>
      </categories>
      <tags>
        <tag>docker</tag>
      </tags>
  </entry>
  <entry>
    <title>docker配置阿里云加速镜像</title>
    <url>/2023/2b35420a425c/index.html</url>
    <content><![CDATA[<ol>
<li>访问<a href="https://cr.console.aliyun.com/">阿里云镜像控制台</a>， 找到镜像加速链接</li>
</ol>
<p><img src="https://blog-1308475102.cos.ap-guangzhou.myqcloud.com/picture/202311272213645.webp" alt="加速镜像"></p>
<ol start="2">
<li>在文档的下方就有不同环境配置的代码，下面是 <code>ubuntu</code> 环境的配置（加速路径替换成自己的）</li>
</ol>
<figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">sudo <span class="hljs-built_in">mkdir</span> -p /etc/docker<br>sudo <span class="hljs-built_in">tee</span> /etc/docker/daemon.json &lt;&lt;-<span class="hljs-string">&#x27;EOF&#x27;</span><br>&#123;<br>  <span class="hljs-string">&quot;registry-mirrors&quot;</span>: [<span class="hljs-string">&quot;https://xxx.mirror.aliyuncs.com&quot;</span>]<br>&#125;<br>EOF<br>sudo systemctl daemon-reload<br>sudo systemctl restart docker<br></code></pre></td></tr></table></figure>

<ol>
<li>修改 Cgroup Driver, 修改 <code>/etc/docker/daemon.json</code> 文件，增加如下内容</li>
</ol>
<figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash"><span class="hljs-string">&quot;exec-opts&quot;</span>: [<span class="hljs-string">&quot;native.cgroupdriver=systemd&quot;</span>]<br></code></pre></td></tr></table></figure>
<p>最终 <code>daemon.json</code> 文件内容如下</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">&#123;<br>  <span class="hljs-string">&quot;registry-mirrors&quot;</span>: [<span class="hljs-string">&quot;https://xxx.mirror.aliyuncs.com&quot;</span>],<br>  <span class="hljs-string">&quot;exec-opts&quot;</span>: [<span class="hljs-string">&quot;native.cgroupdriver=systemd&quot;</span>]<br>&#125;<br></code></pre></td></tr></table></figure>

<ol start="2">
<li>重启 <code>docker</code> 服务</li>
</ol>
<figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">sudo systemctl daemon-reload<br>sudo systemctl restart docker<br></code></pre></td></tr></table></figure>




]]></content>
      <categories>
        <category>云原生</category>
        <category>docker</category>
      </categories>
      <tags>
        <tag>docker</tag>
      </tags>
  </entry>
  <entry>
    <title>ubuntu22.04安装docker-compose</title>
    <url>/2023/71e6b223e45b/index.html</url>
    <content><![CDATA[<ol>
<li><a href="https://github.com/docker/compose/releases">docker-compose下载</a>, 选择对应的版本即可，对于 <code>ubuntu</code> 系统选择的是 <code>Linux</code> 版本， 下载完成后，将其移动到 <code>/usr/local/bin/</code> 目录下即可。</li>
</ol>
<figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash"><span class="hljs-comment"># 重命名</span><br><span class="hljs-built_in">mv</span> docker-compose-linux-x86_64 docker-compose<br><span class="hljs-comment"># 移动到执行目录</span><br>sudo <span class="hljs-built_in">mv</span> docker-compose /usr/local/bin/<br><span class="hljs-comment"># 赋予执行权限</span><br>sudo <span class="hljs-built_in">chmod</span> u+x docker-compose<br></code></pre></td></tr></table></figure>



]]></content>
      <categories>
        <category>云原生</category>
        <category>docker</category>
      </categories>
      <tags>
        <tag>docker</tag>
      </tags>
  </entry>
  <entry>
    <title>使用docker-compose安装mysql和redis基本环境</title>
    <url>/2023/3c61103fd97b/index.html</url>
    <content><![CDATA[<h2 id="创建自定义网络"><a href="#创建自定义网络" class="headerlink" title="创建自定义网络"></a>创建自定义网络</h2><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">docker network create service_default<br></code></pre></td></tr></table></figure>

<h2 id="docker-compose-安装mysql-redis"><a href="#docker-compose-安装mysql-redis" class="headerlink" title="docker-compose 安装mysql,redis"></a>docker-compose 安装mysql,redis</h2><p>下面将所有的软件安装在 <code>/opt/docker_soft</code> 目录下</p>
<ol>
<li>在 <code>/opt/docker_soft</code> 目录下创建如下目录结构</li>
</ol>
<figure class="highlight stylus"><table><tr><td class="code"><pre><code class="hljs stylus">├── config<br>│   ├── mysql8<br>│   │   └── my<span class="hljs-selector-class">.cnf</span><br>│   ├── nginx<br>│   │   └── nginx<span class="hljs-selector-class">.conf</span><br>│   ├── redis7<br>│   │   └── redis<span class="hljs-selector-class">.conf</span><br>│   └── zk<br>│       └── zoo<span class="hljs-selector-class">.cfg</span><br>├── data<br>│   ├── mysql8<br>│   ├── nginx<br>│   ├── redis<br>├── docker-compose<span class="hljs-selector-class">.yml</span><br>└── log<br>    ├── mysql8<br>    └── nginx<br></code></pre></td></tr></table></figure>

<figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">cd</span> /opt/docker_soft<br><span class="hljs-built_in">mkdir</span> -p config data <span class="hljs-built_in">log</span><br><span class="hljs-built_in">cd</span> config/<br><span class="hljs-built_in">mkdir</span> mysql8<br><span class="hljs-built_in">mkdir</span> nginx<br><span class="hljs-built_in">mkdir</span> redis7<br><span class="hljs-built_in">touch</span> mysql8/my.cnf<br><span class="hljs-built_in">touch</span> nginx/nginx.conf<br><span class="hljs-built_in">touch</span> redis7/redis.conf<br><br><span class="hljs-built_in">cd</span> ..<br><span class="hljs-built_in">mkdir</span> data/mysql8<br><span class="hljs-built_in">mkdir</span> data/nginx<br><span class="hljs-built_in">mkdir</span> data/redis<br><span class="hljs-built_in">mkdir</span> <span class="hljs-built_in">log</span>/mysql8<br><span class="hljs-built_in">mkdir</span> <span class="hljs-built_in">log</span>/nginx<br><br><span class="hljs-comment"># 给mysql挂载目录赋予所有权限，避免mysql启动报错</span><br><span class="hljs-built_in">chmod</span> -R 777 data/mysql8<br></code></pre></td></tr></table></figure>

<p>通过上面命令创建了三个配置文件，其中 <code>my.cnf</code> 是 mysql 的配置文件，<code>nginx.conf</code> 是 nginx 的配置文件，<code>redis.conf</code> 是 redis 的配置文件， 可以将下面内容拷贝到对应的配置文件中。</p>
<div class="tabs" id="配置文件"><ul class="nav-tabs"><button type="button" class="tab  active" data-href="配置文件-1">mysql的配置文件(my.cnf)</button><button type="button" class="tab " data-href="配置文件-2">nginx的配置文件(nginx.conf)</button><button type="button" class="tab " data-href="配置文件-3">redis的配置文件(redis.conf)</button></ul><div class="tab-contents"><div class="tab-item-content active" id="配置文件-1"><figure class="highlight ini"><table><tr><td class="code"><pre><code class="hljs ini"><span class="hljs-comment">###### [mysql]配置模块 ######</span><br><span class="hljs-section">[mysql]</span><br><span class="hljs-comment"># 设置MySQL客户端默认字符集</span><br><span class="hljs-attr">default-character-set</span>=utf8mb4<br><span class="hljs-attr">socket</span>=/var/lib/mysql/mysql.sock<br><br><br><span class="hljs-comment">###### [mysqld]配置模块 ######</span><br><span class="hljs-section">[mysqld]</span><br><span class="hljs-attr">port</span>=<span class="hljs-number">3306</span><br><span class="hljs-attr">user</span>=root<br><span class="hljs-comment"># 注释1</span><br><span class="hljs-comment">#skip-grant-tables</span><br><span class="hljs-attr">sql_mode</span>=STRICT_TRANS_TABLES,NO_ZERO_IN_DATE,NO_ZERO_DATE,ERROR_FOR_DIVISION_BY_ZERO,NO_ENGINE_SUBSTITUTION<br><br><span class="hljs-attr">datadir</span>=/var/lib/mysql<br><span class="hljs-attr">socket</span>=/var/lib/mysql/mysql.sock<br><br><span class="hljs-comment"># MySQL8 的密码认证插件</span><br><span class="hljs-comment">#default_authentication_plugin=mysql_native_password</span><br><br><span class="hljs-comment"># 禁用符号链接以防止各种安全风险</span><br><span class="hljs-comment">#symbolic-links=0</span><br><br><span class="hljs-comment"># 允许最大连接数</span><br><span class="hljs-attr">max_connections</span>=<span class="hljs-number">1000</span><br><br><span class="hljs-comment"># 服务端使用的字符集默认为8比特编码的latin1字符集</span><br><span class="hljs-attr">character-set-server</span>=utf8mb4<br><br><span class="hljs-comment"># 创建新表时将使用的默认存储引擎</span><br><span class="hljs-attr">default-storage-engine</span>=INNODB<br><br><span class="hljs-comment"># 表名存储在磁盘是小写的，但是比较的时候是不区分大小写</span><br><span class="hljs-attr">lower_case_table_names</span>=<span class="hljs-number">1</span><br><span class="hljs-attr">max_allowed_packet</span>=<span class="hljs-number">16</span>M <br><br><span class="hljs-comment"># 设置时区</span><br><span class="hljs-attr">default-time_zone</span>=<span class="hljs-string">&#x27;+8:00&#x27;</span><br><br><span class="hljs-comment"># binlog 配置</span><br><span class="hljs-attr">log-bin</span> = /logs/mysql-bin.log<br><span class="hljs-attr">expire-logs-days</span> = <span class="hljs-number">90</span><br><span class="hljs-attr">max-binlog-size</span> = <span class="hljs-number">500</span>M<br><br><span class="hljs-comment"># server-id 配置</span><br><span class="hljs-attr">server-id</span> = <span class="hljs-number">1</span><br><br><br><span class="hljs-comment">###### [client]配置模块 ######</span><br><span class="hljs-section">[client]</span><br><span class="hljs-attr">default-character-set</span>=utf8mb4<br><br><span class="hljs-comment">#–initialize --lower-case-table-names=1</span><br></code></pre></td></tr></table></figure></div><div class="tab-item-content" id="配置文件-2"><figure class="highlight nginx"><table><tr><td class="code"><pre><code class="hljs nginx"><br><span class="hljs-comment">#user  nobody;</span><br><span class="hljs-comment"># 如果设置了 auto， 则 work-processes 的数量为 CPU 的核数</span><br><span class="hljs-comment"># worker_processes  auto;</span><br><span class="hljs-attribute">worker_processes</span>  <span class="hljs-number">2</span>;<br><br><span class="hljs-comment">#error_log  logs/error.log;</span><br><span class="hljs-comment">#error_log  logs/error.log  notice;</span><br><span class="hljs-comment">#error_log  logs/error.log  info;</span><br><br><span class="hljs-attribute">pid</span>        /var/log/nginx/nginx.pid;<br><br><br><span class="hljs-section">events</span> &#123;<br>    <span class="hljs-attribute">worker_connections</span>  <span class="hljs-number">1024</span>;<br>&#125;<br><br><br><span class="hljs-section">http</span> &#123;<br>    <span class="hljs-comment">#include       mime.types;</span><br>    <span class="hljs-attribute">default_type</span>  application/octet-stream;<br><br>    <span class="hljs-attribute">sendfile</span>        <span class="hljs-literal">on</span>;<br>    <span class="hljs-comment">#tcp_nopush     on;</span><br><br>    <span class="hljs-comment">#keepalive_timeout  0;</span><br>    <span class="hljs-attribute">keepalive_timeout</span>  <span class="hljs-number">65</span>;<br><br>    <span class="hljs-comment">#gzip  on;</span><br><br>    <span class="hljs-section">server</span> &#123;<br>        <span class="hljs-attribute">listen</span>       <span class="hljs-number">80</span>;<br>        <span class="hljs-attribute">server_name</span>  localhost;<br><br>        <span class="hljs-section">location</span> / &#123;<br>            <span class="hljs-attribute">root</span>   /usr/share/nginx/html;<br>            <span class="hljs-attribute">index</span>  index.html index.htm;<br>        &#125;<br><br>        <span class="hljs-comment">#error_page  404              /404.html;</span><br><br>        <span class="hljs-comment"># redirect server error pages to the static page /50x.html</span><br>        <span class="hljs-comment">#</span><br>        <span class="hljs-attribute">error_page</span>   <span class="hljs-number">500</span> <span class="hljs-number">502</span> <span class="hljs-number">503</span> <span class="hljs-number">504</span>  /50x.html;<br>        <span class="hljs-section">location</span> = /50x.html &#123;<br>            <span class="hljs-attribute">root</span>   html;<br>        &#125;<br><br>    &#125;<br><br>&#125;<br><br></code></pre></td></tr></table></figure></div><div class="tab-item-content" id="配置文件-3"><figure class="highlight pgsql"><table><tr><td class="code"><pre><code class="hljs pgsql">protected-mode <span class="hljs-keyword">no</span><br>port <span class="hljs-number">6379</span><br>tcp-backlog <span class="hljs-number">511</span><br>timeout <span class="hljs-number">0</span><br>tcp-keepalive <span class="hljs-number">300</span><br>daemonize <span class="hljs-keyword">no</span><br>pidfile /var/run/redis_6379.pid<br>loglevel <span class="hljs-keyword">notice</span><br>logfile &quot;&quot;<br>databases <span class="hljs-number">16</span><br><span class="hljs-keyword">always</span>-<span class="hljs-keyword">show</span>-logo <span class="hljs-keyword">no</span><br><span class="hljs-keyword">set</span>-proc-title yes<br>proc-title-<span class="hljs-keyword">template</span> &quot;&#123;title&#125; &#123;listen-addr&#125; &#123;server-mode&#125;&quot;<br>stop-writes-<span class="hljs-keyword">on</span>-bgsave-error yes<br>rdbcompression yes<br>rdbchecksum yes<br>dbfilename dump.rdb<br>rdb-del-sync-files <span class="hljs-keyword">no</span><br>dir ./<br><span class="hljs-keyword">replica</span>-serve-stale-data yes<br><span class="hljs-keyword">replica</span>-<span class="hljs-keyword">read</span>-<span class="hljs-keyword">only</span> yes<br>repl-diskless-sync yes<br>repl-diskless-sync-max-replicas <span class="hljs-number">0</span><br>repl-diskless-<span class="hljs-keyword">load</span> disabled<br>repl-<span class="hljs-keyword">disable</span>-tcp-nodelay <span class="hljs-keyword">no</span><br><span class="hljs-keyword">replica</span>-priority <span class="hljs-number">100</span><br>acllog-max-len <span class="hljs-number">128</span><br><br>lazyfree-lazy-eviction <span class="hljs-keyword">no</span><br>lazyfree-lazy-expire <span class="hljs-keyword">no</span><br>lazyfree-lazy-<span class="hljs-keyword">server</span>-del <span class="hljs-keyword">no</span><br><span class="hljs-keyword">replica</span>-lazy-flush <span class="hljs-keyword">no</span><br><br><br>lazyfree-lazy-<span class="hljs-keyword">user</span>-del <span class="hljs-keyword">no</span><br><br><br>lazyfree-lazy-<span class="hljs-keyword">user</span>-flush <span class="hljs-keyword">no</span><br>oom-score-adj <span class="hljs-keyword">no</span><br><br>oom-score-adj-<span class="hljs-keyword">values</span> <span class="hljs-number">0</span> <span class="hljs-number">200</span> <span class="hljs-number">800</span><br><br><span class="hljs-keyword">disable</span>-thp yes<br><br>appendonly <span class="hljs-keyword">no</span><br><br>appendfilename &quot;appendonly.aof&quot;<br>appenddirname &quot;appendonlydir&quot;<br>appendfsync everysec<br><span class="hljs-keyword">no</span>-appendfsync-<span class="hljs-keyword">on</span>-rewrite <span class="hljs-keyword">no</span><br>auto-aof-rewrite-percentage <span class="hljs-number">100</span><br>auto-aof-rewrite-min-size <span class="hljs-number">64</span>mb<br><br>aof-<span class="hljs-keyword">load</span>-truncated yes<br><br>aof-use-rdb-preamble yes<br><br>aof-<span class="hljs-type">timestamp</span>-enabled <span class="hljs-keyword">no</span><br><br>slowlog-<span class="hljs-keyword">log</span>-slower-than <span class="hljs-number">10000</span><br><br>slowlog-max-len <span class="hljs-number">128</span><br><br><br>latency-monitor-threshold <span class="hljs-number">0</span><br><br><span class="hljs-keyword">notify</span>-keyspace-events &quot;&quot;<br><br><br>hash-max-listpack-entries <span class="hljs-number">512</span><br>hash-max-listpack-<span class="hljs-keyword">value</span> <span class="hljs-number">64</span><br><br>list-max-listpack-size <span class="hljs-number">-2</span><br><br>list-compress-depth <span class="hljs-number">0</span><br><br><span class="hljs-keyword">set</span>-max-intset-entries <span class="hljs-number">512</span><br><br>zset-max-listpack-entries <span class="hljs-number">128</span><br>zset-max-listpack-<span class="hljs-keyword">value</span> <span class="hljs-number">64</span><br><br>hll-sparse-max-bytes <span class="hljs-number">3000</span><br><br>stream-node-max-bytes <span class="hljs-number">4096</span><br>stream-node-max-entries <span class="hljs-number">100</span><br><br>activerehashing yes<br><br>client-output-buffer-<span class="hljs-keyword">limit</span> normal <span class="hljs-number">0</span> <span class="hljs-number">0</span> <span class="hljs-number">0</span><br>client-output-buffer-<span class="hljs-keyword">limit</span> <span class="hljs-keyword">replica</span> <span class="hljs-number">256</span>mb <span class="hljs-number">64</span>mb <span class="hljs-number">60</span><br>client-output-buffer-<span class="hljs-keyword">limit</span> pubsub <span class="hljs-number">32</span>mb <span class="hljs-number">8</span>mb <span class="hljs-number">60</span><br>hz <span class="hljs-number">10</span><br><br>dynamic-hz yes<br><br>aof-rewrite-incremental-fsync yes<br><br>rdb-save-incremental-fsync yes<br>jemalloc-bg-thread yes<br></code></pre></td></tr></table></figure></div></div><div class="tab-to-top"><button type="button" aria-label="scroll to top"><i class="fas fa-arrow-up"></i></button></div></div>

<ol start="3">
<li>在 <code>/opt/docker_soft</code> 目录下创建 <code>docker-compose.yml</code> 文件，内容如下：</li>
</ol>
<figure class="highlight yaml"><table><tr><td class="code"><pre><code class="hljs yaml"><span class="hljs-attr">version:</span> <span class="hljs-string">&#x27;3&#x27;</span><br><span class="hljs-attr">networks:</span><br>  <span class="hljs-attr">service_default:</span><br>    <span class="hljs-attr">external:</span> <span class="hljs-literal">true</span><br><span class="hljs-attr">services:</span><br>  <span class="hljs-attr">mysql8:</span><br>    <span class="hljs-attr">image:</span> <span class="hljs-string">mysql:8.0</span><br>    <span class="hljs-attr">container_name:</span> <span class="hljs-string">mysql8</span><br>    <span class="hljs-comment">#restart: always</span><br>    <span class="hljs-attr">networks:</span><br>      <span class="hljs-bullet">-</span> <span class="hljs-string">service_default</span><br>    <span class="hljs-attr">ports:</span><br>      <span class="hljs-bullet">-</span> <span class="hljs-number">3306</span><span class="hljs-string">:3306</span><br>    <span class="hljs-attr">environment:</span><br>      <span class="hljs-attr">TZ:</span> <span class="hljs-string">Asia/Shanghai</span><br>      <span class="hljs-attr">MYSQL_ROOT_PASSWORD:</span> <span class="hljs-string">admin</span><br>      <span class="hljs-attr">MYSQL_DATABASE:</span> <span class="hljs-string">test</span><br>      <span class="hljs-attr">MYSQL_USER:</span> <span class="hljs-string">mysql</span><br>      <span class="hljs-attr">MYSQL_PASSWORD:</span> <span class="hljs-number">123456</span><br>    <span class="hljs-attr">volumes:</span><br>      <span class="hljs-comment"># 数据挂载</span><br>      <span class="hljs-bullet">-</span> <span class="hljs-string">/opt/docker_soft/data/mysql8/:/var/lib/mysql/</span><br>      <span class="hljs-comment"># 配置挂载</span><br>      <span class="hljs-bullet">-</span> <span class="hljs-string">/opt/docker_soft/config/mysql8/:/etc/mysql/conf.d/</span><br>      <span class="hljs-comment"># 日志挂载</span><br>      <span class="hljs-bullet">-</span> <span class="hljs-string">/opt/docker_soft/log/mysql8:/logs</span><br>    <span class="hljs-attr">command:</span><br>      <span class="hljs-comment"># 将mysql8.0默认密码策略 修改为 原先 策略 (mysql8.0对其默认策略做了更改 会导致密码无法匹配)</span><br>      <span class="hljs-string">--authentication_policy=mysql_native_password</span><br>      <span class="hljs-string">--character-set-server=utf8mb4</span><br>      <span class="hljs-string">--collation-server=utf8mb4_general_ci</span><br>      <span class="hljs-string">--explicit_defaults_for_timestamp=true</span><br>      <span class="hljs-string">--lower_case_table_names=1</span><br>  <span class="hljs-attr">nginx:</span><br>    <span class="hljs-attr">image:</span> <span class="hljs-string">nginx:1.25.0</span><br>    <span class="hljs-attr">container_name:</span> <span class="hljs-string">nginx</span><br>    <span class="hljs-comment">#restart: always</span><br>    <span class="hljs-attr">networks:</span><br>      <span class="hljs-bullet">-</span> <span class="hljs-string">service_default</span><br>    <span class="hljs-attr">ports:</span><br>      <span class="hljs-bullet">-</span> <span class="hljs-number">80</span><span class="hljs-string">:80</span><br>    <span class="hljs-attr">volumes:</span><br>      <span class="hljs-bullet">-</span> <span class="hljs-string">/opt/docker_soft/config/nginx:/etc/nginx</span><br>      <span class="hljs-bullet">-</span> <span class="hljs-string">/opt/docker_soft/log/nginx:/var/log/nginx</span><br>  <span class="hljs-attr">redis:</span><br>    <span class="hljs-attr">image:</span> <span class="hljs-string">redis:7-alpine3.18</span><br>    <span class="hljs-attr">container_name:</span> <span class="hljs-string">redis</span><br>    <span class="hljs-comment">#restart: always</span><br>    <span class="hljs-attr">networks:</span><br>      <span class="hljs-bullet">-</span> <span class="hljs-string">service_default</span><br>    <span class="hljs-attr">ports:</span><br>      <span class="hljs-bullet">-</span> <span class="hljs-number">6379</span><span class="hljs-string">:6379</span><br>    <span class="hljs-attr">volumes:</span><br>      <span class="hljs-bullet">-</span> <span class="hljs-string">/opt/docker_soft/config/redis7/:/usr/local/etc/redis/</span><br>      <span class="hljs-bullet">-</span> <span class="hljs-string">/opt/docker_soft/data/redis:/data</span><br></code></pre></td></tr></table></figure>

<ol start="2">
<li>安装 <code>docker</code> 镜像</li>
</ol>
<figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">docker pull mysql:8.0<br>docker pull redis:7-alpine3.18<br>docker pull nginx:1.25.0<br></code></pre></td></tr></table></figure>


]]></content>
      <categories>
        <category>云原生</category>
        <category>docker</category>
      </categories>
      <tags>
        <tag>docker</tag>
      </tags>
  </entry>
  <entry>
    <title>Win11通过脚本切换主题</title>
    <url>/2023/a3ce1a2f074a/index.html</url>
    <content><![CDATA[<blockquote>
<p>具体主题的文件名称可以到 <code>C:\\Windows\\Resources\\Themes</code> 目录下进行查看</p>
</blockquote>
<p>切换 Light 主题</p>
<figure class="highlight bat"><table><tr><td class="code"><pre><code class="hljs bat"><span class="hljs-built_in">start</span> &quot;&quot; &quot;C:\\Windows\\Resources\\Themes\\Light.theme&quot; &amp; timeout /t <span class="hljs-number">2</span> &amp; <span class="hljs-built_in">taskkill</span> /im &quot;systemsettings.exe&quot; /f<br></code></pre></td></tr></table></figure>

<p>切换 Dark 主题</p>
<figure class="highlight bat"><table><tr><td class="code"><pre><code class="hljs bat"><span class="hljs-built_in">start</span> &quot;&quot; &quot;C:\\Windows\\Resources\\Themes\\dark.theme&quot; &amp; timeout /t <span class="hljs-number">2</span> &amp; <span class="hljs-built_in">taskkill</span> /im &quot;systemsettings.exe&quot; /f<br></code></pre></td></tr></table></figure>

<h2 id="隐藏执行脚本时的黑框"><a href="#隐藏执行脚本时的黑框" class="headerlink" title="隐藏执行脚本时的黑框"></a>隐藏执行脚本时的黑框</h2><p><strong>方法一</strong><br>执行上面两个脚本都会出现黑色窗口，可以修改 <code>bat</code> 文件，变成如下形式</p>
<p>切换黑色主题</p>
<figure class="highlight bat"><table><tr><td class="code"><pre><code class="hljs bat"><span class="hljs-keyword">if</span> &quot;%<span class="hljs-number">1</span>&quot;==&quot;hide&quot; <span class="hljs-keyword">goto</span> CmdBegin<br><span class="hljs-built_in">start</span> mshta vbscript:createobject(&quot;wscript.shell&quot;).run(&quot;&quot;&quot;%~<span class="hljs-number">0</span>&quot;&quot; hide&quot;,<span class="hljs-number">0</span>)(window.close)&amp;&amp;<span class="hljs-keyword">exit</span><br>:CmdBegin<br><br><span class="hljs-built_in">start</span> &quot;&quot; &quot;C:\\Windows\\Resources\\Themes\\dark.theme&quot; &amp; timeout /t <span class="hljs-number">2</span> &amp; <span class="hljs-built_in">taskkill</span> /im &quot;systemsettings.exe&quot; /f<br></code></pre></td></tr></table></figure>

<p>切换白色主题</p>
<figure class="highlight bat"><table><tr><td class="code"><pre><code class="hljs bat"><span class="hljs-keyword">if</span> &quot;%<span class="hljs-number">1</span>&quot;==&quot;hide&quot; <span class="hljs-keyword">goto</span> CmdBegin<br><span class="hljs-built_in">start</span> mshta vbscript:createobject(&quot;wscript.shell&quot;).run(&quot;&quot;&quot;%~<span class="hljs-number">0</span>&quot;&quot; hide&quot;,<span class="hljs-number">0</span>)(window.close)&amp;&amp;<span class="hljs-keyword">exit</span><br>:CmdBegin<br><br><span class="hljs-built_in">start</span> &quot;&quot; &quot;C:\\Windows\\Resources\\Themes\\Light.theme&quot; &amp; timeout /t <span class="hljs-number">2</span> &amp; <span class="hljs-built_in">taskkill</span> /im &quot;systemsettings.exe&quot; /f<br></code></pre></td></tr></table></figure>

<p><strong>方法二</strong><br>第二种方式是另外再创建一个 <code>vbs</code> 文件，在 <code>vbs</code> 中执行 <code>bat</code> 文件</p>
<figure class="highlight routeros"><table><tr><td class="code"><pre><code class="hljs routeros"><span class="hljs-built_in">set</span> <span class="hljs-attribute">ws</span>=WScript.CreateObject(&quot;WScript.Shell&quot;)<br>ws.<span class="hljs-built_in">Run</span> <span class="hljs-string">&quot;C:\xxx.bat&quot;</span>,0<br></code></pre></td></tr></table></figure>

<p>然后可以结合 <a href="https://u.tools/">utools</a> 使用，就可以快速执行主题切换</p>
]]></content>
      <categories>
        <category>tools</category>
        <category>other</category>
      </categories>
      <tags>
        <tag>tools</tag>
      </tags>
  </entry>
  <entry>
    <title>WindTerm功能设置</title>
    <url>/2023/0ff9c6825506/index.html</url>
    <content><![CDATA[<h2 id="自定义快捷键"><a href="#自定义快捷键" class="headerlink" title="自定义快捷键"></a>自定义快捷键</h2><p><a href="https://github.com/kingToolbox/WindTerm/issues/267">参考github</a></p>
<h2 id="去除锁屏"><a href="#去除锁屏" class="headerlink" title="去除锁屏"></a>去除锁屏</h2><ol>
<li>找到配置文件，可以在 <strong>会话-&gt;首选项-&gt;配置文件</strong> 中查看配置文件的位置</li>
<li>找到 <code>application.lockScreenTimeout</code>， 将值改成 0</li>
</ol>
<h2 id="安装-Nord-主题"><a href="#安装-Nord-主题" class="headerlink" title="安装 Nord 主题"></a>安装 Nord 主题</h2><p><a href="https://github.com/teaper/windterm-nord-theme">下载Nord主题</a></p>
]]></content>
      <categories>
        <category>tools</category>
        <category>other</category>
      </categories>
      <tags>
        <tag>tools</tag>
      </tags>
  </entry>
  <entry>
    <title>普通用户执行docker命令</title>
    <url>/2023/6987d518f2d5/index.html</url>
    <content><![CDATA[<p>以下操作是在 <code>ubuntu22.04</code> 版本安装 <code>docker</code> 后，普通用户执行 <code>docker</code> 命令时不用添加 <code>sudo</code></p>
<ol>
<li>创建 <code>docker</code> 用户组</li>
</ol>
<figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">sudo groupadd docker<br></code></pre></td></tr></table></figure>

<ol start="2">
<li>将当前用户加入到 <code>docker</code> 用户组</li>
</ol>
<figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">sudo usermod -aG docker <span class="hljs-variable">$USER</span><br></code></pre></td></tr></table></figure>

<ol start="3">
<li>退出当前用户，重新登录，即可使用 <code>docker</code> 命令</li>
</ol>
<figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">exit</span><br></code></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>云原生</category>
        <category>docker</category>
      </categories>
      <tags>
        <tag>docker</tag>
      </tags>
  </entry>
  <entry>
    <title>常用的工具网站记录</title>
    <url>/2023/366386a79bec/index.html</url>
    <content><![CDATA[<h2 id="pdf"><a href="#pdf" class="headerlink" title="pdf"></a>pdf</h2><p><a href="https://www.ilovepdf.com/compress_pdf">pdf在线压缩</a><br><a href="https://tools.pdf24.org/zh/compress-pdf#s=1604459737862%20%20%20%20%20pdf%E5%8E%8B%E7%BC%A9%E8%BD%AF%E4%BB%B6">pdf在线压缩</a></p>
<h2 id="资源网站"><a href="#资源网站" class="headerlink" title="资源网站"></a>资源网站</h2><ul>
<li><a href="https://cmsblogs.cn/">大白菜博客</a></li>
</ul>
<h2 id="截图工具"><a href="#截图工具" class="headerlink" title="截图工具"></a>截图工具</h2><p><a href="https://pixpinapp.com/">pixpin</a>,  该截图工具包含常用的，标注，贴图，长截图，<code>ocr</code> 等功能</p>
<ul>
<li>对于长截图需要打开 <strong>配置 -&gt; 快捷键&#x2F;动作 -&gt; 添加新动作</strong> 中找到长截图，然后将 <strong>托盘菜单</strong> 勾上就可以看到长截图功能</li>
<li><code>ocr</code> 功能，是指贴图后可以直接复制图片中的文字</li>
</ul>
]]></content>
      <categories>
        <category>tools</category>
        <category>other</category>
      </categories>
      <tags>
        <tag>tools</tag>
      </tags>
  </entry>
  <entry>
    <title>utools自动化脚本插件</title>
    <url>/2023/d899f5ff1973/index.html</url>
    <content><![CDATA[<h2 id="utools-使用自动化脚本打开本地程序"><a href="#utools-使用自动化脚本打开本地程序" class="headerlink" title="utools 使用自动化脚本打开本地程序"></a>utools 使用自动化脚本打开本地程序</h2><ol>
<li>比如 typora 可以打开之前的文件和目录，但是目录只能到文件的上一个层级，如果源目录包含很多子目录，重新打开时可能不是我们想要的结果，那可以在命令行执行 <code>typora folder</code>, eg: <code>typora E:\\blog</code> <span id="more"></span></li>
</ol>
<blockquote>
<p>在命令行执行 typora 命令需要先配置 typora 的环境变量</p>
</blockquote>
<ol>
<li>更简便的方式是直接使用 utools 的自动化脚本这个插件，新增一个自己的插件脚本，内容如下</li>
</ol>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-keyword">const</span> &#123; execFile &#125; = <span class="hljs-built_in">require</span>(<span class="hljs-string">&quot;child_process&quot;</span>)<br><br> <span class="hljs-comment">// 以下是关键代码，调用 typora 来执行目标文件</span><br><span class="hljs-title function_">execFile</span>(<span class="hljs-string">&#x27;typora&#x27;</span>, [<span class="hljs-string">&#x27;E:/code/learnerguo&#x27;</span>], <span class="hljs-function">(<span class="hljs-params">error, stdout, stderr</span>) =&gt;</span> &#123;<br>    process.<span class="hljs-title function_">exit</span>()<br>&#125;)<br></code></pre></td></tr></table></figure>

<p>然后自定义关键字，后续当打开 utools 输入对应关键字就可以打开 typora 的目录</p>
<h2 id="使用自动化脚本获取当天起始时间"><a href="#使用自动化脚本获取当天起始时间" class="headerlink" title="使用自动化脚本获取当天起始时间"></a>使用自动化脚本获取当天起始时间</h2><figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-keyword">const</span> now = <span class="hljs-keyword">new</span> <span class="hljs-title class_">Date</span>();<br><span class="hljs-comment">// 设置当前时间的小时、分钟、秒和毫秒为0，即将时间设置为当天的开始时间</span><br>now.<span class="hljs-title function_">setHours</span>(<span class="hljs-number">0</span>, <span class="hljs-number">0</span>, <span class="hljs-number">0</span>, <span class="hljs-number">0</span>);<br><span class="hljs-comment">// 获取当天开始时间点的毫秒数</span><br><span class="hljs-keyword">const</span> startTime = now.<span class="hljs-title function_">getTime</span>();<br>utools.<span class="hljs-title function_">copyText</span>(<span class="hljs-string">&quot;&quot;</span>+startTime);<br><span class="hljs-title function_">print</span>(<span class="hljs-string">&#x27;开始时间已复制&#x27;</span> + startTime);<br>utools.<span class="hljs-title function_">hideMainWindow</span>();<br>process.<span class="hljs-title function_">exit</span>()<br></code></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>tools</category>
        <category>utools</category>
      </categories>
      <tags>
        <tag>tools</tag>
        <tag>utools</tag>
      </tags>
  </entry>
  <entry>
    <title>vscode相关设置</title>
    <url>/2023/6b613d963af3/index.html</url>
    <content><![CDATA[<h2 id="vscode-中-markdown-的-snippets-的设置"><a href="#vscode-中-markdown-的-snippets-的设置" class="headerlink" title="vscode 中 markdown 的 snippets 的设置"></a>vscode 中 markdown 的 snippets 的设置</h2><h3 id="snippets-的设置"><a href="#snippets-的设置" class="headerlink" title="snippets 的设置"></a>snippets 的设置</h3><ol>
<li>打开 ctrl+shift+p , 搜索 <code>configure user snippets</code> </li>
<li>选择 markdown.json, 会出现如下内容</li>
</ol>
<span id="more"></span>
<figure class="highlight json"><table><tr><td class="code"><pre><code class="hljs json"><span class="hljs-punctuation">&#123;</span><br>	<span class="hljs-comment">// Place your snippets for markdown here. Each snippet is defined under a snippet name and has a prefix, body and </span><br>	<span class="hljs-comment">// description. The prefix is what is used to trigger the snippet and the body will be expanded and inserted. Possible variables are:</span><br>	<span class="hljs-comment">// $1, $2 for tab stops, $0 for the final cursor position, and $&#123;1:label&#125;, $&#123;2:another&#125; for placeholders. Placeholders with the </span><br>	<span class="hljs-comment">// same ids are connected.</span><br>	<span class="hljs-comment">// Example:</span><br>	<span class="hljs-comment">// &quot;Print to console&quot;: &#123;</span><br>	<span class="hljs-comment">// 	&quot;prefix&quot;: &quot;log&quot;,</span><br>	<span class="hljs-comment">// 	&quot;body&quot;: [</span><br>	<span class="hljs-comment">// 		&quot;console.log(&#x27;$1&#x27;);&quot;,</span><br>	<span class="hljs-comment">// 		&quot;$2&quot;</span><br>	<span class="hljs-comment">// 	],</span><br>	<span class="hljs-comment">// 	&quot;description&quot;: &quot;Log output to console&quot;</span><br>	<span class="hljs-comment">// &#125;</span><br><span class="hljs-punctuation">&#125;</span><br></code></pre></td></tr></table></figure>

<ol>
<li>可以根据示例文件进行修改，如下图所示：<figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash"><span class="hljs-string">&quot;markdown code graph&quot;</span>: &#123;<br>	<span class="hljs-string">&quot;prefix&quot;</span>: <span class="hljs-string">&quot;codeblock&quot;</span>,<br>	<span class="hljs-string">&quot;body&quot;</span>: [<br>		<span class="hljs-string">&quot;&lt;pre class=&quot;</span>mermaid<span class="hljs-string">&quot;&gt;<span class="hljs-variable">$0</span>&quot;</span>,<br>		<span class="hljs-string">&quot;```&quot;</span><br>	],<br>	<span class="hljs-string">&quot;description&quot;</span>: <span class="hljs-string">&quot;insert markdown code graph&quot;</span><br>&#125;&lt;/pre&gt;<br><br>上面示例的意思是 当输入 codeblock 时就会出现 mermaid 代码块， <span class="hljs-variable">$0</span> 是光标出现的地方<br><br><span class="hljs-comment">### markdown snippets 不生效的解决方式</span><br>使用上面的步骤设置完 snippets 之后，在 markdown 文档中编写关键字，但是并没有生效，还需要在  settings.json 中（可以输入 ctrl+<span class="hljs-built_in">shift</span>+p, 搜索 settings， 选择 Open User Settings(JSON)）, 然后填写下面的内容<br><br>```java<br><span class="hljs-string">&quot;[markdown]&quot;</span>:&#123;<br>	<span class="hljs-string">&quot;editor.formatOnSave&quot;</span>: <span class="hljs-literal">true</span>,<br>	<span class="hljs-string">&quot;editor.renderWhitespace&quot;</span>: <span class="hljs-string">&quot;all&quot;</span>,<br>	<span class="hljs-string">&quot;editor.quickSuggestions&quot;</span>: &#123;<br>		<span class="hljs-string">&quot;other&quot;</span>: <span class="hljs-string">&quot;on&quot;</span>,<br>		<span class="hljs-string">&quot;comments&quot;</span>: <span class="hljs-string">&quot;on&quot;</span>,<br>		<span class="hljs-string">&quot;strings&quot;</span>: <span class="hljs-string">&quot;on&quot;</span><br>	&#125;,<br>	<span class="hljs-string">&quot;editor.acceptSuggestionOnEnter&quot;</span>: <span class="hljs-string">&quot;on&quot;</span><br>&#125;<br></code></pre></td></tr></table></figure></li>
</ol>
<p>添加上述内容后就可以生效了</p>
<h2 id="vscode-设置白天黑夜自动切换主题"><a href="#vscode-设置白天黑夜自动切换主题" class="headerlink" title="vscode 设置白天黑夜自动切换主题"></a>vscode 设置白天黑夜自动切换主题</h2><ol>
<li>vscode 可以根据系统设置的 <code>light</code> 或 <code>dark</code> 主题来进行切换</li>
<li>系统自动切换主题软件可以在 <code>store</code> 中安装 <code>Auto Dark Mode</code> 这个软件并进行设置</li>
<li><code>window</code> 系统设置完成之后可以在 <code>vscode</code> 的 <code>user setting</code> 中添加如下内容, 第一个配置是开启自动切换，后面的配置是设置的 <code>light</code> 和 <code>dark</code> 主题的名称</li>
</ol>
<figure class="highlight json"><table><tr><td class="code"><pre><code class="hljs json"><span class="hljs-attr">&quot;window.autoDetectColorScheme&quot;</span><span class="hljs-punctuation">:</span> <span class="hljs-literal"><span class="hljs-keyword">true</span></span><span class="hljs-punctuation">,</span><br><span class="hljs-attr">&quot;workbench.preferredLightColorTheme&quot;</span><span class="hljs-punctuation">:</span> <span class="hljs-string">&quot;Atom One Light&quot;</span><span class="hljs-punctuation">,</span><br><span class="hljs-attr">&quot;workbench.preferredDarkColorTheme&quot;</span><span class="hljs-punctuation">:</span> <span class="hljs-string">&quot;Default Dark Modern&quot;</span><br></code></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>tools</category>
        <category>vscode</category>
      </categories>
      <tags>
        <tag>tools</tag>
        <tag>vscode</tag>
      </tags>
  </entry>
  <entry>
    <title>vscode设置markdown图片默认路径</title>
    <url>/2023/5b4d784e18c2/index.html</url>
    <content><![CDATA[<p><code>vscode</code> 在 <code>markdown</code> 文档中直接复制图片时是放在当前目录，如果需要修改可以按照下面方式设置</p>
<span id="more"></span>
<ol>
<li>输入快捷键 <code>ctrl+,</code> 可以快速打开设置</li>
<li>搜索 <code>markdown.copy: Destination</code></li>
</ol>
<p><img src="/2023/5b4d784e18c2/index/1700033681108.png" alt="Alt text"></p>
]]></content>
      <categories>
        <category>tools</category>
        <category>vscode</category>
      </categories>
      <tags>
        <tag>tools</tag>
      </tags>
  </entry>
</search>
